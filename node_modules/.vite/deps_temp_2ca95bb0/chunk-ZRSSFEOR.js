import {
  d,
  d2,
  d3,
  d4,
  e4 as e,
  e5 as e2,
  e6,
  f,
  i as i2,
  i2 as i3,
  m,
  o2,
  p2 as p,
  r2 as r5,
  s,
  t3,
  u,
  w
} from "./chunk-5ZHZAKF4.js";
import {
  e as e4,
  e2 as e5,
  r as r3
} from "./chunk-HU2EI3QQ.js";
import {
  s as s2
} from "./chunk-RLUHC4MC.js";
import {
  e as e3,
  i,
  n as n2,
  r as r4,
  t as t2
} from "./chunk-4YX4B24K.js";
import {
  n
} from "./chunk-62YH3FZ6.js";
import {
  a,
  r as r2
} from "./chunk-NLWLBMYL.js";
import {
  o,
  r
} from "./chunk-MACCKOIR.js";
import {
  t
} from "./chunk-TORASWX2.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/hud/HUD.glsl.js
var p2 = 0.5;
function d5(d6, u2) {
  d6.include(s), d6.attributes.add("position", "vec3"), d6.attributes.add("normal", "vec3"), d6.attributes.add("centerOffsetAndDistance", "vec4");
  const v = d6.vertex;
  f(v, u2), d(v, u2), v.uniforms.add(new e5("viewport", (e8) => e8.camera.fullViewport), new r4("polygonOffset", (e8) => e8.shaderPolygonOffset), new r3("cameraGroundRelative", (e8) => e8.camera.aboveGround ? 1 : -1)), u2.hasVerticalOffset && d2(v), v.code.add(t2`struct ProjectHUDAux {
vec3 posModel;
vec3 posView;
vec3 vnormal;
float distanceToCamera;
float absCosAngle;
};`), v.code.add(t2`
    float applyHUDViewDependentPolygonOffset(float pointGroundDistance, float absCosAngle, inout vec3 posView) {
      float pointGroundSign = ${u2.terrainDepthTest ? t2.float(0) : t2`sign(pointGroundDistance)`};
      if (pointGroundSign == 0.0) {
        pointGroundSign = cameraGroundRelative;
      }

      // cameraGroundRelative is -1 if camera is below ground, 1 if above ground
      // groundRelative is 1 if both camera and symbol are on the same side of the ground, -1 otherwise
      float groundRelative = cameraGroundRelative * pointGroundSign;

      // view angle dependent part of polygon offset emulation: we take the absolute value because the sign that is
      // dropped is instead introduced using the ground-relative position of the symbol and the camera
      if (polygonOffset > .0) {
        float cosAlpha = clamp(absCosAngle, 0.01, 1.0);
        float tanAlpha = sqrt(1.0 - cosAlpha * cosAlpha) / cosAlpha;
        float factor = (1.0 - tanAlpha / viewport[2]);

        // same side of the terrain
        if (groundRelative > 0.0) {
          posView *= factor;
        }
        // opposite sides of the terrain
        else {
          posView /= factor;
        }
      }

      return groundRelative;
    }
  `), u2.draped && !u2.hasVerticalOffset || p(v), u2.draped || (v.uniforms.add(new r3("perDistancePixelRatio", (e8) => Math.tan(e8.camera.fovY / 2) / (e8.camera.fullViewport[2] / 2))), v.code.add(t2`
    void applyHUDVerticalGroundOffset(vec3 normalModel, inout vec3 posModel, inout vec3 posView) {
      float distanceToCamera = length(posView);

      // Compute offset in world units for a half pixel shift
      float pixelOffset = distanceToCamera * perDistancePixelRatio * ${t2.float(p2)};

      // Apply offset along normal in the direction away from the ground surface
      vec3 modelOffset = normalModel * cameraGroundRelative * pixelOffset;

      // Apply the same offset also on the view space position
      vec3 viewOffset = (viewNormal * vec4(modelOffset, 1.0)).xyz;

      posModel += modelOffset;
      posView += viewOffset;
    }
  `)), u2.screenCenterOffsetUnitsEnabled && w(v), u2.hasScreenSizePerspective && i2(v), v.code.add(t2`
    vec4 projectPositionHUD(out ProjectHUDAux aux) {
      vec3 centerOffset = centerOffsetAndDistance.xyz;
      float pointGroundDistance = centerOffsetAndDistance.w;

      aux.posModel = position;
      aux.posView = (view * vec4(aux.posModel, 1.0)).xyz;
      aux.vnormal = normal;
      ${u2.draped ? "" : "applyHUDVerticalGroundOffset(aux.vnormal, aux.posModel, aux.posView);"}

      // Screen sized offset in world space, used for example for line callouts
      // Note: keep this implementation in sync with the CPU implementation, see
      //   - MaterialUtil.verticalOffsetAtDistance
      //   - HUDMaterial.applyVerticalOffsetTransformation

      aux.distanceToCamera = length(aux.posView);

      vec3 viewDirObjSpace = normalize(cameraPosition - aux.posModel);
      float cosAngle = dot(aux.vnormal, viewDirObjSpace);

      aux.absCosAngle = abs(cosAngle);

      ${u2.hasScreenSizePerspective && (u2.hasVerticalOffset || u2.screenCenterOffsetUnitsEnabled) ? "vec3 perspectiveFactor = screenSizePerspectiveScaleFactor(aux.absCosAngle, aux.distanceToCamera, screenSizePerspectiveAlignment);" : ""}

      ${u2.hasVerticalOffset ? u2.hasScreenSizePerspective ? "float verticalOffsetScreenHeight = applyScreenSizePerspectiveScaleFactorFloat(verticalOffset.x, perspectiveFactor);" : "float verticalOffsetScreenHeight = verticalOffset.x;" : ""}

      ${u2.hasVerticalOffset ? t2`
            float worldOffset = clamp(verticalOffsetScreenHeight * verticalOffset.y * aux.distanceToCamera, verticalOffset.z, verticalOffset.w);
            vec3 modelOffset = aux.vnormal * worldOffset;
            aux.posModel += modelOffset;
            vec3 viewOffset = (viewNormal * vec4(modelOffset, 1.0)).xyz;
            aux.posView += viewOffset;
            // Since we elevate the object, we need to take that into account
            // in the distance to ground
            pointGroundDistance += worldOffset;` : ""}

      float groundRelative = applyHUDViewDependentPolygonOffset(pointGroundDistance, aux.absCosAngle, aux.posView);

      ${u2.screenCenterOffsetUnitsEnabled ? "" : t2`
            // Apply x/y in view space, but z in screen space (i.e. along posView direction)
            aux.posView += vec3(centerOffset.x, centerOffset.y, 0.0);

            // Same material all have same z != 0.0 condition so should not lead to
            // branch fragmentation and will save a normalization if it's not needed
            if (centerOffset.z != 0.0) {
              aux.posView -= normalize(aux.posView) * centerOffset.z;
            }
          `}

      vec4 posProj = proj * vec4(aux.posView, 1.0);

      ${u2.screenCenterOffsetUnitsEnabled ? u2.hasScreenSizePerspective ? "float centerOffsetY = applyScreenSizePerspectiveScaleFactorFloat(centerOffset.y, perspectiveFactor);" : "float centerOffsetY = centerOffset.y;" : ""}

      ${u2.screenCenterOffsetUnitsEnabled ? "posProj.xy += vec2(centerOffset.x, centerOffsetY) * pixelRatio * 2.0 / viewport.zw * posProj.w;" : ""}

      // constant part of polygon offset emulation
      posProj.z -= groundRelative * polygonOffset * posProj.w;
      return posProj;
    }
  `);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/hud/AlignPixel.glsl.js
function l(l2) {
  l2.uniforms.add(new r5("alignPixelEnabled", (e8) => e8.alignPixelEnabled)), l2.code.add(t2`vec4 alignToPixelCenter(vec4 clipCoord, vec2 widthHeight) {
if (!alignPixelEnabled)
return clipCoord;
vec2 xy = vec2(0.500123) + 0.5 * clipCoord.xy / clipCoord.w;
vec2 pixelSz = vec2(1.0) / widthHeight;
vec2 ij = (floor(xy * widthHeight) + vec2(0.5)) * pixelSz;
vec2 result = (ij * 2.0 - vec2(1.0)) * clipCoord.w;
return vec4(result, clipCoord.zw);
}`), l2.code.add(t2`vec4 alignToPixelOrigin(vec4 clipCoord, vec2 widthHeight) {
if (!alignPixelEnabled)
return clipCoord;
vec2 xy = vec2(0.5) + 0.5 * clipCoord.xy / clipCoord.w;
vec2 pixelSz = vec2(1.0) / widthHeight;
vec2 ij = floor((xy + 0.5 * pixelSz) * widthHeight) * pixelSz;
vec2 result = (ij * 2.0 - vec2(1.0)) * clipCoord.w;
return vec4(result, clipCoord.zw);
}`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/hud/HUDOcclusionPass.glsl.js
function i4(i5, t4) {
  const { vertex: s3, fragment: p3 } = i5;
  i5.include(i3, t4), s3.include(l), s3.main.add(t2`vec4 posProjCenter;
if (dot(position, position) > 0.0) {
ProjectHUDAux projectAux;
vec4 posProj = projectPositionHUD(projectAux);
posProjCenter = alignToPixelCenter(posProj, viewport.zw);
forwardViewPosDepth(projectAux.posView);
vec3 vpos = projectAux.posModel;
if (rejectBySlice(vpos)) {
posProjCenter = vec4(1e038, 1e038, 1e038, 1.0);
}
} else {
posProjCenter = vec4(1e038, 1e038, 1e038, 1.0);
}
gl_Position = posProjCenter;
gl_PointSize = 1.0;`), p3.main.add(t2`fragColor = vec4(1);
if(discardByTerrainDepth()) {
fragColor.g = 0.5;
}`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/hud/HUDVisibility.glsl.js
function n3(n4) {
  n4.vertex.uniforms.add(new r3("renderTransparentlyOccludedHUD", (e8) => 0 === e8.hudRenderStyle ? 1 : 1 === e8.hudRenderStyle ? 0 : 0.75), new e5("viewport", (e8) => e8.camera.fullViewport), new e2("hudVisibilityTexture", (e8) => {
    var _a;
    return (_a = e8.hudVisibility) == null ? void 0 : _a.getTexture();
  })), n4.vertex.include(l), n4.vertex.code.add(t2`bool testHUDVisibility(vec4 posProj) {
vec4 posProjCenter = alignToPixelCenter(posProj, viewport.zw);
vec4 occlusionPixel = texture(hudVisibilityTexture, .5 + .5 * posProjCenter.xy / posProjCenter.w);
if (renderTransparentlyOccludedHUD > 0.5) {
return occlusionPixel.r * occlusionPixel.g > 0.0 && occlusionPixel.g * renderTransparentlyOccludedHUD < 1.0;
}
return occlusionPixel.r * occlusionPixel.g > 0.0 && occlusionPixel.g == 1.0;
}`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderModules/Float4DrawUniform.js
var e7 = class extends i {
  constructor(r6, e8, o3) {
    super(r6, "vec4", 2, (s3, t4, c) => s3.setUniform4fv(r6, e8(t4, c), o3));
  }
};

// node_modules/@arcgis/core/chunks/HUDMaterial.glsl.js
function F(e8) {
  const i5 = new s2(), { signedDistanceFieldEnabled: F2, occlusionTestEnabled: M2, horizonCullingEnabled: U2, pixelSnappingEnabled: H2, hasScreenSizePerspective: E, debugDrawLabelBorder: _, hasVVSize: q, hasVVColor: R, hasRotation: k, occludedFragmentFade: I, sampleSignedDistanceFieldTexelCenter: G } = e8;
  i5.include(d5, e8), i5.vertex.include(m, e8);
  const { occlusionPass: J, output: K, oitPass: N } = e8;
  if (J) return i5.include(i4, e8), i5;
  const { vertex: Q, fragment: W } = i5;
  i5.include(s), i5.include(u, e8), i5.include(d3, e8), M2 && i5.include(n3), W.include(e6), i5.varyings.add("vcolor", "vec4"), i5.varyings.add("vtc", "vec2"), i5.varyings.add("vsize", "vec2");
  const X = 9 === K, Y = X && M2;
  Y && i5.varyings.add("voccluded", "float"), Q.uniforms.add(new e5("viewport", (e9) => e9.camera.fullViewport), new e4("screenOffset", (e9, i6) => o(L, 2 * e9.screenOffset[0] * i6.camera.pixelRatio, 2 * e9.screenOffset[1] * i6.camera.pixelRatio)), new e4("anchorPosition", (e9) => B(e9)), new e("materialColor", (e9) => e9.color), new r4("materialRotation", (e9) => e9.rotation), new e3("tex", (e9) => e9.texture)), w(Q), F2 && (Q.uniforms.add(new e("outlineColor", (e9) => e9.outlineColor)), W.uniforms.add(new e("outlineColor", (e9) => T(e9) ? e9.outlineColor : a), new r4("outlineSize", (e9) => T(e9) ? e9.outlineSize : 0))), U2 && Q.uniforms.add(new e7("pointDistanceSphere", (e9, o3) => {
    const i6 = o3.camera.eye, r6 = e9.origin;
    return r2(r6[0] - i6[0], r6[1] - i6[1], r6[2] - i6[2], t.radius);
  })), H2 && Q.include(l), E && (t3(Q), i2(Q)), _ && i5.varyings.add("debugBorderCoords", "vec4"), i5.attributes.add("uv0", "vec2"), i5.attributes.add("uvi", "vec4"), i5.attributes.add("color", "vec4"), i5.attributes.add("size", "vec2"), i5.attributes.add("rotation", "float"), (q || R) && i5.attributes.add("featureAttribute", "vec4"), Q.code.add(U2 ? t2`bool behindHorizon(vec3 posModel) {
vec3 camToEarthCenter = pointDistanceSphere.xyz - localOrigin;
vec3 camToPos = pointDistanceSphere.xyz + posModel;
float earthRadius = pointDistanceSphere.w;
float a = dot(camToPos, camToPos);
float b = dot(camToPos, camToEarthCenter);
float c = dot(camToEarthCenter, camToEarthCenter) - earthRadius * earthRadius;
return b > 0.0 && b < a && b * b  > a * c;
}` : t2`bool behindHorizon(vec3 posModel) { return false; }`), Q.main.add(t2`
    ProjectHUDAux projectAux;
    vec4 posProj = projectPositionHUD(projectAux);
    forwardObjectAndLayerIdColor();

    if (rejectBySlice(projectAux.posModel)) {
      // Project outside of clip plane
      gl_Position = vec4(1e038, 1e038, 1e038, 1.0);
      return;
    }

    if (behindHorizon(projectAux.posModel)) {
      // Project outside of clip plane
      gl_Position = vec4(1e038, 1e038, 1e038, 1.0);
      return;
    }

    vec2 inputSize;
    ${n2(E, t2`
        inputSize = screenSizePerspectiveScaleVec2(size, projectAux.absCosAngle, projectAux.distanceToCamera, screenSizePerspective);
        vec2 screenOffsetScaled = screenSizePerspectiveScaleVec2(screenOffset, projectAux.absCosAngle, projectAux.distanceToCamera, screenSizePerspectiveAlignment);`, t2`
        inputSize = size;
        vec2 screenOffsetScaled = screenOffset;`)}
    ${n2(q, t2`inputSize *= vvScale(featureAttribute).xx;`)}

    vec2 combinedSize = inputSize * pixelRatio;
    vec4 quadOffset = vec4(0.0);

    ${n2(M2, t2`
    bool visible = testHUDVisibility(posProj);
    if (!visible) {
      vtc = vec2(0.0);
      ${n2(_, "debugBorderCoords = vec4(0.5, 0.5, 1.5 / combinedSize);")}
      return;
    }`)}
    ${n2(Y, t2`voccluded = visible ? 0.0 : 1.0;`)}
  `);
  const Z = t2`
      vec2 uv = mix(uvi.xy, uvi.zw, bvec2(uv0));
      vec2 texSize = vec2(textureSize(tex, 0));
      uv = mix(vec2(1.0), uv / texSize, lessThan(uv, vec2(${V})));
      quadOffset.xy = (uv0 - anchorPosition) * 2.0 * combinedSize;

      ${n2(k, t2`
          float angle = radians(materialRotation + rotation);
          float cosAngle = cos(angle);
          float sinAngle = sin(angle);
          mat2 rotate = mat2(cosAngle, -sinAngle, sinAngle,  cosAngle);

          quadOffset.xy = rotate * quadOffset.xy;
        `)}

      quadOffset.xy = (quadOffset.xy + screenOffsetScaled) / viewport.zw * posProj.w;
  `, ee = H2 ? F2 ? t2`posProj = alignToPixelOrigin(posProj, viewport.zw) + quadOffset;` : t2`posProj += quadOffset;
if (inputSize.x == size.x) {
posProj = alignToPixelOrigin(posProj, viewport.zw);
}` : t2`posProj += quadOffset;`;
  Q.main.add(t2`
    ${Z}
    ${R ? "vcolor = interpolateVVColor(featureAttribute.y) * materialColor;" : "vcolor = color / 255.0 * materialColor;"}

    ${n2(10 === K, t2`vcolor.a = 1.0;`)}

    bool alphaDiscard = vcolor.a < ${t2.float(o2)};
    ${n2(F2, `alphaDiscard = alphaDiscard && outlineColor.a < ${t2.float(o2)};`)}
    if (alphaDiscard) {
      // "early discard" if both symbol color (= fill) and outline color (if applicable) are transparent
      gl_Position = vec4(1e38, 1e38, 1e38, 1.0);
      return;
    } else {
      ${ee}
      gl_Position = posProj;
    }

    vtc = uv;

    ${n2(_, t2`debugBorderCoords = vec4(uv01, 1.5 / combinedSize);`)}
    vsize = inputSize;
  `), W.uniforms.add(new e3("tex", (e9) => e9.texture)), I && !X && W.uniforms.add(new e2("depthMap", (e9) => e9.mainDepth), new r3("occludedOpacity", (e9) => e9.hudOccludedFragmentOpacity));
  const oe = _ ? t2`(isBorder > 0.0 ? 0.0 : ${t2.float(o2)})` : t2.float(o2), ie = t2`
    ${n2(_, t2`float isBorder = float(any(lessThan(debugBorderCoords.xy, debugBorderCoords.zw)) || any(greaterThan(debugBorderCoords.xy, 1.0 - debugBorderCoords.zw)));`)}

    vec2 samplePos = vtc;

    ${n2(G, t2`
      float txSize = float(textureSize(tex, 0).x);
      float texelSize = 1.0 / txSize;

      // Calculate how much we have to add/subtract to/from each texel to reach the size of an onscreen pixel
      vec2 scaleFactor = (vsize - txSize) * texelSize;
      samplePos += (vec2(1.0, -1.0) * texelSize) * scaleFactor;`)}

    ${F2 ? t2`
      vec4 fillPixelColor = vcolor;

      // Get distance in output units (i.e. pixels)

      float sdf = texture(tex, samplePos).r;
      float pixelDistance = sdf * vsize.x;

      // Create smooth transition from the icon into its outline
      float fillAlphaFactor = clamp(0.5 - pixelDistance, 0.0, 1.0);
      fillPixelColor.a *= fillAlphaFactor;

      if (outlineSize > 0.25) {
        vec4 outlinePixelColor = outlineColor;
        float clampedOutlineSize = min(outlineSize, 0.5*vsize.x);

        // Create smooth transition around outline
        float outlineAlphaFactor = clamp(0.5 - (abs(pixelDistance) - 0.5*clampedOutlineSize), 0.0, 1.0);
        outlinePixelColor.a *= outlineAlphaFactor;

        if (
          outlineAlphaFactor + fillAlphaFactor < ${oe} ||
          fillPixelColor.a + outlinePixelColor.a < ${t2.float(o2)}
        ) {
          discard;
        }

        // perform un-premultiplied over operator (see https://en.wikipedia.org/wiki/Alpha_compositing#Description)
        float compositeAlpha = outlinePixelColor.a + fillPixelColor.a * (1.0 - outlinePixelColor.a);
        vec3 compositeColor = vec3(outlinePixelColor) * outlinePixelColor.a +
          vec3(fillPixelColor) * fillPixelColor.a * (1.0 - outlinePixelColor.a);

        ${n2(!X, t2`fragColor = vec4(compositeColor, compositeAlpha);`)}
      } else {
        if (fillAlphaFactor < ${oe}) {
          discard;
        }

        ${n2(!X, t2`fragColor = premultiplyAlpha(fillPixelColor);`)}
      }

      // visualize SDF:
      // fragColor = vec4(clamp(-pixelDistance/vsize.x*2.0, 0.0, 1.0), clamp(pixelDistance/vsize.x*2.0, 0.0, 1.0), 0.0, 1.0);
      ` : t2`
          vec4 texColor = texture(tex, samplePos, -0.5);
          if (texColor.a < ${oe}) {
            discard;
          }
          ${n2(!X, t2`fragColor = texColor * premultiplyAlpha(vcolor);`)}
          `}

    ${n2(I && !X, t2`
        float zSample = texelFetch(depthMap, ivec2(gl_FragCoord.xy), 0).x;
        if (zSample < gl_FragCoord.z) {
          fragColor *= occludedOpacity;
        }
        `)}

    ${n2(!X && _, t2`fragColor = mix(fragColor, vec4(1.0, 0.0, 1.0, 1.0), isBorder * 0.5);`)}
  `;
  switch (K) {
    case 0:
    case 1:
      i5.outputs.add("fragColor", "vec4", 0), 1 === K && i5.outputs.add("fragEmission", "vec4", 1), 1 === N && i5.outputs.add("fragAlpha", "float", 1 === K ? 2 : 1), W.main.add(t2`
        ${ie}
        ${n2(2 === N, t2`fragColor.rgb /= fragColor.a;`)}
        ${n2(1 === K, t2`fragEmission = vec4(0.0);`)}
        ${n2(1 === N, t2`fragAlpha = fragColor.a;`)}`);
      break;
    case 10:
      W.main.add(t2`
        ${ie}
        outputObjectAndLayerIdColor();`);
      break;
    case 9:
      i5.include(d4, e8), W.main.add(t2`
        ${ie}
        outputHighlight(${n2(Y, t2`voccluded == 1.0`, t2`false`)});`);
  }
  return i5;
}
function T(e8) {
  return e8.outlineColor[3] > 0 && e8.outlineSize > 0;
}
function B(o3) {
  return o3.textureIsSignedDistanceField ? M(o3.anchorPosition, o3.distanceFieldBoundingBox, L) : r(L, o3.anchorPosition), L;
}
function M(e8, i5, r6) {
  o(r6, e8[0] * (i5[2] - i5[0]) + i5[0], e8[1] * (i5[3] - i5[1]) + i5[1]);
}
var L = n();
var U = 32e3;
var V = t2.float(U);
var H = Object.freeze(Object.defineProperty({ __proto__: null, build: F, calculateAnchorPosition: B, fullUV: U }, Symbol.toStringTag, { value: "Module" }));

export {
  p2 as p,
  F,
  B,
  U,
  H
};
//# sourceMappingURL=chunk-ZRSSFEOR.js.map
