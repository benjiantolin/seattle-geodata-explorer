import {
  G as G3
} from "./chunk-GCPUKR7G.js";
import "./chunk-NPBIIIFG.js";
import {
  P as P2,
  a as a5
} from "./chunk-TEIC2ZQX.js";
import {
  b as b4
} from "./chunk-HYRVFNQ4.js";
import {
  a as a6,
  c as c5,
  c2 as c6,
  i as i3,
  l as l8,
  l2 as l9
} from "./chunk-MFGB2SND.js";
import {
  V,
  p as p2
} from "./chunk-MBGFVGBP.js";
import {
  Y
} from "./chunk-APEX3SKN.js";
import {
  u as u3
} from "./chunk-E4I27YH3.js";
import "./chunk-4C3ICTLH.js";
import "./chunk-QLTQDZE3.js";
import "./chunk-45HBRDFP.js";
import "./chunk-GJUA2PO4.js";
import "./chunk-FDT7ORK5.js";
import {
  a as a4,
  m as m3,
  t as t2
} from "./chunk-OUDWAZ6C.js";
import "./chunk-Z3NLL7BL.js";
import "./chunk-SD4MUREZ.js";
import {
  r as r3
} from "./chunk-FK4XGZ74.js";
import "./chunk-6UTKK5E2.js";
import "./chunk-GSNVFSLW.js";
import "./chunk-CD53ZEOB.js";
import "./chunk-DZHIWTIE.js";
import "./chunk-7DRHOPHF.js";
import "./chunk-KP4YG7V2.js";
import "./chunk-FNTVWQBJ.js";
import "./chunk-EU7GSWOL.js";
import "./chunk-VIQ3APXQ.js";
import "./chunk-EK7HOTRR.js";
import "./chunk-RA6G6Y5I.js";
import {
  U as U2
} from "./chunk-HFXAIYNY.js";
import {
  s as s6
} from "./chunk-H26UYAA4.js";
import {
  s as s10
} from "./chunk-BYGCE5TN.js";
import "./chunk-ETP77TON.js";
import "./chunk-RMRI2MOR.js";
import "./chunk-ZLEN73Y5.js";
import "./chunk-CCWVXJPX.js";
import "./chunk-3KF2ZGIA.js";
import {
  n as n7
} from "./chunk-JTMHRPM3.js";
import "./chunk-N43PN74E.js";
import {
  _ as _2
} from "./chunk-S5R37GDB.js";
import "./chunk-C3N4ODHC.js";
import {
  M
} from "./chunk-G34IWJDK.js";
import "./chunk-XNTURHBL.js";
import "./chunk-QCIL6WNY.js";
import "./chunk-YP7MYCLC.js";
import "./chunk-P36ESQT6.js";
import "./chunk-HOEWLCGP.js";
import "./chunk-PEJX2KTK.js";
import "./chunk-2FGTJV3P.js";
import "./chunk-QZWNFZUD.js";
import "./chunk-JVJFN3DI.js";
import {
  u as u2
} from "./chunk-ACQH5G5T.js";
import "./chunk-LDTJ57MC.js";
import "./chunk-VTWAVKG3.js";
import "./chunk-UMZRTZ6I.js";
import "./chunk-OB5BIAYS.js";
import {
  $e,
  Ee,
  Fe,
  G as G2,
  H,
  Ie,
  K,
  W as W2,
  be,
  c as c4,
  e as e7,
  f as f3,
  g,
  ge,
  p,
  s as s9,
  u,
  we,
  xe,
  y as y3,
  ye
} from "./chunk-4TFK37K5.js";
import "./chunk-T36N6JHK.js";
import "./chunk-WULQ4UBI.js";
import {
  e as e6,
  n as n6
} from "./chunk-NV4FBUWV.js";
import {
  l as l7
} from "./chunk-VHLW6Z37.js";
import {
  l as l6
} from "./chunk-KEQQSEWI.js";
import "./chunk-O3FEXQID.js";
import {
  e as e5
} from "./chunk-IXHB4UWX.js";
import {
  $ as $2,
  G,
  I,
  c as c3,
  d,
  l as l5,
  m as m2,
  z
} from "./chunk-VJR6QL57.js";
import {
  n as n5
} from "./chunk-2WEHUA5E.js";
import "./chunk-ORRNI4IU.js";
import "./chunk-PY3DSTUV.js";
import {
  o as o2
} from "./chunk-Q5YBF7XB.js";
import "./chunk-46KDNVO7.js";
import "./chunk-UXOL3P23.js";
import {
  m as m4
} from "./chunk-IDVKQMHS.js";
import "./chunk-LL3S2X3V.js";
import {
  O
} from "./chunk-IZYA7RGI.js";
import "./chunk-IX7YRQHL.js";
import "./chunk-FKWVWVTW.js";
import "./chunk-VXIMEMZC.js";
import "./chunk-LYOZ7XMW.js";
import "./chunk-CL7E6LCF.js";
import "./chunk-5NKSBQZF.js";
import "./chunk-6LTC54B6.js";
import "./chunk-Q7XV7SOJ.js";
import "./chunk-JPXJSBUK.js";
import "./chunk-3OVERDFX.js";
import "./chunk-QYJPKVOE.js";
import "./chunk-MACCKOIR.js";
import "./chunk-Q75WYS7K.js";
import {
  r as r4
} from "./chunk-WNZTAI7W.js";
import "./chunk-HAANEHSB.js";
import "./chunk-4YD4R3KX.js";
import "./chunk-LAJBZNTW.js";
import "./chunk-ZTJQU2RH.js";
import "./chunk-ABFMWZM5.js";
import "./chunk-TORASWX2.js";
import "./chunk-TRQAGPC5.js";
import {
  n as n4
} from "./chunk-7DCPEAUN.js";
import {
  o as o3
} from "./chunk-ILFCTMRT.js";
import {
  e as e4
} from "./chunk-2O6SPRT2.js";
import {
  B,
  c as c2,
  e as e3
} from "./chunk-2RTXELG3.js";
import {
  h as h4,
  j,
  n as n3,
  y as y2
} from "./chunk-DVLDZKEN.js";
import {
  s as s8,
  t
} from "./chunk-GN3U7ENT.js";
import {
  s as s5
} from "./chunk-NATAHEZJ.js";
import {
  c,
  l as l3,
  s as s4
} from "./chunk-5LVYWV4X.js";
import "./chunk-QF7FRVN6.js";
import {
  U,
  a as a2,
  f,
  h as h2,
  l
} from "./chunk-PWRHAS2K.js";
import {
  f as f2
} from "./chunk-OA23GZJG.js";
import {
  l as l4
} from "./chunk-MLAT2KTD.js";
import {
  C,
  F,
  N as N2,
  P,
  W,
  b as b2,
  k,
  x
} from "./chunk-RTBCHVHT.js";
import {
  e as e2,
  h as h3,
  n as n2
} from "./chunk-MRUYFF2S.js";
import "./chunk-OOEHBQ6Y.js";
import {
  DateTime,
  FixedOffsetZone
} from "./chunk-IQYRAWGM.js";
import {
  b,
  l as l2
} from "./chunk-LW56SJFX.js";
import {
  s as s7
} from "./chunk-THUQUQVK.js";
import {
  b as b3,
  m2 as m,
  r3 as r2,
  s
} from "./chunk-6HTJSLOW.js";
import {
  a2 as a3
} from "./chunk-WFBS5ZME.js";
import "./chunk-YN6OROIK.js";
import "./chunk-2BIZMKNP.js";
import {
  __decorate
} from "./chunk-LDGQQYYE.js";
import "./chunk-T6DBK2Y7.js";
import "./chunk-RLIQR4ST.js";
import "./chunk-3ZPDDBK5.js";
import "./chunk-7WC7GR3F.js";
import {
  _,
  ct,
  it
} from "./chunk-5B2RE5AQ.js";
import "./chunk-K5NFV5DN.js";
import {
  $,
  L,
  T,
  e2 as e,
  n2 as n,
  o,
  s as s3,
  y
} from "./chunk-NEED4PQC.js";
import {
  a,
  i2 as i,
  i3 as i2,
  r,
  s2
} from "./chunk-UKO437OJ.js";
import {
  N,
  h
} from "./chunk-V7ANDVX5.js";
import "./chunk-IGFLZ7B3.js";
import {
  CSSResult,
  LitElement,
  adoptStyles,
  css
} from "./chunk-M5U4LZFZ.js";
import {
  __publicField
} from "./chunk-3F74YA3Z.js";

// node_modules/@arcgis/core/widgets/Attachments/support/attachmentUtils.js
function e8(i10) {
  const e17 = i10.toLowerCase();
  return "image/bmp" === e17 || "image/emf" === e17 || "image/exif" === e17 || "image/gif" === e17 || "image/x-icon" === e17 || "image/jpeg" === e17 || "image/png" === e17 || "image/tiff" === e17 || "image/x-wmf" === e17;
}
function a7(i10) {
  return i10 ? "text/plain" === i10 ? "file-text" : "application/pdf" === i10 ? "file-pdf" : "text/csv" === i10 ? "file-csv" : "application/gpx+xml" === i10 ? "file-gpx" : "application/x-dwf" === i10 ? "file-cad" : "application/postscript" === i10 || "application/json" === i10 || "text/xml" === i10 || "model/vrml" === i10 ? "file-code" : "application/x-zip-compressed" === i10 || "application/x-7z-compressed" === i10 || "application/x-gzip" === i10 || "application/x-tar" === i10 || "application/x-gtar" === i10 || "application/x-bzip2" === i10 || "application/gzip" === i10 || "application/x-compress" === i10 || "application/x-apple-diskimage" === i10 || "application/x-rar-compressed" === i10 || "application/zip" === i10 ? "file-zip" : i10.includes("image/") ? "file-image" : i10.includes("audio/") ? "file-sound" : i10.includes("video/") ? "file-video" : i10.includes("msexcel") || i10.includes("ms-excel") || i10.includes("spreadsheetml") ? "file-excel" : i10.includes("msword") || i10.includes("ms-word") || i10.includes("wordprocessingml") ? "file-word" : i10.includes("powerpoint") || i10.includes("presentationml") ? "file-ppt" : "file" : "file";
}

// node_modules/@arcgis/core/widgets/support/iconUtils.js
var e9 = { danger: "exclamation-mark-circle", info: "information", success: "check", warning: "exclamation-mark-triangle", brand: void 0 };
function r5(e17) {
  if (null == e17 || "object" != typeof e17) return "layer";
  if ("isTable" in e17 && e17.isTable) return "table";
  if ("type" in e17) {
    if ("feature" === e17.type) return "feature-layer";
    if ("group" === e17.type) return "layers";
    if ("subtype-group" === e17.type) return "subtype";
    if ("knowledge-graph" === e17.type) return "knowledge-layer";
    if ("link-chart" === e17.type) return "knowledge-graph";
    if ("graphics" === e17.type) return "layer-graphics";
  }
  return "layer";
}

// node_modules/@arcgis/core/widgets/Editor/components/Prompt.js
var c7 = "esri-editor__prompt";
var n9 = { base: c7, header: `${c7}__header`, heading: `${c7}__header__heading`, message: `${c7}__message`, divider: `${c7}__divider`, actions: `${c7}__actions`, halfWidthButton: `${c7}__half-width-button` };
var s11 = () => c2({ button: () => import("./calcite-button-LQNLDGVJ.js"), icon: () => import("./calcite-icon-J7CTACBF.js"), scrim: () => import("./calcite-scrim-JWSJAJ34.js"), label: () => import("./calcite-label-VMVGTYRU.js"), "radio-button": () => import("./calcite-radio-button-WHHIAM6J.js"), "radio-button-group": () => import("./calcite-radio-button-group-EFZQNYWV.js") });
function r6({ actions: t5, context: c17, headingLevel: s22, message: r12, title: d6, ...p14 }) {
  const { primary: m11, secondary: u8 } = t5;
  let b7;
  switch (c17) {
    case "danger":
      b7 = "danger";
      break;
    case "info":
      b7 = "brand";
      break;
    default:
      b7 = "neutral";
  }
  const h9 = n3("calcite-button", { afterCreate: (t6) => j(t6), appearance: "solid", classes: { [n9.halfWidthButton]: !!u8 }, "data-testid": "primary-prompt-button", key: "prompt-primary-button", kind: b7, onclick: m11.action, width: "full" }, m11.label), g5 = u8 && n3("calcite-button", { appearance: "outline", class: n9.halfWidthButton, "data-testid": "secondary-prompt-button", key: "prompt-secondary-button", kind: b7, onclick: u8.action, width: "full" }, u8.label);
  return n3("calcite-scrim", { "data-testid": "prompt", key: "prompt" }, n3("div", { class: `${n9.base}--${c17}` }, n3("div", { class: n9.header }, n3("calcite-icon", { icon: e9.warning }), n3(s10, { class: n9.heading, level: s22 }, d6)), n3("div", { class: n9.message }, r12), l10(p14) ?? n3("div", { class: n9.divider }), n3("div", { class: n9.actions }, g5, h9)));
}
function l10({ radios: t5, onRadioSelection: e17, defaultRadioSelection: o8 }) {
  return (t5 == null ? void 0 : t5.length) ? n3("calcite-radio-button-group", { name: "PromptChoiceList" }, t5.map((t6) => n3("calcite-label", { layout: "inline" }, n3("calcite-radio-button", { checked: t6.value === o8, value: t6.value, onCalciteRadioButtonChange: () => e17 == null ? void 0 : e17(t6.value) }), t6.label))) : void 0;
}

// node_modules/@arcgis/core/widgets/FeatureTable/ColumnMenuVisibleElements.js
var t3 = class extends b3 {
  constructor(o8) {
    super(o8), this.sortAscending = true, this.sortDescending = true;
  }
};
__decorate([m({ type: Boolean, nonNullable: true })], t3.prototype, "sortAscending", void 0), __decorate([m({ type: Boolean, nonNullable: true })], t3.prototype, "sortDescending", void 0), t3 = __decorate([a3("esri.widgets.FeatureTable.ColumnMenuVisibleElements")], t3);
var c8 = t3;

// node_modules/@arcgis/core/core/throttle.js
function l11(l17, a17, u8, t5) {
  let o8 = null, n19 = 1e3;
  "number" == typeof a17 ? (n19 = a17, t5 = u8) : (o8 = a17 ?? null, n19 = u8);
  const r12 = r3(0);
  let v2;
  const p14 = () => {
    r12.value = 0, l17.apply(t5, v2);
  }, i10 = (...e17) => {
    o8 && o8.apply(t5, e17), v2 = e17, n19 ? r12.value || (r12.value = setTimeout(p14, n19)) : p14();
  };
  return i10.remove = () => {
    r12.value && (clearTimeout(r12.value), r12.value = 0);
  }, i10.forceUpdate = () => {
    r12.value && (clearTimeout(r12.value), p14());
  }, i10.hasPendingUpdates = () => !!r12.value, i10;
}

// node_modules/@arcgis/core/editing/fieldUtils.js
function e10(e17, t5) {
  return I(t5) ? t5.getFieldAlias(e17.name) : e17.alias;
}

// node_modules/@arcgis/core/views/support/HighlightDefaults.js
var e11 = new u2("cyan");
var n10 = new u2("black");
var c9 = "default";
var m5 = "temporary";
var p3 = new u2("yellow");

// node_modules/@arcgis/core/views/support/selectionUtils.js
function e12(e17) {
  return !(null == e17 || "object" != typeof e17 || !("createQuery" in e17) || !e17.createQuery);
}

// node_modules/@arcgis/core/widgets/FeatureTable/Grid/ColumnCSS.js
var e13 = "esri-column";
var n11 = { contentFull: `${e13}__content--full`, sorter: `${e13}__sorter`, headerContent: `${e13}__header-content`, headerDescription: `${e13}__header-description`, headerLabel: `${e13}__header-label`, headerMenuIcon: `${e13}__header-menu-icon` };

// node_modules/@arcgis/core/widgets/FeatureTable/support/columnUtils.js
function n12(n19) {
  return null != n19 && "object" == typeof n19 && "type" in n19 && null !== n19.type && ("column" === n19.type || "field" === n19.type || "group" === n19.type || "attachment" === n19.type || "relationship" === n19.type);
}
function t4(n19) {
  return !(!n19 || !("field" in n19));
}
function e14(n19) {
  return !(!n19 || !("columns" in n19));
}
function i4(n19) {
  return !(!n19 || !("relationshipId" in n19));
}
function o4(n19) {
  return !(!n19 || !("onShowAttachments" in n19));
}

// node_modules/@arcgis/core/widgets/FeatureTable/support/tableUtils.js
var o5 = { action: "EsriFeatureTableActionColumn", attachments: "EsriFeatureTableAttachmentsColumn", relationship: "EsriFeatureTableRelationshipColumn" };
var r7 = () => c2({ action: () => import("./calcite-action-N754OQKO.js"), button: () => import("./calcite-button-LQNLDGVJ.js"), dropdown: () => import("./calcite-dropdown-X3NUDWH3.js"), "dropdown-item": () => import("./calcite-dropdown-item-KWOBFAHU.js"), "dropdown-group": () => import("./calcite-dropdown-group-MUIQVPXG.js"), icon: () => import("./calcite-icon-J7CTACBF.js") });
var c10 = () => Promise.all([c2({ input: () => import("./calcite-input-43II23ME.js"), option: () => import("./calcite-option-Z36KIRT4.js"), select: () => import("./calcite-select-T7HEFGX3.js"), "input-date-picker": () => import("./calcite-input-date-picker-AGE5W7KE.js"), "input-time-picker": () => import("./calcite-input-time-picker-VWFB7GYN.js") }), r7()]);
var s12 = () => Promise.all([c2({ "action-bar": () => import("./calcite-action-bar-QLZ74S6D.js") }), c10()]);
function a8(t5) {
  return null != t5 && "object" == typeof t5 && "createQuery" in t5 && "getField" in t5 && "queryFeatureCount" in t5 && "queryFeatures" in t5 && "queryObjectIds" in t5 && "capabilities" in t5 && "fields" in t5 && "fieldsIndex" in t5 && "id" in t5 && "load" in t5 && "objectIdField" in t5 && "type" in t5 && "when" in t5;
}
function l12(t5) {
  return a8(t5) && "queryAttachments" in t5;
}
function u4(e17) {
  return a8(e17) && I(e17) && "fieldConfigurations" in e17 && "getFieldConfiguration" in e17;
}
function p4(t5) {
  return l12(t5) && "addAttachment" in t5 && "deleteAttachments" in t5 && "updateAttachment" in t5;
}
function m6(t5) {
  return a8(t5) && "editingEnabled" in t5 && "applyEdits" in t5;
}
function d2(t5) {
  return a8(t5) && "relationships" in t5 && "queryRelatedFeatures" in t5 && "queryRelatedFeaturesCount" in t5;
}
function f4(t5, e17, n19) {
  if (e17 == null ? void 0 : e17.map) return y4(t5, [...e17.map.allLayers, ...e17.map.allTables], n19);
}
function y4(t5, n19, { relatedTableId: i10 }) {
  var _a2, _b, _c, _d;
  const o8 = "scene" === t5.type && t5.associatedLayer ? t5.associatedLayer.url : t5.url, r12 = (e17) => {
    var _a3;
    return !(!d2(e17) || e17 === t5) && ("sublayer" === e17.type && ((_a3 = e17.layer) == null ? void 0 : _a3.url) === t5.url ? e17.id === i10 : o8 === e17.url && e17.layerId === i10);
  };
  if ("sublayer" === t5.type && "layer" in t5 && c3(t5.layer)) {
    const e17 = (e18) => d2(e18) && e18 !== t5 && e18.id === i10, n20 = t5.layer, o9 = ((_a2 = n20.allSublayers) == null ? void 0 : _a2.find(e17)) || ((_b = n20.subtables) == null ? void 0 : _b.find(e17));
    if (d2(o9)) return o9;
  }
  let c17 = null;
  for (const s22 of n19) if (c3(s22)) {
    const t6 = ((_c = s22.allSublayers) == null ? void 0 : _c.find(r12)) || ((_d = s22.subtables) == null ? void 0 : _d.find(r12));
    if (t6) {
      c17 = t6;
      break;
    }
  } else if (r12(s22)) {
    c17 = s22;
    break;
  }
  if (d2(c17)) return c17;
}
function b5(t5, e17) {
  return null != t5 && Number.isInteger(t5) && t5 > -1 && t5 < e17;
}
function h5(t5) {
  return null == t5 || 0 === t5.trim().length;
}
function g2(t5, e17) {
  return (t5.relationships ?? []).filter((t6) => !(t6.id === e17 && "one-to-one" === t6.cardinality)).map(({ id: t6 }) => ({ relationshipId: t6 }));
}
function F2(t5, e17) {
  var _a2;
  return (_a2 = t5.relationships) == null ? void 0 : _a2.find(({ id: t6 }) => t6 === e17);
}
function w(t5, e17) {
  var _a2;
  return null == e17 ? null : (_a2 = t5 == null ? void 0 : t5.fields) == null ? void 0 : _a2.find((t6) => {
    var _a3;
    return e17.toLowerCase() === ((_a3 = t6.name) == null ? void 0 : _a3.toLowerCase());
  });
}
function I2(t5, e17) {
  return (e17 == null ? void 0 : e17.some((e18) => !(!e14(e18) || !I2(t5, e18.columns)) || e18.fieldName === t5)) ?? false;
}
function j2(t5, e17) {
  return `${t5.id}-${e17}`;
}

// node_modules/@arcgis/core/widgets/support/uriUtils.js
var p5 = [{ pattern: /^\s*(https?:\/\/([^\s]+))\s*$/i, target: "_blank", label: "{messages.view}" }, { pattern: /^\s*(tel:([^\s]+))\s*$/i, label: "{hierPart}" }, { pattern: /^\s*(mailto:([^\s]+))\s*$/i, label: "{hierPart}" }, { pattern: /^\s*(arcgis-appstudio-player:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "App Studio Player" }, { pattern: /^\s*(arcgis-collector:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "Collector" }, { pattern: /^\s*(arcgis-explorer:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "Explorer" }, { pattern: /^\s*(arcgis-navigator:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "Navigator" }, { pattern: /^\s*(arcgis-survey123:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "Survey123" }, { pattern: /^\s*(arcgis-trek2there:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "Trek2There" }, { pattern: /^\s*(arcgis-workforce:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "Workforce" }, { pattern: /^\s*(iform:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "iForm" }, { pattern: /^\s*(flow:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "FlowFinity" }, { pattern: /^\s*(lfmobile:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "Laserfische" }, { pattern: /^\s*(mspbi:\/\/([^\s]+))\s*$/i, label: "{messages.openInApp}", appName: "Microsoft Power Bi" }];
function r8(r12, t5) {
  if ("string" != typeof t5 || !t5) return t5;
  const n19 = p5.find((e17) => e17.pattern.test(t5));
  if (!n19) return t5;
  const i10 = t5.match(n19.pattern), l17 = i10 == null ? void 0 : i10[2], o8 = r(r(n19.label, { messages: r12, hierPart: l17 }), { appName: n19.appName }), m11 = n19.target ? ` target="${n19.target}"` : "", g5 = "_blank" === n19.target ? ' rel="noreferrer"' : "";
  return t.sanitizeUrl(_(t5)).replace(n19.pattern, `<a${m11} href="$1"${g5}>${o8}</a>`);
}

// node_modules/@arcgis/core/widgets/FeatureTable/Grid/Column.js
var m7 = /<br\s*\/*>/gi;
var h6 = class extends b3 {
  constructor(e17) {
    super(e17), this._menuIsOpen = false, this.autoWidth = false, this.cellValueFormatFunction = ({ root: e18, rowData: t5, value: i10 }) => {
      const { formatFunction: n19 } = this, o8 = this.sanitizeContent(i10);
      if (n19 && t5) {
        const { index: i11, item: { attachments: s22, feature: r12, relatedRecords: l17 } } = t5;
        return n19({ attachments: s22, column: this, feature: r12, index: i11, relatedRecords: l17, value: o8, virtualIndex: this.getVirtualRowIndex(e18), field: void 0 });
      }
      return o8;
    }, this.description = null, this.direction = null, this.fieldName = null, this.flexGrow = 1, this.footerRenderFunction = null, this.formatFunction = null, this.frozen = false, this.frozenToEnd = false, this.grid = null, this.headerRenderFunction = (e18) => {
      const { root: t5 } = e18, i10 = this.createHeaderContent();
      this.removeCellContent(t5), t5.appendChild(i10);
    }, this.hidden = false, this.icon = null, this.iconText = null, this.initialSortPriority = null, this.label = null, this.labelTooltipText = null, this.menuConfig = null, this.messages = null, this.messagesCommon = null, this.messagesURIUtils = null, this.renderFunction = ({ root: e18, rowData: t5 }) => {
      const i10 = this.getCellValue(t5), n19 = this.cellValueFormatFunction({ root: e18, rowData: t5, value: i10 });
      let o8 = null;
      if (o8 = n19 instanceof HTMLElement ? n19 : r8(this.messagesURIUtils, n19), this.removeCellContent(e18), o8 instanceof HTMLElement) e18.removeAttribute("title"), e18.appendChild(o8);
      else if (null != o8) {
        const t6 = o8.toString();
        e18.innerHTML = t6, e18.title = t6;
      }
    }, this.resizable = true, this.sortable = false, this.store = null, this.tableTimeZone = null, this.textAlign = "start", this.textWrap = false, this.timeZone = null, this.visibleElements = null, this.width = 200;
  }
  initialize() {
    const { fieldName: e17 } = this;
    this._set("sortElement", this.createSortElement()), this.sortElement.setAttribute("path", e17), this.addHandles([l(() => this.direction, (e18) => {
      const { sortElement: t5 } = this;
      this.effectiveSortable && t5 && (e18 ? this.sortElement.direction !== e18 && t5.setAttribute("direction", e18) : t5.removeAttribute("direction"));
    }), f(() => {
      var _a2;
      return ((_a2 = this.grid) == null ? void 0 : _a2.isReady) && this.sortElement;
    }, () => {
      const { direction: e18, grid: t5, hidden: i10, sortElement: n19 } = this;
      i10 && e18 && n19 && t5 && !t5.hasSorter(n19) && t5.addSorter(n19);
    }, h2), l(() => [this.timeZone, this.icon, this.iconText, this.labelTooltipText], () => {
      var _a2;
      return (_a2 = this.grid) == null ? void 0 : _a2.requestContentUpdate();
    }), l(() => this.hidden, () => this.closeMenu()), l(() => this.fieldName, (e18) => {
      const { sortElement: t5 } = this;
      t5 && t5.path !== e18 && t5.setAttribute("path", e18);
    }), l(() => [this.effectiveLabel, this.labelTooltipText], ([e18, t5]) => {
      const { sortElement: i10 } = this;
      i10 && (i10.setAttribute("title", t5 || e18), i10.textContent = e18);
    })]);
  }
  get _headerRequiresTextContainer() {
    return !(!this.effectiveSortable && "" === this.effectiveLabel && null == this.effectiveDescription);
  }
  get customMenuItems() {
    var _a2, _b;
    return ((_b = (_a2 = this.menuConfig) == null ? void 0 : _a2.items) == null ? void 0 : _b.map(({ disabled: e17, hidden: t5, icon: i10, iconClass: n19, label: o8, selected: s22, clickFunction: r12 }) => this.createCalciteDropdownItem({ disabled: e17, hidden: t5, iconClass: n19, iconStart: i10, selected: s22, textContent: o8, onclick: (e18) => r12(e18) }))) ?? [];
  }
  get defaultMenuItems() {
    var _a2;
    if (!this.effectiveSortable) return [];
    const e17 = this.messages, t5 = [], i10 = (_a2 = this.visibleElements) == null ? void 0 : _a2.columnMenuItems;
    if (false !== (i10 == null ? void 0 : i10.sortAscending)) {
      const i11 = this.createCalciteDropdownItem({ iconStart: "sort-ascending-arrow", textContent: e17 == null ? void 0 : e17.sortAsc, onclick: () => this.direction = "asc" });
      t5.push(i11);
    }
    if (false !== (i10 == null ? void 0 : i10.sortDescending)) {
      const i11 = this.createCalciteDropdownItem({ iconStart: "sort-descending-arrow", textContent: e17 == null ? void 0 : e17.sortDesc, onclick: () => this.direction = "desc" });
      t5.push(i11);
    }
    return t5;
  }
  get effectiveDescription() {
    const { description: e17 } = this;
    return h5(e17) ? null : this.sanitizeLabel(e17);
  }
  get effectiveLabel() {
    return this.sanitizeLabel(this.label || this.fieldName);
  }
  get effectiveSortable() {
    return this.sortable;
  }
  get iconNode() {
    const { icon: e17, iconText: t5 } = this, i10 = t5 ? this.sanitizeLabel(t5) : void 0;
    return e17 ? this.createCalciteIcon({ icon: e17, textLabel: i10 }) : null;
  }
  set invalid(e17) {
    var _a2;
    this._set("invalid", e17), (_a2 = this.grid) == null ? void 0 : _a2.generateCellPartNames();
  }
  get invalid() {
    return this._get("invalid") ?? false;
  }
  get menu() {
    var _a2;
    const { customMenuItems: e17, defaultMenuItems: t5, menuConfig: i10 } = this, n19 = !!(i10 == null ? void 0 : i10.open), o8 = this.createCalciteAction({ icon: (i10 == null ? void 0 : i10.icon) ?? "ellipsis", text: ((_a2 = this.messages) == null ? void 0 : _a2.menu) ?? "", slot: "trigger" }), s22 = document.createElement("calcite-dropdown");
    if (s22.maxItems = (i10 == null ? void 0 : i10.maxItems) ?? 4, s22.placement = "top-end", s22.scale = (i10 == null ? void 0 : i10.scale) ?? "m", n19 && (s22.open = n19, this._menuIsOpen = true), s22.appendChild(o8), s22.addEventListener("calciteDropdownOpen", () => this._menuIsOpen = true), s22.addEventListener("calciteDropdownClose", () => this._menuIsOpen = false), s22.onkeydown = this._stopPropagationOnSelect, s22.addEventListener("mousewheel", (e18) => {
      e18.stopPropagation();
    }), t5.length) {
      const e18 = s22.appendChild(document.createElement("calcite-dropdown-group"));
      e18.selectionMode = "none", e18.append(...t5);
    }
    if (e17.length) {
      const t6 = s22.appendChild(document.createElement("calcite-dropdown-group"));
      t6.selectionMode = (i10 == null ? void 0 : i10.selectionMode) ?? "none", t6.append(...e17);
    }
    return s22;
  }
  get menuItems() {
    return [...this.defaultMenuItems, ...this.customMenuItems];
  }
  get menuIsOpen() {
    return this._menuIsOpen;
  }
  get menuIsVisible() {
    var _a2;
    return !(false === ((_a2 = this.visibleElements) == null ? void 0 : _a2.columnMenus) || !this.menuItems.length);
  }
  closeMenu() {
    this.menu.open = false;
  }
  createSortElement() {
    const { effectiveLabel: e17, direction: t5, fieldName: i10, initialSortPriority: n19 } = this, o8 = document.createElement("vaadin-grid-sorter");
    return o8.classList.add(n11.sorter), o8.setAttribute("path", i10), o8.setAttribute("title", this.labelTooltipText || e17), o8.textContent = e17, t5 && o8.setAttribute("direction", t5), null != n19 && (o8._initialOrder = n19), o8.addEventListener("direction-changed", () => {
      this.direction !== o8.direction && this._set("direction", o8.direction);
    }), o8;
  }
  createCalciteAction(e17) {
    const t5 = document.createElement("calcite-action"), { alignment: i10, appearance: n19, className: o8, disabled: s22, icon: r12, label: l17, scale: a17, slot: c17, text: d6, textEnabled: p14, onclick: u8 } = e17;
    return t5.alignment = i10 ?? "center", t5.appearance = n19 ?? "transparent", t5.className = o8 ?? "", t5.disabled = !!s22, t5.icon = r12 ?? "", t5.label = l17 ?? "", t5.scale = a17 ?? "s", t5.slot = c17 ?? "", t5.text = t5.title = d6 ?? "", t5.textEnabled = !!p14, t5.onclick = u8 ?? null, t5;
  }
  createCalciteButton(e17) {
    const t5 = document.createElement("calcite-button"), { alignment: i10, appearance: n19, className: o8, disabled: s22, iconEnd: r12, iconFlipRtl: l17, kind: a17, loading: c17, onclick: d6, scale: p14, textContent: u8, title: m11, width: h9 } = e17;
    return t5.alignment = i10 ?? "center", t5.className = o8 ?? "", t5.scale = p14 ?? "s", t5.appearance = n19 ?? "transparent", t5.disabled = !!s22, t5.iconEnd = r12 ?? "", l17 && (t5.iconFlipRtl = l17), t5.kind = a17 ?? "brand", t5.loading = !!c17, t5.textContent = u8 ?? "", m11 && (t5.title = m11), t5.width = h9 ?? "auto", t5.onclick = d6 ?? null, t5;
  }
  createCalciteDropdownItem(e17) {
    const t5 = document.createElement("calcite-dropdown-item"), { disabled: i10, hidden: n19, iconClass: o8, iconStart: s22, textContent: r12, onclick: l17, selected: a17 } = e17, d6 = r12 ?? "";
    if (o8 && !s22) {
      const e18 = document.createElement("span"), i11 = document.createElement("span");
      i11.classList.add(n11.headerMenuIcon, o8), e18.textContent = e18.title = d6, e18.insertBefore(i11, e18.firstChild), t5.appendChild(e18);
    } else t5.iconStart = s22 ?? "", t5.textContent = t5.title = d6;
    return t5.disabled = !!i10, t5.hidden = "function" == typeof n19 ? n19() : !!n19, t5.selected = !!a17, l17 && (t5.addEventListener("calciteDropdownItemSelect", l17), t5.onkeydown = this._stopPropagationOnSelect), t5;
  }
  createCalciteIcon(e17) {
    const { effectiveLabel: t5 } = this, { icon: i10, scale: n19, textLabel: o8 } = e17, s22 = document.createElement("calcite-icon"), r12 = o8 ?? t5;
    return s22.icon = i10, s22.scale = n19 ?? "s", s22.textLabel = r12, s22.title = r12, s22;
  }
  createHeaderContent() {
    var _a2;
    const { effectiveDescription: e17, effectiveLabel: t5, iconNode: i10 } = this, n19 = document.createElement("div");
    if (n19.classList.add(n11.headerContent), i10 && n19.appendChild(i10), !this._headerRequiresTextContainer) return n19;
    const o8 = n19.appendChild(document.createElement("div"));
    return this.effectiveSortable ? o8.appendChild(this.sortElement) : "" !== t5 && o8.appendChild(this._createHeaderTextElement({ label: t5, cssClass: n11.headerLabel, title: this.labelTooltipText })), null != e17 && false !== ((_a2 = this.visibleElements) == null ? void 0 : _a2.columnDescriptions) && o8.appendChild(this._createHeaderTextElement({ label: e17, cssClass: n11.headerDescription })), this.menuIsVisible && n19.appendChild(this.menu), n19;
  }
  getCellValue(e17) {
    var _a2;
    return (_a2 = e17 == null ? void 0 : e17.item.feature.attributes) == null ? void 0 : _a2[this.fieldName];
  }
  getVirtualRowIndex(e17) {
    var _a2;
    const t5 = (_a2 = this.grid) == null ? void 0 : _a2.getRowContainingNode(e17);
    return (t5 == null ? void 0 : t5.parentElement) ? [...t5.parentElement.children].indexOf(t5) : -1;
  }
  removeCellContent(e17) {
    if (e17 && e17.firstChild) try {
      for (; e17 == null ? void 0 : e17.firstChild; ) e17 == null ? void 0 : e17.removeChild(e17.firstChild);
    } catch (t5) {
    }
  }
  openMenu() {
    this.menuIsVisible && (this.menu.open = true, j(this.menuItems[0]));
  }
  sanitizeContent(e17) {
    return t.sanitize(e17);
  }
  sanitizeLabel(e17) {
    return null == e17 || "" === e17 ? "" : s8.sanitize(e17.toString()).replaceAll(m7, " ");
  }
  sort() {
    var _a2;
    this.effectiveSortable && ((_a2 = this.sortElement) == null ? void 0 : _a2.click());
  }
  _stopPropagationOnSelect(e17) {
    "Enter" !== e17.key && " " !== e17.key || e17.stopPropagation();
  }
  _createHeaderTextElement({ cssClass: e17, label: t5, title: i10 }) {
    const n19 = document.createElement("span");
    n19.classList.add(e17);
    const o8 = n19.appendChild(document.createElement("span"));
    return o8.textContent = t5, o8.setAttribute("title", i10 || t5), n19;
  }
};
__decorate([m()], h6.prototype, "_headerRequiresTextContainer", null), __decorate([m()], h6.prototype, "_menuIsOpen", void 0), __decorate([m()], h6.prototype, "autoWidth", void 0), __decorate([m()], h6.prototype, "cellValueFormatFunction", void 0), __decorate([m()], h6.prototype, "customMenuItems", null), __decorate([m()], h6.prototype, "defaultMenuItems", null), __decorate([m()], h6.prototype, "description", void 0), __decorate([m()], h6.prototype, "direction", void 0), __decorate([m()], h6.prototype, "effectiveDescription", null), __decorate([m()], h6.prototype, "effectiveLabel", null), __decorate([m()], h6.prototype, "effectiveSortable", null), __decorate([m({ constructOnly: true })], h6.prototype, "fieldName", void 0), __decorate([m()], h6.prototype, "flexGrow", void 0), __decorate([m()], h6.prototype, "footerRenderFunction", void 0), __decorate([m()], h6.prototype, "formatFunction", void 0), __decorate([m()], h6.prototype, "frozen", void 0), __decorate([m()], h6.prototype, "frozenToEnd", void 0), __decorate([m()], h6.prototype, "grid", void 0), __decorate([m()], h6.prototype, "iconNode", null), __decorate([m()], h6.prototype, "invalid", null), __decorate([m()], h6.prototype, "headerRenderFunction", void 0), __decorate([m()], h6.prototype, "hidden", void 0), __decorate([m()], h6.prototype, "icon", void 0), __decorate([m()], h6.prototype, "iconText", void 0), __decorate([m({ constructOnly: true })], h6.prototype, "initialSortPriority", void 0), __decorate([m()], h6.prototype, "label", void 0), __decorate([m()], h6.prototype, "labelTooltipText", void 0), __decorate([m()], h6.prototype, "menu", null), __decorate([m()], h6.prototype, "menuItems", null), __decorate([m()], h6.prototype, "menuConfig", void 0), __decorate([m()], h6.prototype, "menuIsOpen", null), __decorate([m()], h6.prototype, "menuIsVisible", null), __decorate([m()], h6.prototype, "messages", void 0), __decorate([m()], h6.prototype, "messagesCommon", void 0), __decorate([m()], h6.prototype, "messagesURIUtils", void 0), __decorate([m()], h6.prototype, "renderFunction", void 0), __decorate([m()], h6.prototype, "resizable", void 0), __decorate([m()], h6.prototype, "sortable", void 0), __decorate([m({ readOnly: true })], h6.prototype, "sortElement", void 0), __decorate([m()], h6.prototype, "store", void 0), __decorate([m()], h6.prototype, "tableTimeZone", void 0), __decorate([m()], h6.prototype, "textAlign", void 0), __decorate([m()], h6.prototype, "textWrap", void 0), __decorate([m()], h6.prototype, "timeZone", void 0), __decorate([m()], h6.prototype, "visibleElements", void 0), __decorate([m()], h6.prototype, "width", void 0), h6 = __decorate([a3("esri.widgets.FeatureTable.Grid.Column")], h6);
var f5 = h6;

// node_modules/@arcgis/core/widgets/FeatureTable/ActionColumn.js
var n13 = { action: "esri-column__action" };
var a9 = class extends f5 {
  constructor(o8) {
    super(o8), this.autoWidth = false, this.callback = () => {
    }, this.fieldName = o5.action, this.flexGrow = 0, this.frozenToEnd = true, this.disabled = null, this.headerRenderFunction = () => {
    }, this.icon = "pencil", this.renderFunction = ({ root: o9, rowData: t5 }) => {
      if (!t5) return;
      const { index: e17, item: { feature: i10 } } = t5, { callback: s22, disabled: a17, icon: d6, effectiveLabel: c17 } = this, l17 = "function" == typeof a17 ? a17({ feature: i10, index: e17 }) : !!a17, p14 = (o10) => {
        o10.stopPropagation(), s22({ index: e17, feature: i10, native: o10 });
      };
      if (o9.firstChild) {
        const t6 = o9.firstChild;
        return t6.disabled = l17, t6.icon = d6, t6.text = c17, void (t6.onclick = p14);
      }
      const m11 = this.createCalciteAction({ alignment: "center", className: `${n13.action} ${n11.contentFull}`, disabled: l17, icon: d6, text: c17, onclick: p14 });
      this.removeCellContent(o9), o9.appendChild(m11);
    }, this.resizable = false, this.sortable = false, this.width = "50px";
  }
};
__decorate([m({ readOnly: true })], a9.prototype, "autoWidth", void 0), __decorate([m()], a9.prototype, "callback", void 0), __decorate([m({ readOnly: true })], a9.prototype, "fieldName", void 0), __decorate([m({ readOnly: true })], a9.prototype, "flexGrow", void 0), __decorate([m()], a9.prototype, "frozenToEnd", void 0), __decorate([m()], a9.prototype, "disabled", void 0), __decorate([m()], a9.prototype, "headerRenderFunction", void 0), __decorate([m()], a9.prototype, "icon", void 0), __decorate([m()], a9.prototype, "renderFunction", void 0), __decorate([m({ readOnly: true })], a9.prototype, "resizable", void 0), __decorate([m({ readOnly: true })], a9.prototype, "sortable", void 0), __decorate([m({ readOnly: true })], a9.prototype, "width", void 0), a9 = __decorate([a3("esri.widgets.FeatureTable.ActionColumn")], a9);
var d3 = a9;

// node_modules/@arcgis/core/widgets/FeatureTable/AttachmentsColumn.js
var c11 = "esri-attachments-column";
var h7 = { contentContainer: `${c11}__content`, showAttachmentsButton: `${c11}__button` };
var m8 = class extends f5 {
  constructor(t5) {
    super(t5), this._buttonElements = new s6(), this._thumbnailElements = new s6(), this.attachmentsViewEnabled = true, this.cellValueFormatFunction = ({ root: t6, rowData: e17 }) => {
      const { _showAttachmentsViewEnabled: n19, formatFunction: o8 } = this, s22 = this.getCellValue(e17);
      if (o8 && e17) {
        const { index: n20, item: { attachments: a18, feature: i11, relatedRecords: r13 } } = e17;
        return o8({ attachments: a18, column: this, feature: i11, index: n20, relatedRecords: r13, value: s22, virtualIndex: this.getVirtualRowIndex(t6), field: void 0 });
      }
      if (!e17) return s22;
      const { index: a17, item: { attachments: i10, objectId: r12 } } = e17, l17 = `(${s22})`;
      if (!this.thumbnailsEnabled && !n19) return s22;
      const c17 = document.createElement("span");
      c17.textContent = c17.title = l17;
      let m11 = null;
      if (this.thumbnailsEnabled && (m11 = this._createOrSyncThumbnails(r12, i10 ?? [])), this._showAttachmentsViewEnabled) {
        const t7 = document.createElement("div");
        m11 && t7.appendChild(m11), t7.appendChild(c17);
        const e18 = this._createOrSyncShowAttachmentsButton(r12, a17);
        return e18.appendChild(t7), e18;
      }
      const u8 = document.createElement("div");
      return u8.classList.add(h7.contentContainer), m11 && u8.appendChild(m11), u8.appendChild(c17), u8;
    }, this.icon = "attachment", this.layer = null, this.onShowAttachments = null, this.sortable = false, this.store = null, this.textAlign = "center", this.thumbnailAppearance = "image", this.thumbnailCount = 8, this.thumbnailIconScale = "m", this.thumbnailsEnabled = true;
  }
  get _showAttachmentsViewEnabled() {
    return !(!this.attachmentsViewEnabled || !this.onShowAttachments);
  }
  get _supportsResize() {
    var _a2;
    return !!((_a2 = this.store) == null ? void 0 : _a2.supportsResizeAttachments);
  }
  get effectiveLabel() {
    var _a2;
    return t.sanitize(this.label ?? (((_a2 = this.messages) == null ? void 0 : _a2.attachments) || this.fieldName));
  }
  getCellValue(t5) {
    var _a2, _b;
    return ((_b = (_a2 = t5 == null ? void 0 : t5.item) == null ? void 0 : _a2.attachments) == null ? void 0 : _b.length) ?? 0;
  }
  _createOrSyncThumbnails(t5, e17) {
    const n19 = document.createElement("div"), o8 = this._thumbnailElements.get(t5);
    return (o8 == null ? void 0 : o8.length) && o8.length === e17.length ? o8.forEach((t6) => n19.appendChild(t6)) : this._thumbnailElements.set(t5, this._createThumbnailElements(e17, n19)), n19;
  }
  _createOrSyncShowAttachmentsButton(t5, e17) {
    var _a2;
    const n19 = this._buttonElements.get(t5);
    if (n19) return n19.textContent = "", n19.onclick = (n20) => this._onShowAttachmentsClick({ index: e17, objectId: t5, event: n20 }), n19;
    const o8 = this.createCalciteButton({ alignment: "icon-end-space-between", className: `${h7.showAttachmentsButton} ${n11.contentFull}`, iconEnd: "chevron-right", iconFlipRtl: "both", scale: this.thumbnailIconScale, textContent: "", title: (_a2 = this.messages) == null ? void 0 : _a2.viewAttachments, width: "full", onclick: (n20) => this._onShowAttachmentsClick({ index: e17, objectId: t5, event: n20 }) });
    return this._buttonElements.set(t5, o8), o8;
  }
  _createThumbnailAnchor({ name: t5, url: e17 }, n19) {
    const o8 = document.createElement("a");
    return o8.href = `${e17}`, o8.download = t5, o8.rel = "noreferrer", o8.target = "_blank", o8.appendChild(n19), o8;
  }
  _createThumbnailElements(t5, e17) {
    var _a2;
    const { thumbnailAppearance: n19, thumbnailCount: o8, thumbnailIconScale: s22 } = this, r12 = [], l17 = Math.min(o8, t5.length), c17 = !this.onShowAttachments, h9 = "image" === n19 && !!((_a2 = this.store) == null ? void 0 : _a2.supportsResizeAttachments);
    for (let m11 = 0; m11 < l17; m11++) {
      const n20 = t5[m11], { name: o9, contentType: l18 } = n20, u8 = e8(l18) && h9 ? this._createThumbnailImage(n20) : this.createCalciteIcon({ icon: a7(l18), scale: s22, textLabel: o9 }), p14 = c17 ? this._createThumbnailAnchor(n20, u8) : u8;
      e17 && e17.appendChild(p14), r12.push(p14);
    }
    return r12;
  }
  _createThumbnailImage(t5) {
    var _a2;
    const { name: e17, size: n19, url: o8 } = t5, s22 = document.createElement("img"), a17 = `${o8}${(o8 == null ? void 0 : o8.includes("?")) ? "&" : "?"}${this._supportsResize ? "w=24" : ""}&fs=${n19}`;
    return s22.alt = ((_a2 = this.messages) == null ? void 0 : _a2.attachmentThumbnail) ?? e17, s22.src = a17, s22.title = e17, s22;
  }
  _onShowAttachmentsClick({ index: t5, objectId: e17, event: n19 }) {
    var _a2;
    n19.preventDefault(), n19.stopPropagation(), (_a2 = this.onShowAttachments) == null ? void 0 : _a2.call(this, { index: t5, objectId: e17 });
  }
};
__decorate([m()], m8.prototype, "_showAttachmentsViewEnabled", null), __decorate([m()], m8.prototype, "_buttonElements", void 0), __decorate([m()], m8.prototype, "_supportsResize", null), __decorate([m()], m8.prototype, "_thumbnailElements", void 0), __decorate([m()], m8.prototype, "attachmentsViewEnabled", void 0), __decorate([m()], m8.prototype, "cellValueFormatFunction", void 0), __decorate([m()], m8.prototype, "effectiveLabel", null), __decorate([m()], m8.prototype, "icon", void 0), __decorate([m()], m8.prototype, "layer", void 0), __decorate([m()], m8.prototype, "onShowAttachments", void 0), __decorate([m({ readOnly: true })], m8.prototype, "sortable", void 0), __decorate([m()], m8.prototype, "store", void 0), __decorate([m()], m8.prototype, "textAlign", void 0), __decorate([m()], m8.prototype, "thumbnailAppearance", void 0), __decorate([m()], m8.prototype, "thumbnailCount", void 0), __decorate([m()], m8.prototype, "thumbnailIconScale", void 0), __decorate([m()], m8.prototype, "thumbnailsEnabled", void 0), m8 = __decorate([a3("esri.widgets.FeatureTable.AttachmentsColumn")], m8);
var u5 = m8;

// node_modules/@arcgis/core/layers/support/editableLayers.js
function n14(t5) {
  return "object" == typeof t5 && null != t5 && "loaded" in t5 && true === t5.loaded && "type" in t5;
}
function e15(e17) {
  var _a2, _b;
  return !(!n14(e17) || !((_b = (_a2 = G(e17)) == null ? void 0 : _a2.operations) == null ? void 0 : _b.supportsEditing) || "editingEnabled" in e17 && !$2(e17) || o2(e17));
}

// node_modules/@arcgis/core/widgets/support/dateUtils.js
var S = n2;
var w2 = { date: null, time: null, timeZoneOffset: null };
var g3 = /* @__PURE__ */ new Map([["hours", 3600], ["minutes", 60], ["seconds", 1], ["deciseconds", 0.1], ["centiseconds", 0.01], ["milliseconds", 1e-3]]);
var j3 = "latn";
var Z = (t5) => "valueAsDate" in t5;
function I3(t5) {
  return { locale: l2(), numberingSystem: j3, zone: t5 ?? S };
}
function D(t5, e17) {
  return null == t5 ? null : DateTime.fromMillis(t5, I3(e17));
}
function V2(t5, e17) {
  if (null == t5 || !e6(t5) || "" === t5) {
    if (e17) {
      const t6 = DateTime.local({ zone: e17 });
      return { date: null, time: null, timeZoneOffset: t6.isValid ? t6.offset.toString() : null };
    }
    return w2;
  }
  const n19 = DateTime.fromISO(t5, { setZone: true });
  return { date: n19.toFormat(s9, I3()), time: n19.toFormat(u.HMS_MS, I3()), timeZoneOffset: n19.offset.toString() };
}
function C2(t5, e17) {
  if (null == t5 || !n6(t5) || isNaN(t5)) return w2;
  const n19 = D(t5, e17);
  return n19 ? { date: n19.toFormat(s9, I3(e17)), time: n19.toFormat(f3, I3(e17)) } : w2;
}
function F3(t5) {
  switch (t5.type) {
    case "date":
    default:
      return N2("short-date-short-time");
    case "date-only":
      return N2("short-date");
    case "time-only":
      return N2("short-time");
    case "timestamp-offset":
      return { ...N2("short-date-short-time"), timeZone: void 0, timeZoneName: "short" };
  }
}
function T2(t5, e17, i10) {
  if (!t5 || null == e17) return null;
  switch (t5.type) {
    case "date":
      return b2(e17, i10);
    case "date-only":
      return x(e17, i10);
    case "time-only":
      return P(e17, i10);
    case "timestamp-offset":
      return W(e17, i10);
    default:
      return null;
  }
}
function b6(t5) {
  const { dateComponent: e17, defaultTimeZone: n19, timeComponent: o8, timeZoneComponent: l17, oldValue: r12 } = t5;
  if (!(e17 == null ? void 0 : e17.value)) return null;
  const { year: i10, month: u8, day: a17 } = DateTime.fromFormat(e17.value, s9), { hour: s22, minute: m11, second: c17, millisecond: d6 } = A(o8, n2) ?? DateTime.now(), p14 = V2(r12, n19), O3 = "" === (l17 == null ? void 0 : l17.value) ? null : l17 == null ? void 0 : l17.value, S2 = O3 ? parseInt(O3, 10) : null != p14.timeZoneOffset ? parseInt(p14.timeZoneOffset, 10) : (/* @__PURE__ */ new Date()).getTimezoneOffset(), w4 = FixedOffsetZone.instance(S2), g5 = DateTime.fromObject({ year: i10, month: u8, day: a17, hour: s22, minute: m11, second: c17, millisecond: d6 }, { zone: w4 });
  return g5.isValid ? g5.toISO({ includeOffset: true }) : null;
}
function N3(t5) {
  const { dateComponent: e17, timeComponent: n19, timeZone: o8, max: l17, min: r12, oldValue: i10 } = t5, u8 = A(e17, o8);
  let a17 = !!t5.applyRange;
  if (!u8) return null;
  let s22 = null;
  if (null != (n19 == null ? void 0 : n19.value)) {
    const t6 = A(n19, o8), e18 = Date.now(), r13 = null != l17 && l17 < e18 ? l17 : e18, i11 = DateTime.fromMillis(r13, I3(o8)), a18 = u8 || i11, { year: m12, month: f9, day: c17 } = a18, { hour: d6, minute: p14, second: y6, millisecond: v2 } = t6 || i11;
    s22 = a18.set({ year: m12, month: f9, day: c17, hour: d6, minute: p14, second: y6, millisecond: v2 });
  } else {
    const t6 = D(U3({ value: i10 ?? Date.now(), max: l17, min: r12 }), o8), { year: e18, month: n20, day: m12 } = u8, { hour: f9, minute: c17, second: d6, millisecond: p14 } = t6;
    a17 = true, s22 = u8.set({ year: e18, month: n20, day: m12, hour: f9, minute: c17, second: d6, millisecond: p14 });
  }
  if (!s22.isValid) return null;
  const m11 = s22.toMillis();
  return a17 ? U3({ value: m11, max: l17, min: r12 }) : m11;
}
function A(t5, e17) {
  if (null == (t5 == null ? void 0 : t5.value) || Array.isArray(t5.value)) return null;
  let n19 = null;
  return n19 = Z(t5) ? DateTime.fromFormat(t5.value, s9, I3(e17)) : H(t5.value), (n19 == null ? void 0 : n19.isValid) ? n19 : null;
}
function k2(t5) {
  const { max: e17, min: n19, value: o8 } = t5;
  return !isNaN(o8) && (!(null != e17 && o8 > e17) && !(null != n19 && o8 < n19));
}
function U3(t5) {
  const { max: n19, min: o8, value: l17 } = t5;
  return null != n19 && null != o8 ? r4(l17, o8, n19) : null != n19 && l17 > n19 ? n19 : null != o8 && l17 < o8 ? o8 : l17;
}
function E(e17) {
  if (!e17) return "";
  const n19 = e17.split(".").at(0);
  if (!n19 || n19.length < 1) throw new s2("invalid time-only field", `Cannot parse valid time-only field value from ${e17}`);
  const o8 = n19.split(":"), l17 = new Array(3);
  for (let t5 = 0; t5 < 3; t5++) {
    const e18 = o8.at(t5) ?? "";
    l17[t5] = e18.padStart(2, "0");
  }
  return l17.join(":");
}
function H2(t5, e17) {
  switch (e17) {
    case "date":
      return t5.getTime();
    case "date-only":
      return t5.toISODate();
    case "time-only":
      return E(t5.toISOTime(true, false));
    case "timestamp-offset":
      return t5.toISOString(false);
    default:
      return null;
  }
}
function J(t5) {
  return !!t5 && "object" == typeof t5 && "getTime" in t5 && "toISOString" in t5 && "timeZone" in t5;
}
function R(t5) {
  const { type: e17, range: n19, value: o8 } = t5;
  if (null == o8 || !n19 || o8 === n19.min || o8 === n19.max) return true;
  if ("date" === e17) return k2({ value: o8, min: n19.min, max: n19.max });
  const { rawMax: l17, rawMin: r12 } = n19;
  let i10 = null, m11 = null, f9 = null;
  switch (e17) {
    case "date-only":
      i10 = e7(l17), m11 = e7(r12), f9 = e7(o8);
      break;
    case "time-only":
      i10 = H(l17), m11 = H(r12), f9 = H(o8);
      break;
    case "timestamp-offset":
      i10 = c4(l17), m11 = c4(r12), f9 = c4(o8);
  }
  return !f9 || k2({ value: f9.valueOf(), min: m11 == null ? void 0 : m11.valueOf(), max: i10 == null ? void 0 : i10.valueOf() });
}

// node_modules/@arcgis/core/widgets/support/forms/formUtils.js
var o6 = (t5, n19) => {
  if (!t5) return true;
  const { operations: e17 } = t5;
  switch (n19) {
    case "INSERT":
      return e17.supportsAdd;
    case "UPDATE":
    case "DELETE":
      return e17.supportsUpdate;
    default:
      return true;
  }
};
function i5(t5, o8) {
  const { dataType: i10, domain: u8, field: a17, maxLength: s22, minLength: l17, range: f9, required: p14 } = o8;
  if (null == t5) return p14 ? "input-validation-error::cannot-be-empty" : null;
  if ("text" === i10 && "string" == typeof t5) {
    if (l17 > -1 && t5.length < l17) return "length-validation-error::too-short";
    if (s22 > -1 && t5.length > s22) return "length-validation-error::too-long";
  }
  return "date" !== i10 || R({ type: a17.type, range: f9, value: t5 }) ? u8 ? p(a17, t5, u8) : Ee(a17, t5) : "numeric-range-validation-error::out-of-range";
}
function s13({ dataType: n19, field: e17, input: r12 }) {
  const o8 = -1;
  if ("date" === n19) return o8;
  const i10 = f6(e17.length);
  if (!r12 || !a5(r12)) return i10;
  const u8 = f6(r12.maxLength);
  return u8 === o8 ? i10 : i10 === o8 ? u8 : Math.min(i10, u8);
}
function l13({ dataType: n19, field: e17, input: r12 }) {
  const o8 = -1;
  if ("date" === n19) return o8;
  if (!r12 || !a5(r12)) return o8;
  const i10 = f6(r12.minLength);
  if (i10 < 0) return o8;
  const u8 = f6(e17.length);
  return u8 > o8 && u8 < i10 ? o8 : i10;
}
function f6(t5) {
  return "number" == typeof t5 && t5 > -1 ? t5 : -1;
}
var g4 = (t5) => t5 && !Number.isNaN(t5.valueOf());

// node_modules/@arcgis/core/widgets/FeatureForm/featureFormUtils.js
var Z2 = (e17) => null != e17 && (C3(e17, "combo-box") || C3(e17, "radio-buttons"));
var C3 = (e17, t5) => {
  var _a2;
  return null != e17 && ((_a2 = e17.input) == null ? void 0 : _a2.type) === t5;
};
function k3(t5) {
  const { attributes: r12, layer: n19, label: i10, timeZone: l17 } = t5;
  if (!r12 || "object" != typeof r12) return i10;
  const a17 = Object.keys(r12).filter((e17) => i(i10, e17)), s22 = a17.map((e17) => r12[e17]), u8 = a17.map((e17) => n19.fieldsIndex.get(e17)).filter(N);
  return J2(i10, G4({ fields: u8, layer: n19, timeZone: l17, values: s22 }));
}
function G4(e17) {
  const { layer: t5, values: r12 } = e17, n19 = e17.fields ?? t5.fields ?? [], o8 = e17.timeZone ?? void 0, i10 = n19.map((e18, n20) => {
    let i11 = r12[n20];
    if (e18.domain && "coded-value" === e18.domain.type) {
      const t6 = e18.domain.codedValues.find((e19) => e19.code === i11);
      (t6 == null ? void 0 : t6.name) && (i11 = t6.name);
    }
    return (U2(e18) || xe(e18)) && (i11 = T2(e18, i11, { timeZone: o8, ...H3(t5, e18) })), [e18.name, i11];
  });
  return Object.fromEntries(i10);
}
function H3(e17, t5) {
  var _a2;
  if (I(e17)) {
    const r12 = ((_a2 = e17.getFieldConfiguration(t5.name)) == null ? void 0 : _a2.fieldFormat) ?? M(t5);
    if ("date-time" === (r12 == null ? void 0 : r12.type)) return F(r12);
  }
  return F3(t5);
}
function J2(e17, t5) {
  return r(r(e17, (e18) => `{${e18.toLowerCase()}}`), Object.fromEntries(Object.entries(t5).map(([e18, t6]) => [e18.toLowerCase(), t6])));
}
function Y2(e17, t5) {
  return null == e17 || t5.onValue !== e17 && t5.offValue !== e17;
}

// node_modules/@arcgis/core/widgets/FeatureForm/InputBase.js
var l14 = class extends b3 {
  constructor(t5) {
    super(t5), this.element = null, this.feature = null, this.layer = null, this.timeZone = null, this.type = null, this.visibilityExpressionExecutor = null;
  }
  get description() {
    var _a2;
    return this.getFormattedLabel((_a2 = this.element) == null ? void 0 : _a2.description);
  }
  get evaluatedVisibilityExpression() {
    const { visibilityExpressionExecutor: t5 } = this;
    return null != t5 ? !!t5.lastEvaluatedValue : null;
  }
  get label() {
    var _a2;
    return this.getFormattedLabel((_a2 = this.element) == null ? void 0 : _a2.label);
  }
  get updating() {
    return false;
  }
  get visible() {
    return null != this.evaluatedVisibilityExpression ? this.evaluatedVisibilityExpression : null != this.element;
  }
  getFormattedLabel(t5) {
    const { feature: e17, layer: r12, timeZone: o8 } = this;
    return null == r12 || null == t5 ? t5 : k3({ attributes: e17.attributes, label: t5, layer: r12, timeZone: o8 });
  }
};
__decorate([m({ readOnly: true })], l14.prototype, "description", null), __decorate([m({ constructOnly: true })], l14.prototype, "element", void 0), __decorate([m()], l14.prototype, "evaluatedVisibilityExpression", null), __decorate([m()], l14.prototype, "feature", void 0), __decorate([m()], l14.prototype, "label", null), __decorate([m()], l14.prototype, "layer", void 0), __decorate([m()], l14.prototype, "timeZone", void 0), __decorate([m()], l14.prototype, "type", void 0), __decorate([m()], l14.prototype, "updating", null), __decorate([m()], l14.prototype, "visible", null), __decorate([m()], l14.prototype, "visibilityExpressionExecutor", void 0), l14 = __decorate([a3("esri.widgets.FeatureForm.InputBase")], l14);
var s15 = l14;

// node_modules/@arcgis/core/widgets/FeatureForm/EditableInput.js
var l15 = class extends s15 {
  constructor(t5) {
    super(t5), this.editType = "NA", this.editableExpressionExecutor = null;
  }
  get editable() {
    return !!this.layerAllowsEdits && (this.evaluatedEditableExpression ?? true);
  }
  get evaluatedEditableExpression() {
    const { editableExpressionExecutor: t5 } = this;
    return null != t5 ? !!t5.lastEvaluatedValue : null;
  }
  get layerAllowsEdits() {
    const { layer: t5 } = this;
    if (!t5) return false;
    const e17 = G(t5), r12 = e17 == null ? void 0 : e17.operations.supportsEditing, s22 = o6(e17, this.editType);
    return !(!r12 || !s22);
  }
};
__decorate([m()], l15.prototype, "editType", void 0), __decorate([m()], l15.prototype, "editable", null), __decorate([m()], l15.prototype, "editableExpressionExecutor", void 0), __decorate([m()], l15.prototype, "evaluatedEditableExpression", null), __decorate([m()], l15.prototype, "layerAllowsEdits", null), l15 = __decorate([a3("esri.widgets.FeatureForm.EditableInput")], l15);
var p7 = l15;

// node_modules/@arcgis/core/widgets/FeatureForm/FieldInput.js
var M2 = class extends p7 {
  constructor(e17) {
    super(e17), this._storedValue = null, this.error = null, this.preservesValueWhenHidden = true, this.field = null, this.group = null, this.requiredExpressionExecutor = null, this.type = "field", this._fieldInputWasVisibleDuringLifetime = false, this.valueExpressionExecutor = null;
  }
  initialize() {
    this.addHandles(l(() => this.feature, () => this._fieldInputWasVisibleDuringLifetime = false, U));
  }
  get _dateFormRange() {
    const { element: e17, field: t5 } = this;
    if ("date" !== this.dataType) return {};
    const i10 = (e17 == null ? void 0 : e17.domain) ? y3(t5, e17.domain) : null;
    if (!(e17 == null ? void 0 : e17.input)) return i10 ?? {};
    const l17 = e17.input, { type: n19 } = l17;
    let r12 = {};
    if ("date-picker" !== n19 && "time-picker" !== n19 && "datetimeoffset-picker" !== n19 || (r12 = g(t5, l17.max, l17.min)), "datetime-picker" === n19) {
      const { max: e18, min: t6 } = l17;
      r12 = { max: null != e18 && g4(e18) ? e18.getTime() : null, min: null != t6 && g4(t6) ? t6.getTime() : null };
    }
    const { max: s22, min: o8 } = r12;
    if (i10) {
      const { max: e18, min: t6 } = i10, l18 = n6(e18) && (null == s22 || null != s22 && e18 < s22), n20 = n6(t6) && (null == o8 || null != o8 && t6 > o8);
      return { max: l18 ? e18 : s22 ?? null, min: n20 ? t6 : o8 ?? null, rawMax: l18 ? i10.rawMax : (r12 == null ? void 0 : r12.rawMax) ?? null, rawMin: n20 ? i10.rawMin : (r12 == null ? void 0 : r12.rawMin) ?? null };
    }
    return { min: o8, max: s22, rawMax: (r12 == null ? void 0 : r12.rawMax) ?? null, rawMin: (r12 == null ? void 0 : r12.rawMin) ?? null };
  }
  get _dateRange() {
    const { _dateFormRange: e17, field: t5 } = this;
    if ("date" !== this.dataType) return {};
    const i10 = y3(t5);
    if (!i10) return e17;
    const { max: l17, min: n19, rawMax: r12, rawMin: s22 } = e17;
    if ("date" === t5.type) {
      const { max: e18, min: t6 } = i10;
      return { max: n6(l17) && (null === e18 || null != e18 && l17 < e18) ? l17 : e18 ?? null, min: n6(n19) && (null === t6 || null != t6 && n19 > t6) ? n19 : t6 ?? null };
    }
    if ("date-only" === t5.type || "time-only" === t5.type || "timestamp-offset" === t5.type) {
      const { max: e18, min: t6, rawMax: u8, rawMin: a17 } = i10, o8 = n6(l17) && r12 && (null == e18 || l17 < e18), p14 = n6(n19) && s22 && (null == t6 || n19 > t6);
      return { max: o8 ? l17 : e18, min: p14 ? n19 : t6, rawMax: o8 ? r12 : u8, rawMin: p14 ? s22 : a17 };
    }
    return { max: null, min: null };
  }
  get _configAllowsEdits() {
    var _a2, _b;
    const { element: e17, layer: t5, name: i10 } = this;
    if (null != e17) return e17.editableExpression ? !!this.evaluatedEditableExpression : false !== e17.editable;
    if (t5 == null ? void 0 : t5.userHasUpdateItemPrivileges) return true;
    const l17 = t5 && "popupTemplate" in t5 ? (_b = (_a2 = t5 == null ? void 0 : t5.popupTemplate) == null ? void 0 : _a2.fieldInfos) == null ? void 0 : _b.find(({ fieldName: e18 }) => e18 === i10) : null;
    return (l17 == null ? void 0 : l17.isEditable) ?? true;
  }
  get _layerAndFieldAllowEdits() {
    var _a2;
    return this.layerAllowsEdits && ((_a2 = this.field) == null ? void 0 : _a2.editable);
  }
  get _isVisibleByDefault() {
    const { field: e17, layer: t5 } = this;
    return !!(e17 == null ? void 0 : e17.visible) && K(e17, t5);
  }
  get _isEditTrackingField() {
    var _a2;
    return W2(this.layer).includes((_a2 = this.name) == null ? void 0 : _a2.toLowerCase());
  }
  get _shouldUseValueExpression() {
    return this._layerAndFieldAllowEdits && !this._configAllowsEdits && null != this.valueExpressionExecutor && this.valueExpressionExecutor.initialExecutionComplete;
  }
  get isSubtypeField() {
    var _a2;
    const { layer: e17 } = this;
    if (e17 && "subtypeField" in e17) {
      const { subtypeField: t5, fieldsIndex: i10 } = e17;
      return (((_a2 = i10.get(t5)) == null ? void 0 : _a2.name) ?? t5) === this.name;
    }
    return false;
  }
  get valueIsOutOfDomain() {
    const { domain: e17, value: t5, field: i10 } = this;
    return !(!e17 || null == t5) && null !== p(i10, t5, e17);
  }
  get alias() {
    return e10(this.field, this.layer);
  }
  get dataType() {
    const { field: e17 } = this;
    return ge(e17) ? "number" : Fe(e17) ? "text" : U2(e17) || xe(e17) ? "date" : "unsupported";
  }
  get dateDataType() {
    if ("date" === this.dataType) return "date" !== this.field.type ? "string" : "number";
  }
  get domain() {
    const { layer: e17, feature: t5, name: i10, element: l17 } = this, n19 = l17 == null ? void 0 : l17.domain;
    return null != n19 && this._isDomainCompatible(n19) ? n19 : e17.getFieldDomain(i10, { feature: t5 });
  }
  get editable() {
    return !!this._layerAndFieldAllowEdits && (this.evaluatedEditableExpression ?? this._configAllowsEdits);
  }
  get evaluatedRequiredExpression() {
    const { requiredExpressionExecutor: e17 } = this;
    return null != e17 ? !!e17.lastEvaluatedValue : null;
  }
  get evaluatedValueExpression() {
    const { valueExpressionExecutor: e17 } = this;
    return null != e17 ? e17.lastEvaluatedValue : null;
  }
  get hint() {
    var _a2;
    return (_a2 = this.element) == null ? void 0 : _a2.hint;
  }
  get includeDate() {
    return !("date" !== this.dataType || "time-only" === this.field.type);
  }
  get includeTime() {
    var _a2;
    const { element: e17, field: t5 } = this;
    if ("date" !== this.dataType) return false;
    if ("time-only" === t5.type) return true;
    if ("date-only" === t5.type) return false;
    const i10 = "datetime-picker" === ((_a2 = e17 == null ? void 0 : e17.input) == null ? void 0 : _a2.type) ? e17.input.includeTime : void 0;
    return void 0 === i10 || i10;
  }
  get includeTimeOffset() {
    var _a2;
    if ("timestamp-offset" !== this.field.type) return false;
    const e17 = (_a2 = this.element) == null ? void 0 : _a2.input;
    return !e17 || "datetimeoffset-picker" === e17.type && e17.includeTimeOffset;
  }
  set initialFeature(e17) {
    this._set("initialFeature", e17), this.notifyChange("valid");
  }
  get inputType() {
    var _a2, _b;
    return (_b = (_a2 = this.element) == null ? void 0 : _a2.input) == null ? void 0 : _b.type;
  }
  get hasInvalidSwitchValue() {
    const { element: e17 } = this, t5 = C3(e17, "switch") ? e17.input : null;
    return !t5 || Y2(this.value, t5);
  }
  get isRelationshipKeyField() {
    var _a2;
    const { field: e17, layer: t5 } = this;
    return Y(t5) && !!((_a2 = t5.relationships) == null ? void 0 : _a2.some((t6) => t6.keyField === e17.name));
  }
  get label() {
    var _a2;
    return this.getFormattedLabel((_a2 = this.element) == null ? void 0 : _a2.label) || this.alias || this.field.name;
  }
  get maxLength() {
    var _a2;
    return s13({ dataType: this.dataType, field: this.field, input: (_a2 = this.element) == null ? void 0 : _a2.input });
  }
  get minLength() {
    var _a2;
    return l13({ dataType: this.dataType, field: this.field, input: (_a2 = this.element) == null ? void 0 : _a2.input });
  }
  get name() {
    var _a2;
    return (_a2 = this.field) == null ? void 0 : _a2.name;
  }
  get range() {
    const { domain: e17, element: t5, field: i10 } = this;
    if ("date" === this.dataType) return this._dateRange;
    const l17 = y3(i10, e17) || $e(i10, e17), n19 = (l17 == null ? void 0 : l17.max) === Number.MAX_VALUE ? null : (l17 == null ? void 0 : l17.max) ?? null, r12 = (l17 == null ? void 0 : l17.min) === -Number.MAX_VALUE ? null : (l17 == null ? void 0 : l17.min) ?? null;
    if (!(t5 == null ? void 0 : t5.domain) || "range" !== t5.domain.type) return { max: n19, min: r12 };
    const { max: s22, min: a17 } = y3(i10) || {};
    return { max: null != s22 && (null === n19 || null != n19 && s22 < n19) ? s22 : n19, min: null != a17 && (null === r12 || null != r12 && a17 > r12) ? a17 : r12 };
  }
  get required() {
    var _a2;
    const { editable: e17, evaluatedRequiredExpression: t5, field: i10, visible: l17, isSubtypeField: n19 } = this;
    if (!e17) return false;
    if (false === (i10 == null ? void 0 : i10.nullable)) return true;
    if (n19) return true;
    return !(!l17 || false === ((_a2 = this.group) == null ? void 0 : _a2.visible) || null == t5) && t5;
  }
  set required(e17) {
    this._overrideIfSome("required", e17);
  }
  get showNoValueOptionEnabled() {
    const { element: e17 } = this;
    return !this.required && (!Z2(e17) || e17.input.showNoValueOption);
  }
  get showNoValueLabel() {
    const { element: e17 } = this;
    return Z2(e17) ? e17.input.noValueOptionLabel : null;
  }
  get submittable() {
    const { field: e17, required: t5, valid: i10, value: l17 } = this;
    return (!t5 || null != l17) && (!!i10 || this.initialFeature.getAttribute(e17.name) === l17);
  }
  get timeResolution() {
    var _a2;
    const e17 = (_a2 = this.element) == null ? void 0 : _a2.input;
    if (e17 && ("datetimeoffset-picker" === e17.type || "time-picker" === e17.type)) return e17.timeResolution ?? "minutes";
  }
  get timeStep() {
    return null != this.timeResolution ? g3.get(this.timeResolution) : void 0;
  }
  get updating() {
    const { editableExpressionExecutor: e17, valueExpressionExecutor: t5, visibilityExpressionExecutor: i10, preservesValueWhenHidden: l17 } = this;
    return null != t5 && t5.updating || null != e17 && e17.updating || false === l17 && null != i10 && i10.updating;
  }
  get valid() {
    const e17 = this.editable ? this._validate() : null;
    return this._set("error", e17), null === e17;
  }
  get value() {
    var _a2, _b, _c, _d;
    if (this._fieldInputWasVisibleDuringLifetime = this._fieldInputWasVisibleDuringLifetime || this.visible && false !== ((_a2 = this.group) == null ? void 0 : _a2.visible), false === this.preservesValueWhenHidden && this._fieldInputWasVisibleDuringLifetime && (false === ((_b = this.visibilityExpressionExecutor) == null ? void 0 : _b.lastEvaluatedValue) || false === ((_d = (_c = this.group) == null ? void 0 : _c.visibilityExpressionExecutor) == null ? void 0 : _d.lastEvaluatedValue))) return null !== this._storedValue && this.set("_storedValue", null), null;
    if (this._shouldUseValueExpression) {
      const e17 = this.evaluatedValueExpression;
      return "date" === this.dataType ? this._arcadeOutputToDateFieldValue(e17) : null != e17 && "object" == typeof e17 ? `${e17}` : e17;
    }
    return this._storedValue;
  }
  set value(e17) {
    this.notifyChange("evaluatedVisibilityExpression"), this.set("_storedValue", e17), this.notifyChange("value"), this.notifyChange("valid");
  }
  get visible() {
    var _a2;
    return !this._isEditTrackingField && (null != this.evaluatedVisibilityExpression ? this.evaluatedVisibilityExpression : null != this.element && false !== ((_a2 = this.field) == null ? void 0 : _a2.visible) || this._isVisibleByDefault);
  }
  _arcadeOutputToDateFieldValue(e17) {
    const i10 = this.field.type;
    try {
      if ("object" == typeof e17) {
        if (null === e17) return null;
        if (J(e17)) return H2(e17, i10);
        if ("getTime" in e17 && "function" == typeof e17.getTime && "date" === i10) return parseInt(e17.getTime(), 10);
        if ("date-only" === i10 || "time-only" === i10 || "timestamp-offset" === i10) return e17.toString();
      } else {
        if ("string" == typeof e17) return "date" === i10 ? parseInt(e17, 10) : e17;
        if ("number" == typeof e17 && "date" === i10) return e17;
      }
      throw new s2("feature-form:invalid-date-value", "Date value is invalid");
    } catch {
      return "date" === i10 ? NaN : "";
    }
  }
  _isDomainCompatible(e17) {
    const { field: t5 } = this;
    if ("coded-value" === (e17 == null ? void 0 : e17.type)) {
      const i10 = typeof e17.codedValues[0].code;
      if ("string" === i10 && Fe(t5) || "number" === i10 && ge(t5)) return true;
    }
    return !!("range" === (e17 == null ? void 0 : e17.type) && ge(t5) || U2(t5) || xe(t5));
  }
  _validate() {
    const { dataType: e17, domain: t5, field: i10, minLength: l17, range: n19, required: r12, value: s22, maxLength: u8 } = this;
    return i5(s22, { dataType: e17, domain: t5, field: i10, maxLength: u8, minLength: l17, range: n19, required: r12 });
  }
};
__decorate([m()], M2.prototype, "_dateFormRange", null), __decorate([m()], M2.prototype, "_dateRange", null), __decorate([m()], M2.prototype, "_storedValue", void 0), __decorate([m()], M2.prototype, "_configAllowsEdits", null), __decorate([m()], M2.prototype, "_layerAndFieldAllowEdits", null), __decorate([m()], M2.prototype, "_isVisibleByDefault", null), __decorate([m()], M2.prototype, "_isEditTrackingField", null), __decorate([m()], M2.prototype, "_shouldUseValueExpression", null), __decorate([m()], M2.prototype, "isSubtypeField", null), __decorate([m()], M2.prototype, "valueIsOutOfDomain", null), __decorate([m()], M2.prototype, "alias", null), __decorate([m()], M2.prototype, "dataType", null), __decorate([m()], M2.prototype, "dateDataType", null), __decorate([m()], M2.prototype, "domain", null), __decorate([m()], M2.prototype, "editable", null), __decorate([m({ readOnly: true })], M2.prototype, "error", void 0), __decorate([m({ constructOnly: true })], M2.prototype, "preservesValueWhenHidden", void 0), __decorate([m()], M2.prototype, "evaluatedRequiredExpression", null), __decorate([m()], M2.prototype, "evaluatedValueExpression", null), __decorate([m()], M2.prototype, "field", void 0), __decorate([m()], M2.prototype, "group", void 0), __decorate([m({ readOnly: true })], M2.prototype, "hint", null), __decorate([m()], M2.prototype, "includeDate", null), __decorate([m()], M2.prototype, "includeTime", null), __decorate([m()], M2.prototype, "includeTimeOffset", null), __decorate([m()], M2.prototype, "initialFeature", null), __decorate([m({ readOnly: true })], M2.prototype, "inputType", null), __decorate([m()], M2.prototype, "hasInvalidSwitchValue", null), __decorate([m()], M2.prototype, "isRelationshipKeyField", null), __decorate([m()], M2.prototype, "label", null), __decorate([m()], M2.prototype, "maxLength", null), __decorate([m()], M2.prototype, "minLength", null), __decorate([m({ readOnly: true })], M2.prototype, "name", null), __decorate([m()], M2.prototype, "range", null), __decorate([m()], M2.prototype, "required", null), __decorate([m()], M2.prototype, "requiredExpressionExecutor", void 0), __decorate([m()], M2.prototype, "showNoValueOptionEnabled", null), __decorate([m()], M2.prototype, "submittable", null), __decorate([m()], M2.prototype, "timeResolution", null), __decorate([m()], M2.prototype, "timeStep", null), __decorate([m({ readOnly: true })], M2.prototype, "type", void 0), __decorate([m()], M2.prototype, "updating", null), __decorate([m()], M2.prototype, "valid", null), __decorate([m({ value: null })], M2.prototype, "value", null), __decorate([m()], M2.prototype, "valueExpressionExecutor", void 0), __decorate([m()], M2.prototype, "visible", null), M2 = __decorate([a3("esri.widgets.FeatureForm.FieldInput")], M2);
var O2 = M2;

// node_modules/@arcgis/core/widgets/FeatureTable/Grid/EditorColumn.js
var a10 = { showInput: "Enter", cancelEdit: "Escape" };
var s16 = Symbol();
var c13 = class extends f5 {
  constructor(t5) {
    super(t5), this.editInfo = null, this.cellValueValidatorFunction = ({ oldValue: t6, value: e17 }) => t6 !== e17, this.editable = true, this.headerRenderFunction = (t6) => {
      const { root: e17 } = t6, o8 = this.createHeaderContent();
      this.tableEditingEnabled && !this.effectiveEditable && o8.prepend(this._lockIconNode), this.removeCellContent(e17), e17.appendChild(o8);
    }, this.inputRenderFunction = ({ root: t6, column: e17, rowData: o8 }) => {
      if (this.editInfo || !this.effectiveEditable || !o8) return;
      const n19 = this.getCellValue(o8), i10 = this.createInputElement({ value: n19 });
      this._set("editInfo", { column: e17, inputs: [i10], root: t6, rowData: o8, oldValue: n19 });
      const l17 = document.createElement("div");
      l17.appendChild(i10), this.removeCellContent(t6), t6.appendChild(l17), i10.focus(), i10 instanceof HTMLInputElement && i10.select();
    }, this.loadingMessage = "", this.inputType = "text", this.maxLength = null, this.onShowPromptCallback = () => {
    }, this.parseInputValueFunction = ({ inputs: t6 }) => (t6 == null ? void 0 : t6.length) || 1 !== t6.length || Array.isArray(t6[0].value) ? null : t6[0].value, this.renderFunction = (t6) => {
      var _a2;
      const { root: e17, rowData: o8 } = t6, { editInfo: n19 } = this;
      if (n19) return;
      const i10 = this.getCellValue(o8), l17 = this.cellValueFormatFunction({ root: e17, rowData: o8, value: i10 });
      let s22 = null;
      e17.onclick = () => e17.focus(), e17.ondblclick = () => this.inputRenderFunction(t6), e17.ontouchend = () => this.inputRenderFunction(t6);
      const c17 = (_a2 = this.grid) == null ? void 0 : _a2.getSlotElementByName(e17.slot), u8 = c17 == null ? void 0 : c17.parentElement;
      u8 && !u8.onkeydown && (u8.onkeydown = (e18) => {
        e18.key !== a10.showInput || this.editInfo || this.inputRenderFunction(t6), e18.key === a10.cancelEdit && this.editInfo && this.cancel();
      }), null != i10 && null != l17 ? e17.title = l17.toString() : e17.title && e17.removeAttribute("title"), s22 = l17 instanceof HTMLElement ? l17 : r8(this.messagesURIUtils, l17), this.removeCellContent(e17), s22 instanceof HTMLElement ? e17.appendChild(s22) : null != s22 && (e17.innerHTML = s22.toString());
    }, this.required = false, this.tableEditingEnabled = false;
  }
  get _lockIconNode() {
    var _a2;
    return this.createCalciteIcon({ icon: "lock", textLabel: (_a2 = this.messages) == null ? void 0 : _a2.editingPreventedColumn });
  }
  get effectiveEditable() {
    return this.tableEditingEnabled && this.editable;
  }
  get effectiveRequired() {
    return this.required;
  }
  get editing() {
    return !!this.editInfo;
  }
  get shouldShowPrompt() {
    return false;
  }
  cancel() {
    this.onEditComplete();
  }
  createCalciteOption(t5, e17, o8 = false) {
    const n19 = document.createElement("calcite-option");
    return n19.label = t5, n19.value = `${e17}`, n19.selected = o8, n19;
  }
  createCalciteSelect(t5, e17) {
    const { effectiveRequired: o8, messages: n19 } = this;
    let i10 = false;
    const l17 = e17.map(({ name: e18, value: o9 }) => {
      const n20 = o9 === t5;
      return n20 && (i10 = true), this.createCalciteOption(e18, o9, n20);
    });
    if (null != t5 && "" !== t5 && !i10) {
      const e18 = t5.toString(), o9 = this.createCalciteOption(e18, e18);
      l17.unshift(o9);
    }
    o8 || l17.unshift(this.createCalciteOption(`<${n19 == null ? void 0 : n19.noValue}>`, ""));
    const r12 = document.createElement("calcite-select");
    return l17.forEach((t6) => r12.appendChild(t6)), r12;
  }
  createInputElement({ value: t5 }) {
    const { effectiveRequired: n19, maxLength: i10 } = this, l17 = document.createElement("calcite-input");
    return null != i10 && (l17.maxLength = i10), l17.required = n19, l17.value = null != t5 ? t5.toString() : "", this.addHandles([a2(() => l17, "calciteInputChange", () => this.onInputBlur(), U), a2(() => l17, "keydown", (t6) => {
      t6.key === a10.cancelEdit && this.onInputBlur(true);
    }, U), a2(() => l17, "blur", () => this.onInputBlur(), U)], s16), l17;
  }
  createDateComponent() {
    const t5 = document.createElement("calcite-input-date-picker");
    return t5.focusTrapDisabled = true, t5.overlayPositioning = "fixed", t5;
  }
  createTimeComponent() {
    const t5 = document.createElement("calcite-input-time-picker");
    return t5.overlayPositioning = "fixed", t5.placement = "auto-start", t5.step = 1, t5;
  }
  createTimeZoneComponent() {
    const t5 = document.createElement("calcite-input-time-zone");
    return t5.maxItems = 4, t5.overlayPositioning = "fixed", t5;
  }
  onEditComplete() {
    var _a2, _b;
    this._set("editInfo", null), (_a2 = this.grid) == null ? void 0 : _a2.generateCellPartNames(), (_b = this.grid) == null ? void 0 : _b.requestContentUpdate();
  }
  onInputBlur(t5 = false) {
    this.removeHandles(s16), t5 ? this.cancel() : this.submit();
  }
  async submit() {
    const { editInfo: t5 } = this;
    if (!t5) return void this.cancel();
    const { inputs: e17, rowData: o8, oldValue: n19 } = t5, i10 = this.parseInputValueFunction({ inputs: e17 });
    if (!this.cellValueValidatorFunction({ value: i10, oldValue: n19 })) return void this.cancel();
    const { root: l17 } = t5, r12 = o8.item.objectId, { fieldName: a17 } = this;
    l17.textContent = this.loadingMessage, this.shouldShowPrompt ? this.onShowPromptCallback({ column: this, objectId: r12, oldValue: n19, value: i10 }) : await this.updateItems({ objectId: r12, updates: [{ fieldName: a17, value: i10 }] });
  }
  async updateItems(t5) {
    const { store: e17 } = this;
    if (e17) try {
      await e17.updateItem(t5), this.onEditComplete();
    } catch {
      this.cancel();
    }
    else this.cancel();
  }
};
__decorate([m()], c13.prototype, "_lockIconNode", null), __decorate([m({ readOnly: true })], c13.prototype, "editInfo", void 0), __decorate([m()], c13.prototype, "cellValueValidatorFunction", void 0), __decorate([m()], c13.prototype, "editable", void 0), __decorate([m()], c13.prototype, "effectiveEditable", null), __decorate([m()], c13.prototype, "effectiveRequired", null), __decorate([m()], c13.prototype, "editing", null), __decorate([m()], c13.prototype, "headerRenderFunction", void 0), __decorate([m()], c13.prototype, "inputRenderFunction", void 0), __decorate([m({ constructOnly: true })], c13.prototype, "loadingMessage", void 0), __decorate([m()], c13.prototype, "inputType", void 0), __decorate([m()], c13.prototype, "maxLength", void 0), __decorate([m()], c13.prototype, "onShowPromptCallback", void 0), __decorate([m()], c13.prototype, "parseInputValueFunction", void 0), __decorate([m()], c13.prototype, "renderFunction", void 0), __decorate([m()], c13.prototype, "required", void 0), __decorate([m()], c13.prototype, "shouldShowPrompt", null), __decorate([m()], c13.prototype, "tableEditingEnabled", void 0), c13 = __decorate([a3("esri.widgets.FeatureTable.Grid.EditorColumn")], c13);
var u6 = c13;

// node_modules/@arcgis/core/widgets/FeatureTable/FieldColumn.js
var H4 = { cancelEdit: "Escape" };
var z2 = Symbol();
var P3 = "esri-column";
var M3 = { input: `${P3}__cell-input`, inputContainer: `${P3}__cell__input-container` };
var W3 = class extends u6 {
  constructor(e17) {
    super(e17), this._activeFieldInput = null, this.cellValueFormatFunction = ({ root: e18, rowData: t5, value: i10 }) => {
      var _a2;
      const { formatFunction: n19 } = this;
      if (n19 && t5) {
        const { index: o9, item: { attachments: l18, feature: r12, relatedRecords: a17 } } = t5;
        return n19({ attachments: l18, column: this, feature: r12, field: this.field, index: o9, relatedRecords: a17, value: this.sanitizeContent(i10), virtualIndex: this.getVirtualRowIndex(e18) });
      }
      if (null === i10) return "&nbsp;";
      const o8 = this._getDomainForFeature((_a2 = t5 == null ? void 0 : t5.item) == null ? void 0 : _a2.feature), l17 = this._getComputedDomain(i10, o8) || i10;
      return this._isAnyDateOrTimeField && "coded-value" !== (o8 == null ? void 0 : o8.type) ? this._formatDateValueForDisplay(this.field, l17) : this._isNumericField && "coded-value" !== (o8 == null ? void 0 : o8.type) ? c(parseFloat(l17), this._numberFormatOptions) : this.sanitizeContent(l17);
    }, this.cellValueValidatorFunction = ({ oldValue: e18, value: i10 }) => {
      const { _effectiveRange: n19, field: o8 } = this, { max: l17, min: r12 } = n19;
      if (this.effectiveRequired && (null == i10 || "" === i10)) return false;
      if (this._isNumericField && (Ee(o8, i10) || !k2({ max: l17, min: r12, value: i10 }))) return i2.getLogger(this).warn("value-exceeds-valid-range", "Field value exceeds valid range. Attempted edit was rejected."), false;
      if (this._isAnyDateOrTimeField) {
        const e19 = o8.type;
        if (!R({ type: e19, range: n19, value: i10 })) return false;
      }
      return e18 !== i10;
    }, this.field = null, this.formatFunction = null, this.inputRenderFunction = async ({ root: e18, column: i10, rowData: n19 }) => {
      var _a2, _b;
      if (this.editInfo || !this.effectiveEditable) return;
      const o8 = this.getCellValue(n19), { field: l17 } = this;
      if ("big-integer" === l17.type && Ee(l17, o8)) return void i2.getLogger(this).warn("value-exceeds-valid-range", "Field value is beyond supported limit. Editing is disabled for this field value.");
      const r12 = n19 == null ? void 0 : n19.item.feature;
      if (!r12) return;
      this._activeFieldInput = this._setUpFieldInput(r12, o8);
      const a17 = document.createElement("div");
      a17.classList.add(M3.inputContainer);
      const s22 = [], u8 = "coded-value" === ((_a2 = this._effectiveDomain) == null ? void 0 : _a2.type);
      if (this._isAnyDateOrTimeField && !u8) {
        const e19 = document.createElement("div"), t5 = this._setUpDateComponents(o8), i11 = this._setUpDateActionBar();
        t5.forEach((t6) => e19.appendChild(t6)), a17.appendChild(e19), a17.appendChild(i11), s22.push(...t5);
      } else {
        const e19 = this.createInputElement({ value: o8 });
        a17.appendChild(e19), s22.push(e19);
      }
      this._set("editInfo", { column: i10, inputs: s22, root: e18, rowData: n19, oldValue: o8 }), this.removeCellContent(e18), e18.appendChild(a17), this._syncRowEditingState(e18, true), (_b = this.grid) == null ? void 0 : _b.generateCellPartNames();
      const p14 = s22[0];
      await j(p14), "selectText" in p14 && await p14.selectText();
    }, this.layer = null, this.parseInputValueFunction = ({ inputs: e18 }) => {
      var _a2;
      const { editInfo: t5, field: i10, includeTime: n19 } = this;
      if (!t5 || !e18.length) return null;
      const o8 = e18[0];
      if (this._isAnyDateOrTimeField) {
        if ("coded-value" === ((_a2 = this._effectiveDomain) == null ? void 0 : _a2.type)) {
          if (null == o8.value || "" === o8.value) return null;
          const e19 = String(o8.value);
          return Ie(i10) ? parseFloat(e19) : e19;
        }
        const { timeZone: l18 } = this.effectiveTimeZoneOptions, r13 = t5.oldValue ?? void 0;
        switch (i10.type) {
          case "date-only": {
            const e19 = o8.value;
            return "" !== e19 ? e19 : null;
          }
          case "time-only": {
            const e19 = E(o8.value);
            return "" !== e19 ? e19 : null;
          }
          case "timestamp-offset": {
            const t6 = n19 ? e18[1] : void 0, i11 = e18.at(-1);
            return b6({ oldValue: r13, dateComponent: o8, timeComponent: t6, timeZoneComponent: i11, defaultTimeZone: l18 });
          }
          case "date": {
            const { max: t6, min: i11 } = this._effectiveRange;
            return N3({ oldValue: r13, dateComponent: o8, timeComponent: e18[1], timeZone: l18, max: t6, min: i11 });
          }
        }
        return null;
      }
      const l17 = o8.value, { effectiveRequired: r12 } = this;
      return r12 || null != l17 && "" !== l17 ? this._isNumericField ? parseFloat(l17) : this._isStringField ? l17.trim() : l17 : null;
    }, this.sortable = true, this.store = null, this.template = null, this.view = null;
  }
  get _effectiveDomain() {
    var _a2;
    const e17 = (_a2 = this._activeFieldInput) == null ? void 0 : _a2.feature;
    return e17 ? this._getDomainForFeature(e17) : null;
  }
  get _effectiveRange() {
    var _a2;
    return ((_a2 = this._activeFieldInput) == null ? void 0 : _a2.range) ?? { max: null, min: null };
  }
  get _isAnyDateOrTimeField() {
    const { field: e17 } = this;
    return U2(e17) || xe(e17);
  }
  get _isIntegerField() {
    return ye(this.field);
  }
  get _isNumericField() {
    return ge(this.field);
  }
  get _isStringField() {
    return Fe(this.field);
  }
  get _isSubtypeField() {
    var _a2;
    const { layer: e17 } = this;
    if (e17 == null ? void 0 : e17.subtypeField) {
      const { subtypeField: t5, fieldsIndex: i10 } = e17;
      return (((_a2 = i10.get(t5)) == null ? void 0 : _a2.name) ?? t5) === this.fieldName;
    }
    return false;
  }
  get _layerWithFieldConfigurations() {
    const { layer: e17 } = this;
    return u4(e17) ? e17 : null;
  }
  get _dateTimeFormatOptions() {
    var _a2;
    const { layerFieldConfigurationFormat: e17, template: t5 } = this;
    return ((_a2 = t5 == null ? void 0 : t5.format) == null ? void 0 : _a2.dateFormat) ? N2(t5.format.dateFormat) : (t5 == null ? void 0 : t5.input) && "includeTime" in t5.input && false === t5.input.includeTime ? C : "date-time" === (e17 == null ? void 0 : e17.type) ? F(e17) : this._isAnyDateOrTimeField ? k : void 0;
  }
  get _numberFormatOptions() {
    const { layerFieldConfigurationFormat: e17, template: t5 } = this;
    return (t5 == null ? void 0 : t5.format) ? l3(t5.format) : "number" === (e17 == null ? void 0 : e17.type) ? s4(e17) : this._isNumericField ? { useGrouping: true, maximumFractionDigits: 20 } : void 0;
  }
  get alias() {
    return e10(this.field, this._layerWithFieldConfigurations);
  }
  get defaultValue() {
    var _a2;
    return (_a2 = this.field) == null ? void 0 : _a2.defaultValue;
  }
  get effectiveDescription() {
    const { description: e17, field: t5 } = this, i10 = t5.description, n19 = h5(e17) ? h5(i10) ? null : i10 : e17;
    return null == n19 ? null : this.sanitizeLabel(n19);
  }
  get effectiveEditable() {
    var _a2;
    const { editable: e17, field: t5, layer: i10, tableEditingEnabled: n19 } = this;
    if (!t5 || !i10) return false;
    const o8 = e15(i10), l17 = i10.effectiveCapabilities ?? i10.capabilities, r12 = (_a2 = l17 == null ? void 0 : l17.operations) == null ? void 0 : _a2.supportsUpdate;
    return !!(t5.editable && o8 && r12 && "oid" !== t5.type && n19) && e17;
  }
  get effectiveLabel() {
    return this.sanitizeLabel(this.label || this.alias || this.fieldName);
  }
  get effectiveRequired() {
    var _a2;
    return !this.nullable || this._isSubtypeField || ((_a2 = this._activeFieldInput) == null ? void 0 : _a2.required) || this.required;
  }
  get effectiveSortable() {
    var _a2, _b, _c;
    return !(!((_c = (_b = (_a2 = this.layer) == null ? void 0 : _a2.capabilities) == null ? void 0 : _b.query) == null ? void 0 : _c.supportsOrderBy) || !this.sortable);
  }
  get effectiveTimeZoneOptions() {
    const { layer: e17, field: t5 } = this;
    if (!e17 || "date" !== (t5 == null ? void 0 : t5.type) && "timestamp-offset" !== (t5 == null ? void 0 : t5.type)) return { timeZone: void 0, timeZoneName: void 0 };
    const { tableTimeZone: i10, timeZone: n19, view: o8 } = this;
    return h3(e17.preferredTimeZone || null, !!e17.datesInUnknownTimezone, n19 ?? i10 ?? (o8 == null ? void 0 : o8.timeZone) ?? e2, this._dateTimeFormatOptions ?? k, t5.type);
  }
  get includeTime() {
    const { field: e17, template: t5 } = this;
    return "time-only" === e17.type || "date-only" !== e17.type && (!(t5 == null ? void 0 : t5.input) || "includeTime" in t5.input && false !== t5.input.includeTime);
  }
  get layerFieldConfigurationFormat() {
    var _a2;
    const { _layerWithFieldConfigurations: e17 } = this;
    if (e17) return ((_a2 = e17.getFieldConfiguration(this.fieldName)) == null ? void 0 : _a2.fieldFormat) ?? M(this.field);
  }
  get loadingMessage() {
    var _a2;
    return ((_a2 = this.messages) == null ? void 0 : _a2.loading) || "...";
  }
  get maxLength() {
    const { field: e17, template: t5 } = this, i10 = (e17 == null ? void 0 : e17.length) ?? -1, n19 = (t5 == null ? void 0 : t5.input) && "maxLength" in t5.input ? t5.input.maxLength : null;
    return null != n19 && !isNaN(n19) && n19 >= -1 && (-1 === i10 || n19 <= i10) ? n19 : i10;
  }
  get minLength() {
    const { template: e17, maxLength: t5 } = this, i10 = (e17 == null ? void 0 : e17.input) && "minLength" in e17.input ? e17.input.minLength : null;
    return t5 && i10 <= t5 ? i10 : null;
  }
  get name() {
    var _a2;
    return (_a2 = this.field) == null ? void 0 : _a2.name;
  }
  get nullable() {
    return false !== this.field.nullable;
  }
  get shouldShowPrompt() {
    return this._isSubtypeField;
  }
  createInputElement({ value: e17 }) {
    const { _effectiveDomain: t5, maxLength: o8, minLength: l17, effectiveRequired: r12 } = this;
    let a17;
    if ("coded-value" === (t5 == null ? void 0 : t5.type)) a17 = this.createCalciteSelect(e17, t5.codedValues.map(({ code: e18, name: t6 }) => ({ value: e18, name: t6 }))), this.addHandles([a2(() => a17, "calciteSelectChange", () => this.onInputBlur(), U)], z2);
    else if (this._isNumericField) {
      const { _effectiveRange: { max: t6, min: i10 } } = this;
      a17 = document.createElement("calcite-input"), a17.type = "number", this._isIntegerField && (a17.step = 1), null != t6 && (a17.max = t6), null != i10 && (a17.min = i10), a17.status = k2({ max: t6, min: i10, value: e17 }) ? "idle" : "invalid";
    } else a17 = document.createElement("calcite-input"), this.addHandles([a2(() => a17, "calciteInputChange", () => this.onInputBlur(), U)], z2), o8 > -1 && (a17.maxLength = o8), l17 > 0 && (a17.minLength = l17);
    return a17.classList.add(M3.input), a17.required = r12, a17.value = null != e17 ? e17.toString() : "", this.addHandles([a2(() => a17, "keydown", (e18) => {
      e18.key === H4.cancelEdit && this.onInputBlur(true);
    }, U), a2(() => a17, "blur", () => this.onInputBlur(), U)], z2), a17;
  }
  onInputBlur(e17 = false) {
    this.removeHandles(z2), super.onInputBlur(e17);
  }
  onEditComplete() {
    var _a2;
    const e17 = (_a2 = this.editInfo) == null ? void 0 : _a2.root;
    e17 && this._syncRowEditingState(e17, false), this._activeFieldInput = null, super.onEditComplete();
  }
  get test() {
  }
  _clearActiveEditValues() {
    var _a2, _b;
    (_b = (_a2 = this.editInfo) == null ? void 0 : _a2.inputs) == null ? void 0 : _b.forEach((e17) => e17.value = "");
  }
  _setUpDateActionBar() {
    const { messagesCommon: e17 } = this, t5 = (e17 == null ? void 0 : e17.cancel) ?? "", i10 = (e17 == null ? void 0 : e17.clear) ?? "", n19 = (e17 == null ? void 0 : e17.save) ?? "", o8 = document.createElement("calcite-action-bar");
    return o8.expandDisabled = true, o8.layout = "horizontal", o8.appendChild(this.createCalciteAction({ text: n19, icon: "save", onclick: () => this.submit() })), this.effectiveRequired || o8.appendChild(this.createCalciteAction({ text: i10, icon: "trash", onclick: () => {
      this._clearActiveEditValues(), this.submit();
    } })), o8.appendChild(this.createCalciteAction({ text: t5, icon: "x", onclick: () => this.cancel() })), o8;
  }
  _formatDateValueForDisplay(e17, t5) {
    const { timeZone: i10, timeZoneName: n19 } = this.effectiveTimeZoneOptions;
    return null != t5 ? T2(e17, t5, { ...this._dateTimeFormatOptions, timeZone: i10, timeZoneName: n19 }) : null;
  }
  _setUpDateComponents(e17) {
    const { _effectiveRange: { max: t5, min: n19, rawMax: o8, rawMin: l17 }, field: r12, effectiveRequired: a17 } = this, s22 = this._getDateFieldValuesForComponents(r12, e17), u8 = [];
    if (U2(r12)) {
      const e18 = this.createDateComponent(), p14 = be(r12) || we(r12) ? o8 : t5, d6 = be(r12) || we(r12) ? l17 : n19, m11 = this._getDateFieldValuesForComponents(r12, p14 ?? null), c17 = this._getDateFieldValuesForComponents(r12, d6 ?? null);
      e18.value = s22.date ?? "", e18.max = m11.date ?? "", e18.min = c17.date ?? "", e18.required = a17, this.addHandles([a2(() => e18, "calciteInputDatePickerOpen", () => this._onDateComponentOpen(e18))], z2), u8.push(e18);
    }
    if (this.includeTime) {
      const e18 = this.createTimeComponent();
      e18.value = s22.time ?? "", e18.required = a17, this.addHandles([a2(() => e18, "calciteInputTimePickerOpen", () => this._onDateComponentOpen(e18))], z2), u8.push(e18);
    }
    if ("timestamp-offset" === r12.type) {
      const e18 = this.createTimeZoneComponent();
      e18.value = s22.timeZoneOffset ?? "0", this.addHandles([a2(() => e18, "calciteInputTimeZoneOpen", () => this._onDateComponentOpen(e18))], z2), u8.push(e18);
    }
    return u8;
  }
  _onDateComponentOpen(e17) {
    var _a2;
    (_a2 = this.editInfo) == null ? void 0 : _a2.inputs.forEach((t5) => {
      t5 !== e17 && "open" in t5 && (t5.open = false);
    });
  }
  _syncRowEditingState(e17, t5 = false) {
    var _a2;
    const i10 = (_a2 = this.grid) == null ? void 0 : _a2.getRowContainingNode(e17);
    i10 && (t5 ? i10.setAttribute("editing", "true") : i10.removeAttribute("editing"));
  }
  _getDateFieldValuesForComponents(e17, t5) {
    switch (e17.type) {
      case "date":
        return C2(t5, this.effectiveTimeZoneOptions.timeZone);
      case "date-only":
        return { date: t5 };
      case "time-only":
        return { time: t5 };
      case "timestamp-offset":
        return V2(t5);
      default:
        return {};
    }
  }
  _setUpFieldInput(e17, t5) {
    const { field: i10, layer: n19, effectiveTimeZoneOptions: { timeZone: o8 } } = this, l17 = new O2({ feature: e17, field: i10, initialFeature: e17.clone(), layer: n19, timeZone: o8 });
    return l17.set("value", t5), l17;
  }
  _isDomainCompatible(e17) {
    if (!e17) return false;
    const { _isNumericField: t5, _isStringField: i10 } = this, { type: n19 } = e17;
    if ("coded-value" === n19) {
      const n20 = typeof e17.codedValues[0].code;
      if ("string" === n20 && i10 || "number" === n20 && t5) return true;
    }
    return !("range" !== n19 || !t5);
  }
  _getDomainForFeature(e17) {
    var _a2;
    const { fieldName: t5, layer: i10, template: n19 } = this;
    if (!e17 || !(i10 == null ? void 0 : i10.getFieldDomain)) return null;
    if ("wfs" === i10.type || "geojson" === i10.type || "csv" === i10.type || "knowledge-graph-sublayer" === i10.type) return null;
    if ((n19 == null ? void 0 : n19.domain) && this._isDomainCompatible(n19.domain)) return n19.domain;
    if ("feature" !== i10.type && "subtype-group" !== i10.type && null == ((_a2 = i10.getField(t5)) == null ? void 0 : _a2.domain)) {
      const e18 = V(i10, t5);
      if (e18) return e18;
    }
    return i10.getFieldDomain(t5, { feature: e17 });
  }
  _getComputedDomain(e17, t5) {
    if (!t5) return null;
    if ("range" === t5.type) return e17;
    if ("coded-value" === t5.type) {
      const i10 = t5.codedValues.filter((t6) => t6.hasOwnProperty("code") && t6.code === e17);
      return i10 && i10.length ? i10[0].name : e17;
    }
    return null;
  }
};
__decorate([m()], W3.prototype, "_effectiveDomain", null), __decorate([m()], W3.prototype, "_activeFieldInput", void 0), __decorate([m()], W3.prototype, "_effectiveRange", null), __decorate([m()], W3.prototype, "_isAnyDateOrTimeField", null), __decorate([m()], W3.prototype, "_isIntegerField", null), __decorate([m()], W3.prototype, "_isNumericField", null), __decorate([m()], W3.prototype, "_isStringField", null), __decorate([m()], W3.prototype, "_isSubtypeField", null), __decorate([m()], W3.prototype, "_layerWithFieldConfigurations", null), __decorate([m()], W3.prototype, "_dateTimeFormatOptions", null), __decorate([m()], W3.prototype, "_numberFormatOptions", null), __decorate([m({ readOnly: true })], W3.prototype, "alias", null), __decorate([m()], W3.prototype, "cellValueFormatFunction", void 0), __decorate([m()], W3.prototype, "cellValueValidatorFunction", void 0), __decorate([m({ readOnly: true })], W3.prototype, "defaultValue", null), __decorate([m()], W3.prototype, "effectiveDescription", null), __decorate([m()], W3.prototype, "effectiveEditable", null), __decorate([m()], W3.prototype, "effectiveLabel", null), __decorate([m()], W3.prototype, "effectiveRequired", null), __decorate([m()], W3.prototype, "effectiveSortable", null), __decorate([m()], W3.prototype, "effectiveTimeZoneOptions", null), __decorate([m({ type: m3 })], W3.prototype, "field", void 0), __decorate([m()], W3.prototype, "formatFunction", void 0), __decorate([m()], W3.prototype, "includeTime", null), __decorate([m()], W3.prototype, "inputRenderFunction", void 0), __decorate([m()], W3.prototype, "layer", void 0), __decorate([m()], W3.prototype, "layerFieldConfigurationFormat", null), __decorate([m({ readOnly: true })], W3.prototype, "loadingMessage", null), __decorate([m()], W3.prototype, "maxLength", null), __decorate([m()], W3.prototype, "minLength", null), __decorate([m({ readOnly: true })], W3.prototype, "name", null), __decorate([m({ readOnly: true })], W3.prototype, "nullable", null), __decorate([m()], W3.prototype, "parseInputValueFunction", void 0), __decorate([m()], W3.prototype, "shouldShowPrompt", null), __decorate([m()], W3.prototype, "sortable", void 0), __decorate([m()], W3.prototype, "store", void 0), __decorate([m()], W3.prototype, "template", void 0), __decorate([m()], W3.prototype, "view", void 0), W3 = __decorate([a3("esri.widgets.FeatureTable.FieldColumn")], W3);
var G5 = W3;

// node_modules/@arcgis/core/widgets/FeatureTable/RelationshipColumn.js
var a11 = { showRecordsButton: "esri-column__show-related-records-button" };
var p8 = class extends f5 {
  constructor(t5) {
    super(t5), this.flexGrow = 1, this.icon = "link", this.layer = null, this.relatedLayer = null, this.relationshipId = null, this.renderFunction = ({ root: t6, rowData: e17 }) => {
      var _a2;
      if (!e17) return;
      const o8 = e17.item, { layer: r12, relatedLayer: n19 } = this;
      if (!(o8 == null ? void 0 : o8.feature) || !d2(r12) || !d2(n19)) return;
      const { messages: p14 } = this, { index: c17 } = e17, { feature: d6, objectId: h9 } = o8, { collapsed: u8, showRelatedTableCallback: b7, relationshipId: m11 } = this, y6 = o8.relatedRecords, f9 = ((_a2 = y6 == null ? void 0 : y6.find((t7) => t7.relationshipId === m11)) == null ? void 0 : _a2.count) ?? 0, C6 = !u8 && b7 && (p14 == null ? void 0 : p14.recordsCount) ? s5(p14.recordsCount, { count: f9 }) : `${f9}`, g5 = (t7) => {
        var _a3;
        t7.preventDefault(), u8 || ((_a3 = this.showRelatedTableCallback) == null ? void 0 : _a3.call(this, { feature: d6, index: c17, layer: r12, objectId: h9, relatedLayer: n19, relationshipId: m11 }));
      }, w4 = u8 ? "" : "chevron-right";
      if (t6.firstChild) {
        const e18 = t6.firstChild;
        return e18.textContent = C6, e18.onclick = g5, void (e18.iconEnd = w4);
      }
      const v2 = this.createCalciteButton({ className: `${a11.showRecordsButton} ${n11.contentFull}`, iconEnd: w4, iconFlipRtl: "both", scale: "m", textContent: C6, title: C6, onclick: g5 });
      this.removeCellContent(t6), t6.appendChild(v2);
    }, this.resizable = true, this.showRelatedTableCallback = null, this.textAlign = "center", this.width = "200px";
  }
  get _relationshipLabel() {
    var _a2;
    return this.label || ((_a2 = this.originRelationship) == null ? void 0 : _a2.name) || this.layer.title || "";
  }
  get collapsed() {
    return this._get("collapsed") || false;
  }
  set collapsed(t5) {
    t5 ? this.set({ flexGrow: 0, resizable: false, width: "80px" }) : this.set({ flexGrow: 1, resizable: true, width: "200px" }), this._set("collapsed", t5);
  }
  get effectiveLabel() {
    return this.collapsed ? "" : this._relationshipLabel;
  }
  get iconNode() {
    const { icon: t5 } = this;
    return t5 ? this.createCalciteIcon({ icon: t5, textLabel: this._relationshipLabel }) : null;
  }
  get originRelationship() {
    var _a2;
    const t5 = this.relationshipId;
    return (_a2 = this.relatedLayer.relationships) == null ? void 0 : _a2.find(({ id: e17 }) => e17 === t5);
  }
  get relationship() {
    return F2(this.layer, this.relationshipId);
  }
};
__decorate([m()], p8.prototype, "_relationshipLabel", null), __decorate([m()], p8.prototype, "collapsed", null), __decorate([m()], p8.prototype, "effectiveLabel", null), __decorate([m()], p8.prototype, "flexGrow", void 0), __decorate([m()], p8.prototype, "icon", void 0), __decorate([m()], p8.prototype, "iconNode", null), __decorate([m()], p8.prototype, "layer", void 0), __decorate([m()], p8.prototype, "originRelationship", null), __decorate([m()], p8.prototype, "relatedLayer", void 0), __decorate([m()], p8.prototype, "relationship", null), __decorate([m()], p8.prototype, "relationshipId", void 0), __decorate([m()], p8.prototype, "renderFunction", void 0), __decorate([m()], p8.prototype, "resizable", void 0), __decorate([m()], p8.prototype, "showRelatedTableCallback", void 0), __decorate([m()], p8.prototype, "textAlign", void 0), __decorate([m()], p8.prototype, "width", void 0), p8 = __decorate([a3("esri.widgets.FeatureTable.RelationshipColumn")], p8);
var c14 = p8;

// node_modules/@vaadin/component-base/src/define.js
window.Vaadin || (window.Vaadin = {});
var _a;
(_a = window.Vaadin).featureFlags || (_a.featureFlags = {});
function dashToCamelCase(dash) {
  return dash.replace(/-[a-z]/gu, (m11) => m11[1].toUpperCase());
}
var experimentalMap = {};
function defineCustomElement(CustomElement, version2 = "24.9.14") {
  Object.defineProperty(CustomElement, "version", {
    get() {
      return version2;
    }
  });
  if (CustomElement.experimental) {
    const featureFlagKey = typeof CustomElement.experimental === "string" ? CustomElement.experimental : `${dashToCamelCase(CustomElement.is.split("-").slice(1).join("-"))}Component`;
    if (!window.Vaadin.featureFlags[featureFlagKey] && !experimentalMap[featureFlagKey]) {
      experimentalMap[featureFlagKey] = /* @__PURE__ */ new Set();
      experimentalMap[featureFlagKey].add(CustomElement);
      Object.defineProperty(window.Vaadin.featureFlags, featureFlagKey, {
        get() {
          return experimentalMap[featureFlagKey].size === 0;
        },
        set(value) {
          if (!!value && experimentalMap[featureFlagKey].size > 0) {
            experimentalMap[featureFlagKey].forEach((elementClass) => {
              customElements.define(elementClass.is, elementClass);
            });
            experimentalMap[featureFlagKey].clear();
          }
        }
      });
      return;
    } else if (experimentalMap[featureFlagKey]) {
      experimentalMap[featureFlagKey].add(CustomElement);
      return;
    }
  }
  const defined = customElements.get(CustomElement.is);
  if (!defined) {
    customElements.define(CustomElement.is, CustomElement);
  } else {
    const definedVersion = defined.version;
    if (definedVersion && CustomElement.version && definedVersion === CustomElement.version) {
      console.warn(`The component ${CustomElement.is} has been loaded twice`);
    } else {
      console.error(
        `Tried to define ${CustomElement.is} version ${CustomElement.version} when version ${defined.version} is already in use. Something will probably break.`
      );
    }
  }
}

// node_modules/@vaadin/vaadin-lumo-styles/version.js
var Lumo = class extends HTMLElement {
  static get is() {
    return "vaadin-lumo-styles";
  }
};
defineCustomElement(Lumo);

// node_modules/@vaadin/vaadin-themable-mixin/src/css-utils.js
function getEffectiveStyles(component) {
  const componentClass = component.constructor;
  const styleSheet = component.__cssInjectorStyleSheet;
  if (styleSheet) {
    return [...componentClass.baseStyles, styleSheet, ...componentClass.themeStyles];
  }
  return componentClass.elementStyles;
}
function applyInstanceStyles(component) {
  [...component.shadowRoot.querySelectorAll("style")].forEach((style2) => style2.remove());
  adoptStyles(component.shadowRoot, getEffectiveStyles(component));
}

// node_modules/@vaadin/vaadin-themable-mixin/vaadin-theme-property-mixin.js
var ThemePropertyMixin = (superClass) => class VaadinThemePropertyMixin extends superClass {
  static get properties() {
    return {
      /**
       * Helper property with theme attribute value facilitating propagation
       * in shadow DOM.
       *
       * Enables the component implementation to propagate the `theme`
       * attribute value to the sub-components in Shadow DOM by binding
       * the sub-component's "theme" attribute to the `theme` property of
       * the host.
       *
       * **NOTE:** Extending the mixin only provides the property for binding,
       * and does not make the propagation alone.
       *
       * See [Styling Components: Sub-components](https://vaadin.com/docs/latest/styling/styling-components/#sub-components).
       * page for more information.
       *
       * @protected
       */
      _theme: {
        type: String,
        readOnly: true
      }
    };
  }
  static get observedAttributes() {
    return [...super.observedAttributes, "theme"];
  }
  /** @protected */
  attributeChangedCallback(name, oldValue, newValue) {
    super.attributeChangedCallback(name, oldValue, newValue);
    if (name === "theme") {
      this._set_theme(newValue);
    }
  }
};

// node_modules/@vaadin/vaadin-themable-mixin/vaadin-themable-mixin.js
var themeRegistry = [];
var themableInstances = /* @__PURE__ */ new Set();
var themableTagNames = /* @__PURE__ */ new Set();
function classHasThemes(elementClass) {
  return elementClass && Object.prototype.hasOwnProperty.call(elementClass, "__themes");
}
function hasThemes(tagName) {
  return classHasThemes(customElements.get(tagName));
}
function flattenStyles(styles = []) {
  return [styles].flat(Infinity).filter((style2) => {
    if (style2 instanceof CSSResult) {
      return true;
    }
    console.warn("An item in styles is not of type CSSResult. Use `unsafeCSS` or `css`.");
    return false;
  });
}
function matchesThemeFor(themeFor, tagName) {
  return (themeFor || "").split(" ").some((themeForToken) => {
    return new RegExp(`^${themeForToken.split("*").join(".*")}$`, "u").test(tagName);
  });
}
function getCssText(styles) {
  return styles.map((style2) => style2.cssText).join("\n");
}
var STYLE_ID = "vaadin-themable-mixin-style";
function addStylesToTemplate(styles, template2) {
  const styleEl = document.createElement("style");
  styleEl.id = STYLE_ID;
  styleEl.textContent = getCssText(styles);
  template2.content.appendChild(styleEl);
}
function updateInstanceStyles(instance) {
  if (!instance.shadowRoot) {
    return;
  }
  const componentClass = instance.constructor;
  if (instance instanceof LitElement) {
    applyInstanceStyles(instance);
  } else {
    const style2 = instance.shadowRoot.getElementById(STYLE_ID);
    const template2 = componentClass.prototype._template;
    style2.textContent = template2.content.getElementById(STYLE_ID).textContent;
  }
}
function updateInstanceStylesOfType(componentClass) {
  themableInstances.forEach((ref) => {
    const instance = ref.deref();
    if (instance instanceof componentClass) {
      updateInstanceStyles(instance);
    } else if (!instance) {
      themableInstances.delete(ref);
    }
  });
}
function updateComponentStyles(componentClass) {
  if (componentClass.prototype instanceof LitElement) {
    componentClass.elementStyles = componentClass.finalizeStyles(componentClass.styles);
  } else {
    const template2 = componentClass.prototype._template;
    template2.content.getElementById(STYLE_ID).textContent = getCssText(componentClass.getStylesForThis());
  }
  themableTagNames.forEach((inheritingTagName) => {
    const inheritingClass = customElements.get(inheritingTagName);
    if (inheritingClass !== componentClass && inheritingClass.prototype instanceof componentClass) {
      updateComponentStyles(inheritingClass);
    }
  });
}
function hasMatchingStyle(componentClass, styles) {
  const themes = componentClass.__themes;
  if (!themes || !styles) {
    return false;
  }
  return themes.some(
    (theme) => theme.styles.some((themeStyle) => styles.some((style2) => style2.cssText === themeStyle.cssText))
  );
}
function registerStyles(themeFor, styles, options = {}) {
  styles = flattenStyles(styles);
  if (window.Vaadin && window.Vaadin.styleModules) {
    window.Vaadin.styleModules.registerStyles(themeFor, styles, options);
  } else {
    themeRegistry.push({
      themeFor,
      styles,
      include: options.include,
      moduleId: options.moduleId
    });
  }
  if (themeFor) {
    themableTagNames.forEach((tagName) => {
      if (matchesThemeFor(themeFor, tagName) && hasThemes(tagName)) {
        const componentClass = customElements.get(tagName);
        if (hasMatchingStyle(componentClass, styles)) {
          console.warn(`Registering styles that already exist for ${tagName}`);
        } else if (!window.Vaadin || !window.Vaadin.suppressPostFinalizeStylesWarning) {
          console.warn(
            `The custom element definition for "${tagName}" was finalized before a style module was registered. Ideally, import component specific style modules before importing the corresponding custom element. This warning can be suppressed by setting "window.Vaadin.suppressPostFinalizeStylesWarning = true".`
          );
        }
        updateComponentStyles(componentClass);
        updateInstanceStylesOfType(componentClass);
      }
    });
  }
}
function getAllThemes() {
  if (window.Vaadin && window.Vaadin.styleModules) {
    return window.Vaadin.styleModules.getAllThemes();
  }
  return themeRegistry;
}
function getIncludePriority(moduleName = "") {
  let includePriority = 0;
  if (moduleName.startsWith("lumo-") || moduleName.startsWith("material-")) {
    includePriority = 1;
  } else if (moduleName.startsWith("vaadin-")) {
    includePriority = 2;
  }
  return includePriority;
}
function getIncludedStyles(theme) {
  const includedStyles = [];
  if (theme.include) {
    [].concat(theme.include).forEach((includeModuleId) => {
      const includedTheme = getAllThemes().find((s22) => s22.moduleId === includeModuleId);
      if (includedTheme) {
        includedStyles.push(...getIncludedStyles(includedTheme), ...includedTheme.styles);
      } else {
        console.warn(`Included moduleId ${includeModuleId} not found in style registry`);
      }
    }, theme.styles);
  }
  return includedStyles;
}
function getThemes(tagName) {
  const defaultModuleName = `${tagName}-default-theme`;
  const themes = getAllThemes().filter((theme) => theme.moduleId !== defaultModuleName && matchesThemeFor(theme.themeFor, tagName)).map((theme) => ({
    ...theme,
    // Prepend styles from included themes
    styles: [...getIncludedStyles(theme), ...theme.styles],
    // Map moduleId to includePriority
    includePriority: getIncludePriority(theme.moduleId)
  })).sort((themeA, themeB) => themeB.includePriority - themeA.includePriority);
  if (themes.length > 0) {
    return themes;
  }
  return getAllThemes().filter((theme) => theme.moduleId === defaultModuleName);
}
var ThemableMixin = (superClass) => class VaadinThemableMixin extends ThemePropertyMixin(superClass) {
  constructor() {
    super();
    themableInstances.add(new WeakRef(this));
  }
  /**
   * Covers PolymerElement based component styling
   * @protected
   */
  static finalize() {
    super.finalize();
    if (this.is) {
      themableTagNames.add(this.is);
    }
    if (this.elementStyles) {
      return;
    }
    const template2 = this.prototype._template;
    if (!template2 || classHasThemes(this)) {
      return;
    }
    addStylesToTemplate(this.getStylesForThis(), template2);
  }
  /**
   * Covers LitElement based component styling
   *
   * @protected
   */
  static finalizeStyles(styles) {
    this.baseStyles = styles ? [styles].flat(Infinity) : [];
    this.themeStyles = this.getStylesForThis();
    return [...this.baseStyles, ...this.themeStyles];
  }
  /**
   * Get styles for the component type
   *
   * @private
   */
  static getStylesForThis() {
    const superClassThemes = superClass.__themes || [];
    const parent = Object.getPrototypeOf(this.prototype);
    const inheritedThemes = (parent ? parent.constructor.__themes : []) || [];
    this.__themes = [...superClassThemes, ...inheritedThemes, ...getThemes(this.is)];
    const themeStyles = this.__themes.flatMap((theme) => theme.styles);
    return themeStyles.filter((style2, index) => index === themeStyles.lastIndexOf(style2));
  }
};

// node_modules/@vaadin/vaadin-themable-mixin/register-styles.js
var addGlobalThemeStyles = (id, ...styles) => {
  const styleTag = document.createElement("style");
  styleTag.id = id;
  styleTag.textContent = styles.map((style2) => style2.toString()).join("\n").replace(":host", "html");
  document.head.insertAdjacentElement("afterbegin", styleTag);
};

// node_modules/@vaadin/vaadin-lumo-styles/global.js
var addLumoGlobalStyles = (id, ...styles) => {
  addGlobalThemeStyles(`lumo-${id}`, styles);
};

// node_modules/@vaadin/vaadin-lumo-styles/color.js
var colorBase = css`
  :host {
    /* Base (background) */
    --lumo-base-color: #fff;

    /* Tint */
    --lumo-tint-5pct: hsla(0, 0%, 100%, 0.3);
    --lumo-tint-10pct: hsla(0, 0%, 100%, 0.37);
    --lumo-tint-20pct: hsla(0, 0%, 100%, 0.44);
    --lumo-tint-30pct: hsla(0, 0%, 100%, 0.5);
    --lumo-tint-40pct: hsla(0, 0%, 100%, 0.57);
    --lumo-tint-50pct: hsla(0, 0%, 100%, 0.64);
    --lumo-tint-60pct: hsla(0, 0%, 100%, 0.7);
    --lumo-tint-70pct: hsla(0, 0%, 100%, 0.77);
    --lumo-tint-80pct: hsla(0, 0%, 100%, 0.84);
    --lumo-tint-90pct: hsla(0, 0%, 100%, 0.9);
    --lumo-tint: #fff;

    /* Shade */
    --lumo-shade-5pct: hsla(214, 61%, 25%, 0.05);
    --lumo-shade-10pct: hsla(214, 57%, 24%, 0.1);
    --lumo-shade-20pct: hsla(214, 53%, 23%, 0.16);
    --lumo-shade-30pct: hsla(214, 50%, 22%, 0.26);
    --lumo-shade-40pct: hsla(214, 47%, 21%, 0.38);
    --lumo-shade-50pct: hsla(214, 45%, 20%, 0.52);
    --lumo-shade-60pct: hsla(214, 43%, 19%, 0.6);
    --lumo-shade-70pct: hsla(214, 42%, 18%, 0.69);
    --lumo-shade-80pct: hsla(214, 41%, 17%, 0.83);
    --lumo-shade-90pct: hsla(214, 40%, 16%, 0.94);
    --lumo-shade: hsl(214, 35%, 15%);

    /* Contrast */
    --lumo-contrast-5pct: var(--lumo-shade-5pct);
    --lumo-contrast-10pct: var(--lumo-shade-10pct);
    --lumo-contrast-20pct: var(--lumo-shade-20pct);
    --lumo-contrast-30pct: var(--lumo-shade-30pct);
    --lumo-contrast-40pct: var(--lumo-shade-40pct);
    --lumo-contrast-50pct: var(--lumo-shade-50pct);
    --lumo-contrast-60pct: var(--lumo-shade-60pct);
    --lumo-contrast-70pct: var(--lumo-shade-70pct);
    --lumo-contrast-80pct: var(--lumo-shade-80pct);
    --lumo-contrast-90pct: var(--lumo-shade-90pct);
    --lumo-contrast: var(--lumo-shade);

    /* Text */
    --lumo-header-text-color: var(--lumo-contrast);
    --lumo-body-text-color: var(--lumo-contrast-90pct);
    --lumo-secondary-text-color: var(--lumo-contrast-70pct);
    --lumo-tertiary-text-color: var(--lumo-contrast-50pct);
    --lumo-disabled-text-color: var(--lumo-contrast-30pct);

    /* Primary */
    --lumo-primary-color: hsl(214, 100%, 48%);
    --lumo-primary-color-50pct: hsla(214, 100%, 49%, 0.76);
    --lumo-primary-color-10pct: hsla(214, 100%, 60%, 0.13);
    --lumo-primary-text-color: hsl(214, 100%, 43%);
    --lumo-primary-contrast-color: #fff;

    /* Error */
    --lumo-error-color: hsl(3, 85%, 48%);
    --lumo-error-color-50pct: hsla(3, 85%, 49%, 0.5);
    --lumo-error-color-10pct: hsla(3, 85%, 49%, 0.1);
    --lumo-error-text-color: hsl(3, 89%, 42%);
    --lumo-error-contrast-color: #fff;

    /* Success */
    --lumo-success-color: hsl(145, 72%, 30%);
    --lumo-success-color-50pct: hsla(145, 72%, 31%, 0.5);
    --lumo-success-color-10pct: hsla(145, 72%, 31%, 0.1);
    --lumo-success-text-color: hsl(145, 85%, 25%);
    --lumo-success-contrast-color: #fff;

    /* Warning */
    --lumo-warning-color: hsl(48, 100%, 50%);
    --lumo-warning-color-10pct: hsla(48, 100%, 50%, 0.25);
    --lumo-warning-text-color: hsl(32, 100%, 30%);
    --lumo-warning-contrast-color: var(--lumo-shade-90pct);
  }

  /* forced-colors mode adjustments */
  @media (forced-colors: active) {
    html {
      --lumo-disabled-text-color: GrayText;
    }
  }
`;
addLumoGlobalStyles("color-props", colorBase);
var color = css`
  [theme~='dark'] {
    /* Base (background) */
    --lumo-base-color: hsl(214, 35%, 21%);

    /* Tint */
    --lumo-tint-5pct: hsla(214, 65%, 85%, 0.06);
    --lumo-tint-10pct: hsla(214, 60%, 80%, 0.14);
    --lumo-tint-20pct: hsla(214, 64%, 82%, 0.23);
    --lumo-tint-30pct: hsla(214, 69%, 84%, 0.32);
    --lumo-tint-40pct: hsla(214, 73%, 86%, 0.41);
    --lumo-tint-50pct: hsla(214, 78%, 88%, 0.5);
    --lumo-tint-60pct: hsla(214, 82%, 90%, 0.58);
    --lumo-tint-70pct: hsla(214, 87%, 92%, 0.69);
    --lumo-tint-80pct: hsla(214, 91%, 94%, 0.8);
    --lumo-tint-90pct: hsla(214, 96%, 96%, 0.9);
    --lumo-tint: hsl(214, 100%, 98%);

    /* Shade */
    --lumo-shade-5pct: hsla(214, 0%, 0%, 0.07);
    --lumo-shade-10pct: hsla(214, 4%, 2%, 0.15);
    --lumo-shade-20pct: hsla(214, 8%, 4%, 0.23);
    --lumo-shade-30pct: hsla(214, 12%, 6%, 0.32);
    --lumo-shade-40pct: hsla(214, 16%, 8%, 0.41);
    --lumo-shade-50pct: hsla(214, 20%, 10%, 0.5);
    --lumo-shade-60pct: hsla(214, 24%, 12%, 0.6);
    --lumo-shade-70pct: hsla(214, 28%, 13%, 0.7);
    --lumo-shade-80pct: hsla(214, 32%, 13%, 0.8);
    --lumo-shade-90pct: hsla(214, 33%, 13%, 0.9);
    --lumo-shade: hsl(214, 33%, 13%);

    /* Contrast */
    --lumo-contrast-5pct: var(--lumo-tint-5pct);
    --lumo-contrast-10pct: var(--lumo-tint-10pct);
    --lumo-contrast-20pct: var(--lumo-tint-20pct);
    --lumo-contrast-30pct: var(--lumo-tint-30pct);
    --lumo-contrast-40pct: var(--lumo-tint-40pct);
    --lumo-contrast-50pct: var(--lumo-tint-50pct);
    --lumo-contrast-60pct: var(--lumo-tint-60pct);
    --lumo-contrast-70pct: var(--lumo-tint-70pct);
    --lumo-contrast-80pct: var(--lumo-tint-80pct);
    --lumo-contrast-90pct: var(--lumo-tint-90pct);
    --lumo-contrast: var(--lumo-tint);

    /* Text */
    --lumo-header-text-color: var(--lumo-contrast);
    --lumo-body-text-color: var(--lumo-contrast-90pct);
    --lumo-secondary-text-color: var(--lumo-contrast-70pct);
    --lumo-tertiary-text-color: var(--lumo-contrast-50pct);
    --lumo-disabled-text-color: var(--lumo-contrast-30pct);

    /* Primary */
    --lumo-primary-color: hsl(214, 90%, 48%);
    --lumo-primary-color-50pct: hsla(214, 90%, 70%, 0.69);
    --lumo-primary-color-10pct: hsla(214, 90%, 55%, 0.13);
    --lumo-primary-text-color: hsl(214, 90%, 77%);
    --lumo-primary-contrast-color: #fff;

    /* Error */
    --lumo-error-color: hsl(3, 79%, 49%);
    --lumo-error-color-50pct: hsla(3, 75%, 62%, 0.5);
    --lumo-error-color-10pct: hsla(3, 75%, 62%, 0.14);
    --lumo-error-text-color: hsl(3, 100%, 80%);

    /* Success */
    --lumo-success-color: hsl(145, 72%, 30%);
    --lumo-success-color-50pct: hsla(145, 92%, 51%, 0.5);
    --lumo-success-color-10pct: hsla(145, 92%, 51%, 0.1);
    --lumo-success-text-color: hsl(145, 85%, 46%);

    /* Warning */
    --lumo-warning-color: hsl(43, 100%, 48%);
    --lumo-warning-color-10pct: hsla(40, 100%, 50%, 0.2);
    --lumo-warning-text-color: hsl(45, 100%, 60%);
    --lumo-warning-contrast-color: var(--lumo-shade-90pct);
  }

  html {
    color: var(--lumo-body-text-color);
    background-color: var(--lumo-base-color);
    color-scheme: light;
  }

  [theme~='dark'] {
    color: var(--lumo-body-text-color);
    background-color: var(--lumo-base-color);
    color-scheme: dark;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    color: var(--lumo-header-text-color);
  }

  a:where(:any-link) {
    color: var(--lumo-primary-text-color);
  }

  a:not(:any-link) {
    color: var(--lumo-disabled-text-color);
  }

  blockquote {
    color: var(--lumo-secondary-text-color);
  }

  code,
  pre {
    background-color: var(--lumo-contrast-10pct);
    border-radius: var(--lumo-border-radius-m);
  }
  pre code {
    background: transparent;
  }
`;
registerStyles("", color, { moduleId: "lumo-color" });

// node_modules/@vaadin/vaadin-lumo-styles/font-icons.js
var fontIcons = css`
  @font-face {
    font-family: 'lumo-icons';
    src: url(data:application/font-woff;charset=utf-8;base64,)
      format('woff');
    font-weight: normal;
    font-style: normal;
  }

  html {
    --lumo-icons-align-center: '\\ea01';
    --lumo-icons-align-left: '\\ea02';
    --lumo-icons-align-right: '\\ea03';
    --lumo-icons-angle-down: '\\ea04';
    --lumo-icons-angle-left: '\\ea05';
    --lumo-icons-angle-right: '\\ea06';
    --lumo-icons-angle-up: '\\ea07';
    --lumo-icons-arrow-down: '\\ea08';
    --lumo-icons-arrow-left: '\\ea09';
    --lumo-icons-arrow-right: '\\ea0a';
    --lumo-icons-arrow-up: '\\ea0b';
    --lumo-icons-bar-chart: '\\ea0c';
    --lumo-icons-bell: '\\ea0d';
    --lumo-icons-calendar: '\\ea0e';
    --lumo-icons-checkmark: '\\ea0f';
    --lumo-icons-chevron-down: '\\ea10';
    --lumo-icons-chevron-left: '\\ea11';
    --lumo-icons-chevron-right: '\\ea12';
    --lumo-icons-chevron-up: '\\ea13';
    --lumo-icons-clock: '\\ea14';
    --lumo-icons-cog: '\\ea15';
    --lumo-icons-cross: '\\ea16';
    --lumo-icons-download: '\\ea17';
    --lumo-icons-drag-handle: '\\ea18';
    --lumo-icons-dropdown: '\\ea19';
    --lumo-icons-edit: '\\ea1a';
    --lumo-icons-error: '\\ea1b';
    --lumo-icons-eye: '\\ea1c';
    --lumo-icons-eye-disabled: '\\ea1d';
    --lumo-icons-menu: '\\ea1e';
    --lumo-icons-minus: '\\ea1f';
    --lumo-icons-ordered-list: '\\ea20';
    --lumo-icons-phone: '\\ea21';
    --lumo-icons-photo: '\\ea22';
    --lumo-icons-play: '\\ea23';
    --lumo-icons-plus: '\\ea24';
    --lumo-icons-redo: '\\ea25';
    --lumo-icons-reload: '\\ea26';
    --lumo-icons-resize-handle: '\\ea27';
    --lumo-icons-search: '\\ea28';
    --lumo-icons-undo: '\\ea29';
    --lumo-icons-unordered-list: '\\ea2a';
    --lumo-icons-upload: '\\ea2b';
    --lumo-icons-user: '\\ea2c';
  }
`;
addLumoGlobalStyles("font-icons", fontIcons);

// node_modules/@vaadin/vaadin-lumo-styles/sizing.js
var sizing = css`
  :host {
    --lumo-size-xs: 1.625rem;
    --lumo-size-s: 1.875rem;
    --lumo-size-m: 2.25rem;
    --lumo-size-l: 2.75rem;
    --lumo-size-xl: 3.5rem;

    /* Icons */
    --lumo-icon-size-s: 1.25em;
    --lumo-icon-size-m: 1.5em;
    --lumo-icon-size-l: 2.25em;
    /* For backwards compatibility */
    --lumo-icon-size: var(--lumo-icon-size-m);
  }
`;
addLumoGlobalStyles("sizing-props", sizing);

// node_modules/@vaadin/vaadin-lumo-styles/spacing.js
var spacing = css`
  :host {
    /* Square */
    --lumo-space-xs: 0.25rem;
    --lumo-space-s: 0.5rem;
    --lumo-space-m: 1rem;
    --lumo-space-l: 1.5rem;
    --lumo-space-xl: 2.5rem;

    /* Wide */
    --lumo-space-wide-xs: calc(var(--lumo-space-xs) / 2) var(--lumo-space-xs);
    --lumo-space-wide-s: calc(var(--lumo-space-s) / 2) var(--lumo-space-s);
    --lumo-space-wide-m: calc(var(--lumo-space-m) / 2) var(--lumo-space-m);
    --lumo-space-wide-l: calc(var(--lumo-space-l) / 2) var(--lumo-space-l);
    --lumo-space-wide-xl: calc(var(--lumo-space-xl) / 2) var(--lumo-space-xl);

    /* Tall */
    --lumo-space-tall-xs: var(--lumo-space-xs) calc(var(--lumo-space-xs) / 2);
    --lumo-space-tall-s: var(--lumo-space-s) calc(var(--lumo-space-s) / 2);
    --lumo-space-tall-m: var(--lumo-space-m) calc(var(--lumo-space-m) / 2);
    --lumo-space-tall-l: var(--lumo-space-l) calc(var(--lumo-space-l) / 2);
    --lumo-space-tall-xl: var(--lumo-space-xl) calc(var(--lumo-space-xl) / 2);
  }
`;
addLumoGlobalStyles("spacing-props", spacing);

// node_modules/@vaadin/vaadin-lumo-styles/style.js
var style = css`
  :host {
    /* Border radius */
    --lumo-border-radius-s: 0.25em; /* Checkbox, badge, date-picker year indicator, etc */
    --lumo-border-radius-m: var(--lumo-border-radius, 0.25em); /* Button, text field, menu overlay, etc */
    --lumo-border-radius-l: 0.5em; /* Dialog, notification, etc */

    /* Shadow */
    --lumo-box-shadow-xs: 0 1px 4px -1px var(--lumo-shade-50pct);
    --lumo-box-shadow-s: 0 2px 4px -1px var(--lumo-shade-20pct), 0 3px 12px -1px var(--lumo-shade-30pct);
    --lumo-box-shadow-m: 0 2px 6px -1px var(--lumo-shade-20pct), 0 8px 24px -4px var(--lumo-shade-40pct);
    --lumo-box-shadow-l: 0 3px 18px -2px var(--lumo-shade-20pct), 0 12px 48px -6px var(--lumo-shade-40pct);
    --lumo-box-shadow-xl: 0 4px 24px -3px var(--lumo-shade-20pct), 0 18px 64px -8px var(--lumo-shade-40pct);

    /* Clickable element cursor */
    --lumo-clickable-cursor: default;
  }
`;
var globals = css`
  html {
    /* Button */
    --vaadin-button-background: var(--lumo-contrast-5pct);
    --vaadin-button-border: none;
    --vaadin-button-border-radius: var(--lumo-border-radius-m);
    --vaadin-button-font-size: var(--lumo-font-size-m);
    --vaadin-button-font-weight: 500;
    --vaadin-button-height: var(--lumo-size-m);
    --vaadin-button-margin: var(--lumo-space-xs) 0;
    --vaadin-button-min-width: calc(var(--vaadin-button-height) * 2);
    --vaadin-button-padding: 0 calc(var(--vaadin-button-height) / 3 + var(--lumo-border-radius-m) / 2);
    --vaadin-button-text-color: var(--lumo-primary-text-color);
    --vaadin-button-primary-background: var(--lumo-primary-color);
    --vaadin-button-primary-border: none;
    --vaadin-button-primary-font-weight: 600;
    --vaadin-button-primary-text-color: var(--lumo-primary-contrast-color);
    --vaadin-button-tertiary-background: transparent !important;
    --vaadin-button-tertiary-text-color: var(--lumo-primary-text-color);
    --vaadin-button-tertiary-font-weight: 500;
    --vaadin-button-tertiary-padding: 0 calc(var(--vaadin-button-height) / 6);
    /* Checkbox */
    --vaadin-checkbox-background: var(--lumo-contrast-20pct);
    --vaadin-checkbox-background-hover: var(--lumo-contrast-30pct);
    --vaadin-checkbox-border-radius: var(--lumo-border-radius-s);
    --vaadin-checkbox-checkmark-char: var(--lumo-icons-checkmark);
    --vaadin-checkbox-checkmark-char-indeterminate: '';
    --vaadin-checkbox-checkmark-color: var(--lumo-primary-contrast-color);
    --vaadin-checkbox-checkmark-size: calc(var(--vaadin-checkbox-size) + 2px);
    --vaadin-checkbox-label-color: var(--lumo-body-text-color);
    --vaadin-checkbox-label-font-size: var(--lumo-font-size-m);
    --vaadin-checkbox-label-padding: var(--lumo-space-xs) var(--lumo-space-s) var(--lumo-space-xs) var(--lumo-space-xs);
    --vaadin-checkbox-size: calc(var(--lumo-size-m) / 2);
    --vaadin-checkbox-disabled-checkmark-color: var(--lumo-contrast-30pct);
    --vaadin-checkbox-disabled-background: var(--lumo-contrast-10pct);
    /* Radio button */
    --vaadin-radio-button-background: var(--lumo-contrast-20pct);
    --vaadin-radio-button-background-hover: var(--lumo-contrast-30pct);
    --vaadin-radio-button-dot-color: var(--lumo-primary-contrast-color);
    --vaadin-radio-button-dot-size: 3px;
    --vaadin-radio-button-label-color: var(--lumo-body-text-color);
    --vaadin-radio-button-label-font-size: var(--lumo-font-size-m);
    --vaadin-radio-button-label-padding: var(--lumo-space-xs) var(--lumo-space-s) var(--lumo-space-xs)
      var(--lumo-space-xs);
    --vaadin-radio-button-size: calc(var(--lumo-size-m) / 2);
    --vaadin-radio-button-disabled-background: var(--lumo-contrast-10pct);
    --vaadin-radio-button-disabled-dot-color: var(--lumo-contrast-30pct);
    --vaadin-selection-color: var(--lumo-primary-color);
    --vaadin-selection-color-text: var(--lumo-primary-text-color);
    --vaadin-input-field-border-radius: var(--lumo-border-radius-m);
    --vaadin-focus-ring-color: var(--lumo-primary-color-50pct);
    --vaadin-focus-ring-width: 2px;
    /* Label */
    --vaadin-input-field-label-color: var(--lumo-secondary-text-color);
    --vaadin-input-field-focused-label-color: var(--lumo-primary-text-color);
    --vaadin-input-field-hovered-label-color: var(--lumo-body-text-color);
    --vaadin-input-field-label-font-size: var(--lumo-font-size-s);
    --vaadin-input-field-label-font-weight: 500;
    /* Helper */
    --vaadin-input-field-helper-color: var(--lumo-secondary-text-color);
    --vaadin-input-field-helper-font-size: var(--lumo-font-size-xs);
    --vaadin-input-field-helper-font-weight: 400;
    --vaadin-input-field-helper-spacing: 0.4em;
    /* Error message */
    --vaadin-input-field-error-color: var(--lumo-error-text-color);
    --vaadin-input-field-error-font-size: var(--lumo-font-size-xs);
    --vaadin-input-field-error-font-weight: 400;
    /* Input field */
    --vaadin-input-field-background: var(--lumo-contrast-10pct);
    --vaadin-input-field-icon-color: var(--lumo-contrast-60pct);
    --vaadin-input-field-icon-size: var(--lumo-icon-size-m);
    --vaadin-input-field-invalid-background: var(--lumo-error-color-10pct);
    --vaadin-input-field-invalid-hover-highlight: var(--lumo-error-color-50pct);
    --vaadin-input-field-disabled-background: var(--lumo-contrast-5pct);
    --vaadin-input-field-disabled-value-color: var(--lumo-disabled-text-color);
    --vaadin-input-field-height: var(--lumo-size-m);
    --vaadin-input-field-hover-highlight: var(--lumo-contrast-50pct);
    --vaadin-input-field-placeholder-color: var(--lumo-secondary-text-color);
    --vaadin-input-field-readonly-border: 1px dashed var(--lumo-contrast-30pct);
    --vaadin-input-field-value-color: var(--lumo-body-text-color);
    --vaadin-input-field-value-font-size: var(--lumo-font-size-m);
    --vaadin-input-field-value-font-weight: 500;
  }
`;
addLumoGlobalStyles("style-props", style);

// node_modules/@vaadin/vaadin-lumo-styles/typography.js
var font = css`
  :host {
    /* prettier-ignore */
    --lumo-font-family: -apple-system, BlinkMacSystemFont, 'Roboto', 'Segoe UI', Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol';

    /* Font sizes */
    --lumo-font-size-xxs: 0.75rem;
    --lumo-font-size-xs: 0.8125rem;
    --lumo-font-size-s: 0.875rem;
    --lumo-font-size-m: 1rem;
    --lumo-font-size-l: 1.125rem;
    --lumo-font-size-xl: 1.375rem;
    --lumo-font-size-xxl: 1.75rem;
    --lumo-font-size-xxxl: 2.5rem;

    /* Line heights */
    --lumo-line-height-xs: 1.25;
    --lumo-line-height-s: 1.375;
    --lumo-line-height-m: 1.625;
  }
`;
var typography = css`
  body,
  :host {
    font-family: var(--lumo-font-family);
    font-size: var(--lumo-font-size-m);
    line-height: var(--lumo-line-height-m);
    -webkit-text-size-adjust: 100%;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  small,
  [theme~='font-size-s'] {
    font-size: var(--lumo-font-size-s);
    line-height: var(--lumo-line-height-s);
  }

  [theme~='font-size-xs'] {
    font-size: var(--lumo-font-size-xs);
    line-height: var(--lumo-line-height-xs);
  }

  :where(h1, h2, h3, h4, h5, h6) {
    font-weight: 600;
    line-height: var(--lumo-line-height-xs);
    margin-block: 0;
  }

  :where(h1) {
    font-size: var(--lumo-font-size-xxxl);
  }

  :where(h2) {
    font-size: var(--lumo-font-size-xxl);
  }

  :where(h3) {
    font-size: var(--lumo-font-size-xl);
  }

  :where(h4) {
    font-size: var(--lumo-font-size-l);
  }

  :where(h5) {
    font-size: var(--lumo-font-size-m);
  }

  :where(h6) {
    font-size: var(--lumo-font-size-xs);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  p,
  blockquote {
    margin-top: 0.5em;
    margin-bottom: 0.75em;
  }

  a {
    text-decoration: none;
  }

  a:where(:any-link):hover {
    text-decoration: underline;
  }

  hr {
    display: block;
    align-self: stretch;
    height: 1px;
    border: 0;
    padding: 0;
    margin: var(--lumo-space-s) calc(var(--lumo-border-radius-m) / 2);
    background-color: var(--lumo-contrast-10pct);
  }

  blockquote {
    border-left: 2px solid var(--lumo-contrast-30pct);
  }

  b,
  strong {
    font-weight: 600;
  }

  /* RTL specific styles */
  blockquote[dir='rtl'] {
    border-left: none;
    border-right: 2px solid var(--lumo-contrast-30pct);
  }
`;
registerStyles("", typography, { moduleId: "lumo-typography" });
addLumoGlobalStyles("typography-props", font);

// node_modules/@vaadin/grid/theme/lumo/vaadin-grid-styles.js
registerStyles(
  "vaadin-grid",
  css`
    :host {
      font-family: var(--lumo-font-family);
      font-size: var(--lumo-font-size-m);
      line-height: var(--lumo-line-height-s);
      color: var(--lumo-body-text-color);
      background-color: var(--lumo-base-color);
      box-sizing: border-box;
      -webkit-text-size-adjust: 100%;
      -webkit-tap-highlight-color: transparent;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      --_focus-ring-color: var(--vaadin-focus-ring-color, var(--lumo-primary-color-50pct));
      --_focus-ring-width: var(--vaadin-focus-ring-width, 2px);
      /* For internal use only */
      --_lumo-grid-border-color: var(--lumo-contrast-20pct);
      --_lumo-grid-secondary-border-color: var(--lumo-contrast-10pct);
      --_lumo-grid-border-width: 1px;
      --_lumo-grid-selected-row-color: var(--lumo-primary-color-10pct);
    }

    /* No (outer) border */

    :host(:not([theme~='no-border'])) {
      border: var(--_lumo-grid-border-width) solid var(--_lumo-grid-border-color);
    }

    :host([disabled]) {
      opacity: 0.7;
    }

    /* Cell styles */

    [part~='cell'] {
      min-height: var(--lumo-size-m);
      background-color: var(--vaadin-grid-cell-background, var(--lumo-base-color));
      cursor: default;
      --_cell-padding: var(--vaadin-grid-cell-padding, var(--_cell-default-padding));
      --_cell-default-padding: var(--lumo-space-xs) var(--lumo-space-m);
    }

    [part~='cell'] ::slotted(vaadin-grid-cell-content) {
      cursor: inherit;
      padding: var(--_cell-padding);
    }

    /* Apply row borders by default and introduce the "no-row-borders" variant */
    :host(:not([theme~='no-row-borders'])) [part~='cell']:not([part~='details-cell']) {
      border-top: var(--_lumo-grid-border-width) solid var(--_lumo-grid-secondary-border-color);
    }

    /* Hide first body row top border */
    :host(:not([theme~='no-row-borders'])) [part~='first-row'] [part~='cell']:not([part~='details-cell']) {
      border-top: 0;
      min-height: calc(var(--lumo-size-m) - var(--_lumo-grid-border-width));
    }

    /* Focus-ring */

    [part~='row'] {
      position: relative;
    }

    [part~='row']:focus,
    [part~='focused-cell']:focus {
      outline: none;
    }

    :host([navigating]) [part~='row']:focus::before,
    :host([navigating]) [part~='focused-cell']:focus::before {
      content: '';
      position: absolute;
      inset: 0;
      pointer-events: none;
      box-shadow: inset 0 0 0 var(--_focus-ring-width) var(--_focus-ring-color);
    }

    :host([navigating]) [part~='row']:focus::before {
      transform: translateX(calc(-1 * var(--_grid-horizontal-scroll-position)));
      z-index: 3;
    }

    /* Empty state */
    [part~='empty-state'] {
      padding: var(--lumo-space-m);
      color: var(--lumo-secondary-text-color);
    }

    /* Drag and Drop styles */
    :host([dragover])::after {
      content: '';
      position: absolute;
      z-index: 100;
      inset: 0;
      pointer-events: none;
      box-shadow: inset 0 0 0 var(--_focus-ring-width) var(--_focus-ring-color);
    }

    [part~='row'][dragover] {
      z-index: 100 !important;
    }

    [part~='row'][dragover] [part~='cell'] {
      overflow: visible;
    }

    [part~='row'][dragover] [part~='cell']::after {
      content: '';
      position: absolute;
      inset: 0;
      height: calc(var(--_lumo-grid-border-width) + 2px);
      pointer-events: none;
      background: var(--lumo-primary-color-50pct);
    }

    [part~='row'][dragover] [part~='cell'][last-frozen]::after {
      right: -1px;
    }

    :host([theme~='no-row-borders']) [dragover] [part~='cell']::after {
      height: 2px;
    }

    [part~='row'][dragover='below'] [part~='cell']::after {
      top: 100%;
      bottom: auto;
      margin-top: -1px;
    }

    :host([all-rows-visible]) [part~='last-row'][dragover='below'] [part~='cell']::after {
      height: 1px;
    }

    [part~='row'][dragover='above'] [part~='cell']::after {
      top: auto;
      bottom: 100%;
      margin-bottom: -1px;
    }

    [part~='row'][details-opened][dragover='below'] [part~='cell']:not([part~='details-cell'])::after,
    [part~='row'][details-opened][dragover='above'] [part~='details-cell']::after {
      display: none;
    }

    [part~='row'][dragover][dragover='on-top'] [part~='cell']::after {
      height: 100%;
      opacity: 0.5;
    }

    [part~='row'][dragstart] [part~='cell'] {
      border: none !important;
      box-shadow: none !important;
    }

    [part~='row'][dragstart] [part~='cell'][last-column] {
      border-radius: 0 var(--lumo-border-radius-s) var(--lumo-border-radius-s) 0;
    }

    [part~='row'][dragstart] [part~='cell'][first-column] {
      border-radius: var(--lumo-border-radius-s) 0 0 var(--lumo-border-radius-s);
    }

    #scroller [part~='row'][dragstart]:not([dragstart=''])::after {
      display: block;
      position: absolute;
      left: var(--_grid-drag-start-x);
      top: var(--_grid-drag-start-y);
      z-index: 100;
      content: attr(dragstart);
      align-items: center;
      justify-content: center;
      box-sizing: border-box;
      padding: calc(var(--lumo-space-xs) * 0.8);
      color: var(--lumo-error-contrast-color);
      background-color: var(--lumo-error-color);
      border-radius: var(--lumo-border-radius-m);
      font-family: var(--lumo-font-family);
      font-size: var(--lumo-font-size-xxs);
      line-height: 1;
      font-weight: 500;
      text-transform: initial;
      letter-spacing: initial;
      min-width: calc(var(--lumo-size-s) * 0.7);
      text-align: center;
    }

    /* Headers and footers */

    [part~='header-cell'],
    [part~='footer-cell'],
    [part~='reorder-ghost'] {
      font-size: var(--lumo-font-size-s);
      font-weight: 500;
    }

    [part~='footer-cell'] {
      font-weight: 400;
    }

    [part~='row']:only-child [part~='header-cell'] {
      min-height: var(--lumo-size-xl);
    }

    /* Header borders */

    /* Hide first header row top border */
    :host(:not([theme~='no-row-borders'])) [part~='row']:first-child [part~='header-cell'] {
      border-top: 0;
    }

    /* Hide header row top border if previous row is hidden */
    [part~='row'][hidden] + [part~='row'] [part~='header-cell'] {
      border-top: 0;
    }

    [part~='row']:last-child [part~='header-cell'] {
      border-bottom: var(--_lumo-grid-border-width) solid transparent;
    }

    :host(:not([theme~='no-row-borders'])) [part~='row']:last-child [part~='header-cell'] {
      border-bottom-color: var(--_lumo-grid-secondary-border-color);
    }

    /* Overflow uses a stronger border color */
    :host([overflow~='top']) [part~='row']:last-child [part~='header-cell'] {
      border-bottom-color: var(--_lumo-grid-border-color);
    }

    /* Footer borders */

    [part~='row']:first-child [part~='footer-cell'] {
      border-top: var(--_lumo-grid-border-width) solid transparent;
    }

    :host(:not([theme~='no-row-borders'])) [part~='row']:first-child [part~='footer-cell'] {
      border-top-color: var(--_lumo-grid-secondary-border-color);
    }

    /* Overflow uses a stronger border color */
    :host([overflow~='bottom']) [part~='row']:first-child [part~='footer-cell'] {
      border-top-color: var(--_lumo-grid-border-color);
    }

    /* Column reordering */

    :host([reordering]) [part~='cell'] {
      background: linear-gradient(var(--lumo-shade-20pct), var(--lumo-shade-20pct)) var(--lumo-base-color);
    }

    :host([reordering]) [part~='cell'][reorder-status='allowed'] {
      background: var(--lumo-base-color);
    }

    :host([reordering]) [part~='cell'][reorder-status='dragging'] {
      background: linear-gradient(var(--lumo-contrast-5pct), var(--lumo-contrast-5pct)) var(--lumo-base-color);
    }

    [part~='reorder-ghost'] {
      opacity: 0.85;
      box-shadow: var(--lumo-box-shadow-s);
      /* TODO Use the same styles as for the cell element (reorder-ghost copies styles from the cell element) */
      padding: var(--lumo-space-s) var(--lumo-space-m) !important;
    }

    /* Column resizing */

    [part='resize-handle'] {
      --_resize-handle-width: 3px;
      width: var(--_resize-handle-width);
      background-color: var(--lumo-primary-color-50pct);
      opacity: 0;
      transition: opacity 0.2s;
    }

    [part='resize-handle']::before {
      transform: translateX(calc(-50% + var(--_resize-handle-width) / 2));
      width: var(--lumo-size-s);
    }

    :host(:not([reordering])) *:not([column-resizing]) [part~='cell']:hover [part='resize-handle'],
    [part='resize-handle']:active {
      opacity: 1;
      transition-delay: 0.15s;
    }

    /* Column borders */

    :host([theme~='column-borders']) [part~='cell']:not([last-column]):not([part~='details-cell']) {
      border-right: var(--_lumo-grid-border-width) solid var(--_lumo-grid-secondary-border-color);
    }

    /* Frozen columns */

    [last-frozen] {
      border-right: var(--_lumo-grid-border-width) solid transparent;
      overflow: hidden;
    }

    :host([overflow~='start']) [part~='cell'][last-frozen]:not([part~='details-cell']) {
      border-right-color: var(--_lumo-grid-border-color);
    }

    [first-frozen-to-end] {
      border-left: var(--_lumo-grid-border-width) solid transparent;
    }

    :host([overflow~='end']) [part~='cell'][first-frozen-to-end]:not([part~='details-cell']) {
      border-left-color: var(--_lumo-grid-border-color);
    }

    /* Row stripes */

    :host([theme~='row-stripes']) [part~='even-row'] [part~='body-cell'],
    :host([theme~='row-stripes']) [part~='even-row'] [part~='details-cell'] {
      background-image: linear-gradient(var(--lumo-contrast-5pct), var(--lumo-contrast-5pct));
      background-repeat: repeat-x;
    }

    /* Selected row */

    /* Raise the selected rows above unselected rows (so that box-shadow can cover unselected rows) */
    :host(:not([reordering])) [part~='row'][selected] {
      z-index: 1;
    }

    :host(:not([reordering])) [part~='row'][selected] [part~='body-cell']:not([part~='details-cell']) {
      background-image: linear-gradient(var(--_lumo-grid-selected-row-color), var(--_lumo-grid-selected-row-color));
      background-repeat: repeat;
    }

    /* Cover the border of an unselected row */
    :host(:not([theme~='no-row-borders'])) [part~='row'][selected] [part~='cell']:not([part~='details-cell']) {
      box-shadow: 0 var(--_lumo-grid-border-width) 0 0 var(--_lumo-grid-selected-row-color);
    }

    /* Compact */

    :host([theme~='compact']) [part~='row']:only-child [part~='header-cell'] {
      min-height: var(--lumo-size-m);
    }

    :host([theme~='compact']) [part~='cell'] {
      min-height: var(--lumo-size-s);
      --_cell-default-padding: var(--lumo-space-xs) var(--lumo-space-s);
    }

    :host([theme~='compact']) [part~='first-row'] [part~='cell']:not([part~='details-cell']) {
      min-height: calc(var(--lumo-size-s) - var(--_lumo-grid-border-width));
    }

    :host([theme~='compact']) [part~='empty-state'] {
      padding: var(--lumo-space-s);
    }

    /* Wrap cell contents */

    :host([theme~='wrap-cell-content']) [part~='cell'] ::slotted(vaadin-grid-cell-content) {
      white-space: normal;
    }

    /* RTL specific styles */

    :host([dir='rtl']) [part~='row'][dragstart] [part~='cell'][last-column] {
      border-radius: var(--lumo-border-radius-s) 0 0 var(--lumo-border-radius-s);
    }

    :host([dir='rtl']) [part~='row'][dragstart] [part~='cell'][first-column] {
      border-radius: 0 var(--lumo-border-radius-s) var(--lumo-border-radius-s) 0;
    }

    :host([dir='rtl'][theme~='column-borders']) [part~='cell']:not([last-column]):not([part~='details-cell']) {
      border-right: none;
      border-left: var(--_lumo-grid-border-width) solid var(--_lumo-grid-secondary-border-color);
    }

    :host([dir='rtl']) [last-frozen] {
      border-right: none;
      border-left: var(--_lumo-grid-border-width) solid transparent;
    }

    :host([dir='rtl']) [first-frozen-to-end] {
      border-left: none;
      border-right: var(--_lumo-grid-border-width) solid transparent;
    }

    :host([dir='rtl'][overflow~='start']) [part~='cell'][last-frozen]:not([part~='details-cell']) {
      border-left-color: var(--_lumo-grid-border-color);
    }

    :host([dir='rtl'][overflow~='end']) [part~='cell'][first-frozen-to-end]:not([part~='details-cell']) {
      border-right-color: var(--_lumo-grid-border-color);
    }
  `,
  { moduleId: "lumo-grid" }
);

// node_modules/@polymer/polymer/lib/utils/boot.js
window.JSCompiler_renameProperty = function(prop, obj) {
  return prop;
};

// node_modules/@polymer/polymer/lib/utils/resolve-url.js
var CSS_URL_RX = /(url\()([^)]*)(\))/g;
var ABS_URL = /(^\/[^\/])|(^#)|(^[\w-\d]*:)/;
var workingURL;
var resolveDoc;
function resolveUrl(url, baseURI) {
  if (url && ABS_URL.test(url)) {
    return url;
  }
  if (url === "//") {
    return url;
  }
  if (workingURL === void 0) {
    workingURL = false;
    try {
      const u8 = new URL("b", "http://a");
      u8.pathname = "c%20d";
      workingURL = u8.href === "http://a/c%20d";
    } catch (e17) {
    }
  }
  if (!baseURI) {
    baseURI = document.baseURI || window.location.href;
  }
  if (workingURL) {
    try {
      return new URL(url, baseURI).href;
    } catch (e17) {
      return url;
    }
  }
  if (!resolveDoc) {
    resolveDoc = document.implementation.createHTMLDocument("temp");
    resolveDoc.base = resolveDoc.createElement("base");
    resolveDoc.head.appendChild(resolveDoc.base);
    resolveDoc.anchor = resolveDoc.createElement("a");
    resolveDoc.body.appendChild(resolveDoc.anchor);
  }
  resolveDoc.base.href = baseURI;
  resolveDoc.anchor.href = url;
  return resolveDoc.anchor.href || url;
}
function resolveCss(cssText, baseURI) {
  return cssText.replace(CSS_URL_RX, function(m11, pre, url, post) {
    return pre + "'" + resolveUrl(url.replace(/["']/g, ""), baseURI) + "'" + post;
  });
}
function pathFromUrl(url) {
  return url.substring(0, url.lastIndexOf("/") + 1);
}

// node_modules/@polymer/polymer/lib/utils/settings.js
var useShadow = !window.ShadyDOM || !window.ShadyDOM.inUse;
var useNativeCSSProperties = Boolean(!window.ShadyCSS || window.ShadyCSS.nativeCss);
var useNativeCustomElements = !window.customElements.polyfillWrapFlushCallback;
var supportsAdoptingStyleSheets = useShadow && "adoptedStyleSheets" in Document.prototype && "replaceSync" in CSSStyleSheet.prototype && // Since spec may change, feature detect exact API we need
(() => {
  try {
    const sheet = new CSSStyleSheet();
    sheet.replaceSync("");
    const host = document.createElement("div");
    host.attachShadow({ mode: "open" });
    host.shadowRoot.adoptedStyleSheets = [sheet];
    return host.shadowRoot.adoptedStyleSheets[0] === sheet;
  } catch (e17) {
    return false;
  }
})();
var rootPath = window.Polymer && window.Polymer.rootPath || pathFromUrl(document.baseURI || window.location.href);
var sanitizeDOMValue = window.Polymer && window.Polymer.sanitizeDOMValue || void 0;
var passiveTouchGestures = window.Polymer && window.Polymer.setPassiveTouchGestures || false;
var strictTemplatePolicy = window.Polymer && window.Polymer.strictTemplatePolicy || false;
var allowTemplateFromDomModule = window.Polymer && window.Polymer.allowTemplateFromDomModule || false;
var legacyOptimizations = window.Polymer && window.Polymer.legacyOptimizations || false;
var legacyWarnings = window.Polymer && window.Polymer.legacyWarnings || false;
var syncInitialRender = window.Polymer && window.Polymer.syncInitialRender || false;
var legacyUndefined = window.Polymer && window.Polymer.legacyUndefined || false;
var orderedComputed = window.Polymer && window.Polymer.orderedComputed || false;
var cancelSyntheticClickEvents = true;
var setCancelSyntheticClickEvents = function(useCancelSyntheticClickEvents) {
  cancelSyntheticClickEvents = useCancelSyntheticClickEvents;
};
var removeNestedTemplates = window.Polymer && window.Polymer.removeNestedTemplates || false;
var fastDomIf = window.Polymer && window.Polymer.fastDomIf || false;
var suppressTemplateNotifications = window.Polymer && window.Polymer.suppressTemplateNotifications || false;
var legacyNoObservedAttributes = window.Polymer && window.Polymer.legacyNoObservedAttributes || false;
var useAdoptedStyleSheetsWithBuiltCSS = window.Polymer && window.Polymer.useAdoptedStyleSheetsWithBuiltCSS || false;

// node_modules/@polymer/polymer/lib/utils/mixin.js
var dedupeId = 0;
function MixinFunction() {
}
MixinFunction.prototype.__mixinApplications;
MixinFunction.prototype.__mixinSet;
var dedupingMixin = function(mixin) {
  let mixinApplications = (
    /** @type {!MixinFunction} */
    mixin.__mixinApplications
  );
  if (!mixinApplications) {
    mixinApplications = /* @__PURE__ */ new WeakMap();
    mixin.__mixinApplications = mixinApplications;
  }
  let mixinDedupeId = dedupeId++;
  function dedupingMixin2(base) {
    let baseSet = (
      /** @type {!MixinFunction} */
      base.__mixinSet
    );
    if (baseSet && baseSet[mixinDedupeId]) {
      return base;
    }
    let map = mixinApplications;
    let extended = map.get(base);
    if (!extended) {
      extended = /** @type {!Function} */
      mixin(base);
      map.set(base, extended);
      let mixinSet = Object.create(
        /** @type {!MixinFunction} */
        extended.__mixinSet || baseSet || null
      );
      mixinSet[mixinDedupeId] = true;
      extended.__mixinSet = mixinSet;
    }
    return extended;
  }
  return dedupingMixin2;
};

// node_modules/@polymer/polymer/lib/elements/dom-module.js
var modules = {};
var lcModules = {};
function setModule(id, module) {
  modules[id] = lcModules[id.toLowerCase()] = module;
}
function findModule(id) {
  return modules[id] || lcModules[id.toLowerCase()];
}
function styleOutsideTemplateCheck(inst) {
  if (inst.querySelector("style")) {
    console.warn("dom-module %s has style outside template", inst.id);
  }
}
var DomModule = class extends HTMLElement {
  /** @override */
  static get observedAttributes() {
    return ["id"];
  }
  /**
   * Retrieves the element specified by the css `selector` in the module
   * registered by `id`. For example, this.import('foo', 'img');
   * @param {string} id The id of the dom-module in which to search.
   * @param {string=} selector The css selector by which to find the element.
   * @return {Element} Returns the element which matches `selector` in the
   * module registered at the specified `id`.
   *
   * @export
   * @nocollapse Referred to indirectly in style-gather.js
   */
  static import(id, selector) {
    if (id) {
      let m11 = findModule(id);
      if (m11 && selector) {
        return m11.querySelector(selector);
      }
      return m11;
    }
    return null;
  }
  /* eslint-disable no-unused-vars */
  /**
   * @param {string} name Name of attribute.
   * @param {?string} old Old value of attribute.
   * @param {?string} value Current value of attribute.
   * @param {?string} namespace Attribute namespace.
   * @return {void}
   * @override
   */
  attributeChangedCallback(name, old, value, namespace) {
    if (old !== value) {
      this.register();
    }
  }
  /* eslint-enable no-unused-args */
  /**
   * The absolute URL of the original location of this `dom-module`.
   *
   * This value will differ from this element's `ownerDocument` in the
   * following ways:
   * - Takes into account any `assetpath` attribute added during bundling
   *   to indicate the original location relative to the bundled location
   * - Uses the HTMLImports polyfill's `importForElement` API to ensure
   *   the path is relative to the import document's location since
   *   `ownerDocument` is not currently polyfilled
   */
  get assetpath() {
    if (!this.__assetpath) {
      const owner = window.HTMLImports && HTMLImports.importForElement ? HTMLImports.importForElement(this) || document : this.ownerDocument;
      const url = resolveUrl(
        this.getAttribute("assetpath") || "",
        owner.baseURI
      );
      this.__assetpath = pathFromUrl(url);
    }
    return this.__assetpath;
  }
  /**
   * Registers the dom-module at a given id. This method should only be called
   * when a dom-module is imperatively created. For
   * example, `document.createElement('dom-module').register('foo')`.
   * @param {string=} id The id at which to register the dom-module.
   * @return {void}
   */
  register(id) {
    id = id || this.id;
    if (id) {
      if (strictTemplatePolicy && findModule(id) !== void 0) {
        setModule(id, null);
        throw new Error(`strictTemplatePolicy: dom-module ${id} re-registered`);
      }
      this.id = id;
      setModule(id, this);
      styleOutsideTemplateCheck(this);
    }
  }
};
DomModule.prototype["modules"] = modules;
customElements.define("dom-module", DomModule);

// node_modules/@polymer/polymer/lib/utils/style-gather.js
var MODULE_STYLE_LINK_SELECTOR = "link[rel=import][type~=css]";
var INCLUDE_ATTR = "include";
var SHADY_UNSCOPED_ATTR = "shady-unscoped";
function importModule(moduleId) {
  return (
    /** @type {?DomModule} */
    DomModule.import(moduleId)
  );
}
function styleForImport(importDoc) {
  let container = importDoc.body ? importDoc.body : importDoc;
  const importCss = resolveCss(
    container.textContent,
    importDoc.baseURI
  );
  const style2 = document.createElement("style");
  style2.textContent = importCss;
  return style2;
}
function stylesFromModules(moduleIds) {
  const modules2 = moduleIds.trim().split(/\s+/);
  const styles = [];
  for (let i10 = 0; i10 < modules2.length; i10++) {
    styles.push(...stylesFromModule(modules2[i10]));
  }
  return styles;
}
function stylesFromModule(moduleId) {
  const m11 = importModule(moduleId);
  if (!m11) {
    console.warn("Could not find style data in module named", moduleId);
    return [];
  }
  if (m11._styles === void 0) {
    const styles = [];
    styles.push(..._stylesFromModuleImports(m11));
    const template2 = (
      /** @type {?HTMLTemplateElement} */
      m11.querySelector("template")
    );
    if (template2) {
      styles.push(...stylesFromTemplate(
        template2,
        /** @type {templateWithAssetPath} */
        m11.assetpath
      ));
    }
    m11._styles = styles;
  }
  return m11._styles;
}
function stylesFromTemplate(template2, baseURI) {
  if (!template2._styles) {
    const styles = [];
    const e$ = template2.content.querySelectorAll("style");
    for (let i10 = 0; i10 < e$.length; i10++) {
      let e17 = e$[i10];
      let include = e17.getAttribute(INCLUDE_ATTR);
      if (include) {
        styles.push(...stylesFromModules(include).filter(function(item, index, self) {
          return self.indexOf(item) === index;
        }));
      }
      if (baseURI) {
        e17.textContent = resolveCss(
          e17.textContent,
          /** @type {string} */
          baseURI
        );
      }
      styles.push(e17);
    }
    template2._styles = styles;
  }
  return template2._styles;
}
function stylesFromModuleImports(moduleId) {
  let m11 = importModule(moduleId);
  return m11 ? _stylesFromModuleImports(m11) : [];
}
function _stylesFromModuleImports(module) {
  const styles = [];
  const p$ = module.querySelectorAll(MODULE_STYLE_LINK_SELECTOR);
  for (let i10 = 0; i10 < p$.length; i10++) {
    let p14 = p$[i10];
    if (p14.import) {
      const importDoc = p14.import;
      const unscoped = p14.hasAttribute(SHADY_UNSCOPED_ATTR);
      if (unscoped && !importDoc._unscopedStyle) {
        const style2 = styleForImport(importDoc);
        style2.setAttribute(SHADY_UNSCOPED_ATTR, "");
        importDoc._unscopedStyle = style2;
      } else if (!importDoc._style) {
        importDoc._style = styleForImport(importDoc);
      }
      styles.push(unscoped ? importDoc._unscopedStyle : importDoc._style);
    }
  }
  return styles;
}

// node_modules/@polymer/polymer/lib/utils/wrap.js
var wrap = window["ShadyDOM"] && window["ShadyDOM"]["noPatch"] && window["ShadyDOM"]["wrap"] ? window["ShadyDOM"]["wrap"] : window["ShadyDOM"] ? (n19) => ShadyDOM["patch"](n19) : (n19) => n19;

// node_modules/@polymer/polymer/lib/utils/path.js
function isPath(path) {
  return path.indexOf(".") >= 0;
}
function root(path) {
  let dotIndex = path.indexOf(".");
  if (dotIndex === -1) {
    return path;
  }
  return path.slice(0, dotIndex);
}
function isAncestor(base, path) {
  return base.indexOf(path + ".") === 0;
}
function isDescendant(base, path) {
  return path.indexOf(base + ".") === 0;
}
function translate(base, newBase, path) {
  return newBase + path.slice(base.length);
}
function normalize(path) {
  if (Array.isArray(path)) {
    let parts = [];
    for (let i10 = 0; i10 < path.length; i10++) {
      let args = path[i10].toString().split(".");
      for (let j5 = 0; j5 < args.length; j5++) {
        parts.push(args[j5]);
      }
    }
    return parts.join(".");
  } else {
    return path;
  }
}
function split(path) {
  if (Array.isArray(path)) {
    return normalize(path).split(".");
  }
  return path.toString().split(".");
}
function get(root2, path, info) {
  let prop = root2;
  let parts = split(path);
  for (let i10 = 0; i10 < parts.length; i10++) {
    if (!prop) {
      return;
    }
    let part = parts[i10];
    prop = prop[part];
  }
  if (info) {
    info.path = parts.join(".");
  }
  return prop;
}
function set(root2, path, value) {
  let prop = root2;
  let parts = split(path);
  let last = parts[parts.length - 1];
  if (parts.length > 1) {
    for (let i10 = 0; i10 < parts.length - 1; i10++) {
      let part = parts[i10];
      prop = prop[part];
      if (!prop) {
        return;
      }
    }
    prop[last] = value;
  } else {
    prop[path] = value;
  }
  return parts.join(".");
}

// node_modules/@polymer/polymer/lib/utils/case-map.js
var caseMap = {};
var DASH_TO_CAMEL = /-[a-z]/g;
var CAMEL_TO_DASH = /([A-Z])/g;
function dashToCamelCase2(dash) {
  return caseMap[dash] || (caseMap[dash] = dash.indexOf("-") < 0 ? dash : dash.replace(
    DASH_TO_CAMEL,
    (m11) => m11[1].toUpperCase()
  ));
}
function camelToDashCase(camel) {
  return caseMap[camel] || (caseMap[camel] = camel.replace(CAMEL_TO_DASH, "-$1").toLowerCase());
}

// node_modules/@polymer/polymer/lib/utils/async.js
var microtaskCurrHandle = 0;
var microtaskLastHandle = 0;
var microtaskCallbacks = [];
var microtaskNodeContent = 0;
var microtaskScheduled = false;
var microtaskNode = document.createTextNode("");
new window.MutationObserver(microtaskFlush).observe(microtaskNode, { characterData: true });
function microtaskFlush() {
  microtaskScheduled = false;
  const len = microtaskCallbacks.length;
  for (let i10 = 0; i10 < len; i10++) {
    let cb = microtaskCallbacks[i10];
    if (cb) {
      try {
        cb();
      } catch (e17) {
        setTimeout(() => {
          throw e17;
        });
      }
    }
  }
  microtaskCallbacks.splice(0, len);
  microtaskLastHandle += len;
}
var microTask = {
  /**
   * Enqueues a function called at microtask timing.
   *
   * @memberof microTask
   * @param {!Function=} callback Callback to run
   * @return {number} Handle used for canceling task
   */
  run(callback) {
    if (!microtaskScheduled) {
      microtaskScheduled = true;
      microtaskNode.textContent = microtaskNodeContent++;
    }
    microtaskCallbacks.push(callback);
    return microtaskCurrHandle++;
  },
  /**
   * Cancels a previously enqueued `microTask` callback.
   *
   * @memberof microTask
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(handle) {
    const idx = handle - microtaskLastHandle;
    if (idx >= 0) {
      if (!microtaskCallbacks[idx]) {
        throw new Error("invalid async handle: " + handle);
      }
      microtaskCallbacks[idx] = null;
    }
  }
};

// node_modules/@polymer/polymer/lib/mixins/properties-changed.js
var microtask = microTask;
var PropertiesChanged = dedupingMixin(
  /**
   * @template T
   * @param {function(new:T)} superClass Class to apply mixin to.
   * @return {function(new:T)} superClass with mixin applied.
   */
  (superClass) => {
    class PropertiesChanged2 extends superClass {
      /**
       * Creates property accessors for the given property names.
       * @param {!Object} props Object whose keys are names of accessors.
       * @return {void}
       * @protected
       * @nocollapse
       */
      static createProperties(props) {
        const proto2 = this.prototype;
        for (let prop in props) {
          if (!(prop in proto2)) {
            proto2._createPropertyAccessor(prop);
          }
        }
      }
      /**
       * Returns an attribute name that corresponds to the given property.
       * The attribute name is the lowercased property name. Override to
       * customize this mapping.
       * @param {string} property Property to convert
       * @return {string} Attribute name corresponding to the given property.
       *
       * @protected
       * @nocollapse
       */
      static attributeNameForProperty(property) {
        return property.toLowerCase();
      }
      /**
       * Override point to provide a type to which to deserialize a value to
       * a given property.
       * @param {string} name Name of property
       *
       * @protected
       * @nocollapse
       */
      static typeForProperty(name) {
      }
      //eslint-disable-line no-unused-vars
      /**
       * Creates a setter/getter pair for the named property with its own
       * local storage.  The getter returns the value in the local storage,
       * and the setter calls `_setProperty`, which updates the local storage
       * for the property and enqueues a `_propertiesChanged` callback.
       *
       * This method may be called on a prototype or an instance.  Calling
       * this method may overwrite a property value that already exists on
       * the prototype/instance by creating the accessor.
       *
       * @param {string} property Name of the property
       * @param {boolean=} readOnly When true, no setter is created; the
       *   protected `_setProperty` function must be used to set the property
       * @return {void}
       * @protected
       * @override
       */
      _createPropertyAccessor(property, readOnly) {
        this._addPropertyToAttributeMap(property);
        if (!this.hasOwnProperty(JSCompiler_renameProperty("__dataHasAccessor", this))) {
          this.__dataHasAccessor = Object.assign({}, this.__dataHasAccessor);
        }
        if (!this.__dataHasAccessor[property]) {
          this.__dataHasAccessor[property] = true;
          this._definePropertyAccessor(property, readOnly);
        }
      }
      /**
       * Adds the given `property` to a map matching attribute names
       * to property names, using `attributeNameForProperty`. This map is
       * used when deserializing attribute values to properties.
       *
       * @param {string} property Name of the property
       * @override
       */
      _addPropertyToAttributeMap(property) {
        if (!this.hasOwnProperty(JSCompiler_renameProperty("__dataAttributes", this))) {
          this.__dataAttributes = Object.assign({}, this.__dataAttributes);
        }
        let attr = this.__dataAttributes[property];
        if (!attr) {
          attr = this.constructor.attributeNameForProperty(property);
          this.__dataAttributes[attr] = property;
        }
        return attr;
      }
      /**
       * Defines a property accessor for the given property.
       * @param {string} property Name of the property
       * @param {boolean=} readOnly When true, no setter is created
       * @return {void}
       * @override
       */
      _definePropertyAccessor(property, readOnly) {
        Object.defineProperty(this, property, {
          /* eslint-disable valid-jsdoc */
          /** @this {PropertiesChanged} */
          get() {
            return this.__data[property];
          },
          /** @this {PropertiesChanged} */
          set: readOnly ? function() {
          } : function(value) {
            if (this._setPendingProperty(property, value, true)) {
              this._invalidateProperties();
            }
          }
          /* eslint-enable */
        });
      }
      constructor() {
        super();
        this.__dataEnabled = false;
        this.__dataReady = false;
        this.__dataInvalid = false;
        this.__data = {};
        this.__dataPending = null;
        this.__dataOld = null;
        this.__dataInstanceProps = null;
        this.__dataCounter = 0;
        this.__serializing = false;
        this._initializeProperties();
      }
      /**
       * Lifecycle callback called when properties are enabled via
       * `_enableProperties`.
       *
       * Users may override this function to implement behavior that is
       * dependent on the element having its property data initialized, e.g.
       * from defaults (initialized from `constructor`, `_initializeProperties`),
       * `attributeChangedCallback`, or values propagated from host e.g. via
       * bindings.  `super.ready()` must be called to ensure the data system
       * becomes enabled.
       *
       * @return {void}
       * @public
       * @override
       */
      ready() {
        this.__dataReady = true;
        this._flushProperties();
      }
      /**
       * Initializes the local storage for property accessors.
       *
       * Provided as an override point for performing any setup work prior
       * to initializing the property accessor system.
       *
       * @return {void}
       * @protected
       * @override
       */
      _initializeProperties() {
        for (let p14 in this.__dataHasAccessor) {
          if (this.hasOwnProperty(p14)) {
            this.__dataInstanceProps = this.__dataInstanceProps || {};
            this.__dataInstanceProps[p14] = this[p14];
            delete this[p14];
          }
        }
      }
      /**
       * Called at ready time with bag of instance properties that overwrote
       * accessors when the element upgraded.
       *
       * The default implementation sets these properties back into the
       * setter at ready time.  This method is provided as an override
       * point for customizing or providing more efficient initialization.
       *
       * @param {Object} props Bag of property values that were overwritten
       *   when creating property accessors.
       * @return {void}
       * @protected
       * @override
       */
      _initializeInstanceProperties(props) {
        Object.assign(this, props);
      }
      /**
       * Updates the local storage for a property (via `_setPendingProperty`)
       * and enqueues a `_proeprtiesChanged` callback.
       *
       * @param {string} property Name of the property
       * @param {*} value Value to set
       * @return {void}
       * @protected
       * @override
       */
      _setProperty(property, value) {
        if (this._setPendingProperty(property, value)) {
          this._invalidateProperties();
        }
      }
      /**
       * Returns the value for the given property.
       * @param {string} property Name of property
       * @return {*} Value for the given property
       * @protected
       * @override
       */
      _getProperty(property) {
        return this.__data[property];
      }
      /* eslint-disable no-unused-vars */
      /**
       * Updates the local storage for a property, records the previous value,
       * and adds it to the set of "pending changes" that will be passed to the
       * `_propertiesChanged` callback.  This method does not enqueue the
       * `_propertiesChanged` callback.
       *
       * @param {string} property Name of the property
       * @param {*} value Value to set
       * @param {boolean=} ext Not used here; affordance for closure
       * @return {boolean} Returns true if the property changed
       * @protected
       * @override
       */
      _setPendingProperty(property, value, ext) {
        let old = this.__data[property];
        let changed = this._shouldPropertyChange(property, value, old);
        if (changed) {
          if (!this.__dataPending) {
            this.__dataPending = {};
            this.__dataOld = {};
          }
          if (this.__dataOld && !(property in this.__dataOld)) {
            this.__dataOld[property] = old;
          }
          this.__data[property] = value;
          this.__dataPending[property] = value;
        }
        return changed;
      }
      /* eslint-enable */
      /**
       * @param {string} property Name of the property
       * @return {boolean} Returns true if the property is pending.
       */
      _isPropertyPending(property) {
        return !!(this.__dataPending && this.__dataPending.hasOwnProperty(property));
      }
      /**
       * Marks the properties as invalid, and enqueues an async
       * `_propertiesChanged` callback.
       *
       * @return {void}
       * @protected
       * @override
       */
      _invalidateProperties() {
        if (!this.__dataInvalid && this.__dataReady) {
          this.__dataInvalid = true;
          microtask.run(() => {
            if (this.__dataInvalid) {
              this.__dataInvalid = false;
              this._flushProperties();
            }
          });
        }
      }
      /**
       * Call to enable property accessor processing. Before this method is
       * called accessor values will be set but side effects are
       * queued. When called, any pending side effects occur immediately.
       * For elements, generally `connectedCallback` is a normal spot to do so.
       * It is safe to call this method multiple times as it only turns on
       * property accessors once.
       *
       * @return {void}
       * @protected
       * @override
       */
      _enableProperties() {
        if (!this.__dataEnabled) {
          this.__dataEnabled = true;
          if (this.__dataInstanceProps) {
            this._initializeInstanceProperties(this.__dataInstanceProps);
            this.__dataInstanceProps = null;
          }
          this.ready();
        }
      }
      /**
       * Calls the `_propertiesChanged` callback with the current set of
       * pending changes (and old values recorded when pending changes were
       * set), and resets the pending set of changes. Generally, this method
       * should not be called in user code.
       *
       * @return {void}
       * @protected
       * @override
       */
      _flushProperties() {
        this.__dataCounter++;
        const props = this.__data;
        const changedProps = this.__dataPending;
        const old = this.__dataOld;
        if (this._shouldPropertiesChange(props, changedProps, old)) {
          this.__dataPending = null;
          this.__dataOld = null;
          this._propertiesChanged(props, changedProps, old);
        }
        this.__dataCounter--;
      }
      /**
       * Called in `_flushProperties` to determine if `_propertiesChanged`
       * should be called. The default implementation returns true if
       * properties are pending. Override to customize when
       * `_propertiesChanged` is called.
       * @param {!Object} currentProps Bag of all current accessor values
       * @param {?Object} changedProps Bag of properties changed since the last
       *   call to `_propertiesChanged`
       * @param {?Object} oldProps Bag of previous values for each property
       *   in `changedProps`
       * @return {boolean} true if changedProps is truthy
       * @override
       */
      _shouldPropertiesChange(currentProps, changedProps, oldProps) {
        return Boolean(changedProps);
      }
      /**
       * Callback called when any properties with accessors created via
       * `_createPropertyAccessor` have been set.
       *
       * @param {!Object} currentProps Bag of all current accessor values
       * @param {?Object} changedProps Bag of properties changed since the last
       *   call to `_propertiesChanged`
       * @param {?Object} oldProps Bag of previous values for each property
       *   in `changedProps`
       * @return {void}
       * @protected
       * @override
       */
      _propertiesChanged(currentProps, changedProps, oldProps) {
      }
      /**
       * Method called to determine whether a property value should be
       * considered as a change and cause the `_propertiesChanged` callback
       * to be enqueued.
       *
       * The default implementation returns `true` if a strict equality
       * check fails. The method always returns false for `NaN`.
       *
       * Override this method to e.g. provide stricter checking for
       * Objects/Arrays when using immutable patterns.
       *
       * @param {string} property Property name
       * @param {*} value New property value
       * @param {*} old Previous property value
       * @return {boolean} Whether the property should be considered a change
       *   and enqueue a `_proeprtiesChanged` callback
       * @protected
       * @override
       */
      _shouldPropertyChange(property, value, old) {
        return (
          // Strict equality check
          old !== value && // This ensures (old==NaN, value==NaN) always returns false
          (old === old || value === value)
        );
      }
      /**
       * Implements native Custom Elements `attributeChangedCallback` to
       * set an attribute value to a property via `_attributeToProperty`.
       *
       * @param {string} name Name of attribute that changed
       * @param {?string} old Old attribute value
       * @param {?string} value New attribute value
       * @param {?string} namespace Attribute namespace.
       * @return {void}
       * @suppress {missingProperties} Super may or may not implement the callback
       * @override
       */
      attributeChangedCallback(name, old, value, namespace) {
        if (old !== value) {
          this._attributeToProperty(name, value);
        }
        if (super.attributeChangedCallback) {
          super.attributeChangedCallback(name, old, value, namespace);
        }
      }
      /**
       * Deserializes an attribute to its associated property.
       *
       * This method calls the `_deserializeValue` method to convert the string to
       * a typed value.
       *
       * @param {string} attribute Name of attribute to deserialize.
       * @param {?string} value of the attribute.
       * @param {*=} type type to deserialize to, defaults to the value
       * returned from `typeForProperty`
       * @return {void}
       * @override
       */
      _attributeToProperty(attribute, value, type) {
        if (!this.__serializing) {
          const map = this.__dataAttributes;
          const property = map && map[attribute] || attribute;
          this[property] = this._deserializeValue(value, type || this.constructor.typeForProperty(property));
        }
      }
      /**
       * Serializes a property to its associated attribute.
       *
       * @suppress {invalidCasts} Closure can't figure out `this` is an element.
       *
       * @param {string} property Property name to reflect.
       * @param {string=} attribute Attribute name to reflect to.
       * @param {*=} value Property value to refect.
       * @return {void}
       * @override
       */
      _propertyToAttribute(property, attribute, value) {
        this.__serializing = true;
        value = arguments.length < 3 ? this[property] : value;
        this._valueToNodeAttribute(
          /** @type {!HTMLElement} */
          this,
          value,
          attribute || this.constructor.attributeNameForProperty(property)
        );
        this.__serializing = false;
      }
      /**
       * Sets a typed value to an HTML attribute on a node.
       *
       * This method calls the `_serializeValue` method to convert the typed
       * value to a string.  If the `_serializeValue` method returns `undefined`,
       * the attribute will be removed (this is the default for boolean
       * type `false`).
       *
       * @param {Element} node Element to set attribute to.
       * @param {*} value Value to serialize.
       * @param {string} attribute Attribute name to serialize to.
       * @return {void}
       * @override
       */
      _valueToNodeAttribute(node, value, attribute) {
        const str = this._serializeValue(value);
        if (attribute === "class" || attribute === "name" || attribute === "slot") {
          node = /** @type {?Element} */
          wrap(node);
        }
        if (str === void 0) {
          node.removeAttribute(attribute);
        } else {
          node.setAttribute(
            attribute,
            // Closure's type for `setAttribute`'s second parameter incorrectly
            // excludes `TrustedScript`.
            str === "" && window.trustedTypes ? (
              /** @type {?} */
              window.trustedTypes.emptyScript
            ) : str
          );
        }
      }
      /**
       * Converts a typed JavaScript value to a string.
       *
       * This method is called when setting JS property values to
       * HTML attributes.  Users may override this method to provide
       * serialization for custom types.
       *
       * @param {*} value Property value to serialize.
       * @return {string | undefined} String serialized from the provided
       * property  value.
       * @override
       */
      _serializeValue(value) {
        switch (typeof value) {
          case "boolean":
            return value ? "" : void 0;
          default:
            return value != null ? value.toString() : void 0;
        }
      }
      /**
       * Converts a string to a typed JavaScript value.
       *
       * This method is called when reading HTML attribute values to
       * JS properties.  Users may override this method to provide
       * deserialization for custom `type`s. Types for `Boolean`, `String`,
       * and `Number` convert attributes to the expected types.
       *
       * @param {?string} value Value to deserialize.
       * @param {*=} type Type to deserialize the string to.
       * @return {*} Typed value deserialized from the provided string.
       * @override
       */
      _deserializeValue(value, type) {
        switch (type) {
          case Boolean:
            return value !== null;
          case Number:
            return Number(value);
          default:
            return value;
        }
      }
    }
    return PropertiesChanged2;
  }
);

// node_modules/@polymer/polymer/lib/mixins/property-accessors.js
var nativeProperties = {};
var proto = HTMLElement.prototype;
while (proto) {
  let props = Object.getOwnPropertyNames(proto);
  for (let i10 = 0; i10 < props.length; i10++) {
    nativeProperties[props[i10]] = true;
  }
  proto = Object.getPrototypeOf(proto);
}
var isTrustedType = (() => {
  if (!window.trustedTypes) {
    return () => false;
  }
  return (val) => trustedTypes.isHTML(val) || trustedTypes.isScript(val) || trustedTypes.isScriptURL(val);
})();
function saveAccessorValue(model, property) {
  if (!nativeProperties[property]) {
    let value = model[property];
    if (value !== void 0) {
      if (model.__data) {
        model._setPendingProperty(property, value);
      } else {
        if (!model.__dataProto) {
          model.__dataProto = {};
        } else if (!model.hasOwnProperty(JSCompiler_renameProperty("__dataProto", model))) {
          model.__dataProto = Object.create(model.__dataProto);
        }
        model.__dataProto[property] = value;
      }
    }
  }
}
var PropertyAccessors = dedupingMixin((superClass) => {
  const base = PropertiesChanged(superClass);
  class PropertyAccessors2 extends base {
    /**
     * Generates property accessors for all attributes in the standard
     * static `observedAttributes` array.
     *
     * Attribute names are mapped to property names using the `dash-case` to
     * `camelCase` convention
     *
     * @return {void}
     * @nocollapse
     */
    static createPropertiesForAttributes() {
      let a$ = (
        /** @type {?} */
        this.observedAttributes
      );
      for (let i10 = 0; i10 < a$.length; i10++) {
        this.prototype._createPropertyAccessor(dashToCamelCase2(a$[i10]));
      }
    }
    /**
     * Returns an attribute name that corresponds to the given property.
     * By default, converts camel to dash case, e.g. `fooBar` to `foo-bar`.
     * @param {string} property Property to convert
     * @return {string} Attribute name corresponding to the given property.
     *
     * @protected
     * @nocollapse
     */
    static attributeNameForProperty(property) {
      return camelToDashCase(property);
    }
    /**
     * Overrides PropertiesChanged implementation to initialize values for
     * accessors created for values that already existed on the element
     * prototype.
     *
     * @return {void}
     * @protected
     * @override
     */
    _initializeProperties() {
      if (this.__dataProto) {
        this._initializeProtoProperties(this.__dataProto);
        this.__dataProto = null;
      }
      super._initializeProperties();
    }
    /**
     * Called at instance time with bag of properties that were overwritten
     * by accessors on the prototype when accessors were created.
     *
     * The default implementation sets these properties back into the
     * setter at instance time.  This method is provided as an override
     * point for customizing or providing more efficient initialization.
     *
     * @param {Object} props Bag of property values that were overwritten
     *   when creating property accessors.
     * @return {void}
     * @protected
     * @override
     */
    _initializeProtoProperties(props) {
      for (let p14 in props) {
        this._setProperty(p14, props[p14]);
      }
    }
    /**
     * Ensures the element has the given attribute. If it does not,
     * assigns the given value to the attribute.
     *
     * @suppress {invalidCasts} Closure can't figure out `this` is infact an
     *     element
     *
     * @param {string} attribute Name of attribute to ensure is set.
     * @param {string} value of the attribute.
     * @return {void}
     * @override
     */
    _ensureAttribute(attribute, value) {
      const el = (
        /** @type {!HTMLElement} */
        this
      );
      if (!el.hasAttribute(attribute)) {
        this._valueToNodeAttribute(el, value, attribute);
      }
    }
    /**
     * Overrides PropertiesChanged implemention to serialize objects as JSON.
     *
     * @param {*} value Property value to serialize.
     * @return {string | undefined} String serialized from the provided property
     *     value.
     * @override
     */
    _serializeValue(value) {
      switch (typeof value) {
        case "object":
          if (value instanceof Date) {
            return value.toString();
          } else if (value) {
            if (isTrustedType(value)) {
              return (
                /** @type {?} */
                value
              );
            }
            try {
              return JSON.stringify(value);
            } catch (x2) {
              return "";
            }
          }
        default:
          return super._serializeValue(value);
      }
    }
    /**
     * Converts a string to a typed JavaScript value.
     *
     * This method is called by Polymer when reading HTML attribute values to
     * JS properties.  Users may override this method on Polymer element
     * prototypes to provide deserialization for custom `type`s.  Note,
     * the `type` argument is the value of the `type` field provided in the
     * `properties` configuration object for a given property, and is
     * by convention the constructor for the type to deserialize.
     *
     *
     * @param {?string} value Attribute value to deserialize.
     * @param {*=} type Type to deserialize the string to.
     * @return {*} Typed value deserialized from the provided string.
     * @override
     */
    _deserializeValue(value, type) {
      let outValue;
      switch (type) {
        case Object:
          try {
            outValue = JSON.parse(
              /** @type {string} */
              value
            );
          } catch (x2) {
            outValue = value;
          }
          break;
        case Array:
          try {
            outValue = JSON.parse(
              /** @type {string} */
              value
            );
          } catch (x2) {
            outValue = null;
            console.warn(`Polymer::Attributes: couldn't decode Array as JSON: ${value}`);
          }
          break;
        case Date:
          outValue = isNaN(value) ? String(value) : Number(value);
          outValue = new Date(outValue);
          break;
        default:
          outValue = super._deserializeValue(value, type);
          break;
      }
      return outValue;
    }
    /* eslint-enable no-fallthrough */
    /**
     * Overrides PropertiesChanged implementation to save existing prototype
     * property value so that it can be reset.
     * @param {string} property Name of the property
     * @param {boolean=} readOnly When true, no setter is created
     *
     * When calling on a prototype, any overwritten values are saved in
     * `__dataProto`, and it is up to the subclasser to decide how/when
     * to set those properties back into the accessor.  When calling on an
     * instance, the overwritten value is set via `_setPendingProperty`,
     * and the user should call `_invalidateProperties` or `_flushProperties`
     * for the values to take effect.
     * @protected
     * @return {void}
     * @override
     */
    _definePropertyAccessor(property, readOnly) {
      saveAccessorValue(this, property);
      super._definePropertyAccessor(property, readOnly);
    }
    /**
     * Returns true if this library created an accessor for the given property.
     *
     * @param {string} property Property name
     * @return {boolean} True if an accessor was created
     * @override
     */
    _hasAccessor(property) {
      return this.__dataHasAccessor && this.__dataHasAccessor[property];
    }
    /**
     * Returns true if the specified property has a pending change.
     *
     * @param {string} prop Property name
     * @return {boolean} True if property has a pending change
     * @protected
     * @override
     */
    _isPropertyPending(prop) {
      return Boolean(this.__dataPending && prop in this.__dataPending);
    }
  }
  return PropertyAccessors2;
});

// node_modules/@polymer/polymer/lib/mixins/template-stamp.js
var templateExtensions = {
  "dom-if": true,
  "dom-repeat": true
};
var placeholderBugDetect = false;
var placeholderBug = false;
function hasPlaceholderBug() {
  if (!placeholderBugDetect) {
    placeholderBugDetect = true;
    const t5 = document.createElement("textarea");
    t5.placeholder = "a";
    placeholderBug = t5.placeholder === t5.textContent;
  }
  return placeholderBug;
}
function fixPlaceholder(node) {
  if (hasPlaceholderBug() && node.localName === "textarea" && node.placeholder && node.placeholder === node.textContent) {
    node.textContent = null;
  }
}
var copyAttributeWithTemplateEventPolicy = (() => {
  const polymerTemplateEventAttributePolicy = window.trustedTypes && window.trustedTypes.createPolicy(
    "polymer-template-event-attribute-policy",
    {
      createScript: (x2) => x2
    }
  );
  return (dest, src, name) => {
    const value = src.getAttribute(name);
    if (polymerTemplateEventAttributePolicy && name.startsWith("on-")) {
      dest.setAttribute(
        name,
        polymerTemplateEventAttributePolicy.createScript(value, name)
      );
      return;
    }
    dest.setAttribute(name, value);
  };
})();
function wrapTemplateExtension(node) {
  let is = node.getAttribute("is");
  if (is && templateExtensions[is]) {
    let t5 = node;
    t5.removeAttribute("is");
    node = t5.ownerDocument.createElement(is);
    t5.parentNode.replaceChild(node, t5);
    node.appendChild(t5);
    while (t5.attributes.length) {
      const { name } = t5.attributes[0];
      copyAttributeWithTemplateEventPolicy(node, t5, name);
      t5.removeAttribute(name);
    }
  }
  return node;
}
function findTemplateNode(root2, nodeInfo) {
  let parent = nodeInfo.parentInfo && findTemplateNode(root2, nodeInfo.parentInfo);
  if (parent) {
    for (let n19 = parent.firstChild, i10 = 0; n19; n19 = n19.nextSibling) {
      if (nodeInfo.parentIndex === i10++) {
        return n19;
      }
    }
  } else {
    return root2;
  }
}
function applyIdToMap(inst, map, node, nodeInfo) {
  if (nodeInfo.id) {
    map[nodeInfo.id] = node;
  }
}
function applyEventListener(inst, node, nodeInfo) {
  if (nodeInfo.events && nodeInfo.events.length) {
    for (let j5 = 0, e$ = nodeInfo.events, e17; j5 < e$.length && (e17 = e$[j5]); j5++) {
      inst._addMethodEventListenerToNode(node, e17.name, e17.value, inst);
    }
  }
}
function applyTemplateInfo(inst, node, nodeInfo, parentTemplateInfo) {
  if (nodeInfo.templateInfo) {
    node._templateInfo = nodeInfo.templateInfo;
    node._parentTemplateInfo = parentTemplateInfo;
  }
}
function createNodeEventHandler(context, eventName, methodName) {
  context = context._methodHost || context;
  let handler = function(e17) {
    if (context[methodName]) {
      context[methodName](e17, e17.detail);
    } else {
      console.warn("listener method `" + methodName + "` not defined");
    }
  };
  return handler;
}
var TemplateStamp = dedupingMixin(
  /**
   * @template T
   * @param {function(new:T)} superClass Class to apply mixin to.
   * @return {function(new:T)} superClass with mixin applied.
   */
  (superClass) => {
    class TemplateStamp2 extends superClass {
      /**
       * Scans a template to produce template metadata.
       *
       * Template-specific metadata are stored in the object returned, and node-
       * specific metadata are stored in objects in its flattened `nodeInfoList`
       * array.  Only nodes in the template that were parsed as nodes of
       * interest contain an object in `nodeInfoList`.  Each `nodeInfo` object
       * contains an `index` (`childNodes` index in parent) and optionally
       * `parent`, which points to node info of its parent (including its index).
       *
       * The template metadata object returned from this method has the following
       * structure (many fields optional):
       *
       * ```js
       *   {
       *     // Flattened list of node metadata (for nodes that generated metadata)
       *     nodeInfoList: [
       *       {
       *         // `id` attribute for any nodes with id's for generating `$` map
       *         id: {string},
       *         // `on-event="handler"` metadata
       *         events: [
       *           {
       *             name: {string},   // event name
       *             value: {string},  // handler method name
       *           }, ...
       *         ],
       *         // Notes when the template contained a `<slot>` for shady DOM
       *         // optimization purposes
       *         hasInsertionPoint: {boolean},
       *         // For nested `<template>`` nodes, nested template metadata
       *         templateInfo: {object}, // nested template metadata
       *         // Metadata to allow efficient retrieval of instanced node
       *         // corresponding to this metadata
       *         parentInfo: {number},   // reference to parent nodeInfo>
       *         parentIndex: {number},  // index in parent's `childNodes` collection
       *         infoIndex: {number},    // index of this `nodeInfo` in `templateInfo.nodeInfoList`
       *       },
       *       ...
       *     ],
       *     // When true, the template had the `strip-whitespace` attribute
       *     // or was nested in a template with that setting
       *     stripWhitespace: {boolean},
       *     // For nested templates, nested template content is moved into
       *     // a document fragment stored here; this is an optimization to
       *     // avoid the cost of nested template cloning
       *     content: {DocumentFragment}
       *   }
       * ```
       *
       * This method kicks off a recursive treewalk as follows:
       *
       * ```
       *    _parseTemplate <---------------------+
       *      _parseTemplateContent              |
       *        _parseTemplateNode  <------------|--+
       *          _parseTemplateNestedTemplate --+  |
       *          _parseTemplateChildNodes ---------+
       *          _parseTemplateNodeAttributes
       *            _parseTemplateNodeAttribute
       *
       * ```
       *
       * These methods may be overridden to add custom metadata about templates
       * to either `templateInfo` or `nodeInfo`.
       *
       * Note that this method may be destructive to the template, in that
       * e.g. event annotations may be removed after being noted in the
       * template metadata.
       *
       * @param {!HTMLTemplateElement} template Template to parse
       * @param {TemplateInfo=} outerTemplateInfo Template metadata from the outer
       *   template, for parsing nested templates
       * @return {!TemplateInfo} Parsed template metadata
       * @nocollapse
       */
      static _parseTemplate(template2, outerTemplateInfo) {
        if (!template2._templateInfo) {
          let templateInfo = template2._templateInfo = {};
          templateInfo.nodeInfoList = [];
          templateInfo.nestedTemplate = Boolean(outerTemplateInfo);
          templateInfo.stripWhiteSpace = outerTemplateInfo && outerTemplateInfo.stripWhiteSpace || template2.hasAttribute && template2.hasAttribute("strip-whitespace");
          this._parseTemplateContent(
            template2,
            templateInfo,
            /** @type {?} */
            { parent: null }
          );
        }
        return template2._templateInfo;
      }
      /**
       * See docs for _parseTemplateNode.
       *
       * @param {!HTMLTemplateElement} template .
       * @param {!TemplateInfo} templateInfo .
       * @param {!NodeInfo} nodeInfo .
       * @return {boolean} .
       * @nocollapse
       */
      static _parseTemplateContent(template2, templateInfo, nodeInfo) {
        return this._parseTemplateNode(template2.content, templateInfo, nodeInfo);
      }
      /**
       * Parses template node and adds template and node metadata based on
       * the current node, and its `childNodes` and `attributes`.
       *
       * This method may be overridden to add custom node or template specific
       * metadata based on this node.
       *
       * @param {Node} node Node to parse
       * @param {!TemplateInfo} templateInfo Template metadata for current template
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @return {boolean} `true` if the visited node added node-specific
       *   metadata to `nodeInfo`
       * @nocollapse
       */
      static _parseTemplateNode(node, templateInfo, nodeInfo) {
        let noted = false;
        let element = (
          /** @type {!HTMLTemplateElement} */
          node
        );
        if (element.localName == "template" && !element.hasAttribute("preserve-content")) {
          noted = this._parseTemplateNestedTemplate(element, templateInfo, nodeInfo) || noted;
        } else if (element.localName === "slot") {
          templateInfo.hasInsertionPoint = true;
        }
        fixPlaceholder(element);
        if (element.firstChild) {
          this._parseTemplateChildNodes(element, templateInfo, nodeInfo);
        }
        if (element.hasAttributes && element.hasAttributes()) {
          noted = this._parseTemplateNodeAttributes(element, templateInfo, nodeInfo) || noted;
        }
        return noted || nodeInfo.noted;
      }
      /**
       * Parses template child nodes for the given root node.
       *
       * This method also wraps whitelisted legacy template extensions
       * (`is="dom-if"` and `is="dom-repeat"`) with their equivalent element
       * wrappers, collapses text nodes, and strips whitespace from the template
       * if the `templateInfo.stripWhitespace` setting was provided.
       *
       * @param {Node} root Root node whose `childNodes` will be parsed
       * @param {!TemplateInfo} templateInfo Template metadata for current template
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @return {void}
       */
      static _parseTemplateChildNodes(root2, templateInfo, nodeInfo) {
        if (root2.localName === "script" || root2.localName === "style") {
          return;
        }
        for (let node = root2.firstChild, parentIndex = 0, next; node; node = next) {
          if (node.localName == "template") {
            node = wrapTemplateExtension(node);
          }
          next = node.nextSibling;
          if (node.nodeType === Node.TEXT_NODE) {
            let n19 = next;
            while (n19 && n19.nodeType === Node.TEXT_NODE) {
              node.textContent += n19.textContent;
              next = n19.nextSibling;
              root2.removeChild(n19);
              n19 = next;
            }
            if (templateInfo.stripWhiteSpace && !node.textContent.trim()) {
              root2.removeChild(node);
              continue;
            }
          }
          let childInfo = (
            /** @type {!NodeInfo} */
            { parentIndex, parentInfo: nodeInfo }
          );
          if (this._parseTemplateNode(node, templateInfo, childInfo)) {
            childInfo.infoIndex = templateInfo.nodeInfoList.push(childInfo) - 1;
          }
          if (node.parentNode) {
            parentIndex++;
          }
        }
      }
      /**
       * Parses template content for the given nested `<template>`.
       *
       * Nested template info is stored as `templateInfo` in the current node's
       * `nodeInfo`. `template.content` is removed and stored in `templateInfo`.
       * It will then be the responsibility of the host to set it back to the
       * template and for users stamping nested templates to use the
       * `_contentForTemplate` method to retrieve the content for this template
       * (an optimization to avoid the cost of cloning nested template content).
       *
       * @param {HTMLTemplateElement} node Node to parse (a <template>)
       * @param {TemplateInfo} outerTemplateInfo Template metadata for current template
       *   that includes the template `node`
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @return {boolean} `true` if the visited node added node-specific
       *   metadata to `nodeInfo`
       * @nocollapse
       */
      static _parseTemplateNestedTemplate(node, outerTemplateInfo, nodeInfo) {
        let element = (
          /** @type {!HTMLTemplateElement} */
          node
        );
        let templateInfo = this._parseTemplate(element, outerTemplateInfo);
        let content = templateInfo.content = element.content.ownerDocument.createDocumentFragment();
        content.appendChild(element.content);
        nodeInfo.templateInfo = templateInfo;
        return true;
      }
      /**
       * Parses template node attributes and adds node metadata to `nodeInfo`
       * for nodes of interest.
       *
       * @param {Element} node Node to parse
       * @param {!TemplateInfo} templateInfo Template metadata for current
       *     template
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @return {boolean} `true` if the visited node added node-specific
       *   metadata to `nodeInfo`
       * @nocollapse
       */
      static _parseTemplateNodeAttributes(node, templateInfo, nodeInfo) {
        let noted = false;
        let attrs = Array.from(node.attributes);
        for (let i10 = attrs.length - 1, a17; a17 = attrs[i10]; i10--) {
          noted = this._parseTemplateNodeAttribute(node, templateInfo, nodeInfo, a17.name, a17.value) || noted;
        }
        return noted;
      }
      /**
       * Parses a single template node attribute and adds node metadata to
       * `nodeInfo` for attributes of interest.
       *
       * This implementation adds metadata for `on-event="handler"` attributes
       * and `id` attributes.
       *
       * @param {Element} node Node to parse
       * @param {!TemplateInfo} templateInfo Template metadata for current template
       * @param {!NodeInfo} nodeInfo Node metadata for current template.
       * @param {string} name Attribute name
       * @param {string} value Attribute value
       * @return {boolean} `true` if the visited node added node-specific
       *   metadata to `nodeInfo`
       * @nocollapse
       */
      static _parseTemplateNodeAttribute(node, templateInfo, nodeInfo, name, value) {
        if (name.slice(0, 3) === "on-") {
          node.removeAttribute(name);
          nodeInfo.events = nodeInfo.events || [];
          nodeInfo.events.push({
            name: name.slice(3),
            value
          });
          return true;
        } else if (name === "id") {
          nodeInfo.id = value;
          return true;
        }
        return false;
      }
      /**
       * Returns the `content` document fragment for a given template.
       *
       * For nested templates, Polymer performs an optimization to cache nested
       * template content to avoid the cost of cloning deeply nested templates.
       * This method retrieves the cached content for a given template.
       *
       * @param {HTMLTemplateElement} template Template to retrieve `content` for
       * @return {DocumentFragment} Content fragment
       * @nocollapse
       */
      static _contentForTemplate(template2) {
        let templateInfo = (
          /** @type {HTMLTemplateElementWithInfo} */
          template2._templateInfo
        );
        return templateInfo && templateInfo.content || template2.content;
      }
      /**
       * Clones the provided template content and returns a document fragment
       * containing the cloned dom.
       *
       * The template is parsed (once and memoized) using this library's
       * template parsing features, and provides the following value-added
       * features:
       * * Adds declarative event listeners for `on-event="handler"` attributes
       * * Generates an "id map" for all nodes with id's under `$` on returned
       *   document fragment
       * * Passes template info including `content` back to templates as
       *   `_templateInfo` (a performance optimization to avoid deep template
       *   cloning)
       *
       * Note that the memoized template parsing process is destructive to the
       * template: attributes for bindings and declarative event listeners are
       * removed after being noted in notes, and any nested `<template>.content`
       * is removed and stored in notes as well.
       *
       * @param {!HTMLTemplateElement} template Template to stamp
       * @param {TemplateInfo=} templateInfo Optional template info associated
       *   with the template to be stamped; if omitted the template will be
       *   automatically parsed.
       * @return {!StampedTemplate} Cloned template content
       * @override
       */
      _stampTemplate(template2, templateInfo) {
        if (template2 && !template2.content && window.HTMLTemplateElement && HTMLTemplateElement.decorate) {
          HTMLTemplateElement.decorate(template2);
        }
        templateInfo = templateInfo || this.constructor._parseTemplate(template2);
        let nodeInfo = templateInfo.nodeInfoList;
        let content = templateInfo.content || template2.content;
        let dom = (
          /** @type {DocumentFragment} */
          document.importNode(content, true)
        );
        dom.__noInsertionPoint = !templateInfo.hasInsertionPoint;
        let nodes = dom.nodeList = new Array(nodeInfo.length);
        dom.$ = {};
        for (let i10 = 0, l17 = nodeInfo.length, info; i10 < l17 && (info = nodeInfo[i10]); i10++) {
          let node = nodes[i10] = findTemplateNode(dom, info);
          applyIdToMap(this, dom.$, node, info);
          applyTemplateInfo(this, node, info, templateInfo);
          applyEventListener(this, node, info);
        }
        dom = /** @type {!StampedTemplate} */
        dom;
        return dom;
      }
      /**
       * Adds an event listener by method name for the event provided.
       *
       * This method generates a handler function that looks up the method
       * name at handling time.
       *
       * @param {!EventTarget} node Node to add listener on
       * @param {string} eventName Name of event
       * @param {string} methodName Name of method
       * @param {*=} context Context the method will be called on (defaults
       *   to `node`)
       * @return {Function} Generated handler function
       * @override
       */
      _addMethodEventListenerToNode(node, eventName, methodName, context) {
        context = context || node;
        let handler = createNodeEventHandler(context, eventName, methodName);
        this._addEventListenerToNode(node, eventName, handler);
        return handler;
      }
      /**
       * Override point for adding custom or simulated event handling.
       *
       * @param {!EventTarget} node Node to add event listener to
       * @param {string} eventName Name of event
       * @param {function(!Event):void} handler Listener function to add
       * @return {void}
       * @override
       */
      _addEventListenerToNode(node, eventName, handler) {
        node.addEventListener(eventName, handler);
      }
      /**
       * Override point for adding custom or simulated event handling.
       *
       * @param {!EventTarget} node Node to remove event listener from
       * @param {string} eventName Name of event
       * @param {function(!Event):void} handler Listener function to remove
       * @return {void}
       * @override
       */
      _removeEventListenerFromNode(node, eventName, handler) {
        node.removeEventListener(eventName, handler);
      }
    }
    return TemplateStamp2;
  }
);

// node_modules/@polymer/polymer/lib/mixins/property-effects.js
var dedupeId2 = 0;
var NOOP = [];
var TYPES = {
  COMPUTE: "__computeEffects",
  REFLECT: "__reflectEffects",
  NOTIFY: "__notifyEffects",
  PROPAGATE: "__propagateEffects",
  OBSERVE: "__observeEffects",
  READ_ONLY: "__readOnly"
};
var COMPUTE_INFO = "__computeInfo";
var capitalAttributeRegex = /[A-Z]/;
function ensureOwnEffectMap(model, type, cloneArrays) {
  let effects = model[type];
  if (!effects) {
    effects = model[type] = {};
  } else if (!model.hasOwnProperty(type)) {
    effects = model[type] = Object.create(model[type]);
    if (cloneArrays) {
      for (let p14 in effects) {
        let protoFx = effects[p14];
        let instFx = effects[p14] = Array(protoFx.length);
        for (let i10 = 0; i10 < protoFx.length; i10++) {
          instFx[i10] = protoFx[i10];
        }
      }
    }
  }
  return effects;
}
function runEffects(inst, effects, props, oldProps, hasPaths, extraArgs) {
  if (effects) {
    let ran = false;
    const id = dedupeId2++;
    for (let prop in props) {
      let rootProperty = hasPaths ? root(prop) : prop;
      let fxs = effects[rootProperty];
      if (fxs) {
        for (let i10 = 0, l17 = fxs.length, fx; i10 < l17 && (fx = fxs[i10]); i10++) {
          if ((!fx.info || fx.info.lastRun !== id) && (!hasPaths || pathMatchesTrigger(prop, fx.trigger))) {
            if (fx.info) {
              fx.info.lastRun = id;
            }
            fx.fn(inst, prop, props, oldProps, fx.info, hasPaths, extraArgs);
            ran = true;
          }
        }
      }
    }
    return ran;
  }
  return false;
}
function runEffectsForProperty(inst, effects, dedupeId3, prop, props, oldProps, hasPaths, extraArgs) {
  let ran = false;
  let rootProperty = hasPaths ? root(prop) : prop;
  let fxs = effects[rootProperty];
  if (fxs) {
    for (let i10 = 0, l17 = fxs.length, fx; i10 < l17 && (fx = fxs[i10]); i10++) {
      if ((!fx.info || fx.info.lastRun !== dedupeId3) && (!hasPaths || pathMatchesTrigger(prop, fx.trigger))) {
        if (fx.info) {
          fx.info.lastRun = dedupeId3;
        }
        fx.fn(inst, prop, props, oldProps, fx.info, hasPaths, extraArgs);
        ran = true;
      }
    }
  }
  return ran;
}
function pathMatchesTrigger(path, trigger) {
  if (trigger) {
    let triggerPath = (
      /** @type {string} */
      trigger.name
    );
    return triggerPath == path || !!(trigger.structured && isAncestor(triggerPath, path)) || !!(trigger.wildcard && isDescendant(triggerPath, path));
  } else {
    return true;
  }
}
function runObserverEffect(inst, property, props, oldProps, info) {
  let fn = typeof info.method === "string" ? inst[info.method] : info.method;
  let changedProp = info.property;
  if (fn) {
    fn.call(inst, inst.__data[changedProp], oldProps[changedProp]);
  } else if (!info.dynamicFn) {
    console.warn("observer method `" + info.method + "` not defined");
  }
}
function runNotifyEffects(inst, notifyProps, props, oldProps, hasPaths) {
  let fxs = inst[TYPES.NOTIFY];
  let notified;
  let id = dedupeId2++;
  for (let prop in notifyProps) {
    if (notifyProps[prop]) {
      if (fxs && runEffectsForProperty(inst, fxs, id, prop, props, oldProps, hasPaths)) {
        notified = true;
      } else if (hasPaths && notifyPath(inst, prop, props)) {
        notified = true;
      }
    }
  }
  let host;
  if (notified && (host = inst.__dataHost) && host._invalidateProperties) {
    host._invalidateProperties();
  }
}
function notifyPath(inst, path, props) {
  let rootProperty = root(path);
  if (rootProperty !== path) {
    let eventName = camelToDashCase(rootProperty) + "-changed";
    dispatchNotifyEvent(inst, eventName, props[path], path);
    return true;
  }
  return false;
}
function dispatchNotifyEvent(inst, eventName, value, path) {
  let detail = {
    value,
    queueProperty: true
  };
  if (path) {
    detail.path = path;
  }
  wrap(
    /** @type {!HTMLElement} */
    inst
  ).dispatchEvent(new CustomEvent(eventName, { detail }));
}
function runNotifyEffect(inst, property, props, oldProps, info, hasPaths) {
  let rootProperty = hasPaths ? root(property) : property;
  let path = rootProperty != property ? property : null;
  let value = path ? get(inst, path) : inst.__data[property];
  if (path && value === void 0) {
    value = props[property];
  }
  dispatchNotifyEvent(inst, info.eventName, value, path);
}
function handleNotification(event, inst, fromProp, toPath, negate) {
  let value;
  let detail = (
    /** @type {Object} */
    event.detail
  );
  let fromPath = detail && detail.path;
  if (fromPath) {
    toPath = translate(fromProp, toPath, fromPath);
    value = detail && detail.value;
  } else {
    value = event.currentTarget[fromProp];
  }
  value = negate ? !value : value;
  if (!inst[TYPES.READ_ONLY] || !inst[TYPES.READ_ONLY][toPath]) {
    if (inst._setPendingPropertyOrPath(toPath, value, true, Boolean(fromPath)) && (!detail || !detail.queueProperty)) {
      inst._invalidateProperties();
    }
  }
}
function runReflectEffect(inst, property, props, oldProps, info) {
  let value = inst.__data[property];
  if (sanitizeDOMValue) {
    value = sanitizeDOMValue(
      value,
      info.attrName,
      "attribute",
      /** @type {Node} */
      inst
    );
  }
  inst._propertyToAttribute(property, info.attrName, value);
}
function runComputedEffects(inst, changedProps, oldProps, hasPaths) {
  let computeEffects = inst[TYPES.COMPUTE];
  if (computeEffects) {
    if (orderedComputed) {
      dedupeId2++;
      const order = getComputedOrder(inst);
      const queue = [];
      for (let p14 in changedProps) {
        enqueueEffectsFor(p14, computeEffects, queue, order, hasPaths);
      }
      let info;
      while (info = queue.shift()) {
        if (runComputedEffect(inst, "", changedProps, oldProps, info)) {
          enqueueEffectsFor(info.methodInfo, computeEffects, queue, order, hasPaths);
        }
      }
      Object.assign(
        /** @type {!Object} */
        oldProps,
        inst.__dataOld
      );
      Object.assign(
        /** @type {!Object} */
        changedProps,
        inst.__dataPending
      );
      inst.__dataPending = null;
    } else {
      let inputProps = changedProps;
      while (runEffects(inst, computeEffects, inputProps, oldProps, hasPaths)) {
        Object.assign(
          /** @type {!Object} */
          oldProps,
          inst.__dataOld
        );
        Object.assign(
          /** @type {!Object} */
          changedProps,
          inst.__dataPending
        );
        inputProps = inst.__dataPending;
        inst.__dataPending = null;
      }
    }
  }
}
var insertEffect = (info, queue, order) => {
  let start = 0;
  let end = queue.length - 1;
  let idx = -1;
  while (start <= end) {
    const mid = start + end >> 1;
    const cmp = order.get(queue[mid].methodInfo) - order.get(info.methodInfo);
    if (cmp < 0) {
      start = mid + 1;
    } else if (cmp > 0) {
      end = mid - 1;
    } else {
      idx = mid;
      break;
    }
  }
  if (idx < 0) {
    idx = end + 1;
  }
  queue.splice(idx, 0, info);
};
var enqueueEffectsFor = (prop, computeEffects, queue, order, hasPaths) => {
  const rootProperty = hasPaths ? root(prop) : prop;
  const fxs = computeEffects[rootProperty];
  if (fxs) {
    for (let i10 = 0; i10 < fxs.length; i10++) {
      const fx = fxs[i10];
      if (fx.info.lastRun !== dedupeId2 && (!hasPaths || pathMatchesTrigger(prop, fx.trigger))) {
        fx.info.lastRun = dedupeId2;
        insertEffect(fx.info, queue, order);
      }
    }
  }
};
function getComputedOrder(inst) {
  let ordered = inst.constructor.__orderedComputedDeps;
  if (!ordered) {
    ordered = /* @__PURE__ */ new Map();
    const effects = inst[TYPES.COMPUTE];
    let { counts, ready, total } = dependencyCounts(inst);
    let curr;
    while (curr = ready.shift()) {
      ordered.set(curr, ordered.size);
      const computedByCurr = effects[curr];
      if (computedByCurr) {
        computedByCurr.forEach((fx) => {
          const computedProp = fx.info.methodInfo;
          --total;
          if (--counts[computedProp] === 0) {
            ready.push(computedProp);
          }
        });
      }
    }
    if (total !== 0) {
      const el = (
        /** @type {HTMLElement} */
        inst
      );
      console.warn(`Computed graph for ${el.localName} incomplete; circular?`);
    }
    inst.constructor.__orderedComputedDeps = ordered;
  }
  return ordered;
}
function dependencyCounts(inst) {
  const infoForComputed = inst[COMPUTE_INFO];
  const counts = {};
  const computedDeps = inst[TYPES.COMPUTE];
  const ready = [];
  let total = 0;
  for (let p14 in infoForComputed) {
    const info = infoForComputed[p14];
    total += counts[p14] = info.args.filter((a17) => !a17.literal).length + (info.dynamicFn ? 1 : 0);
  }
  for (let p14 in computedDeps) {
    if (!infoForComputed[p14]) {
      ready.push(p14);
    }
  }
  return { counts, ready, total };
}
function runComputedEffect(inst, property, changedProps, oldProps, info) {
  let result = runMethodEffect(inst, property, changedProps, oldProps, info);
  if (result === NOOP) {
    return false;
  }
  let computedProp = info.methodInfo;
  if (inst.__dataHasAccessor && inst.__dataHasAccessor[computedProp]) {
    return inst._setPendingProperty(computedProp, result, true);
  } else {
    inst[computedProp] = result;
    return false;
  }
}
function computeLinkedPaths(inst, path, value) {
  let links = inst.__dataLinkedPaths;
  if (links) {
    let link;
    for (let a17 in links) {
      let b7 = links[a17];
      if (isDescendant(a17, path)) {
        link = translate(a17, b7, path);
        inst._setPendingPropertyOrPath(link, value, true, true);
      } else if (isDescendant(b7, path)) {
        link = translate(b7, a17, path);
        inst._setPendingPropertyOrPath(link, value, true, true);
      }
    }
  }
}
function addBinding(constructor, templateInfo, nodeInfo, kind, target, parts, literal) {
  nodeInfo.bindings = nodeInfo.bindings || [];
  let binding = { kind, target, parts, literal, isCompound: parts.length !== 1 };
  nodeInfo.bindings.push(binding);
  if (shouldAddListener(binding)) {
    let { event, negate } = binding.parts[0];
    binding.listenerEvent = event || camelToDashCase(target) + "-changed";
    binding.listenerNegate = negate;
  }
  let index = templateInfo.nodeInfoList.length;
  for (let i10 = 0; i10 < binding.parts.length; i10++) {
    let part = binding.parts[i10];
    part.compoundIndex = i10;
    addEffectForBindingPart(constructor, templateInfo, binding, part, index);
  }
}
function addEffectForBindingPart(constructor, templateInfo, binding, part, index) {
  if (!part.literal) {
    if (binding.kind === "attribute" && binding.target[0] === "-") {
      console.warn("Cannot set attribute " + binding.target + ' because "-" is not a valid attribute starting character');
    } else {
      let dependencies = part.dependencies;
      let info = { index, binding, part, evaluator: constructor };
      for (let j5 = 0; j5 < dependencies.length; j5++) {
        let trigger = dependencies[j5];
        if (typeof trigger == "string") {
          trigger = parseArg(trigger);
          trigger.wildcard = true;
        }
        constructor._addTemplatePropertyEffect(templateInfo, trigger.rootProperty, {
          fn: runBindingEffect,
          info,
          trigger
        });
      }
    }
  }
}
function runBindingEffect(inst, path, props, oldProps, info, hasPaths, nodeList) {
  let node = nodeList[info.index];
  let binding = info.binding;
  let part = info.part;
  if (hasPaths && part.source && path.length > part.source.length && binding.kind == "property" && !binding.isCompound && node.__isPropertyEffectsClient && node.__dataHasAccessor && node.__dataHasAccessor[binding.target]) {
    let value = props[path];
    path = translate(part.source, binding.target, path);
    if (node._setPendingPropertyOrPath(path, value, false, true)) {
      inst._enqueueClient(node);
    }
  } else {
    let value = info.evaluator._evaluateBinding(inst, part, path, props, oldProps, hasPaths);
    if (value !== NOOP) {
      applyBindingValue(inst, node, binding, part, value);
    }
  }
}
function applyBindingValue(inst, node, binding, part, value) {
  value = computeBindingValue(node, value, binding, part);
  if (sanitizeDOMValue) {
    value = sanitizeDOMValue(value, binding.target, binding.kind, node);
  }
  if (binding.kind == "attribute") {
    inst._valueToNodeAttribute(
      /** @type {Element} */
      node,
      value,
      binding.target
    );
  } else {
    let prop = binding.target;
    if (node.__isPropertyEffectsClient && node.__dataHasAccessor && node.__dataHasAccessor[prop]) {
      if (!node[TYPES.READ_ONLY] || !node[TYPES.READ_ONLY][prop]) {
        if (node._setPendingProperty(prop, value)) {
          inst._enqueueClient(node);
        }
      }
    } else {
      inst._setUnmanagedPropertyToNode(node, prop, value);
    }
  }
}
function computeBindingValue(node, value, binding, part) {
  if (binding.isCompound) {
    let storage = node.__dataCompoundStorage[binding.target];
    storage[part.compoundIndex] = value;
    value = storage.join("");
  }
  if (binding.kind !== "attribute") {
    if (binding.target === "textContent" || binding.target === "value" && (node.localName === "input" || node.localName === "textarea")) {
      value = value == void 0 ? "" : value;
    }
  }
  return value;
}
function shouldAddListener(binding) {
  return Boolean(binding.target) && binding.kind != "attribute" && binding.kind != "text" && !binding.isCompound && binding.parts[0].mode === "{";
}
function setupBindings(inst, templateInfo) {
  let { nodeList, nodeInfoList } = templateInfo;
  if (nodeInfoList.length) {
    for (let i10 = 0; i10 < nodeInfoList.length; i10++) {
      let info = nodeInfoList[i10];
      let node = nodeList[i10];
      let bindings = info.bindings;
      if (bindings) {
        for (let i11 = 0; i11 < bindings.length; i11++) {
          let binding = bindings[i11];
          setupCompoundStorage(node, binding);
          addNotifyListener(node, inst, binding);
        }
      }
      node.__dataHost = inst;
    }
  }
}
function setupCompoundStorage(node, binding) {
  if (binding.isCompound) {
    let storage = node.__dataCompoundStorage || (node.__dataCompoundStorage = {});
    let parts = binding.parts;
    let literals = new Array(parts.length);
    for (let j5 = 0; j5 < parts.length; j5++) {
      literals[j5] = parts[j5].literal;
    }
    let target = binding.target;
    storage[target] = literals;
    if (binding.literal && binding.kind == "property") {
      if (target === "className") {
        node = wrap(node);
      }
      node[target] = binding.literal;
    }
  }
}
function addNotifyListener(node, inst, binding) {
  if (binding.listenerEvent) {
    let part = binding.parts[0];
    node.addEventListener(binding.listenerEvent, function(e17) {
      handleNotification(e17, inst, binding.target, part.source, part.negate);
    });
  }
}
function createMethodEffect(model, sig, type, effectFn, methodInfo, dynamicFn) {
  dynamicFn = sig.static || dynamicFn && (typeof dynamicFn !== "object" || dynamicFn[sig.methodName]);
  let info = {
    methodName: sig.methodName,
    args: sig.args,
    methodInfo,
    dynamicFn
  };
  for (let i10 = 0, arg; i10 < sig.args.length && (arg = sig.args[i10]); i10++) {
    if (!arg.literal) {
      model._addPropertyEffect(arg.rootProperty, type, {
        fn: effectFn,
        info,
        trigger: arg
      });
    }
  }
  if (dynamicFn) {
    model._addPropertyEffect(sig.methodName, type, {
      fn: effectFn,
      info
    });
  }
  return info;
}
function runMethodEffect(inst, property, props, oldProps, info) {
  let context = inst._methodHost || inst;
  let fn = context[info.methodName];
  if (fn) {
    let args = inst._marshalArgs(info.args, property, props);
    return args === NOOP ? NOOP : fn.apply(context, args);
  } else if (!info.dynamicFn) {
    console.warn("method `" + info.methodName + "` not defined");
  }
}
var emptyArray = [];
var IDENT = "(?:[a-zA-Z_$][\\w.:$\\-*]*)";
var NUMBER = "(?:[-+]?[0-9]*\\.?[0-9]+(?:[eE][-+]?[0-9]+)?)";
var SQUOTE_STRING = "(?:'(?:[^'\\\\]|\\\\.)*')";
var DQUOTE_STRING = '(?:"(?:[^"\\\\]|\\\\.)*")';
var STRING = "(?:" + SQUOTE_STRING + "|" + DQUOTE_STRING + ")";
var ARGUMENT = "(?:(" + IDENT + "|" + NUMBER + "|" + STRING + ")\\s*)";
var ARGUMENTS = "(?:" + ARGUMENT + "(?:,\\s*" + ARGUMENT + ")*)";
var ARGUMENT_LIST = "(?:\\(\\s*(?:" + ARGUMENTS + "?)\\)\\s*)";
var BINDING = "(" + IDENT + "\\s*" + ARGUMENT_LIST + "?)";
var OPEN_BRACKET = "(\\[\\[|{{)\\s*";
var CLOSE_BRACKET = "(?:]]|}})";
var NEGATE = "(?:(!)\\s*)?";
var EXPRESSION = OPEN_BRACKET + NEGATE + BINDING + CLOSE_BRACKET;
var bindingRegex = new RegExp(EXPRESSION, "g");
function literalFromParts(parts) {
  let s22 = "";
  for (let i10 = 0; i10 < parts.length; i10++) {
    let literal = parts[i10].literal;
    s22 += literal || "";
  }
  return s22;
}
function parseMethod(expression) {
  let m11 = expression.match(/([^\s]+?)\(([\s\S]*)\)/);
  if (m11) {
    let methodName = m11[1];
    let sig = { methodName, static: true, args: emptyArray };
    if (m11[2].trim()) {
      let args = m11[2].replace(/\\,/g, "&comma;").split(",");
      return parseArgs(args, sig);
    } else {
      return sig;
    }
  }
  return null;
}
function parseArgs(argList, sig) {
  sig.args = argList.map(function(rawArg) {
    let arg = parseArg(rawArg);
    if (!arg.literal) {
      sig.static = false;
    }
    return arg;
  }, this);
  return sig;
}
function parseArg(rawArg) {
  let arg = rawArg.trim().replace(/&comma;/g, ",").replace(/\\(.)/g, "$1");
  let a17 = {
    name: arg,
    value: "",
    literal: false
  };
  let fc = arg[0];
  if (fc === "-") {
    fc = arg[1];
  }
  if (fc >= "0" && fc <= "9") {
    fc = "#";
  }
  switch (fc) {
    case "'":
    case '"':
      a17.value = arg.slice(1, -1);
      a17.literal = true;
      break;
    case "#":
      a17.value = Number(arg);
      a17.literal = true;
      break;
  }
  if (!a17.literal) {
    a17.rootProperty = root(arg);
    a17.structured = isPath(arg);
    if (a17.structured) {
      a17.wildcard = arg.slice(-2) == ".*";
      if (a17.wildcard) {
        a17.name = arg.slice(0, -2);
      }
    }
  }
  return a17;
}
function getArgValue(data, props, path) {
  let value = get(data, path);
  if (value === void 0) {
    value = props[path];
  }
  return value;
}
function notifySplices(inst, array, path, splices) {
  const splicesData = { indexSplices: splices };
  if (legacyUndefined && !inst._overrideLegacyUndefined) {
    array.splices = splicesData;
  }
  inst.notifyPath(path + ".splices", splicesData);
  inst.notifyPath(path + ".length", array.length);
  if (legacyUndefined && !inst._overrideLegacyUndefined) {
    splicesData.indexSplices = [];
  }
}
function notifySplice(inst, array, path, index, addedCount, removed) {
  notifySplices(inst, array, path, [{
    index,
    addedCount,
    removed,
    object: array,
    type: "splice"
  }]);
}
function upper(name) {
  return name[0].toUpperCase() + name.substring(1);
}
var PropertyEffects = dedupingMixin((superClass) => {
  const propertyEffectsBase = TemplateStamp(PropertyAccessors(superClass));
  class PropertyEffects2 extends propertyEffectsBase {
    constructor() {
      super();
      this.__isPropertyEffectsClient = true;
      this.__dataClientsReady;
      this.__dataPendingClients;
      this.__dataToNotify;
      this.__dataLinkedPaths;
      this.__dataHasPaths;
      this.__dataCompoundStorage;
      this.__dataHost;
      this.__dataTemp;
      this.__dataClientsInitialized;
      this.__data;
      this.__dataPending;
      this.__dataOld;
      this.__computeEffects;
      this.__computeInfo;
      this.__reflectEffects;
      this.__notifyEffects;
      this.__propagateEffects;
      this.__observeEffects;
      this.__readOnly;
      this.__templateInfo;
      this._overrideLegacyUndefined;
    }
    get PROPERTY_EFFECT_TYPES() {
      return TYPES;
    }
    /**
     * @override
     * @return {void}
     */
    _initializeProperties() {
      super._initializeProperties();
      this._registerHost();
      this.__dataClientsReady = false;
      this.__dataPendingClients = null;
      this.__dataToNotify = null;
      this.__dataLinkedPaths = null;
      this.__dataHasPaths = false;
      this.__dataCompoundStorage = this.__dataCompoundStorage || null;
      this.__dataHost = this.__dataHost || null;
      this.__dataTemp = {};
      this.__dataClientsInitialized = false;
    }
    _registerHost() {
      if (hostStack.length) {
        let host = hostStack[hostStack.length - 1];
        host._enqueueClient(this);
        this.__dataHost = host;
      }
    }
    /**
     * Overrides `PropertyAccessors` implementation to provide a
     * more efficient implementation of initializing properties from
     * the prototype on the instance.
     *
     * @override
     * @param {Object} props Properties to initialize on the prototype
     * @return {void}
     */
    _initializeProtoProperties(props) {
      this.__data = Object.create(props);
      this.__dataPending = Object.create(props);
      this.__dataOld = {};
    }
    /**
     * Overrides `PropertyAccessors` implementation to avoid setting
     * `_setProperty`'s `shouldNotify: true`.
     *
     * @override
     * @param {Object} props Properties to initialize on the instance
     * @return {void}
     */
    _initializeInstanceProperties(props) {
      let readOnly = this[TYPES.READ_ONLY];
      for (let prop in props) {
        if (!readOnly || !readOnly[prop]) {
          this.__dataPending = this.__dataPending || {};
          this.__dataOld = this.__dataOld || {};
          this.__data[prop] = this.__dataPending[prop] = props[prop];
        }
      }
    }
    // Prototype setup ----------------------------------------
    /**
     * Equivalent to static `addPropertyEffect` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property that should trigger the effect
     * @param {string} type Effect type, from this.PROPERTY_EFFECT_TYPES
     * @param {Object=} effect Effect metadata object
     * @return {void}
     * @protected
     */
    _addPropertyEffect(property, type, effect) {
      this._createPropertyAccessor(property, type == TYPES.READ_ONLY);
      let effects = ensureOwnEffectMap(this, type, true)[property];
      if (!effects) {
        effects = this[type][property] = [];
      }
      effects.push(effect);
    }
    /**
     * Removes the given property effect.
     *
     * @override
     * @param {string} property Property the effect was associated with
     * @param {string} type Effect type, from this.PROPERTY_EFFECT_TYPES
     * @param {Object=} effect Effect metadata object to remove
     * @return {void}
     */
    _removePropertyEffect(property, type, effect) {
      let effects = ensureOwnEffectMap(this, type, true)[property];
      let idx = effects.indexOf(effect);
      if (idx >= 0) {
        effects.splice(idx, 1);
      }
    }
    /**
     * Returns whether the current prototype/instance has a property effect
     * of a certain type.
     *
     * @override
     * @param {string} property Property name
     * @param {string=} type Effect type, from this.PROPERTY_EFFECT_TYPES
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasPropertyEffect(property, type) {
      let effects = this[type];
      return Boolean(effects && effects[property]);
    }
    /**
     * Returns whether the current prototype/instance has a "read only"
     * accessor for the given property.
     *
     * @override
     * @param {string} property Property name
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasReadOnlyEffect(property) {
      return this._hasPropertyEffect(property, TYPES.READ_ONLY);
    }
    /**
     * Returns whether the current prototype/instance has a "notify"
     * property effect for the given property.
     *
     * @override
     * @param {string} property Property name
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasNotifyEffect(property) {
      return this._hasPropertyEffect(property, TYPES.NOTIFY);
    }
    /**
     * Returns whether the current prototype/instance has a "reflect to
     * attribute" property effect for the given property.
     *
     * @override
     * @param {string} property Property name
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasReflectEffect(property) {
      return this._hasPropertyEffect(property, TYPES.REFLECT);
    }
    /**
     * Returns whether the current prototype/instance has a "computed"
     * property effect for the given property.
     *
     * @override
     * @param {string} property Property name
     * @return {boolean} True if the prototype/instance has an effect of this
     *     type
     * @protected
     */
    _hasComputedEffect(property) {
      return this._hasPropertyEffect(property, TYPES.COMPUTE);
    }
    // Runtime ----------------------------------------
    /**
     * Sets a pending property or path.  If the root property of the path in
     * question had no accessor, the path is set, otherwise it is enqueued
     * via `_setPendingProperty`.
     *
     * This function isolates relatively expensive functionality necessary
     * for the public API (`set`, `setProperties`, `notifyPath`, and property
     * change listeners via {{...}} bindings), such that it is only done
     * when paths enter the system, and not at every propagation step.  It
     * also sets a `__dataHasPaths` flag on the instance which is used to
     * fast-path slower path-matching code in the property effects host paths.
     *
     * `path` can be a path string or array of path parts as accepted by the
     * public API.
     *
     * @override
     * @param {string | !Array<number|string>} path Path to set
     * @param {*} value Value to set
     * @param {boolean=} shouldNotify Set to true if this change should
     *  cause a property notification event dispatch
     * @param {boolean=} isPathNotification If the path being set is a path
     *   notification of an already changed value, as opposed to a request
     *   to set and notify the change.  In the latter `false` case, a dirty
     *   check is performed and then the value is set to the path before
     *   enqueuing the pending property change.
     * @return {boolean} Returns true if the property/path was enqueued in
     *   the pending changes bag.
     * @protected
     */
    _setPendingPropertyOrPath(path, value, shouldNotify, isPathNotification) {
      if (isPathNotification || root(Array.isArray(path) ? path[0] : path) !== path) {
        if (!isPathNotification) {
          let old = get(this, path);
          path = /** @type {string} */
          set(this, path, value);
          if (!path || !super._shouldPropertyChange(path, value, old)) {
            return false;
          }
        }
        this.__dataHasPaths = true;
        if (this._setPendingProperty(
          /**@type{string}*/
          path,
          value,
          shouldNotify
        )) {
          computeLinkedPaths(
            this,
            /**@type{string}*/
            path,
            value
          );
          return true;
        }
      } else {
        if (this.__dataHasAccessor && this.__dataHasAccessor[path]) {
          return this._setPendingProperty(
            /**@type{string}*/
            path,
            value,
            shouldNotify
          );
        } else {
          this[path] = value;
        }
      }
      return false;
    }
    /**
     * Applies a value to a non-Polymer element/node's property.
     *
     * The implementation makes a best-effort at binding interop:
     * Some native element properties have side-effects when
     * re-setting the same value (e.g. setting `<input>.value` resets the
     * cursor position), so we do a dirty-check before setting the value.
     * However, for better interop with non-Polymer custom elements that
     * accept objects, we explicitly re-set object changes coming from the
     * Polymer world (which may include deep object changes without the
     * top reference changing), erring on the side of providing more
     * information.
     *
     * Users may override this method to provide alternate approaches.
     *
     * @override
     * @param {!Node} node The node to set a property on
     * @param {string} prop The property to set
     * @param {*} value The value to set
     * @return {void}
     * @protected
     */
    _setUnmanagedPropertyToNode(node, prop, value) {
      if (value !== node[prop] || typeof value == "object") {
        if (prop === "className") {
          node = /** @type {!Node} */
          wrap(node);
        }
        node[prop] = value;
      }
    }
    /**
     * Overrides the `PropertiesChanged` implementation to introduce special
     * dirty check logic depending on the property & value being set:
     *
     * 1. Any value set to a path (e.g. 'obj.prop': 42 or 'obj.prop': {...})
     *    Stored in `__dataTemp`, dirty checked against `__dataTemp`
     * 2. Object set to simple property (e.g. 'prop': {...})
     *    Stored in `__dataTemp` and `__data`, dirty checked against
     *    `__dataTemp` by default implementation of `_shouldPropertyChange`
     * 3. Primitive value set to simple property (e.g. 'prop': 42)
     *    Stored in `__data`, dirty checked against `__data`
     *
     * The dirty-check is important to prevent cycles due to two-way
     * notification, but paths and objects are only dirty checked against any
     * previous value set during this turn via a "temporary cache" that is
     * cleared when the last `_propertiesChanged` exits. This is so:
     * a. any cached array paths (e.g. 'array.3.prop') may be invalidated
     *    due to array mutations like shift/unshift/splice; this is fine
     *    since path changes are dirty-checked at user entry points like `set`
     * b. dirty-checking for objects only lasts one turn to allow the user
     *    to mutate the object in-place and re-set it with the same identity
     *    and have all sub-properties re-propagated in a subsequent turn.
     *
     * The temp cache is not necessarily sufficient to prevent invalid array
     * paths, since a splice can happen during the same turn (with pathological
     * user code); we could introduce a "fixup" for temporarily cached array
     * paths if needed: https://github.com/Polymer/polymer/issues/4227
     *
     * @override
     * @param {string} property Name of the property
     * @param {*} value Value to set
     * @param {boolean=} shouldNotify True if property should fire notification
     *   event (applies only for `notify: true` properties)
     * @return {boolean} Returns true if the property changed
     */
    _setPendingProperty(property, value, shouldNotify) {
      let propIsPath = this.__dataHasPaths && isPath(property);
      let prevProps = propIsPath ? this.__dataTemp : this.__data;
      if (this._shouldPropertyChange(property, value, prevProps[property])) {
        if (!this.__dataPending) {
          this.__dataPending = {};
          this.__dataOld = {};
        }
        if (!(property in this.__dataOld)) {
          this.__dataOld[property] = this.__data[property];
        }
        if (propIsPath) {
          this.__dataTemp[property] = value;
        } else {
          this.__data[property] = value;
        }
        this.__dataPending[property] = value;
        if (propIsPath || this[TYPES.NOTIFY] && this[TYPES.NOTIFY][property]) {
          this.__dataToNotify = this.__dataToNotify || {};
          this.__dataToNotify[property] = shouldNotify;
        }
        return true;
      }
      return false;
    }
    /**
     * Overrides base implementation to ensure all accessors set `shouldNotify`
     * to true, for per-property notification tracking.
     *
     * @override
     * @param {string} property Name of the property
     * @param {*} value Value to set
     * @return {void}
     */
    _setProperty(property, value) {
      if (this._setPendingProperty(property, value, true)) {
        this._invalidateProperties();
      }
    }
    /**
     * Overrides `PropertyAccessor`'s default async queuing of
     * `_propertiesChanged`: if `__dataReady` is false (has not yet been
     * manually flushed), the function no-ops; otherwise flushes
     * `_propertiesChanged` synchronously.
     *
     * @override
     * @return {void}
     */
    _invalidateProperties() {
      if (this.__dataReady) {
        this._flushProperties();
      }
    }
    /**
     * Enqueues the given client on a list of pending clients, whose
     * pending property changes can later be flushed via a call to
     * `_flushClients`.
     *
     * @override
     * @param {Object} client PropertyEffects client to enqueue
     * @return {void}
     * @protected
     */
    _enqueueClient(client) {
      this.__dataPendingClients = this.__dataPendingClients || [];
      if (client !== this) {
        this.__dataPendingClients.push(client);
      }
    }
    /**
     * Flushes any clients previously enqueued via `_enqueueClient`, causing
     * their `_flushProperties` method to run.
     *
     * @override
     * @return {void}
     * @protected
     */
    _flushClients() {
      if (!this.__dataClientsReady) {
        this.__dataClientsReady = true;
        this._readyClients();
        this.__dataReady = true;
      } else {
        this.__enableOrFlushClients();
      }
    }
    // NOTE: We ensure clients either enable or flush as appropriate. This
    // handles two corner cases:
    // (1) clients flush properly when connected/enabled before the host
    // enables; e.g.
    //   (a) Templatize stamps with no properties and does not flush and
    //   (b) the instance is inserted into dom and
    //   (c) then the instance flushes.
    // (2) clients enable properly when not connected/enabled when the host
    // flushes; e.g.
    //   (a) a template is runtime stamped and not yet connected/enabled
    //   (b) a host sets a property, causing stamped dom to flush
    //   (c) the stamped dom enables.
    __enableOrFlushClients() {
      let clients = this.__dataPendingClients;
      if (clients) {
        this.__dataPendingClients = null;
        for (let i10 = 0; i10 < clients.length; i10++) {
          let client = clients[i10];
          if (!client.__dataEnabled) {
            client._enableProperties();
          } else if (client.__dataPending) {
            client._flushProperties();
          }
        }
      }
    }
    /**
     * Perform any initial setup on client dom. Called before the first
     * `_flushProperties` call on client dom and before any element
     * observers are called.
     *
     * @override
     * @return {void}
     * @protected
     */
    _readyClients() {
      this.__enableOrFlushClients();
    }
    /**
     * Sets a bag of property changes to this instance, and
     * synchronously processes all effects of the properties as a batch.
     *
     * Property names must be simple properties, not paths.  Batched
     * path propagation is not supported.
     *
     * @override
     * @param {Object} props Bag of one or more key-value pairs whose key is
     *   a property and value is the new value to set for that property.
     * @param {boolean=} setReadOnly When true, any private values set in
     *   `props` will be set. By default, `setProperties` will not set
     *   `readOnly: true` root properties.
     * @return {void}
     * @public
     */
    setProperties(props, setReadOnly) {
      for (let path in props) {
        if (setReadOnly || !this[TYPES.READ_ONLY] || !this[TYPES.READ_ONLY][path]) {
          this._setPendingPropertyOrPath(path, props[path], true);
        }
      }
      this._invalidateProperties();
    }
    /**
     * Overrides `PropertyAccessors` so that property accessor
     * side effects are not enabled until after client dom is fully ready.
     * Also calls `_flushClients` callback to ensure client dom is enabled
     * that was not enabled as a result of flushing properties.
     *
     * @override
     * @return {void}
     */
    ready() {
      this._flushProperties();
      if (!this.__dataClientsReady) {
        this._flushClients();
      }
      if (this.__dataPending) {
        this._flushProperties();
      }
    }
    /**
     * Implements `PropertyAccessors`'s properties changed callback.
     *
     * Runs each class of effects for the batch of changed properties in
     * a specific order (compute, propagate, reflect, observe, notify).
     *
     * @override
     * @param {!Object} currentProps Bag of all current accessor values
     * @param {?Object} changedProps Bag of properties changed since the last
     *   call to `_propertiesChanged`
     * @param {?Object} oldProps Bag of previous values for each property
     *   in `changedProps`
     * @return {void}
     */
    _propertiesChanged(currentProps, changedProps, oldProps) {
      let hasPaths = this.__dataHasPaths;
      this.__dataHasPaths = false;
      let notifyProps;
      runComputedEffects(this, changedProps, oldProps, hasPaths);
      notifyProps = this.__dataToNotify;
      this.__dataToNotify = null;
      this._propagatePropertyChanges(changedProps, oldProps, hasPaths);
      this._flushClients();
      runEffects(this, this[TYPES.REFLECT], changedProps, oldProps, hasPaths);
      runEffects(this, this[TYPES.OBSERVE], changedProps, oldProps, hasPaths);
      if (notifyProps) {
        runNotifyEffects(this, notifyProps, changedProps, oldProps, hasPaths);
      }
      if (this.__dataCounter == 1) {
        this.__dataTemp = {};
      }
    }
    /**
     * Called to propagate any property changes to stamped template nodes
     * managed by this element.
     *
     * @override
     * @param {Object} changedProps Bag of changed properties
     * @param {Object} oldProps Bag of previous values for changed properties
     * @param {boolean} hasPaths True with `props` contains one or more paths
     * @return {void}
     * @protected
     */
    _propagatePropertyChanges(changedProps, oldProps, hasPaths) {
      if (this[TYPES.PROPAGATE]) {
        runEffects(this, this[TYPES.PROPAGATE], changedProps, oldProps, hasPaths);
      }
      if (this.__templateInfo) {
        this._runEffectsForTemplate(this.__templateInfo, changedProps, oldProps, hasPaths);
      }
    }
    _runEffectsForTemplate(templateInfo, changedProps, oldProps, hasPaths) {
      const baseRunEffects = (changedProps2, hasPaths2) => {
        runEffects(
          this,
          templateInfo.propertyEffects,
          changedProps2,
          oldProps,
          hasPaths2,
          templateInfo.nodeList
        );
        for (let info = templateInfo.firstChild; info; info = info.nextSibling) {
          this._runEffectsForTemplate(info, changedProps2, oldProps, hasPaths2);
        }
      };
      if (templateInfo.runEffects) {
        templateInfo.runEffects(baseRunEffects, changedProps, hasPaths);
      } else {
        baseRunEffects(changedProps, hasPaths);
      }
    }
    /**
     * Aliases one data path as another, such that path notifications from one
     * are routed to the other.
     *
     * @override
     * @param {string | !Array<string|number>} to Target path to link.
     * @param {string | !Array<string|number>} from Source path to link.
     * @return {void}
     * @public
     */
    linkPaths(to, from) {
      to = normalize(to);
      from = normalize(from);
      this.__dataLinkedPaths = this.__dataLinkedPaths || {};
      this.__dataLinkedPaths[to] = from;
    }
    /**
     * Removes a data path alias previously established with `_linkPaths`.
     *
     * Note, the path to unlink should be the target (`to`) used when
     * linking the paths.
     *
     * @override
     * @param {string | !Array<string|number>} path Target path to unlink.
     * @return {void}
     * @public
     */
    unlinkPaths(path) {
      path = normalize(path);
      if (this.__dataLinkedPaths) {
        delete this.__dataLinkedPaths[path];
      }
    }
    /**
     * Notify that an array has changed.
     *
     * Example:
     *
     *     this.items = [ {name: 'Jim'}, {name: 'Todd'}, {name: 'Bill'} ];
     *     ...
     *     this.items.splice(1, 1, {name: 'Sam'});
     *     this.items.push({name: 'Bob'});
     *     this.notifySplices('items', [
     *       { index: 1, removed: [{name: 'Todd'}], addedCount: 1,
     *         object: this.items, type: 'splice' },
     *       { index: 3, removed: [], addedCount: 1,
     *         object: this.items, type: 'splice'}
     *     ]);
     *
     * @param {string} path Path that should be notified.
     * @param {Array} splices Array of splice records indicating ordered
     *   changes that occurred to the array. Each record should have the
     *   following fields:
     *    * index: index at which the change occurred
     *    * removed: array of items that were removed from this index
     *    * addedCount: number of new items added at this index
     *    * object: a reference to the array in question
     *    * type: the string literal 'splice'
     *
     *   Note that splice records _must_ be normalized such that they are
     *   reported in index order (raw results from `Object.observe` are not
     *   ordered and must be normalized/merged before notifying).
     *
     * @override
     * @return {void}
     * @public
     */
    notifySplices(path, splices) {
      let info = { path: "" };
      let array = (
        /** @type {Array} */
        get(this, path, info)
      );
      notifySplices(this, array, info.path, splices);
    }
    /**
     * Convenience method for reading a value from a path.
     *
     * Note, if any part in the path is undefined, this method returns
     * `undefined` (this method does not throw when dereferencing undefined
     * paths).
     *
     * @override
     * @param {(string|!Array<(string|number)>)} path Path to the value
     *   to read.  The path may be specified as a string (e.g. `foo.bar.baz`)
     *   or an array of path parts (e.g. `['foo.bar', 'baz']`).  Note that
     *   bracketed expressions are not supported; string-based path parts
     *   *must* be separated by dots.  Note that when dereferencing array
     *   indices, the index may be used as a dotted part directly
     *   (e.g. `users.12.name` or `['users', 12, 'name']`).
     * @param {Object=} root Root object from which the path is evaluated.
     * @return {*} Value at the path, or `undefined` if any part of the path
     *   is undefined.
     * @public
     */
    get(path, root2) {
      return get(root2 || this, path);
    }
    /**
     * Convenience method for setting a value to a path and notifying any
     * elements bound to the same path.
     *
     * Note, if any part in the path except for the last is undefined,
     * this method does nothing (this method does not throw when
     * dereferencing undefined paths).
     *
     * @override
     * @param {(string|!Array<(string|number)>)} path Path to the value
     *   to write.  The path may be specified as a string (e.g. `'foo.bar.baz'`)
     *   or an array of path parts (e.g. `['foo.bar', 'baz']`).  Note that
     *   bracketed expressions are not supported; string-based path parts
     *   *must* be separated by dots.  Note that when dereferencing array
     *   indices, the index may be used as a dotted part directly
     *   (e.g. `'users.12.name'` or `['users', 12, 'name']`).
     * @param {*} value Value to set at the specified path.
     * @param {Object=} root Root object from which the path is evaluated.
     *   When specified, no notification will occur.
     * @return {void}
     * @public
     */
    set(path, value, root2) {
      if (root2) {
        set(root2, path, value);
      } else {
        if (!this[TYPES.READ_ONLY] || !this[TYPES.READ_ONLY][
          /** @type {string} */
          path
        ]) {
          if (this._setPendingPropertyOrPath(path, value, true)) {
            this._invalidateProperties();
          }
        }
      }
    }
    /**
     * Adds items onto the end of the array at the path specified.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.push`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @param {...*} items Items to push onto array
     * @return {number} New length of the array.
     * @public
     */
    push(path, ...items) {
      let info = { path: "" };
      let array = (
        /** @type {Array}*/
        get(this, path, info)
      );
      let len = array.length;
      let ret = array.push(...items);
      if (items.length) {
        notifySplice(this, array, info.path, len, items.length, []);
      }
      return ret;
    }
    /**
     * Removes an item from the end of array at the path specified.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.pop`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @return {*} Item that was removed.
     * @public
     */
    pop(path) {
      let info = { path: "" };
      let array = (
        /** @type {Array} */
        get(this, path, info)
      );
      let hadLength = Boolean(array.length);
      let ret = array.pop();
      if (hadLength) {
        notifySplice(this, array, info.path, array.length, 0, [ret]);
      }
      return ret;
    }
    /**
     * Starting from the start index specified, removes 0 or more items
     * from the array and inserts 0 or more new items in their place.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.splice`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @param {number} start Index from which to start removing/inserting.
     * @param {number=} deleteCount Number of items to remove.
     * @param {...*} items Items to insert into array.
     * @return {!Array} Array of removed items.
     * @public
     */
    splice(path, start, deleteCount, ...items) {
      let info = { path: "" };
      let array = (
        /** @type {Array} */
        get(this, path, info)
      );
      if (start < 0) {
        start = array.length - Math.floor(-start);
      } else if (start) {
        start = Math.floor(start);
      }
      let ret;
      if (arguments.length === 2) {
        ret = array.splice(start);
      } else {
        ret = array.splice(start, deleteCount, ...items);
      }
      if (items.length || ret.length) {
        notifySplice(this, array, info.path, start, items.length, ret);
      }
      return ret;
    }
    /**
     * Removes an item from the beginning of array at the path specified.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.pop`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @return {*} Item that was removed.
     * @public
     */
    shift(path) {
      let info = { path: "" };
      let array = (
        /** @type {Array} */
        get(this, path, info)
      );
      let hadLength = Boolean(array.length);
      let ret = array.shift();
      if (hadLength) {
        notifySplice(this, array, info.path, 0, 0, [ret]);
      }
      return ret;
    }
    /**
     * Adds items onto the beginning of the array at the path specified.
     *
     * The arguments after `path` and return value match that of
     * `Array.prototype.push`.
     *
     * This method notifies other paths to the same array that a
     * splice occurred to the array.
     *
     * @override
     * @param {string | !Array<string|number>} path Path to array.
     * @param {...*} items Items to insert info array
     * @return {number} New length of the array.
     * @public
     */
    unshift(path, ...items) {
      let info = { path: "" };
      let array = (
        /** @type {Array} */
        get(this, path, info)
      );
      let ret = array.unshift(...items);
      if (items.length) {
        notifySplice(this, array, info.path, 0, items.length, []);
      }
      return ret;
    }
    /**
     * Notify that a path has changed.
     *
     * Example:
     *
     *     this.item.user.name = 'Bob';
     *     this.notifyPath('item.user.name');
     *
     * @override
     * @param {string} path Path that should be notified.
     * @param {*=} value Value at the path (optional).
     * @return {void}
     * @public
     */
    notifyPath(path, value) {
      let propPath;
      if (arguments.length == 1) {
        let info = { path: "" };
        value = get(this, path, info);
        propPath = info.path;
      } else if (Array.isArray(path)) {
        propPath = normalize(path);
      } else {
        propPath = /** @type{string} */
        path;
      }
      if (this._setPendingPropertyOrPath(propPath, value, true, true)) {
        this._invalidateProperties();
      }
    }
    /**
     * Equivalent to static `createReadOnlyProperty` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property name
     * @param {boolean=} protectedSetter Creates a custom protected setter
     *   when `true`.
     * @return {void}
     * @protected
     */
    _createReadOnlyProperty(property, protectedSetter) {
      this._addPropertyEffect(property, TYPES.READ_ONLY);
      if (protectedSetter) {
        this["_set" + upper(property)] = /** @this {PropertyEffects} */
        function(value) {
          this._setProperty(property, value);
        };
      }
    }
    /**
     * Equivalent to static `createPropertyObserver` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property name
     * @param {string|function(*,*)} method Function or name of observer method
     *     to call
     * @param {boolean=} dynamicFn Whether the method name should be included as
     *   a dependency to the effect.
     * @return {void}
     * @protected
     */
    _createPropertyObserver(property, method, dynamicFn) {
      let info = { property, method, dynamicFn: Boolean(dynamicFn) };
      this._addPropertyEffect(property, TYPES.OBSERVE, {
        fn: runObserverEffect,
        info,
        trigger: { name: property }
      });
      if (dynamicFn) {
        this._addPropertyEffect(
          /** @type {string} */
          method,
          TYPES.OBSERVE,
          {
            fn: runObserverEffect,
            info,
            trigger: { name: method }
          }
        );
      }
    }
    /**
     * Equivalent to static `createMethodObserver` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} expression Method expression
     * @param {boolean|Object=} dynamicFn Boolean or object map indicating
     *   whether method names should be included as a dependency to the effect.
     * @return {void}
     * @protected
     */
    _createMethodObserver(expression, dynamicFn) {
      let sig = parseMethod(expression);
      if (!sig) {
        throw new Error("Malformed observer expression '" + expression + "'");
      }
      createMethodEffect(this, sig, TYPES.OBSERVE, runMethodEffect, null, dynamicFn);
    }
    /**
     * Equivalent to static `createNotifyingProperty` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property name
     * @return {void}
     * @protected
     */
    _createNotifyingProperty(property) {
      this._addPropertyEffect(property, TYPES.NOTIFY, {
        fn: runNotifyEffect,
        info: {
          eventName: camelToDashCase(property) + "-changed",
          property
        }
      });
    }
    /**
     * Equivalent to static `createReflectedProperty` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Property name
     * @return {void}
     * @protected
     * @suppress {missingProperties} go/missingfnprops
     */
    _createReflectedProperty(property) {
      let attr = this.constructor.attributeNameForProperty(property);
      if (attr[0] === "-") {
        console.warn("Property " + property + " cannot be reflected to attribute " + attr + ' because "-" is not a valid starting attribute name. Use a lowercase first letter for the property instead.');
      } else {
        this._addPropertyEffect(property, TYPES.REFLECT, {
          fn: runReflectEffect,
          info: {
            attrName: attr
          }
        });
      }
    }
    /**
     * Equivalent to static `createComputedProperty` API but can be called on
     * an instance to add effects at runtime.  See that method for
     * full API docs.
     *
     * @override
     * @param {string} property Name of computed property to set
     * @param {string} expression Method expression
     * @param {boolean|Object=} dynamicFn Boolean or object map indicating
     *   whether method names should be included as a dependency to the effect.
     * @return {void}
     * @protected
     */
    _createComputedProperty(property, expression, dynamicFn) {
      let sig = parseMethod(expression);
      if (!sig) {
        throw new Error("Malformed computed expression '" + expression + "'");
      }
      const info = createMethodEffect(this, sig, TYPES.COMPUTE, runComputedEffect, property, dynamicFn);
      ensureOwnEffectMap(this, COMPUTE_INFO)[property] = info;
    }
    /**
     * Gather the argument values for a method specified in the provided array
     * of argument metadata.
     *
     * The `path` and `value` arguments are used to fill in wildcard descriptor
     * when the method is being called as a result of a path notification.
     *
     * @param {!Array<!MethodArg>} args Array of argument metadata
     * @param {string} path Property/path name that triggered the method effect
     * @param {Object} props Bag of current property changes
     * @return {!Array<*>} Array of argument values
     * @private
     */
    _marshalArgs(args, path, props) {
      const data = this.__data;
      const values = [];
      for (let i10 = 0, l17 = args.length; i10 < l17; i10++) {
        let { name, structured, wildcard, value, literal } = args[i10];
        if (!literal) {
          if (wildcard) {
            const matches = isDescendant(name, path);
            const pathValue = getArgValue(data, props, matches ? path : name);
            value = {
              path: matches ? path : name,
              value: pathValue,
              base: matches ? get(data, name) : pathValue
            };
          } else {
            value = structured ? getArgValue(data, props, name) : data[name];
          }
        }
        if (legacyUndefined && !this._overrideLegacyUndefined && value === void 0 && args.length > 1) {
          return NOOP;
        }
        values[i10] = value;
      }
      return values;
    }
    // -- static class methods ------------
    /**
     * Ensures an accessor exists for the specified property, and adds
     * to a list of "property effects" that will run when the accessor for
     * the specified property is set.  Effects are grouped by "type", which
     * roughly corresponds to a phase in effect processing.  The effect
     * metadata should be in the following form:
     *
     *     {
     *       fn: effectFunction, // Reference to function to call to perform effect
     *       info: { ... }       // Effect metadata passed to function
     *       trigger: {          // Optional triggering metadata; if not provided
     *         name: string      // the property is treated as a wildcard
     *         structured: boolean
     *         wildcard: boolean
     *       }
     *     }
     *
     * Effects are called from `_propertiesChanged` in the following order by
     * type:
     *
     * 1. COMPUTE
     * 2. PROPAGATE
     * 3. REFLECT
     * 4. OBSERVE
     * 5. NOTIFY
     *
     * Effect functions are called with the following signature:
     *
     *     effectFunction(inst, path, props, oldProps, info, hasPaths)
     *
     * @param {string} property Property that should trigger the effect
     * @param {string} type Effect type, from this.PROPERTY_EFFECT_TYPES
     * @param {Object=} effect Effect metadata object
     * @return {void}
     * @protected
     * @nocollapse
     */
    static addPropertyEffect(property, type, effect) {
      this.prototype._addPropertyEffect(property, type, effect);
    }
    /**
     * Creates a single-property observer for the given property.
     *
     * @param {string} property Property name
     * @param {string|function(*,*)} method Function or name of observer method to call
     * @param {boolean=} dynamicFn Whether the method name should be included as
     *   a dependency to the effect.
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createPropertyObserver(property, method, dynamicFn) {
      this.prototype._createPropertyObserver(property, method, dynamicFn);
    }
    /**
     * Creates a multi-property "method observer" based on the provided
     * expression, which should be a string in the form of a normal JavaScript
     * function signature: `'methodName(arg1, [..., argn])'`.  Each argument
     * should correspond to a property or path in the context of this
     * prototype (or instance), or may be a literal string or number.
     *
     * @param {string} expression Method expression
     * @param {boolean|Object=} dynamicFn Boolean or object map indicating
     * @return {void}
     *   whether method names should be included as a dependency to the effect.
     * @protected
     * @nocollapse
     */
    static createMethodObserver(expression, dynamicFn) {
      this.prototype._createMethodObserver(expression, dynamicFn);
    }
    /**
     * Causes the setter for the given property to dispatch `<property>-changed`
     * events to notify of changes to the property.
     *
     * @param {string} property Property name
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createNotifyingProperty(property) {
      this.prototype._createNotifyingProperty(property);
    }
    /**
     * Creates a read-only accessor for the given property.
     *
     * To set the property, use the protected `_setProperty` API.
     * To create a custom protected setter (e.g. `_setMyProp()` for
     * property `myProp`), pass `true` for `protectedSetter`.
     *
     * Note, if the property will have other property effects, this method
     * should be called first, before adding other effects.
     *
     * @param {string} property Property name
     * @param {boolean=} protectedSetter Creates a custom protected setter
     *   when `true`.
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createReadOnlyProperty(property, protectedSetter) {
      this.prototype._createReadOnlyProperty(property, protectedSetter);
    }
    /**
     * Causes the setter for the given property to reflect the property value
     * to a (dash-cased) attribute of the same name.
     *
     * @param {string} property Property name
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createReflectedProperty(property) {
      this.prototype._createReflectedProperty(property);
    }
    /**
     * Creates a computed property whose value is set to the result of the
     * method described by the given `expression` each time one or more
     * arguments to the method changes.  The expression should be a string
     * in the form of a normal JavaScript function signature:
     * `'methodName(arg1, [..., argn])'`
     *
     * @param {string} property Name of computed property to set
     * @param {string} expression Method expression
     * @param {boolean|Object=} dynamicFn Boolean or object map indicating whether
     *   method names should be included as a dependency to the effect.
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createComputedProperty(property, expression, dynamicFn) {
      this.prototype._createComputedProperty(property, expression, dynamicFn);
    }
    /**
     * Parses the provided template to ensure binding effects are created
     * for them, and then ensures property accessors are created for any
     * dependent properties in the template.  Binding effects for bound
     * templates are stored in a linked list on the instance so that
     * templates can be efficiently stamped and unstamped.
     *
     * @param {!HTMLTemplateElement} template Template containing binding
     *   bindings
     * @return {!TemplateInfo} Template metadata object
     * @protected
     * @nocollapse
     */
    static bindTemplate(template2) {
      return this.prototype._bindTemplate(template2);
    }
    // -- binding ----------------------------------------------
    /*
     * Overview of binding flow:
     *
     * During finalization (`instanceBinding==false`, `wasPreBound==false`):
     *  `_bindTemplate(t, false)` called directly during finalization - parses
     *  the template (for the first time), and then assigns that _prototypical_
     *  template info to `__preboundTemplateInfo` _on the prototype_; note in
     *  this case `wasPreBound` is false; this is the first time we're binding
     *  it, thus we create accessors.
     *
     * During first stamping (`instanceBinding==true`, `wasPreBound==true`):
     *   `_stampTemplate` calls `_bindTemplate(t, true)`: the `templateInfo`
     *   returned matches the prebound one, and so this is `wasPreBound == true`
     *   state; thus we _skip_ creating accessors, but _do_ create an instance
     *   of the template info to serve as the start of our linked list (needs to
     *   be an instance, not the prototypical one, so that we can add `nodeList`
     *   to it to contain the `nodeInfo`-ordered list of instance nodes for
     *   bindings, and so we can chain runtime-stamped template infos off of
     *   it). At this point, the call to `_stampTemplate` calls
     *   `applyTemplateInfo` for each nested `<template>` found during parsing
     *   to hand prototypical `_templateInfo` to them; we also pass the _parent_
     *   `templateInfo` to the `<template>` so that we have the instance-time
     *   parent to link the `templateInfo` under in the case it was
     *   runtime-stamped.
     *
     * During subsequent runtime stamping (`instanceBinding==true`,
     *   `wasPreBound==false`): `_stampTemplate` calls `_bindTemplate(t, true)`
     *   - here `templateInfo` is guaranteed to _not_ match the prebound one,
     *   because it was either a different template altogether, or even if it
     *   was the same template, the step above created a instance of the info;
     *   in this case `wasPreBound == false`, so we _do_ create accessors, _and_
     *   link a instance into the linked list.
     */
    /**
     * Equivalent to static `bindTemplate` API but can be called on an instance
     * to add effects at runtime.  See that method for full API docs.
     *
     * This method may be called on the prototype (for prototypical template
     * binding, to avoid creating accessors every instance) once per prototype,
     * and will be called with `runtimeBinding: true` by `_stampTemplate` to
     * create and link an instance of the template metadata associated with a
     * particular stamping.
     *
     * @override
     * @param {!HTMLTemplateElement} template Template containing binding
     * bindings
     * @param {boolean=} instanceBinding When false (default), performs
     * "prototypical" binding of the template and overwrites any previously
     * bound template for the class. When true (as passed from
     * `_stampTemplate`), the template info is instanced and linked into the
     * list of bound templates.
     * @return {!TemplateInfo} Template metadata object; for `runtimeBinding`,
     * this is an instance of the prototypical template info
     * @protected
     * @suppress {missingProperties} go/missingfnprops
     */
    _bindTemplate(template2, instanceBinding) {
      let templateInfo = this.constructor._parseTemplate(template2);
      let wasPreBound = this.__preBoundTemplateInfo == templateInfo;
      if (!wasPreBound) {
        for (let prop in templateInfo.propertyEffects) {
          this._createPropertyAccessor(prop);
        }
      }
      if (instanceBinding) {
        templateInfo = /** @type {!TemplateInfo} */
        Object.create(templateInfo);
        templateInfo.wasPreBound = wasPreBound;
        if (!this.__templateInfo) {
          this.__templateInfo = templateInfo;
        } else {
          const parent = template2._parentTemplateInfo || this.__templateInfo;
          const previous = parent.lastChild;
          templateInfo.parent = parent;
          parent.lastChild = templateInfo;
          templateInfo.previousSibling = previous;
          if (previous) {
            previous.nextSibling = templateInfo;
          } else {
            parent.firstChild = templateInfo;
          }
        }
      } else {
        this.__preBoundTemplateInfo = templateInfo;
      }
      return templateInfo;
    }
    /**
     * Adds a property effect to the given template metadata, which is run
     * at the "propagate" stage of `_propertiesChanged` when the template
     * has been bound to the element via `_bindTemplate`.
     *
     * The `effect` object should match the format in `_addPropertyEffect`.
     *
     * @param {Object} templateInfo Template metadata to add effect to
     * @param {string} prop Property that should trigger the effect
     * @param {Object=} effect Effect metadata object
     * @return {void}
     * @protected
     * @nocollapse
     */
    static _addTemplatePropertyEffect(templateInfo, prop, effect) {
      let hostProps = templateInfo.hostProps = templateInfo.hostProps || {};
      hostProps[prop] = true;
      let effects = templateInfo.propertyEffects = templateInfo.propertyEffects || {};
      let propEffects = effects[prop] = effects[prop] || [];
      propEffects.push(effect);
    }
    /**
     * Stamps the provided template and performs instance-time setup for
     * Polymer template features, including data bindings, declarative event
     * listeners, and the `this.$` map of `id`'s to nodes.  A document fragment
     * is returned containing the stamped DOM, ready for insertion into the
     * DOM.
     *
     * This method may be called more than once; however note that due to
     * `shadycss` polyfill limitations, only styles from templates prepared
     * using `ShadyCSS.prepareTemplate` will be correctly polyfilled (scoped
     * to the shadow root and support CSS custom properties), and note that
     * `ShadyCSS.prepareTemplate` may only be called once per element. As such,
     * any styles required by in runtime-stamped templates must be included
     * in the main element template.
     *
     * @param {!HTMLTemplateElement} template Template to stamp
     * @param {TemplateInfo=} templateInfo Optional bound template info associated
     *   with the template to be stamped; if omitted the template will be
     *   automatically bound.
     * @return {!StampedTemplate} Cloned template content
     * @override
     * @protected
     */
    _stampTemplate(template2, templateInfo) {
      templateInfo = templateInfo || /** @type {!TemplateInfo} */
      this._bindTemplate(template2, true);
      hostStack.push(this);
      let dom = super._stampTemplate(template2, templateInfo);
      hostStack.pop();
      templateInfo.nodeList = dom.nodeList;
      if (!templateInfo.wasPreBound) {
        let nodes = templateInfo.childNodes = [];
        for (let n19 = dom.firstChild; n19; n19 = n19.nextSibling) {
          nodes.push(n19);
        }
      }
      dom.templateInfo = templateInfo;
      setupBindings(this, templateInfo);
      if (this.__dataClientsReady) {
        this._runEffectsForTemplate(templateInfo, this.__data, null, false);
        this._flushClients();
      }
      return dom;
    }
    /**
     * Removes and unbinds the nodes previously contained in the provided
     * DocumentFragment returned from `_stampTemplate`.
     *
     * @override
     * @param {!StampedTemplate} dom DocumentFragment previously returned
     *   from `_stampTemplate` associated with the nodes to be removed
     * @return {void}
     * @protected
     */
    _removeBoundDom(dom) {
      const templateInfo = dom.templateInfo;
      const { previousSibling, nextSibling, parent } = templateInfo;
      if (previousSibling) {
        previousSibling.nextSibling = nextSibling;
      } else if (parent) {
        parent.firstChild = nextSibling;
      }
      if (nextSibling) {
        nextSibling.previousSibling = previousSibling;
      } else if (parent) {
        parent.lastChild = previousSibling;
      }
      templateInfo.nextSibling = templateInfo.previousSibling = null;
      let nodes = templateInfo.childNodes;
      for (let i10 = 0; i10 < nodes.length; i10++) {
        let node = nodes[i10];
        wrap(wrap(node).parentNode).removeChild(node);
      }
    }
    /**
     * Overrides default `TemplateStamp` implementation to add support for
     * parsing bindings from `TextNode`'s' `textContent`.  A `bindings`
     * array is added to `nodeInfo` and populated with binding metadata
     * with information capturing the binding target, and a `parts` array
     * with one or more metadata objects capturing the source(s) of the
     * binding.
     *
     * @param {Node} node Node to parse
     * @param {TemplateInfo} templateInfo Template metadata for current template
     * @param {NodeInfo} nodeInfo Node metadata for current template node
     * @return {boolean} `true` if the visited node added node-specific
     *   metadata to `nodeInfo`
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _parseTemplateNode(node, templateInfo, nodeInfo) {
      let noted = propertyEffectsBase._parseTemplateNode.call(
        this,
        node,
        templateInfo,
        nodeInfo
      );
      if (node.nodeType === Node.TEXT_NODE) {
        let parts = this._parseBindings(node.textContent, templateInfo);
        if (parts) {
          node.textContent = literalFromParts(parts) || " ";
          addBinding(this, templateInfo, nodeInfo, "text", "textContent", parts);
          noted = true;
        }
      }
      return noted;
    }
    /**
     * Overrides default `TemplateStamp` implementation to add support for
     * parsing bindings from attributes.  A `bindings`
     * array is added to `nodeInfo` and populated with binding metadata
     * with information capturing the binding target, and a `parts` array
     * with one or more metadata objects capturing the source(s) of the
     * binding.
     *
     * @param {Element} node Node to parse
     * @param {TemplateInfo} templateInfo Template metadata for current template
     * @param {NodeInfo} nodeInfo Node metadata for current template node
     * @param {string} name Attribute name
     * @param {string} value Attribute value
     * @return {boolean} `true` if the visited node added node-specific
     *   metadata to `nodeInfo`
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _parseTemplateNodeAttribute(node, templateInfo, nodeInfo, name, value) {
      let parts = this._parseBindings(value, templateInfo);
      if (parts) {
        let origName = name;
        let kind = "property";
        if (capitalAttributeRegex.test(name)) {
          kind = "attribute";
        } else if (name[name.length - 1] == "$") {
          name = name.slice(0, -1);
          kind = "attribute";
        }
        let literal = literalFromParts(parts);
        if (literal && kind == "attribute") {
          if (name == "class" && node.hasAttribute("class")) {
            literal += " " + node.getAttribute(name);
          }
          node.setAttribute(name, literal);
        }
        if (kind == "attribute" && origName == "disable-upgrade$") {
          node.setAttribute(name, "");
        }
        if (node.localName === "input" && origName === "value") {
          node.setAttribute(origName, "");
        }
        node.removeAttribute(origName);
        if (kind === "property") {
          name = dashToCamelCase2(name);
        }
        addBinding(this, templateInfo, nodeInfo, kind, name, parts, literal);
        return true;
      } else {
        return propertyEffectsBase._parseTemplateNodeAttribute.call(
          this,
          node,
          templateInfo,
          nodeInfo,
          name,
          value
        );
      }
    }
    /**
     * Overrides default `TemplateStamp` implementation to add support for
     * binding the properties that a nested template depends on to the template
     * as `_host_<property>`.
     *
     * @param {Node} node Node to parse
     * @param {TemplateInfo} templateInfo Template metadata for current template
     * @param {NodeInfo} nodeInfo Node metadata for current template node
     * @return {boolean} `true` if the visited node added node-specific
     *   metadata to `nodeInfo`
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _parseTemplateNestedTemplate(node, templateInfo, nodeInfo) {
      let noted = propertyEffectsBase._parseTemplateNestedTemplate.call(
        this,
        node,
        templateInfo,
        nodeInfo
      );
      const parent = node.parentNode;
      const nestedTemplateInfo = nodeInfo.templateInfo;
      const isDomIf = parent.localName === "dom-if";
      const isDomRepeat = parent.localName === "dom-repeat";
      if (removeNestedTemplates && (isDomIf || isDomRepeat)) {
        parent.removeChild(node);
        nodeInfo = nodeInfo.parentInfo;
        nodeInfo.templateInfo = nestedTemplateInfo;
        nodeInfo.noted = true;
        noted = false;
      }
      let hostProps = nestedTemplateInfo.hostProps;
      if (fastDomIf && isDomIf) {
        if (hostProps) {
          templateInfo.hostProps = Object.assign(templateInfo.hostProps || {}, hostProps);
          if (!removeNestedTemplates) {
            nodeInfo.parentInfo.noted = true;
          }
        }
      } else {
        let mode = "{";
        for (let source in hostProps) {
          let parts = [{ mode, source, dependencies: [source], hostProp: true }];
          addBinding(this, templateInfo, nodeInfo, "property", "_host_" + source, parts);
        }
      }
      return noted;
    }
    /**
     * Called to parse text in a template (either attribute values or
     * textContent) into binding metadata.
     *
     * Any overrides of this method should return an array of binding part
     * metadata  representing one or more bindings found in the provided text
     * and any "literal" text in between.  Any non-literal parts will be passed
     * to `_evaluateBinding` when any dependencies change.  The only required
     * fields of each "part" in the returned array are as follows:
     *
     * - `dependencies` - Array containing trigger metadata for each property
     *   that should trigger the binding to update
     * - `literal` - String containing text if the part represents a literal;
     *   in this case no `dependencies` are needed
     *
     * Additional metadata for use by `_evaluateBinding` may be provided in
     * each part object as needed.
     *
     * The default implementation handles the following types of bindings
     * (one or more may be intermixed with literal strings):
     * - Property binding: `[[prop]]`
     * - Path binding: `[[object.prop]]`
     * - Negated property or path bindings: `[[!prop]]` or `[[!object.prop]]`
     * - Two-way property or path bindings (supports negation):
     *   `{{prop}}`, `{{object.prop}}`, `{{!prop}}` or `{{!object.prop}}`
     * - Inline computed method (supports negation):
     *   `[[compute(a, 'literal', b)]]`, `[[!compute(a, 'literal', b)]]`
     *
     * The default implementation uses a regular expression for best
     * performance. However, the regular expression uses a white-list of
     * allowed characters in a data-binding, which causes problems for
     * data-bindings that do use characters not in this white-list.
     *
     * Instead of updating the white-list with all allowed characters,
     * there is a StrictBindingParser (see lib/mixins/strict-binding-parser)
     * that uses a state machine instead. This state machine is able to handle
     * all characters. However, it is slightly less performant, therefore we
     * extracted it into a separate optional mixin.
     *
     * @param {string} text Text to parse from attribute or textContent
     * @param {Object} templateInfo Current template metadata
     * @return {Array<!BindingPart>} Array of binding part metadata
     * @protected
     * @nocollapse
     */
    static _parseBindings(text, templateInfo) {
      let parts = [];
      let lastIndex = 0;
      let m11;
      while ((m11 = bindingRegex.exec(text)) !== null) {
        if (m11.index > lastIndex) {
          parts.push({ literal: text.slice(lastIndex, m11.index) });
        }
        let mode = m11[1][0];
        let negate = Boolean(m11[2]);
        let source = m11[3].trim();
        let customEvent = false, notifyEvent = "", colon = -1;
        if (mode == "{" && (colon = source.indexOf("::")) > 0) {
          notifyEvent = source.substring(colon + 2);
          source = source.substring(0, colon);
          customEvent = true;
        }
        let signature = parseMethod(source);
        let dependencies = [];
        if (signature) {
          let { args, methodName } = signature;
          for (let i10 = 0; i10 < args.length; i10++) {
            let arg = args[i10];
            if (!arg.literal) {
              dependencies.push(arg);
            }
          }
          let dynamicFns = templateInfo.dynamicFns;
          if (dynamicFns && dynamicFns[methodName] || signature.static) {
            dependencies.push(methodName);
            signature.dynamicFn = true;
          }
        } else {
          dependencies.push(source);
        }
        parts.push({
          source,
          mode,
          negate,
          customEvent,
          signature,
          dependencies,
          event: notifyEvent
        });
        lastIndex = bindingRegex.lastIndex;
      }
      if (lastIndex && lastIndex < text.length) {
        let literal = text.substring(lastIndex);
        if (literal) {
          parts.push({
            literal
          });
        }
      }
      if (parts.length) {
        return parts;
      } else {
        return null;
      }
    }
    /**
     * Called to evaluate a previously parsed binding part based on a set of
     * one or more changed dependencies.
     *
     * @param {!Polymer_PropertyEffects} inst Element that should be used as
     *     scope for binding dependencies
     * @param {BindingPart} part Binding part metadata
     * @param {string} path Property/path that triggered this effect
     * @param {Object} props Bag of current property changes
     * @param {Object} oldProps Bag of previous values for changed properties
     * @param {boolean} hasPaths True with `props` contains one or more paths
     * @return {*} Value the binding part evaluated to
     * @protected
     * @nocollapse
     */
    static _evaluateBinding(inst, part, path, props, oldProps, hasPaths) {
      let value;
      if (part.signature) {
        value = runMethodEffect(inst, path, props, oldProps, part.signature);
      } else if (path != part.source) {
        value = get(inst, part.source);
      } else {
        if (hasPaths && isPath(path)) {
          value = get(inst, path);
        } else {
          value = inst.__data[path];
        }
      }
      if (part.negate) {
        value = !value;
      }
      return value;
    }
  }
  return PropertyEffects2;
});
var hostStack = [];

// node_modules/@polymer/polymer/lib/utils/telemetry.js
var instanceCount = 0;
function incrementInstanceCount() {
  instanceCount++;
}
var registrations = [];
function register(prototype) {
  registrations.push(prototype);
}

// node_modules/@polymer/polymer/lib/mixins/properties-mixin.js
function normalizeProperties(props) {
  const output = {};
  for (let p14 in props) {
    const o8 = props[p14];
    output[p14] = typeof o8 === "function" ? { type: o8 } : o8;
  }
  return output;
}
var PropertiesMixin = dedupingMixin((superClass) => {
  const base = PropertiesChanged(superClass);
  function superPropertiesClass(constructor) {
    const superCtor = Object.getPrototypeOf(constructor);
    return superCtor.prototype instanceof PropertiesMixin2 ? (
      /** @type {!PropertiesMixinConstructor} */
      superCtor
    ) : null;
  }
  function ownProperties(constructor) {
    if (!constructor.hasOwnProperty(JSCompiler_renameProperty("__ownProperties", constructor))) {
      let props = null;
      if (constructor.hasOwnProperty(JSCompiler_renameProperty("properties", constructor))) {
        const properties = constructor.properties;
        if (properties) {
          props = normalizeProperties(properties);
        }
      }
      constructor.__ownProperties = props;
    }
    return constructor.__ownProperties;
  }
  class PropertiesMixin2 extends base {
    /**
     * Implements standard custom elements getter to observes the attributes
     * listed in `properties`.
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static get observedAttributes() {
      if (!this.hasOwnProperty(JSCompiler_renameProperty("__observedAttributes", this))) {
        register(this.prototype);
        const props = this._properties;
        this.__observedAttributes = props ? Object.keys(props).map((p14) => this.prototype._addPropertyToAttributeMap(p14)) : [];
      }
      return this.__observedAttributes;
    }
    /**
     * Finalizes an element definition, including ensuring any super classes
     * are also finalized. This includes ensuring property
     * accessors exist on the element prototype. This method calls
     * `_finalizeClass` to finalize each constructor in the prototype chain.
     * @return {void}
     * @nocollapse
     */
    static finalize() {
      if (!this.hasOwnProperty(JSCompiler_renameProperty("__finalized", this))) {
        const superCtor = superPropertiesClass(
          /** @type {!PropertiesMixinConstructor} */
          this
        );
        if (superCtor) {
          superCtor.finalize();
        }
        this.__finalized = true;
        this._finalizeClass();
      }
    }
    /**
     * Finalize an element class. This includes ensuring property
     * accessors exist on the element prototype. This method is called by
     * `finalize` and finalizes the class constructor.
     *
     * @protected
     * @nocollapse
     */
    static _finalizeClass() {
      const props = ownProperties(
        /** @type {!PropertiesMixinConstructor} */
        this
      );
      if (props) {
        this.createProperties(props);
      }
    }
    /**
     * Returns a memoized version of all properties, including those inherited
     * from super classes. Properties not in object format are converted to
     * at least {type}.
     *
     * @return {Object} Object containing properties for this class
     * @protected
     * @nocollapse
     */
    static get _properties() {
      if (!this.hasOwnProperty(
        JSCompiler_renameProperty("__properties", this)
      )) {
        const superCtor = superPropertiesClass(
          /** @type {!PropertiesMixinConstructor} */
          this
        );
        this.__properties = Object.assign(
          {},
          superCtor && superCtor._properties,
          ownProperties(
            /** @type {PropertiesMixinConstructor} */
            this
          )
        );
      }
      return this.__properties;
    }
    /**
     * Overrides `PropertiesChanged` method to return type specified in the
     * static `properties` object for the given property.
     * @param {string} name Name of property
     * @return {*} Type to which to deserialize attribute
     *
     * @protected
     * @nocollapse
     */
    static typeForProperty(name) {
      const info = this._properties[name];
      return info && info.type;
    }
    /**
     * Overrides `PropertiesChanged` method and adds a call to
     * `finalize` which lazily configures the element's property accessors.
     * @override
     * @return {void}
     */
    _initializeProperties() {
      incrementInstanceCount();
      this.constructor.finalize();
      super._initializeProperties();
    }
    /**
     * Called when the element is added to a document.
     * Calls `_enableProperties` to turn on property system from
     * `PropertiesChanged`.
     * @suppress {missingProperties} Super may or may not implement the callback
     * @return {void}
     * @override
     */
    connectedCallback() {
      if (super.connectedCallback) {
        super.connectedCallback();
      }
      this._enableProperties();
    }
    /**
     * Called when the element is removed from a document
     * @suppress {missingProperties} Super may or may not implement the callback
     * @return {void}
     * @override
     */
    disconnectedCallback() {
      if (super.disconnectedCallback) {
        super.disconnectedCallback();
      }
    }
  }
  return PropertiesMixin2;
});

// node_modules/@polymer/polymer/lib/mixins/element-mixin.js
var version = "3.5.2";
var builtCSS = window.ShadyCSS && window.ShadyCSS["cssBuild"];
var ElementMixin = dedupingMixin((base) => {
  const polymerElementBase = PropertiesMixin(PropertyEffects(base));
  function propertyDefaults(constructor) {
    if (!constructor.hasOwnProperty(
      JSCompiler_renameProperty("__propertyDefaults", constructor)
    )) {
      constructor.__propertyDefaults = null;
      let props = constructor._properties;
      for (let p14 in props) {
        let info = props[p14];
        if ("value" in info) {
          constructor.__propertyDefaults = constructor.__propertyDefaults || {};
          constructor.__propertyDefaults[p14] = info;
        }
      }
    }
    return constructor.__propertyDefaults;
  }
  function ownObservers(constructor) {
    if (!constructor.hasOwnProperty(
      JSCompiler_renameProperty("__ownObservers", constructor)
    )) {
      constructor.__ownObservers = constructor.hasOwnProperty(
        JSCompiler_renameProperty("observers", constructor)
      ) ? (
        /** @type {PolymerElementConstructor} */
        constructor.observers
      ) : null;
    }
    return constructor.__ownObservers;
  }
  function createPropertyFromConfig(proto2, name, info, allProps) {
    if (info.computed) {
      info.readOnly = true;
    }
    if (info.computed) {
      if (proto2._hasReadOnlyEffect(name)) {
        console.warn(`Cannot redefine computed property '${name}'.`);
      } else {
        proto2._createComputedProperty(name, info.computed, allProps);
      }
    }
    if (info.readOnly && !proto2._hasReadOnlyEffect(name)) {
      proto2._createReadOnlyProperty(name, !info.computed);
    } else if (info.readOnly === false && proto2._hasReadOnlyEffect(name)) {
      console.warn(`Cannot make readOnly property '${name}' non-readOnly.`);
    }
    if (info.reflectToAttribute && !proto2._hasReflectEffect(name)) {
      proto2._createReflectedProperty(name);
    } else if (info.reflectToAttribute === false && proto2._hasReflectEffect(name)) {
      console.warn(`Cannot make reflected property '${name}' non-reflected.`);
    }
    if (info.notify && !proto2._hasNotifyEffect(name)) {
      proto2._createNotifyingProperty(name);
    } else if (info.notify === false && proto2._hasNotifyEffect(name)) {
      console.warn(`Cannot make notify property '${name}' non-notify.`);
    }
    if (info.observer) {
      proto2._createPropertyObserver(name, info.observer, allProps[info.observer]);
    }
    proto2._addPropertyToAttributeMap(name);
  }
  function processElementStyles(klass, template2, is, baseURI) {
    if (!builtCSS) {
      const templateStyles = template2.content.querySelectorAll("style");
      const stylesWithImports = stylesFromTemplate(template2);
      const linkedStyles = stylesFromModuleImports(is);
      const firstTemplateChild = template2.content.firstElementChild;
      for (let idx = 0; idx < linkedStyles.length; idx++) {
        let s22 = linkedStyles[idx];
        s22.textContent = klass._processStyleText(s22.textContent, baseURI);
        template2.content.insertBefore(s22, firstTemplateChild);
      }
      let templateStyleIndex = 0;
      for (let i10 = 0; i10 < stylesWithImports.length; i10++) {
        let s22 = stylesWithImports[i10];
        let templateStyle = templateStyles[templateStyleIndex];
        if (templateStyle !== s22) {
          s22 = s22.cloneNode(true);
          templateStyle.parentNode.insertBefore(s22, templateStyle);
        } else {
          templateStyleIndex++;
        }
        s22.textContent = klass._processStyleText(s22.textContent, baseURI);
      }
    }
    if (window.ShadyCSS) {
      window.ShadyCSS.prepareTemplate(template2, is);
    }
    if (useAdoptedStyleSheetsWithBuiltCSS && builtCSS && supportsAdoptingStyleSheets) {
      const styles = template2.content.querySelectorAll("style");
      if (styles) {
        let css2 = "";
        Array.from(styles).forEach((s22) => {
          css2 += s22.textContent;
          s22.parentNode.removeChild(s22);
        });
        klass._styleSheet = new CSSStyleSheet();
        klass._styleSheet.replaceSync(css2);
      }
    }
  }
  function getTemplateFromDomModule(is) {
    let template2 = null;
    if (is && (!strictTemplatePolicy || allowTemplateFromDomModule)) {
      template2 = /** @type {?HTMLTemplateElement} */
      DomModule.import(is, "template");
      if (strictTemplatePolicy && !template2) {
        throw new Error(`strictTemplatePolicy: expecting dom-module or null template for ${is}`);
      }
    }
    return template2;
  }
  class PolymerElement2 extends polymerElementBase {
    /**
     * Current Polymer version in Semver notation.
     * @type {string} Semver notation of the current version of Polymer.
     * @nocollapse
     */
    static get polymerElementVersion() {
      return version;
    }
    /**
     * Override of PropertiesMixin _finalizeClass to create observers and
     * find the template.
     * @return {void}
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _finalizeClass() {
      polymerElementBase._finalizeClass.call(this);
      const observers = ownObservers(this);
      if (observers) {
        this.createObservers(observers, this._properties);
      }
      this._prepareTemplate();
    }
    /** @nocollapse */
    static _prepareTemplate() {
      let template2 = (
        /** @type {PolymerElementConstructor} */
        this.template
      );
      if (template2) {
        if (typeof template2 === "string") {
          console.error("template getter must return HTMLTemplateElement");
          template2 = null;
        } else if (!legacyOptimizations) {
          template2 = template2.cloneNode(true);
        }
      }
      this.prototype._template = template2;
    }
    /**
     * Override of PropertiesChanged createProperties to create accessors
     * and property effects for all of the properties.
     * @param {!Object} props .
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createProperties(props) {
      for (let p14 in props) {
        createPropertyFromConfig(
          /** @type {?} */
          this.prototype,
          p14,
          props[p14],
          props
        );
      }
    }
    /**
     * Creates observers for the given `observers` array.
     * Leverages `PropertyEffects` to create observers.
     * @param {Object} observers Array of observer descriptors for
     *   this class
     * @param {Object} dynamicFns Object containing keys for any properties
     *   that are functions and should trigger the effect when the function
     *   reference is changed
     * @return {void}
     * @protected
     * @nocollapse
     */
    static createObservers(observers, dynamicFns) {
      const proto2 = this.prototype;
      for (let i10 = 0; i10 < observers.length; i10++) {
        proto2._createMethodObserver(observers[i10], dynamicFns);
      }
    }
    /**
     * Returns the template that will be stamped into this element's shadow root.
     *
     * If a `static get is()` getter is defined, the default implementation will
     * return the first `<template>` in a `dom-module` whose `id` matches this
     * element's `is` (note that a `_template` property on the class prototype
     * takes precedence over the `dom-module` template, to maintain legacy
     * element semantics; a subclass will subsequently fall back to its super
     * class template if neither a `prototype._template` or a `dom-module` for
     * the class's `is` was found).
     *
     * Users may override this getter to return an arbitrary template
     * (in which case the `is` getter is unnecessary). The template returned
     * must be an `HTMLTemplateElement`.
     *
     * Note that when subclassing, if the super class overrode the default
     * implementation and the subclass would like to provide an alternate
     * template via a `dom-module`, it should override this getter and
     * return `DomModule.import(this.is, 'template')`.
     *
     * If a subclass would like to modify the super class template, it should
     * clone it rather than modify it in place.  If the getter does expensive
     * work such as cloning/modifying a template, it should memoize the
     * template for maximum performance:
     *
     *   let memoizedTemplate;
     *   class MySubClass extends MySuperClass {
     *     static get template() {
     *       if (!memoizedTemplate) {
     *         memoizedTemplate = super.template.cloneNode(true);
     *         let subContent = document.createElement('div');
     *         subContent.textContent = 'This came from MySubClass';
     *         memoizedTemplate.content.appendChild(subContent);
     *       }
     *       return memoizedTemplate;
     *     }
     *   }
     *
     * @return {!HTMLTemplateElement|string} Template to be stamped
     * @nocollapse
     */
    static get template() {
      if (!this.hasOwnProperty(JSCompiler_renameProperty("_template", this))) {
        let protoTemplate = this.prototype.hasOwnProperty(
          JSCompiler_renameProperty("_template", this.prototype)
        ) ? this.prototype._template : void 0;
        if (typeof protoTemplate === "function") {
          protoTemplate = protoTemplate();
        }
        this._template = // If user has put template on prototype (e.g. in legacy via registered
        // callback or info object), prefer that first. Note that `null` is
        // used as a sentinel to indicate "no template" and can be used to
        // override a super template, whereas `undefined` is used as a
        // sentinel to mean "fall-back to default template lookup" via
        // dom-module and/or super.template.
        protoTemplate !== void 0 ? protoTemplate : (
          // Look in dom-module associated with this element's is
          this.hasOwnProperty(JSCompiler_renameProperty("is", this)) && getTemplateFromDomModule(
            /** @type {PolymerElementConstructor}*/
            this.is
          ) || // Next look for superclass template (call the super impl this
          // way so that `this` points to the superclass)
          Object.getPrototypeOf(
            /** @type {PolymerElementConstructor}*/
            this.prototype
          ).constructor.template
        );
      }
      return this._template;
    }
    /**
     * Set the template.
     *
     * @param {!HTMLTemplateElement|string} value Template to set.
     * @nocollapse
     */
    static set template(value) {
      this._template = value;
    }
    /**
     * Path matching the url from which the element was imported.
     *
     * This path is used to resolve url's in template style cssText.
     * The `importPath` property is also set on element instances and can be
     * used to create bindings relative to the import path.
     *
     * For elements defined in ES modules, users should implement
     * `static get importMeta() { return import.meta; }`, and the default
     * implementation of `importPath` will  return `import.meta.url`'s path.
     * For elements defined in HTML imports, this getter will return the path
     * to the document containing a `dom-module` element matching this
     * element's static `is` property.
     *
     * Note, this path should contain a trailing `/`.
     *
     * @return {string} The import path for this element class
     * @suppress {missingProperties}
     * @nocollapse
     */
    static get importPath() {
      if (!this.hasOwnProperty(JSCompiler_renameProperty("_importPath", this))) {
        const meta = this.importMeta;
        if (meta) {
          this._importPath = pathFromUrl(meta.url);
        } else {
          const module = DomModule.import(
            /** @type {PolymerElementConstructor} */
            this.is
          );
          this._importPath = module && module.assetpath || Object.getPrototypeOf(
            /** @type {PolymerElementConstructor}*/
            this.prototype
          ).constructor.importPath;
        }
      }
      return this._importPath;
    }
    constructor() {
      super();
      this._template;
      this._importPath;
      this.rootPath;
      this.importPath;
      this.root;
      this.$;
    }
    /**
     * Overrides the default `PropertyAccessors` to ensure class
     * metaprogramming related to property accessors and effects has
     * completed (calls `finalize`).
     *
     * It also initializes any property defaults provided via `value` in
     * `properties` metadata.
     *
     * @return {void}
     * @override
     * @suppress {invalidCasts,missingProperties} go/missingfnprops
     */
    _initializeProperties() {
      this.constructor.finalize();
      this.constructor._finalizeTemplate(
        /** @type {!HTMLElement} */
        this.localName
      );
      super._initializeProperties();
      this.rootPath = rootPath;
      this.importPath = this.constructor.importPath;
      let p$ = propertyDefaults(this.constructor);
      if (!p$) {
        return;
      }
      for (let p14 in p$) {
        let info = p$[p14];
        if (this._canApplyPropertyDefault(p14)) {
          let value = typeof info.value == "function" ? info.value.call(this) : info.value;
          if (this._hasAccessor(p14)) {
            this._setPendingProperty(p14, value, true);
          } else {
            this[p14] = value;
          }
        }
      }
    }
    /**
     * Determines if a property dfeault can be applied. For example, this
     * prevents a default from being applied when a property that has no
     * accessor is overridden by its host before upgrade (e.g. via a binding).
     * @override
     * @param {string} property Name of the property
     * @return {boolean} Returns true if the property default can be applied.
     */
    _canApplyPropertyDefault(property) {
      return !this.hasOwnProperty(property);
    }
    /**
     * Gather style text for a style element in the template.
     *
     * @param {string} cssText Text containing styling to process
     * @param {string} baseURI Base URI to rebase CSS paths against
     * @return {string} The processed CSS text
     * @protected
     * @nocollapse
     */
    static _processStyleText(cssText, baseURI) {
      return resolveCss(cssText, baseURI);
    }
    /**
    * Configures an element `proto` to function with a given `template`.
    * The element name `is` and extends `ext` must be specified for ShadyCSS
    * style scoping.
    *
    * @param {string} is Tag name (or type extension name) for this element
    * @return {void}
    * @protected
    * @nocollapse
    */
    static _finalizeTemplate(is) {
      const template2 = this.prototype._template;
      if (template2 && !template2.__polymerFinalized) {
        template2.__polymerFinalized = true;
        const importPath = this.importPath;
        const baseURI = importPath ? resolveUrl(importPath) : "";
        processElementStyles(this, template2, is, baseURI);
        this.prototype._bindTemplate(template2);
      }
    }
    /**
     * Provides a default implementation of the standard Custom Elements
     * `connectedCallback`.
     *
     * The default implementation enables the property effects system and
     * flushes any pending properties, and updates shimmed CSS properties
     * when using the ShadyCSS scoping/custom properties polyfill.
     *
     * @override
     * @suppress {missingProperties, invalidCasts} Super may or may not
     *     implement the callback
     * @return {void}
     */
    connectedCallback() {
      if (window.ShadyCSS && this._template) {
        window.ShadyCSS.styleElement(
          /** @type {!HTMLElement} */
          this
        );
      }
      super.connectedCallback();
    }
    /**
     * Stamps the element template.
     *
     * @return {void}
     * @override
     */
    ready() {
      if (this._template) {
        this.root = this._stampTemplate(this._template);
        this.$ = this.root.$;
      }
      super.ready();
    }
    /**
     * Implements `PropertyEffects`'s `_readyClients` call. Attaches
     * element dom by calling `_attachDom` with the dom stamped from the
     * element's template via `_stampTemplate`. Note that this allows
     * client dom to be attached to the element prior to any observers
     * running.
     *
     * @return {void}
     * @override
     */
    _readyClients() {
      if (this._template) {
        this.root = this._attachDom(
          /** @type {StampedTemplate} */
          this.root
        );
      }
      super._readyClients();
    }
    /**
     * Attaches an element's stamped dom to itself. By default,
     * this method creates a `shadowRoot` and adds the dom to it.
     * However, this method may be overridden to allow an element
     * to put its dom in another location.
     *
     * @override
     * @throws {Error}
     * @suppress {missingReturn}
     * @param {StampedTemplate} dom to attach to the element.
     * @return {ShadowRoot} node to which the dom has been attached.
     */
    _attachDom(dom) {
      const n19 = wrap(this);
      if (n19.attachShadow) {
        if (dom) {
          if (!n19.shadowRoot) {
            n19.attachShadow({ mode: "open", shadyUpgradeFragment: dom });
            n19.shadowRoot.appendChild(dom);
            if (this.constructor._styleSheet) {
              n19.shadowRoot.adoptedStyleSheets = [this.constructor._styleSheet];
            }
          }
          if (syncInitialRender && window.ShadyDOM) {
            window.ShadyDOM.flushInitial(n19.shadowRoot);
          }
          return n19.shadowRoot;
        }
        return null;
      } else {
        throw new Error("ShadowDOM not available. PolymerElement can create dom as children instead of in ShadowDOM by setting `this.root = this;` before `ready`.");
      }
    }
    /**
     * When using the ShadyCSS scoping and custom property shim, causes all
     * shimmed styles in this element (and its subtree) to be updated
     * based on current custom property values.
     *
     * The optional parameter overrides inline custom property styles with an
     * object of properties where the keys are CSS properties, and the values
     * are strings.
     *
     * Example: `this.updateStyles({'--color': 'blue'})`
     *
     * These properties are retained unless a value of `null` is set.
     *
     * Note: This function does not support updating CSS mixins.
     * You can not dynamically change the value of an `@apply`.
     *
     * @override
     * @param {Object=} properties Bag of custom property key/values to
     *   apply to this element.
     * @return {void}
     * @suppress {invalidCasts}
     */
    updateStyles(properties) {
      if (window.ShadyCSS) {
        window.ShadyCSS.styleSubtree(
          /** @type {!HTMLElement} */
          this,
          properties
        );
      }
    }
    /**
     * Rewrites a given URL relative to a base URL. The base URL defaults to
     * the original location of the document containing the `dom-module` for
     * this element. This method will return the same URL before and after
     * bundling.
     *
     * Note that this function performs no resolution for URLs that start
     * with `/` (absolute URLs) or `#` (hash identifiers).  For general purpose
     * URL resolution, use `window.URL`.
     *
     * @override
     * @param {string} url URL to resolve.
     * @param {string=} base Optional base URL to resolve against, defaults
     * to the element's `importPath`
     * @return {string} Rewritten URL relative to base
     */
    resolveUrl(url, base2) {
      if (!base2 && this.importPath) {
        base2 = resolveUrl(this.importPath);
      }
      return resolveUrl(url, base2);
    }
    /**
     * Overrides `PropertyEffects` to add map of dynamic functions on
     * template info, for consumption by `PropertyEffects` template binding
     * code. This map determines which method templates should have accessors
     * created for them.
     *
     * @param {!HTMLTemplateElement} template Template
     * @param {!TemplateInfo} templateInfo Template metadata for current template
     * @param {!NodeInfo} nodeInfo Node metadata for current template.
     * @return {boolean} .
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _parseTemplateContent(template2, templateInfo, nodeInfo) {
      templateInfo.dynamicFns = templateInfo.dynamicFns || this._properties;
      return polymerElementBase._parseTemplateContent.call(
        this,
        template2,
        templateInfo,
        nodeInfo
      );
    }
    /**
     * Overrides `PropertyEffects` to warn on use of undeclared properties in
     * template.
     *
     * @param {Object} templateInfo Template metadata to add effect to
     * @param {string} prop Property that should trigger the effect
     * @param {Object=} effect Effect metadata object
     * @return {void}
     * @protected
     * @suppress {missingProperties} Interfaces in closure do not inherit statics, but classes do
     * @nocollapse
     */
    static _addTemplatePropertyEffect(templateInfo, prop, effect) {
      if (legacyWarnings && !(prop in this._properties) && // Methods used in templates with no dependencies (or only literal
      // dependencies) become accessors with template effects; ignore these
      !(effect.info.part.signature && effect.info.part.signature.static) && // Warnings for bindings added to nested templates are handled by
      // templatizer so ignore both the host-to-template bindings
      // (`hostProp`) and TemplateInstance-to-child bindings
      // (`nestedTemplate`)
      !effect.info.part.hostProp && !templateInfo.nestedTemplate) {
        console.warn(`Property '${prop}' used in template but not declared in 'properties'; attribute will not be observed.`);
      }
      return polymerElementBase._addTemplatePropertyEffect.call(
        this,
        templateInfo,
        prop,
        effect
      );
    }
  }
  return PolymerElement2;
});

// node_modules/@polymer/polymer/lib/utils/html-tag.js
var policy = window.trustedTypes && trustedTypes.createPolicy("polymer-html-literal", { createHTML: (s22) => s22 });
var LiteralString = class {
  /**
   * @param {!ITemplateArray} strings Constant parts of tagged template literal
   * @param {!Array<*>} values Variable parts of tagged template literal
   */
  constructor(strings, values) {
    assertValidTemplateStringParameters(strings, values);
    const string = values.reduce(
      (acc, v2, idx) => acc + literalValue(v2) + strings[idx + 1],
      strings[0]
    );
    this.value = string.toString();
  }
  /**
   * @return {string} LiteralString string value
   * @override
   */
  toString() {
    return this.value;
  }
};
function literalValue(value) {
  if (value instanceof LiteralString) {
    return (
      /** @type {!LiteralString} */
      value.value
    );
  } else {
    throw new Error(
      `non-literal value passed to Polymer's htmlLiteral function: ${value}`
    );
  }
}
function htmlValue(value) {
  if (value instanceof HTMLTemplateElement) {
    return (
      /** @type {!HTMLTemplateElement } */
      value.innerHTML
    );
  } else if (value instanceof LiteralString) {
    return literalValue(value);
  } else {
    throw new Error(
      `non-template value passed to Polymer's html function: ${value}`
    );
  }
}
var html = function html2(strings, ...values) {
  assertValidTemplateStringParameters(strings, values);
  const template2 = (
    /** @type {!HTMLTemplateElement} */
    document.createElement("template")
  );
  let value = values.reduce(
    (acc, v2, idx) => acc + htmlValue(v2) + strings[idx + 1],
    strings[0]
  );
  if (policy) {
    value = policy.createHTML(value);
  }
  template2.innerHTML = value;
  return template2;
};
var assertValidTemplateStringParameters = (strings, values) => {
  if (!Array.isArray(strings) || !Array.isArray(strings.raw) || values.length !== strings.length - 1) {
    throw new TypeError("Invalid call to the html template tag");
  }
};

// node_modules/@polymer/polymer/polymer-element.js
var PolymerElement = ElementMixin(HTMLElement);

// node_modules/@vaadin/component-base/src/async.js
var microtaskCurrHandle2 = 0;
var microtaskLastHandle2 = 0;
var microtaskCallbacks2 = [];
var microtaskScheduled2 = false;
function microtaskFlush2() {
  microtaskScheduled2 = false;
  const len = microtaskCallbacks2.length;
  for (let i10 = 0; i10 < len; i10++) {
    const cb = microtaskCallbacks2[i10];
    if (cb) {
      try {
        cb();
      } catch (e17) {
        setTimeout(() => {
          throw e17;
        });
      }
    }
  }
  microtaskCallbacks2.splice(0, len);
  microtaskLastHandle2 += len;
}
var timeOut = {
  /**
   * Returns a sub-module with the async interface providing the provided
   * delay.
   *
   * @memberof timeOut
   * @param {number=} delay Time to wait before calling callbacks in ms
   * @return {!AsyncInterface} An async timeout interface
   */
  after(delay) {
    return {
      run(fn) {
        return window.setTimeout(fn, delay);
      },
      cancel(handle) {
        window.clearTimeout(handle);
      }
    };
  },
  /**
   * Enqueues a function called in the next task.
   *
   * @memberof timeOut
   * @param {!Function} fn Callback to run
   * @param {number=} delay Delay in milliseconds
   * @return {number} Handle used for canceling task
   */
  run(fn, delay) {
    return window.setTimeout(fn, delay);
  },
  /**
   * Cancels a previously enqueued `timeOut` callback.
   *
   * @memberof timeOut
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(handle) {
    window.clearTimeout(handle);
  }
};
var animationFrame = {
  /**
   * Enqueues a function called at `requestAnimationFrame` timing.
   *
   * @memberof animationFrame
   * @param {function(number):void} fn Callback to run
   * @return {number} Handle used for canceling task
   */
  run(fn) {
    return window.requestAnimationFrame(fn);
  },
  /**
   * Cancels a previously enqueued `animationFrame` callback.
   *
   * @memberof animationFrame
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(handle) {
    window.cancelAnimationFrame(handle);
  }
};
var idlePeriod = {
  /**
   * Enqueues a function called at `requestIdleCallback` timing.
   *
   * @memberof idlePeriod
   * @param {function(!IdleDeadline):void} fn Callback to run
   * @return {number} Handle used for canceling task
   */
  run(fn) {
    return window.requestIdleCallback ? window.requestIdleCallback(fn) : window.setTimeout(fn, 16);
  },
  /**
   * Cancels a previously enqueued `idlePeriod` callback.
   *
   * @memberof idlePeriod
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(handle) {
    if (window.cancelIdleCallback) {
      window.cancelIdleCallback(handle);
    } else {
      window.clearTimeout(handle);
    }
  }
};
var microTask2 = {
  /**
   * Enqueues a function called at microtask timing.
   *
   * @memberof microTask
   * @param {!Function=} callback Callback to run
   * @return {number} Handle used for canceling task
   */
  run(callback) {
    if (!microtaskScheduled2) {
      microtaskScheduled2 = true;
      queueMicrotask(() => microtaskFlush2());
    }
    microtaskCallbacks2.push(callback);
    const result = microtaskCurrHandle2;
    microtaskCurrHandle2 += 1;
    return result;
  },
  /**
   * Cancels a previously enqueued `microTask` callback.
   *
   * @memberof microTask
   * @param {number} handle Handle returned from `run` of callback to cancel
   * @return {void}
   */
  cancel(handle) {
    const idx = handle - microtaskLastHandle2;
    if (idx >= 0) {
      if (!microtaskCallbacks2[idx]) {
        throw new Error(`invalid async handle: ${handle}`);
      }
      microtaskCallbacks2[idx] = null;
    }
  }
};

// node_modules/@vaadin/component-base/src/debounce.js
var debouncerQueue = /* @__PURE__ */ new Set();
var Debouncer = class _Debouncer {
  /**
   * Creates a debouncer if no debouncer is passed as a parameter
   * or it cancels an active debouncer otherwise. The following
   * example shows how a debouncer can be called multiple times within a
   * microtask and "debounced" such that the provided callback function is
   * called once. Add this method to a custom element:
   *
   * ```js
   * import {microTask} from '@vaadin/component-base/src/async.js';
   * import {Debouncer} from '@vaadin/component-base/src/debounce.js';
   * // ...
   *
   * _debounceWork() {
   *   this._debounceJob = Debouncer.debounce(this._debounceJob,
   *       microTask, () => this._doWork());
   * }
   * ```
   *
   * If the `_debounceWork` method is called multiple times within the same
   * microtask, the `_doWork` function will be called only once at the next
   * microtask checkpoint.
   *
   * Note: In testing it is often convenient to avoid asynchrony. To accomplish
   * this with a debouncer, you can use `enqueueDebouncer` and
   * `flush`. For example, extend the above example by adding
   * `enqueueDebouncer(this._debounceJob)` at the end of the
   * `_debounceWork` method. Then in a test, call `flush` to ensure
   * the debouncer has completed.
   *
   * @param {Debouncer?} debouncer Debouncer object.
   * @param {!AsyncInterface} asyncModule Object with Async interface
   * @param {function()} callback Callback to run.
   * @return {!Debouncer} Returns a debouncer object.
   */
  static debounce(debouncer, asyncModule, callback) {
    if (debouncer instanceof _Debouncer) {
      debouncer._cancelAsync();
    } else {
      debouncer = new _Debouncer();
    }
    debouncer.setConfig(asyncModule, callback);
    return debouncer;
  }
  constructor() {
    this._asyncModule = null;
    this._callback = null;
    this._timer = null;
  }
  /**
   * Sets the scheduler; that is, a module with the Async interface,
   * a callback and optional arguments to be passed to the run function
   * from the async module.
   *
   * @param {!AsyncInterface} asyncModule Object with Async interface.
   * @param {function()} callback Callback to run.
   * @return {void}
   */
  setConfig(asyncModule, callback) {
    this._asyncModule = asyncModule;
    this._callback = callback;
    this._timer = this._asyncModule.run(() => {
      this._timer = null;
      debouncerQueue.delete(this);
      this._callback();
    });
  }
  /**
   * Cancels an active debouncer and returns a reference to itself.
   *
   * @return {void}
   */
  cancel() {
    if (this.isActive()) {
      this._cancelAsync();
      debouncerQueue.delete(this);
    }
  }
  /**
   * Cancels a debouncer's async callback.
   *
   * @return {void}
   */
  _cancelAsync() {
    if (this.isActive()) {
      this._asyncModule.cancel(
        /** @type {number} */
        this._timer
      );
      this._timer = null;
    }
  }
  /**
   * Flushes an active debouncer and returns a reference to itself.
   *
   * @return {void}
   */
  flush() {
    if (this.isActive()) {
      this.cancel();
      this._callback();
    }
  }
  /**
   * Returns true if the debouncer is active.
   *
   * @return {boolean} True if active.
   */
  isActive() {
    return this._timer != null;
  }
};
function enqueueDebouncer(debouncer) {
  debouncerQueue.add(debouncer);
}
function flushDebouncers() {
  const didFlush = Boolean(debouncerQueue.size);
  debouncerQueue.forEach((debouncer) => {
    try {
      debouncer.flush();
    } catch (e17) {
      setTimeout(() => {
        throw e17;
      });
    }
  });
  return didFlush;
}
var flush = () => {
  let debouncers;
  do {
    debouncers = flushDebouncers();
  } while (debouncers);
};

// node_modules/@vaadin/component-base/src/dir-mixin.js
var directionSubscribers = [];
function alignDirs(element, documentDir, elementDir = element.getAttribute("dir")) {
  if (documentDir) {
    element.setAttribute("dir", documentDir);
  } else if (elementDir != null) {
    element.removeAttribute("dir");
  }
}
function getDocumentDir() {
  return document.documentElement.getAttribute("dir");
}
function directionUpdater() {
  const documentDir = getDocumentDir();
  directionSubscribers.forEach((element) => {
    alignDirs(element, documentDir);
  });
}
var directionObserver = new MutationObserver(directionUpdater);
directionObserver.observe(document.documentElement, { attributes: true, attributeFilter: ["dir"] });
var DirMixin = (superClass) => class VaadinDirMixin extends superClass {
  static get properties() {
    return {
      /**
       * @protected
       */
      dir: {
        type: String,
        value: "",
        reflectToAttribute: true,
        converter: {
          fromAttribute: (attr) => {
            return !attr ? "" : attr;
          },
          toAttribute: (prop) => {
            return prop === "" ? null : prop;
          }
        }
      }
    };
  }
  /**
   * @return {boolean}
   * @protected
   */
  get __isRTL() {
    return this.getAttribute("dir") === "rtl";
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    if (!this.hasAttribute("dir") || this.__restoreSubscription) {
      this.__subscribe();
      alignDirs(this, getDocumentDir(), null);
    }
  }
  /** @protected */
  attributeChangedCallback(name, oldValue, newValue) {
    super.attributeChangedCallback(name, oldValue, newValue);
    if (name !== "dir") {
      return;
    }
    const documentDir = getDocumentDir();
    const newValueEqlDocDir = newValue === documentDir && directionSubscribers.indexOf(this) === -1;
    const newValueEmptied = !newValue && oldValue && directionSubscribers.indexOf(this) === -1;
    const newDiffValue = newValue !== documentDir && oldValue === documentDir;
    if (newValueEqlDocDir || newValueEmptied) {
      this.__subscribe();
      alignDirs(this, documentDir, newValue);
    } else if (newDiffValue) {
      this.__unsubscribe();
    }
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    this.__restoreSubscription = directionSubscribers.includes(this);
    this.__unsubscribe();
  }
  /** @protected */
  _valueToNodeAttribute(node, value, attribute) {
    if (attribute === "dir" && value === "" && !node.hasAttribute("dir")) {
      return;
    }
    super._valueToNodeAttribute(node, value, attribute);
  }
  /** @protected */
  _attributeToProperty(attribute, value, type) {
    if (attribute === "dir" && !value) {
      this.dir = "";
    } else {
      super._attributeToProperty(attribute, value, type);
    }
  }
  /** @private */
  __subscribe() {
    if (!directionSubscribers.includes(this)) {
      directionSubscribers.push(this);
    }
  }
  /** @private */
  __unsubscribe() {
    if (directionSubscribers.includes(this)) {
      directionSubscribers.splice(directionSubscribers.indexOf(this), 1);
    }
  }
};

// node_modules/@vaadin/component-base/src/path-utils.js
function get2(path, object) {
  return path.split(".").reduce((obj, property) => obj ? obj[property] : void 0, object);
}

// node_modules/@vaadin/component-base/src/templates.js
function processTemplates(component) {
  if (window.Vaadin && window.Vaadin.templateRendererCallback) {
    window.Vaadin.templateRendererCallback(component);
    return;
  }
  if (component.querySelector("template")) {
    console.warn(
      `WARNING: <template> inside <${component.localName}> is no longer supported. Import @vaadin/polymer-legacy-adapter/template-renderer.js to enable compatibility.`
    );
  }
}

// node_modules/@vaadin/component-base/src/dom-utils.js
function getClosestElement(selector, node) {
  if (!node) {
    return null;
  }
  return node.closest(selector) || getClosestElement(selector, node.getRootNode().host);
}
function deserializeAttributeValue(value) {
  if (!value) {
    return /* @__PURE__ */ new Set();
  }
  return new Set(value.split(" "));
}
function serializeAttributeValue(values) {
  return values ? [...values].join(" ") : "";
}
function addValueToAttribute(element, attr, value) {
  const values = deserializeAttributeValue(element.getAttribute(attr));
  values.add(value);
  element.setAttribute(attr, serializeAttributeValue(values));
}
function removeValueFromAttribute(element, attr, value) {
  const values = deserializeAttributeValue(element.getAttribute(attr));
  values.delete(value);
  if (values.size === 0) {
    element.removeAttribute(attr);
    return;
  }
  element.setAttribute(attr, serializeAttributeValue(values));
}
function isEmptyTextNode(node) {
  return node.nodeType === Node.TEXT_NODE && node.textContent.trim() === "";
}

// node_modules/@vaadin/grid/src/vaadin-grid-helpers.js
function getBodyRowCells(row) {
  return row.__cells || Array.from(row.querySelectorAll('[part~="cell"]:not([part~="details-cell"])'));
}
function iterateChildren(container, callback) {
  [...container.children].forEach(callback);
}
function iterateRowCells(row, callback) {
  getBodyRowCells(row).forEach(callback);
  if (row.__detailsCell) {
    callback(row.__detailsCell);
  }
}
function updateColumnOrders(columns, scope, baseOrder) {
  let c17 = 1;
  columns.forEach((column) => {
    if (c17 % 10 === 0) {
      c17 += 1;
    }
    column._order = baseOrder + c17 * scope;
    c17 += 1;
  });
}
function updateState(element, attribute, value) {
  switch (typeof value) {
    case "boolean":
      element.toggleAttribute(attribute, value);
      break;
    case "string":
      element.setAttribute(attribute, value);
      break;
    default:
      element.removeAttribute(attribute);
      break;
  }
}
function updatePart(element, value, part) {
  if (value || value === "") {
    addValueToAttribute(element, "part", part);
  } else {
    removeValueFromAttribute(element, "part", part);
  }
}
function updateCellsPart(cells, part, value) {
  cells.forEach((cell) => {
    updatePart(cell, value, part);
  });
}
function updateBooleanRowStates(row, states) {
  const cells = getBodyRowCells(row);
  Object.entries(states).forEach(([state, value]) => {
    updateState(row, state, value);
    const rowPart = `${state}-row`;
    updatePart(row, value, rowPart);
    updateCellsPart(cells, `${rowPart}-cell`, value);
  });
}
function updateStringRowStates(row, states) {
  const cells = getBodyRowCells(row);
  Object.entries(states).forEach(([state, value]) => {
    const prevValue = row.getAttribute(state);
    updateState(row, state, value);
    if (prevValue) {
      const prevRowPart = `${state}-${prevValue}-row`;
      updatePart(row, false, prevRowPart);
      updateCellsPart(cells, `${prevRowPart}-cell`, false);
    }
    if (value) {
      const rowPart = `${state}-${value}-row`;
      updatePart(row, value, rowPart);
      updateCellsPart(cells, `${rowPart}-cell`, value);
    }
  });
}
function updateCellState(cell, attribute, value, part, oldPart) {
  updateState(cell, attribute, value);
  if (oldPart) {
    updatePart(cell, false, oldPart);
  }
  updatePart(cell, value, part || `${attribute}-cell`);
}
function findTreeToggleCell(row) {
  return getBodyRowCells(row).find((cell) => cell._content.querySelector("vaadin-grid-tree-toggle"));
}
var ColumnObserver = class _ColumnObserver {
  constructor(host, callback) {
    this.__host = host;
    this.__callback = callback;
    this.__currentSlots = [];
    this.__onMutation = this.__onMutation.bind(this);
    this.__observer = new MutationObserver(this.__onMutation);
    this.__observer.observe(host, {
      childList: true
    });
    this.__initialCallDebouncer = Debouncer.debounce(this.__initialCallDebouncer, microTask2, () => this.__onMutation());
  }
  disconnect() {
    this.__observer.disconnect();
    this.__initialCallDebouncer.cancel();
    this.__toggleSlotChangeListeners(false);
  }
  flush() {
    this.__onMutation();
  }
  __toggleSlotChangeListeners(add) {
    this.__currentSlots.forEach((slot) => {
      if (add) {
        slot.addEventListener("slotchange", this.__onMutation);
      } else {
        slot.removeEventListener("slotchange", this.__onMutation);
      }
    });
  }
  __onMutation() {
    const initialCall = !this.__currentColumns;
    this.__currentColumns = this.__currentColumns || [];
    const columns = _ColumnObserver.getColumns(this.__host);
    const addedColumns = columns.filter((column) => !this.__currentColumns.includes(column));
    const removedColumns = this.__currentColumns.filter((column) => !columns.includes(column));
    const orderChanged = this.__currentColumns.some((column, index) => column !== columns[index]);
    this.__currentColumns = columns;
    this.__toggleSlotChangeListeners(false);
    this.__currentSlots = [...this.__host.children].filter((child) => child instanceof HTMLSlotElement);
    this.__toggleSlotChangeListeners(true);
    const invokeCallback = initialCall || addedColumns.length || removedColumns.length || orderChanged;
    if (invokeCallback) {
      this.__callback(addedColumns, removedColumns);
    }
  }
  /**
   * Default filter for column elements.
   */
  static __isColumnElement(node) {
    return node.nodeType === Node.ELEMENT_NODE && /\bcolumn\b/u.test(node.localName);
  }
  static getColumns(host) {
    const columns = [];
    const isColumnElement = host._isColumnElement || _ColumnObserver.__isColumnElement;
    [...host.children].forEach((child) => {
      if (isColumnElement(child)) {
        columns.push(child);
      } else if (child instanceof HTMLSlotElement) {
        [...child.assignedElements({ flatten: true })].filter((assignedElement) => isColumnElement(assignedElement)).forEach((assignedElement) => columns.push(assignedElement));
      }
    });
    return columns;
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-column-mixin.js
var ColumnBaseMixin = (superClass) => class ColumnBaseMixin extends superClass {
  static get properties() {
    return {
      /**
       * When set to true, the column is user-resizable.
       * @default false
       */
      resizable: {
        type: Boolean,
        sync: true,
        value() {
          if (this.localName === "vaadin-grid-column-group") {
            return;
          }
          const parent = this.parentNode;
          if (parent && parent.localName === "vaadin-grid-column-group") {
            return parent.resizable || false;
          }
          return false;
        }
      },
      /**
       * When true, the column is frozen. When a column inside of a column group is frozen,
       * all of the sibling columns inside the group will get frozen also.
       * @type {boolean}
       */
      frozen: {
        type: Boolean,
        value: false,
        sync: true
      },
      /**
       * When true, the column is frozen to end of grid.
       *
       * When a column inside of a column group is frozen to end, all of the sibling columns
       * inside the group will get frozen to end also.
       *
       * Column can not be set as `frozen` and `frozenToEnd` at the same time.
       * @attr {boolean} frozen-to-end
       * @type {boolean}
       */
      frozenToEnd: {
        type: Boolean,
        value: false,
        sync: true
      },
      /**
       * When true, the cells for this column will be rendered with the `role` attribute
       * set as `rowheader`, instead of the `gridcell` role value used by default.
       *
       * When a column is set as row header, its cells will be announced by screen readers
       * while navigating to help user identify the current row as uniquely as possible.
       *
       * @attr {boolean} row-header
       * @type {boolean}
       */
      rowHeader: {
        type: Boolean,
        value: false,
        sync: true
      },
      /**
       * When set to true, the cells for this column are hidden.
       */
      hidden: {
        type: Boolean,
        value: false,
        sync: true
      },
      /**
       * Text content to display in the header cell of the column.
       */
      header: {
        type: String,
        sync: true
      },
      /**
       * Aligns the columns cell content horizontally.
       * Supported values: "start", "center" and "end".
       * @attr {start|center|end} text-align
       * @type {GridColumnTextAlign | null | undefined}
       */
      textAlign: {
        type: String,
        sync: true
      },
      /**
       * Custom part name for the header cell.
       *
       * @attr {string} header-part-name
       */
      headerPartName: {
        type: String,
        sync: true
      },
      /**
       * Custom part name for the footer cell.
       *
       * @attr {string} footer-part-name
       */
      footerPartName: {
        type: String,
        sync: true
      },
      /**
       * @type {boolean}
       * @protected
       */
      _lastFrozen: {
        type: Boolean,
        value: false,
        sync: true
      },
      /**
       * @type {boolean}
       * @protected
       */
      _bodyContentHidden: {
        type: Boolean,
        value: false,
        sync: true
      },
      /**
       * @type {boolean}
       * @protected
       */
      _firstFrozenToEnd: {
        type: Boolean,
        value: false,
        sync: true
      },
      /** @protected */
      _order: {
        type: Number,
        sync: true
      },
      /** @private */
      _reorderStatus: {
        type: Boolean,
        sync: true
      },
      /**
       * @type {Array<!HTMLElement>}
       * @protected
       */
      _emptyCells: Array,
      /** @private */
      _headerCell: {
        type: Object,
        sync: true
      },
      /** @private */
      _footerCell: {
        type: Object,
        sync: true
      },
      /** @protected */
      _grid: Object,
      /**
       * By default, the Polymer doesn't invoke the observer
       * during initialization if all of its dependencies are `undefined`.
       * This internal property can be used to force initial invocation of an observer
       * even the other dependencies of the observer are `undefined`.
       *
       * @private
       */
      __initialized: {
        type: Boolean,
        value: true
      },
      /**
       * Custom function for rendering the header content.
       * Receives two arguments:
       *
       * - `root` The header cell content DOM element. Append your content to it.
       * - `column` The `<vaadin-grid-column>` element.
       *
       * @type {GridHeaderFooterRenderer | null | undefined}
       */
      headerRenderer: {
        type: Function,
        sync: true
      },
      /**
       * Represents the final header renderer computed on the set of observable arguments.
       * It is supposed to be used internally when rendering the header cell content.
       *
       * @protected
       * @type {GridHeaderFooterRenderer | undefined}
       */
      _headerRenderer: {
        type: Function,
        computed: "_computeHeaderRenderer(headerRenderer, header, __initialized)"
      },
      /**
       * Custom function for rendering the footer content.
       * Receives two arguments:
       *
       * - `root` The footer cell content DOM element. Append your content to it.
       * - `column` The `<vaadin-grid-column>` element.
       *
       * @type {GridHeaderFooterRenderer | null | undefined}
       */
      footerRenderer: {
        type: Function,
        sync: true
      },
      /**
       * Represents the final footer renderer computed on the set of observable arguments.
       * It is supposed to be used internally when rendering the footer cell content.
       *
       * @protected
       * @type {GridHeaderFooterRenderer | undefined}
       */
      _footerRenderer: {
        type: Function,
        computed: "_computeFooterRenderer(footerRenderer, __initialized)"
      },
      /**
       * An internal property that is mainly used by `vaadin-template-renderer`
       * to identify grid column elements.
       *
       * @private
       */
      __gridColumnElement: {
        type: Boolean,
        value: true
      }
    };
  }
  static get observers() {
    return [
      "_widthChanged(width, _headerCell, _footerCell, _cells)",
      "_frozenChanged(frozen, _headerCell, _footerCell, _cells)",
      "_frozenToEndChanged(frozenToEnd, _headerCell, _footerCell, _cells)",
      "_flexGrowChanged(flexGrow, _headerCell, _footerCell, _cells)",
      "_textAlignChanged(textAlign, _cells, _headerCell, _footerCell)",
      "_orderChanged(_order, _headerCell, _footerCell, _cells)",
      "_lastFrozenChanged(_lastFrozen)",
      "_firstFrozenToEndChanged(_firstFrozenToEnd)",
      "_onRendererOrBindingChanged(_renderer, _cells, _bodyContentHidden, path)",
      "_onHeaderRendererOrBindingChanged(_headerRenderer, _headerCell, path, header)",
      "_onFooterRendererOrBindingChanged(_footerRenderer, _footerCell)",
      "_resizableChanged(resizable, _headerCell)",
      "_reorderStatusChanged(_reorderStatus, _headerCell, _footerCell, _cells)",
      "_hiddenChanged(hidden, _headerCell, _footerCell, _cells)",
      "_rowHeaderChanged(rowHeader, _cells)",
      "__headerFooterPartNameChanged(_headerCell, _footerCell, headerPartName, footerPartName)"
    ];
  }
  /**
   * @return {!Grid | undefined}
   * @protected
   */
  get _grid() {
    if (!this._gridValue) {
      this._gridValue = this._findHostGrid();
    }
    return this._gridValue;
  }
  /**
   * @return {!Array<!HTMLElement>}
   * @protected
   */
  get _allCells() {
    return [].concat(this._cells || []).concat(this._emptyCells || []).concat(this._headerCell).concat(this._footerCell).filter((cell) => cell);
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    requestAnimationFrame(() => {
      if (!this._grid) {
        return;
      }
      this._allCells.forEach((cell) => {
        if (!cell._content.parentNode) {
          this._grid.appendChild(cell._content);
        }
      });
    });
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    requestAnimationFrame(() => {
      if (this._grid) {
        return;
      }
      this._allCells.forEach((cell) => {
        if (cell._content.parentNode) {
          cell._content.parentNode.removeChild(cell._content);
        }
      });
    });
    this._gridValue = void 0;
  }
  /** @protected */
  ready() {
    super.ready();
    processTemplates(this);
  }
  /**
   * @return {!Grid | undefined}
   * @protected
   */
  _findHostGrid() {
    let el = this;
    while (el && !/^vaadin.*grid(-pro)?$/u.test(el.localName)) {
      el = el.assignedSlot ? el.assignedSlot.parentNode : el.parentNode;
    }
    return el || void 0;
  }
  /** @protected */
  _renderHeaderAndFooter() {
    this._renderHeaderCellContent(this._headerRenderer, this._headerCell);
    this._renderFooterCellContent(this._footerRenderer, this._footerCell);
  }
  /** @private */
  _flexGrowChanged(flexGrow) {
    if (this.parentElement && this.parentElement._columnPropChanged) {
      this.parentElement._columnPropChanged("flexGrow");
    }
    this._allCells.forEach((cell) => {
      cell.style.flexGrow = flexGrow;
    });
  }
  /** @private */
  _orderChanged(order) {
    this._allCells.forEach((cell) => {
      cell.style.order = order;
    });
  }
  /** @private */
  _widthChanged(width) {
    if (this.parentElement && this.parentElement._columnPropChanged) {
      this.parentElement._columnPropChanged("width");
    }
    this._allCells.forEach((cell) => {
      cell.style.width = width;
    });
  }
  /** @private */
  _frozenChanged(frozen) {
    if (this.parentElement && this.parentElement._columnPropChanged) {
      this.parentElement._columnPropChanged("frozen", frozen);
    }
    this._allCells.forEach((cell) => {
      updateCellState(cell, "frozen", frozen);
    });
    if (this._grid && this._grid._frozenCellsChanged) {
      this._grid._frozenCellsChanged();
    }
  }
  /** @private */
  _frozenToEndChanged(frozenToEnd) {
    if (this.parentElement && this.parentElement._columnPropChanged) {
      this.parentElement._columnPropChanged("frozenToEnd", frozenToEnd);
    }
    this._allCells.forEach((cell) => {
      if (this._grid && cell.parentElement === this._grid.$.sizer) {
        return;
      }
      updateCellState(cell, "frozen-to-end", frozenToEnd);
    });
    if (this._grid && this._grid._frozenCellsChanged) {
      this._grid._frozenCellsChanged();
    }
  }
  /** @private */
  _lastFrozenChanged(lastFrozen) {
    this._allCells.forEach((cell) => {
      updateCellState(cell, "last-frozen", lastFrozen);
    });
    if (this.parentElement && this.parentElement._columnPropChanged) {
      this.parentElement._lastFrozen = lastFrozen;
    }
  }
  /** @private */
  _firstFrozenToEndChanged(firstFrozenToEnd) {
    this._allCells.forEach((cell) => {
      if (this._grid && cell.parentElement === this._grid.$.sizer) {
        return;
      }
      updateCellState(cell, "first-frozen-to-end", firstFrozenToEnd);
    });
    if (this.parentElement && this.parentElement._columnPropChanged) {
      this.parentElement._firstFrozenToEnd = firstFrozenToEnd;
    }
  }
  /** @private */
  _rowHeaderChanged(rowHeader, cells) {
    if (!cells) {
      return;
    }
    cells.forEach((cell) => {
      cell.setAttribute("role", rowHeader ? "rowheader" : "gridcell");
    });
  }
  /**
   * @param {string} path
   * @return {string}
   * @protected
   */
  _generateHeader(path) {
    return path.substr(path.lastIndexOf(".") + 1).replace(/([A-Z])/gu, "-$1").toLowerCase().replace(/-/gu, " ").replace(/^./u, (match) => match.toUpperCase());
  }
  /** @private */
  _reorderStatusChanged(reorderStatus) {
    const prevStatus = this.__previousReorderStatus;
    const oldPart = prevStatus ? `reorder-${prevStatus}-cell` : "";
    const newPart = `reorder-${reorderStatus}-cell`;
    this._allCells.forEach((cell) => {
      updateCellState(cell, "reorder-status", reorderStatus, newPart, oldPart);
    });
    this.__previousReorderStatus = reorderStatus;
  }
  /** @private */
  _resizableChanged(resizable, headerCell) {
    if (resizable === void 0 || headerCell === void 0) {
      return;
    }
    if (headerCell) {
      [headerCell].concat(this._emptyCells).forEach((cell) => {
        if (cell) {
          const existingHandle = cell.querySelector('[part~="resize-handle"]');
          if (existingHandle) {
            cell.removeChild(existingHandle);
          }
          if (resizable) {
            const handle = document.createElement("div");
            handle.setAttribute("part", "resize-handle");
            cell.appendChild(handle);
          }
        }
      });
    }
  }
  /** @private */
  _textAlignChanged(textAlign) {
    if (textAlign === void 0 || this._grid === void 0) {
      return;
    }
    if (["start", "end", "center"].indexOf(textAlign) === -1) {
      console.warn('textAlign can only be set as "start", "end" or "center"');
      return;
    }
    this._allCells.forEach((cell) => {
      cell._content.style.textAlign = textAlign;
    });
  }
  /** @private */
  _hiddenChanged(hidden) {
    if (this.parentElement && this.parentElement._columnPropChanged) {
      this.parentElement._columnPropChanged("hidden", hidden);
    }
    if (!!hidden !== !!this._previousHidden && this._grid) {
      if (hidden === true) {
        this._allCells.forEach((cell) => {
          if (cell._content.parentNode) {
            cell._content.parentNode.removeChild(cell._content);
          }
        });
      }
      this._grid._debouncerHiddenChanged = Debouncer.debounce(
        this._grid._debouncerHiddenChanged,
        animationFrame,
        () => {
          if (this._grid && this._grid._renderColumnTree) {
            this._grid._renderColumnTree(this._grid._columnTree);
          }
        }
      );
      if (this._grid._debounceUpdateFrozenColumn) {
        this._grid._debounceUpdateFrozenColumn();
      }
      if (this._grid._resetKeyboardNavigation) {
        this._grid._resetKeyboardNavigation();
      }
    }
    this._previousHidden = hidden;
  }
  /** @protected */
  _runRenderer(renderer, cell, model) {
    const isVisibleBodyCell = model && model.item && !cell.parentElement.hidden;
    const shouldRender = isVisibleBodyCell || renderer === this._headerRenderer || renderer === this._footerRenderer;
    if (!shouldRender) {
      return;
    }
    const args = [cell._content, this];
    if (isVisibleBodyCell) {
      args.push(model);
    }
    renderer.apply(this, args);
  }
  /**
   * Renders the content to the given cells using a renderer.
   *
   * @private
   */
  __renderCellsContent(renderer, cells) {
    if (this.hidden || !this._grid) {
      return;
    }
    cells.forEach((cell) => {
      if (!cell.parentElement) {
        return;
      }
      const model = this._grid.__getRowModel(cell.parentElement);
      if (!renderer) {
        return;
      }
      if (cell._renderer !== renderer) {
        this._clearCellContent(cell);
      }
      cell._renderer = renderer;
      this._runRenderer(renderer, cell, model);
    });
  }
  /**
   * Clears the content of a cell.
   *
   * @protected
   */
  _clearCellContent(cell) {
    cell._content.innerHTML = "";
    delete cell._content._$litPart$;
  }
  /**
   * Renders the header cell content using a renderer,
   * and then updates the visibility of the parent row depending on
   * whether all its children cells are empty or not.
   *
   * @protected
   */
  _renderHeaderCellContent(headerRenderer, headerCell) {
    if (!headerCell || !headerRenderer) {
      return;
    }
    this.__renderCellsContent(headerRenderer, [headerCell]);
    if (this._grid && headerCell.parentElement) {
      this._grid.__debounceUpdateHeaderFooterRowVisibility(headerCell.parentElement);
    }
  }
  /** @protected */
  _onHeaderRendererOrBindingChanged(headerRenderer, headerCell, ..._bindings) {
    this._renderHeaderCellContent(headerRenderer, headerCell);
  }
  /** @private */
  __headerFooterPartNameChanged(headerCell, footerCell, headerPartName, footerPartName) {
    [
      { cell: headerCell, partName: headerPartName },
      { cell: footerCell, partName: footerPartName }
    ].forEach(({ cell, partName }) => {
      if (cell) {
        const customParts = cell.__customParts || [];
        cell.part.remove(...customParts);
        cell.__customParts = partName ? partName.trim().split(" ") : [];
        cell.part.add(...cell.__customParts);
      }
    });
  }
  /**
   * Renders the content of body cells using a renderer.
   *
   * @protected
   */
  _renderBodyCellsContent(renderer, cells) {
    if (!cells || !renderer) {
      return;
    }
    this.__renderCellsContent(renderer, cells);
  }
  /** @protected */
  _onRendererOrBindingChanged(renderer, cells, ..._bindings) {
    this._renderBodyCellsContent(renderer, cells);
  }
  /**
   * Renders the footer cell content using a renderer
   * and then updates the visibility of the parent row depending on
   * whether all its children cells are empty or not.
   *
   * @protected
   */
  _renderFooterCellContent(footerRenderer, footerCell) {
    if (!footerCell || !footerRenderer) {
      return;
    }
    this.__renderCellsContent(footerRenderer, [footerCell]);
    if (this._grid && footerCell.parentElement) {
      this._grid.__debounceUpdateHeaderFooterRowVisibility(footerCell.parentElement);
    }
  }
  /** @protected */
  _onFooterRendererOrBindingChanged(footerRenderer, footerCell) {
    this._renderFooterCellContent(footerRenderer, footerCell);
  }
  /** @private */
  __setTextContent(node, textContent) {
    if (node.textContent !== textContent) {
      node.textContent = textContent;
    }
  }
  /**
   * Renders the text header to the header cell.
   *
   * @private
   */
  __textHeaderRenderer() {
    this.__setTextContent(this._headerCell._content, this.header);
  }
  /**
   * Computes the property name based on the path and renders it to the header cell.
   * If the path is not defined, then nothing is rendered.
   *
   * @protected
   */
  _defaultHeaderRenderer() {
    if (!this.path) {
      return;
    }
    this.__setTextContent(this._headerCell._content, this._generateHeader(this.path));
  }
  /**
   * Computes the item property value based on the path and renders it to the body cell.
   * If the path is not defined, then nothing is rendered.
   *
   * @protected
   */
  _defaultRenderer(root2, _owner, { item }) {
    if (!this.path) {
      return;
    }
    this.__setTextContent(root2, get2(this.path, item));
  }
  /**
   * By default, nothing is rendered to the footer cell.
   *
   * @protected
   */
  _defaultFooterRenderer() {
  }
  /**
   * Computes the final header renderer for the `_headerRenderer` computed property.
   * All the arguments are observable by the Polymer, it re-calls the method
   * once an argument is changed to update the property value.
   *
   * @protected
   * @return {GridHeaderFooterRenderer | undefined}
   */
  _computeHeaderRenderer(headerRenderer, header) {
    if (headerRenderer) {
      return headerRenderer;
    }
    if (header !== void 0 && header !== null) {
      return this.__textHeaderRenderer;
    }
    return this._defaultHeaderRenderer;
  }
  /**
   * Computes the final renderer for the `_renderer` property.
   * All the arguments are observable by the Polymer, it re-calls the method
   * once an argument is changed to update the property value.
   *
   * @protected
   * @return {GridBodyRenderer | undefined}
   */
  _computeRenderer(renderer) {
    if (renderer) {
      return renderer;
    }
    return this._defaultRenderer;
  }
  /**
   * Computes the final footer renderer for the `_footerRenderer` property.
   * All the arguments are observable by the Polymer, it re-calls the method
   * once an argument is changed to update the property value.
   *
   * @protected
   * @return {GridHeaderFooterRenderer | undefined}
   */
  _computeFooterRenderer(footerRenderer) {
    if (footerRenderer) {
      return footerRenderer;
    }
    return this._defaultFooterRenderer;
  }
};
var GridColumnMixin = (superClass) => class extends ColumnBaseMixin(DirMixin(superClass)) {
  static get properties() {
    return {
      /**
       * Width of the cells for this column.
       *
       * Please note that using the `em` length unit is discouraged as
       * it might lead to misalignment issues if the header, body, and footer
       * cells have different font sizes. Instead, use `rem` if you need
       * a length unit relative to the font size.
       */
      width: {
        type: String,
        value: "100px",
        sync: true
      },
      /**
       * Flex grow ratio for the cell widths. When set to 0, cell width is fixed.
       * @attr {number} flex-grow
       * @type {number}
       */
      flexGrow: {
        type: Number,
        value: 1,
        sync: true
      },
      /**
       * Custom function for rendering the cell content.
       * Receives three arguments:
       *
       * - `root` The cell content DOM element. Append your content to it.
       * - `column` The `<vaadin-grid-column>` element.
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *   - `model.detailsOpened` Details opened state.
       *
       * @type {GridBodyRenderer | null | undefined}
       */
      renderer: {
        type: Function,
        sync: true
      },
      /**
       * Represents the final renderer computed on the set of observable arguments.
       * It is supposed to be used internally when rendering the content of a body cell.
       *
       * @protected
       * @type {GridBodyRenderer | undefined}
       */
      _renderer: {
        type: Function,
        computed: "_computeRenderer(renderer, __initialized)"
      },
      /**
       * Path to an item sub-property whose value gets displayed in the column body cells.
       * The property name is also shown in the column header if an explicit header or renderer isn't defined.
       */
      path: {
        type: String,
        sync: true
      },
      /**
       * Automatically sets the width of the column based on the column contents when this is set to `true`.
       *
       * For performance reasons the column width is calculated automatically only once when the grid items
       * are rendered for the first time and the calculation only considers the rows which are currently
       * rendered in DOM (a bit more than what is currently visible). If the grid is scrolled, or the cell
       * content changes, the column width might not match the contents anymore.
       *
       * Hidden columns are ignored in the calculation and their widths are not automatically updated when
       * you show a column that was initially hidden.
       *
       * You can manually trigger the auto sizing behavior again by calling `grid.recalculateColumnWidths()`.
       *
       * The column width may still grow larger when `flexGrow` is not 0.
       * @attr {boolean} auto-width
       * @type {boolean}
       */
      autoWidth: {
        type: Boolean,
        value: false
      },
      /**
       * When true, wraps the cell's slot into an element with role="button", and sets
       * the tabindex attribute on the button element, instead of the cell itself.
       * This is needed to keep focus in sync with VoiceOver cursor when navigating
       * with Control + Option + arrow keys: focusing the `<td>` element does not fire
       * a focus event, but focusing an element with role="button" inside a cell fires it.
       * @protected
       */
      _focusButtonMode: {
        type: Boolean,
        value: false
      },
      /**
       * @type {Array<!HTMLElement>}
       * @protected
       */
      _cells: {
        type: Array,
        sync: true
      }
    };
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-column.js
var GridColumn = class extends GridColumnMixin(PolymerElement) {
  static get is() {
    return "vaadin-grid-column";
  }
};
defineCustomElement(GridColumn);

// node_modules/@open-wc/dedupe-mixin/src/dedupeMixin.js
var appliedClassMixins = /* @__PURE__ */ new WeakMap();
function wasMixinPreviouslyApplied(mixin, superClass) {
  let klass = superClass;
  while (klass) {
    if (appliedClassMixins.get(klass) === mixin) {
      return true;
    }
    klass = Object.getPrototypeOf(klass);
  }
  return false;
}
function dedupeMixin(mixin) {
  return (superClass) => {
    if (wasMixinPreviouslyApplied(mixin, superClass)) {
      return superClass;
    }
    const mixedClass = mixin(superClass);
    appliedClassMixins.set(mixedClass, mixin);
    return mixedClass;
  };
}

// node_modules/@vaadin/component-base/src/controller-mixin.js
var ControllerMixin = dedupeMixin((superClass) => {
  if (typeof superClass.prototype.addController === "function") {
    return superClass;
  }
  return class ControllerMixinClass extends superClass {
    constructor() {
      super();
      this.__controllers = /* @__PURE__ */ new Set();
    }
    /** @protected */
    connectedCallback() {
      super.connectedCallback();
      this.__controllers.forEach((c17) => {
        if (c17.hostConnected) {
          c17.hostConnected();
        }
      });
    }
    /** @protected */
    disconnectedCallback() {
      super.disconnectedCallback();
      this.__controllers.forEach((c17) => {
        if (c17.hostDisconnected) {
          c17.hostDisconnected();
        }
      });
    }
    /**
     * Registers a controller to participate in the element update cycle.
     *
     * @param {ReactiveController} controller
     * @protected
     */
    addController(controller) {
      this.__controllers.add(controller);
      if (this.$ !== void 0 && this.isConnected && controller.hostConnected) {
        controller.hostConnected();
      }
    }
    /**
     * Removes a controller from the element.
     *
     * @param {ReactiveController} controller
     * @protected
     */
    removeController(controller) {
      this.__controllers.delete(controller);
    }
  };
});

// node_modules/@vaadin/vaadin-development-mode-detector/vaadin-development-mode-detector.js
var DEV_MODE_CODE_REGEXP = /\/\*[\*!]\s+vaadin-dev-mode:start([\s\S]*)vaadin-dev-mode:end\s+\*\*\//i;
var FlowClients = window.Vaadin && window.Vaadin.Flow && window.Vaadin.Flow.clients;
function isMinified() {
  function test() {
    return true;
  }
  return uncommentAndRun(test);
}
function isDevelopmentMode() {
  try {
    if (isForcedDevelopmentMode()) {
      return true;
    }
    if (!isLocalhost()) {
      return false;
    }
    if (FlowClients) {
      return !isFlowProductionMode();
    }
    return !isMinified();
  } catch (e17) {
    return false;
  }
}
function isForcedDevelopmentMode() {
  return localStorage.getItem("vaadin.developmentmode.force");
}
function isLocalhost() {
  return ["localhost", "127.0.0.1"].indexOf(window.location.hostname) >= 0;
}
function isFlowProductionMode() {
  if (FlowClients) {
    const productionModeApps = Object.keys(FlowClients).map((key) => FlowClients[key]).filter((client) => client.productionMode);
    if (productionModeApps.length > 0) {
      return true;
    }
  }
  return false;
}
function uncommentAndRun(callback, args) {
  if (typeof callback !== "function") {
    return;
  }
  const match = DEV_MODE_CODE_REGEXP.exec(callback.toString());
  if (match) {
    try {
      callback = new Function(match[1]);
    } catch (e17) {
      console.log("vaadin-development-mode-detector: uncommentAndRun() failed", e17);
    }
  }
  return callback(args);
}
window["Vaadin"] = window["Vaadin"] || {};
var runIfDevelopmentMode = function(callback, args) {
  if (window.Vaadin.developmentMode) {
    return uncommentAndRun(callback, args);
  }
};
if (window.Vaadin.developmentMode === void 0) {
  window.Vaadin.developmentMode = isDevelopmentMode();
}

// node_modules/@vaadin/vaadin-usage-statistics/vaadin-usage-statistics-collect.js
function maybeGatherAndSendStats() {
}
var usageStatistics = function() {
  if (typeof runIfDevelopmentMode === "function") {
    return runIfDevelopmentMode(maybeGatherAndSendStats);
  }
};

// node_modules/@vaadin/component-base/src/element-mixin.js
setCancelSyntheticClickEvents(false);
if (!window.Vaadin) {
  window.Vaadin = {};
}
if (!window.Vaadin.registrations) {
  window.Vaadin.registrations = [];
}
if (!window.Vaadin.developmentModeCallback) {
  window.Vaadin.developmentModeCallback = {};
}
window.Vaadin.developmentModeCallback["vaadin-usage-statistics"] = function() {
  usageStatistics();
};
var statsJob;
var registered = /* @__PURE__ */ new Set();
var ElementMixin2 = (superClass) => class VaadinElementMixin extends DirMixin(superClass) {
  /** @protected */
  static finalize() {
    super.finalize();
    const { is } = this;
    if (is && !registered.has(is)) {
      window.Vaadin.registrations.push(this);
      registered.add(is);
      if (window.Vaadin.developmentModeCallback) {
        statsJob = Debouncer.debounce(statsJob, idlePeriod, () => {
          window.Vaadin.developmentModeCallback["vaadin-usage-statistics"]();
        });
        enqueueDebouncer(statsJob);
      }
    }
  }
  constructor() {
    super();
    if (document.doctype === null) {
      console.warn(
        'Vaadin components require the "standards mode" declaration. Please add <!DOCTYPE html> to the HTML document.'
      );
    }
  }
};

// node_modules/@vaadin/a11y-base/src/disabled-mixin.js
var DisabledMixin = dedupeMixin(
  (superclass) => class DisabledMixinClass extends superclass {
    static get properties() {
      return {
        /**
         * If true, the user cannot interact with this element.
         */
        disabled: {
          type: Boolean,
          value: false,
          observer: "_disabledChanged",
          reflectToAttribute: true,
          sync: true
        }
      };
    }
    /**
     * @param {boolean} disabled
     * @protected
     */
    _disabledChanged(disabled) {
      this._setAriaDisabled(disabled);
    }
    /**
     * @param {boolean} disabled
     * @protected
     */
    _setAriaDisabled(disabled) {
      if (disabled) {
        this.setAttribute("aria-disabled", "true");
      } else {
        this.removeAttribute("aria-disabled");
      }
    }
    /**
     * Overrides the default element `click` method in order to prevent
     * firing the `click` event when the element is disabled.
     * @protected
     * @override
     */
    click() {
      if (!this.disabled) {
        super.click();
      }
    }
  }
);

// node_modules/@vaadin/a11y-base/src/tabindex-mixin.js
var TabindexMixin = (superclass) => class TabindexMixinClass extends DisabledMixin(superclass) {
  static get properties() {
    return {
      /**
       * Indicates whether the element can be focused and where it participates in sequential keyboard navigation.
       *
       * @protected
       */
      tabindex: {
        type: Number,
        reflectToAttribute: true,
        observer: "_tabindexChanged",
        sync: true
      },
      /**
       * Stores the last known tabindex since the element has been disabled.
       *
       * @protected
       */
      _lastTabIndex: {
        type: Number
      }
    };
  }
  /**
   * When the element gets disabled, the observer saves the last known tabindex
   * and makes the element not focusable by setting tabindex to -1.
   * As soon as the element gets enabled, the observer restores the last known tabindex
   * so that the element can be focusable again.
   *
   * @protected
   * @override
   */
  _disabledChanged(disabled, oldDisabled) {
    super._disabledChanged(disabled, oldDisabled);
    if (this.__shouldAllowFocusWhenDisabled()) {
      return;
    }
    if (disabled) {
      if (this.tabindex !== void 0) {
        this._lastTabIndex = this.tabindex;
      }
      this.setAttribute("tabindex", "-1");
    } else if (oldDisabled) {
      if (this._lastTabIndex !== void 0) {
        this.setAttribute("tabindex", this._lastTabIndex);
      } else {
        this.tabindex = void 0;
      }
    }
  }
  /**
   * When the user has changed tabindex while the element is disabled,
   * the observer reverts tabindex to -1 and rather saves the new tabindex value to apply it later.
   * The new value will be applied as soon as the element becomes enabled.
   *
   * @protected
   */
  _tabindexChanged(tabindex) {
    if (this.__shouldAllowFocusWhenDisabled()) {
      return;
    }
    if (this.disabled && tabindex !== -1) {
      this._lastTabIndex = tabindex;
      this.setAttribute("tabindex", "-1");
    }
  }
  /**
   * Overrides the native `focus` method in order to prevent
   * focusing the element when it is disabled. Note, setting
   * `tabindex` to -1 does not prevent the element from being
   * programmatically focusable.
   *
   * @param {FocusOptions=} options
   * @protected
   * @override
   */
  focus(options) {
    if (!this.disabled || this.__shouldAllowFocusWhenDisabled()) {
      super.focus(options);
    }
  }
  /**
   * Returns whether the component should be focusable when disabled.
   * Returns false by default.
   *
   * @private
   * @return {boolean}
   */
  __shouldAllowFocusWhenDisabled() {
    return false;
  }
};

// node_modules/@vaadin/component-base/src/browser-utils.js
var testUserAgent = (regexp) => regexp.test(navigator.userAgent);
var testPlatform = (regexp) => regexp.test(navigator.platform);
var testVendor = (regexp) => regexp.test(navigator.vendor);
var isAndroid = testUserAgent(/Android/u);
var isChrome = testUserAgent(/Chrome/u) && testVendor(/Google Inc/u);
var isFirefox = testUserAgent(/Firefox/u);
var isIPad = testPlatform(/^iPad/u) || testPlatform(/^Mac/u) && navigator.maxTouchPoints > 1;
var isIPhone = testPlatform(/^iPhone/u);
var isIOS = isIPhone || isIPad;
var isSafari = testUserAgent(/^((?!chrome|android).)*safari/iu);
var isTouch = (() => {
  try {
    document.createEvent("TouchEvent");
    return true;
  } catch (_4) {
    return false;
  }
})();
var supportsAdoptingStyleSheets2 = window.ShadowRoot && "adoptedStyleSheets" in Document.prototype && "replace" in CSSStyleSheet.prototype;

// node_modules/@vaadin/component-base/src/gestures.js
var passiveTouchGestures2 = false;
var wrap2 = (node) => node;
var HAS_NATIVE_TA = typeof document.head.style.touchAction === "string";
var GESTURE_KEY = "__polymerGestures";
var HANDLED_OBJ = "__polymerGesturesHandled";
var TOUCH_ACTION = "__polymerGesturesTouchAction";
var TAP_DISTANCE = 25;
var TRACK_DISTANCE = 5;
var TRACK_LENGTH = 2;
var MOUSE_EVENTS = ["mousedown", "mousemove", "mouseup", "click"];
var MOUSE_WHICH_TO_BUTTONS = [0, 1, 4, 2];
var MOUSE_HAS_BUTTONS = function() {
  try {
    return new MouseEvent("test", { buttons: 1 }).buttons === 1;
  } catch (_4) {
    return false;
  }
}();
function isMouseEvent(name) {
  return MOUSE_EVENTS.indexOf(name) > -1;
}
var supportsPassive = false;
(function() {
  try {
    const opts = Object.defineProperty({}, "passive", {
      // eslint-disable-next-line getter-return
      get() {
        supportsPassive = true;
      }
    });
    window.addEventListener("test", null, opts);
    window.removeEventListener("test", null, opts);
  } catch (_4) {
  }
})();
function PASSIVE_TOUCH(eventName) {
  if (isMouseEvent(eventName) || eventName === "touchend") {
    return;
  }
  if (HAS_NATIVE_TA && supportsPassive && passiveTouchGestures2) {
    return { passive: true };
  }
}
var IS_TOUCH_ONLY = navigator.userAgent.match(/iP(?:[oa]d|hone)|Android/u);
var canBeDisabled = {
  button: true,
  command: true,
  fieldset: true,
  input: true,
  keygen: true,
  optgroup: true,
  option: true,
  select: true,
  textarea: true
};
function hasLeftMouseButton(ev) {
  const type = ev.type;
  if (!isMouseEvent(type)) {
    return false;
  }
  if (type === "mousemove") {
    let buttons = ev.buttons === void 0 ? 1 : ev.buttons;
    if (ev instanceof window.MouseEvent && !MOUSE_HAS_BUTTONS) {
      buttons = MOUSE_WHICH_TO_BUTTONS[ev.which] || 0;
    }
    return Boolean(buttons & 1);
  }
  const button = ev.button === void 0 ? 0 : ev.button;
  return button === 0;
}
function isSyntheticClick(ev) {
  if (ev.type === "click") {
    if (ev.detail === 0) {
      return true;
    }
    const t5 = _findOriginalTarget(ev);
    if (!t5.nodeType || /** @type {Element} */
    t5.nodeType !== Node.ELEMENT_NODE) {
      return true;
    }
    const bcr = (
      /** @type {Element} */
      t5.getBoundingClientRect()
    );
    const x2 = ev.pageX, y6 = ev.pageY;
    return !(x2 >= bcr.left && x2 <= bcr.right && y6 >= bcr.top && y6 <= bcr.bottom);
  }
  return false;
}
var POINTERSTATE = {
  mouse: {
    target: null,
    mouseIgnoreJob: null
  },
  touch: {
    x: 0,
    y: 0,
    id: -1,
    scrollDecided: false
  }
};
function firstTouchAction(ev) {
  let ta = "auto";
  const path = getComposedPath(ev);
  for (let i10 = 0, n19; i10 < path.length; i10++) {
    n19 = path[i10];
    if (n19[TOUCH_ACTION]) {
      ta = n19[TOUCH_ACTION];
      break;
    }
  }
  return ta;
}
function trackDocument(stateObj, movefn, upfn) {
  stateObj.movefn = movefn;
  stateObj.upfn = upfn;
  document.addEventListener("mousemove", movefn);
  document.addEventListener("mouseup", upfn);
}
function untrackDocument(stateObj) {
  document.removeEventListener("mousemove", stateObj.movefn);
  document.removeEventListener("mouseup", stateObj.upfn);
  stateObj.movefn = null;
  stateObj.upfn = null;
}
var getComposedPath = window.ShadyDOM && window.ShadyDOM.noPatch ? window.ShadyDOM.composedPath : (event) => event.composedPath && event.composedPath() || [];
var gestures = {};
var recognizers = [];
function deepTargetFind(x2, y6) {
  let node = document.elementFromPoint(x2, y6);
  let next = node;
  while (next && next.shadowRoot && !window.ShadyDOM) {
    const oldNext = next;
    next = next.shadowRoot.elementFromPoint(x2, y6);
    if (oldNext === next) {
      break;
    }
    if (next) {
      node = next;
    }
  }
  return node;
}
function _findOriginalTarget(ev) {
  const path = getComposedPath(
    /** @type {?Event} */
    ev
  );
  return path.length > 0 ? path[0] : ev.target;
}
function _handleNative(ev) {
  const type = ev.type;
  const node = ev.currentTarget;
  const gobj = node[GESTURE_KEY];
  if (!gobj) {
    return;
  }
  const gs = gobj[type];
  if (!gs) {
    return;
  }
  if (!ev[HANDLED_OBJ]) {
    ev[HANDLED_OBJ] = {};
    if (type.startsWith("touch")) {
      const t5 = ev.changedTouches[0];
      if (type === "touchstart") {
        if (ev.touches.length === 1) {
          POINTERSTATE.touch.id = t5.identifier;
        }
      }
      if (POINTERSTATE.touch.id !== t5.identifier) {
        return;
      }
      if (!HAS_NATIVE_TA) {
        if (type === "touchstart" || type === "touchmove") {
          _handleTouchAction(ev);
        }
      }
    }
  }
  const handled = ev[HANDLED_OBJ];
  if (handled.skip) {
    return;
  }
  for (let i10 = 0, r12; i10 < recognizers.length; i10++) {
    r12 = recognizers[i10];
    if (gs[r12.name] && !handled[r12.name]) {
      if (r12.flow && r12.flow.start.indexOf(ev.type) > -1 && r12.reset) {
        r12.reset();
      }
    }
  }
  for (let i10 = 0, r12; i10 < recognizers.length; i10++) {
    r12 = recognizers[i10];
    if (gs[r12.name] && !handled[r12.name]) {
      handled[r12.name] = true;
      r12[type](ev);
    }
  }
}
function _handleTouchAction(ev) {
  const t5 = ev.changedTouches[0];
  const type = ev.type;
  if (type === "touchstart") {
    POINTERSTATE.touch.x = t5.clientX;
    POINTERSTATE.touch.y = t5.clientY;
    POINTERSTATE.touch.scrollDecided = false;
  } else if (type === "touchmove") {
    if (POINTERSTATE.touch.scrollDecided) {
      return;
    }
    POINTERSTATE.touch.scrollDecided = true;
    const ta = firstTouchAction(ev);
    let shouldPrevent = false;
    const dx = Math.abs(POINTERSTATE.touch.x - t5.clientX);
    const dy = Math.abs(POINTERSTATE.touch.y - t5.clientY);
    if (!ev.cancelable) {
    } else if (ta === "none") {
      shouldPrevent = true;
    } else if (ta === "pan-x") {
      shouldPrevent = dy > dx;
    } else if (ta === "pan-y") {
      shouldPrevent = dx > dy;
    }
    if (shouldPrevent) {
      ev.preventDefault();
    } else {
      prevent("track");
    }
  }
}
function addListener(node, evType, handler) {
  if (gestures[evType]) {
    _add(node, evType, handler);
    return true;
  }
  return false;
}
function _add(node, evType, handler) {
  const recognizer = gestures[evType];
  const deps = recognizer.deps;
  const name = recognizer.name;
  let gobj = node[GESTURE_KEY];
  if (!gobj) {
    node[GESTURE_KEY] = gobj = {};
  }
  for (let i10 = 0, dep, gd; i10 < deps.length; i10++) {
    dep = deps[i10];
    if (IS_TOUCH_ONLY && isMouseEvent(dep) && dep !== "click") {
      continue;
    }
    gd = gobj[dep];
    if (!gd) {
      gobj[dep] = gd = { _count: 0 };
    }
    if (gd._count === 0) {
      node.addEventListener(dep, _handleNative, PASSIVE_TOUCH(dep));
    }
    gd[name] = (gd[name] || 0) + 1;
    gd._count = (gd._count || 0) + 1;
  }
  node.addEventListener(evType, handler);
  if (recognizer.touchAction) {
    setTouchAction(node, recognizer.touchAction);
  }
}
function register2(recog) {
  recognizers.push(recog);
  recog.emits.forEach((emit) => {
    gestures[emit] = recog;
  });
}
function _findRecognizerByEvent(evName) {
  for (let i10 = 0, r12; i10 < recognizers.length; i10++) {
    r12 = recognizers[i10];
    for (let j5 = 0, n19; j5 < r12.emits.length; j5++) {
      n19 = r12.emits[j5];
      if (n19 === evName) {
        return r12;
      }
    }
  }
  return null;
}
function setTouchAction(node, value) {
  if (HAS_NATIVE_TA && node instanceof HTMLElement) {
    microTask2.run(() => {
      node.style.touchAction = value;
    });
  }
  node[TOUCH_ACTION] = value;
}
function _fire(target, type, detail) {
  const ev = new Event(type, { bubbles: true, cancelable: true, composed: true });
  ev.detail = detail;
  wrap2(
    /** @type {!Node} */
    target
  ).dispatchEvent(ev);
  if (ev.defaultPrevented) {
    const preventer = detail.preventer || detail.sourceEvent;
    if (preventer && preventer.preventDefault) {
      preventer.preventDefault();
    }
  }
}
function prevent(evName) {
  const recognizer = _findRecognizerByEvent(evName);
  if (recognizer.info) {
    recognizer.info.prevent = true;
  }
}
register2({
  name: "downup",
  deps: ["mousedown", "touchstart", "touchend"],
  flow: {
    start: ["mousedown", "touchstart"],
    end: ["mouseup", "touchend"]
  },
  emits: ["down", "up"],
  info: {
    movefn: null,
    upfn: null
  },
  /**
   * @this {GestureRecognizer}
   * @return {void}
   */
  reset() {
    untrackDocument(this.info);
  },
  /**
   * @this {GestureRecognizer}
   * @param {MouseEvent} e
   * @return {void}
   */
  mousedown(e17) {
    if (!hasLeftMouseButton(e17)) {
      return;
    }
    const t5 = _findOriginalTarget(e17);
    const self = this;
    const movefn = (e18) => {
      if (!hasLeftMouseButton(e18)) {
        downupFire("up", t5, e18);
        untrackDocument(self.info);
      }
    };
    const upfn = (e18) => {
      if (hasLeftMouseButton(e18)) {
        downupFire("up", t5, e18);
      }
      untrackDocument(self.info);
    };
    trackDocument(this.info, movefn, upfn);
    downupFire("down", t5, e17);
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchstart(e17) {
    downupFire("down", _findOriginalTarget(e17), e17.changedTouches[0], e17);
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchend(e17) {
    downupFire("up", _findOriginalTarget(e17), e17.changedTouches[0], e17);
  }
});
function downupFire(type, target, event, preventer) {
  if (!target) {
    return;
  }
  _fire(target, type, {
    x: event.clientX,
    y: event.clientY,
    sourceEvent: event,
    preventer,
    prevent(e17) {
      return prevent(e17);
    }
  });
}
register2({
  name: "track",
  touchAction: "none",
  deps: ["mousedown", "touchstart", "touchmove", "touchend"],
  flow: {
    start: ["mousedown", "touchstart"],
    end: ["mouseup", "touchend"]
  },
  emits: ["track"],
  info: {
    x: 0,
    y: 0,
    state: "start",
    started: false,
    moves: [],
    /** @this {GestureInfo} */
    addMove(move) {
      if (this.moves.length > TRACK_LENGTH) {
        this.moves.shift();
      }
      this.moves.push(move);
    },
    movefn: null,
    upfn: null,
    prevent: false
  },
  /**
   * @this {GestureRecognizer}
   * @return {void}
   */
  reset() {
    this.info.state = "start";
    this.info.started = false;
    this.info.moves = [];
    this.info.x = 0;
    this.info.y = 0;
    this.info.prevent = false;
    untrackDocument(this.info);
  },
  /**
   * @this {GestureRecognizer}
   * @param {MouseEvent} e
   * @return {void}
   */
  mousedown(e17) {
    if (!hasLeftMouseButton(e17)) {
      return;
    }
    const t5 = _findOriginalTarget(e17);
    const self = this;
    const movefn = (e18) => {
      const x2 = e18.clientX, y6 = e18.clientY;
      if (trackHasMovedEnough(self.info, x2, y6)) {
        self.info.state = self.info.started ? e18.type === "mouseup" ? "end" : "track" : "start";
        if (self.info.state === "start") {
          prevent("tap");
        }
        self.info.addMove({ x: x2, y: y6 });
        if (!hasLeftMouseButton(e18)) {
          self.info.state = "end";
          untrackDocument(self.info);
        }
        if (t5) {
          trackFire(self.info, t5, e18);
        }
        self.info.started = true;
      }
    };
    const upfn = (e18) => {
      if (self.info.started) {
        movefn(e18);
      }
      untrackDocument(self.info);
    };
    trackDocument(this.info, movefn, upfn);
    this.info.x = e17.clientX;
    this.info.y = e17.clientY;
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchstart(e17) {
    const ct2 = e17.changedTouches[0];
    this.info.x = ct2.clientX;
    this.info.y = ct2.clientY;
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchmove(e17) {
    const t5 = _findOriginalTarget(e17);
    const ct2 = e17.changedTouches[0];
    const x2 = ct2.clientX, y6 = ct2.clientY;
    if (trackHasMovedEnough(this.info, x2, y6)) {
      if (this.info.state === "start") {
        prevent("tap");
      }
      this.info.addMove({ x: x2, y: y6 });
      trackFire(this.info, t5, ct2);
      this.info.state = "track";
      this.info.started = true;
    }
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchend(e17) {
    const t5 = _findOriginalTarget(e17);
    const ct2 = e17.changedTouches[0];
    if (this.info.started) {
      this.info.state = "end";
      this.info.addMove({ x: ct2.clientX, y: ct2.clientY });
      trackFire(this.info, t5, ct2);
    }
  }
});
function trackHasMovedEnough(info, x2, y6) {
  if (info.prevent) {
    return false;
  }
  if (info.started) {
    return true;
  }
  const dx = Math.abs(info.x - x2);
  const dy = Math.abs(info.y - y6);
  return dx >= TRACK_DISTANCE || dy >= TRACK_DISTANCE;
}
function trackFire(info, target, touch) {
  if (!target) {
    return;
  }
  const secondlast = info.moves[info.moves.length - 2];
  const lastmove = info.moves[info.moves.length - 1];
  const dx = lastmove.x - info.x;
  const dy = lastmove.y - info.y;
  let ddx, ddy = 0;
  if (secondlast) {
    ddx = lastmove.x - secondlast.x;
    ddy = lastmove.y - secondlast.y;
  }
  _fire(target, "track", {
    state: info.state,
    x: touch.clientX,
    y: touch.clientY,
    dx,
    dy,
    ddx,
    ddy,
    sourceEvent: touch,
    hover() {
      return deepTargetFind(touch.clientX, touch.clientY);
    }
  });
}
register2({
  name: "tap",
  deps: ["mousedown", "click", "touchstart", "touchend"],
  flow: {
    start: ["mousedown", "touchstart"],
    end: ["click", "touchend"]
  },
  emits: ["tap"],
  info: {
    x: NaN,
    y: NaN,
    prevent: false
  },
  /**
   * @this {GestureRecognizer}
   * @return {void}
   */
  reset() {
    this.info.x = NaN;
    this.info.y = NaN;
    this.info.prevent = false;
  },
  /**
   * @this {GestureRecognizer}
   * @param {MouseEvent} e
   * @return {void}
   */
  mousedown(e17) {
    if (hasLeftMouseButton(e17)) {
      this.info.x = e17.clientX;
      this.info.y = e17.clientY;
    }
  },
  /**
   * @this {GestureRecognizer}
   * @param {MouseEvent} e
   * @return {void}
   */
  click(e17) {
    if (hasLeftMouseButton(e17)) {
      trackForward(this.info, e17);
    }
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchstart(e17) {
    const touch = e17.changedTouches[0];
    this.info.x = touch.clientX;
    this.info.y = touch.clientY;
  },
  /**
   * @this {GestureRecognizer}
   * @param {TouchEvent} e
   * @return {void}
   */
  touchend(e17) {
    trackForward(this.info, e17.changedTouches[0], e17);
  }
});
function trackForward(info, e17, preventer) {
  const dx = Math.abs(e17.clientX - info.x);
  const dy = Math.abs(e17.clientY - info.y);
  const t5 = _findOriginalTarget(preventer || e17);
  if (!t5 || canBeDisabled[
    /** @type {!HTMLElement} */
    t5.localName
  ] && t5.hasAttribute("disabled")) {
    return;
  }
  if (isNaN(dx) || isNaN(dy) || dx <= TAP_DISTANCE && dy <= TAP_DISTANCE || isSyntheticClick(e17)) {
    if (!info.prevent) {
      _fire(t5, "tap", {
        x: e17.clientX,
        y: e17.clientY,
        sourceEvent: e17,
        preventer
      });
    }
  }
}

// node_modules/@vaadin/component-base/src/slot-observer.js
var SlotObserver = class {
  constructor(slot, callback) {
    this.slot = slot;
    this.callback = callback;
    this._storedNodes = [];
    this._connected = false;
    this._scheduled = false;
    this._boundSchedule = () => {
      this._schedule();
    };
    this.connect();
    this._schedule();
  }
  /**
   * Activates an observer. This method is automatically called when
   * a `SlotObserver` is created. It should only be called to  re-activate
   * an observer that has been deactivated via the `disconnect` method.
   */
  connect() {
    this.slot.addEventListener("slotchange", this._boundSchedule);
    this._connected = true;
  }
  /**
   * Deactivates the observer. After calling this method the observer callback
   * will not be called when changes to slotted nodes occur. The `connect` method
   * may be subsequently called to reactivate the observer.
   */
  disconnect() {
    this.slot.removeEventListener("slotchange", this._boundSchedule);
    this._connected = false;
  }
  /** @private */
  _schedule() {
    if (!this._scheduled) {
      this._scheduled = true;
      queueMicrotask(() => {
        this.flush();
      });
    }
  }
  /**
   * Run the observer callback synchronously.
   */
  flush() {
    if (!this._connected) {
      return;
    }
    this._scheduled = false;
    this._processNodes();
  }
  /** @private */
  _processNodes() {
    const currentNodes = this.slot.assignedNodes({ flatten: true });
    let addedNodes = [];
    const removedNodes = [];
    const movedNodes = [];
    if (currentNodes.length) {
      addedNodes = currentNodes.filter((node) => !this._storedNodes.includes(node));
    }
    if (this._storedNodes.length) {
      this._storedNodes.forEach((node, index) => {
        const idx = currentNodes.indexOf(node);
        if (idx === -1) {
          removedNodes.push(node);
        } else if (idx !== index) {
          movedNodes.push(node);
        }
      });
    }
    if (addedNodes.length || removedNodes.length || movedNodes.length) {
      this.callback({ addedNodes, currentNodes, movedNodes, removedNodes });
    }
    this._storedNodes = currentNodes;
  }
};

// node_modules/@vaadin/component-base/src/unique-id-utils.js
var uniqueId = 0;
function generateUniqueId() {
  return uniqueId++;
}

// node_modules/@vaadin/component-base/src/slot-controller.js
var SlotController = class extends EventTarget {
  /**
   * Ensure that every instance has unique ID.
   *
   * @param {HTMLElement} host
   * @param {string} slotName
   * @return {string}
   * @protected
   */
  static generateId(host, prefix = "default") {
    return `${prefix}-${host.localName}-${generateUniqueId()}`;
  }
  constructor(host, slotName, tagName, config = {}) {
    super();
    const { initializer, multiple, observe, useUniqueId, uniqueIdPrefix } = config;
    this.host = host;
    this.slotName = slotName;
    this.tagName = tagName;
    this.observe = typeof observe === "boolean" ? observe : true;
    this.multiple = typeof multiple === "boolean" ? multiple : false;
    this.slotInitializer = initializer;
    if (multiple) {
      this.nodes = [];
    }
    if (useUniqueId) {
      this.defaultId = this.constructor.generateId(host, uniqueIdPrefix || slotName);
    }
  }
  hostConnected() {
    if (!this.initialized) {
      if (this.multiple) {
        this.initMultiple();
      } else {
        this.initSingle();
      }
      if (this.observe) {
        this.observeSlot();
      }
      this.initialized = true;
    }
  }
  /** @protected */
  initSingle() {
    let node = this.getSlotChild();
    if (!node) {
      node = this.attachDefaultNode();
      this.initNode(node);
    } else {
      this.node = node;
      this.initAddedNode(node);
    }
  }
  /** @protected */
  initMultiple() {
    const children = this.getSlotChildren();
    if (children.length === 0) {
      const defaultNode = this.attachDefaultNode();
      if (defaultNode) {
        this.nodes = [defaultNode];
        this.initNode(defaultNode);
      }
    } else {
      this.nodes = children;
      children.forEach((node) => {
        this.initAddedNode(node);
      });
    }
  }
  /**
   * Create and attach default node using the provided tag name, if any.
   * @return {Node | undefined}
   * @protected
   */
  attachDefaultNode() {
    const { host, slotName, tagName } = this;
    let node = this.defaultNode;
    if (!node && tagName) {
      node = document.createElement(tagName);
      if (node instanceof Element) {
        if (slotName !== "") {
          node.setAttribute("slot", slotName);
        }
        this.defaultNode = node;
      }
    }
    if (node) {
      this.node = node;
      host.appendChild(node);
    }
    return node;
  }
  /**
   * Return the list of nodes matching the slot managed by the controller.
   * @return {Node}
   */
  getSlotChildren() {
    const { slotName } = this;
    return Array.from(this.host.childNodes).filter((node) => {
      if (node.nodeType === Node.ELEMENT_NODE && node.hasAttribute("data-slot-ignore")) {
        return false;
      }
      return node.nodeType === Node.ELEMENT_NODE && node.slot === slotName || node.nodeType === Node.TEXT_NODE && node.textContent.trim() && slotName === "";
    });
  }
  /**
   * Return a reference to the node managed by the controller.
   * @return {Node}
   */
  getSlotChild() {
    return this.getSlotChildren()[0];
  }
  /**
   * Run `slotInitializer` for the node managed by the controller.
   *
   * @param {Node} node
   * @protected
   */
  initNode(node) {
    const { slotInitializer } = this;
    if (slotInitializer) {
      slotInitializer(node, this.host);
    }
  }
  /**
   * Override to initialize the newly added custom node.
   *
   * @param {Node} _node
   * @protected
   */
  initCustomNode(_node) {
  }
  /**
   * Override to teardown slotted node when it's removed.
   *
   * @param {Node} _node
   * @protected
   */
  teardownNode(_node) {
  }
  /**
   * Run both `initCustomNode` and `initNode` for a custom slotted node.
   *
   * @param {Node} node
   * @protected
   */
  initAddedNode(node) {
    if (node !== this.defaultNode) {
      this.initCustomNode(node);
      this.initNode(node);
    }
  }
  /**
   * Setup the observer to manage slot content changes.
   * @protected
   */
  observeSlot() {
    const { slotName } = this;
    const selector = slotName === "" ? "slot:not([name])" : `slot[name=${slotName}]`;
    const slot = this.host.shadowRoot.querySelector(selector);
    this.__slotObserver = new SlotObserver(slot, ({ addedNodes, removedNodes }) => {
      const current = this.multiple ? this.nodes : [this.node];
      const newNodes = addedNodes.filter(
        (node) => !isEmptyTextNode(node) && !current.includes(node) && !(node.nodeType === Node.ELEMENT_NODE && node.hasAttribute("data-slot-ignore"))
      );
      if (removedNodes.length) {
        this.nodes = current.filter((node) => !removedNodes.includes(node));
        removedNodes.forEach((node) => {
          this.teardownNode(node);
        });
      }
      if (newNodes && newNodes.length > 0) {
        if (this.multiple) {
          if (this.defaultNode) {
            this.defaultNode.remove();
          }
          this.nodes = [...current, ...newNodes].filter((node) => node !== this.defaultNode);
          newNodes.forEach((node) => {
            this.initAddedNode(node);
          });
        } else {
          if (this.node) {
            this.node.remove();
          }
          this.node = newNodes[0];
          this.initAddedNode(this.node);
        }
      }
    });
  }
};

// node_modules/@vaadin/component-base/src/tooltip-controller.js
var TooltipController = class extends SlotController {
  constructor(host) {
    super(host, "tooltip");
    this.setTarget(host);
    this.__onContentChange = this.__onContentChange.bind(this);
  }
  /**
   * Override to initialize the newly added custom tooltip.
   *
   * @param {Node} tooltipNode
   * @protected
   * @override
   */
  initCustomNode(tooltipNode) {
    tooltipNode.target = this.target;
    if (this.ariaTarget !== void 0) {
      tooltipNode.ariaTarget = this.ariaTarget;
    }
    if (this.context !== void 0) {
      tooltipNode.context = this.context;
    }
    if (this.manual !== void 0) {
      tooltipNode.manual = this.manual;
    }
    if (this.opened !== void 0) {
      tooltipNode.opened = this.opened;
    }
    if (this.position !== void 0) {
      tooltipNode._position = this.position;
    }
    if (this.shouldShow !== void 0) {
      tooltipNode.shouldShow = this.shouldShow;
    }
    if (!this.manual) {
      this.host.setAttribute("has-tooltip", "");
    }
    this.__notifyChange(tooltipNode);
    tooltipNode.addEventListener("content-changed", this.__onContentChange);
  }
  /**
   * Override to notify the host when the tooltip is removed.
   *
   * @param {Node} tooltipNode
   * @protected
   * @override
   */
  teardownNode(tooltipNode) {
    if (!this.manual) {
      this.host.removeAttribute("has-tooltip");
    }
    tooltipNode.removeEventListener("content-changed", this.__onContentChange);
    this.__notifyChange(null);
  }
  /**
   * Set an HTML element for linking with the tooltip overlay
   * via `aria-describedby` attribute used by screen readers.
   * @param {HTMLElement} ariaTarget
   */
  setAriaTarget(ariaTarget) {
    this.ariaTarget = ariaTarget;
    const tooltipNode = this.node;
    if (tooltipNode) {
      tooltipNode.ariaTarget = ariaTarget;
    }
  }
  /**
   * Set a context object to be used by generator.
   * @param {object} context
   */
  setContext(context) {
    this.context = context;
    const tooltipNode = this.node;
    if (tooltipNode) {
      tooltipNode.context = context;
    }
  }
  /**
   * Toggle manual state on the slotted tooltip.
   * @param {boolean} manual
   */
  setManual(manual) {
    this.manual = manual;
    const tooltipNode = this.node;
    if (tooltipNode) {
      tooltipNode.manual = manual;
    }
  }
  /**
   * Toggle opened state on the slotted tooltip.
   * @param {boolean} opened
   */
  setOpened(opened) {
    this.opened = opened;
    const tooltipNode = this.node;
    if (tooltipNode) {
      tooltipNode.opened = opened;
    }
  }
  /**
   * Set default position for the slotted tooltip.
   * This can be overridden by setting the position
   * using corresponding property or attribute.
   * @param {string} position
   */
  setPosition(position) {
    this.position = position;
    const tooltipNode = this.node;
    if (tooltipNode) {
      tooltipNode._position = position;
    }
  }
  /**
   * Set function used to detect whether to show
   * the tooltip based on a condition.
   * @param {Function} shouldShow
   */
  setShouldShow(shouldShow) {
    this.shouldShow = shouldShow;
    const tooltipNode = this.node;
    if (tooltipNode) {
      tooltipNode.shouldShow = shouldShow;
    }
  }
  /**
   * Set an HTML element to attach the tooltip to.
   * @param {HTMLElement} target
   */
  setTarget(target) {
    this.target = target;
    const tooltipNode = this.node;
    if (tooltipNode) {
      tooltipNode.target = target;
    }
  }
  /** @private */
  __onContentChange(event) {
    this.__notifyChange(event.target);
  }
  /** @private */
  __notifyChange(node) {
    this.dispatchEvent(new CustomEvent("tooltip-changed", { detail: { node } }));
  }
};

// node_modules/@vaadin/component-base/src/iron-list-core.js
var IOS = navigator.userAgent.match(/iP(?:hone|ad;(?: U;)? CPU) OS (\d+)/u);
var IOS_TOUCH_SCROLLING = IOS && IOS[1] >= 8;
var DEFAULT_PHYSICAL_COUNT = 3;
var ironList = {
  /**
   * The ratio of hidden tiles that should remain in the scroll direction.
   * Recommended value ~0.5, so it will distribute tiles evenly in both
   * directions.
   */
  _ratio: 0.5,
  /**
   * The padding-top value for the list.
   */
  _scrollerPaddingTop: 0,
  /**
   * This value is a cached value of `scrollTop` from the last `scroll` event.
   */
  _scrollPosition: 0,
  /**
   * The sum of the heights of all the tiles in the DOM.
   */
  _physicalSize: 0,
  /**
   * The average `offsetHeight` of the tiles observed till now.
   */
  _physicalAverage: 0,
  /**
   * The number of tiles which `offsetHeight` > 0 observed until now.
   */
  _physicalAverageCount: 0,
  /**
   * The Y position of the item rendered in the `_physicalStart`
   * tile relative to the scrolling list.
   */
  _physicalTop: 0,
  /**
   * The number of items in the list.
   */
  _virtualCount: 0,
  /**
   * The estimated scroll height based on `_physicalAverage`
   */
  _estScrollHeight: 0,
  /**
   * The scroll height of the dom node
   */
  _scrollHeight: 0,
  /**
   * The height of the list. This is referred as the viewport in the context of
   * list.
   */
  _viewportHeight: 0,
  /**
   * The width of the list. This is referred as the viewport in the context of
   * list.
   */
  _viewportWidth: 0,
  /**
   * An array of DOM nodes that are currently in the tree
   * @type {?Array<!HTMLElement>}
   */
  _physicalItems: null,
  /**
   * An array of heights for each item in `_physicalItems`
   * @type {?Array<number>}
   */
  _physicalSizes: null,
  /**
   * A cached value for the first visible index.
   * See `firstVisibleIndex`
   * @type {?number}
   */
  _firstVisibleIndexVal: null,
  /**
   * A cached value for the last visible index.
   * See `lastVisibleIndex`
   * @type {?number}
   */
  _lastVisibleIndexVal: null,
  /**
   * The max number of pages to render. One page is equivalent to the height of
   * the list.
   */
  _maxPages: 2,
  /**
   * The cost of stamping a template in ms.
   */
  _templateCost: 0,
  /**
   * The bottom of the physical content.
   */
  get _physicalBottom() {
    return this._physicalTop + this._physicalSize;
  },
  /**
   * The bottom of the scroll.
   */
  get _scrollBottom() {
    return this._scrollPosition + this._viewportHeight;
  },
  /**
   * The n-th item rendered in the last physical item.
   */
  get _virtualEnd() {
    return this._virtualStart + this._physicalCount - 1;
  },
  /**
   * The height of the physical content that isn't on the screen.
   */
  get _hiddenContentSize() {
    return this._physicalSize - this._viewportHeight;
  },
  /**
   * The maximum scroll top value.
   */
  get _maxScrollTop() {
    return this._estScrollHeight - this._viewportHeight + this._scrollOffset;
  },
  /**
   * The largest n-th value for an item such that it can be rendered in
   * `_physicalStart`.
   */
  get _maxVirtualStart() {
    const virtualCount = this._virtualCount;
    return Math.max(0, virtualCount - this._physicalCount);
  },
  get _virtualStart() {
    return this._virtualStartVal || 0;
  },
  set _virtualStart(val) {
    val = this._clamp(val, 0, this._maxVirtualStart);
    this._virtualStartVal = val;
  },
  get _physicalStart() {
    return this._physicalStartVal || 0;
  },
  /**
   * The k-th tile that is at the top of the scrolling list.
   */
  set _physicalStart(val) {
    val %= this._physicalCount;
    if (val < 0) {
      val = this._physicalCount + val;
    }
    this._physicalStartVal = val;
  },
  /**
   * The k-th tile that is at the bottom of the scrolling list.
   */
  get _physicalEnd() {
    return (this._physicalStart + this._physicalCount - 1) % this._physicalCount;
  },
  get _physicalCount() {
    return this._physicalCountVal || 0;
  },
  set _physicalCount(val) {
    this._physicalCountVal = val;
  },
  /**
   * An optimal physical size such that we will have enough physical items
   * to fill up the viewport and recycle when the user scrolls.
   *
   * This default value assumes that we will at least have the equivalent
   * to a viewport of physical items above and below the user's viewport.
   */
  get _optPhysicalSize() {
    return this._viewportHeight === 0 ? Infinity : this._viewportHeight * this._maxPages;
  },
  /**
   * True if the current list is visible.
   */
  get _isVisible() {
    return Boolean(this.offsetWidth || this.offsetHeight);
  },
  /**
   * Gets the index of the first visible item in the viewport.
   *
   * @type {number}
   */
  get firstVisibleIndex() {
    let idx = this._firstVisibleIndexVal;
    if (idx == null) {
      let physicalOffset = this._physicalTop + this._scrollOffset;
      idx = this._iterateItems((pidx, vidx) => {
        physicalOffset += this._getPhysicalSizeIncrement(pidx);
        if (physicalOffset > this._scrollPosition) {
          return vidx;
        }
      }) || 0;
      this._firstVisibleIndexVal = idx;
    }
    return idx;
  },
  /**
   * Gets the index of the last visible item in the viewport.
   *
   * @type {number}
   */
  get lastVisibleIndex() {
    let idx = this._lastVisibleIndexVal;
    if (idx == null) {
      let physicalOffset = this._physicalTop + this._scrollOffset;
      this._iterateItems((pidx, vidx) => {
        if (physicalOffset < this._scrollBottom) {
          idx = vidx;
        }
        physicalOffset += this._getPhysicalSizeIncrement(pidx);
      });
      this._lastVisibleIndexVal = idx;
    }
    return idx;
  },
  get _scrollOffset() {
    return this._scrollerPaddingTop + this.scrollOffset;
  },
  /**
   * Recycles the physical items when needed.
   */
  _scrollHandler() {
    const scrollTop = Math.max(0, Math.min(this._maxScrollTop, this._scrollTop));
    let delta = scrollTop - this._scrollPosition;
    const isScrollingDown = delta >= 0;
    this._scrollPosition = scrollTop;
    this._firstVisibleIndexVal = null;
    this._lastVisibleIndexVal = null;
    if (Math.abs(delta) > this._physicalSize && this._physicalSize > 0) {
      delta -= this._scrollOffset;
      const idxAdjustment = Math.round(delta / this._physicalAverage);
      this._virtualStart += idxAdjustment;
      this._physicalStart += idxAdjustment;
      this._physicalTop = Math.min(Math.floor(this._virtualStart) * this._physicalAverage, this._scrollPosition);
      this._update();
    } else if (this._physicalCount > 0) {
      const reusables = this._getReusables(isScrollingDown);
      if (isScrollingDown) {
        this._physicalTop = reusables.physicalTop;
        this._virtualStart += reusables.indexes.length;
        this._physicalStart += reusables.indexes.length;
      } else {
        this._virtualStart -= reusables.indexes.length;
        this._physicalStart -= reusables.indexes.length;
      }
      this._update(reusables.indexes, isScrollingDown ? null : reusables.indexes);
      this._debounce("_increasePoolIfNeeded", this._increasePoolIfNeeded.bind(this, 0), microTask2);
    }
  },
  /**
   * Returns an object that contains the indexes of the physical items
   * that might be reused and the physicalTop.
   *
   * @param {boolean} fromTop If the potential reusable items are above the scrolling region.
   */
  _getReusables(fromTop) {
    let ith, offsetContent, physicalItemHeight;
    const idxs = [];
    const protectedOffsetContent = this._hiddenContentSize * this._ratio;
    const virtualStart = this._virtualStart;
    const virtualEnd = this._virtualEnd;
    const physicalCount = this._physicalCount;
    let top = this._physicalTop + this._scrollOffset;
    const bottom = this._physicalBottom + this._scrollOffset;
    const scrollTop = this._scrollPosition;
    const scrollBottom = this._scrollBottom;
    if (fromTop) {
      ith = this._physicalStart;
      offsetContent = scrollTop - top;
    } else {
      ith = this._physicalEnd;
      offsetContent = bottom - scrollBottom;
    }
    while (true) {
      physicalItemHeight = this._getPhysicalSizeIncrement(ith);
      offsetContent -= physicalItemHeight;
      if (idxs.length >= physicalCount || offsetContent <= protectedOffsetContent) {
        break;
      }
      if (fromTop) {
        if (virtualEnd + idxs.length + 1 >= this._virtualCount) {
          break;
        }
        if (top + physicalItemHeight >= scrollTop - this._scrollOffset) {
          break;
        }
        idxs.push(ith);
        top += physicalItemHeight;
        ith = (ith + 1) % physicalCount;
      } else {
        if (virtualStart - idxs.length <= 0) {
          break;
        }
        if (top + this._physicalSize - physicalItemHeight <= scrollBottom) {
          break;
        }
        idxs.push(ith);
        top -= physicalItemHeight;
        ith = ith === 0 ? physicalCount - 1 : ith - 1;
      }
    }
    return { indexes: idxs, physicalTop: top - this._scrollOffset };
  },
  /**
   * Update the list of items, starting from the `_virtualStart` item.
   * @param {!Array<number>=} itemSet
   * @param {!Array<number>=} movingUp
   */
  _update(itemSet, movingUp) {
    if (itemSet && itemSet.length === 0 || this._physicalCount === 0) {
      return;
    }
    this._assignModels(itemSet);
    this._updateMetrics(itemSet);
    if (movingUp) {
      while (movingUp.length) {
        const idx = movingUp.pop();
        this._physicalTop -= this._getPhysicalSizeIncrement(idx);
      }
    }
    this._positionItems();
    this._updateScrollerSize();
  },
  _isClientFull() {
    return this._scrollBottom !== 0 && this._physicalBottom - 1 >= this._scrollBottom && this._physicalTop <= this._scrollPosition;
  },
  /**
   * Increases the pool size.
   */
  _increasePoolIfNeeded(count) {
    const nextPhysicalCount = this._clamp(
      this._physicalCount + count,
      DEFAULT_PHYSICAL_COUNT,
      this._virtualCount - this._virtualStart
    );
    const delta = nextPhysicalCount - this._physicalCount;
    let nextIncrease = Math.round(this._physicalCount * 0.5);
    if (delta < 0) {
      return;
    }
    if (delta > 0) {
      const ts = window.performance.now();
      [].push.apply(this._physicalItems, this._createPool(delta));
      for (let i10 = 0; i10 < delta; i10++) {
        this._physicalSizes.push(0);
      }
      this._physicalCount += delta;
      if (this._physicalStart > this._physicalEnd && this._isIndexRendered(this._focusedVirtualIndex) && this._getPhysicalIndex(this._focusedVirtualIndex) < this._physicalEnd) {
        this._physicalStart += delta;
      }
      this._update();
      this._templateCost = (window.performance.now() - ts) / delta;
      nextIncrease = Math.round(this._physicalCount * 0.5);
    }
    if (this._virtualEnd >= this._virtualCount - 1 || nextIncrease === 0) {
    } else if (!this._isClientFull()) {
      this._debounce("_increasePoolIfNeeded", this._increasePoolIfNeeded.bind(this, nextIncrease), microTask2);
    } else if (this._physicalSize < this._optPhysicalSize) {
      this._debounce(
        "_increasePoolIfNeeded",
        this._increasePoolIfNeeded.bind(this, this._clamp(Math.round(50 / this._templateCost), 1, nextIncrease)),
        idlePeriod
      );
    }
  },
  /**
   * Renders the a new list.
   */
  _render() {
    if (!this.isAttached || !this._isVisible) {
      return;
    }
    if (this._physicalCount !== 0) {
      const reusables = this._getReusables(true);
      this._physicalTop = reusables.physicalTop;
      this._virtualStart += reusables.indexes.length;
      this._physicalStart += reusables.indexes.length;
      this._update(reusables.indexes);
      this._update();
      this._increasePoolIfNeeded(0);
    } else if (this._virtualCount > 0) {
      this.updateViewportBoundaries();
      this._increasePoolIfNeeded(DEFAULT_PHYSICAL_COUNT);
    }
  },
  /**
   * Called when the items have changed. That is, reassignments
   * to `items`, splices or updates to a single item.
   */
  _itemsChanged(change) {
    if (change.path === "items") {
      this._virtualStart = 0;
      this._physicalTop = 0;
      this._virtualCount = this.items ? this.items.length : 0;
      this._physicalIndexForKey = {};
      this._firstVisibleIndexVal = null;
      this._lastVisibleIndexVal = null;
      if (!this._physicalItems) {
        this._physicalItems = [];
      }
      if (!this._physicalSizes) {
        this._physicalSizes = [];
      }
      this._physicalStart = 0;
      if (this._scrollTop > this._scrollOffset) {
        this._resetScrollPosition(0);
      }
      this._debounce("_render", this._render, animationFrame);
    }
  },
  /**
   * Executes a provided function per every physical index in `itemSet`
   * `itemSet` default value is equivalent to the entire set of physical
   * indexes.
   *
   * @param {!function(number, number)} fn
   * @param {!Array<number>=} itemSet
   */
  _iterateItems(fn, itemSet) {
    let pidx, vidx, rtn, i10;
    if (arguments.length === 2 && itemSet) {
      for (i10 = 0; i10 < itemSet.length; i10++) {
        pidx = itemSet[i10];
        vidx = this._computeVidx(pidx);
        if ((rtn = fn.call(this, pidx, vidx)) != null) {
          return rtn;
        }
      }
    } else {
      pidx = this._physicalStart;
      vidx = this._virtualStart;
      for (; pidx < this._physicalCount; pidx++, vidx++) {
        if ((rtn = fn.call(this, pidx, vidx)) != null) {
          return rtn;
        }
      }
      for (pidx = 0; pidx < this._physicalStart; pidx++, vidx++) {
        if ((rtn = fn.call(this, pidx, vidx)) != null) {
          return rtn;
        }
      }
    }
  },
  /**
   * Returns the virtual index for a given physical index
   *
   * @param {number} pidx Physical index
   * @return {number}
   */
  _computeVidx(pidx) {
    if (pidx >= this._physicalStart) {
      return this._virtualStart + (pidx - this._physicalStart);
    }
    return this._virtualStart + (this._physicalCount - this._physicalStart) + pidx;
  },
  /**
   * Updates the position of the physical items.
   */
  _positionItems() {
    this._adjustScrollPosition();
    let y6 = this._physicalTop;
    this._iterateItems((pidx) => {
      this.translate3d(0, `${y6}px`, 0, this._physicalItems[pidx]);
      y6 += this._physicalSizes[pidx];
    });
  },
  _getPhysicalSizeIncrement(pidx) {
    return this._physicalSizes[pidx];
  },
  /**
   * Adjusts the scroll position when it was overestimated.
   */
  _adjustScrollPosition() {
    const deltaHeight = this._virtualStart === 0 ? this._physicalTop : Math.min(this._scrollPosition + this._physicalTop, 0);
    if (deltaHeight !== 0) {
      this._physicalTop -= deltaHeight;
      const scrollTop = this._scrollPosition;
      if (!IOS_TOUCH_SCROLLING && scrollTop > 0) {
        this._resetScrollPosition(scrollTop - deltaHeight);
      }
    }
  },
  /**
   * Sets the position of the scroll.
   */
  _resetScrollPosition(pos) {
    if (this.scrollTarget && pos >= 0) {
      this._scrollTop = pos;
      this._scrollPosition = this._scrollTop;
    }
  },
  /**
   * Sets the scroll height, that's the height of the content,
   *
   * @param {boolean=} forceUpdate If true, updates the height no matter what.
   */
  _updateScrollerSize(forceUpdate) {
    const estScrollHeight = this._physicalBottom + Math.max(this._virtualCount - this._physicalCount - this._virtualStart, 0) * this._physicalAverage;
    this._estScrollHeight = estScrollHeight;
    if (forceUpdate || this._scrollHeight === 0 || this._scrollPosition >= estScrollHeight - this._physicalSize || Math.abs(estScrollHeight - this._scrollHeight) >= this._viewportHeight) {
      this.$.items.style.height = `${estScrollHeight}px`;
      this._scrollHeight = estScrollHeight;
    }
  },
  /**
   * Scroll to a specific index in the virtual list regardless
   * of the physical items in the DOM tree.
   *
   * @method scrollToIndex
   * @param {number} idx The index of the item
   */
  scrollToIndex(idx) {
    if (typeof idx !== "number" || idx < 0 || idx > this.items.length - 1) {
      return;
    }
    flush();
    if (this._physicalCount === 0) {
      return;
    }
    idx = this._clamp(idx, 0, this._virtualCount - 1);
    if (!this._isIndexRendered(idx) || idx >= this._maxVirtualStart) {
      this._virtualStart = idx - 1;
    }
    this._assignModels();
    this._updateMetrics();
    this._physicalTop = this._virtualStart * this._physicalAverage;
    let currentTopItem = this._physicalStart;
    let currentVirtualItem = this._virtualStart;
    let targetOffsetTop = 0;
    const hiddenContentSize = this._hiddenContentSize;
    while (currentVirtualItem < idx && targetOffsetTop <= hiddenContentSize) {
      targetOffsetTop += this._getPhysicalSizeIncrement(currentTopItem);
      currentTopItem = (currentTopItem + 1) % this._physicalCount;
      currentVirtualItem += 1;
    }
    this._updateScrollerSize(true);
    this._positionItems();
    this._resetScrollPosition(this._physicalTop + this._scrollOffset + targetOffsetTop);
    this._increasePoolIfNeeded(0);
    this._firstVisibleIndexVal = null;
    this._lastVisibleIndexVal = null;
  },
  /**
   * Reset the physical average and the average count.
   */
  _resetAverage() {
    this._physicalAverage = 0;
    this._physicalAverageCount = 0;
  },
  /**
   * A handler for the `iron-resize` event triggered by `IronResizableBehavior`
   * when the element is resized.
   */
  _resizeHandler() {
    this._debounce(
      "_render",
      () => {
        this._firstVisibleIndexVal = null;
        this._lastVisibleIndexVal = null;
        if (this._isVisible) {
          this.updateViewportBoundaries();
          this.toggleScrollListener(true);
          this._resetAverage();
          this._render();
        } else {
          this.toggleScrollListener(false);
        }
      },
      animationFrame
    );
  },
  _isIndexRendered(idx) {
    return idx >= this._virtualStart && idx <= this._virtualEnd;
  },
  _getPhysicalIndex(vidx) {
    return (this._physicalStart + (vidx - this._virtualStart)) % this._physicalCount;
  },
  _clamp(v2, min, max) {
    return Math.min(max, Math.max(min, v2));
  },
  _debounce(name, cb, asyncModule) {
    if (!this._debouncers) {
      this._debouncers = {};
    }
    this._debouncers[name] = Debouncer.debounce(this._debouncers[name], asyncModule, cb.bind(this));
    enqueueDebouncer(this._debouncers[name]);
  }
};

// node_modules/@vaadin/component-base/src/virtualizer-iron-list-adapter.js
var MAX_VIRTUAL_COUNT = 1e5;
var OFFSET_ADJUST_MIN_THRESHOLD = 1e3;
var IronListAdapter = class {
  constructor({ createElements, updateElement, scrollTarget, scrollContainer, elementsContainer, reorderElements }) {
    this.isAttached = true;
    this._vidxOffset = 0;
    this.createElements = createElements;
    this.updateElement = updateElement;
    this.scrollTarget = scrollTarget;
    this.scrollContainer = scrollContainer;
    this.elementsContainer = elementsContainer || scrollContainer;
    this.reorderElements = reorderElements;
    this._maxPages = 1.3;
    this.__placeholderHeight = 200;
    this.__elementHeightQueue = Array(10);
    this.timeouts = {
      SCROLL_REORDER: 500,
      IGNORE_WHEEL: 500,
      FIX_INVALID_ITEM_POSITIONING: 100
    };
    this.__resizeObserver = new ResizeObserver(() => this._resizeHandler());
    if (getComputedStyle(this.scrollTarget).overflow === "visible") {
      this.scrollTarget.style.overflow = "auto";
    }
    if (getComputedStyle(this.scrollContainer).position === "static") {
      this.scrollContainer.style.position = "relative";
    }
    this.__resizeObserver.observe(this.scrollTarget);
    this.scrollTarget.addEventListener("scroll", () => this._scrollHandler());
    const attachObserver = new ResizeObserver(([{ contentRect }]) => {
      const isHidden = contentRect.width === 0 && contentRect.height === 0;
      if (!isHidden && this.__scrollTargetHidden && this.scrollTarget.scrollTop !== this._scrollPosition) {
        this.scrollTarget.scrollTop = this._scrollPosition;
      }
      this.__scrollTargetHidden = isHidden;
    });
    attachObserver.observe(this.scrollTarget);
    this._scrollLineHeight = this._getScrollLineHeight();
    this.scrollTarget.addEventListener("wheel", (e17) => this.__onWheel(e17));
    this.scrollTarget.addEventListener("virtualizer-element-focused", (e17) => this.__onElementFocused(e17));
    this.elementsContainer.addEventListener("focusin", () => {
      this.scrollTarget.dispatchEvent(
        new CustomEvent("virtualizer-element-focused", { detail: { element: this.__getFocusedElement() } })
      );
    });
    if (this.reorderElements) {
      this.scrollTarget.addEventListener("mousedown", () => {
        this.__mouseDown = true;
      });
      this.scrollTarget.addEventListener("mouseup", () => {
        this.__mouseDown = false;
        if (this.__pendingReorder) {
          this.__reorderElements();
        }
      });
    }
  }
  get scrollOffset() {
    return 0;
  }
  get adjustedFirstVisibleIndex() {
    return this.firstVisibleIndex + this._vidxOffset;
  }
  get adjustedLastVisibleIndex() {
    return this.lastVisibleIndex + this._vidxOffset;
  }
  get _maxVirtualIndexOffset() {
    return this.size - this._virtualCount;
  }
  __hasPlaceholders() {
    return this.__getVisibleElements().some((el) => el.__virtualizerPlaceholder);
  }
  scrollToIndex(index) {
    if (typeof index !== "number" || isNaN(index) || this.size === 0 || !this.scrollTarget.offsetHeight) {
      return;
    }
    delete this.__pendingScrollToIndex;
    if (this._physicalCount <= 3) {
      this.flush();
    }
    index = this._clamp(index, 0, this.size - 1);
    const visibleElementCount = this.__getVisibleElements().length;
    let targetVirtualIndex = Math.floor(index / this.size * this._virtualCount);
    if (this._virtualCount - targetVirtualIndex < visibleElementCount) {
      targetVirtualIndex = this._virtualCount - (this.size - index);
      this._vidxOffset = this._maxVirtualIndexOffset;
    } else if (targetVirtualIndex < visibleElementCount) {
      if (index < OFFSET_ADJUST_MIN_THRESHOLD) {
        targetVirtualIndex = index;
        this._vidxOffset = 0;
      } else {
        targetVirtualIndex = OFFSET_ADJUST_MIN_THRESHOLD;
        this._vidxOffset = index - targetVirtualIndex;
      }
    } else {
      this._vidxOffset = index - targetVirtualIndex;
    }
    this.__skipNextVirtualIndexAdjust = true;
    super.scrollToIndex(targetVirtualIndex);
    if (this.adjustedFirstVisibleIndex !== index && this._scrollTop < this._maxScrollTop && !this.grid) {
      this._scrollTop -= this.__getIndexScrollOffset(index) || 0;
    }
    this._scrollHandler();
    if (this.__hasPlaceholders()) {
      this.__pendingScrollToIndex = index;
    }
  }
  flush() {
    if (this.scrollTarget.offsetHeight === 0) {
      return;
    }
    this._resizeHandler();
    flush();
    this._scrollHandler();
    if (this.__fixInvalidItemPositioningDebouncer) {
      this.__fixInvalidItemPositioningDebouncer.flush();
    }
    if (this.__scrollReorderDebouncer) {
      this.__scrollReorderDebouncer.flush();
    }
    if (this.__debouncerWheelAnimationFrame) {
      this.__debouncerWheelAnimationFrame.flush();
    }
  }
  hostConnected() {
    if (this.scrollTarget.offsetParent && this.scrollTarget.scrollTop !== this._scrollPosition) {
      this.scrollTarget.scrollTop = this._scrollPosition;
    }
  }
  update(startIndex = 0, endIndex = this.size - 1) {
    const updatedElements = [];
    this.__getVisibleElements().forEach((el) => {
      if (el.__virtualIndex >= startIndex && el.__virtualIndex <= endIndex) {
        this.__updateElement(el, el.__virtualIndex, true);
        updatedElements.push(el);
      }
    });
    this.__afterElementsUpdated(updatedElements);
  }
  /**
   * Updates the height for a given set of items.
   *
   * @param {!Array<number>=} itemSet
   */
  _updateMetrics(itemSet) {
    flush();
    let newPhysicalSize = 0;
    let oldPhysicalSize = 0;
    const prevAvgCount = this._physicalAverageCount;
    const prevPhysicalAvg = this._physicalAverage;
    this._iterateItems((pidx, vidx) => {
      oldPhysicalSize += this._physicalSizes[pidx];
      const elementOldPhysicalSize = this._physicalSizes[pidx];
      this._physicalSizes[pidx] = Math.ceil(this.__getBorderBoxHeight(this._physicalItems[pidx]));
      if (this._physicalSizes[pidx] !== elementOldPhysicalSize) {
        this.__resizeObserver.unobserve(this._physicalItems[pidx]);
        this.__resizeObserver.observe(this._physicalItems[pidx]);
      }
      newPhysicalSize += this._physicalSizes[pidx];
      this._physicalAverageCount += this._physicalSizes[pidx] ? 1 : 0;
    }, itemSet);
    this._physicalSize = this._physicalSize + newPhysicalSize - oldPhysicalSize;
    if (this._physicalAverageCount !== prevAvgCount) {
      this._physicalAverage = Math.round(
        (prevPhysicalAvg * prevAvgCount + newPhysicalSize) / this._physicalAverageCount
      );
    }
  }
  __getBorderBoxHeight(el) {
    const style2 = getComputedStyle(el);
    const itemHeight = parseFloat(style2.height) || 0;
    if (style2.boxSizing === "border-box") {
      return itemHeight;
    }
    const paddingBottom = parseFloat(style2.paddingBottom) || 0;
    const paddingTop = parseFloat(style2.paddingTop) || 0;
    const borderBottomWidth = parseFloat(style2.borderBottomWidth) || 0;
    const borderTopWidth = parseFloat(style2.borderTopWidth) || 0;
    return itemHeight + paddingBottom + paddingTop + borderBottomWidth + borderTopWidth;
  }
  __updateElement(el, index, forceSameIndexUpdates) {
    if (el.__virtualizerPlaceholder) {
      el.style.paddingTop = "";
      el.style.opacity = "";
      el.__virtualizerPlaceholder = false;
    }
    if (!this.__preventElementUpdates && (el.__lastUpdatedIndex !== index || forceSameIndexUpdates)) {
      this.updateElement(el, index);
      el.__lastUpdatedIndex = index;
    }
  }
  /**
   * Called synchronously right after elements have been updated.
   * This is a good place to do any post-update work.
   *
   * @param {!Array<!HTMLElement>} updatedElements
   */
  __afterElementsUpdated(updatedElements) {
    updatedElements.forEach((el) => {
      const elementHeight = el.offsetHeight;
      if (elementHeight === 0) {
        el.style.paddingTop = `${this.__placeholderHeight}px`;
        el.style.opacity = "0";
        el.__virtualizerPlaceholder = true;
        this.__placeholderClearDebouncer = Debouncer.debounce(
          this.__placeholderClearDebouncer,
          animationFrame,
          () => this._resizeHandler()
        );
      } else {
        this.__elementHeightQueue.push(elementHeight);
        this.__elementHeightQueue.shift();
        const filteredHeights = this.__elementHeightQueue.filter((h9) => h9 !== void 0);
        this.__placeholderHeight = Math.round(filteredHeights.reduce((a17, b7) => a17 + b7, 0) / filteredHeights.length);
      }
    });
    if (this.__pendingScrollToIndex !== void 0 && !this.__hasPlaceholders()) {
      this.scrollToIndex(this.__pendingScrollToIndex);
    }
  }
  __getIndexScrollOffset(index) {
    const element = this.__getVisibleElements().find((el) => el.__virtualIndex === index);
    return element ? this.scrollTarget.getBoundingClientRect().top - element.getBoundingClientRect().top : void 0;
  }
  get size() {
    return this.__size;
  }
  set size(size) {
    if (size === this.size) {
      return;
    }
    if (this.__fixInvalidItemPositioningDebouncer) {
      this.__fixInvalidItemPositioningDebouncer.cancel();
    }
    if (this._debouncers && this._debouncers._increasePoolIfNeeded) {
      this._debouncers._increasePoolIfNeeded.cancel();
    }
    this.__preventElementUpdates = true;
    let fvi;
    let fviOffsetBefore;
    if (size > 0) {
      fvi = this.adjustedFirstVisibleIndex;
      fviOffsetBefore = this.__getIndexScrollOffset(fvi);
    }
    this.__size = size;
    this._itemsChanged({
      path: "items"
    });
    flush();
    if (size > 0) {
      fvi = Math.min(fvi, size - 1);
      this.scrollToIndex(fvi);
      const fviOffsetAfter = this.__getIndexScrollOffset(fvi);
      if (fviOffsetBefore !== void 0 && fviOffsetAfter !== void 0) {
        this._scrollTop += fviOffsetBefore - fviOffsetAfter;
      }
    }
    this.__preventElementUpdates = false;
    if (!this._isVisible) {
      this._assignModels();
    }
    if (!this.elementsContainer.children.length) {
      requestAnimationFrame(() => this._resizeHandler());
    }
    this._resizeHandler();
    flush();
    this._debounce("_update", this._update, microTask2);
  }
  /** @private */
  get _scrollTop() {
    return this.scrollTarget.scrollTop;
  }
  /** @private */
  set _scrollTop(top) {
    this.scrollTarget.scrollTop = top;
  }
  /** @private */
  get items() {
    return {
      length: Math.min(this.size, MAX_VIRTUAL_COUNT)
    };
  }
  /** @private */
  get offsetHeight() {
    return this.scrollTarget.offsetHeight;
  }
  /** @private */
  get $() {
    return {
      items: this.scrollContainer
    };
  }
  /** @private */
  updateViewportBoundaries() {
    const styles = window.getComputedStyle(this.scrollTarget);
    this._scrollerPaddingTop = this.scrollTarget === this ? 0 : parseInt(styles["padding-top"], 10);
    this._isRTL = Boolean(styles.direction === "rtl");
    this._viewportWidth = this.elementsContainer.offsetWidth;
    this._viewportHeight = this.scrollTarget.offsetHeight;
    this._scrollPageHeight = this._viewportHeight - this._scrollLineHeight;
    if (this.grid) {
      this._updateGridMetrics();
    }
  }
  /** @private */
  setAttribute() {
  }
  /** @private */
  _createPool(size) {
    const physicalItems = this.createElements(size);
    const fragment = document.createDocumentFragment();
    physicalItems.forEach((el) => {
      el.style.position = "absolute";
      fragment.appendChild(el);
      this.__resizeObserver.observe(el);
    });
    this.elementsContainer.appendChild(fragment);
    return physicalItems;
  }
  /** @private */
  _assignModels(itemSet) {
    const updatedElements = [];
    this._iterateItems((pidx, vidx) => {
      const el = this._physicalItems[pidx];
      el.hidden = vidx >= this.size;
      if (!el.hidden) {
        el.__virtualIndex = vidx + (this._vidxOffset || 0);
        this.__updateElement(el, el.__virtualIndex);
        updatedElements.push(el);
      } else {
        delete el.__lastUpdatedIndex;
      }
    }, itemSet);
    this.__afterElementsUpdated(updatedElements);
  }
  /** @private */
  _isClientFull() {
    setTimeout(() => {
      this.__clientFull = true;
    });
    return this.__clientFull || super._isClientFull();
  }
  /** @private */
  translate3d(_x, y6, _z, el) {
    el.style.transform = `translateY(${y6})`;
  }
  /** @private */
  toggleScrollListener() {
  }
  /** @private */
  __getFocusedElement(visibleElements = this.__getVisibleElements()) {
    return visibleElements.find(
      (element) => element.contains(this.elementsContainer.getRootNode().activeElement) || element.contains(this.scrollTarget.getRootNode().activeElement)
    );
  }
  /** @private */
  __nextFocusableSiblingMissing(focusedElement, visibleElements) {
    return (
      // Check if focused element is the last visible DOM element
      visibleElements.indexOf(focusedElement) === visibleElements.length - 1 && // ...while there are more items available
      this.size > focusedElement.__virtualIndex + 1
    );
  }
  /** @private */
  __previousFocusableSiblingMissing(focusedElement, visibleElements) {
    return (
      // Check if focused element is the first visible DOM element
      visibleElements.indexOf(focusedElement) === 0 && // ...while there are preceding items available
      focusedElement.__virtualIndex > 0
    );
  }
  /** @private */
  __onElementFocused(e17) {
    if (!this.reorderElements) {
      return;
    }
    const focusedElement = e17.detail.element;
    if (!focusedElement) {
      return;
    }
    const visibleElements = this.__getVisibleElements();
    if (this.__previousFocusableSiblingMissing(focusedElement, visibleElements) || this.__nextFocusableSiblingMissing(focusedElement, visibleElements)) {
      this.flush();
    }
    const reorderedVisibleElements = this.__getVisibleElements();
    if (this.__nextFocusableSiblingMissing(focusedElement, reorderedVisibleElements)) {
      this._scrollTop += Math.ceil(focusedElement.getBoundingClientRect().bottom) - Math.floor(this.scrollTarget.getBoundingClientRect().bottom - 1);
      this.flush();
    } else if (this.__previousFocusableSiblingMissing(focusedElement, reorderedVisibleElements)) {
      this._scrollTop -= Math.ceil(this.scrollTarget.getBoundingClientRect().top + 1) - Math.floor(focusedElement.getBoundingClientRect().top);
      this.flush();
    }
  }
  _scrollHandler() {
    if (this.scrollTarget.offsetHeight === 0) {
      return;
    }
    this._adjustVirtualIndexOffset(this._scrollTop - (this.__previousScrollTop || 0));
    const delta = this.scrollTarget.scrollTop - this._scrollPosition;
    super._scrollHandler();
    if (this._physicalCount !== 0) {
      const isScrollingDown = delta >= 0;
      const reusables = this._getReusables(!isScrollingDown);
      if (reusables.indexes.length) {
        this._physicalTop = reusables.physicalTop;
        if (isScrollingDown) {
          this._virtualStart -= reusables.indexes.length;
          this._physicalStart -= reusables.indexes.length;
        } else {
          this._virtualStart += reusables.indexes.length;
          this._physicalStart += reusables.indexes.length;
        }
        this._resizeHandler();
      }
    }
    if (delta) {
      this.__fixInvalidItemPositioningDebouncer = Debouncer.debounce(
        this.__fixInvalidItemPositioningDebouncer,
        timeOut.after(this.timeouts.FIX_INVALID_ITEM_POSITIONING),
        () => this.__fixInvalidItemPositioning()
      );
    }
    if (this.reorderElements) {
      this.__scrollReorderDebouncer = Debouncer.debounce(
        this.__scrollReorderDebouncer,
        timeOut.after(this.timeouts.SCROLL_REORDER),
        () => this.__reorderElements()
      );
    }
    this.__previousScrollTop = this._scrollTop;
    if (this._scrollTop === 0 && this.firstVisibleIndex !== 0 && Math.abs(delta) > 0) {
      this.scrollToIndex(0);
    }
  }
  /** @override */
  _resizeHandler() {
    super._resizeHandler();
    const lastIndexVisible = this.adjustedLastVisibleIndex === this.size - 1;
    const emptySpace = this._physicalTop - this._scrollPosition;
    if (lastIndexVisible && emptySpace > 0) {
      const idxAdjustment = Math.ceil(emptySpace / this._physicalAverage);
      this._virtualStart = Math.max(0, this._virtualStart - idxAdjustment);
      this._physicalStart = Math.max(0, this._physicalStart - idxAdjustment);
      super.scrollToIndex(this._virtualCount - 1);
      this.scrollTarget.scrollTop = this.scrollTarget.scrollHeight - this.scrollTarget.clientHeight;
    }
  }
  /**
   * Work around an iron-list issue with invalid item positioning.
   * See https://github.com/vaadin/flow-components/issues/4306
   * @private
   */
  __fixInvalidItemPositioning() {
    if (!this.scrollTarget.isConnected) {
      return;
    }
    const physicalTopBelowTop = this._physicalTop > this._scrollTop;
    const physicalBottomAboveBottom = this._physicalBottom < this._scrollBottom;
    const firstIndexVisible = this.adjustedFirstVisibleIndex === 0;
    const lastIndexVisible = this.adjustedLastVisibleIndex === this.size - 1;
    if (physicalTopBelowTop && !firstIndexVisible || physicalBottomAboveBottom && !lastIndexVisible) {
      const isScrollingDown = physicalBottomAboveBottom;
      const originalRatio = this._ratio;
      this._ratio = 0;
      this._scrollPosition = this._scrollTop + (isScrollingDown ? -1 : 1);
      this._scrollHandler();
      this._ratio = originalRatio;
    }
  }
  /** @private */
  __onWheel(e17) {
    if (e17.ctrlKey || this._hasScrolledAncestor(e17.target, e17.deltaX, e17.deltaY)) {
      return;
    }
    let deltaY = e17.deltaY;
    if (e17.deltaMode === WheelEvent.DOM_DELTA_LINE) {
      deltaY *= this._scrollLineHeight;
    } else if (e17.deltaMode === WheelEvent.DOM_DELTA_PAGE) {
      deltaY *= this._scrollPageHeight;
    }
    if (!this._deltaYAcc) {
      this._deltaYAcc = 0;
    }
    if (this._wheelAnimationFrame) {
      this._deltaYAcc += deltaY;
      e17.preventDefault();
      return;
    }
    deltaY += this._deltaYAcc;
    this._deltaYAcc = 0;
    this._wheelAnimationFrame = true;
    this.__debouncerWheelAnimationFrame = Debouncer.debounce(
      this.__debouncerWheelAnimationFrame,
      animationFrame,
      () => {
        this._wheelAnimationFrame = false;
      }
    );
    const momentum = Math.abs(e17.deltaX) + Math.abs(deltaY);
    if (this._canScroll(this.scrollTarget, e17.deltaX, deltaY)) {
      e17.preventDefault();
      this.scrollTarget.scrollTop += deltaY;
      this.scrollTarget.scrollLeft += e17.deltaX;
      this._hasResidualMomentum = true;
      this._ignoreNewWheel = true;
      this._debouncerIgnoreNewWheel = Debouncer.debounce(
        this._debouncerIgnoreNewWheel,
        timeOut.after(this.timeouts.IGNORE_WHEEL),
        () => {
          this._ignoreNewWheel = false;
        }
      );
    } else if (this._hasResidualMomentum && momentum <= this._previousMomentum || this._ignoreNewWheel) {
      e17.preventDefault();
    } else if (momentum > this._previousMomentum) {
      this._hasResidualMomentum = false;
    }
    this._previousMomentum = momentum;
  }
  /**
   * Determines if the element has an ancestor that handles the scroll delta prior to this
   *
   * @private
   */
  _hasScrolledAncestor(el, deltaX, deltaY) {
    if (el === this.scrollTarget || el === this.scrollTarget.getRootNode().host) {
      return false;
    } else if (this._canScroll(el, deltaX, deltaY) && ["auto", "scroll"].indexOf(getComputedStyle(el).overflow) !== -1) {
      return true;
    } else if (el !== this && el.parentElement) {
      return this._hasScrolledAncestor(el.parentElement, deltaX, deltaY);
    }
  }
  _canScroll(el, deltaX, deltaY) {
    return deltaY > 0 && el.scrollTop < el.scrollHeight - el.offsetHeight || deltaY < 0 && el.scrollTop > 0 || deltaX > 0 && el.scrollLeft < el.scrollWidth - el.offsetWidth || deltaX < 0 && el.scrollLeft > 0;
  }
  /**
   * Increases the pool size.
   * @override
   */
  _increasePoolIfNeeded(count) {
    if (this._physicalCount > 2 && this._physicalAverage > 0 && count > 0) {
      const totalItemCount = Math.ceil(this._optPhysicalSize / this._physicalAverage);
      const missingItemCount = totalItemCount - this._physicalCount;
      super._increasePoolIfNeeded(Math.max(count, Math.min(100, missingItemCount)));
    } else {
      super._increasePoolIfNeeded(count);
    }
  }
  /**
   * An optimal physical size such that we will have enough physical items
   * to fill up the viewport and recycle when the user scrolls.
   *
   * This default value assumes that we will at least have the equivalent
   * to a viewport of physical items above and below the user's viewport.
   * @override
   */
  get _optPhysicalSize() {
    const optPhysicalSize = super._optPhysicalSize;
    if (optPhysicalSize <= 0 || this.__hasPlaceholders()) {
      return optPhysicalSize;
    }
    return optPhysicalSize + this.__getItemHeightBuffer();
  }
  /**
   * Extra item height buffer used when calculating optimal physical size.
   *
   * The iron list core uses the optimal physical size when determining whether to increase the item pool.
   * For the cases where some items are much larger than the average, the iron list core might not increase item pool.
   * This can lead to the large item not being rendered.
   *
   * @returns {Number} - Extra item height buffer
   * @private
   */
  __getItemHeightBuffer() {
    if (this._physicalCount === 0) {
      return 0;
    }
    const bufferZoneHeight = Math.ceil(this._viewportHeight * (this._maxPages - 1) / 2);
    const maxItemHeight = Math.max(...this._physicalSizes);
    if (maxItemHeight > Math.min(...this._physicalSizes)) {
      return Math.max(0, maxItemHeight - bufferZoneHeight);
    }
    return 0;
  }
  /**
   * @returns {Number|undefined} - The browser's default font-size in pixels
   * @private
   */
  _getScrollLineHeight() {
    const el = document.createElement("div");
    el.style.fontSize = "initial";
    el.style.display = "none";
    document.body.appendChild(el);
    const fontSize = window.getComputedStyle(el).fontSize;
    document.body.removeChild(el);
    return fontSize ? window.parseInt(fontSize) : void 0;
  }
  __getVisibleElements() {
    return Array.from(this.elementsContainer.children).filter((element) => !element.hidden);
  }
  /** @private */
  __reorderElements() {
    if (this.__mouseDown) {
      this.__pendingReorder = true;
      return;
    }
    this.__pendingReorder = false;
    const adjustedVirtualStart = this._virtualStart + (this._vidxOffset || 0);
    const visibleElements = this.__getVisibleElements();
    const targetElement = this.__getFocusedElement(visibleElements) || visibleElements[0];
    if (!targetElement) {
      return;
    }
    const targetPhysicalIndex = targetElement.__virtualIndex - adjustedVirtualStart;
    const delta = visibleElements.indexOf(targetElement) - targetPhysicalIndex;
    if (delta > 0) {
      for (let i10 = 0; i10 < delta; i10++) {
        this.elementsContainer.appendChild(visibleElements[i10]);
      }
    } else if (delta < 0) {
      for (let i10 = visibleElements.length + delta; i10 < visibleElements.length; i10++) {
        this.elementsContainer.insertBefore(visibleElements[i10], visibleElements[0]);
      }
    }
    if (isSafari) {
      const { transform } = this.scrollTarget.style;
      this.scrollTarget.style.transform = "translateZ(0)";
      setTimeout(() => {
        this.scrollTarget.style.transform = transform;
      });
    }
  }
  /** @private */
  _adjustVirtualIndexOffset(delta) {
    const maxOffset = this._maxVirtualIndexOffset;
    if (this._virtualCount >= this.size) {
      this._vidxOffset = 0;
    } else if (this.__skipNextVirtualIndexAdjust) {
      this.__skipNextVirtualIndexAdjust = false;
    } else if (Math.abs(delta) > 1e4) {
      const scale = this._scrollTop / (this.scrollTarget.scrollHeight - this.scrollTarget.clientHeight);
      this._vidxOffset = Math.round(scale * maxOffset);
    } else {
      const oldOffset = this._vidxOffset;
      const threshold = OFFSET_ADJUST_MIN_THRESHOLD;
      const maxShift = 100;
      if (this._scrollTop === 0) {
        this._vidxOffset = 0;
        if (oldOffset !== this._vidxOffset) {
          super.scrollToIndex(0);
        }
      } else if (this.firstVisibleIndex < threshold && this._vidxOffset > 0) {
        this._vidxOffset -= Math.min(this._vidxOffset, maxShift);
        super.scrollToIndex(this.firstVisibleIndex + (oldOffset - this._vidxOffset));
      }
      if (this._scrollTop >= this._maxScrollTop && this._maxScrollTop > 0) {
        this._vidxOffset = maxOffset;
        if (oldOffset !== this._vidxOffset) {
          super.scrollToIndex(this._virtualCount - 1);
        }
      } else if (this.firstVisibleIndex > this._virtualCount - threshold && this._vidxOffset < maxOffset) {
        this._vidxOffset += Math.min(maxOffset - this._vidxOffset, maxShift);
        super.scrollToIndex(this.firstVisibleIndex - (this._vidxOffset - oldOffset));
      }
    }
  }
};
Object.setPrototypeOf(IronListAdapter.prototype, ironList);

// node_modules/@vaadin/component-base/src/virtualizer.js
var Virtualizer = class {
  /**
   * @typedef {Object} VirtualizerConfig
   * @property {Function} createElements Function that returns the given number of new elements
   * @property {Function} updateElement Function that updates the element at a specific index
   * @property {HTMLElement} scrollTarget Reference to the scrolling element
   * @property {HTMLElement} scrollContainer Reference to a wrapper for the item elements (or a slot) inside the scrollTarget
   * @property {HTMLElement | undefined} elementsContainer Reference to the container in which the item elements are placed, defaults to scrollContainer
   * @property {boolean | undefined} reorderElements Determines whether the physical item elements should be kept in order in the DOM
   * @param {VirtualizerConfig} config Configuration for the virtualizer
   */
  constructor(config) {
    this.__adapter = new IronListAdapter(config);
  }
  /**
   * Gets the index of the first visible item in the viewport.
   *
   * @return {number}
   */
  get firstVisibleIndex() {
    return this.__adapter.adjustedFirstVisibleIndex;
  }
  /**
   * Gets the index of the last visible item in the viewport.
   *
   * @return {number}
   */
  get lastVisibleIndex() {
    return this.__adapter.adjustedLastVisibleIndex;
  }
  /**
   * The size of the virtualizer
   * @return {number | undefined} The size of the virtualizer
   */
  get size() {
    return this.__adapter.size;
  }
  /**
   * The size of the virtualizer
   * @param {number} size The size of the virtualizer
   */
  set size(size) {
    this.__adapter.size = size;
  }
  /**
   * Scroll to a specific index in the virtual list
   *
   * @method scrollToIndex
   * @param {number} index The index of the item
   */
  scrollToIndex(index) {
    this.__adapter.scrollToIndex(index);
  }
  /**
   * Requests the virtualizer to re-render the item elements on an index range, if currently in the DOM
   *
   * @method update
   * @param {number | undefined} startIndex The start index of the range
   * @param {number | undefined} endIndex The end index of the range
   */
  update(startIndex = 0, endIndex = this.size - 1) {
    this.__adapter.update(startIndex, endIndex);
  }
  /**
   * Flushes active asynchronous tasks so that the component and the DOM end up in a stable state
   *
   * @method update
   * @param {number | undefined} startIndex The start index of the range
   * @param {number | undefined} endIndex The end index of the range
   */
  flush() {
    this.__adapter.flush();
  }
  /**
   * Notifies the virtualizer about its host element connected to the DOM.
   *
   * @method hostConnected
   */
  hostConnected() {
    this.__adapter.hostConnected();
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-a11y-mixin.js
var A11yMixin = (superClass) => class A11yMixin extends superClass {
  static get properties() {
    return {
      /**
       * String used to label the grid to screen reader users.
       * @attr {string} accessible-name
       */
      accessibleName: {
        type: String
      }
    };
  }
  static get observers() {
    return ["_a11yUpdateGridSize(size, _columnTree, __emptyState)"];
  }
  /** @private */
  _a11yGetHeaderRowCount(_columnTree) {
    return _columnTree.filter(
      (level) => level.some((col) => col.headerRenderer || col.path && col.header !== null || col.header)
    ).length;
  }
  /** @private */
  _a11yGetFooterRowCount(_columnTree) {
    return _columnTree.filter((level) => level.some((col) => col.footerRenderer)).length;
  }
  /** @private */
  _a11yUpdateGridSize(size, _columnTree, emptyState) {
    if (size === void 0 || _columnTree === void 0) {
      return;
    }
    const headerRowsCount = this._a11yGetHeaderRowCount(_columnTree);
    const footerRowsCount = this._a11yGetFooterRowCount(_columnTree);
    const bodyRowsCount = emptyState ? 1 : size;
    const rowsCount = bodyRowsCount + headerRowsCount + footerRowsCount;
    this.$.table.setAttribute("aria-rowcount", rowsCount);
    const bodyColumns = _columnTree[_columnTree.length - 1];
    const columnsCount = emptyState ? 1 : rowsCount && bodyColumns && bodyColumns.length || 0;
    this.$.table.setAttribute("aria-colcount", columnsCount);
    this._a11yUpdateHeaderRows();
    this._a11yUpdateFooterRows();
  }
  /** @protected */
  _a11yUpdateHeaderRows() {
    iterateChildren(this.$.header, (headerRow, index) => {
      headerRow.setAttribute("aria-rowindex", index + 1);
    });
  }
  /** @protected */
  _a11yUpdateFooterRows() {
    iterateChildren(this.$.footer, (footerRow, index) => {
      footerRow.setAttribute("aria-rowindex", this._a11yGetHeaderRowCount(this._columnTree) + this.size + index + 1);
    });
  }
  /**
   * @param {!HTMLElement} row
   * @param {number} index
   * @protected
   */
  _a11yUpdateRowRowindex(row, index) {
    row.setAttribute("aria-rowindex", index + this._a11yGetHeaderRowCount(this._columnTree) + 1);
  }
  /**
   * @param {!HTMLElement} row
   * @param {boolean} selected
   * @protected
   */
  _a11yUpdateRowSelected(row, selected) {
    row.setAttribute("aria-selected", Boolean(selected));
    iterateRowCells(row, (cell) => {
      cell.setAttribute("aria-selected", Boolean(selected));
    });
  }
  /**
   * @param {!HTMLElement} row
   * @protected
   */
  _a11yUpdateRowExpanded(row) {
    const toggleCell = findTreeToggleCell(row);
    if (this.__isRowExpandable(row)) {
      row.setAttribute("aria-expanded", "false");
      if (toggleCell) {
        toggleCell.setAttribute("aria-expanded", "false");
      }
    } else if (this.__isRowCollapsible(row)) {
      row.setAttribute("aria-expanded", "true");
      if (toggleCell) {
        toggleCell.setAttribute("aria-expanded", "true");
      }
    } else {
      row.removeAttribute("aria-expanded");
      if (toggleCell) {
        toggleCell.removeAttribute("aria-expanded");
      }
    }
  }
  /**
   * @param {!HTMLElement} row
   * @param {number} level
   * @protected
   */
  _a11yUpdateRowLevel(row, level) {
    if (level > 0 || this.__isRowCollapsible(row) || this.__isRowExpandable(row)) {
      row.setAttribute("aria-level", level + 1);
    } else {
      row.removeAttribute("aria-level");
    }
  }
  /**
   * @param {!HTMLElement} row
   * @param {!HTMLElement} detailsCell
   * @protected
   */
  _a11ySetRowDetailsCell(row, detailsCell) {
    iterateRowCells(row, (cell) => {
      if (cell !== detailsCell) {
        cell.setAttribute("aria-controls", detailsCell.id);
      }
    });
  }
  /**
   * @param {!HTMLElement} row
   * @param {number} colspan
   * @protected
   */
  _a11yUpdateCellColspan(cell, colspan) {
    cell.setAttribute("aria-colspan", Number(colspan));
  }
  /** @protected */
  _a11yUpdateSorters() {
    Array.from(this.querySelectorAll("vaadin-grid-sorter")).forEach((sorter) => {
      let cellContent = sorter.parentNode;
      while (cellContent && cellContent.localName !== "vaadin-grid-cell-content") {
        cellContent = cellContent.parentNode;
      }
      if (cellContent && cellContent.assignedSlot) {
        const cell = cellContent.assignedSlot.parentNode;
        cell.setAttribute(
          "aria-sort",
          {
            asc: "ascending",
            desc: "descending"
          }[String(sorter.direction)] || "none"
        );
      }
    });
  }
};

// node_modules/@vaadin/a11y-base/src/focus-utils.js
var keyboardActive = false;
window.addEventListener(
  "keydown",
  () => {
    keyboardActive = true;
  },
  { capture: true }
);
window.addEventListener(
  "mousedown",
  () => {
    keyboardActive = false;
  },
  { capture: true }
);
function isKeyboardActive() {
  return keyboardActive;
}
function isElementHiddenDirectly(element) {
  const style2 = element.style;
  if (style2.visibility === "hidden" || style2.display === "none") {
    return true;
  }
  const computedStyle = window.getComputedStyle(element);
  if (computedStyle.visibility === "hidden" || computedStyle.display === "none") {
    return true;
  }
  return false;
}
function isElementHidden(element) {
  if (element.checkVisibility) {
    return !element.checkVisibility({ visibilityProperty: true });
  }
  if (element.offsetParent === null && element.clientWidth === 0 && element.clientHeight === 0) {
    return true;
  }
  return isElementHiddenDirectly(element);
}
function isElementFocusable(element) {
  if (element.matches('[tabindex="-1"]')) {
    return false;
  }
  if (element.matches("input, select, textarea, button, object")) {
    return element.matches(":not([disabled])");
  }
  return element.matches("a[href], area[href], iframe, [tabindex], [contentEditable]");
}

// node_modules/@vaadin/grid/src/vaadin-grid-active-item-mixin.js
var isFocusable = (target) => {
  return target.offsetParent && !target.part.contains("body-cell") && isElementFocusable(target) && getComputedStyle(target).visibility !== "hidden";
};
var ActiveItemMixin = (superClass) => class ActiveItemMixin extends superClass {
  static get properties() {
    return {
      /**
       * The item user has last interacted with. Turns to `null` after user deactivates
       * the item by re-interacting with the currently active item.
       * @type {GridItem}
       */
      activeItem: {
        type: Object,
        notify: true,
        value: null,
        sync: true
      }
    };
  }
  /** @protected */
  ready() {
    super.ready();
    this.$.scroller.addEventListener("click", this._onClick.bind(this));
    this.addEventListener("cell-activate", this._activateItem.bind(this));
    this.addEventListener("row-activate", this._activateItem.bind(this));
  }
  /** @private */
  _activateItem(e17) {
    const model = e17.detail.model;
    const clickedItem = model ? model.item : null;
    if (clickedItem) {
      this.activeItem = !this._itemsEqual(this.activeItem, clickedItem) ? clickedItem : null;
    }
  }
  /**
   * Checks whether the click event should not activate the cell on which it occurred.
   *
   * @protected
   */
  _shouldPreventCellActivationOnClick(e17) {
    const { cell } = this._getGridEventLocation(e17);
    return (
      // Something has handled this click already, e. g., <vaadin-grid-sorter>
      e17.defaultPrevented || // No clicked cell available
      !cell || // Cell is a details cell
      cell.getAttribute("part").includes("details-cell") || // Cell is the empty state cell
      cell === this.$.emptystatecell || // Cell content is focused
      cell._content.contains(this.getRootNode().activeElement) || // Clicked on a focusable element
      this._isFocusable(e17.target) || // Clicked on a label element
      e17.target instanceof HTMLLabelElement
    );
  }
  /**
   * @param {!MouseEvent} e
   * @protected
   */
  _onClick(e17) {
    if (this._shouldPreventCellActivationOnClick(e17)) {
      return;
    }
    const { cell } = this._getGridEventLocation(e17);
    if (cell) {
      this.dispatchEvent(
        new CustomEvent("cell-activate", {
          detail: {
            model: this.__getRowModel(cell.parentElement)
          }
        })
      );
    }
  }
  /**
   * @param {!Element} target
   * @return {boolean}
   * @protected
   */
  _isFocusable(target) {
    return isFocusable(target);
  }
  /**
   * Fired when the `activeItem` property changes.
   *
   * @event active-item-changed
   */
  /**
   * Fired when the cell is activated with click or keyboard.
   *
   * @event cell-activate
   */
};

// node_modules/@vaadin/grid/src/array-data-provider.js
function get3(path, object) {
  return path.split(".").reduce((obj, property) => obj[property], object);
}
function checkPaths(arrayToCheck, action, items) {
  if (items.length === 0) {
    return false;
  }
  let result = true;
  arrayToCheck.forEach(({ path }) => {
    if (!path || path.indexOf(".") === -1) {
      return;
    }
    const parentProperty = path.replace(/\.[^.]*$/u, "");
    if (get3(parentProperty, items[0]) === void 0) {
      console.warn(`Path "${path}" used for ${action} does not exist in all of the items, ${action} is disabled.`);
      result = false;
    }
  });
  return result;
}
function normalizeEmptyValue(value) {
  if ([void 0, null].indexOf(value) >= 0) {
    return "";
  } else if (isNaN(value)) {
    return value.toString();
  }
  return value;
}
function compare(a17, b7) {
  a17 = normalizeEmptyValue(a17);
  b7 = normalizeEmptyValue(b7);
  if (a17 < b7) {
    return -1;
  }
  if (a17 > b7) {
    return 1;
  }
  return 0;
}
function multiSort(items, sortOrders) {
  return items.sort((a17, b7) => {
    return sortOrders.map((sortOrder) => {
      if (sortOrder.direction === "asc") {
        return compare(get3(sortOrder.path, a17), get3(sortOrder.path, b7));
      } else if (sortOrder.direction === "desc") {
        return compare(get3(sortOrder.path, b7), get3(sortOrder.path, a17));
      }
      return 0;
    }).reduce((p14, n19) => {
      return p14 !== 0 ? p14 : n19;
    }, 0);
  });
}
function filter(items, filters) {
  return items.filter((item) => {
    return filters.every((filter2) => {
      const value = normalizeEmptyValue(get3(filter2.path, item));
      const filterValueLowercase = normalizeEmptyValue(filter2.value).toString().toLowerCase();
      return value.toString().toLowerCase().includes(filterValueLowercase);
    });
  });
}
var createArrayDataProvider = (allItems) => {
  return (params, callback) => {
    let items = allItems ? [...allItems] : [];
    if (params.filters && checkPaths(params.filters, "filtering", items)) {
      items = filter(items, params.filters);
    }
    if (Array.isArray(params.sortOrders) && params.sortOrders.length && checkPaths(params.sortOrders, "sorting", items)) {
      items = multiSort(items, params.sortOrders);
    }
    const count = Math.min(items.length, params.pageSize);
    const start = params.page * count;
    const end = start + count;
    const slice = items.slice(start, end);
    callback(slice, items.length);
  };
};

// node_modules/@vaadin/grid/src/vaadin-grid-array-data-provider-mixin.js
var ArrayDataProviderMixin = (superClass) => class ArrayDataProviderMixin extends superClass {
  static get properties() {
    return {
      /**
       * An array containing the items which will be passed to renderer functions.
       *
       * @type {Array<!GridItem> | undefined}
       */
      items: {
        type: Array,
        sync: true
      }
    };
  }
  static get observers() {
    return ["__dataProviderOrItemsChanged(dataProvider, items, isAttached, items.*)"];
  }
  /** @private */
  __setArrayDataProvider(items) {
    const arrayDataProvider = createArrayDataProvider(this.items, {});
    arrayDataProvider.__items = items;
    this._arrayDataProvider = arrayDataProvider;
    this.size = items.length;
    this.dataProvider = arrayDataProvider;
  }
  /**
   * @override
   * @protected
   */
  _onDataProviderPageReceived() {
    super._onDataProviderPageReceived();
    if (this._arrayDataProvider) {
      this.size = this._flatSize;
    }
  }
  /** @private */
  __dataProviderOrItemsChanged(dataProvider, items, isAttached) {
    if (!isAttached) {
      return;
    }
    if (this._arrayDataProvider) {
      if (dataProvider !== this._arrayDataProvider) {
        this._arrayDataProvider = void 0;
        this.items = void 0;
      } else if (!items) {
        this._arrayDataProvider = void 0;
        this.dataProvider = void 0;
        this.size = 0;
        this.clearCache();
      } else if (this._arrayDataProvider.__items === items) {
        this.clearCache();
      } else {
        this.__setArrayDataProvider(items);
      }
    } else if (items) {
      this.__setArrayDataProvider(items);
    }
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-column-auto-width-mixin.js
var ColumnAutoWidthMixin = (superClass) => class extends superClass {
  static get properties() {
    return {
      /** @private */
      __pendingRecalculateColumnWidths: {
        type: Boolean,
        value: true
      }
    };
  }
  static get observers() {
    return [
      "__dataProviderChangedAutoWidth(dataProvider)",
      "__columnTreeChangedAutoWidth(_columnTree)",
      "__flatSizeChangedAutoWidth(_flatSize)"
    ];
  }
  constructor() {
    super();
    this.addEventListener("animationend", this.__onAnimationEndAutoWidth);
  }
  /** @private */
  __onAnimationEndAutoWidth(e17) {
    if (e17.animationName.indexOf("vaadin-grid-appear") === 0) {
      this.__tryToRecalculateColumnWidthsIfPending();
    }
  }
  /** @private */
  __dataProviderChangedAutoWidth(_dataProvider) {
    if (this.__hasHadRenderedRowsForColumnWidthCalculation) {
      return;
    }
    this.recalculateColumnWidths();
  }
  /** @private */
  __columnTreeChangedAutoWidth(_columnTree) {
    queueMicrotask(() => this.recalculateColumnWidths());
  }
  /** @private */
  __flatSizeChangedAutoWidth(flatSize) {
    requestAnimationFrame(() => {
      if (!!flatSize && !this.__hasHadRenderedRowsForColumnWidthCalculation) {
        this.recalculateColumnWidths();
      } else {
        this.__tryToRecalculateColumnWidthsIfPending();
      }
    });
  }
  /**
   * @protected
   * @override
   */
  _onDataProviderPageLoaded() {
    super._onDataProviderPageLoaded();
    this.__tryToRecalculateColumnWidthsIfPending();
  }
  /**
   * @protected
   * @override
   */
  _updateFrozenColumn() {
    super._updateFrozenColumn();
    this.__tryToRecalculateColumnWidthsIfPending();
  }
  /** @private */
  __getIntrinsicWidth(col) {
    if (!this.__intrinsicWidthCache.has(col)) {
      this.__calculateAndCacheIntrinsicWidths([col]);
    }
    return this.__intrinsicWidthCache.get(col);
  }
  /** @private */
  __getDistributedWidth(col, innerColumn) {
    if (col == null || col === this) {
      return 0;
    }
    const columnWidth = Math.max(
      this.__getIntrinsicWidth(col),
      this.__getDistributedWidth(this.__getParentColumnGroup(col), col)
    );
    if (!innerColumn) {
      return columnWidth;
    }
    const columnGroup = col;
    const columnGroupWidth = columnWidth;
    const sumOfWidthOfAllChildColumns = columnGroup._visibleChildColumns.map((col2) => this.__getIntrinsicWidth(col2)).reduce((sum, curr) => sum + curr, 0);
    const extraNecessarySpaceForGridColumnGroup = Math.max(0, columnGroupWidth - sumOfWidthOfAllChildColumns);
    const proportionOfExtraSpace = this.__getIntrinsicWidth(innerColumn) / sumOfWidthOfAllChildColumns;
    const shareOfInnerColumnFromNecessaryExtraSpace = proportionOfExtraSpace * extraNecessarySpaceForGridColumnGroup;
    return this.__getIntrinsicWidth(innerColumn) + shareOfInnerColumnFromNecessaryExtraSpace;
  }
  /**
   * @param {!Array<!GridColumn>} cols the columns to auto size based on their content width
   * @private
   */
  _recalculateColumnWidths() {
    this.__virtualizer.flush();
    [...this.$.header.children, ...this.$.footer.children].forEach((row) => {
      if (row.__debounceUpdateHeaderFooterRowVisibility) {
        row.__debounceUpdateHeaderFooterRowVisibility.flush();
      }
    });
    this.__hasHadRenderedRowsForColumnWidthCalculation = this.__hasHadRenderedRowsForColumnWidthCalculation || this._getRenderedRows().length > 0;
    this.__intrinsicWidthCache = /* @__PURE__ */ new Map();
    const fvi = this._firstVisibleIndex;
    const lvi = this._lastVisibleIndex;
    this.__viewportRowsCache = this._getRenderedRows().filter((row) => row.index >= fvi && row.index <= lvi);
    const cols = this.__getAutoWidthColumns();
    const ancestorColumnGroups = /* @__PURE__ */ new Set();
    for (const col of cols) {
      let parent = this.__getParentColumnGroup(col);
      while (parent && !ancestorColumnGroups.has(parent)) {
        ancestorColumnGroups.add(parent);
        parent = this.__getParentColumnGroup(parent);
      }
    }
    this.__calculateAndCacheIntrinsicWidths([...cols, ...ancestorColumnGroups]);
    cols.forEach((col) => {
      col.width = `${this.__getDistributedWidth(col)}px`;
    });
    this.__intrinsicWidthCache.clear();
  }
  /**
   * Returns the parent column group of the given column.
   *
   * @private
   */
  __getParentColumnGroup(col) {
    const parent = (col.assignedSlot || col).parentElement;
    return parent && parent !== this ? parent : null;
  }
  /**
   * Toggles the cell content for the given column to use or not use auto width.
   *
   * While content for all the column cells uses auto width (instead of the default 100%),
   * their offsetWidth can be used to calculate the collective intrinsic width of the column.
   *
   * @private
   */
  __setVisibleCellContentAutoWidth(col, autoWidth) {
    col._allCells.filter((cell) => {
      if (this.$.items.contains(cell)) {
        return this.__viewportRowsCache.includes(cell.parentElement);
      }
      return true;
    }).forEach((cell) => {
      cell.__measuringAutoWidth = autoWidth;
      if (cell.__measuringAutoWidth) {
        cell.__originalWidth = cell.style.width;
        cell.style.width = "auto";
        cell.style.position = "absolute";
      } else {
        cell.style.width = cell.__originalWidth;
        delete cell.__originalWidth;
        cell.style.position = "";
      }
    });
    if (autoWidth) {
      this.$.scroller.setAttribute("measuring-auto-width", "");
    } else {
      this.$.scroller.removeAttribute("measuring-auto-width");
    }
  }
  /**
   * Returns the maximum intrinsic width of the cell content in the given column.
   * Only cells which are marked for measuring auto width are considered.
   *
   * @private
   */
  __getAutoWidthCellsMaxWidth(col) {
    return col._allCells.reduce((width, cell) => {
      return cell.__measuringAutoWidth ? Math.max(width, cell.offsetWidth + 1) : width;
    }, 0);
  }
  /**
   * Calculates and caches the intrinsic width of each given column.
   *
   * @private
   */
  __calculateAndCacheIntrinsicWidths(cols) {
    cols.forEach((col) => this.__setVisibleCellContentAutoWidth(col, true));
    cols.forEach((col) => {
      const width = this.__getAutoWidthCellsMaxWidth(col);
      this.__intrinsicWidthCache.set(col, width);
    });
    cols.forEach((col) => this.__setVisibleCellContentAutoWidth(col, false));
  }
  /**
   * Updates the `width` of all columns which have `autoWidth` set to `true`.
   */
  recalculateColumnWidths() {
    if (!this.__isReadyForColumnWidthCalculation()) {
      this.__pendingRecalculateColumnWidths = true;
      return;
    }
    this._recalculateColumnWidths();
  }
  /**
   * This internal method should be called whenever a condition that may have prevented
   * previous column width calculation is resolved.
   * @private
   */
  __tryToRecalculateColumnWidthsIfPending() {
    if (!this.__pendingRecalculateColumnWidths) {
      return;
    }
    this.__pendingRecalculateColumnWidths = false;
    this.recalculateColumnWidths();
  }
  /** @private */
  __getAutoWidthColumns() {
    return this._getColumns().filter((col) => !col.hidden && col.autoWidth);
  }
  /**
   * Returns true if the grid is ready for column width calculation, false otherwise.
   * @private
   */
  __isReadyForColumnWidthCalculation() {
    if (!this._columnTree) {
      return false;
    }
    const undefinedCols = this.__getAutoWidthColumns().filter((col) => !customElements.get(col.localName));
    if (undefinedCols.length) {
      Promise.all(undefinedCols.map((col) => customElements.whenDefined(col.localName))).then(() => {
        this.__tryToRecalculateColumnWidthsIfPending();
      });
      return false;
    }
    const hasRowsWithUndefinedIndex = [...this.$.items.children].some((row) => row.index === void 0);
    const debouncingHiddenChanged = this._debouncerHiddenChanged && this._debouncerHiddenChanged.isActive();
    const debouncingUpdateFrozenColumn = this.__debounceUpdateFrozenColumn && this.__debounceUpdateFrozenColumn.isActive();
    const hasHeight = this.clientHeight > 0;
    return !this._dataProviderController.isLoading() && !hasRowsWithUndefinedIndex && !isElementHidden(this) && !debouncingHiddenChanged && !debouncingUpdateFrozenColumn && hasHeight;
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-column-reordering-mixin.js
var ColumnReorderingMixin = (superClass) => class ColumnReorderingMixin extends superClass {
  static get properties() {
    return {
      /**
       * Set to true to allow column reordering.
       * @attr {boolean} column-reordering-allowed
       * @type {boolean}
       */
      columnReorderingAllowed: {
        type: Boolean,
        value: false
      },
      /** @private */
      _orderBaseScope: {
        type: Number,
        value: 1e7
      }
    };
  }
  static get observers() {
    return ["_updateOrders(_columnTree)"];
  }
  /** @protected */
  ready() {
    super.ready();
    addListener(this, "track", this._onTrackEvent);
    this._reorderGhost = this.shadowRoot.querySelector('[part="reorder-ghost"]');
    this.addEventListener("touchstart", this._onTouchStart.bind(this));
    this.addEventListener("touchmove", this._onTouchMove.bind(this));
    this.addEventListener("touchend", this._onTouchEnd.bind(this));
    this.addEventListener("contextmenu", this._onContextMenu.bind(this));
  }
  /** @private */
  _onContextMenu(e17) {
    if (this.hasAttribute("reordering")) {
      e17.preventDefault();
      if (!isTouch) {
        this._onTrackEnd();
      }
    }
  }
  /** @private */
  _cancelReorderForMultiTouch(e17) {
    if (e17.touches.length > 1) {
      clearTimeout(this._startTouchReorderTimeout);
      if (this._draggedColumn) {
        this._onTrackEnd();
      }
      return true;
    }
    return false;
  }
  /** @private */
  _onTouchStart(e17) {
    if (this._cancelReorderForMultiTouch(e17)) {
      return;
    }
    this._startTouchReorderTimeout = setTimeout(() => {
      this._onTrackStart({
        detail: {
          x: e17.touches[0].clientX,
          y: e17.touches[0].clientY
        }
      });
    }, 100);
  }
  /** @private */
  _onTouchMove(e17) {
    if (this._cancelReorderForMultiTouch(e17)) {
      return;
    }
    if (this._draggedColumn) {
      e17.preventDefault();
    }
    clearTimeout(this._startTouchReorderTimeout);
  }
  /** @private */
  _onTouchEnd() {
    clearTimeout(this._startTouchReorderTimeout);
    this._onTrackEnd();
  }
  /** @private */
  _onTrackEvent(e17) {
    if (e17.detail.state === "start") {
      const path = e17.composedPath();
      const headerCell = path[path.indexOf(this.$.header) - 2];
      if (!headerCell || !headerCell._content) {
        return;
      }
      if (headerCell._content.contains(this.getRootNode().activeElement)) {
        return;
      }
      if (this.$.scroller.hasAttribute("column-resizing")) {
        return;
      }
      if (!this._touchDevice) {
        this._onTrackStart(e17);
      }
    } else if (e17.detail.state === "track") {
      this._onTrack(e17);
    } else if (e17.detail.state === "end") {
      this._onTrackEnd(e17);
    }
  }
  /** @private */
  _onTrackStart(e17) {
    if (!this.columnReorderingAllowed) {
      return;
    }
    const path = e17.composedPath && e17.composedPath();
    if (path && path.slice(0, Math.max(0, path.indexOf(this))).some((node) => node.draggable)) {
      return;
    }
    const headerCell = this._cellFromPoint(e17.detail.x, e17.detail.y);
    if (!headerCell || !headerCell.getAttribute("part").includes("header-cell")) {
      return;
    }
    this.toggleAttribute("reordering", true);
    this._draggedColumn = headerCell._column;
    while (this._draggedColumn.parentElement.childElementCount === 1) {
      this._draggedColumn = this._draggedColumn.parentElement;
    }
    this._setSiblingsReorderStatus(this._draggedColumn, "allowed");
    this._draggedColumn._reorderStatus = "dragging";
    this._updateGhost(headerCell);
    this._reorderGhost.style.visibility = "visible";
    this._updateGhostPosition(e17.detail.x, this._touchDevice ? e17.detail.y - 50 : e17.detail.y);
    this._autoScroller();
  }
  /** @private */
  _onTrack(e17) {
    if (!this._draggedColumn) {
      return;
    }
    const targetCell = this._cellFromPoint(e17.detail.x, e17.detail.y);
    if (!targetCell) {
      return;
    }
    const targetColumn = this._getTargetColumn(targetCell, this._draggedColumn);
    if (this._isSwapAllowed(this._draggedColumn, targetColumn) && this._isSwappableByPosition(targetColumn, e17.detail.x)) {
      const columnTreeLevel = this._columnTree.findIndex((level) => level.includes(targetColumn));
      const levelColumnsInOrder = this._getColumnsInOrder(columnTreeLevel);
      const startIndex = levelColumnsInOrder.indexOf(this._draggedColumn);
      const endIndex = levelColumnsInOrder.indexOf(targetColumn);
      const direction = startIndex < endIndex ? 1 : -1;
      for (let i10 = startIndex; i10 !== endIndex; i10 += direction) {
        this._swapColumnOrders(this._draggedColumn, levelColumnsInOrder[i10 + direction]);
      }
    }
    this._updateGhostPosition(e17.detail.x, this._touchDevice ? e17.detail.y - 50 : e17.detail.y);
    this._lastDragClientX = e17.detail.x;
  }
  /** @private */
  _onTrackEnd() {
    if (!this._draggedColumn) {
      return;
    }
    this.toggleAttribute("reordering", false);
    this._draggedColumn._reorderStatus = "";
    this._setSiblingsReorderStatus(this._draggedColumn, "");
    this._draggedColumn = null;
    this._lastDragClientX = null;
    this._reorderGhost.style.visibility = "hidden";
    this.dispatchEvent(
      new CustomEvent("column-reorder", {
        detail: {
          columns: this._getColumnsInOrder()
        }
      })
    );
  }
  /**
   * Returns the columns (or column groups) on the specified header level in visual order.
   * By default, the bottom level is used.
   *
   * @return {!Array<!GridColumn>}
   * @protected
   */
  _getColumnsInOrder(headerLevel = this._columnTree.length - 1) {
    return this._columnTree[headerLevel].filter((c17) => !c17.hidden).sort((b7, a17) => b7._order - a17._order);
  }
  /**
   * @param {number} x
   * @param {number} y
   * @return {HTMLElement | undefined}
   * @protected
   */
  _cellFromPoint(x2 = 0, y6 = 0) {
    if (!this._draggedColumn) {
      this.$.scroller.toggleAttribute("no-content-pointer-events", true);
    }
    const elementFromPoint = this.shadowRoot.elementFromPoint(x2, y6);
    this.$.scroller.toggleAttribute("no-content-pointer-events", false);
    return this._getCellFromElement(elementFromPoint);
  }
  /** @private */
  _getCellFromElement(element) {
    if (element) {
      if (element._column) {
        return element;
      }
      const { parentElement } = element;
      if (parentElement && parentElement._focusButton === element) {
        return parentElement;
      }
    }
    return null;
  }
  /**
   * @param {number} eventClientX
   * @param {number} eventClientY
   * @protected
   */
  _updateGhostPosition(eventClientX, eventClientY) {
    const ghostRect = this._reorderGhost.getBoundingClientRect();
    const targetLeft = eventClientX - ghostRect.width / 2;
    const targetTop = eventClientY - ghostRect.height / 2;
    const _left = parseInt(this._reorderGhost._left || 0);
    const _top = parseInt(this._reorderGhost._top || 0);
    this._reorderGhost._left = _left - (ghostRect.left - targetLeft);
    this._reorderGhost._top = _top - (ghostRect.top - targetTop);
    this._reorderGhost.style.transform = `translate(${this._reorderGhost._left}px, ${this._reorderGhost._top}px)`;
  }
  /**
   * @param {!HTMLElement} cell
   * @return {!HTMLElement}
   * @protected
   */
  _updateGhost(cell) {
    const ghost = this._reorderGhost;
    ghost.textContent = cell._content.innerText;
    const style2 = window.getComputedStyle(cell);
    [
      "boxSizing",
      "display",
      "width",
      "height",
      "background",
      "alignItems",
      "padding",
      "border",
      "flex-direction",
      "overflow"
    ].forEach((propertyName) => {
      ghost.style[propertyName] = style2[propertyName];
    });
    return ghost;
  }
  /** @private */
  _updateOrders(columnTree) {
    if (columnTree === void 0) {
      return;
    }
    columnTree[0].forEach((column) => {
      column._order = 0;
    });
    updateColumnOrders(columnTree[0], this._orderBaseScope, 0);
  }
  /**
   * @param {!GridColumn} column
   * @param {string} status
   * @protected
   */
  _setSiblingsReorderStatus(column, status) {
    iterateChildren(column.parentNode, (sibling) => {
      if (/column/u.test(sibling.localName) && this._isSwapAllowed(sibling, column)) {
        sibling._reorderStatus = status;
      }
    });
  }
  /** @protected */
  _autoScroller() {
    if (this._lastDragClientX) {
      const rightDiff = this._lastDragClientX - this.getBoundingClientRect().right + 50;
      const leftDiff = this.getBoundingClientRect().left - this._lastDragClientX + 50;
      if (rightDiff > 0) {
        this.$.table.scrollLeft += rightDiff / 10;
      } else if (leftDiff > 0) {
        this.$.table.scrollLeft -= leftDiff / 10;
      }
    }
    if (this._draggedColumn) {
      setTimeout(() => this._autoScroller(), 10);
    }
  }
  /**
   * @param {GridColumn | undefined} column1
   * @param {GridColumn | undefined} column2
   * @return {boolean | undefined}
   * @protected
   */
  _isSwapAllowed(column1, column2) {
    if (column1 && column2) {
      const differentColumns = column1 !== column2;
      const sameParent = column1.parentElement === column2.parentElement;
      const sameFrozen = column1.frozen && column2.frozen || // Both columns are frozen
      column1.frozenToEnd && column2.frozenToEnd || // Both columns are frozen to end
      !column1.frozen && !column1.frozenToEnd && !column2.frozen && !column2.frozenToEnd;
      return differentColumns && sameParent && sameFrozen;
    }
  }
  /**
   * @param {!GridColumn} targetColumn
   * @param {number} clientX
   * @return {boolean}
   * @protected
   */
  _isSwappableByPosition(targetColumn, clientX) {
    const targetCell = Array.from(this.$.header.querySelectorAll('tr:not([hidden]) [part~="cell"]')).find(
      (cell) => targetColumn.contains(cell._column)
    );
    const sourceCellRect = this.$.header.querySelector("tr:not([hidden]) [reorder-status=dragging]").getBoundingClientRect();
    const targetRect = targetCell.getBoundingClientRect();
    if (targetRect.left > sourceCellRect.left) {
      return clientX > targetRect.right - sourceCellRect.width;
    }
    return clientX < targetRect.left + sourceCellRect.width;
  }
  /**
   * @param {!GridColumn} column1
   * @param {!GridColumn} column2
   * @protected
   */
  _swapColumnOrders(column1, column2) {
    [column1._order, column2._order] = [column2._order, column1._order];
    this._debounceUpdateFrozenColumn();
    this._updateFirstAndLastColumn();
  }
  /**
   * @param {HTMLElement | undefined} targetCell
   * @param {GridColumn} draggedColumn
   * @return {GridColumn | undefined}
   * @protected
   */
  _getTargetColumn(targetCell, draggedColumn) {
    if (targetCell && draggedColumn) {
      let candidate = targetCell._column;
      while (candidate.parentElement !== draggedColumn.parentElement && candidate !== this) {
        candidate = candidate.parentElement;
      }
      if (candidate.parentElement === draggedColumn.parentElement) {
        return candidate;
      }
      return targetCell._column;
    }
  }
  /**
   * Fired when the columns in the grid are reordered.
   *
   * @event column-reorder
   * @param {Object} detail
   * @param {Object} detail.columns the columns in the new order
   */
};

// node_modules/@vaadin/grid/src/vaadin-grid-column-resizing-mixin.js
var ColumnResizingMixin = (superClass) => class ColumnResizingMixin extends superClass {
  /** @protected */
  ready() {
    super.ready();
    const scroller = this.$.scroller;
    addListener(scroller, "track", this._onHeaderTrack.bind(this));
    scroller.addEventListener("touchstart", (e17) => {
      if (e17.touches.length > 1) {
        scroller.removeAttribute("column-resizing");
      }
    });
    scroller.addEventListener("touchmove", (e17) => {
      if (e17.touches.length > 1) {
        scroller.removeAttribute("column-resizing");
        return;
      }
      if (scroller.hasAttribute("column-resizing")) {
        e17.preventDefault();
      }
    });
    scroller.addEventListener(
      "contextmenu",
      (e17) => e17.target.getAttribute("part") === "resize-handle" && e17.preventDefault()
    );
    scroller.addEventListener(
      "mousedown",
      (e17) => e17.target.getAttribute("part") === "resize-handle" && e17.preventDefault()
    );
  }
  /** @private */
  _onHeaderTrack(e17) {
    const handle = e17.target;
    if (handle.getAttribute("part") === "resize-handle") {
      if (e17.detail.state !== "start" && !this.$.scroller.hasAttribute("column-resizing")) {
        return;
      }
      const cell = handle.parentElement;
      let column = cell._column;
      this.$.scroller.toggleAttribute("column-resizing", true);
      while (column.localName === "vaadin-grid-column-group") {
        column = column._childColumns.slice(0).sort((a17, b7) => a17._order - b7._order).filter((column2) => !column2.hidden).pop();
      }
      const isRTL = this.__isRTL;
      const eventX = e17.detail.x;
      const columnRowCells = Array.from(this.$.header.querySelectorAll('[part~="row"]:last-child [part~="cell"]'));
      const targetCell = columnRowCells.find((cell2) => cell2._column === column);
      if (targetCell.offsetWidth) {
        const style2 = getComputedStyle(targetCell._content);
        const minWidth = 10 + parseInt(style2.paddingLeft) + parseInt(style2.paddingRight) + parseInt(style2.borderLeftWidth) + parseInt(style2.borderRightWidth) + parseInt(style2.marginLeft) + parseInt(style2.marginRight);
        let maxWidth;
        const cellWidth = targetCell.offsetWidth;
        const cellRect = targetCell.getBoundingClientRect();
        if (targetCell.hasAttribute("frozen-to-end")) {
          maxWidth = cellWidth + (isRTL ? eventX - cellRect.right : cellRect.left - eventX);
        } else {
          maxWidth = cellWidth + (isRTL ? cellRect.left - eventX : eventX - cellRect.right);
        }
        column.width = `${Math.max(minWidth, maxWidth)}px`;
        column.flexGrow = 0;
      }
      columnRowCells.sort((a17, b7) => a17._column._order - b7._column._order).forEach((cell2, index, array) => {
        if (index < array.indexOf(targetCell)) {
          cell2._column.width = `${cell2.offsetWidth}px`;
          cell2._column.flexGrow = 0;
        }
      });
      const cellFrozenToEnd = this._frozenToEndCells[0];
      if (cellFrozenToEnd && this.$.table.scrollWidth > this.$.table.offsetWidth) {
        const frozenRect = cellFrozenToEnd.getBoundingClientRect();
        const offset = eventX - (isRTL ? frozenRect.right : frozenRect.left);
        if (isRTL && offset <= 0 || !isRTL && offset >= 0) {
          this.$.table.scrollLeft += offset;
        }
      }
      if (e17.detail.state === "end") {
        this.$.scroller.toggleAttribute("column-resizing", false);
        this.dispatchEvent(
          new CustomEvent("column-resize", {
            detail: { resizedColumn: column }
          })
        );
      }
      this._resizeHandler();
    }
  }
  /**
   * Fired when a column in the grid is resized by the user.
   *
   * @event column-resize
   * @param {Object} detail
   * @param {Object} detail.resizedColumn the column that was resized
   */
};

// node_modules/@vaadin/component-base/src/data-provider-controller/helpers.js
function getFlatIndexContext(cache, flatIndex, level = 0) {
  let levelIndex = flatIndex;
  for (const subCache of cache.subCaches) {
    const index = subCache.parentCacheIndex;
    if (levelIndex <= index) {
      break;
    } else if (levelIndex <= index + subCache.flatSize) {
      return getFlatIndexContext(subCache, levelIndex - index - 1, level + 1);
    }
    levelIndex -= subCache.flatSize;
  }
  return {
    cache,
    item: cache.items[levelIndex],
    index: levelIndex,
    page: Math.floor(levelIndex / cache.pageSize),
    level
  };
}
function getItemContext({ getItemId }, cache, targetItem, level = 0, levelFlatIndex = 0) {
  for (let index = 0; index < cache.items.length; index++) {
    const item = cache.items[index];
    if (!!item && getItemId(item) === getItemId(targetItem)) {
      return {
        cache,
        level,
        item,
        index,
        page: Math.floor(index / cache.pageSize),
        subCache: cache.getSubCache(index),
        flatIndex: levelFlatIndex + cache.getFlatIndex(index)
      };
    }
  }
  for (const subCache of cache.subCaches) {
    const parentItemFlatIndex = levelFlatIndex + cache.getFlatIndex(subCache.parentCacheIndex);
    const result = getItemContext({ getItemId }, subCache, targetItem, level + 1, parentItemFlatIndex + 1);
    if (result) {
      return result;
    }
  }
}
function getFlatIndexByPath(cache, [levelIndex, ...subIndexes], flatIndex = 0) {
  if (levelIndex === Infinity) {
    levelIndex = cache.size - 1;
  }
  const flatIndexOnLevel = cache.getFlatIndex(levelIndex);
  const subCache = cache.getSubCache(levelIndex);
  if (subCache && subCache.flatSize > 0 && subIndexes.length) {
    return getFlatIndexByPath(subCache, subIndexes, flatIndex + flatIndexOnLevel + 1);
  }
  return flatIndex + flatIndexOnLevel;
}

// node_modules/@vaadin/component-base/src/data-provider-controller/cache.js
var Cache = class _Cache {
  /**
   * @param {Cache['context']} context
   * @param {number} pageSize
   * @param {number | undefined} size
   * @param {Cache | undefined} parentCache
   * @param {number | undefined} parentCacheIndex
   */
  constructor(context, pageSize, size, parentCache, parentCacheIndex) {
    /**
     * A context object.
     *
     * @type {{ isExpanded: (item: unknown) => boolean }}
     */
    __publicField(this, "context");
    /**
     * The number of items to display per page.
     *
     * @type {number}
     */
    __publicField(this, "pageSize");
    /**
     * An array of cached items.
     *
     * @type {object[]}
     */
    __publicField(this, "items", []);
    /**
     * A map where the key is a requested page and the value is a callback
     * that will be called with data once the request is complete.
     *
     * @type {Record<number, Function>}
     */
    __publicField(this, "pendingRequests", {});
    /**
     * A map where the key is the index of an item in the `items` array
     * and the value is a sub-cache associated with that item.
     *
     * Note, it's intentionally defined as an object instead of a Map
     * to ensure that Object.entries() returns an array with keys sorted
     * in alphabetical order, rather than the order they were added.
     *
     * @type {Record<number, Cache>}
     * @private
     */
    __publicField(this, "__subCacheByIndex", {});
    /**
     * The number of items.
     *
     * @type {number}
     * @private
     */
    __publicField(this, "__size", 0);
    /**
     * The total number of items, including items from expanded sub-caches.
     *
     * @type {number}
     * @private
     */
    __publicField(this, "__flatSize", 0);
    this.context = context;
    this.pageSize = pageSize;
    this.size = size;
    this.parentCache = parentCache;
    this.parentCacheIndex = parentCacheIndex;
    this.__flatSize = size || 0;
  }
  /**
   * An item in the parent cache that the current cache is associated with.
   *
   * @return {object | undefined}
   */
  get parentItem() {
    return this.parentCache && this.parentCache.items[this.parentCacheIndex];
  }
  /**
   * An array of sub-caches sorted in the same order as their associated items
   * appear in the `items` array.
   *
   * @return {Cache[]}
   */
  get subCaches() {
    return Object.values(this.__subCacheByIndex);
  }
  /**
   * Whether the cache or any of its descendant caches have pending requests.
   *
   * @return {boolean}
   */
  get isLoading() {
    if (Object.keys(this.pendingRequests).length > 0) {
      return true;
    }
    return this.subCaches.some((subCache) => subCache.isLoading);
  }
  /**
   * The total number of items, including items from expanded sub-caches.
   *
   * @return {number}
   */
  get flatSize() {
    return this.__flatSize;
  }
  /**
   * The total number of items, including items from expanded sub-caches.
   *
   * @protected
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get effectiveSize() {
    console.warn(
      "<vaadin-grid> The `effectiveSize` property of ItemCache is deprecated and will be removed in Vaadin 25."
    );
    return this.flatSize;
  }
  /**
   * The number of items.
   *
   * @return {number}
   */
  get size() {
    return this.__size;
  }
  /**
   * Sets the number of items.
   *
   * @param {number} size
   */
  set size(size) {
    var _a2;
    const oldSize = this.__size;
    if (oldSize === size) {
      return;
    }
    this.__size = size;
    if (this.context.placeholder !== void 0) {
      this.items.length = size || 0;
      for (let i10 = 0; i10 < size || 0; i10++) {
        (_a2 = this.items)[i10] || (_a2[i10] = this.context.placeholder);
      }
    }
    Object.keys(this.pendingRequests).forEach((page) => {
      const startIndex = parseInt(page) * this.pageSize;
      if (startIndex >= this.size || 0) {
        delete this.pendingRequests[page];
      }
    });
  }
  /**
   * Recalculates the flattened size for the cache and its descendant caches recursively.
   */
  recalculateFlatSize() {
    this.__flatSize = !this.parentItem || this.context.isExpanded(this.parentItem) ? this.size + this.subCaches.reduce((total, subCache) => {
      subCache.recalculateFlatSize();
      return total + subCache.flatSize;
    }, 0) : 0;
  }
  /**
   * Adds an array of items corresponding to the given page
   * to the `items` array.
   *
   * @param {number} page
   * @param {object[]} items
   */
  setPage(page, items) {
    const startIndex = page * this.pageSize;
    items.forEach((item, i10) => {
      const itemIndex = startIndex + i10;
      if (this.size === void 0 || itemIndex < this.size) {
        this.items[itemIndex] = item;
      }
    });
  }
  /**
   * Retrieves the sub-cache associated with the item at the given index
   * in the `items` array.
   *
   * @param {number} index
   * @return {Cache | undefined}
   */
  getSubCache(index) {
    return this.__subCacheByIndex[index];
  }
  /**
   * Removes the sub-cache associated with the item at the given index
   * in the `items` array.
   *
   * @param {number} index
   */
  removeSubCache(index) {
    delete this.__subCacheByIndex[index];
  }
  /**
   * Removes all sub-caches.
   */
  removeSubCaches() {
    this.__subCacheByIndex = {};
  }
  /**
   * Creates and associates a sub-cache for the item at the given index
   * in the `items` array.
   *
   * @param {number} index
   * @return {Cache}
   */
  createSubCache(index) {
    const subCache = new _Cache(this.context, this.pageSize, 0, this, index);
    this.__subCacheByIndex[index] = subCache;
    return subCache;
  }
  /**
   * Retrieves the flattened index corresponding to the given index
   * of an item in the `items` array.
   *
   * @param {number} index
   * @return {number}
   */
  getFlatIndex(index) {
    const clampedIndex = Math.max(0, Math.min(this.size - 1, index));
    return this.subCaches.reduce((prev, subCache) => {
      const index2 = subCache.parentCacheIndex;
      return clampedIndex > index2 ? prev + subCache.flatSize : prev;
    }, clampedIndex);
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  getItemForIndex(index) {
    console.warn(
      "<vaadin-grid> The `getItemForIndex` method of ItemCache is deprecated and will be removed in Vaadin 25."
    );
    const { item } = getFlatIndexContext(this, index);
    return item;
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  getCacheAndIndex(index) {
    console.warn(
      "<vaadin-grid> The `getCacheAndIndex` method of ItemCache is deprecated and will be removed in Vaadin 25."
    );
    const { cache, index: scaledIndex } = getFlatIndexContext(this, index);
    return { cache, scaledIndex };
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  updateSize() {
    console.warn("<vaadin-grid> The `updateSize` method of ItemCache is deprecated and will be removed in Vaadin 25.");
    this.recalculateFlatSize();
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  ensureSubCacheForScaledIndex(scaledIndex) {
    console.warn(
      "<vaadin-grid> The `ensureSubCacheForScaledIndex` method of ItemCache is deprecated and will be removed in Vaadin 25."
    );
    if (!this.getSubCache(scaledIndex)) {
      const subCache = this.createSubCache(scaledIndex);
      this.context.__controller.__loadCachePage(subCache, 0);
    }
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get grid() {
    console.warn("<vaadin-grid> The `grid` property of ItemCache is deprecated and will be removed in Vaadin 25.");
    return this.context.__controller.host;
  }
  /**
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get itemCaches() {
    console.warn(
      "<vaadin-grid> The `itemCaches` property of ItemCache is deprecated and will be removed in Vaadin 25."
    );
    return this.__subCacheByIndex;
  }
};

// node_modules/@vaadin/component-base/src/data-provider-controller/data-provider-controller.js
var DataProviderController = class extends EventTarget {
  constructor(host, { size, pageSize, isExpanded, getItemId, isPlaceholder, placeholder, dataProvider, dataProviderParams }) {
    super();
    /**
     * The controller host element.
     *
     * @param {HTMLElement}
     */
    __publicField(this, "host");
    /**
     * A callback that returns data based on the passed params such as
     * `page`, `pageSize`, `parentItem`, etc.
     */
    __publicField(this, "dataProvider");
    /**
     * A callback that returns additional params that need to be passed
     * to the data provider callback with every request.
     */
    __publicField(this, "dataProviderParams");
    /**
     * A number of items to display per page.
     *
     * @type {number}
     */
    __publicField(this, "pageSize");
    /**
     * A callback that returns whether the given item is expanded.
     *
     * @type {(item: unknown) => boolean}
     */
    __publicField(this, "isExpanded");
    /**
     * A callback that returns the id for the given item and that
     * is used when checking object items for equality.
     *
     * @type { (item: unknown) => unknown}
     */
    __publicField(this, "getItemId");
    /**
     * A reference to the root cache instance.
     *
     * @param {Cache}
     */
    __publicField(this, "rootCache");
    /**
     * A placeholder item that is used to indicate that the item is not loaded yet.
     *
     * @type {unknown}
     */
    __publicField(this, "placeholder");
    /**
     * A callback that returns whether the given item is a placeholder.
     *
     * @type {(item: unknown) => boolean}
     */
    __publicField(this, "isPlaceholder");
    this.host = host;
    this.pageSize = pageSize;
    this.getItemId = getItemId;
    this.isExpanded = isExpanded;
    this.placeholder = placeholder;
    this.isPlaceholder = isPlaceholder;
    this.dataProvider = dataProvider;
    this.dataProviderParams = dataProviderParams;
    this.rootCache = this.__createRootCache(size);
  }
  /**
   * The total number of items, including items from expanded sub-caches.
   */
  get flatSize() {
    return this.rootCache.flatSize;
  }
  /** @private */
  get __cacheContext() {
    return {
      isExpanded: this.isExpanded,
      placeholder: this.placeholder,
      // The controller instance is needed to ensure deprecated cache methods work.
      __controller: this
    };
  }
  /**
   * Whether the root cache or any of its decendant caches have pending requests.
   *
   * @return {boolean}
   */
  isLoading() {
    return this.rootCache.isLoading;
  }
  /**
   * Sets the page size and clears the cache.
   *
   * @param {number} pageSize
   */
  setPageSize(pageSize) {
    this.pageSize = pageSize;
    this.clearCache();
  }
  /**
   * Sets the data provider callback and clears the cache.
   *
   * @type {Function}
   */
  setDataProvider(dataProvider) {
    this.dataProvider = dataProvider;
    this.clearCache();
  }
  /**
   * Recalculates the flattened size.
   */
  recalculateFlatSize() {
    this.rootCache.recalculateFlatSize();
  }
  /**
   * Clears the cache.
   */
  clearCache() {
    this.rootCache = this.__createRootCache(this.rootCache.size);
  }
  /**
   * Returns context for the given flattened index, including:
   * - the corresponding cache
   * - the cache level
   * - the corresponding item (if loaded)
   * - the item's index in the cache's items array
   * - the page containing the item
   *
   * @param {number} flatIndex
   */
  getFlatIndexContext(flatIndex) {
    return getFlatIndexContext(this.rootCache, flatIndex);
  }
  /**
   * Returns context for the given item, including:
   * - the cache containing the item
   * - the cache level
   * - the item
   * - the item's index in the cache's items array
   * - the item's flattened index
   * - the item's sub-cache (if exists)
   * - the page containing the item
   *
   * If the item isn't found, the method returns undefined.
   */
  getItemContext(item) {
    return getItemContext({ getItemId: this.getItemId }, this.rootCache, item);
  }
  /**
   * Returns the flattened index for the item that the given indexes point to.
   * Each index in the path array points to a sub-item of the previous index.
   * Using `Infinity` as an index will point to the last item on the level.
   *
   * @param {number[]} path
   * @return {number}
   */
  getFlatIndexByPath(path) {
    return getFlatIndexByPath(this.rootCache, path);
  }
  /**
   * Requests the data provider to load the page with the item corresponding
   * to the given flattened index. If the item is already loaded, the method
   * returns immediatelly.
   *
   * @param {number} flatIndex
   */
  ensureFlatIndexLoaded(flatIndex) {
    const { cache, page, item } = this.getFlatIndexContext(flatIndex);
    if (!this.__isItemLoaded(item)) {
      this.__loadCachePage(cache, page);
    }
  }
  /**
   * Creates a sub-cache for the item corresponding to the given flattened index and
   * requests the data provider to load the first page into the created sub-cache.
   * If the sub-cache already exists, the method returns immediatelly.
   *
   * @param {number} flatIndex
   */
  ensureFlatIndexHierarchy(flatIndex) {
    const { cache, item, index } = this.getFlatIndexContext(flatIndex);
    if (this.__isItemLoaded(item) && this.isExpanded(item) && !cache.getSubCache(index)) {
      const subCache = cache.createSubCache(index);
      this.__loadCachePage(subCache, 0);
    }
  }
  /**
   * Loads the first page into the root cache.
   */
  loadFirstPage() {
    this.__loadCachePage(this.rootCache, 0);
  }
  /** @private */
  __createRootCache(size) {
    return new Cache(this.__cacheContext, this.pageSize, size);
  }
  /** @private */
  __loadCachePage(cache, page) {
    if (!this.dataProvider || cache.pendingRequests[page]) {
      return;
    }
    let params = {
      page,
      pageSize: this.pageSize,
      parentItem: cache.parentItem
    };
    if (this.dataProviderParams) {
      params = { ...params, ...this.dataProviderParams() };
    }
    const callback = (items, size) => {
      if (cache.pendingRequests[page] !== callback) {
        return;
      }
      if (size !== void 0) {
        cache.size = size;
      } else if (params.parentItem) {
        cache.size = items.length;
      }
      cache.setPage(page, items);
      this.recalculateFlatSize();
      this.dispatchEvent(new CustomEvent("page-received"));
      delete cache.pendingRequests[page];
      this.dispatchEvent(new CustomEvent("page-loaded"));
    };
    cache.pendingRequests[page] = callback;
    this.dispatchEvent(new CustomEvent("page-requested"));
    this.dataProvider(params, callback);
  }
  /** @private */
  __isItemLoaded(item) {
    if (this.isPlaceholder) {
      return !this.isPlaceholder(item);
    } else if (this.placeholder) {
      return item !== this.placeholder;
    }
    return !!item;
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-data-provider-mixin.js
var DataProviderMixin = (superClass) => class DataProviderMixin extends superClass {
  static get properties() {
    return {
      /**
       * The number of root-level items in the grid.
       * @attr {number} size
       * @type {number}
       */
      size: {
        type: Number,
        notify: true,
        sync: true
      },
      /**
       * @type {number}
       * @protected
       */
      _flatSize: {
        type: Number,
        sync: true
      },
      /**
       * Number of items fetched at a time from the dataprovider.
       * @attr {number} page-size
       * @type {number}
       */
      pageSize: {
        type: Number,
        value: 50,
        observer: "_pageSizeChanged",
        sync: true
      },
      /**
       * Function that provides items lazily. Receives arguments `params`, `callback`
       *
       * `params.page` Requested page index
       *
       * `params.pageSize` Current page size
       *
       * `params.filters` Currently applied filters
       *
       * `params.sortOrders` Currently applied sorting orders
       *
       * `params.parentItem` When tree is used, and sublevel items
       * are requested, reference to parent item of the requested sublevel.
       * Otherwise `undefined`.
       *
       * `callback(items, size)` Callback function with arguments:
       *   - `items` Current page of items
       *   - `size` Total number of items. When tree sublevel items
       *     are requested, total number of items in the requested sublevel.
       *     Optional when tree is not used, required for tree.
       *
       * @type {GridDataProvider | null | undefined}
       */
      dataProvider: {
        type: Object,
        notify: true,
        observer: "_dataProviderChanged",
        sync: true
      },
      /**
       * `true` while data is being requested from the data provider.
       */
      loading: {
        type: Boolean,
        notify: true,
        readOnly: true,
        reflectToAttribute: true
      },
      /**
       * @protected
       */
      _hasData: {
        type: Boolean,
        value: false,
        sync: true
      },
      /**
       * Path to an item sub-property that indicates whether the item has child items.
       * @attr {string} item-has-children-path
       */
      itemHasChildrenPath: {
        type: String,
        value: "children",
        observer: "__itemHasChildrenPathChanged",
        sync: true
      },
      /**
       * Path to an item sub-property that identifies the item.
       * @attr {string} item-id-path
       */
      itemIdPath: {
        type: String,
        value: null,
        sync: true
      },
      /**
       * An array that contains the expanded items.
       * @type {!Array<!GridItem>}
       */
      expandedItems: {
        type: Object,
        notify: true,
        value: () => [],
        sync: true
      },
      /**
       * @private
       */
      __expandedKeys: {
        type: Object,
        computed: "__computeExpandedKeys(itemIdPath, expandedItems)"
      }
    };
  }
  static get observers() {
    return ["_sizeChanged(size)", "_expandedItemsChanged(expandedItems)"];
  }
  constructor() {
    super();
    this._dataProviderController = new DataProviderController(this, {
      size: this.size || 0,
      pageSize: this.pageSize,
      getItemId: this.getItemId.bind(this),
      isExpanded: this._isExpanded.bind(this),
      dataProvider: this.dataProvider ? this.dataProvider.bind(this) : null,
      dataProviderParams: () => {
        return {
          sortOrders: this._mapSorters(),
          filters: this._mapFilters()
        };
      }
    });
    this._dataProviderController.addEventListener("page-requested", this._onDataProviderPageRequested.bind(this));
    this._dataProviderController.addEventListener("page-received", this._onDataProviderPageReceived.bind(this));
    this._dataProviderController.addEventListener("page-loaded", this._onDataProviderPageLoaded.bind(this));
  }
  /**
   * @protected
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get _cache() {
    console.warn("<vaadin-grid> The `_cache` property is deprecated and will be removed in Vaadin 25.");
    return this._dataProviderController.rootCache;
  }
  /**
   * @protected
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  get _effectiveSize() {
    console.warn("<vaadin-grid> The `_effectiveSize` property is deprecated and will be removed in Vaadin 25.");
    return this._flatSize;
  }
  /** @private */
  _sizeChanged(size) {
    this._dataProviderController.rootCache.size = size;
    this._dataProviderController.recalculateFlatSize();
    this._flatSize = this._dataProviderController.flatSize;
  }
  /** @private */
  __itemHasChildrenPathChanged(value, oldValue) {
    if (!oldValue && value === "children") {
      return;
    }
    this.requestContentUpdate();
  }
  /**
   * @param {number} index
   * @param {HTMLElement} el
   * @protected
   */
  _getItem(index, el) {
    el.index = index;
    const { item } = this._dataProviderController.getFlatIndexContext(index);
    if (item) {
      this.__updateLoading(el, false);
      this._updateItem(el, item);
      if (this._isExpanded(item)) {
        this._dataProviderController.ensureFlatIndexHierarchy(index);
      }
    } else {
      this.__updateLoading(el, true);
      this._dataProviderController.ensureFlatIndexLoaded(index);
    }
  }
  /**
   * @param {!HTMLElement} row
   * @param {boolean} loading
   * @private
   */
  __updateLoading(row, loading) {
    const cells = getBodyRowCells(row);
    updateState(row, "loading", loading);
    updateCellsPart(cells, "loading-row-cell", loading);
    if (loading) {
      this._generateCellClassNames(row);
      this._generateCellPartNames(row);
    }
  }
  /**
   * Returns a value that identifies the item. Uses `itemIdPath` if available.
   * Can be customized by overriding.
   * @param {!GridItem} item
   * @return {!GridItem | !unknown}
   */
  getItemId(item) {
    return this.itemIdPath ? get2(this.itemIdPath, item) : item;
  }
  /**
   * @param {!GridItem} item
   * @return {boolean}
   * @protected
   */
  _isExpanded(item) {
    return this.__expandedKeys && this.__expandedKeys.has(this.getItemId(item));
  }
  /** @private */
  _expandedItemsChanged() {
    this._dataProviderController.recalculateFlatSize();
    this._flatSize = this._dataProviderController.flatSize;
    this.__updateVisibleRows();
  }
  /** @private */
  __computeExpandedKeys(_itemIdPath, expandedItems) {
    const expanded = expandedItems || [];
    const expandedKeys = /* @__PURE__ */ new Set();
    expanded.forEach((item) => {
      expandedKeys.add(this.getItemId(item));
    });
    return expandedKeys;
  }
  /**
   * Expands the given item tree.
   * @param {!GridItem} item
   */
  expandItem(item) {
    if (!this._isExpanded(item)) {
      this.expandedItems = [...this.expandedItems, item];
    }
  }
  /**
   * Collapses the given item tree.
   * @param {!GridItem} item
   */
  collapseItem(item) {
    if (this._isExpanded(item)) {
      this.expandedItems = this.expandedItems.filter((i10) => !this._itemsEqual(i10, item));
    }
  }
  /**
   * @param {number} index
   * @return {number}
   * @protected
   */
  _getIndexLevel(index = 0) {
    const { level } = this._dataProviderController.getFlatIndexContext(index);
    return level;
  }
  /**
   * @param {number} page
   * @param {ItemCache} cache
   * @protected
   * @deprecated since 24.3 and will be removed in Vaadin 25.
   */
  _loadPage(page, cache) {
    console.warn("<vaadin-grid> The `_loadPage` method is deprecated and will be removed in Vaadin 25.");
    this._dataProviderController.__loadCachePage(cache, page);
  }
  /** @protected */
  _onDataProviderPageRequested() {
    this._setLoading(true);
  }
  /** @protected */
  _onDataProviderPageReceived() {
    if (this._flatSize !== this._dataProviderController.flatSize) {
      this._shouldUpdateAllRenderedRowsAfterPageLoad = true;
      this._flatSize = this._dataProviderController.flatSize;
    }
    this._getRenderedRows().forEach((row) => {
      this._dataProviderController.ensureFlatIndexHierarchy(row.index);
    });
    this._hasData = true;
  }
  /** @protected */
  _onDataProviderPageLoaded() {
    this._debouncerApplyCachedData = Debouncer.debounce(this._debouncerApplyCachedData, timeOut.after(0), () => {
      this._setLoading(false);
      const shouldUpdateAllRenderedRowsAfterPageLoad = this._shouldUpdateAllRenderedRowsAfterPageLoad;
      this._shouldUpdateAllRenderedRowsAfterPageLoad = false;
      this._getRenderedRows().forEach((row) => {
        const { item } = this._dataProviderController.getFlatIndexContext(row.index);
        if (item || shouldUpdateAllRenderedRowsAfterPageLoad) {
          this._getItem(row.index, row);
        }
      });
      this.__scrollToPendingIndexes();
      this.__dispatchPendingBodyCellFocus();
    });
    if (!this._dataProviderController.isLoading()) {
      this._debouncerApplyCachedData.flush();
    }
  }
  /** @private */
  __debounceClearCache() {
    this.__clearCacheDebouncer = Debouncer.debounce(this.__clearCacheDebouncer, microTask2, () => this.clearCache());
  }
  /**
   * Clears the cached pages and reloads data from dataprovider when needed.
   */
  clearCache() {
    this._dataProviderController.clearCache();
    this._dataProviderController.rootCache.size = this.size || 0;
    this._dataProviderController.recalculateFlatSize();
    this._hasData = false;
    this.__updateVisibleRows();
    if (!this.__virtualizer || !this.__virtualizer.size) {
      this._dataProviderController.loadFirstPage();
    }
  }
  /** @private */
  _pageSizeChanged(pageSize, oldPageSize) {
    this._dataProviderController.setPageSize(pageSize);
    if (oldPageSize !== void 0 && pageSize !== oldPageSize) {
      this.clearCache();
    }
  }
  /** @protected */
  _checkSize() {
    if (this.size === void 0 && this._flatSize === 0) {
      console.warn(
        "The <vaadin-grid> needs the total number of items in order to display rows, which you can specify either by setting the `size` property, or by providing it to the second argument of the `dataProvider` function `callback` call."
      );
    }
  }
  /** @private */
  _dataProviderChanged(dataProvider, oldDataProvider) {
    this._dataProviderController.setDataProvider(dataProvider ? dataProvider.bind(this) : null);
    if (oldDataProvider !== void 0) {
      this.clearCache();
    }
    this._ensureFirstPageLoaded();
    this._debouncerCheckSize = Debouncer.debounce(
      this._debouncerCheckSize,
      timeOut.after(2e3),
      this._checkSize.bind(this)
    );
  }
  /** @protected */
  _ensureFirstPageLoaded() {
    if (!this._hasData) {
      this._dataProviderController.loadFirstPage();
    }
  }
  /**
   * @param {!GridItem} item1
   * @param {!GridItem} item2
   * @return {boolean}
   * @protected
   */
  _itemsEqual(item1, item2) {
    return this.getItemId(item1) === this.getItemId(item2);
  }
  /**
   * @param {!GridItem} item
   * @param {!Array<!GridItem>} array
   * @return {number}
   * @protected
   */
  _getItemIndexInArray(item, array) {
    let result = -1;
    array.forEach((i10, idx) => {
      if (this._itemsEqual(i10, item)) {
        result = idx;
      }
    });
    return result;
  }
  /**
   * Scroll to a specific row index in the virtual list. Note that the row index is
   * not always the same for any particular item. For example, sorting or filtering
   * items can affect the row index related to an item.
   *
   * The `indexes` parameter can be either a single number or multiple numbers.
   * The grid will first try to scroll to the item at the first index on the top level.
   * In case the item at the first index is expanded, the grid will then try scroll to the
   * item at the second index within the children of the expanded first item, and so on.
   * Each given index points to a child of the item at the previous index.
   *
   * Using `Infinity` as an index will point to the last item on the level.
   *
   * @param indexes {...number} Row indexes to scroll to
   */
  scrollToIndex(...indexes) {
    if (!this.__virtualizer || !this.clientHeight || !this._columnTree) {
      this.__pendingScrollToIndexes = indexes;
      return;
    }
    let targetIndex;
    while (targetIndex !== (targetIndex = this._dataProviderController.getFlatIndexByPath(indexes))) {
      this._scrollToFlatIndex(targetIndex);
    }
    if (this._dataProviderController.isLoading()) {
      this.__pendingScrollToIndexes = indexes;
    }
  }
  /** @private */
  __scrollToPendingIndexes() {
    if (this.__pendingScrollToIndexes && this.$.items.children.length) {
      const indexes = this.__pendingScrollToIndexes;
      delete this.__pendingScrollToIndexes;
      this.scrollToIndex(...indexes);
    }
  }
  /**
   * Fired when the `expandedItems` property changes.
   *
   * @event expanded-items-changed
   */
  /**
   * Fired when the `loading` property changes.
   *
   * @event loading-changed
   */
};

// node_modules/@vaadin/grid/src/vaadin-grid-drag-and-drop-mixin.js
var DropMode = {
  BETWEEN: "between",
  ON_TOP: "on-top",
  ON_TOP_OR_BETWEEN: "on-top-or-between",
  ON_GRID: "on-grid"
};
var DropLocation = {
  ON_TOP: "on-top",
  ABOVE: "above",
  BELOW: "below",
  EMPTY: "empty"
};
var DragAndDropMixin = (superClass) => class DragAndDropMixin extends superClass {
  static get properties() {
    return {
      /**
       * Defines the locations within the Grid row where an element can be dropped.
       *
       * Possible values are:
       * - `between`: The drop event can happen between Grid rows.
       * - `on-top`: The drop event can happen on top of Grid rows.
       * - `on-top-or-between`: The drop event can happen either on top of or between Grid rows.
       * - `on-grid`: The drop event will not happen on any specific row, it will show the drop target outline around the whole grid.
       * @attr {between|on-top|on-top-or-between|on-grid} drop-mode
       * @type {GridDropMode | null | undefined}
       */
      dropMode: {
        type: String,
        sync: true
      },
      /**
       * Marks the grid's rows to be available for dragging.
       * @attr {boolean} rows-draggable
       */
      rowsDraggable: {
        type: Boolean,
        sync: true
      },
      /**
       * A function that filters dragging of specific grid rows. The return value should be false
       * if dragging of the row should be disabled.
       *
       * Receives one argument:
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *
       * @type {GridDragAndDropFilter | null | undefined}
       */
      dragFilter: {
        type: Function,
        sync: true
      },
      /**
       * A function that filters dropping on specific grid rows. The return value should be false
       * if dropping on the row should be disabled.
       *
       * Receives one argument:
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *
       * @type {GridDragAndDropFilter | null | undefined}
       */
      dropFilter: {
        type: Function,
        sync: true
      },
      /** @private */
      __dndAutoScrollThreshold: {
        value: 50
      },
      /** @private  */
      __draggedItems: {
        value: () => []
      }
    };
  }
  static get observers() {
    return ["_dragDropAccessChanged(rowsDraggable, dropMode, dragFilter, dropFilter, loading)"];
  }
  constructor() {
    super();
    this.__onDocumentDragStart = this.__onDocumentDragStart.bind(this);
  }
  /** @protected */
  ready() {
    super.ready();
    this.$.table.addEventListener("dragstart", this._onDragStart.bind(this));
    this.$.table.addEventListener("dragend", this._onDragEnd.bind(this));
    this.$.table.addEventListener("dragover", this._onDragOver.bind(this));
    this.$.table.addEventListener("dragleave", this._onDragLeave.bind(this));
    this.$.table.addEventListener("drop", this._onDrop.bind(this));
    this.$.table.addEventListener("dragenter", (e17) => {
      if (this.dropMode) {
        e17.preventDefault();
        e17.stopPropagation();
      }
    });
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    document.addEventListener("dragstart", this.__onDocumentDragStart, { capture: true });
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    document.removeEventListener("dragstart", this.__onDocumentDragStart, { capture: true });
  }
  /** @private */
  _onDragStart(e17) {
    if (this.rowsDraggable) {
      let row = e17.target;
      if (row.localName === "vaadin-grid-cell-content") {
        row = row.assignedSlot.parentNode.parentNode;
      }
      if (row.parentNode !== this.$.items) {
        return;
      }
      e17.stopPropagation();
      this.toggleAttribute("dragging-rows", true);
      if (this._safari) {
        const transform = row.style.transform;
        row.style.top = /translateY\((.*)\)/u.exec(transform)[1];
        row.style.transform = "none";
        requestAnimationFrame(() => {
          row.style.top = "";
          row.style.transform = transform;
        });
      }
      const rowRect = row.getBoundingClientRect();
      e17.dataTransfer.setDragImage(row, e17.clientX - rowRect.left, e17.clientY - rowRect.top);
      let rows = [row];
      if (this._isSelected(row._item)) {
        rows = this.__getViewportRows().filter((row2) => this._isSelected(row2._item)).filter((row2) => !this.dragFilter || this.dragFilter(this.__getRowModel(row2)));
      }
      this.__draggedItems = rows.map((row2) => row2._item);
      e17.dataTransfer.setData("text", this.__formatDefaultTransferData(rows));
      updateBooleanRowStates(row, { dragstart: rows.length > 1 ? `${rows.length}` : "" });
      this.style.setProperty("--_grid-drag-start-x", `${e17.clientX - rowRect.left + 20}px`);
      this.style.setProperty("--_grid-drag-start-y", `${e17.clientY - rowRect.top + 10}px`);
      requestAnimationFrame(() => {
        updateBooleanRowStates(row, { dragstart: false });
        this.style.setProperty("--_grid-drag-start-x", "");
        this.style.setProperty("--_grid-drag-start-y", "");
        this.requestContentUpdate();
      });
      const event = new CustomEvent("grid-dragstart", {
        detail: {
          draggedItems: [...this.__draggedItems],
          setDragData: (type, data) => e17.dataTransfer.setData(type, data),
          setDraggedItemsCount: (count) => row.setAttribute("dragstart", count)
        }
      });
      event.originalEvent = e17;
      this.dispatchEvent(event);
    }
  }
  /** @private */
  _onDragEnd(e17) {
    this.toggleAttribute("dragging-rows", false);
    e17.stopPropagation();
    const event = new CustomEvent("grid-dragend");
    event.originalEvent = e17;
    this.dispatchEvent(event);
    this.__draggedItems = [];
    this.requestContentUpdate();
  }
  /** @private */
  _onDragLeave(e17) {
    if (!this.dropMode) {
      return;
    }
    e17.stopPropagation();
    this._clearDragStyles();
  }
  /** @private */
  _onDragOver(e17) {
    if (this.dropMode) {
      this._dropLocation = void 0;
      this._dragOverItem = void 0;
      if (this.__dndAutoScroll(e17.clientY)) {
        this._clearDragStyles();
        return;
      }
      let row = e17.composedPath().find((node) => node.localName === "tr");
      if (!this._flatSize || this.dropMode === DropMode.ON_GRID) {
        this._dropLocation = DropLocation.EMPTY;
      } else if (!row || row.parentNode !== this.$.items) {
        if (row) {
          return;
        } else if (this.dropMode === DropMode.BETWEEN || this.dropMode === DropMode.ON_TOP_OR_BETWEEN) {
          row = Array.from(this.$.items.children).filter((row2) => !row2.hidden).pop();
          this._dropLocation = DropLocation.BELOW;
        } else {
          return;
        }
      } else {
        const rowRect = row.getBoundingClientRect();
        this._dropLocation = DropLocation.ON_TOP;
        if (this.dropMode === DropMode.BETWEEN) {
          const dropAbove = e17.clientY - rowRect.top < rowRect.bottom - e17.clientY;
          this._dropLocation = dropAbove ? DropLocation.ABOVE : DropLocation.BELOW;
        } else if (this.dropMode === DropMode.ON_TOP_OR_BETWEEN) {
          if (e17.clientY - rowRect.top < rowRect.height / 3) {
            this._dropLocation = DropLocation.ABOVE;
          } else if (e17.clientY - rowRect.top > rowRect.height / 3 * 2) {
            this._dropLocation = DropLocation.BELOW;
          }
        }
      }
      if (row && row.hasAttribute("drop-disabled")) {
        this._dropLocation = void 0;
        return;
      }
      e17.stopPropagation();
      e17.preventDefault();
      if (this._dropLocation === DropLocation.EMPTY) {
        this.toggleAttribute("dragover", true);
      } else if (row) {
        this._dragOverItem = row._item;
        if (row.getAttribute("dragover") !== this._dropLocation) {
          updateStringRowStates(row, { dragover: this._dropLocation });
        }
      } else {
        this._clearDragStyles();
      }
    }
  }
  /**
   * Webkit-based browsers have issues with generating drag images
   * for elements that have children with massive heights. Chromium
   * browsers crash, while Safari experiences significant performance
   * issues. To mitigate these issues, we hide the scroller element
   * when drag starts to remove it from the drag image.
   *
   * Grids with fewer rows also have issues on Chromium and Safari
   * where the drag image is not properly clipped and may include
   * content outside the grid. Temporary inline styles are applied
   * to mitigate this issue.
   *
   * Related issues:
   * - https://github.com/vaadin/web-components/issues/7985
   * - https://issues.chromium.org/issues/383356871
   * - https://github.com/vaadin/web-components/issues/8386
   *
   * @private
   */
  __onDocumentDragStart(e17) {
    if (e17.target.contains(this)) {
      const elements = [e17.target, this.$.items, this.$.scroller];
      const originalInlineStyles = elements.map((element) => element.style.cssText);
      if (this.$.table.scrollHeight > 2e4) {
        this.$.scroller.style.display = "none";
      }
      if (isChrome) {
        e17.target.style.willChange = "transform";
      }
      if (isSafari) {
        this.$.items.style.flexShrink = 1;
      }
      requestAnimationFrame(() => {
        elements.forEach((element, index) => {
          element.style.cssText = originalInlineStyles[index];
        });
      });
    }
  }
  /** @private */
  __dndAutoScroll(clientY) {
    if (this.__dndAutoScrolling) {
      return true;
    }
    const headerBottom = this.$.header.getBoundingClientRect().bottom;
    const footerTop = this.$.footer.getBoundingClientRect().top;
    const topDiff = headerBottom - clientY + this.__dndAutoScrollThreshold;
    const bottomDiff = clientY - footerTop + this.__dndAutoScrollThreshold;
    let scrollTopDelta = 0;
    if (bottomDiff > 0) {
      scrollTopDelta = bottomDiff * 2;
    } else if (topDiff > 0) {
      scrollTopDelta = -topDiff * 2;
    }
    if (scrollTopDelta) {
      const scrollTop = this.$.table.scrollTop;
      this.$.table.scrollTop += scrollTopDelta;
      const scrollTopChanged = scrollTop !== this.$.table.scrollTop;
      if (scrollTopChanged) {
        this.__dndAutoScrolling = true;
        setTimeout(() => {
          this.__dndAutoScrolling = false;
        }, 20);
        return true;
      }
    }
  }
  /** @private */
  __getViewportRows() {
    const headerBottom = this.$.header.getBoundingClientRect().bottom;
    const footerTop = this.$.footer.getBoundingClientRect().top;
    return Array.from(this.$.items.children).filter((row) => {
      const rowRect = row.getBoundingClientRect();
      return rowRect.bottom > headerBottom && rowRect.top < footerTop;
    });
  }
  /** @protected */
  _clearDragStyles() {
    this.removeAttribute("dragover");
    iterateChildren(this.$.items, (row) => {
      updateStringRowStates(row, { dragover: null });
    });
  }
  /** @private */
  __updateDragSourceParts(row, model) {
    updateBooleanRowStates(row, { "drag-source": this.__draggedItems.includes(model.item) });
  }
  /** @private */
  _onDrop(e17) {
    if (this.dropMode && this._dropLocation) {
      e17.stopPropagation();
      e17.preventDefault();
      const dragData = e17.dataTransfer.types && Array.from(e17.dataTransfer.types).map((type) => {
        return {
          type,
          data: e17.dataTransfer.getData(type)
        };
      });
      this._clearDragStyles();
      const event = new CustomEvent("grid-drop", {
        bubbles: e17.bubbles,
        cancelable: e17.cancelable,
        detail: {
          dropTargetItem: this._dragOverItem,
          dropLocation: this._dropLocation,
          dragData
        }
      });
      event.originalEvent = e17;
      this.dispatchEvent(event);
    }
  }
  /** @private */
  __formatDefaultTransferData(rows) {
    return rows.map((row) => {
      return Array.from(row.children).filter((cell) => !cell.hidden && cell.getAttribute("part").indexOf("details-cell") === -1).sort((a17, b7) => {
        return a17._column._order > b7._column._order ? 1 : -1;
      }).map((cell) => cell._content.textContent.trim()).filter((content) => content).join("	");
    }).join("\n");
  }
  /** @private */
  _dragDropAccessChanged() {
    this.filterDragAndDrop();
  }
  /**
   * Runs the `dragFilter` and `dropFilter` hooks for the visible cells.
   * If the filter depends on varying conditions, you may need to
   * call this function manually in order to update the draggability when
   * the conditions change.
   */
  filterDragAndDrop() {
    iterateChildren(this.$.items, (row) => {
      if (!row.hidden) {
        this._filterDragAndDrop(row, this.__getRowModel(row));
      }
    });
  }
  /**
   * @param {!HTMLElement} row
   * @param {!GridItemModel} model
   * @protected
   */
  _filterDragAndDrop(row, model) {
    const loading = this.loading || row.hasAttribute("loading");
    const dragDisabled = !this.rowsDraggable || loading || this.dragFilter && !this.dragFilter(model);
    const dropDisabled = !this.dropMode || loading || this.dropFilter && !this.dropFilter(model);
    iterateRowCells(row, (cell) => {
      if (dragDisabled) {
        cell._content.removeAttribute("draggable");
      } else {
        cell._content.setAttribute("draggable", true);
      }
    });
    updateBooleanRowStates(row, {
      "drag-disabled": !!dragDisabled,
      "drop-disabled": !!dropDisabled
    });
  }
  /**
   * Fired when starting to drag grid rows.
   *
   * @event grid-dragstart
   * @param {Object} originalEvent The native dragstart event
   * @param {Object} detail
   * @param {Object} detail.draggedItems the items in the visible viewport that are dragged
   * @param {Function} detail.setDraggedItemsCount Overrides the default number shown in the drag image on multi row drag.
   * Parameter is of type number.
   * @param {Function} detail.setDragData Sets dataTransfer data for the drag operation.
   * Note that "text" is the only data type supported by all the browsers the grid currently supports (including IE11).
   * The function takes two parameters:
   * - type:string The type of the data
   * - data:string The data
   */
  /**
   * Fired when the dragging of the rows ends.
   *
   * @event grid-dragend
   * @param {Object} originalEvent The native dragend event
   */
  /**
   * Fired when a drop occurs on top of the grid.
   *
   * @event grid-drop
   * @param {Object} originalEvent The native drop event
   * @param {Object} detail
   * @param {Object} detail.dropTargetItem The item of the grid row on which the drop occurred.
   * @param {string} detail.dropLocation The position at which the drop event took place relative to a row.
   * Depending on the dropMode value, the drop location can be one of the following
   * - `on-top`: when the drop occurred on top of the row
   * - `above`: when the drop occurred above the row
   * - `below`: when the drop occurred below the row
   * - `empty`: when the drop occurred over the grid, not relative to any specific row
   * @param {string} detail.dragData An array of items with the payload as a string representation as the
   * `data` property and the type of the data as `type` property.
   */
};

// node_modules/@vaadin/grid/src/vaadin-grid-dynamic-columns-mixin.js
function arrayEquals(arr1, arr2) {
  if (!arr1 || !arr2 || arr1.length !== arr2.length) {
    return false;
  }
  for (let i10 = 0, l17 = arr1.length; i10 < l17; i10++) {
    if (arr1[i10] instanceof Array && arr2[i10] instanceof Array) {
      if (!arrayEquals(arr1[i10], arr2[i10])) {
        return false;
      }
    } else if (arr1[i10] !== arr2[i10]) {
      return false;
    }
  }
  return true;
}
var DynamicColumnsMixin = (superClass) => class DynamicColumnsMixin extends superClass {
  static get properties() {
    return {
      /**
       * @protected
       */
      _columnTree: {
        type: Object,
        sync: true
      }
    };
  }
  /** @protected */
  ready() {
    super.ready();
    this._addNodeObserver();
  }
  /** @private */
  _hasColumnGroups(columns) {
    return columns.some((column) => column.localName === "vaadin-grid-column-group");
  }
  /**
   * @param {!GridColumnGroup} el
   * @return {!Array<!GridColumn>}
   * @protected
   */
  _getChildColumns(el) {
    return ColumnObserver.getColumns(el);
  }
  /** @private */
  _flattenColumnGroups(columns) {
    return columns.map((col) => {
      if (col.localName === "vaadin-grid-column-group") {
        return this._getChildColumns(col);
      }
      return [col];
    }).reduce((prev, curr) => {
      return prev.concat(curr);
    }, []);
  }
  /** @private */
  _getColumnTree() {
    const rootColumns = ColumnObserver.getColumns(this);
    const columnTree = [rootColumns];
    let c17 = rootColumns;
    while (this._hasColumnGroups(c17)) {
      c17 = this._flattenColumnGroups(c17);
      columnTree.push(c17);
    }
    return columnTree;
  }
  /** @protected */
  _debounceUpdateColumnTree() {
    this.__updateColumnTreeDebouncer = Debouncer.debounce(
      this.__updateColumnTreeDebouncer,
      microTask2,
      () => this._updateColumnTree()
    );
  }
  /** @protected */
  _updateColumnTree() {
    const columnTree = this._getColumnTree();
    if (!arrayEquals(columnTree, this._columnTree)) {
      this._columnTree = columnTree;
    }
  }
  /** @private */
  _addNodeObserver() {
    this._observer = new ColumnObserver(this, (_addedColumns, removedColumns) => {
      const allRemovedCells = removedColumns.flatMap((c17) => c17._allCells);
      const filterNotConnected = (element) => allRemovedCells.filter((cell) => cell && cell._content.contains(element)).length;
      this.__removeSorters(this._sorters.filter(filterNotConnected));
      this.__removeFilters(this._filters.filter(filterNotConnected));
      this._debounceUpdateColumnTree();
      this._debouncerCheckImports = Debouncer.debounce(
        this._debouncerCheckImports,
        timeOut.after(2e3),
        this._checkImports.bind(this)
      );
      this._ensureFirstPageLoaded();
    });
  }
  /** @protected */
  _checkImports() {
    [
      "vaadin-grid-column-group",
      "vaadin-grid-filter",
      "vaadin-grid-filter-column",
      "vaadin-grid-tree-toggle",
      "vaadin-grid-selection-column",
      "vaadin-grid-sort-column",
      "vaadin-grid-sorter"
    ].forEach((elementName) => {
      const element = this.querySelector(elementName);
      if (element && !customElements.get(elementName)) {
        console.warn(`Make sure you have imported the required module for <${elementName}> element.`);
      }
    });
  }
  /** @protected */
  _updateFirstAndLastColumn() {
    Array.from(this.shadowRoot.querySelectorAll("tr")).forEach((row) => this._updateFirstAndLastColumnForRow(row));
  }
  /**
   * @param {!HTMLElement} row
   * @protected
   */
  _updateFirstAndLastColumnForRow(row) {
    Array.from(row.querySelectorAll('[part~="cell"]:not([part~="details-cell"])')).sort((a17, b7) => {
      return a17._column._order - b7._column._order;
    }).forEach((cell, cellIndex, children) => {
      updateCellState(cell, "first-column", cellIndex === 0);
      updateCellState(cell, "last-column", cellIndex === children.length - 1);
    });
  }
  /**
   * @param {!Node} node
   * @return {boolean}
   * @protected
   */
  _isColumnElement(node) {
    return node.nodeType === Node.ELEMENT_NODE && /\bcolumn\b/u.test(node.localName);
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-event-context-mixin.js
var EventContextMixin = (superClass) => class EventContextMixin extends superClass {
  /**
   * Returns an object with context information about the event target:
   * - `item`: the data object corresponding to the targeted row (not specified when targeting header or footer)
   * - `column`: the column element corresponding to the targeted cell (not specified when targeting row details)
   * - `section`: whether the event targeted the body, header, footer or details of the grid
   *
   * These additional properties are included when `item` is specified:
   * - `index`: the index of the item
   * - `selected`: the selected state of the item
   * - `detailsOpened`: whether the row details are open for the item
   * - `expanded`: the expanded state of the tree toggle
   * - `level`: the tree hierarchy level
   *
   * The returned object is populated only when a grid cell, header, footer or row details is found in `event.composedPath()`.
   * This means mostly mouse and keyboard events. If such a grid part is not found in the path, an empty object is returned.
   * This may be the case eg. if the event is fired on the `<vaadin-grid>` element and not any deeper in the DOM, or if
   * the event targets the empty part of the grid body.
   *
   * @param {!Event} event
   * @return {GridEventContext}
   */
  getEventContext(event) {
    const context = {};
    const { cell } = this._getGridEventLocation(event);
    if (!cell) {
      return context;
    }
    context.section = ["body", "header", "footer", "details"].find(
      (section) => cell.getAttribute("part").indexOf(section) > -1
    );
    if (cell._column) {
      context.column = cell._column;
    }
    if (context.section === "body" || context.section === "details") {
      Object.assign(context, this.__getRowModel(cell.parentElement));
    }
    return context;
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-filter-mixin.js
var FilterMixin = (superClass) => class FilterMixin extends superClass {
  static get properties() {
    return {
      /** @private */
      _filters: {
        type: Array,
        value: () => []
      }
    };
  }
  constructor() {
    super();
    this._filterChanged = this._filterChanged.bind(this);
    this.addEventListener("filter-changed", this._filterChanged);
  }
  /** @private */
  _filterChanged(e17) {
    e17.stopPropagation();
    this.__addFilter(e17.target);
    this.__applyFilters();
  }
  /** @private */
  __removeFilters(filtersToRemove) {
    if (filtersToRemove.length === 0) {
      return;
    }
    this._filters = this._filters.filter((filter2) => filtersToRemove.indexOf(filter2) < 0);
    this.__applyFilters();
  }
  /** @private */
  __addFilter(filter2) {
    const filterIndex = this._filters.indexOf(filter2);
    if (filterIndex === -1) {
      this._filters.push(filter2);
    }
  }
  /** @private */
  __applyFilters() {
    if (this.dataProvider && this.isAttached) {
      this.clearCache();
    }
  }
  /**
   * @return {!Array<!GridFilterDefinition>}
   * @protected
   */
  _mapFilters() {
    return this._filters.map((filter2) => {
      return {
        path: filter2.path,
        value: filter2.value
      };
    });
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-keyboard-navigation-mixin.js
function isRow(element) {
  return element instanceof HTMLTableRowElement;
}
function isCell(element) {
  return element instanceof HTMLTableCellElement;
}
function isDetailsCell(element) {
  return element.matches('[part~="details-cell"]');
}
var KeyboardNavigationMixin = (superClass) => class KeyboardNavigationMixin extends superClass {
  static get properties() {
    return {
      /** @private */
      _headerFocusable: {
        type: Object,
        observer: "_focusableChanged",
        sync: true
      },
      /**
       * @type {!HTMLElement | undefined}
       * @protected
       */
      _itemsFocusable: {
        type: Object,
        observer: "_focusableChanged",
        sync: true
      },
      /** @private */
      _footerFocusable: {
        type: Object,
        observer: "_focusableChanged",
        sync: true
      },
      /** @private */
      _navigatingIsHidden: Boolean,
      /**
       * @type {number}
       * @protected
       */
      _focusedItemIndex: {
        type: Number,
        value: 0
      },
      /** @private */
      _focusedColumnOrder: Number,
      /** @private */
      _focusedCell: {
        type: Object,
        observer: "_focusedCellChanged",
        sync: true
      },
      /**
       * Indicates whether the grid is currently in interaction mode.
       * In interaction mode the user is currently interacting with a control,
       * such as an input or a select, within a cell.
       * In interaction mode keyboard navigation between cells is disabled.
       * Interaction mode also prevents the focus target cell of that section of
       * the grid from receiving focus, allowing the user to switch focus to
       * controls in adjacent cells, rather than focussing the outer cell
       * itself.
       * @type {boolean}
       * @private
       */
      interacting: {
        type: Boolean,
        value: false,
        reflectToAttribute: true,
        readOnly: true,
        observer: "_interactingChanged"
      }
    };
  }
  /** @private */
  get __rowFocusMode() {
    return [this._headerFocusable, this._itemsFocusable, this._footerFocusable].some(isRow);
  }
  set __rowFocusMode(value) {
    ["_itemsFocusable", "_footerFocusable", "_headerFocusable"].forEach((prop) => {
      const focusable = this[prop];
      if (value) {
        const parent = focusable && focusable.parentElement;
        if (isCell(focusable)) {
          this[prop] = parent;
        } else if (isCell(parent)) {
          this[prop] = parent.parentElement;
        }
      } else if (!value && isRow(focusable)) {
        const cell = focusable.firstElementChild;
        this[prop] = cell._focusButton || cell;
      }
    });
  }
  /** @private */
  get _visibleItemsCount() {
    return this._lastVisibleIndex - this._firstVisibleIndex - 1;
  }
  /** @protected */
  ready() {
    super.ready();
    if (this._ios || this._android) {
      return;
    }
    this.addEventListener("keydown", this._onKeyDown);
    this.addEventListener("keyup", this._onKeyUp);
    this.addEventListener("focusin", this._onFocusIn);
    this.addEventListener("focusout", this._onFocusOut);
    this.$.table.addEventListener("focusin", this._onContentFocusIn.bind(this));
    this.addEventListener("mousedown", () => {
      this.toggleAttribute("navigating", false);
      this._isMousedown = true;
      this._focusedColumnOrder = void 0;
    });
    this.addEventListener("mouseup", () => {
      this._isMousedown = false;
    });
  }
  /** @private */
  _focusableChanged(focusable, oldFocusable) {
    if (oldFocusable) {
      oldFocusable.setAttribute("tabindex", "-1");
    }
    if (focusable) {
      this._updateGridSectionFocusTarget(focusable);
    }
  }
  /** @private */
  _focusedCellChanged(focusedCell, oldFocusedCell) {
    if (oldFocusedCell) {
      removeValueFromAttribute(oldFocusedCell, "part", "focused-cell");
    }
    if (focusedCell) {
      addValueToAttribute(focusedCell, "part", "focused-cell");
    }
  }
  /** @private */
  _interactingChanged() {
    this._updateGridSectionFocusTarget(this._headerFocusable);
    this._updateGridSectionFocusTarget(this._itemsFocusable);
    this._updateGridSectionFocusTarget(this._footerFocusable);
  }
  /**
   * Since the focused cell/row state is stored as an element reference, the reference may get
   * out of sync when the virtual indexes for elements update due to effective size change.
   * This function updates the reference to the correct element after a possible index change.
   * @private
   */
  __updateItemsFocusable() {
    if (!this._itemsFocusable) {
      return;
    }
    const wasFocused = this.shadowRoot.activeElement === this._itemsFocusable;
    this._getRenderedRows().forEach((row) => {
      if (row.index === this._focusedItemIndex) {
        if (this.__rowFocusMode) {
          this._itemsFocusable = row;
        } else {
          let parent = this._itemsFocusable.parentElement;
          let cell = this._itemsFocusable;
          if (parent) {
            if (isCell(parent)) {
              cell = parent;
              parent = parent.parentElement;
            }
            const cellIndex = [...parent.children].indexOf(cell);
            this._itemsFocusable = this.__getFocusable(row, row.children[cellIndex]);
          }
        }
      }
    });
    if (wasFocused) {
      this._itemsFocusable.focus();
    }
  }
  /**
   * @param {!KeyboardEvent} e
   * @protected
   */
  _onKeyDown(e17) {
    const key = e17.key;
    let keyGroup;
    switch (key) {
      case "ArrowUp":
      case "ArrowDown":
      case "ArrowLeft":
      case "ArrowRight":
      case "PageUp":
      case "PageDown":
      case "Home":
      case "End":
        keyGroup = "Navigation";
        break;
      case "Enter":
      case "Escape":
      case "F2":
        keyGroup = "Interaction";
        break;
      case "Tab":
        keyGroup = "Tab";
        break;
      case " ":
        keyGroup = "Space";
        break;
      default:
        break;
    }
    this._detectInteracting(e17);
    if (this.interacting && keyGroup !== "Interaction") {
      keyGroup = void 0;
    }
    if (keyGroup) {
      this[`_on${keyGroup}KeyDown`](e17, key);
    }
  }
  /** @private */
  __ensureFlatIndexInViewport(index) {
    const targetRowInDom = [...this.$.items.children].find((child) => child.index === index);
    if (!targetRowInDom) {
      this._scrollToFlatIndex(index);
    } else {
      this.__scrollIntoViewport(targetRowInDom);
    }
  }
  /** @private */
  __isRowExpandable(row) {
    if (this.itemHasChildrenPath) {
      const item = row._item;
      return !!(item && get2(this.itemHasChildrenPath, item) && !this._isExpanded(item));
    }
  }
  /** @private */
  __isRowCollapsible(row) {
    return this._isExpanded(row._item);
  }
  /** @private */
  _onNavigationKeyDown(e17, key) {
    e17.preventDefault();
    const isRTL = this.__isRTL;
    const activeRow = e17.composedPath().find(isRow);
    const activeCell = e17.composedPath().find(isCell);
    let dx = 0, dy = 0;
    switch (key) {
      case "ArrowRight":
        dx = isRTL ? -1 : 1;
        break;
      case "ArrowLeft":
        dx = isRTL ? 1 : -1;
        break;
      case "Home":
        if (this.__rowFocusMode) {
          dy = -Infinity;
        } else if (e17.ctrlKey) {
          dy = -Infinity;
        } else {
          dx = -Infinity;
        }
        break;
      case "End":
        if (this.__rowFocusMode) {
          dy = Infinity;
        } else if (e17.ctrlKey) {
          dy = Infinity;
        } else {
          dx = Infinity;
        }
        break;
      case "ArrowDown":
        dy = 1;
        break;
      case "ArrowUp":
        dy = -1;
        break;
      case "PageDown":
        if (this.$.items.contains(activeRow)) {
          const currentRowIndex = this.__getIndexInGroup(activeRow, this._focusedItemIndex);
          this._scrollToFlatIndex(currentRowIndex);
        }
        dy = this._visibleItemsCount;
        break;
      case "PageUp":
        dy = -this._visibleItemsCount;
        break;
      default:
        break;
    }
    if (this.__rowFocusMode && !activeRow || !this.__rowFocusMode && !activeCell) {
      return;
    }
    const forwardsKey = isRTL ? "ArrowLeft" : "ArrowRight";
    const backwardsKey = isRTL ? "ArrowRight" : "ArrowLeft";
    if (key === forwardsKey) {
      if (this.__rowFocusMode) {
        if (this.__isRowExpandable(activeRow)) {
          this.expandItem(activeRow._item);
          return;
        }
        this.__rowFocusMode = false;
        this._onCellNavigation(activeRow.firstElementChild, 0, 0);
        return;
      }
    } else if (key === backwardsKey) {
      if (this.__rowFocusMode) {
        if (this.__isRowCollapsible(activeRow)) {
          this.collapseItem(activeRow._item);
          return;
        }
      } else {
        const activeRowCells = [...activeRow.children].sort((a17, b7) => a17._order - b7._order);
        if (activeCell === activeRowCells[0] || isDetailsCell(activeCell)) {
          this.__rowFocusMode = true;
          this._onRowNavigation(activeRow, 0);
          return;
        }
      }
    }
    if (this.__rowFocusMode) {
      this._onRowNavigation(activeRow, dy);
    } else {
      this._onCellNavigation(activeCell, dx, dy);
    }
  }
  /**
   * Focuses the target row after navigating by the given dy offset.
   * If the row is not in the viewport, it is first scrolled to.
   * @private
   */
  _onRowNavigation(activeRow, dy) {
    const { dstRow } = this.__navigateRows(dy, activeRow);
    if (dstRow) {
      dstRow.focus();
    }
  }
  /** @private */
  __getIndexInGroup(row, bodyFallbackIndex) {
    const rowGroup = row.parentNode;
    if (rowGroup === this.$.items) {
      return bodyFallbackIndex !== void 0 ? bodyFallbackIndex : row.index;
    }
    return [...rowGroup.children].indexOf(row);
  }
  /**
   * Returns the target row after navigating by the given dy offset.
   * Also returns information whether the details cell should be the target on the target row.
   * If the row is not in the viewport, it is first scrolled to.
   * @private
   */
  __navigateRows(dy, activeRow, activeCell) {
    const currentRowIndex = this.__getIndexInGroup(activeRow, this._focusedItemIndex);
    const activeRowGroup = activeRow.parentNode;
    const maxRowIndex = (activeRowGroup === this.$.items ? this._flatSize : activeRowGroup.children.length) - 1;
    let dstRowIndex = Math.max(0, Math.min(currentRowIndex + dy, maxRowIndex));
    if (activeRowGroup !== this.$.items) {
      if (dstRowIndex > currentRowIndex) {
        while (dstRowIndex < maxRowIndex && activeRowGroup.children[dstRowIndex].hidden) {
          dstRowIndex += 1;
        }
      } else if (dstRowIndex < currentRowIndex) {
        while (dstRowIndex > 0 && activeRowGroup.children[dstRowIndex].hidden) {
          dstRowIndex -= 1;
        }
      }
      this.toggleAttribute("navigating", true);
      return { dstRow: activeRowGroup.children[dstRowIndex] };
    }
    let dstIsRowDetails = false;
    if (activeCell) {
      const isRowDetails = isDetailsCell(activeCell);
      if (activeRowGroup === this.$.items) {
        const item = activeRow._item;
        const { item: dstItem } = this._dataProviderController.getFlatIndexContext(dstRowIndex);
        if (isRowDetails) {
          dstIsRowDetails = dy === 0;
        } else {
          dstIsRowDetails = dy === 1 && this._isDetailsOpened(item) || dy === -1 && dstRowIndex !== currentRowIndex && this._isDetailsOpened(dstItem);
        }
        if (dstIsRowDetails !== isRowDetails && (dy === 1 && dstIsRowDetails || dy === -1 && !dstIsRowDetails)) {
          dstRowIndex = currentRowIndex;
        }
      }
    }
    this.__ensureFlatIndexInViewport(dstRowIndex);
    this._focusedItemIndex = dstRowIndex;
    this.toggleAttribute("navigating", true);
    return {
      dstRow: [...activeRowGroup.children].find((el) => !el.hidden && el.index === dstRowIndex),
      dstIsRowDetails
    };
  }
  /**
   * Focuses the target cell after navigating by the given dx and dy offset.
   * If the cell is not in the viewport, it is first scrolled to.
   * @private
   */
  _onCellNavigation(activeCell, dx, dy) {
    const activeRow = activeCell.parentNode;
    const { dstRow, dstIsRowDetails } = this.__navigateRows(dy, activeRow, activeCell);
    if (!dstRow) {
      return;
    }
    let columnIndex = [...activeRow.children].indexOf(activeCell);
    if (this.$.items.contains(activeCell)) {
      columnIndex = [...this.$.sizer.children].findIndex((sizerCell) => sizerCell._column === activeCell._column);
    }
    const isCurrentCellRowDetails = isDetailsCell(activeCell);
    const activeRowGroup = activeRow.parentNode;
    const currentRowIndex = this.__getIndexInGroup(activeRow, this._focusedItemIndex);
    if (this._focusedColumnOrder === void 0) {
      if (isCurrentCellRowDetails) {
        this._focusedColumnOrder = 0;
      } else {
        this._focusedColumnOrder = this._getColumns(activeRowGroup, currentRowIndex).filter((c17) => !c17.hidden)[columnIndex]._order;
      }
    }
    if (dstIsRowDetails) {
      const dstCell = [...dstRow.children].find(isDetailsCell);
      dstCell.focus();
    } else {
      const dstRowIndex = this.__getIndexInGroup(dstRow, this._focusedItemIndex);
      const dstColumns = this._getColumns(activeRowGroup, dstRowIndex).filter((c17) => !c17.hidden);
      const dstSortedColumnOrders = dstColumns.map((c17) => c17._order).sort((b7, a17) => b7 - a17);
      const maxOrderedColumnIndex = dstSortedColumnOrders.length - 1;
      const orderedColumnIndex = dstSortedColumnOrders.indexOf(
        dstSortedColumnOrders.slice(0).sort((b7, a17) => Math.abs(b7 - this._focusedColumnOrder) - Math.abs(a17 - this._focusedColumnOrder))[0]
      );
      const dstOrderedColumnIndex = dy === 0 && isCurrentCellRowDetails ? orderedColumnIndex : Math.max(0, Math.min(orderedColumnIndex + dx, maxOrderedColumnIndex));
      if (dstOrderedColumnIndex !== orderedColumnIndex) {
        this._focusedColumnOrder = void 0;
      }
      const columnIndexByOrder = dstColumns.reduce((acc, col, i10) => {
        acc[col._order] = i10;
        return acc;
      }, {});
      const dstColumnIndex = columnIndexByOrder[dstSortedColumnOrders[dstOrderedColumnIndex]];
      let dstCell;
      if (this.$.items.contains(activeCell)) {
        const dstSizerCell = this.$.sizer.children[dstColumnIndex];
        if (this._lazyColumns) {
          if (!this.__isColumnInViewport(dstSizerCell._column)) {
            dstSizerCell.scrollIntoView();
          }
          this.__updateColumnsBodyContentHidden();
          this.__updateHorizontalScrollPosition();
        }
        dstCell = [...dstRow.children].find((cell) => cell._column === dstSizerCell._column);
        this._scrollHorizontallyToCell(dstCell);
      } else {
        dstCell = dstRow.children[dstColumnIndex];
        this._scrollHorizontallyToCell(dstCell);
      }
      dstCell.focus();
    }
  }
  /** @private */
  _onInteractionKeyDown(e17, key) {
    const localTarget = e17.composedPath()[0];
    const localTargetIsTextInput = localTarget.localName === "input" && !/^(button|checkbox|color|file|image|radio|range|reset|submit)$/iu.test(localTarget.type);
    let wantInteracting;
    switch (key) {
      case "Enter":
        wantInteracting = this.interacting ? !localTargetIsTextInput : true;
        break;
      case "Escape":
        wantInteracting = false;
        break;
      case "F2":
        wantInteracting = !this.interacting;
        break;
      default:
        break;
    }
    const { cell } = this._getGridEventLocation(e17);
    if (this.interacting !== wantInteracting && cell !== null) {
      if (wantInteracting) {
        const focusTarget = cell._content.querySelector("[focus-target]") || // If a child element hasn't been explicitly marked as a focus target,
        // fall back to any focusable element inside the cell.
        [...cell._content.querySelectorAll("*")].find((node) => this._isFocusable(node));
        if (focusTarget) {
          e17.preventDefault();
          focusTarget.focus();
          this._setInteracting(true);
          this.toggleAttribute("navigating", false);
        }
      } else {
        e17.preventDefault();
        this._focusedColumnOrder = void 0;
        cell.focus();
        this._setInteracting(false);
        this.toggleAttribute("navigating", true);
      }
    }
    if (key === "Escape") {
      this._hideTooltip(true);
    }
  }
  /** @private */
  _predictFocusStepTarget(srcElement, step) {
    const tabOrder = [
      this.$.table,
      this._headerFocusable,
      this.__emptyState ? this.$.emptystatecell : this._itemsFocusable,
      this._footerFocusable,
      this.$.focusexit
    ];
    let index = tabOrder.indexOf(srcElement);
    index += step;
    while (index >= 0 && index <= tabOrder.length - 1) {
      let rowElement = tabOrder[index];
      if (rowElement && !this.__rowFocusMode) {
        rowElement = tabOrder[index].parentNode;
      }
      if (!rowElement || rowElement.hidden) {
        index += step;
      } else {
        break;
      }
    }
    let focusStepTarget = tabOrder[index];
    if (focusStepTarget && !this.__isHorizontallyInViewport(focusStepTarget)) {
      const firstVisibleColumn = this._getColumnsInOrder().find((column) => this.__isColumnInViewport(column));
      if (firstVisibleColumn) {
        if (focusStepTarget === this._headerFocusable) {
          focusStepTarget = firstVisibleColumn._headerCell;
        } else if (focusStepTarget === this._itemsFocusable) {
          const rowIndex = focusStepTarget._column._cells.indexOf(focusStepTarget);
          focusStepTarget = firstVisibleColumn._cells[rowIndex];
        } else if (focusStepTarget === this._footerFocusable) {
          focusStepTarget = firstVisibleColumn._footerCell;
        }
      }
    }
    return focusStepTarget;
  }
  /** @private */
  _onTabKeyDown(e17) {
    let focusTarget = this._predictFocusStepTarget(e17.composedPath()[0], e17.shiftKey ? -1 : 1);
    if (!focusTarget) {
      return;
    }
    e17.stopPropagation();
    if (focusTarget === this._itemsFocusable) {
      this.__ensureFlatIndexInViewport(this._focusedItemIndex);
      this.__updateItemsFocusable();
      focusTarget = this._itemsFocusable;
    }
    focusTarget.focus();
    if (focusTarget !== this.$.table && focusTarget !== this.$.focusexit) {
      e17.preventDefault();
    }
    this.toggleAttribute("navigating", true);
  }
  /** @private */
  _onSpaceKeyDown(e17) {
    e17.preventDefault();
    const element = e17.composedPath()[0];
    const isElementRow = isRow(element);
    if (isElementRow || !element._content || !element._content.firstElementChild) {
      this.dispatchEvent(
        new CustomEvent(isElementRow ? "row-activate" : "cell-activate", {
          detail: {
            model: this.__getRowModel(isElementRow ? element : element.parentElement)
          }
        })
      );
    }
  }
  /** @private */
  _onKeyUp(e17) {
    if (!/^( |SpaceBar)$/u.test(e17.key) || this.interacting) {
      return;
    }
    e17.preventDefault();
    const cell = e17.composedPath()[0];
    if (cell._content && cell._content.firstElementChild) {
      const wasNavigating = this.hasAttribute("navigating");
      cell._content.firstElementChild.dispatchEvent(
        new MouseEvent("click", {
          shiftKey: e17.shiftKey,
          bubbles: true,
          composed: true,
          cancelable: true
        })
      );
      this.toggleAttribute("navigating", wasNavigating);
    }
  }
  /**
   * @param {!FocusEvent} e
   * @protected
   */
  _onFocusIn(e17) {
    if (!this._isMousedown) {
      this.toggleAttribute("navigating", true);
    }
    const rootTarget = e17.composedPath()[0];
    if (rootTarget === this.$.table || rootTarget === this.$.focusexit) {
      if (!this._isMousedown) {
        this._predictFocusStepTarget(rootTarget, rootTarget === this.$.table ? 1 : -1).focus();
      }
      this._setInteracting(false);
    } else {
      this._detectInteracting(e17);
    }
  }
  /**
   * @param {!FocusEvent} e
   * @protected
   */
  _onFocusOut(e17) {
    this.toggleAttribute("navigating", false);
    this._detectInteracting(e17);
    this._hideTooltip();
    this._focusedCell = null;
  }
  /** @private */
  _onContentFocusIn(e17) {
    const { section, cell, row } = this._getGridEventLocation(e17);
    if (!cell && !this.__rowFocusMode) {
      return;
    }
    this._detectInteracting(e17);
    if (section && (cell || row)) {
      this._activeRowGroup = section;
      if (section === this.$.header) {
        this._headerFocusable = this.__getFocusable(row, cell);
      } else if (section === this.$.items) {
        this._itemsFocusable = this.__getFocusable(row, cell);
        this._focusedItemIndex = row.index;
      } else if (section === this.$.footer) {
        this._footerFocusable = this.__getFocusable(row, cell);
      }
      if (cell) {
        const context = this.getEventContext(e17);
        this.__pendingBodyCellFocus = this.loading && context.section === "body";
        if (!this.__pendingBodyCellFocus && cell !== this.$.emptystatecell) {
          cell.dispatchEvent(new CustomEvent("cell-focus", { bubbles: true, composed: true, detail: { context } }));
        }
        this._focusedCell = cell._focusButton || cell;
        if (isKeyboardActive() && e17.target === cell) {
          this._showTooltip(e17);
        }
      } else {
        this._focusedCell = null;
      }
    }
  }
  /**
   * @private
   */
  __dispatchPendingBodyCellFocus() {
    if (this.__pendingBodyCellFocus && this.shadowRoot.activeElement === this._itemsFocusable) {
      this._itemsFocusable.dispatchEvent(new Event("focusin", { bubbles: true, composed: true }));
    }
  }
  /**
   * Get the focusable element depending on the current focus mode.
   * It can be a row, a cell, or a focusable div inside a cell.
   *
   * @param {HTMLElement} row
   * @param {HTMLElement} cell
   * @return {HTMLElement}
   * @private
   */
  __getFocusable(row, cell) {
    return this.__rowFocusMode ? row : cell._focusButton || cell;
  }
  /**
   * Enables interaction mode if a cells descendant receives focus or keyboard
   * input. Disables it if the event is not related to cell content.
   * @param {!KeyboardEvent|!FocusEvent} e
   * @private
   */
  _detectInteracting(e17) {
    const isInteracting = e17.composedPath().some((el) => el.localName === "slot" && this.shadowRoot.contains(el));
    this._setInteracting(isInteracting);
    this.__updateHorizontalScrollPosition();
  }
  /**
   * Enables or disables the focus target of the containing section of the
   * grid from receiving focus, based on whether the user is interacting with
   * that section of the grid.
   * @param {HTMLElement} focusTarget
   * @private
   */
  _updateGridSectionFocusTarget(focusTarget) {
    if (!focusTarget) {
      return;
    }
    const section = this._getGridSectionFromFocusTarget(focusTarget);
    const isInteractingWithinActiveSection = this.interacting && section === this._activeRowGroup;
    focusTarget.tabIndex = isInteractingWithinActiveSection ? -1 : 0;
  }
  /** @protected */
  _preventScrollerRotatingCellFocus() {
    if (this._activeRowGroup !== this.$.items) {
      return;
    }
    this.__preventScrollerRotatingCellFocusDebouncer = Debouncer.debounce(
      this.__preventScrollerRotatingCellFocusDebouncer,
      animationFrame,
      () => {
        const isItemsRowGroupActive = this._activeRowGroup === this.$.items;
        const isFocusedItemRendered = this._getRenderedRows().some((row) => row.index === this._focusedItemIndex);
        if (isFocusedItemRendered) {
          this.__updateItemsFocusable();
          if (isItemsRowGroupActive && !this.__rowFocusMode) {
            this._focusedCell = this._itemsFocusable;
          }
          if (this._navigatingIsHidden) {
            this.toggleAttribute("navigating", true);
            this._navigatingIsHidden = false;
          }
        } else if (isItemsRowGroupActive) {
          this._focusedCell = null;
          if (this.hasAttribute("navigating")) {
            this._navigatingIsHidden = true;
            this.toggleAttribute("navigating", false);
          }
        }
      }
    );
  }
  /**
   * @param {HTMLTableSectionElement=} rowGroup
   * @param {number=} rowIndex
   * @return {!Array<!GridColumn>}
   * @protected
   */
  _getColumns(rowGroup, rowIndex) {
    let columnTreeLevel = this._columnTree.length - 1;
    if (rowGroup === this.$.header) {
      columnTreeLevel = rowIndex;
    } else if (rowGroup === this.$.footer) {
      columnTreeLevel = this._columnTree.length - 1 - rowIndex;
    }
    return this._columnTree[columnTreeLevel];
  }
  /** @private */
  __isValidFocusable(element) {
    return this.$.table.contains(element) && element.offsetHeight;
  }
  /** @protected */
  _resetKeyboardNavigation() {
    ["header", "footer"].forEach((section) => {
      if (!this.__isValidFocusable(this[`_${section}Focusable`])) {
        const firstVisibleRow = [...this.$[section].children].find((row) => row.offsetHeight);
        const firstVisibleCell = firstVisibleRow ? [...firstVisibleRow.children].find((cell) => !cell.hidden) : null;
        if (firstVisibleRow && firstVisibleCell) {
          this[`_${section}Focusable`] = this.__getFocusable(firstVisibleRow, firstVisibleCell);
        }
      }
    });
    if (!this.__isValidFocusable(this._itemsFocusable) && this.$.items.firstElementChild) {
      const firstVisibleRow = this.__getFirstVisibleItem();
      const firstVisibleCell = firstVisibleRow ? [...firstVisibleRow.children].find((cell) => !cell.hidden) : null;
      if (firstVisibleCell && firstVisibleRow) {
        this._focusedColumnOrder = void 0;
        this._itemsFocusable = this.__getFocusable(firstVisibleRow, firstVisibleCell);
      }
    } else {
      this.__updateItemsFocusable();
    }
  }
  /**
   * @param {!HTMLElement} dstCell
   * @protected
   */
  _scrollHorizontallyToCell(dstCell) {
    if (dstCell.hasAttribute("frozen") || dstCell.hasAttribute("frozen-to-end") || isDetailsCell(dstCell)) {
      return;
    }
    const dstCellRect = dstCell.getBoundingClientRect();
    const dstRow = dstCell.parentNode;
    const dstCellIndex = Array.from(dstRow.children).indexOf(dstCell);
    const tableRect = this.$.table.getBoundingClientRect();
    let leftBoundary = tableRect.left, rightBoundary = tableRect.right;
    for (let i10 = dstCellIndex - 1; i10 >= 0; i10--) {
      const cell = dstRow.children[i10];
      if (cell.hasAttribute("hidden") || isDetailsCell(cell)) {
        continue;
      }
      if (cell.hasAttribute("frozen") || cell.hasAttribute("frozen-to-end")) {
        leftBoundary = cell.getBoundingClientRect().right;
        break;
      }
    }
    for (let i10 = dstCellIndex + 1; i10 < dstRow.children.length; i10++) {
      const cell = dstRow.children[i10];
      if (cell.hasAttribute("hidden") || isDetailsCell(cell)) {
        continue;
      }
      if (cell.hasAttribute("frozen") || cell.hasAttribute("frozen-to-end")) {
        rightBoundary = cell.getBoundingClientRect().left;
        break;
      }
    }
    if (dstCellRect.left < leftBoundary) {
      this.$.table.scrollLeft += Math.round(dstCellRect.left - leftBoundary);
    }
    if (dstCellRect.right > rightBoundary) {
      this.$.table.scrollLeft += Math.round(dstCellRect.right - rightBoundary);
    }
  }
  /**
   * @typedef {Object} GridEventLocation
   * @property {HTMLTableSectionElement | null} section - The table section element that the event occurred in (header, body, or footer), or null if the event did not occur in a section
   * @property {HTMLTableRowElement | null} row - The row element that the event occurred in, or null if the event did not occur in a row
   * @property {HTMLTableCellElement | null} cell - The cell element that the event occurred in, or null if the event did not occur in a cell
   * @private
   */
  /**
   * Takes an event and returns a location object describing in which part of the grid the event occurred.
   * The event may either target table section, a row, a cell or contents of a cell.
   * @param {Event} e
   * @returns {GridEventLocation}
   * @protected
   */
  _getGridEventLocation(e17) {
    const path = e17.__composedPath || e17.composedPath();
    const tableIndex = path.indexOf(this.$.table);
    const section = tableIndex >= 1 ? path[tableIndex - 1] : null;
    const row = tableIndex >= 2 ? path[tableIndex - 2] : null;
    const cell = tableIndex >= 3 ? path[tableIndex - 3] : null;
    return {
      section,
      row,
      cell
    };
  }
  /**
   * Helper method that maps a focus target cell to the containing grid section
   * @param {HTMLElement} focusTarget
   * @returns {HTMLTableSectionElement | null}
   * @private
   */
  _getGridSectionFromFocusTarget(focusTarget) {
    if (focusTarget === this._headerFocusable) {
      return this.$.header;
    }
    if (focusTarget === this._itemsFocusable) {
      return this.$.items;
    }
    if (focusTarget === this._footerFocusable) {
      return this.$.footer;
    }
    return null;
  }
  /**
   * Fired when a cell is focused with click or keyboard navigation.
   *
   * Use context property of @see {@link GridCellFocusEvent} to get detail information about the event.
   *
   * @event cell-focus
   */
};

// node_modules/@vaadin/grid/src/vaadin-grid-row-details-mixin.js
var RowDetailsMixin = (superClass) => class RowDetailsMixin extends superClass {
  static get properties() {
    return {
      /**
       * An array containing references to items with open row details.
       * @type {!Array<!GridItem>}
       */
      detailsOpenedItems: {
        type: Array,
        value: () => [],
        sync: true
      },
      /**
       * Custom function for rendering the content of the row details.
       * Receives three arguments:
       *
       * - `root` The row details content DOM element. Append your content to it.
       * - `grid` The `<vaadin-grid>` element.
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.level` The number of the item's tree sublevel, starts from 0.
       *   - `model.expanded` True if the item's tree sublevel is expanded.
       *   - `model.selected` True if the item is selected.
       *
       * @type {GridRowDetailsRenderer | null | undefined}
       */
      rowDetailsRenderer: {
        type: Function,
        sync: true
      },
      /**
       * @type {!Array<!HTMLElement> | undefined}
       * @protected
       */
      _detailsCells: {
        type: Array
      }
    };
  }
  static get observers() {
    return [
      "_detailsOpenedItemsChanged(detailsOpenedItems, rowDetailsRenderer)",
      "_rowDetailsRendererChanged(rowDetailsRenderer)"
    ];
  }
  /** @protected */
  ready() {
    super.ready();
    this._detailsCellResizeObserver = new ResizeObserver((entries) => {
      entries.forEach(({ target: cell }) => {
        this._updateDetailsCellHeight(cell.parentElement);
      });
      this.__virtualizer.__adapter._resizeHandler();
    });
  }
  /** @private */
  _rowDetailsRendererChanged(rowDetailsRenderer) {
    if (!rowDetailsRenderer) {
      return;
    }
    if (this._columnTree) {
      iterateChildren(this.$.items, (row) => {
        if (!row.querySelector("[part~=details-cell]")) {
          this._updateRow(row, this._columnTree[this._columnTree.length - 1]);
          const isDetailsOpened = this._isDetailsOpened(row._item);
          this._toggleDetailsCell(row, isDetailsOpened);
        }
      });
    }
  }
  /** @private */
  _detailsOpenedItemsChanged(_detailsOpenedItems, rowDetailsRenderer) {
    iterateChildren(this.$.items, (row) => {
      if (row.hasAttribute("details-opened")) {
        this._updateItem(row, row._item);
        return;
      }
      if (rowDetailsRenderer && this._isDetailsOpened(row._item)) {
        this._updateItem(row, row._item);
      }
    });
  }
  /**
   * @param {!HTMLElement} cell
   * @protected
   */
  _configureDetailsCell(cell) {
    cell.setAttribute("part", "cell details-cell");
    cell.toggleAttribute("frozen", true);
    this._detailsCellResizeObserver.observe(cell);
  }
  /**
   * @param {!HTMLElement} row
   * @param {!GridItem} item
   * @protected
   */
  _toggleDetailsCell(row, detailsOpened) {
    const cell = row.querySelector('[part~="details-cell"]');
    if (!cell) {
      return;
    }
    cell.hidden = !detailsOpened;
    if (cell.hidden) {
      return;
    }
    if (this.rowDetailsRenderer) {
      cell._renderer = this.rowDetailsRenderer;
    }
  }
  /** @protected */
  _updateDetailsCellHeight(row) {
    const cell = row.querySelector('[part~="details-cell"]');
    if (!cell) {
      return;
    }
    this.__updateDetailsRowPadding(row, cell);
    requestAnimationFrame(() => this.__updateDetailsRowPadding(row, cell));
  }
  /** @private */
  __updateDetailsRowPadding(row, cell) {
    if (cell.hidden) {
      row.style.removeProperty("padding-bottom");
    } else {
      row.style.setProperty("padding-bottom", `${cell.offsetHeight}px`);
    }
  }
  /** @protected */
  _updateDetailsCellHeights() {
    iterateChildren(this.$.items, (row) => {
      this._updateDetailsCellHeight(row);
    });
  }
  /**
   * @param {!GridItem} item
   * @return {boolean}
   * @protected
   */
  _isDetailsOpened(item) {
    return this.detailsOpenedItems && this._getItemIndexInArray(item, this.detailsOpenedItems) !== -1;
  }
  /**
   * Open the details row of a given item.
   * @param {!GridItem} item
   */
  openItemDetails(item) {
    if (!this._isDetailsOpened(item)) {
      this.detailsOpenedItems = [...this.detailsOpenedItems, item];
    }
  }
  /**
   * Close the details row of a given item.
   * @param {!GridItem} item
   */
  closeItemDetails(item) {
    if (this._isDetailsOpened(item)) {
      this.detailsOpenedItems = this.detailsOpenedItems.filter((i10) => !this._itemsEqual(i10, item));
    }
  }
};

// node_modules/@vaadin/a11y-base/src/announce.js
var region = document.createElement("div");
region.style.position = "fixed";
region.style.clip = "rect(0px, 0px, 0px, 0px)";
region.setAttribute("aria-live", "polite");
document.body.appendChild(region);
var alertDebouncer;
function announce(text, options = {}) {
  const mode = options.mode || "polite";
  const timeout = options.timeout === void 0 ? 150 : options.timeout;
  if (mode === "alert") {
    region.removeAttribute("aria-live");
    region.removeAttribute("role");
    alertDebouncer = Debouncer.debounce(alertDebouncer, animationFrame, () => {
      region.setAttribute("role", "alert");
    });
  } else {
    if (alertDebouncer) {
      alertDebouncer.cancel();
    }
    region.removeAttribute("role");
    region.setAttribute("aria-live", mode);
  }
  region.textContent = "";
  setTimeout(() => {
    region.textContent = text;
  }, timeout);
}

// node_modules/@vaadin/a11y-base/src/keyboard-mixin.js
var KeyboardMixin = dedupeMixin(
  (superclass) => class KeyboardMixinClass extends superclass {
    /** @protected */
    ready() {
      super.ready();
      this.addEventListener("keydown", (event) => {
        this._onKeyDown(event);
      });
      this.addEventListener("keyup", (event) => {
        this._onKeyUp(event);
      });
    }
    /**
     * A handler for the `keydown` event. By default, it calls
     * separate methods for handling "Enter" and "Escape" keys.
     * Override the method to implement your own behavior.
     *
     * @param {KeyboardEvent} event
     * @protected
     */
    _onKeyDown(event) {
      switch (event.key) {
        case "Enter":
          this._onEnter(event);
          break;
        case "Escape":
          this._onEscape(event);
          break;
        default:
          break;
      }
    }
    /**
     * A handler for the `keyup` event. By default, it does nothing.
     * Override the method to implement your own behavior.
     *
     * @param {KeyboardEvent} _event
     * @protected
     */
    _onKeyUp(_event) {
    }
    /**
     * A handler for the "Enter" key. By default, it does nothing.
     * Override the method to implement your own behavior.
     *
     * @param {KeyboardEvent} _event
     * @protected
     */
    _onEnter(_event) {
    }
    /**
     * A handler for the "Escape" key. By default, it does nothing.
     * Override the method to implement your own behavior.
     *
     * @param {KeyboardEvent} _event
     * @protected
     */
    _onEscape(_event) {
    }
  }
);

// node_modules/@vaadin/a11y-base/src/active-mixin.js
var ActiveMixin = (superclass) => class ActiveMixinClass extends DisabledMixin(KeyboardMixin(superclass)) {
  /**
   * An array of activation keys.
   *
   * See possible values here:
   * https://developer.mozilla.org/ru/docs/Web/API/KeyboardEvent/key/Key_Values
   *
   * @protected
   * @return {!Array<!string>}
   */
  get _activeKeys() {
    return [" "];
  }
  /** @protected */
  ready() {
    super.ready();
    addListener(this, "down", (event) => {
      if (this._shouldSetActive(event)) {
        this._setActive(true);
      }
    });
    addListener(this, "up", () => {
      this._setActive(false);
    });
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    this._setActive(false);
  }
  /**
   * @param {KeyboardEvent | MouseEvent} _event
   * @protected
   */
  _shouldSetActive(_event) {
    return !this.disabled;
  }
  /**
   * Sets the `active` attribute on the element if an activation key is pressed.
   *
   * @param {KeyboardEvent} event
   * @protected
   * @override
   */
  _onKeyDown(event) {
    super._onKeyDown(event);
    if (this._shouldSetActive(event) && this._activeKeys.includes(event.key)) {
      this._setActive(true);
      document.addEventListener(
        "keyup",
        (e17) => {
          if (this._activeKeys.includes(e17.key)) {
            this._setActive(false);
          }
        },
        { once: true }
      );
    }
  }
  /**
   * Toggles the `active` attribute on the element.
   *
   * @param {boolean} active
   * @protected
   */
  _setActive(active) {
    this.toggleAttribute("active", active);
  }
};

// node_modules/@vaadin/a11y-base/src/focus-mixin.js
var FocusMixin = dedupeMixin(
  (superclass) => class FocusMixinClass extends superclass {
    /**
     * @protected
     * @return {boolean}
     */
    get _keyboardActive() {
      return isKeyboardActive();
    }
    /** @protected */
    ready() {
      this.addEventListener("focusin", (e17) => {
        if (this._shouldSetFocus(e17)) {
          this._setFocused(true);
        }
      });
      this.addEventListener("focusout", (e17) => {
        if (this._shouldRemoveFocus(e17)) {
          this._setFocused(false);
        }
      });
      super.ready();
    }
    /** @protected */
    disconnectedCallback() {
      super.disconnectedCallback();
      if (this.hasAttribute("focused")) {
        this._setFocused(false);
      }
    }
    /**
     * @param {FocusOptions=} options
     * @protected
     * @override
     */
    focus(options) {
      super.focus(options);
      if (!(options && options.focusVisible === false)) {
        this.setAttribute("focus-ring", "");
      }
    }
    /**
     * Override to change how focused and focus-ring attributes are set.
     *
     * @param {boolean} focused
     * @protected
     */
    _setFocused(focused) {
      this.toggleAttribute("focused", focused);
      this.toggleAttribute("focus-ring", focused && this._keyboardActive);
    }
    /**
     * Override to define if the field receives focus based on the event.
     *
     * @param {FocusEvent} _event
     * @return {boolean}
     * @protected
     */
    _shouldSetFocus(_event) {
      return true;
    }
    /**
     * Override to define if the field loses focus based on the event.
     *
     * @param {FocusEvent} _event
     * @return {boolean}
     * @protected
     */
    _shouldRemoveFocus(_event) {
      return true;
    }
  }
);

// node_modules/@vaadin/a11y-base/src/delegate-focus-mixin.js
var DelegateFocusMixin = dedupeMixin(
  (superclass) => class DelegateFocusMixinClass extends FocusMixin(TabindexMixin(superclass)) {
    static get properties() {
      return {
        /**
         * Specify that this control should have input focus when the page loads.
         */
        autofocus: {
          type: Boolean
        },
        /**
         * A reference to the focusable element controlled by the mixin.
         * It can be an input, textarea, button or any element with tabindex > -1.
         *
         * Any component implementing this mixin is expected to provide it
         * by using `this._setFocusElement(input)` Polymer API.
         *
         * Toggling `tabindex` attribute on the host element propagates its value to `focusElement`.
         *
         * @protected
         * @type {!HTMLElement}
         */
        focusElement: {
          type: Object,
          readOnly: true,
          observer: "_focusElementChanged",
          sync: true
        },
        /**
         * Override the property from `TabIndexMixin`
         * to ensure the `tabindex` attribute of the focus element
         * will be restored to `0` after re-enabling the element.
         *
         * @protected
         * @override
         */
        _lastTabIndex: {
          value: 0
        }
      };
    }
    constructor() {
      super();
      this._boundOnBlur = this._onBlur.bind(this);
      this._boundOnFocus = this._onFocus.bind(this);
    }
    /** @protected */
    ready() {
      super.ready();
      if (this.autofocus && !this.disabled) {
        requestAnimationFrame(() => {
          this.focus();
        });
      }
    }
    /**
     * @param {FocusOptions=} options
     * @protected
     * @override
     */
    focus(options) {
      if (this.focusElement && !this.disabled) {
        this.focusElement.focus();
        if (!(options && options.focusVisible === false)) {
          this.setAttribute("focus-ring", "");
        }
      }
    }
    /**
     * @protected
     * @override
     */
    blur() {
      if (this.focusElement) {
        this.focusElement.blur();
      }
    }
    /**
     * @protected
     * @override
     */
    click() {
      if (this.focusElement && !this.disabled) {
        this.focusElement.click();
      }
    }
    /** @protected */
    _focusElementChanged(element, oldElement) {
      if (element) {
        element.disabled = this.disabled;
        this._addFocusListeners(element);
        this.__forwardTabIndex(this.tabindex);
      } else if (oldElement) {
        this._removeFocusListeners(oldElement);
      }
    }
    /**
     * @param {HTMLElement} element
     * @protected
     */
    _addFocusListeners(element) {
      element.addEventListener("blur", this._boundOnBlur);
      element.addEventListener("focus", this._boundOnFocus);
    }
    /**
     * @param {HTMLElement} element
     * @protected
     */
    _removeFocusListeners(element) {
      element.removeEventListener("blur", this._boundOnBlur);
      element.removeEventListener("focus", this._boundOnFocus);
    }
    /**
     * Focus event does not bubble, so we dispatch it manually
     * on the host element to support adding focus listeners
     * when the focusable element is placed in light DOM.
     * @param {FocusEvent} event
     * @protected
     */
    _onFocus(event) {
      event.stopPropagation();
      this.dispatchEvent(new Event("focus"));
    }
    /**
     * Blur event does not bubble, so we dispatch it manually
     * on the host element to support adding blur listeners
     * when the focusable element is placed in light DOM.
     * @param {FocusEvent} event
     * @protected
     */
    _onBlur(event) {
      event.stopPropagation();
      this.dispatchEvent(new Event("blur"));
    }
    /**
     * @param {FocusEvent} event
     * @return {boolean}
     * @protected
     * @override
     */
    _shouldSetFocus(event) {
      return event.target === this.focusElement;
    }
    /**
     * @param {FocusEvent} event
     * @return {boolean}
     * @protected
     * @override
     */
    _shouldRemoveFocus(event) {
      return event.target === this.focusElement;
    }
    /**
     * @param {boolean} disabled
     * @param {boolean} oldDisabled
     * @protected
     * @override
     */
    _disabledChanged(disabled, oldDisabled) {
      super._disabledChanged(disabled, oldDisabled);
      if (this.focusElement) {
        this.focusElement.disabled = disabled;
      }
      if (disabled) {
        this.blur();
      }
    }
    /**
     * Override an observer from `TabindexMixin`.
     * Do not call super to remove tabindex attribute
     * from the host after it has been forwarded.
     * @param {string} tabindex
     * @protected
     * @override
     */
    _tabindexChanged(tabindex) {
      this.__forwardTabIndex(tabindex);
    }
    /** @private */
    __forwardTabIndex(tabindex) {
      if (tabindex !== void 0 && this.focusElement) {
        this.focusElement.tabIndex = tabindex;
        if (tabindex !== -1) {
          this.tabindex = void 0;
        }
      }
      if (this.disabled && tabindex) {
        if (tabindex !== -1) {
          this._lastTabIndex = tabindex;
        }
        this.tabindex = void 0;
      }
      if (tabindex === void 0 && this.hasAttribute("tabindex")) {
        this.removeAttribute("tabindex");
      }
    }
  }
);

// node_modules/@vaadin/a11y-base/src/aria-id-reference.js
var attributeToTargets = /* @__PURE__ */ new Map();
function getAttrMap(attr) {
  if (!attributeToTargets.has(attr)) {
    attributeToTargets.set(attr, /* @__PURE__ */ new WeakMap());
  }
  return attributeToTargets.get(attr);
}
function cleanAriaIDReference(target, attr) {
  if (!target) {
    return;
  }
  target.removeAttribute(attr);
}
function storeAriaIDReference(target, attr) {
  if (!target || !attr) {
    return;
  }
  const attributeMap = getAttrMap(attr);
  if (attributeMap.has(target)) {
    return;
  }
  const values = deserializeAttributeValue(target.getAttribute(attr));
  attributeMap.set(target, new Set(values));
}
function restoreGeneratedAriaIDReference(target, attr) {
  if (!target || !attr) {
    return;
  }
  const attributeMap = getAttrMap(attr);
  const values = attributeMap.get(target);
  if (!values || values.size === 0) {
    target.removeAttribute(attr);
  } else {
    addValueToAttribute(target, attr, serializeAttributeValue(values));
  }
  attributeMap.delete(target);
}
function setAriaIDReference(target, attr, config = { newId: null, oldId: null, fromUser: false }) {
  if (!target || !attr) {
    return;
  }
  const { newId, oldId, fromUser } = config;
  const attributeMap = getAttrMap(attr);
  const storedValues = attributeMap.get(target);
  if (!fromUser && !!storedValues) {
    oldId && storedValues.delete(oldId);
    newId && storedValues.add(newId);
    return;
  }
  if (fromUser) {
    if (!storedValues) {
      storeAriaIDReference(target, attr);
    } else if (!newId) {
      attributeMap.delete(target);
    }
    cleanAriaIDReference(target, attr);
  }
  removeValueFromAttribute(target, attr, oldId);
  const attributeValue = !newId ? serializeAttributeValue(storedValues) : newId;
  if (attributeValue) {
    addValueToAttribute(target, attr, attributeValue);
  }
}
function removeAriaIDReference(target, attr) {
  storeAriaIDReference(target, attr);
  cleanAriaIDReference(target, attr);
}

// node_modules/@vaadin/a11y-base/src/field-aria-controller.js
var FieldAriaController = class {
  constructor(host) {
    this.host = host;
    this.__required = false;
  }
  /**
   * Sets a target element to which ARIA attributes are added.
   *
   * @param {HTMLElement} target
   */
  setTarget(target) {
    this.__target = target;
    this.__setAriaRequiredAttribute(this.__required);
    this.__setLabelIdToAriaAttribute(this.__labelId, this.__labelId);
    if (this.__labelIdFromUser != null) {
      this.__setLabelIdToAriaAttribute(this.__labelIdFromUser, this.__labelIdFromUser, true);
    }
    this.__setErrorIdToAriaAttribute(this.__errorId);
    this.__setHelperIdToAriaAttribute(this.__helperId);
    this.setAriaLabel(this.__label);
  }
  /**
   * Toggles the `aria-required` attribute on the target element
   * if the target is the host component (e.g. a field group).
   * Otherwise, it does nothing.
   *
   * @param {boolean} required
   */
  setRequired(required) {
    this.__setAriaRequiredAttribute(required);
    this.__required = required;
  }
  /**
   * Defines the `aria-label` attribute of the target element.
   *
   * To remove the attribute, pass `null` as `label`.
   *
   * @param {string | null | undefined} label
   */
  setAriaLabel(label) {
    this.__setAriaLabelToAttribute(label);
    this.__label = label;
  }
  /**
   * Links the target element with a slotted label element
   * via the target's attribute `aria-labelledby`.
   *
   * To unlink the previous slotted label element, pass `null` as `labelId`.
   *
   * @param {string | null} labelId
   */
  setLabelId(labelId, fromUser = false) {
    const oldLabelId = fromUser ? this.__labelIdFromUser : this.__labelId;
    this.__setLabelIdToAriaAttribute(labelId, oldLabelId, fromUser);
    if (fromUser) {
      this.__labelIdFromUser = labelId;
    } else {
      this.__labelId = labelId;
    }
  }
  /**
   * Links the target element with a slotted error element via the target's attribute:
   * - `aria-labelledby` if the target is the host component (e.g a field group).
   * - `aria-describedby` otherwise.
   *
   * To unlink the previous slotted error element, pass `null` as `errorId`.
   *
   * @param {string | null} errorId
   */
  setErrorId(errorId) {
    this.__setErrorIdToAriaAttribute(errorId, this.__errorId);
    this.__errorId = errorId;
  }
  /**
   * Links the target element with a slotted helper element via the target's attribute:
   * - `aria-labelledby` if the target is the host component (e.g a field group).
   * - `aria-describedby` otherwise.
   *
   * To unlink the previous slotted helper element, pass `null` as `helperId`.
   *
   * @param {string | null} helperId
   */
  setHelperId(helperId) {
    this.__setHelperIdToAriaAttribute(helperId, this.__helperId);
    this.__helperId = helperId;
  }
  /**
   * @param {string | null | undefined} label
   * @private
   * */
  __setAriaLabelToAttribute(label) {
    if (!this.__target) {
      return;
    }
    if (label) {
      removeAriaIDReference(this.__target, "aria-labelledby");
      this.__target.setAttribute("aria-label", label);
    } else if (this.__label) {
      restoreGeneratedAriaIDReference(this.__target, "aria-labelledby");
      this.__target.removeAttribute("aria-label");
    }
  }
  /**
   * @param {string | null | undefined} labelId
   * @param {string | null | undefined} oldLabelId
   * @param {boolean | null | undefined} fromUser
   * @private
   */
  __setLabelIdToAriaAttribute(labelId, oldLabelId, fromUser) {
    setAriaIDReference(this.__target, "aria-labelledby", { newId: labelId, oldId: oldLabelId, fromUser });
  }
  /**
   * @param {string | null | undefined} errorId
   * @param {string | null | undefined} oldErrorId
   * @private
   */
  __setErrorIdToAriaAttribute(errorId, oldErrorId) {
    setAriaIDReference(this.__target, "aria-describedby", { newId: errorId, oldId: oldErrorId, fromUser: false });
  }
  /**
   * @param {string | null | undefined} helperId
   * @param {string | null | undefined} oldHelperId
   * @private
   */
  __setHelperIdToAriaAttribute(helperId, oldHelperId) {
    setAriaIDReference(this.__target, "aria-describedby", { newId: helperId, oldId: oldHelperId, fromUser: false });
  }
  /**
   * @param {boolean} required
   * @private
   */
  __setAriaRequiredAttribute(required) {
    if (!this.__target) {
      return;
    }
    if (["input", "textarea"].includes(this.__target.localName)) {
      return;
    }
    if (required) {
      this.__target.setAttribute("aria-required", "true");
    } else {
      this.__target.removeAttribute("aria-required");
    }
  }
};

// node_modules/@vaadin/component-base/src/dir-utils.js
function getNormalizedScrollLeft(element, direction) {
  const { scrollLeft } = element;
  if (direction !== "rtl") {
    return scrollLeft;
  }
  return element.scrollWidth - element.clientWidth + scrollLeft;
}

// node_modules/@vaadin/component-base/src/resize-mixin.js
var observer = new ResizeObserver((entries) => {
  setTimeout(() => {
    entries.forEach((entry) => {
      if (!entry.target.isConnected) {
        return;
      }
      if (entry.target.resizables) {
        entry.target.resizables.forEach((resizable) => {
          resizable._onResize(entry.contentRect);
        });
      } else {
        entry.target._onResize(entry.contentRect);
      }
    });
  });
});
var ResizeMixin = dedupeMixin(
  (superclass) => class ResizeMixinClass extends superclass {
    /**
     * When true, the parent element resize will be also observed.
     * Override this getter and return `true` to enable this.
     *
     * @protected
     */
    get _observeParent() {
      return false;
    }
    /** @protected */
    connectedCallback() {
      super.connectedCallback();
      observer.observe(this);
      if (this._observeParent) {
        const parent = this.parentNode instanceof ShadowRoot ? this.parentNode.host : this.parentNode;
        if (!parent.resizables) {
          parent.resizables = /* @__PURE__ */ new Set();
          observer.observe(parent);
        }
        parent.resizables.add(this);
        this.__parent = parent;
      }
    }
    /** @protected */
    disconnectedCallback() {
      super.disconnectedCallback();
      observer.unobserve(this);
      const parent = this.__parent;
      if (this._observeParent && parent) {
        const resizables = parent.resizables;
        if (resizables) {
          resizables.delete(this);
          if (resizables.size === 0) {
            observer.unobserve(parent);
          }
        }
        this.__parent = null;
      }
    }
    /**
     * A handler invoked on host resize. By default, it does nothing.
     * Override the method to implement your own behavior.
     *
     * @protected
     */
    _onResize(_contentRect) {
    }
  }
);

// node_modules/@vaadin/grid/src/vaadin-grid-scroll-mixin.js
var timeouts = {
  SCROLLING: 500,
  UPDATE_CONTENT_VISIBILITY: 100
};
var ScrollMixin = (superClass) => class ScrollMixin extends ResizeMixin(superClass) {
  static get properties() {
    return {
      /**
       * Allows you to choose between modes for rendering columns in the grid:
       *
       * "eager" (default): All columns are rendered upfront, regardless of their visibility within the viewport.
       * This mode should generally be preferred, as it avoids the limitations imposed by the "lazy" mode.
       * Use this mode unless the grid has a large number of columns and performance outweighs the limitations
       * in priority.
       *
       * "lazy": Optimizes the rendering of cells when there are multiple columns in the grid by virtualizing
       * horizontal scrolling. In this mode, body cells are rendered only when their corresponding columns are
       * inside the visible viewport.
       *
       * Using "lazy" rendering should be used only if you're dealing with a large number of columns and performance
       * is your highest priority. For most use cases, the default "eager" mode is recommended due to the
       * limitations imposed by the "lazy" mode.
       *
       * When using the "lazy" mode, keep the following limitations in mind:
       *
       * - Row Height: When only a number of columns are visible at once, the height of a row can only be that of
       * the highest cell currently visible on that row. Make sure each cell on a single row has the same height
       * as all other cells on that row. If row cells have different heights, users may experience jumpiness when
       * scrolling the grid horizontally as lazily rendered cells with different heights are scrolled into view.
       *
       * - Auto-width Columns: For the columns that are initially outside the visible viewport but still use auto-width,
       * only the header content is taken into account when calculating the column width because the body cells
       * of the columns outside the viewport are not initially rendered.
       *
       * - Screen Reader Compatibility: Screen readers may not be able to associate the focused cells with the correct
       * headers when only a subset of the body cells on a row is rendered.
       *
       * - Keyboard Navigation: Tabbing through focusable elements inside the grid body may not work as expected because
       * some of the columns that would include focusable elements in the body cells may be outside the visible viewport
       * and thus not rendered.
       *
       * @attr {eager|lazy} column-rendering
       * @type {!ColumnRendering}
       */
      columnRendering: {
        type: String,
        value: "eager",
        sync: true
      },
      /**
       * Cached array of frozen cells
       * @private
       */
      _frozenCells: {
        type: Array,
        value: () => []
      },
      /**
       * Cached array of cells frozen to end
       * @private
       */
      _frozenToEndCells: {
        type: Array,
        value: () => []
      }
    };
  }
  static get observers() {
    return ["__columnRenderingChanged(_columnTree, columnRendering)"];
  }
  /** @private */
  get _scrollLeft() {
    return this.$.table.scrollLeft;
  }
  /** @private */
  get _scrollTop() {
    return this.$.table.scrollTop;
  }
  /**
   * Override (from iron-scroll-target-behavior) to avoid document scroll
   * @private
   */
  set _scrollTop(top) {
    this.$.table.scrollTop = top;
  }
  /** @protected */
  get _lazyColumns() {
    return this.columnRendering === "lazy";
  }
  /** @protected */
  ready() {
    super.ready();
    this.scrollTarget = this.$.table;
    this.$.items.addEventListener("focusin", (e17) => {
      const composedPath = e17.composedPath();
      const row = composedPath[composedPath.indexOf(this.$.items) - 1];
      if (row) {
        if (!this._isMousedown) {
          const tableHeight = this.$.table.clientHeight;
          const headerHeight = this.$.header.clientHeight;
          const footerHeight = this.$.footer.clientHeight;
          const viewportHeight = tableHeight - headerHeight - footerHeight;
          const isRowLargerThanViewport = row.clientHeight > viewportHeight;
          const scrollTarget = isRowLargerThanViewport ? e17.target : row;
          this.__scrollIntoViewport(scrollTarget);
        }
        if (!this.$.table.contains(e17.relatedTarget)) {
          this.$.table.dispatchEvent(new CustomEvent("virtualizer-element-focused", { detail: { element: row } }));
        }
      }
    });
    this.$.table.addEventListener("scroll", () => this._afterScroll());
  }
  /**
   * @protected
   * @override
   */
  _onResize() {
    this._updateOverflow();
    this.__updateHorizontalScrollPosition();
    if (this._firefox) {
      const isVisible = !isElementHidden(this);
      if (isVisible && this.__previousVisible === false) {
        this._scrollTop = this.__memorizedScrollTop || 0;
      }
      this.__previousVisible = isVisible;
    }
  }
  /**
   * Scroll to a flat index in the grid. The method doesn't take into account
   * the hierarchy of the items.
   *
   * @param {number} index Row index to scroll to
   * @protected
   */
  _scrollToFlatIndex(index) {
    index = Math.min(this._flatSize - 1, Math.max(0, index));
    this.__virtualizer.scrollToIndex(index);
    const rowElement = [...this.$.items.children].find((child) => child.index === index);
    this.__scrollIntoViewport(rowElement);
  }
  /**
   * Makes sure the given element is fully inside the visible viewport,
   * taking header/footer into account.
   * @private
   */
  __scrollIntoViewport(element) {
    if (!element) {
      return;
    }
    const dstRect = element.getBoundingClientRect();
    const footerTop = this.$.footer.getBoundingClientRect().top;
    const headerBottom = this.$.header.getBoundingClientRect().bottom;
    if (dstRect.bottom > footerTop) {
      this.$.table.scrollTop += dstRect.bottom - footerTop;
    } else if (dstRect.top < headerBottom) {
      this.$.table.scrollTop -= headerBottom - dstRect.top;
    }
  }
  /** @private */
  _scheduleScrolling() {
    if (!this._scrollingFrame) {
      this._scrollingFrame = requestAnimationFrame(() => this.$.scroller.toggleAttribute("scrolling", true));
    }
    this._debounceScrolling = Debouncer.debounce(this._debounceScrolling, timeOut.after(timeouts.SCROLLING), () => {
      cancelAnimationFrame(this._scrollingFrame);
      delete this._scrollingFrame;
      this.$.scroller.toggleAttribute("scrolling", false);
    });
  }
  /** @private */
  _afterScroll() {
    this.__updateHorizontalScrollPosition();
    if (!this.hasAttribute("reordering")) {
      this._scheduleScrolling();
    }
    if (!this.hasAttribute("navigating")) {
      this._hideTooltip(true);
    }
    this._updateOverflow();
    this._debounceColumnContentVisibility = Debouncer.debounce(
      this._debounceColumnContentVisibility,
      timeOut.after(timeouts.UPDATE_CONTENT_VISIBILITY),
      () => {
        if (this._lazyColumns && this.__cachedScrollLeft !== this._scrollLeft) {
          this.__cachedScrollLeft = this._scrollLeft;
          this.__updateColumnsBodyContentHidden();
        }
      }
    );
    if (this._firefox) {
      const isVisible = !isElementHidden(this);
      if (isVisible && this.__previousVisible !== false) {
        this.__memorizedScrollTop = this._scrollTop;
      }
    }
  }
  /** @private */
  __updateColumnsBodyContentHidden() {
    if (!this._columnTree || !this._areSizerCellsAssigned()) {
      return;
    }
    const columnsInOrder = this._getColumnsInOrder();
    let bodyContentHiddenChanged = false;
    columnsInOrder.forEach((column) => {
      const bodyContentHidden = this._lazyColumns && !this.__isColumnInViewport(column);
      if (column._bodyContentHidden !== bodyContentHidden) {
        bodyContentHiddenChanged = true;
        column._cells.forEach((cell) => {
          if (cell !== column._sizerCell) {
            if (bodyContentHidden) {
              cell.remove();
            } else if (cell.__parentRow) {
              const followingColumnCell = [...cell.__parentRow.children].find(
                (child) => columnsInOrder.indexOf(child._column) > columnsInOrder.indexOf(column)
              );
              cell.__parentRow.insertBefore(cell, followingColumnCell);
            }
          }
        });
      }
      column._bodyContentHidden = bodyContentHidden;
    });
    if (bodyContentHiddenChanged) {
      this._frozenCellsChanged();
    }
    if (this._lazyColumns) {
      const lastFrozenColumn = [...columnsInOrder].reverse().find((column) => column.frozen);
      const lastFrozenColumnEnd = this.__getColumnEnd(lastFrozenColumn);
      const firstVisibleColumn = columnsInOrder.find((column) => !column.frozen && !column._bodyContentHidden);
      this.__lazyColumnsStart = this.__getColumnStart(firstVisibleColumn) - lastFrozenColumnEnd;
      this.$.items.style.setProperty("--_grid-lazy-columns-start", `${this.__lazyColumnsStart}px`);
      this._resetKeyboardNavigation();
    }
  }
  /** @private */
  __getColumnEnd(column) {
    if (!column) {
      return this.__isRTL ? this.$.table.clientWidth : 0;
    }
    return column._sizerCell.offsetLeft + (this.__isRTL ? 0 : column._sizerCell.offsetWidth);
  }
  /** @private */
  __getColumnStart(column) {
    if (!column) {
      return this.__isRTL ? this.$.table.clientWidth : 0;
    }
    return column._sizerCell.offsetLeft + (this.__isRTL ? column._sizerCell.offsetWidth : 0);
  }
  /**
   * Returns true if the given column is horizontally inside the viewport.
   * @private
   */
  __isColumnInViewport(column) {
    if (column.frozen || column.frozenToEnd) {
      return true;
    }
    return this.__isHorizontallyInViewport(column._sizerCell);
  }
  /** @private */
  __isHorizontallyInViewport(element) {
    return element.offsetLeft + element.offsetWidth >= this._scrollLeft && element.offsetLeft <= this._scrollLeft + this.clientWidth;
  }
  /** @private */
  __columnRenderingChanged(_columnTree, columnRendering) {
    if (columnRendering === "eager") {
      this.$.scroller.removeAttribute("column-rendering");
    } else {
      this.$.scroller.setAttribute("column-rendering", columnRendering);
    }
    this.__updateColumnsBodyContentHidden();
  }
  /** @private */
  _updateOverflow() {
    this._debounceOverflow = Debouncer.debounce(this._debounceOverflow, animationFrame, () => {
      this.__doUpdateOverflow();
    });
  }
  /** @private */
  __doUpdateOverflow() {
    let overflow = "";
    const table = this.$.table;
    if (table.scrollTop < table.scrollHeight - table.clientHeight) {
      overflow += " bottom";
    }
    if (table.scrollTop > 0) {
      overflow += " top";
    }
    const scrollLeft = getNormalizedScrollLeft(table, this.getAttribute("dir"));
    if (scrollLeft > 0) {
      overflow += " start";
    }
    if (scrollLeft < table.scrollWidth - table.clientWidth) {
      overflow += " end";
    }
    if (this.__isRTL) {
      overflow = overflow.replace(/start|end/giu, (matched) => {
        return matched === "start" ? "end" : "start";
      });
    }
    if (table.scrollLeft < table.scrollWidth - table.clientWidth) {
      overflow += " right";
    }
    if (table.scrollLeft > 0) {
      overflow += " left";
    }
    const value = overflow.trim();
    if (value.length > 0 && this.getAttribute("overflow") !== value) {
      this.setAttribute("overflow", value);
    } else if (value.length === 0 && this.hasAttribute("overflow")) {
      this.removeAttribute("overflow");
    }
  }
  /** @protected */
  _frozenCellsChanged() {
    this._debouncerCacheElements = Debouncer.debounce(this._debouncerCacheElements, microTask2, () => {
      Array.from(this.shadowRoot.querySelectorAll('[part~="cell"]')).forEach((cell) => {
        cell.style.transform = "";
      });
      this._frozenCells = Array.prototype.slice.call(this.$.table.querySelectorAll("[frozen]"));
      this._frozenToEndCells = Array.prototype.slice.call(this.$.table.querySelectorAll("[frozen-to-end]"));
      this.__updateHorizontalScrollPosition();
    });
    this._debounceUpdateFrozenColumn();
  }
  /** @protected */
  _debounceUpdateFrozenColumn() {
    this.__debounceUpdateFrozenColumn = Debouncer.debounce(
      this.__debounceUpdateFrozenColumn,
      microTask2,
      () => this._updateFrozenColumn()
    );
  }
  /** @private */
  _updateFrozenColumn() {
    if (!this._columnTree) {
      return;
    }
    const columnsRow = this._columnTree[this._columnTree.length - 1].slice(0);
    columnsRow.sort((a17, b7) => {
      return a17._order - b7._order;
    });
    let lastFrozen;
    let firstFrozenToEnd;
    for (let i10 = 0; i10 < columnsRow.length; i10++) {
      const col = columnsRow[i10];
      col._lastFrozen = false;
      col._firstFrozenToEnd = false;
      if (firstFrozenToEnd === void 0 && col.frozenToEnd && !col.hidden) {
        firstFrozenToEnd = i10;
      }
      if (col.frozen && !col.hidden) {
        lastFrozen = i10;
      }
    }
    if (lastFrozen !== void 0) {
      columnsRow[lastFrozen]._lastFrozen = true;
    }
    if (firstFrozenToEnd !== void 0) {
      columnsRow[firstFrozenToEnd]._firstFrozenToEnd = true;
    }
    this.__updateColumnsBodyContentHidden();
  }
  /** @private */
  __updateHorizontalScrollPosition() {
    if (!this._columnTree) {
      return;
    }
    const scrollWidth = this.$.table.scrollWidth;
    const clientWidth = this.$.table.clientWidth;
    const scrollLeft = Math.max(0, this.$.table.scrollLeft);
    const normalizedScrollLeft = getNormalizedScrollLeft(this.$.table, this.getAttribute("dir"));
    const transform = `translate(${-scrollLeft}px, 0)`;
    this.$.header.style.transform = transform;
    this.$.footer.style.transform = transform;
    this.$.items.style.transform = transform;
    const x2 = this.__isRTL ? normalizedScrollLeft + clientWidth - scrollWidth : scrollLeft;
    const transformFrozen = `translate(${x2}px, 0)`;
    this._frozenCells.forEach((cell) => {
      cell.style.transform = transformFrozen;
    });
    const remaining = this.__isRTL ? normalizedScrollLeft : scrollLeft + clientWidth - scrollWidth;
    const transformFrozenToEnd = `translate(${remaining}px, 0)`;
    let transformFrozenToEndBody = transformFrozenToEnd;
    if (this._lazyColumns && this._areSizerCellsAssigned()) {
      const columnsInOrder = this._getColumnsInOrder();
      const lastVisibleColumn = [...columnsInOrder].reverse().find((column) => !column.frozenToEnd && !column._bodyContentHidden);
      const lastVisibleColumnEnd = this.__getColumnEnd(lastVisibleColumn);
      const firstFrozenToEndColumn = columnsInOrder.find((column) => column.frozenToEnd);
      const firstFrozenToEndColumnStart = this.__getColumnStart(firstFrozenToEndColumn);
      const translateX = remaining + (firstFrozenToEndColumnStart - lastVisibleColumnEnd) + this.__lazyColumnsStart;
      transformFrozenToEndBody = `translate(${translateX}px, 0)`;
    }
    this._frozenToEndCells.forEach((cell) => {
      if (this.$.items.contains(cell)) {
        cell.style.transform = transformFrozenToEndBody;
      } else {
        cell.style.transform = transformFrozenToEnd;
      }
    });
    if (this.hasAttribute("navigating") && this.__rowFocusMode) {
      this.$.table.style.setProperty("--_grid-horizontal-scroll-position", `${-x2}px`);
    }
  }
  /** @private */
  _areSizerCellsAssigned() {
    return this._getColumnsInOrder().every((column) => column._sizerCell);
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-selection-mixin.js
var SelectionMixin = (superClass) => class SelectionMixin extends superClass {
  static get properties() {
    return {
      /**
       * An array that contains the selected items.
       * @type {!Array<!GridItem>}
       */
      selectedItems: {
        type: Object,
        notify: true,
        value: () => [],
        sync: true
      },
      /**
       * A function to check whether a specific item in the grid may be
       * selected or deselected by the user. Used by the selection column to
       * conditionally enable to disable checkboxes for individual items. This
       * function does not prevent programmatic selection/deselection of
       * items. Changing the function does not modify the currently selected
       * items.
       *
       * Configuring this function hides the select all checkbox of the grid
       * selection column, which means users can not select or deselect all
       * items anymore, nor do they get feedback on whether all items are
       * selected or not.
       *
       * Receives an item instance and should return a boolean indicating
       * whether users may change the selection state of that item.
       *
       * @type {(item: !GridItem) => boolean}
       */
      isItemSelectable: {
        type: Function,
        notify: /* @__PURE__ */ (() => true)()
        // prevent Polymer analyzer from documenting a changed event
      },
      /**
       * Set of selected item ids
       * @private
       */
      __selectedKeys: {
        type: Object,
        computed: "__computeSelectedKeys(itemIdPath, selectedItems)"
      }
    };
  }
  static get observers() {
    return ["__selectedItemsChanged(itemIdPath, selectedItems, isItemSelectable)"];
  }
  /**
   * @param {!GridItem} item
   * @return {boolean}
   * @protected
   */
  _isSelected(item) {
    return this.__selectedKeys.has(this.getItemId(item));
  }
  /**
   * Determines whether the selection state of an item may be changed by the
   * user.
   *
   * @private
   */
  __isItemSelectable(item) {
    if (!this.isItemSelectable || !item) {
      return true;
    }
    return this.isItemSelectable(item);
  }
  /**
   * Selects the given item.
   *
   * @method selectItem
   * @param {!GridItem} item The item object
   */
  selectItem(item) {
    if (!this._isSelected(item)) {
      this.selectedItems = [...this.selectedItems, item];
    }
  }
  /**
   * Deselects the given item if it is already selected.
   *
   * @method deselect
   * @param {!GridItem} item The item object
   */
  deselectItem(item) {
    if (this._isSelected(item)) {
      this.selectedItems = this.selectedItems.filter((i10) => !this._itemsEqual(i10, item));
    }
  }
  /** @private */
  __selectedItemsChanged() {
    this.requestContentUpdate();
  }
  /** @private */
  __computeSelectedKeys(_itemIdPath, selectedItems) {
    const selected = selectedItems || [];
    const selectedKeys = /* @__PURE__ */ new Set();
    selected.forEach((item) => {
      selectedKeys.add(this.getItemId(item));
    });
    return selectedKeys;
  }
  /**
   * Fired when the `selectedItems` property changes.
   *
   * @event selected-items-changed
   */
  /**
   * Fired when the user selects or deselects an item through the selection column.
   *
   * @event item-toggle
   * @param {Object} detail
   * @param {GridItem} detail.item the item that was selected or deselected
   * @param {boolean} detail.selected true if the item was selected
   * @param {boolean} detail.shiftKey true if the shift key was pressed
   */
};

// node_modules/@vaadin/grid/src/vaadin-grid-sort-mixin.js
var defaultMultiSortPriority = "prepend";
var SortMixin = (superClass) => class SortMixin extends superClass {
  static get properties() {
    return {
      /**
       * When `true`, all `<vaadin-grid-sorter>` are applied for sorting.
       * @attr {boolean} multi-sort
       * @type {boolean}
       */
      multiSort: {
        type: Boolean,
        value: false
      },
      /**
       * Controls how columns are added to the sort order when using multi-sort.
       * The sort order is visually indicated by numbers in grid sorters placed in column headers.
       *
       * By default, whenever an unsorted column is sorted, or the sort-direction of a column is
       * changed, that column gets sort priority 1, thus affecting the priority for all the other
       * sorted columns. This is identical to using `multi-sort-priority="prepend"`.
       *
       * Using this property allows to change this behavior so that sorting an unsorted column
       * would add it to the "end" of the sort, and changing column's sort direction would retain
       * it's previous priority. To set this, use `multi-sort-priority="append"`.
       *
       * @attr {string} multi-sort-priority
       */
      multiSortPriority: {
        type: String,
        value: () => defaultMultiSortPriority
      },
      /**
       * When `true`, Shift-clicking an unsorted column's sorter adds it to the multi-sort.
       * Shift + Space does the same action via keyboard. This property has precedence over the
       * `multiSort` property. If `multiSortOnShiftClick` is true, the multiSort property is effectively ignored.
       *
       * @attr {boolean} multi-sort-on-shift-click
       * @type {boolean}
       */
      multiSortOnShiftClick: {
        type: Boolean,
        value: false
      },
      /**
       * @type {!Array<!GridSorterDefinition>}
       * @protected
       */
      _sorters: {
        type: Array,
        value: () => []
      },
      /** @private */
      _previousSorters: {
        type: Array,
        value: () => []
      }
    };
  }
  /**
   * Sets the default multi-sort priority to use for all grid instances.
   * This method should be called before creating any grid instances.
   * Changing this setting does not affect the default for existing grids.
   *
   * @param {string} priority
   */
  static setDefaultMultiSortPriority(priority) {
    defaultMultiSortPriority = ["append", "prepend"].includes(priority) ? priority : "prepend";
  }
  /** @protected */
  ready() {
    super.ready();
    this.addEventListener("sorter-changed", this._onSorterChanged);
  }
  /** @private */
  _onSorterChanged(e17) {
    const sorter = e17.target;
    e17.stopPropagation();
    sorter._grid = this;
    this.__updateSorter(sorter, e17.detail.shiftClick, e17.detail.fromSorterClick);
    this.__applySorters();
  }
  /** @private */
  __removeSorters(sortersToRemove) {
    if (sortersToRemove.length === 0) {
      return;
    }
    this._sorters = this._sorters.filter((sorter) => !sortersToRemove.includes(sorter));
    this.__applySorters();
  }
  /** @private */
  __updateSortOrders() {
    this._sorters.forEach((sorter) => {
      sorter._order = null;
    });
    const activeSorters = this._getActiveSorters();
    if (activeSorters.length > 1) {
      activeSorters.forEach((sorter, index) => {
        sorter._order = index;
      });
    }
  }
  /** @private */
  __updateSorter(sorter, shiftClick, fromSorterClick) {
    if (!sorter.direction && !this._sorters.includes(sorter)) {
      return;
    }
    sorter._order = null;
    const restSorters = this._sorters.filter((s22) => s22 !== sorter);
    if (this.multiSort && (!this.multiSortOnShiftClick || !fromSorterClick) || this.multiSortOnShiftClick && shiftClick) {
      if (this.multiSortPriority === "append") {
        this._sorters = [...restSorters, sorter];
      } else {
        this._sorters = [sorter, ...restSorters];
      }
    } else if (sorter.direction || this.multiSortOnShiftClick) {
      this._sorters = sorter.direction ? [sorter] : [];
      restSorters.forEach((sorter2) => {
        sorter2._order = null;
        sorter2.direction = null;
      });
    }
  }
  /** @private */
  __applySorters() {
    this.__updateSortOrders();
    if (this.dataProvider && // No need to clear cache if sorters didn't change and grid is attached
    this.isAttached && JSON.stringify(this._previousSorters) !== JSON.stringify(this._mapSorters())) {
      this.__debounceClearCache();
    }
    this._a11yUpdateSorters();
    this._previousSorters = this._mapSorters();
  }
  /**
   * @type {GridSorterDefinition[]}
   * @protected
   */
  _getActiveSorters() {
    return this._sorters.filter((sorter) => sorter.direction && sorter.isConnected);
  }
  /**
   * @return {!Array<!GridSorterDefinition>}
   * @protected
   */
  _mapSorters() {
    return this._getActiveSorters().map((sorter) => {
      return {
        path: sorter.path,
        direction: sorter.direction
      };
    });
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-styling-mixin.js
var StylingMixin = (superClass) => class StylingMixin extends superClass {
  static get properties() {
    return {
      /**
       * A function that allows generating CSS class names for grid cells
       * based on their row and column. The return value should be the generated
       * class name as a string, or multiple class names separated by whitespace
       * characters.
       *
       * Receives two arguments:
       * - `column` The `<vaadin-grid-column>` element (`undefined` for details-cell).
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *
       * @type {GridCellClassNameGenerator | null | undefined}
       * @deprecated Use `cellPartNameGenerator` instead.
       */
      cellClassNameGenerator: {
        type: Function,
        sync: true
      },
      /**
       * A function that allows generating CSS `part` names for grid cells in Shadow DOM based
       * on their row and column, for styling from outside using the `::part()` selector.
       *
       * The return value should be the generated part name as a string, or multiple part names
       * separated by whitespace characters.
       *
       * Receives two arguments:
       * - `column` The `<vaadin-grid-column>` element (`undefined` for details-cell).
       * - `model` The object with the properties related with
       *   the rendered item, contains:
       *   - `model.index` The index of the item.
       *   - `model.item` The item.
       *   - `model.expanded` Sublevel toggle state.
       *   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
       *   - `model.selected` Selected state.
       *
       * @type {GridCellPartNameGenerator | null | undefined}
       */
      cellPartNameGenerator: {
        type: Function,
        sync: true
      }
    };
  }
  static get observers() {
    return [
      "__cellClassNameGeneratorChanged(cellClassNameGenerator)",
      "__cellPartNameGeneratorChanged(cellPartNameGenerator)"
    ];
  }
  /** @private */
  __cellClassNameGeneratorChanged() {
    this.generateCellClassNames();
  }
  /** @private */
  __cellPartNameGeneratorChanged() {
    this.generateCellPartNames();
  }
  /**
   * Runs the `cellClassNameGenerator` for the visible cells.
   * If the generator depends on varying conditions, you need to
   * call this function manually in order to update the styles when
   * the conditions change.
   *
   * @deprecated Use `cellPartNameGenerator` and `generateCellPartNames()` instead.
   */
  generateCellClassNames() {
    iterateChildren(this.$.items, (row) => {
      if (!row.hidden) {
        this._generateCellClassNames(row, this.__getRowModel(row));
      }
    });
  }
  /**
   * Runs the `cellPartNameGenerator` for the visible cells.
   * If the generator depends on varying conditions, you need to
   * call this function manually in order to update the styles when
   * the conditions change.
   */
  generateCellPartNames() {
    iterateChildren(this.$.items, (row) => {
      if (!row.hidden) {
        this._generateCellPartNames(row, this.__getRowModel(row));
      }
    });
  }
  /** @private */
  _generateCellClassNames(row, model) {
    iterateRowCells(row, (cell) => {
      if (cell.__generatedClasses) {
        cell.__generatedClasses.forEach((className) => cell.classList.remove(className));
      }
      if (this.cellClassNameGenerator && !row.hasAttribute("loading")) {
        const result = this.cellClassNameGenerator(cell._column, model);
        cell.__generatedClasses = result && result.split(" ").filter((className) => className.length > 0);
        if (cell.__generatedClasses) {
          cell.__generatedClasses.forEach((className) => cell.classList.add(className));
        }
      }
    });
  }
  /** @private */
  _generateCellPartNames(row, model) {
    iterateRowCells(row, (cell) => {
      if (cell.__generatedParts) {
        cell.__generatedParts.forEach((partName) => {
          updatePart(cell, null, partName);
        });
      }
      if (this.cellPartNameGenerator && !row.hasAttribute("loading")) {
        const result = this.cellPartNameGenerator(cell._column, model);
        cell.__generatedParts = result && result.split(" ").filter((partName) => partName.length > 0);
        if (cell.__generatedParts) {
          cell.__generatedParts.forEach((partName) => {
            updatePart(cell, true, partName);
          });
        }
      }
    });
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-mixin.js
var GridMixin = (superClass) => class extends ColumnAutoWidthMixin(
  ArrayDataProviderMixin(
    DataProviderMixin(
      DynamicColumnsMixin(
        ActiveItemMixin(
          ScrollMixin(
            SelectionMixin(
              SortMixin(
                RowDetailsMixin(
                  KeyboardNavigationMixin(
                    A11yMixin(
                      FilterMixin(
                        ColumnReorderingMixin(
                          ColumnResizingMixin(
                            EventContextMixin(DragAndDropMixin(StylingMixin(TabindexMixin(superClass))))
                          )
                        )
                      )
                    )
                  )
                )
              )
            )
          )
        )
      )
    )
  )
) {
  static get observers() {
    return ["_columnTreeChanged(_columnTree)", "_flatSizeChanged(_flatSize, __virtualizer, _hasData, _columnTree)"];
  }
  static get properties() {
    return {
      /** @private */
      _safari: {
        type: Boolean,
        value: isSafari
      },
      /** @private */
      _ios: {
        type: Boolean,
        value: isIOS
      },
      /** @private */
      _firefox: {
        type: Boolean,
        value: isFirefox
      },
      /** @private */
      _android: {
        type: Boolean,
        value: isAndroid
      },
      /** @private */
      _touchDevice: {
        type: Boolean,
        value: isTouch
      },
      /**
       * If true, the grid's height is defined by its rows.
       *
       * Effectively, this disables the grid's virtual scrolling so that all the rows are rendered in the DOM at once.
       * If the grid has a large number of items, using the feature is discouraged to avoid performance issues.
       * @attr {boolean} all-rows-visible
       * @type {boolean}
       */
      allRowsVisible: {
        type: Boolean,
        value: false,
        reflectToAttribute: true
      },
      /** @private */
      isAttached: {
        value: false
      },
      /**
       * An internal property that is mainly used by `vaadin-template-renderer`
       * to identify grid elements.
       *
       * @private
       */
      __gridElement: {
        type: Boolean,
        value: true
      },
      /** @private */
      __hasEmptyStateContent: {
        type: Boolean,
        value: false
      },
      /** @private */
      __emptyState: {
        type: Boolean,
        computed: "__computeEmptyState(_flatSize, __hasEmptyStateContent)"
      }
    };
  }
  constructor() {
    super();
    this.addEventListener("animationend", this._onAnimationEnd);
  }
  /** @private */
  get _firstVisibleIndex() {
    const firstVisibleItem = this.__getFirstVisibleItem();
    return firstVisibleItem ? firstVisibleItem.index : void 0;
  }
  /** @private */
  get _lastVisibleIndex() {
    const lastVisibleItem = this.__getLastVisibleItem();
    return lastVisibleItem ? lastVisibleItem.index : void 0;
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    this.isAttached = true;
    this.__virtualizer.hostConnected();
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    this.isAttached = false;
    this._hideTooltip(true);
  }
  /** @private */
  __getFirstVisibleItem() {
    return this._getRenderedRows().find((row) => this._isInViewport(row));
  }
  /** @private */
  __getLastVisibleItem() {
    return this._getRenderedRows().reverse().find((row) => this._isInViewport(row));
  }
  /** @private */
  _isInViewport(item) {
    const scrollTargetRect = this.$.table.getBoundingClientRect();
    const itemRect = item.getBoundingClientRect();
    const headerHeight = this.$.header.getBoundingClientRect().height;
    const footerHeight = this.$.footer.getBoundingClientRect().height;
    return itemRect.bottom > scrollTargetRect.top + headerHeight && itemRect.top < scrollTargetRect.bottom - footerHeight;
  }
  /** @private */
  _getRenderedRows() {
    return Array.from(this.$.items.children).filter((item) => !item.hidden).sort((a17, b7) => a17.index - b7.index);
  }
  /** @protected */
  _getRowContainingNode(node) {
    const content = getClosestElement("vaadin-grid-cell-content", node);
    if (!content) {
      return;
    }
    const cell = content.assignedSlot.parentElement;
    return cell.parentElement;
  }
  /** @protected */
  _isItemAssignedToRow(item, row) {
    const model = this.__getRowModel(row);
    return this.getItemId(item) === this.getItemId(model.item);
  }
  /** @protected */
  ready() {
    super.ready();
    setTouchAction(this, "");
    setTouchAction(this.$.scroller, "");
    this.__virtualizer = new Virtualizer({
      createElements: this._createScrollerRows.bind(this),
      updateElement: this._updateScrollerItem.bind(this),
      scrollContainer: this.$.items,
      scrollTarget: this.$.table,
      reorderElements: true
    });
    new ResizeObserver(() => {
      setTimeout(() => {
        this.__updateColumnsBodyContentHidden();
      });
      this.__updateHorizontalScrollPosition();
    }).observe(this.$.table);
    const minHeightObserver = new ResizeObserver(
      () => setTimeout(() => {
        this.__updateMinHeight();
      })
    );
    minHeightObserver.observe(this.$.header);
    minHeightObserver.observe(this.$.items);
    minHeightObserver.observe(this.$.footer);
    processTemplates(this);
    this._tooltipController = new TooltipController(this);
    this.addController(this._tooltipController);
    this._tooltipController.setManual(true);
    this.__emptyStateContentObserver = new SlotObserver(this.$.emptystateslot, ({ currentNodes }) => {
      this.$.emptystatecell._content = currentNodes[0];
      this.__hasEmptyStateContent = !!this.$.emptystatecell._content;
    });
  }
  /** @private */
  __getBodyCellCoordinates(cell) {
    if (this.$.items.contains(cell) && cell.localName === "td") {
      return {
        item: cell.parentElement._item,
        column: cell._column
      };
    }
  }
  /** @private */
  __focusBodyCell({ item, column }) {
    const row = this._getRenderedRows().find((row2) => row2._item === item);
    const cell = row && [...row.children].find((cell2) => cell2._column === column);
    if (cell) {
      cell.focus();
    }
  }
  /** @protected */
  _focusFirstVisibleRow() {
    const row = this.__getFirstVisibleItem();
    this.__rowFocusMode = true;
    row.focus();
  }
  /** @private */
  _flatSizeChanged(flatSize, virtualizer, hasData, columnTree) {
    if (virtualizer && hasData && columnTree) {
      const cell = this.shadowRoot.activeElement;
      const cellCoordinates = this.__getBodyCellCoordinates(cell);
      const previousSize = virtualizer.size || 0;
      virtualizer.size = flatSize;
      virtualizer.update(previousSize - 1, previousSize - 1);
      if (flatSize < previousSize) {
        virtualizer.update(flatSize - 1, flatSize - 1);
      }
      if (cellCoordinates && cell.parentElement.hidden) {
        this.__focusBodyCell(cellCoordinates);
      }
      this._resetKeyboardNavigation();
    }
  }
  /** @private */
  _createScrollerRows(count) {
    const rows = [];
    for (let i10 = 0; i10 < count; i10++) {
      const row = document.createElement("tr");
      row.setAttribute("part", "row body-row");
      row.setAttribute("role", "row");
      row.setAttribute("tabindex", "-1");
      if (this._columnTree) {
        this._updateRow(row, this._columnTree[this._columnTree.length - 1], "body", false, true);
      }
      rows.push(row);
    }
    if (this._columnTree) {
      this._columnTree[this._columnTree.length - 1].forEach((c17) => {
        if (c17.isConnected && c17._cells) {
          c17._cells = [...c17._cells];
        }
      });
    }
    this.__afterCreateScrollerRowsDebouncer = Debouncer.debounce(
      this.__afterCreateScrollerRowsDebouncer,
      animationFrame,
      () => {
        this._afterScroll();
      }
    );
    return rows;
  }
  /** @private */
  _createCell(tagName, column) {
    const contentId = this._contentIndex = this._contentIndex + 1 || 0;
    const slotName = `vaadin-grid-cell-content-${contentId}`;
    const cellContent = document.createElement("vaadin-grid-cell-content");
    cellContent.setAttribute("slot", slotName);
    const cell = document.createElement(tagName);
    cell.id = slotName.replace("-content-", "-");
    cell.setAttribute("role", tagName === "td" ? "gridcell" : "columnheader");
    if (!isAndroid && !isIOS) {
      cell.addEventListener("mouseenter", (event) => {
        if (!this.$.scroller.hasAttribute("scrolling")) {
          this._showTooltip(event);
        }
      });
      cell.addEventListener("mouseleave", () => {
        this._hideTooltip();
      });
      cell.addEventListener("mousedown", () => {
        this._hideTooltip(true);
      });
    }
    const slot = document.createElement("slot");
    slot.setAttribute("name", slotName);
    if (column && column._focusButtonMode) {
      const div = document.createElement("div");
      div.setAttribute("role", "button");
      div.setAttribute("tabindex", "-1");
      cell.appendChild(div);
      cell._focusButton = div;
      cell.focus = function(options) {
        cell._focusButton.focus(options);
      };
      div.appendChild(slot);
    } else {
      cell.setAttribute("tabindex", "-1");
      cell.appendChild(slot);
    }
    cell._content = cellContent;
    cellContent.addEventListener("mousedown", () => {
      if (isChrome) {
        const mouseUpListener = (event) => {
          const contentContainsFocusedElement = cellContent.contains(this.getRootNode().activeElement);
          const mouseUpWithinCell = event.composedPath().includes(cellContent);
          if (!contentContainsFocusedElement && mouseUpWithinCell) {
            cell.focus({ preventScroll: true });
          }
          document.removeEventListener("mouseup", mouseUpListener, true);
        };
        document.addEventListener("mouseup", mouseUpListener, true);
      } else {
        setTimeout(() => {
          if (!cellContent.contains(this.getRootNode().activeElement)) {
            cell.focus({ preventScroll: true });
          }
        });
      }
    });
    return cell;
  }
  /**
   * @param {!HTMLTableRowElement} row
   * @param {!Array<!GridColumn>} columns
   * @param {?string} section
   * @param {boolean} isColumnRow
   * @param {boolean} noNotify
   * @protected
   */
  _updateRow(row, columns, section = "body", isColumnRow = false, noNotify = false) {
    const contentsFragment = document.createDocumentFragment();
    iterateRowCells(row, (cell) => {
      cell._vacant = true;
    });
    row.innerHTML = "";
    if (section === "body") {
      row.__cells = [];
      row.__detailsCell = null;
    }
    columns.filter((column) => !column.hidden).forEach((column, index, cols) => {
      let cell;
      if (section === "body") {
        if (!column._cells) {
          column._cells = [];
        }
        cell = column._cells.find((cell2) => cell2._vacant);
        if (!cell) {
          cell = this._createCell("td", column);
          if (column._onCellKeyDown) {
            cell.addEventListener("keydown", column._onCellKeyDown.bind(column));
          }
          column._cells.push(cell);
        }
        cell.setAttribute("part", "cell body-cell");
        cell.__parentRow = row;
        row.__cells.push(cell);
        const isSizerRow = row === this.$.sizer;
        if (!column._bodyContentHidden || isSizerRow) {
          row.appendChild(cell);
        }
        if (isSizerRow) {
          column._sizerCell = cell;
        }
        if (index === cols.length - 1 && this.rowDetailsRenderer) {
          if (!this._detailsCells) {
            this._detailsCells = [];
          }
          const detailsCell = this._detailsCells.find((cell2) => cell2._vacant) || this._createCell("td");
          if (this._detailsCells.indexOf(detailsCell) === -1) {
            this._detailsCells.push(detailsCell);
          }
          if (!detailsCell._content.parentElement) {
            contentsFragment.appendChild(detailsCell._content);
          }
          this._configureDetailsCell(detailsCell);
          row.appendChild(detailsCell);
          row.__detailsCell = detailsCell;
          this._a11ySetRowDetailsCell(row, detailsCell);
          detailsCell._vacant = false;
        }
        if (!noNotify) {
          column._cells = [...column._cells];
        }
      } else {
        const tagName = section === "header" ? "th" : "td";
        if (isColumnRow || column.localName === "vaadin-grid-column-group") {
          cell = column[`_${section}Cell`];
          if (!cell) {
            cell = this._createCell(tagName);
            if (column._onCellKeyDown) {
              cell.addEventListener("keydown", column._onCellKeyDown.bind(column));
            }
          }
          cell._column = column;
          row.appendChild(cell);
          column[`_${section}Cell`] = cell;
        } else {
          if (!column._emptyCells) {
            column._emptyCells = [];
          }
          cell = column._emptyCells.find((cell2) => cell2._vacant) || this._createCell(tagName);
          cell._column = column;
          row.appendChild(cell);
          if (column._emptyCells.indexOf(cell) === -1) {
            column._emptyCells.push(cell);
          }
        }
        cell.part.add("cell", `${section}-cell`);
      }
      if (!cell._content.parentElement) {
        contentsFragment.appendChild(cell._content);
      }
      cell._vacant = false;
      cell._column = column;
    });
    if (section !== "body") {
      this.__debounceUpdateHeaderFooterRowVisibility(row);
    }
    this.appendChild(contentsFragment);
    this._frozenCellsChanged();
    this._updateFirstAndLastColumnForRow(row);
  }
  /**
   * @param {HTMLTableRowElement} row
   * @protected
   */
  __debounceUpdateHeaderFooterRowVisibility(row) {
    row.__debounceUpdateHeaderFooterRowVisibility = Debouncer.debounce(
      row.__debounceUpdateHeaderFooterRowVisibility,
      microTask2,
      () => this.__updateHeaderFooterRowVisibility(row)
    );
  }
  /**
   * @param {HTMLTableRowElement} row
   * @protected
   */
  __updateHeaderFooterRowVisibility(row) {
    if (!row) {
      return;
    }
    const visibleRowCells = Array.from(row.children).filter((cell) => {
      const column = cell._column;
      if (column._emptyCells && column._emptyCells.indexOf(cell) > -1) {
        return false;
      }
      if (row.parentElement === this.$.header) {
        if (column.headerRenderer) {
          return true;
        }
        if (column.header === null) {
          return false;
        }
        if (column.path || column.header !== void 0) {
          return true;
        }
      } else if (column.footerRenderer) {
        return true;
      }
      return false;
    });
    if (row.hidden !== !visibleRowCells.length) {
      row.hidden = !visibleRowCells.length;
    }
    this._resetKeyboardNavigation();
    this._a11yUpdateGridSize(this.size, this._columnTree, this.__emptyState);
  }
  /** @private */
  _updateScrollerItem(row, index) {
    this._preventScrollerRotatingCellFocus(row, index);
    if (!this._columnTree) {
      return;
    }
    this._updateRowOrderParts(row, index);
    this._a11yUpdateRowRowindex(row, index);
    this._getItem(index, row);
  }
  /** @private */
  _columnTreeChanged(columnTree) {
    this._renderColumnTree(columnTree);
    this.__updateColumnsBodyContentHidden();
  }
  /** @private */
  _updateRowOrderParts(row, index = row.index) {
    updateBooleanRowStates(row, {
      first: index === 0,
      last: index === this._flatSize - 1,
      odd: index % 2 !== 0,
      even: index % 2 === 0
    });
  }
  /** @private */
  _updateRowStateParts(row, { item, expanded, selected, detailsOpened }) {
    updateBooleanRowStates(row, {
      expanded,
      collapsed: this.__isRowExpandable(row),
      selected,
      nonselectable: this.__isItemSelectable(item) === false,
      "details-opened": detailsOpened
    });
  }
  /** @private */
  __computeEmptyState(flatSize, hasEmptyStateContent) {
    return flatSize === 0 && hasEmptyStateContent;
  }
  /**
   * @param {!Array<!GridColumn>} columnTree
   * @protected
   */
  _renderColumnTree(columnTree) {
    iterateChildren(this.$.items, (row) => {
      this._updateRow(row, columnTree[columnTree.length - 1], "body", false, true);
      const model = this.__getRowModel(row);
      this._updateRowOrderParts(row);
      this._updateRowStateParts(row, model);
      this._filterDragAndDrop(row, model);
    });
    while (this.$.header.children.length < columnTree.length) {
      const headerRow = document.createElement("tr");
      headerRow.setAttribute("part", "row");
      headerRow.setAttribute("role", "row");
      headerRow.setAttribute("tabindex", "-1");
      this.$.header.appendChild(headerRow);
      const footerRow = document.createElement("tr");
      footerRow.setAttribute("part", "row");
      footerRow.setAttribute("role", "row");
      footerRow.setAttribute("tabindex", "-1");
      this.$.footer.appendChild(footerRow);
    }
    while (this.$.header.children.length > columnTree.length) {
      this.$.header.removeChild(this.$.header.firstElementChild);
      this.$.footer.removeChild(this.$.footer.firstElementChild);
    }
    iterateChildren(this.$.header, (headerRow, index, rows) => {
      this._updateRow(headerRow, columnTree[index], "header", index === columnTree.length - 1);
      const cells = getBodyRowCells(headerRow);
      updateCellsPart(cells, "first-header-row-cell", index === 0);
      updateCellsPart(cells, "last-header-row-cell", index === rows.length - 1);
    });
    iterateChildren(this.$.footer, (footerRow, index, rows) => {
      this._updateRow(footerRow, columnTree[columnTree.length - 1 - index], "footer", index === 0);
      const cells = getBodyRowCells(footerRow);
      updateCellsPart(cells, "first-footer-row-cell", index === 0);
      updateCellsPart(cells, "last-footer-row-cell", index === rows.length - 1);
    });
    this._updateRow(this.$.sizer, columnTree[columnTree.length - 1]);
    this._resizeHandler();
    this._frozenCellsChanged();
    this._updateFirstAndLastColumn();
    this._resetKeyboardNavigation();
    this._a11yUpdateHeaderRows();
    this._a11yUpdateFooterRows();
    this.generateCellClassNames();
    this.generateCellPartNames();
    this.__updateHeaderAndFooter();
  }
  /**
   * @param {!HTMLElement} row
   * @param {GridItem} item
   * @protected
   */
  _updateItem(row, item) {
    row._item = item;
    const model = this.__getRowModel(row);
    this._toggleDetailsCell(row, model.detailsOpened);
    this._a11yUpdateRowLevel(row, model.level);
    this._a11yUpdateRowSelected(row, model.selected);
    this._updateRowStateParts(row, model);
    this._generateCellClassNames(row, model);
    this._generateCellPartNames(row, model);
    this._filterDragAndDrop(row, model);
    this.__updateDragSourceParts(row, model);
    iterateChildren(row, (cell) => {
      if (cell._column && !cell._column.isConnected) {
        return;
      }
      if (cell._renderer) {
        const owner = cell._column || this;
        cell._renderer.call(owner, cell._content, owner, model);
      }
    });
    this._updateDetailsCellHeight(row);
    this._a11yUpdateRowExpanded(row, model.expanded);
  }
  /** @private */
  _resizeHandler() {
    this._updateDetailsCellHeights();
    this.__updateHorizontalScrollPosition();
  }
  /** @private */
  _onAnimationEnd(e17) {
    if (e17.animationName.indexOf("vaadin-grid-appear") === 0) {
      e17.stopPropagation();
      this._resetKeyboardNavigation();
      requestAnimationFrame(() => {
        this.__scrollToPendingIndexes();
      });
    }
  }
  /**
   * @param {!HTMLTableRowElement} row
   * @return {!GridItemModel}
   * @protected
   */
  __getRowModel(row) {
    return {
      index: row.index,
      item: row._item,
      level: this._getIndexLevel(row.index),
      expanded: this._isExpanded(row._item),
      selected: this._isSelected(row._item),
      detailsOpened: !!this.rowDetailsRenderer && this._isDetailsOpened(row._item)
    };
  }
  /**
   * @param {Event} event
   * @protected
   */
  _showTooltip(event) {
    const tooltip = this._tooltipController.node;
    if (tooltip && tooltip.isConnected) {
      const target = event.target;
      if (!this.__isCellFullyVisible(target)) {
        return;
      }
      this._tooltipController.setTarget(target);
      this._tooltipController.setContext(this.getEventContext(event));
      tooltip._stateController.open({
        focus: event.type === "focusin",
        hover: event.type === "mouseenter"
      });
    }
  }
  /** @private */
  __isCellFullyVisible(cell) {
    if (cell.hasAttribute("frozen") || cell.hasAttribute("frozen-to-end")) {
      return true;
    }
    let { left, right } = this.getBoundingClientRect();
    const frozen = [...cell.parentNode.children].find((cell2) => cell2.hasAttribute("last-frozen"));
    if (frozen) {
      const frozenRect = frozen.getBoundingClientRect();
      left = this.__isRTL ? left : frozenRect.right;
      right = this.__isRTL ? frozenRect.left : right;
    }
    const frozenToEnd = [...cell.parentNode.children].find((cell2) => cell2.hasAttribute("first-frozen-to-end"));
    if (frozenToEnd) {
      const frozenToEndRect = frozenToEnd.getBoundingClientRect();
      left = this.__isRTL ? frozenToEndRect.right : left;
      right = this.__isRTL ? right : frozenToEndRect.left;
    }
    const cellRect = cell.getBoundingClientRect();
    return cellRect.left >= left && cellRect.right <= right;
  }
  /** @protected */
  _hideTooltip(immediate) {
    const tooltip = this._tooltipController && this._tooltipController.node;
    if (tooltip) {
      tooltip._stateController.close(immediate);
    }
  }
  /**
   * Requests an update for the content of cells.
   *
   * While performing the update, the following renderers are invoked:
   * - `Grid.rowDetailsRenderer`
   * - `GridColumn.renderer`
   * - `GridColumn.headerRenderer`
   * - `GridColumn.footerRenderer`
   *
   * It is not guaranteed that the update happens immediately (synchronously) after it is requested.
   */
  requestContentUpdate() {
    this.__updateHeaderAndFooter();
    this.__updateVisibleRows();
  }
  /** @private */
  __updateHeaderAndFooter() {
    (this._columnTree || []).forEach((level) => {
      level.forEach((column) => {
        if (column._renderHeaderAndFooter) {
          column._renderHeaderAndFooter();
        }
      });
    });
  }
  /** @protected */
  __updateVisibleRows(start, end) {
    if (this.__virtualizer) {
      this.__virtualizer.update(start, end);
    }
  }
  /** @private */
  __updateMinHeight() {
    const rowHeight = 36;
    const headerHeight = this.$.header.clientHeight;
    const footerHeight = this.$.footer.clientHeight;
    const scrollbarHeight = this.$.table.offsetHeight - this.$.table.clientHeight;
    const minHeight = headerHeight + rowHeight + footerHeight + scrollbarHeight;
    if (!this.__minHeightStyleSheet && supportsAdoptingStyleSheets2) {
      this.__minHeightStyleSheet = new CSSStyleSheet();
      this.shadowRoot.adoptedStyleSheets = [...this.shadowRoot.adoptedStyleSheets, this.__minHeightStyleSheet];
    }
    if (this.__minHeightStyleSheet) {
      this.__minHeightStyleSheet.replaceSync(`:host { --_grid-min-height: ${minHeight}px; }`);
    } else {
      this.style.setProperty("--_grid-min-height", `${minHeight}px`);
    }
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-styles.js
var gridStyles = css`
  @keyframes vaadin-grid-appear {
    to {
      opacity: 1;
    }
  }

  :host {
    display: flex;
    flex-direction: column;
    animation: 1ms vaadin-grid-appear;
    height: 400px;
    min-height: var(--_grid-min-height, 0);
    flex: 1 1 auto;
    align-self: stretch;
    position: relative;
  }

  :host([hidden]) {
    display: none !important;
  }

  :host([disabled]) {
    pointer-events: none;
  }

  #scroller {
    display: flex;
    flex-direction: column;
    min-height: 100%;
    transform: translateY(0);
    width: auto;
    height: auto;
    position: absolute;
    inset: 0;
  }

  :host([all-rows-visible]) {
    height: auto;
    align-self: flex-start;
    min-height: auto;
    flex-grow: 0;
    width: 100%;
  }

  :host([all-rows-visible]) #scroller {
    width: 100%;
    height: 100%;
    position: relative;
  }

  :host([all-rows-visible]) #items {
    min-height: 1px;
  }

  #table {
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
    overflow: auto;
    position: relative;
    outline: none;
    /* Workaround for a Desktop Safari bug: new stacking context here prevents the scrollbar from getting hidden */
    z-index: 0;
  }

  #header,
  #footer {
    display: block;
    position: -webkit-sticky;
    position: sticky;
    left: 0;
    overflow: visible;
    width: 100%;
    z-index: 1;
  }

  #header {
    top: 0;
  }

  th {
    text-align: inherit;
  }

  /* Safari doesn't work with "inherit" */
  [safari] th {
    text-align: initial;
  }

  #footer {
    bottom: 0;
  }

  #items {
    flex-grow: 1;
    flex-shrink: 0;
    display: block;
    position: -webkit-sticky;
    position: sticky;
    width: 100%;
    left: 0;
    overflow: visible;
  }

  [part~='row'] {
    display: flex;
    width: 100%;
    box-sizing: border-box;
    margin: 0;
  }

  [part~='row'][loading] [part~='body-cell'] ::slotted(vaadin-grid-cell-content) {
    visibility: hidden;
  }

  [column-rendering='lazy'] [part~='body-cell']:not([frozen]):not([frozen-to-end]) {
    transform: translateX(var(--_grid-lazy-columns-start));
  }

  #items [part~='row'] {
    position: absolute;
  }

  #items [part~='row']:empty {
    height: 100%;
  }

  [part~='cell']:not([part~='details-cell']) {
    flex-shrink: 0;
    flex-grow: 1;
    box-sizing: border-box;
    display: flex;
    width: 100%;
    position: relative;
    align-items: center;
    padding: 0;
    white-space: nowrap;
  }

  [part~='cell'] {
    outline: none;
  }

  [part~='cell'] > [tabindex] {
    display: flex;
    align-items: inherit;
    outline: none;
    position: absolute;
    inset: 0;
  }

  /* Switch the focusButtonMode wrapping element to "position: static" temporarily
     when measuring real width of the cells in the auto-width columns. */
  [measuring-auto-width] [part~='cell'] > [tabindex] {
    position: static;
  }

  [part~='details-cell'] {
    position: absolute;
    bottom: 0;
    width: 100%;
    box-sizing: border-box;
    padding: 0;
  }

  [part~='cell'] ::slotted(vaadin-grid-cell-content) {
    display: block;
    width: 100%;
    box-sizing: border-box;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  [hidden] {
    display: none !important;
  }

  [frozen],
  [frozen-to-end] {
    z-index: 2;
    will-change: transform;
  }

  [no-scrollbars][safari] #table,
  [no-scrollbars][firefox] #table {
    overflow: hidden;
  }

  /* Empty state */

  #scroller:not([empty-state]) #emptystatebody,
  #scroller[empty-state] #items {
    display: none;
  }

  #emptystatebody {
    display: flex;
    position: sticky;
    inset: 0;
    flex: 1;
    overflow: hidden;
  }

  #emptystaterow {
    display: flex;
    flex: 1;
  }

  #emptystatecell {
    display: block;
    flex: 1;
    overflow: auto;
  }

  /* Reordering styles */
  :host([reordering]) [part~='cell'] ::slotted(vaadin-grid-cell-content),
  :host([reordering]) [part~='resize-handle'],
  #scroller[no-content-pointer-events] [part~='cell'] ::slotted(vaadin-grid-cell-content) {
    pointer-events: none;
  }

  [part~='reorder-ghost'] {
    visibility: hidden;
    position: fixed;
    pointer-events: none;
    opacity: 0.5;

    /* Prevent overflowing the grid in Firefox */
    top: 0;
    left: 0;
  }

  :host([reordering]) {
    -webkit-user-select: none;
    user-select: none;
  }

  /* Resizing styles */
  [part~='resize-handle'] {
    position: absolute;
    top: 0;
    right: 0;
    height: 100%;
    cursor: col-resize;
    z-index: 1;
  }

  [part~='resize-handle']::before {
    position: absolute;
    content: '';
    height: 100%;
    width: 35px;
    transform: translateX(-50%);
  }

  [last-column] [part~='resize-handle']::before,
  [last-frozen] [part~='resize-handle']::before {
    width: 18px;
    transform: none;
    right: 0;
  }

  [frozen-to-end] [part~='resize-handle'] {
    left: 0;
    right: auto;
  }

  [frozen-to-end] [part~='resize-handle']::before {
    left: 0;
    right: auto;
  }

  [first-frozen-to-end] [part~='resize-handle']::before {
    width: 18px;
    transform: none;
  }

  [first-frozen-to-end] {
    margin-inline-start: auto;
  }

  /* Hide resize handle if scrolled to end */
  :host(:not([overflow~='end'])) [first-frozen-to-end] [part~='resize-handle'] {
    display: none;
  }

  #scroller[column-resizing],
  #scroller[range-selecting] {
    -webkit-user-select: none;
    user-select: none;
  }

  /* Sizer styles */
  #sizer {
    display: flex;
    position: absolute;
    visibility: hidden;
  }

  #sizer [part~='details-cell'] {
    display: none !important;
  }

  #sizer [part~='cell'][hidden] {
    display: none !important;
  }

  #sizer [part~='cell'] {
    display: block;
    flex-shrink: 0;
    line-height: 0;
    height: 0 !important;
    min-height: 0 !important;
    max-height: 0 !important;
    padding: 0 !important;
    border: none !important;
  }

  #sizer [part~='cell']::before {
    content: '-';
  }

  #sizer [part~='cell'] ::slotted(vaadin-grid-cell-content) {
    display: none !important;
  }

  /* RTL specific styles */

  :host([dir='rtl']) #items,
  :host([dir='rtl']) #header,
  :host([dir='rtl']) #footer {
    left: auto;
  }

  :host([dir='rtl']) [part~='reorder-ghost'] {
    left: auto;
    right: 0;
  }

  :host([dir='rtl']) [part~='resize-handle'] {
    left: 0;
    right: auto;
  }

  :host([dir='rtl']) [part~='resize-handle']::before {
    transform: translateX(50%);
  }

  :host([dir='rtl']) [last-column] [part~='resize-handle']::before,
  :host([dir='rtl']) [last-frozen] [part~='resize-handle']::before {
    left: 0;
    right: auto;
  }

  :host([dir='rtl']) [frozen-to-end] [part~='resize-handle'] {
    right: 0;
    left: auto;
  }

  :host([dir='rtl']) [frozen-to-end] [part~='resize-handle']::before {
    right: 0;
    left: auto;
  }

  @media (forced-colors: active) {
    [part~='selected-row'] [part~='first-column-cell']::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      bottom: 0;
      border: 2px solid;
    }

    [part~='focused-cell']::before {
      outline: 2px solid !important;
      outline-offset: -1px;
    }
  }
`;

// node_modules/@vaadin/grid/src/vaadin-grid.js
registerStyles("vaadin-grid", gridStyles, { moduleId: "vaadin-grid-styles" });
var Grid = class extends GridMixin(ElementMixin2(ThemableMixin(ControllerMixin(PolymerElement)))) {
  static get template() {
    return html`
      <div
        id="scroller"
        safari$="[[_safari]]"
        ios$="[[_ios]]"
        loading$="[[loading]]"
        column-reordering-allowed$="[[columnReorderingAllowed]]"
        empty-state$="[[__emptyState]]"
      >
        <table id="table" role="treegrid" aria-multiselectable="true" tabindex="0" aria-label$="[[accessibleName]]">
          <caption id="sizer" part="row"></caption>
          <thead id="header" role="rowgroup"></thead>
          <tbody id="items" role="rowgroup"></tbody>
          <tbody id="emptystatebody">
            <tr id="emptystaterow">
              <td part="empty-state" id="emptystatecell" tabindex="0">
                <slot name="empty-state" id="emptystateslot"></slot>
              </td>
            </tr>
          </tbody>
          <tfoot id="footer" role="rowgroup"></tfoot>
        </table>

        <div part="reorder-ghost"></div>
      </div>

      <slot name="tooltip"></slot>

      <div id="focusexit" tabindex="0"></div>
    `;
  }
  static get is() {
    return "vaadin-grid";
  }
};
defineCustomElement(Grid);

// node_modules/@vaadin/grid/src/vaadin-grid-column-group-mixin.js
var GridColumnGroupMixin = (superClass) => class extends ColumnBaseMixin(superClass) {
  static get properties() {
    return {
      /** @private */
      _childColumns: {
        value() {
          return this._getChildColumns(this);
        }
      },
      /**
       * Flex grow ratio for the column group as the sum of the ratios of its child columns.
       * @attr {number} flex-grow
       */
      flexGrow: {
        type: Number,
        readOnly: true,
        sync: true
      },
      /**
       * Width of the column group as the sum of the widths of its child columns.
       */
      width: {
        type: String,
        readOnly: true,
        sync: true
      },
      /** @private */
      _visibleChildColumns: Array,
      /** @private */
      _colSpan: Number,
      /** @private */
      _rootColumns: Array
    };
  }
  static get observers() {
    return [
      "_groupFrozenChanged(frozen, _rootColumns)",
      "_groupFrozenToEndChanged(frozenToEnd, _rootColumns)",
      "_groupHiddenChanged(hidden)",
      "_colSpanChanged(_colSpan, _headerCell, _footerCell)",
      "_groupOrderChanged(_order, _rootColumns)",
      "_groupReorderStatusChanged(_reorderStatus, _rootColumns)",
      "_groupResizableChanged(resizable, _rootColumns)"
    ];
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    this._addNodeObserver();
    this._updateFlexAndWidth();
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    if (this._observer) {
      this._observer.disconnect();
    }
  }
  /**
   * @param {string} path
   * @param {unknown=} value
   * @protected
   */
  _columnPropChanged(path, value) {
    if (path === "hidden") {
      this._preventHiddenSynchronization = true;
      this._updateVisibleChildColumns(this._childColumns);
      this._preventHiddenSynchronization = false;
    }
    if (/flexGrow|width|hidden|_childColumns/u.test(path)) {
      this._updateFlexAndWidth();
    }
    if (path === "frozen" && !this.frozen) {
      this.frozen = value;
    }
    if (path === "lastFrozen" && !this._lastFrozen) {
      this._lastFrozen = value;
    }
    if (path === "frozenToEnd" && !this.frozenToEnd) {
      this.frozenToEnd = value;
    }
    if (path === "firstFrozenToEnd" && !this._firstFrozenToEnd) {
      this._firstFrozenToEnd = value;
    }
  }
  /** @private */
  _groupOrderChanged(order, rootColumns) {
    if (rootColumns) {
      const _rootColumns = rootColumns.slice(0);
      if (!order) {
        _rootColumns.forEach((column) => {
          column._order = 0;
        });
        return;
      }
      const trailingZeros = /(0+)$/u.exec(order).pop().length;
      const childCountDigits = ~~(Math.log(rootColumns.length) / Math.LN10) + 1;
      const scope = 10 ** (trailingZeros - childCountDigits);
      if (_rootColumns[0] && _rootColumns[0]._order) {
        _rootColumns.sort((a17, b7) => a17._order - b7._order);
      }
      updateColumnOrders(_rootColumns, scope, order);
    }
  }
  /** @private */
  _groupReorderStatusChanged(reorderStatus, rootColumns) {
    if (reorderStatus === void 0 || rootColumns === void 0) {
      return;
    }
    rootColumns.forEach((column) => {
      column._reorderStatus = reorderStatus;
    });
  }
  /** @private */
  _groupResizableChanged(resizable, rootColumns) {
    if (resizable === void 0 || rootColumns === void 0) {
      return;
    }
    rootColumns.forEach((column) => {
      column.resizable = resizable;
    });
  }
  /** @private */
  _updateVisibleChildColumns(childColumns) {
    this._visibleChildColumns = Array.prototype.filter.call(childColumns, (col) => !col.hidden);
    this._colSpan = this._visibleChildColumns.length;
    this._updateAutoHidden();
  }
  /** @protected */
  _updateFlexAndWidth() {
    if (!this._visibleChildColumns) {
      return;
    }
    if (this._visibleChildColumns.length > 0) {
      const width = this._visibleChildColumns.reduce((prev, curr) => {
        prev += ` + ${(curr.width || "0px").replace("calc", "")}`;
        return prev;
      }, "").substring(3);
      this._setWidth(`calc(${width})`);
    } else {
      this._setWidth("0px");
    }
    this._setFlexGrow(
      Array.prototype.reduce.call(this._visibleChildColumns, (prev, curr) => prev + curr.flexGrow, 0)
    );
  }
  /**
   * This method is called before the group's frozen value is being propagated to the child columns.
   * In case some of the child columns are frozen, while others are not, the non-frozen ones
   * will get automatically frozen as well. As this may sometimes be unintended, this method
   * shows a warning in the console in such cases.
   * @private
   */
  __scheduleAutoFreezeWarning(columns, frozenProp) {
    if (this._grid) {
      const frozenAttr = frozenProp.replace(/([A-Z])/gu, "-$1").toLowerCase();
      const firstColumnFrozen = columns[0][frozenProp] || columns[0].hasAttribute(frozenAttr);
      const allSameFrozen = columns.every((column) => {
        return (column[frozenProp] || column.hasAttribute(frozenAttr)) === firstColumnFrozen;
      });
      if (!allSameFrozen) {
        this._grid.__autoFreezeWarningDebouncer = Debouncer.debounce(
          this._grid.__autoFreezeWarningDebouncer,
          animationFrame,
          () => {
            console.warn(
              `WARNING: Joining ${frozenProp} and non-${frozenProp} Grid columns inside the same column group! This will automatically freeze all the joined columns to avoid rendering issues. If this was intentional, consider marking each joined column explicitly as ${frozenProp}. Otherwise, exclude the ${frozenProp} columns from the joined group.`
            );
          }
        );
      }
    }
  }
  /** @private */
  _groupFrozenChanged(frozen, rootColumns) {
    if (rootColumns === void 0 || frozen === void 0) {
      return;
    }
    if (frozen !== false) {
      this.__scheduleAutoFreezeWarning(rootColumns, "frozen");
      Array.from(rootColumns).forEach((col) => {
        col.frozen = frozen;
      });
    }
  }
  /** @private */
  _groupFrozenToEndChanged(frozenToEnd, rootColumns) {
    if (rootColumns === void 0 || frozenToEnd === void 0) {
      return;
    }
    if (frozenToEnd !== false) {
      this.__scheduleAutoFreezeWarning(rootColumns, "frozenToEnd");
      Array.from(rootColumns).forEach((col) => {
        col.frozenToEnd = frozenToEnd;
      });
    }
  }
  /** @private */
  _groupHiddenChanged(hidden) {
    if (hidden || this.__groupHiddenInitialized) {
      this._synchronizeHidden();
    }
    this.__groupHiddenInitialized = true;
  }
  /** @private */
  _updateAutoHidden() {
    const wasAutoHidden = this._autoHidden;
    this._autoHidden = (this._visibleChildColumns || []).length === 0;
    if (wasAutoHidden || this._autoHidden) {
      this.hidden = this._autoHidden;
    }
  }
  /** @private */
  _synchronizeHidden() {
    if (this._childColumns && !this._preventHiddenSynchronization) {
      this._childColumns.forEach((column) => {
        column.hidden = this.hidden;
      });
    }
  }
  /** @private */
  _colSpanChanged(colSpan, headerCell, footerCell) {
    if (headerCell) {
      headerCell.setAttribute("colspan", colSpan);
      if (this._grid) {
        this._grid._a11yUpdateCellColspan(headerCell, colSpan);
      }
    }
    if (footerCell) {
      footerCell.setAttribute("colspan", colSpan);
      if (this._grid) {
        this._grid._a11yUpdateCellColspan(footerCell, colSpan);
      }
    }
  }
  /**
   * @param {!GridColumnGroup} el
   * @return {!Array<!GridColumn>}
   * @protected
   */
  _getChildColumns(el) {
    return ColumnObserver.getColumns(el);
  }
  /** @private */
  _addNodeObserver() {
    this._observer = new ColumnObserver(this, () => {
      this._preventHiddenSynchronization = true;
      this._rootColumns = this._getChildColumns(this);
      this._childColumns = this._rootColumns;
      this._updateVisibleChildColumns(this._childColumns);
      this._preventHiddenSynchronization = false;
      if (this._grid && this._grid._debounceUpdateColumnTree) {
        this._grid._debounceUpdateColumnTree();
      }
    });
    this._observer.flush();
  }
  /**
   * @param {!Node} node
   * @return {boolean}
   * @protected
   */
  _isColumnElement(node) {
    return node.nodeType === Node.ELEMENT_NODE && /\bcolumn\b/u.test(node.localName);
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-column-group.js
var GridColumnGroup = class extends GridColumnGroupMixin(PolymerElement) {
  static get is() {
    return "vaadin-grid-column-group";
  }
};
defineCustomElement(GridColumnGroup);

// node_modules/@vaadin/checkbox/theme/lumo/vaadin-checkbox-styles.js
registerStyles(
  "vaadin-checkbox",
  css`
    :host {
      color: var(--vaadin-checkbox-label-color, var(--lumo-body-text-color));
      font-size: var(--vaadin-checkbox-label-font-size, var(--lumo-font-size-m));
      font-family: var(--lumo-font-family);
      line-height: var(--lumo-line-height-s);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      -webkit-tap-highlight-color: transparent;
      -webkit-user-select: none;
      user-select: none;
      cursor: default;
      outline: none;
      --_checkbox-size: var(--vaadin-checkbox-size, calc(var(--lumo-size-m) / 2));
      --_focus-ring-color: var(--vaadin-focus-ring-color, var(--lumo-primary-color-50pct));
      --_focus-ring-width: var(--vaadin-focus-ring-width, 2px);
      --_selection-color: var(--vaadin-selection-color, var(--lumo-primary-color));
      --_invalid-background: var(--vaadin-input-field-invalid-background, var(--lumo-error-color-10pct));
      --_disabled-checkmark-color: var(--vaadin-checkbox-disabled-checkmark-color, var(--lumo-contrast-30pct));
    }

    [part='label'] {
      display: flex;
      position: relative;
      max-width: max-content;
    }

    :host([has-label]) ::slotted(label) {
      padding: var(
        --vaadin-checkbox-label-padding,
        var(--lumo-space-xs) var(--lumo-space-s) var(--lumo-space-xs) var(--lumo-space-xs)
      );
    }

    :host([dir='rtl'][has-label]) ::slotted(label) {
      padding: var(--lumo-space-xs) var(--lumo-space-xs) var(--lumo-space-xs) var(--lumo-space-s);
    }

    :host([has-label][required]) ::slotted(label) {
      padding-inline-end: var(--lumo-space-m);
    }

    [part='checkbox'] {
      width: var(--_checkbox-size);
      height: var(--_checkbox-size);
      margin: var(--lumo-space-xs);
      position: relative;
      border-radius: var(--vaadin-checkbox-border-radius, var(--lumo-border-radius-s));
      background: var(--vaadin-checkbox-background, var(--lumo-contrast-20pct));
      transition:
        transform 0.2s cubic-bezier(0.12, 0.32, 0.54, 2),
        background-color 0.15s;
      cursor: var(--lumo-clickable-cursor);
      /* Default field border color */
      --_input-border-color: var(--vaadin-input-field-border-color, var(--lumo-contrast-50pct));
    }

    :host([indeterminate]),
    :host([checked]) {
      --vaadin-input-field-border-color: transparent;
    }

    :host([indeterminate]) [part='checkbox'],
    :host([checked]) [part='checkbox'] {
      background-color: var(--_selection-color);
    }

    /* Checkmark */
    [part='checkbox']::after {
      pointer-events: none;
      font-family: 'lumo-icons';
      content: var(--vaadin-checkbox-checkmark-char, var(--lumo-icons-checkmark));
      color: var(--vaadin-checkbox-checkmark-color, var(--lumo-primary-contrast-color));
      font-size: var(--vaadin-checkbox-checkmark-size, calc(var(--_checkbox-size) + 2px));
      line-height: 1;
      position: absolute;
      top: -1px;
      left: -1px;
      contain: content;
      opacity: 0;
    }

    :host([checked]) [part='checkbox']::after {
      opacity: 1;
    }

    :host([readonly]:not([checked]):not([indeterminate])) {
      color: var(--lumo-secondary-text-color);
    }

    :host([readonly]:not([checked]):not([indeterminate])) [part='checkbox'] {
      background: transparent;
      box-shadow: none;
    }

    :host([readonly]:not([checked]):not([indeterminate])) [part='checkbox']::after {
      content: '';
      box-sizing: border-box;
      width: 100%;
      height: 100%;
      border-radius: inherit;
      top: 0;
      left: 0;
      opacity: 1;
      border: var(--vaadin-input-field-readonly-border, 1px dashed var(--lumo-contrast-50pct));
    }

    /* Indeterminate checkmark */
    :host([indeterminate]) [part='checkbox']::after {
      content: var(--vaadin-checkbox-checkmark-char-indeterminate, '');
      opacity: 1;
      top: 45%;
      height: 10%;
      left: 22%;
      right: 22%;
      width: auto;
      border: 0;
      background-color: var(--lumo-primary-contrast-color);
    }

    /* Focus ring */
    :host([focus-ring]) [part='checkbox'] {
      box-shadow:
        0 0 0 1px var(--lumo-base-color),
        0 0 0 calc(var(--_focus-ring-width) + 1px) var(--_focus-ring-color),
        inset 0 0 0 var(--_input-border-width, 0) var(--_input-border-color);
    }

    :host([focus-ring][readonly]:not([checked]):not([indeterminate])) [part='checkbox'] {
      box-shadow:
        0 0 0 1px var(--lumo-base-color),
        0 0 0 calc(var(--_focus-ring-width) + 1px) var(--_focus-ring-color);
    }

    /* Disabled */
    :host([disabled]) {
      pointer-events: none;
      --vaadin-input-field-border-color: var(--lumo-contrast-20pct);
    }

    :host([disabled]) ::slotted(label) {
      color: inherit;
    }

    :host([disabled]) [part='checkbox'] {
      background-color: var(--vaadin-checkbox-disabled-background, var(--lumo-contrast-10pct));
    }

    :host([disabled]) [part='checkbox']::after {
      color: var(--_disabled-checkmark-color);
    }

    :host([disabled]) [part='label'],
    :host([disabled]) [part='helper-text'] {
      color: var(--lumo-disabled-text-color);
      -webkit-text-fill-color: var(--lumo-disabled-text-color);
    }

    :host([indeterminate][disabled]) [part='checkbox']::after {
      background-color: var(--_disabled-checkmark-color);
    }

    :host([readonly][checked]:not([disabled])) [part='checkbox'],
    :host([readonly][indeterminate]:not([disabled])) [part='checkbox'] {
      background-color: var(--vaadin-checkbox-readonly-checked-background, var(--lumo-contrast-70pct));
    }

    /* Used for activation "halo" */
    [part='checkbox']::before {
      pointer-events: none;
      color: transparent;
      width: 100%;
      height: 100%;
      line-height: var(--_checkbox-size);
      border-radius: inherit;
      background-color: inherit;
      transform: scale(1.4);
      opacity: 0;
      transition:
        transform 0.1s,
        opacity 0.8s;
    }

    /* Hover */
    :host(:not([checked]):not([indeterminate]):not([disabled]):not([readonly]):not([invalid]):hover) [part='checkbox'] {
      background: var(--vaadin-checkbox-background-hover, var(--lumo-contrast-30pct));
    }

    /* Disable hover for touch devices */
    @media (pointer: coarse) {
      /* prettier-ignore */
      :host(:not([checked]):not([indeterminate]):not([disabled]):not([readonly]):not([invalid]):hover) [part='checkbox'] {
        background: var(--vaadin-checkbox-background, var(--lumo-contrast-20pct));
      }
    }

    /* Active */
    :host([active]) [part='checkbox'] {
      transform: scale(0.9);
      transition-duration: 0.05s;
    }

    :host([active][checked]) [part='checkbox'] {
      transform: scale(1.1);
    }

    :host([active]:not([checked])) [part='checkbox']::before {
      transition-duration: 0.01s, 0.01s;
      transform: scale(0);
      opacity: 0.4;
    }

    /* Required */
    :host([required]) [part='required-indicator'] {
      position: absolute;
      top: var(--lumo-space-xs);
      right: var(--lumo-space-xs);
    }

    :host([required][dir='rtl']) [part='required-indicator'] {
      right: auto;
      left: var(--lumo-space-xs);
    }

    :host([required]) [part='required-indicator']::after {
      content: var(--lumo-required-field-indicator, '\\2022');
      transition: opacity 0.2s;
      color: var(--lumo-required-field-indicator-color, var(--lumo-primary-text-color));
      width: 1em;
      text-align: center;
    }

    :host(:not([has-label])) [part='required-indicator'] {
      display: none;
    }

    /* Invalid */
    :host([invalid]) {
      --vaadin-input-field-border-color: var(--lumo-error-color);
    }

    :host([invalid]) [part='checkbox'] {
      background: var(--_invalid-background);
      background-image: linear-gradient(var(--_invalid-background) 0%, var(--_invalid-background) 100%);
    }

    :host([invalid]:hover) [part='checkbox'] {
      background-image: linear-gradient(var(--_invalid-background) 0%, var(--_invalid-background) 100%),
        linear-gradient(var(--_invalid-background) 0%, var(--_invalid-background) 100%);
    }

    :host([invalid][focus-ring]) {
      --_focus-ring-color: var(--lumo-error-color-50pct);
    }

    :host([invalid]) [part='required-indicator']::after {
      color: var(--lumo-required-field-indicator-color, var(--lumo-error-text-color));
    }

    /* Error message */
    [part='error-message'] {
      font-size: var(--vaadin-input-field-error-font-size, var(--lumo-font-size-xs));
      line-height: var(--lumo-line-height-xs);
      font-weight: var(--vaadin-input-field-error-font-weight, 400);
      color: var(--vaadin-input-field-error-color, var(--lumo-error-text-color));
      will-change: max-height;
      transition: 0.4s max-height;
      max-height: 5em;
      padding-inline-start: var(--lumo-space-xs);
    }

    :host([has-error-message]) [part='error-message']::after,
    :host([has-helper]) [part='helper-text']::after {
      content: '';
      display: block;
      height: 0.4em;
    }

    :host(:not([invalid])) [part='error-message'] {
      max-height: 0;
      overflow: hidden;
    }

    /* Helper */
    [part='helper-text'] {
      display: block;
      color: var(--vaadin-input-field-helper-color, var(--lumo-secondary-text-color));
      font-size: var(--vaadin-input-field-helper-font-size, var(--lumo-font-size-xs));
      line-height: var(--lumo-line-height-xs);
      font-weight: var(--vaadin-input-field-helper-font-weight, 400);
      margin-left: calc(var(--lumo-border-radius-m) / 4);
      transition: color 0.2s;
      padding-inline-start: var(--lumo-space-xs);
    }

    :host(:hover:not([readonly])) [part='helper-text'] {
      color: var(--lumo-body-text-color);
    }

    :host([has-error-message]) ::slotted(label),
    :host([has-helper]) ::slotted(label) {
      padding-bottom: 0;
    }
  `,
  { moduleId: "lumo-checkbox" }
);

// node_modules/@vaadin/component-base/src/slot-styles-mixin.js
var stylesMap = /* @__PURE__ */ new WeakMap();
function getRootStyles(root2) {
  if (!stylesMap.has(root2)) {
    stylesMap.set(root2, /* @__PURE__ */ new Set());
  }
  return stylesMap.get(root2);
}
function insertStyles(styles, root2) {
  const style2 = document.createElement("style");
  style2.textContent = styles;
  if (root2 === document) {
    document.head.appendChild(style2);
  } else {
    root2.insertBefore(style2, root2.firstChild);
  }
}
var SlotStylesMixin = dedupeMixin(
  (superclass) => class SlotStylesMixinClass extends superclass {
    /**
     * List of styles to insert into root.
     * @protected
     */
    get slotStyles() {
      return [];
    }
    /** @protected */
    connectedCallback() {
      super.connectedCallback();
      this.__applySlotStyles();
    }
    /** @private */
    __applySlotStyles() {
      const root2 = this.getRootNode();
      const rootStyles = getRootStyles(root2);
      this.slotStyles.forEach((styles) => {
        if (!rootStyles.has(styles)) {
          insertStyles(styles, root2);
          rootStyles.add(styles);
        }
      });
    }
  }
);

// node_modules/@vaadin/component-base/src/delegate-state-mixin.js
var DelegateStateMixin = dedupeMixin(
  (superclass) => class DelegateStateMixinClass extends superclass {
    static get properties() {
      return {
        /**
         * A target element to which attributes and properties are delegated.
         * @protected
         */
        stateTarget: {
          type: Object,
          observer: "_stateTargetChanged"
        }
      };
    }
    /**
     * An array of the host attributes to delegate to the target element.
     */
    static get delegateAttrs() {
      return [];
    }
    /**
     * An array of the host properties to delegate to the target element.
     */
    static get delegateProps() {
      return [];
    }
    /** @protected */
    ready() {
      super.ready();
      this._createDelegateAttrsObserver();
      this._createDelegatePropsObserver();
    }
    /** @protected */
    _stateTargetChanged(target) {
      if (target) {
        this._ensureAttrsDelegated();
        this._ensurePropsDelegated();
      }
    }
    /** @protected */
    _createDelegateAttrsObserver() {
      this._createMethodObserver(`_delegateAttrsChanged(${this.constructor.delegateAttrs.join(", ")})`);
    }
    /** @protected */
    _createDelegatePropsObserver() {
      this._createMethodObserver(`_delegatePropsChanged(${this.constructor.delegateProps.join(", ")})`);
    }
    /** @protected */
    _ensureAttrsDelegated() {
      this.constructor.delegateAttrs.forEach((name) => {
        this._delegateAttribute(name, this[name]);
      });
    }
    /** @protected */
    _ensurePropsDelegated() {
      this.constructor.delegateProps.forEach((name) => {
        this._delegateProperty(name, this[name]);
      });
    }
    /** @protected */
    _delegateAttrsChanged(...values) {
      this.constructor.delegateAttrs.forEach((name, index) => {
        this._delegateAttribute(name, values[index]);
      });
    }
    /** @protected */
    _delegatePropsChanged(...values) {
      this.constructor.delegateProps.forEach((name, index) => {
        this._delegateProperty(name, values[index]);
      });
    }
    /** @protected */
    _delegateAttribute(name, value) {
      if (!this.stateTarget) {
        return;
      }
      if (name === "invalid") {
        this._delegateAttribute("aria-invalid", value ? "true" : false);
      }
      if (typeof value === "boolean") {
        this.stateTarget.toggleAttribute(name, value);
      } else if (value) {
        this.stateTarget.setAttribute(name, value);
      } else {
        this.stateTarget.removeAttribute(name);
      }
    }
    /** @protected */
    _delegateProperty(name, value) {
      if (!this.stateTarget) {
        return;
      }
      this.stateTarget[name] = value;
    }
  }
);

// node_modules/@vaadin/field-base/src/input-mixin.js
var InputMixin = dedupeMixin(
  (superclass) => class InputMixinClass extends superclass {
    static get properties() {
      return {
        /**
         * A reference to the input element controlled by the mixin.
         * Any component implementing this mixin is expected to provide it
         * by using `this._setInputElement(input)` Polymer API.
         *
         * A typical case is using `InputController` that does this automatically.
         * However, the input element does not have to always be native <input>:
         * as an example, <vaadin-combo-box-light> accepts other components.
         *
         * @protected
         * @type {!HTMLElement}
         */
        inputElement: {
          type: Object,
          readOnly: true,
          observer: "_inputElementChanged",
          sync: true
        },
        /**
         * String used to define input type.
         * @protected
         */
        type: {
          type: String,
          readOnly: true
        },
        /**
         * The value of the field.
         */
        value: {
          type: String,
          value: "",
          observer: "_valueChanged",
          notify: true,
          sync: true
        }
      };
    }
    constructor() {
      super();
      this._boundOnInput = this._onInput.bind(this);
      this._boundOnChange = this._onChange.bind(this);
    }
    /**
     * Indicates whether the value is different from the default one.
     * Override if the `value` property has a type other than `string`.
     *
     * @protected
     */
    get _hasValue() {
      return this.value != null && this.value !== "";
    }
    /**
     * A property for accessing the input element's value.
     *
     * Override this getter if the property is different from the default `value` one.
     *
     * @protected
     * @return {string}
     */
    get _inputElementValueProperty() {
      return "value";
    }
    /**
     * The input element's value.
     *
     * @protected
     * @return {string}
     */
    get _inputElementValue() {
      return this.inputElement ? this.inputElement[this._inputElementValueProperty] : void 0;
    }
    /**
     * The input element's value.
     *
     * @protected
     */
    set _inputElementValue(value) {
      if (this.inputElement) {
        this.inputElement[this._inputElementValueProperty] = value;
      }
    }
    /**
     * Clear the value of the field.
     */
    clear() {
      this.value = "";
      this._inputElementValue = "";
    }
    /**
     * Add event listeners to the input element instance.
     * Override this method to add custom listeners.
     * @param {!HTMLElement} input
     * @protected
     */
    _addInputListeners(input) {
      input.addEventListener("input", this._boundOnInput);
      input.addEventListener("change", this._boundOnChange);
    }
    /**
     * Remove event listeners from the input element instance.
     * @param {!HTMLElement} input
     * @protected
     */
    _removeInputListeners(input) {
      input.removeEventListener("input", this._boundOnInput);
      input.removeEventListener("change", this._boundOnChange);
    }
    /**
     * A method to forward the value property set on the field
     * programmatically back to the input element value.
     * Override this method to perform additional checks,
     * for example to skip this in certain conditions.
     * @param {string} value
     * @protected
     */
    _forwardInputValue(value) {
      if (!this.inputElement) {
        return;
      }
      this._inputElementValue = value != null ? value : "";
    }
    /**
     * @param {HTMLElement | undefined} input
     * @param {HTMLElement | undefined} oldInput
     * @protected
     */
    _inputElementChanged(input, oldInput) {
      if (input) {
        this._addInputListeners(input);
      } else if (oldInput) {
        this._removeInputListeners(oldInput);
      }
    }
    /**
     * An input event listener used to update the field value.
     *
     * @param {Event} event
     * @protected
     */
    _onInput(event) {
      const target = event.composedPath()[0];
      this.__userInput = event.isTrusted;
      this.value = target.value;
      this.__userInput = false;
    }
    /**
     * A change event listener.
     * Override this method with an actual implementation.
     * @param {Event} _event
     * @protected
     */
    _onChange(_event) {
    }
    /**
     * Toggle the has-value attribute based on the value property.
     *
     * @param {boolean} hasValue
     * @protected
     */
    _toggleHasValue(hasValue) {
      this.toggleAttribute("has-value", hasValue);
    }
    /**
     * Observer called when a value property changes.
     * @param {string | undefined} newVal
     * @param {string | undefined} oldVal
     * @protected
     */
    _valueChanged(newVal, oldVal) {
      this._toggleHasValue(this._hasValue);
      if (newVal === "" && oldVal === void 0) {
        return;
      }
      if (this.__userInput) {
        return;
      }
      this._forwardInputValue(newVal);
    }
  }
);

// node_modules/@vaadin/field-base/src/checked-mixin.js
var CheckedMixin = dedupeMixin(
  (superclass) => class CheckedMixinClass extends DelegateStateMixin(DisabledMixin(InputMixin(superclass))) {
    static get properties() {
      return {
        /**
         * True if the element is checked.
         * @type {boolean}
         */
        checked: {
          type: Boolean,
          value: false,
          notify: true,
          reflectToAttribute: true,
          sync: true
        }
      };
    }
    static get delegateProps() {
      return [...super.delegateProps, "checked"];
    }
    /**
     * @param {Event} event
     * @protected
     * @override
     */
    _onChange(event) {
      const input = event.target;
      this._toggleChecked(input.checked);
    }
    /** @protected */
    _toggleChecked(checked) {
      this.checked = checked;
    }
  }
);

// node_modules/@vaadin/component-base/src/slot-child-observe-controller.js
var SlotChildObserveController = class extends SlotController {
  constructor(host, slot, tagName, config = {}) {
    super(host, slot, tagName, { ...config, useUniqueId: true });
  }
  /**
   * Override to initialize the newly added custom node.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initCustomNode(node) {
    this.__updateNodeId(node);
    this.__notifyChange(node);
  }
  /**
   * Override to notify the controller host about removal of
   * the custom node, and to apply the default one if needed.
   *
   * @param {Node} _node
   * @protected
   * @override
   */
  teardownNode(_node) {
    const node = this.getSlotChild();
    if (node && node !== this.defaultNode) {
      this.__notifyChange(node);
    } else {
      this.restoreDefaultNode();
      this.updateDefaultNode(this.node);
    }
  }
  /**
   * Override method inherited from `SlotMixin`
   * to set ID attribute on the default node.
   *
   * @return {Node}
   * @protected
   * @override
   */
  attachDefaultNode() {
    const node = super.attachDefaultNode();
    if (node) {
      this.__updateNodeId(node);
    }
    return node;
  }
  /**
   * Override to restore default node when a custom one is removed.
   *
   * @protected
   */
  restoreDefaultNode() {
  }
  /**
   * Override to update default node text on property change.
   *
   * @param {Node} node
   * @protected
   */
  updateDefaultNode(node) {
    this.__notifyChange(node);
  }
  /**
   * Setup the mutation observer on the node to update ID and notify host.
   * Node doesn't get observed automatically until this method is called.
   *
   * @param {Node} node
   * @protected
   */
  observeNode(node) {
    if (this.__nodeObserver) {
      this.__nodeObserver.disconnect();
    }
    this.__nodeObserver = new MutationObserver((mutations) => {
      mutations.forEach((mutation) => {
        const target = mutation.target;
        const isCurrentNodeMutation = target === this.node;
        if (mutation.type === "attributes") {
          if (isCurrentNodeMutation) {
            this.__updateNodeId(target);
          }
        } else if (isCurrentNodeMutation || target.parentElement === this.node) {
          this.__notifyChange(this.node);
        }
      });
    });
    this.__nodeObserver.observe(node, {
      attributes: true,
      attributeFilter: ["id"],
      childList: true,
      subtree: true,
      characterData: true
    });
  }
  /**
   * Returns true if a node is an HTML element with children,
   * or is a defined custom element, or has non-empty text.
   *
   * @param {Node} node
   * @return {boolean}
   * @private
   */
  __hasContent(node) {
    if (!node) {
      return false;
    }
    return node.nodeType === Node.ELEMENT_NODE && (customElements.get(node.localName) || node.children.length > 0) || node.textContent && node.textContent.trim() !== "";
  }
  /**
   * Fire an event to notify the controller host about node changes.
   *
   * @param {Node} node
   * @private
   */
  __notifyChange(node) {
    this.dispatchEvent(
      new CustomEvent("slot-content-changed", {
        detail: { hasContent: this.__hasContent(node), node }
      })
    );
  }
  /**
   * Set default ID on the node in case it is an HTML element.
   *
   * @param {Node} node
   * @private
   */
  __updateNodeId(node) {
    const isFirstNode = !this.nodes || node === this.nodes[0];
    if (node.nodeType === Node.ELEMENT_NODE && (!this.multiple || isFirstNode) && !node.id) {
      node.id = this.defaultId;
    }
  }
};

// node_modules/@vaadin/field-base/src/error-controller.js
var ErrorController = class extends SlotChildObserveController {
  constructor(host) {
    super(host, "error-message", "div");
  }
  /**
   * Set the error message element text content.
   *
   * @param {string} errorMessage
   */
  setErrorMessage(errorMessage) {
    this.errorMessage = errorMessage;
    this.updateDefaultNode(this.node);
  }
  /**
   * Set invalid state for detecting whether to show error message.
   *
   * @param {boolean} invalid
   */
  setInvalid(invalid) {
    this.invalid = invalid;
    this.updateDefaultNode(this.node);
  }
  /**
   * Override method inherited from `SlotController` to not run
   * initializer on the custom slotted node unnecessarily.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initAddedNode(node) {
    if (node !== this.defaultNode) {
      this.initCustomNode(node);
    }
  }
  /**
   * Override to initialize the newly added default error message.
   *
   * @param {Node} errorNode
   * @protected
   * @override
   */
  initNode(errorNode) {
    this.updateDefaultNode(errorNode);
  }
  /**
   * Override to initialize the newly added custom error message.
   *
   * @param {Node} errorNode
   * @protected
   * @override
   */
  initCustomNode(errorNode) {
    if (errorNode.textContent && !this.errorMessage) {
      this.errorMessage = errorNode.textContent.trim();
    }
    super.initCustomNode(errorNode);
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to restore the default error message element.
   *
   * @protected
   * @override
   */
  restoreDefaultNode() {
    this.attachDefaultNode();
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to update the error message text and hidden state.
   *
   * Note: unlike with other controllers, this method is
   * called for both default and custom error message.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  updateDefaultNode(errorNode) {
    const { errorMessage, invalid } = this;
    const hasError = Boolean(invalid && errorMessage && errorMessage.trim() !== "");
    if (errorNode) {
      errorNode.textContent = hasError ? errorMessage : "";
      errorNode.hidden = !hasError;
      if (hasError) {
        announce(errorMessage, { mode: "assertive" });
      }
    }
    super.updateDefaultNode(errorNode);
  }
};

// node_modules/@vaadin/field-base/src/helper-controller.js
var HelperController = class extends SlotChildObserveController {
  constructor(host) {
    super(host, "helper", null);
  }
  /**
   * Set helper text based on corresponding host property.
   *
   * @param {string} helperText
   */
  setHelperText(helperText) {
    this.helperText = helperText;
    const helperNode = this.getSlotChild();
    if (!helperNode) {
      this.restoreDefaultNode();
    }
    if (this.node === this.defaultNode) {
      this.updateDefaultNode(this.node);
    }
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to create the default helper element lazily as needed.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  restoreDefaultNode() {
    const { helperText } = this;
    if (helperText && helperText.trim() !== "") {
      this.tagName = "div";
      const helperNode = this.attachDefaultNode();
      this.observeNode(helperNode);
    }
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to update the default helper element text content.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  updateDefaultNode(node) {
    if (node) {
      node.textContent = this.helperText;
    }
    super.updateDefaultNode(node);
  }
  /**
   * Override to observe the newly added custom node.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initCustomNode(node) {
    super.initCustomNode(node);
    this.observeNode(node);
  }
};

// node_modules/@vaadin/field-base/src/label-controller.js
var LabelController = class extends SlotChildObserveController {
  constructor(host) {
    super(host, "label", "label");
  }
  /**
   * Set label based on corresponding host property.
   *
   * @param {string} label
   */
  setLabel(label) {
    this.label = label;
    const labelNode = this.getSlotChild();
    if (!labelNode) {
      this.restoreDefaultNode();
    }
    if (this.node === this.defaultNode) {
      this.updateDefaultNode(this.node);
    }
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to restore and observe the default label element.
   *
   * @protected
   * @override
   */
  restoreDefaultNode() {
    const { label } = this;
    if (label && label.trim() !== "") {
      const labelNode = this.attachDefaultNode();
      this.observeNode(labelNode);
    }
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to update the default label element text content.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  updateDefaultNode(node) {
    if (node) {
      node.textContent = this.label;
    }
    super.updateDefaultNode(node);
  }
  /**
   * Override to observe the newly added custom node.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initCustomNode(node) {
    super.initCustomNode(node);
    this.observeNode(node);
  }
};

// node_modules/@vaadin/field-base/src/label-mixin.js
var LabelMixin = dedupeMixin(
  (superclass) => class LabelMixinClass extends ControllerMixin(superclass) {
    static get properties() {
      return {
        /**
         * The label text for the input node.
         * When no light dom defined via [slot=label], this value will be used.
         */
        label: {
          type: String,
          observer: "_labelChanged"
        }
      };
    }
    constructor() {
      super();
      this._labelController = new LabelController(this);
      this._labelController.addEventListener("slot-content-changed", (event) => {
        this.toggleAttribute("has-label", event.detail.hasContent);
      });
    }
    /** @protected */
    get _labelId() {
      const node = this._labelNode;
      return node && node.id;
    }
    /** @protected */
    get _labelNode() {
      return this._labelController.node;
    }
    /** @protected */
    ready() {
      super.ready();
      this.addController(this._labelController);
    }
    /** @protected */
    _labelChanged(label) {
      this._labelController.setLabel(label);
    }
  }
);

// node_modules/@vaadin/field-base/src/validate-mixin.js
var ValidateMixin = dedupeMixin(
  (superclass) => class ValidateMixinClass extends superclass {
    static get properties() {
      return {
        /**
         * Set to true when the field is invalid.
         */
        invalid: {
          type: Boolean,
          reflectToAttribute: true,
          notify: true,
          value: false,
          sync: true
        },
        /**
         * Set to true to enable manual validation mode. This mode disables automatic
         * constraint validation, allowing you to control the validation process yourself.
         * You can still trigger constraint validation manually with the `validate()` method
         * or use `checkValidity()` to assess the component's validity without affecting
         * the invalid state. In manual validation mode, you can also manipulate
         * the `invalid` property directly through your application logic without conflicts
         * with the component's internal validation.
         *
         * @attr {boolean} manual-validation
         */
        manualValidation: {
          type: Boolean,
          value: false
        },
        /**
         * Specifies that the user must fill in a value.
         */
        required: {
          type: Boolean,
          reflectToAttribute: true,
          sync: true
        }
      };
    }
    /**
     * Validates the field and sets the `invalid` property based on the result.
     *
     * The method fires a `validated` event with the result of the validation.
     *
     * @return {boolean} True if the value is valid.
     */
    validate() {
      const isValid = this.checkValidity();
      this._setInvalid(!isValid);
      this.dispatchEvent(new CustomEvent("validated", { detail: { valid: isValid } }));
      return isValid;
    }
    /**
     * Returns true if the field value satisfies all constraints (if any).
     *
     * @return {boolean}
     */
    checkValidity() {
      return !this.required || !!this.value;
    }
    /**
     * @param {boolean} invalid
     * @protected
     */
    _setInvalid(invalid) {
      if (this._shouldSetInvalid(invalid)) {
        this.invalid = invalid;
      }
    }
    /**
     * Override this method to define whether the given `invalid` state should be set.
     *
     * @param {boolean} _invalid
     * @return {boolean}
     * @protected
     */
    _shouldSetInvalid(_invalid) {
      return true;
    }
    /** @protected */
    _requestValidation() {
      if (!this.manualValidation) {
        this.validate();
      }
    }
    /**
     * Fired whenever the field is validated.
     *
     * @event validated
     * @param {Object} detail
     * @param {boolean} detail.valid the result of the validation.
     */
  }
);

// node_modules/@vaadin/field-base/src/field-mixin.js
var FieldMixin = (superclass) => class FieldMixinClass extends ValidateMixin(LabelMixin(ControllerMixin(superclass))) {
  static get properties() {
    return {
      /**
       * A target element to which ARIA attributes are set.
       * @protected
       */
      ariaTarget: {
        type: Object,
        observer: "_ariaTargetChanged"
      },
      /**
       * Error to show when the field is invalid.
       *
       * @attr {string} error-message
       */
      errorMessage: {
        type: String,
        observer: "_errorMessageChanged"
      },
      /**
       * String used for the helper text.
       * @attr {string} helper-text
       */
      helperText: {
        type: String,
        observer: "_helperTextChanged"
      },
      /**
       * String used to label the component to screen reader users.
       * @attr {string} accessible-name
       */
      accessibleName: {
        type: String,
        observer: "_accessibleNameChanged"
      },
      /**
       * Id of the element used as label of the component to screen reader users.
       * @attr {string} accessible-name-ref
       */
      accessibleNameRef: {
        type: String,
        observer: "_accessibleNameRefChanged"
      }
    };
  }
  static get observers() {
    return ["_invalidChanged(invalid)", "_requiredChanged(required)"];
  }
  constructor() {
    super();
    this._fieldAriaController = new FieldAriaController(this);
    this._helperController = new HelperController(this);
    this._errorController = new ErrorController(this);
    this._errorController.addEventListener("slot-content-changed", (event) => {
      this.toggleAttribute("has-error-message", event.detail.hasContent);
    });
    this._labelController.addEventListener("slot-content-changed", (event) => {
      const { hasContent, node } = event.detail;
      this.__labelChanged(hasContent, node);
    });
    this._helperController.addEventListener("slot-content-changed", (event) => {
      const { hasContent, node } = event.detail;
      this.toggleAttribute("has-helper", hasContent);
      this.__helperChanged(hasContent, node);
    });
  }
  /**
   * @protected
   * @return {HTMLElement}
   */
  get _errorNode() {
    return this._errorController.node;
  }
  /**
   * @protected
   * @return {HTMLElement}
   */
  get _helperNode() {
    return this._helperController.node;
  }
  /** @protected */
  ready() {
    super.ready();
    this.addController(this._fieldAriaController);
    this.addController(this._helperController);
    this.addController(this._errorController);
  }
  /** @private */
  __helperChanged(hasHelper, helperNode) {
    if (hasHelper) {
      this._fieldAriaController.setHelperId(helperNode.id);
    } else {
      this._fieldAriaController.setHelperId(null);
    }
  }
  /** @protected */
  _accessibleNameChanged(accessibleName) {
    this._fieldAriaController.setAriaLabel(accessibleName);
  }
  /** @protected */
  _accessibleNameRefChanged(accessibleNameRef) {
    this._fieldAriaController.setLabelId(accessibleNameRef, true);
  }
  /** @private */
  __labelChanged(hasLabel, labelNode) {
    if (hasLabel) {
      this._fieldAriaController.setLabelId(labelNode.id);
    } else {
      this._fieldAriaController.setLabelId(null);
    }
  }
  /**
   * @param {string | null | undefined} errorMessage
   * @protected
   */
  _errorMessageChanged(errorMessage) {
    this._errorController.setErrorMessage(errorMessage);
  }
  /**
   * @param {string} helperText
   * @protected
   */
  _helperTextChanged(helperText) {
    this._helperController.setHelperText(helperText);
  }
  /**
   * @param {HTMLElement | null | undefined} target
   * @protected
   */
  _ariaTargetChanged(target) {
    if (target) {
      this._fieldAriaController.setTarget(target);
    }
  }
  /**
   * @param {boolean} required
   * @protected
   */
  _requiredChanged(required) {
    this._fieldAriaController.setRequired(required);
  }
  /**
   * @param {boolean} invalid
   * @protected
   */
  _invalidChanged(invalid) {
    this._errorController.setInvalid(invalid);
    setTimeout(() => {
      if (invalid) {
        const node = this._errorNode;
        this._fieldAriaController.setErrorId(node && node.id);
      } else {
        this._fieldAriaController.setErrorId(null);
      }
    });
  }
};

// node_modules/@vaadin/field-base/src/input-controller.js
var InputController = class extends SlotController {
  constructor(host, callback, options = {}) {
    const { uniqueIdPrefix } = options;
    super(host, "input", "input", {
      initializer: (node, host2) => {
        if (host2.value) {
          node.value = host2.value;
        }
        if (host2.type) {
          node.setAttribute("type", host2.type);
        }
        node.id = this.defaultId;
        if (typeof callback === "function") {
          callback(node);
        }
      },
      useUniqueId: true,
      uniqueIdPrefix
    });
  }
};

// node_modules/@vaadin/field-base/src/labelled-input-controller.js
var LabelledInputController = class {
  constructor(input, labelController) {
    this.input = input;
    this.__preventDuplicateLabelClick = this.__preventDuplicateLabelClick.bind(this);
    labelController.addEventListener("slot-content-changed", (event) => {
      this.__initLabel(event.detail.node);
    });
    this.__initLabel(labelController.node);
  }
  /**
   * @param {HTMLElement} label
   * @private
   */
  __initLabel(label) {
    if (label) {
      label.addEventListener("click", this.__preventDuplicateLabelClick);
      if (this.input) {
        label.setAttribute("for", this.input.id);
      }
    }
  }
  /**
   * The native platform fires an event for both the click on the label, and also
   * the subsequent click on the native input element caused by label click.
   * This results in two click events arriving at the host, but we only want one.
   * This method prevents the duplicate click and ensures the correct isTrusted event
   * with the correct event.target arrives at the host.
   * @private
   */
  __preventDuplicateLabelClick() {
    const inputClickHandler = (e17) => {
      e17.stopImmediatePropagation();
      this.input.removeEventListener("click", inputClickHandler);
    };
    this.input.addEventListener("click", inputClickHandler);
  }
};

// node_modules/@vaadin/checkbox/src/vaadin-checkbox-mixin.js
var CheckboxMixin = (superclass) => class CheckboxMixinClass extends SlotStylesMixin(
  FieldMixin(CheckedMixin(DelegateFocusMixin(ActiveMixin(superclass))))
) {
  static get properties() {
    return {
      /**
       * True if the checkbox is in the indeterminate state which means
       * it is not possible to say whether it is checked or unchecked.
       * The state is reset once the user switches the checkbox by hand.
       *
       * https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/checkbox#Indeterminate_state_checkboxes
       *
       * @type {boolean}
       */
      indeterminate: {
        type: Boolean,
        notify: true,
        value: false,
        reflectToAttribute: true
      },
      /**
       * The name of the checkbox.
       *
       * @type {string}
       */
      name: {
        type: String,
        value: ""
      },
      /**
       * When true, the user cannot modify the value of the checkbox.
       * The difference between `disabled` and `readonly` is that the
       * read-only checkbox remains focusable, is announced by screen
       * readers and its value can be submitted as part of the form.
       */
      readonly: {
        type: Boolean,
        value: false,
        reflectToAttribute: true
      }
    };
  }
  static get observers() {
    return ["__readonlyChanged(readonly, inputElement)"];
  }
  /** @override */
  static get delegateProps() {
    return [...super.delegateProps, "indeterminate"];
  }
  /** @override */
  static get delegateAttrs() {
    return [...super.delegateAttrs, "name", "invalid", "required"];
  }
  constructor() {
    super();
    this._setType("checkbox");
    this._boundOnInputClick = this._onInputClick.bind(this);
    this.value = "on";
    this.tabindex = 0;
  }
  /** @protected */
  get slotStyles() {
    const tag = this.localName;
    return [
      `
          ${tag} > input[slot='input'] {
            opacity: 0;
          }
        `
    ];
  }
  /** @protected */
  ready() {
    super.ready();
    this.addController(
      new InputController(this, (input) => {
        this._setInputElement(input);
        this._setFocusElement(input);
        this.stateTarget = input;
        this.ariaTarget = input;
      })
    );
    this.addController(new LabelledInputController(this.inputElement, this._labelController));
    this._createMethodObserver("_checkedChanged(checked)");
  }
  /**
   * Override method inherited from `ActiveMixin` to prevent setting `active`
   * attribute when readonly, or when clicking a link placed inside the label,
   * or when clicking slotted helper or error message element.
   *
   * @param {Event} event
   * @return {boolean}
   * @protected
   * @override
   */
  _shouldSetActive(event) {
    if (this.readonly || event.target.localName === "a" || event.target === this._helperNode || event.target === this._errorNode) {
      return false;
    }
    return super._shouldSetActive(event);
  }
  /**
   * Override method inherited from `InputMixin`.
   * @param {!HTMLElement} input
   * @protected
   * @override
   */
  _addInputListeners(input) {
    super._addInputListeners(input);
    input.addEventListener("click", this._boundOnInputClick);
  }
  /**
   * Override method inherited from `InputMixin`.
   * @param {!HTMLElement} input
   * @protected
   * @override
   */
  _removeInputListeners(input) {
    super._removeInputListeners(input);
    input.removeEventListener("click", this._boundOnInputClick);
  }
  /** @private */
  _onInputClick(event) {
    if (this.readonly) {
      event.preventDefault();
    }
  }
  /** @private */
  __readonlyChanged(readonly, inputElement) {
    if (!inputElement) {
      return;
    }
    if (readonly) {
      inputElement.setAttribute("aria-readonly", "true");
    } else {
      inputElement.removeAttribute("aria-readonly");
    }
  }
  /**
   * Override method inherited from `CheckedMixin` to reset
   * `indeterminate` state checkbox is toggled by the user.
   *
   * @param {boolean} checked
   * @protected
   * @override
   */
  _toggleChecked(checked) {
    if (this.indeterminate) {
      this.indeterminate = false;
    }
    super._toggleChecked(checked);
  }
  /**
   * @override
   * @return {boolean}
   */
  checkValidity() {
    return !this.required || !!this.checked;
  }
  /**
   * Override method inherited from `FocusMixin` to validate on blur.
   * @param {boolean} focused
   * @protected
   */
  _setFocused(focused) {
    super._setFocused(focused);
    if (!focused && document.hasFocus()) {
      this._requestValidation();
    }
  }
  /** @private */
  _checkedChanged(checked) {
    if (checked || this.__oldChecked) {
      this._requestValidation();
    }
    this.__oldChecked = checked;
  }
  /**
   * Override an observer from `FieldMixin`
   * to validate when required is removed.
   *
   * @protected
   * @override
   */
  _requiredChanged(required) {
    super._requiredChanged(required);
    if (required === false) {
      this._requestValidation();
    }
  }
  /** @private */
  _onRequiredIndicatorClick() {
    this._labelNode.click();
  }
  /**
   * Fired when the checkbox is checked or unchecked by the user.
   *
   * @event change
   */
};

// node_modules/@vaadin/checkbox/src/vaadin-checkbox-styles.js
var checkboxStyles = css`
  :host {
    display: inline-block;
  }

  :host([hidden]) {
    display: none !important;
  }

  :host([disabled]) {
    -webkit-tap-highlight-color: transparent;
  }

  .vaadin-checkbox-container {
    display: grid;
    grid-template-columns: auto 1fr;
    align-items: baseline;
  }

  [part='checkbox'],
  ::slotted(input),
  [part='label'] {
    grid-row: 1;
  }

  [part='checkbox'],
  ::slotted(input) {
    grid-column: 1;
  }

  [part='helper-text'],
  [part='error-message'] {
    grid-column: 2;
  }

  :host(:not([has-helper])) [part='helper-text'],
  :host(:not([has-error-message])) [part='error-message'] {
    display: none;
  }

  [part='checkbox'] {
    width: var(--vaadin-checkbox-size, 1em);
    height: var(--vaadin-checkbox-size, 1em);
    --_input-border-width: var(--vaadin-input-field-border-width, 0);
    --_input-border-color: var(--vaadin-input-field-border-color, transparent);
    box-shadow: inset 0 0 0 var(--_input-border-width, 0) var(--_input-border-color);
  }

  [part='checkbox']::before {
    display: block;
    content: '\\202F';
    line-height: var(--vaadin-checkbox-size, 1em);
    contain: paint;
  }

  /* visually hidden */
  ::slotted(input) {
    cursor: inherit;
    margin: 0;
    align-self: stretch;
    -webkit-appearance: none;
    width: initial;
    height: initial;
  }

  @media (forced-colors: active) {
    [part='checkbox'] {
      outline: 1px solid;
      outline-offset: -1px;
    }

    :host([disabled]) [part='checkbox'],
    :host([disabled]) [part='checkbox']::after {
      outline-color: GrayText;
    }

    :host(:is([checked], [indeterminate])) [part='checkbox']::after {
      outline: 1px solid;
      outline-offset: -1px;
      border-radius: inherit;
    }

    :host([focused]) [part='checkbox'],
    :host([focused]) [part='checkbox']::after {
      outline-width: 2px;
    }
  }
`;

// node_modules/@vaadin/checkbox/src/vaadin-checkbox.js
registerStyles("vaadin-checkbox", checkboxStyles, { moduleId: "vaadin-checkbox-styles" });
var Checkbox = class extends CheckboxMixin(ElementMixin2(ThemableMixin(PolymerElement))) {
  static get is() {
    return "vaadin-checkbox";
  }
  static get template() {
    return html`
      <div class="vaadin-checkbox-container">
        <div part="checkbox" aria-hidden="true"></div>
        <slot name="input"></slot>
        <div part="label">
          <slot name="label"></slot>
          <div part="required-indicator" on-click="_onRequiredIndicatorClick"></div>
        </div>
        <div part="helper-text">
          <slot name="helper"></slot>
        </div>
        <div part="error-message">
          <slot name="error-message"></slot>
        </div>
      </div>
      <slot name="tooltip"></slot>
    `;
  }
  /** @protected */
  ready() {
    super.ready();
    this._tooltipController = new TooltipController(this);
    this._tooltipController.setAriaTarget(this.inputElement);
    this.addController(this._tooltipController);
  }
};
defineCustomElement(Checkbox);

// node_modules/@vaadin/grid/src/vaadin-grid-selection-column-base-mixin.js
var GridSelectionColumnBaseMixin = (superClass) => class GridSelectionColumnBaseMixin extends superClass {
  static get properties() {
    return {
      /**
       * Width of the cells for this column.
       */
      width: {
        type: String,
        value: "58px",
        sync: true
      },
      /**
       * Override `autoWidth` to enable auto-width
       */
      autoWidth: {
        type: Boolean,
        value: true
      },
      /**
       * Flex grow ratio for the cell widths. When set to 0, cell width is fixed.
       * @attr {number} flex-grow
       * @type {number}
       */
      flexGrow: {
        type: Number,
        value: 0,
        sync: true
      },
      /**
       * When true, all the items are selected.
       * @attr {boolean} select-all
       * @type {boolean}
       */
      selectAll: {
        type: Boolean,
        value: false,
        notify: true,
        sync: true
      },
      /**
       * When true, the active item gets automatically selected.
       * @attr {boolean} auto-select
       * @type {boolean}
       */
      autoSelect: {
        type: Boolean,
        value: false,
        sync: true
      },
      /**
       * When true, rows can be selected by dragging over the selection column.
       * @attr {boolean} drag-select
       * @type {boolean}
       */
      dragSelect: {
        type: Boolean,
        value: false,
        sync: true
      },
      /** @protected */
      _indeterminate: {
        type: Boolean,
        sync: true
      },
      /** @protected */
      _selectAllHidden: Boolean,
      /**
       * Indicates whether the shift key is currently pressed.
       *
       * @protected
       */
      _shiftKeyDown: {
        type: Boolean,
        value: false
      }
    };
  }
  static get observers() {
    return [
      "_onHeaderRendererOrBindingChanged(_headerRenderer, _headerCell, path, header, selectAll, _indeterminate, _selectAllHidden)"
    ];
  }
  constructor() {
    super();
    this.__onCellTrack = this.__onCellTrack.bind(this);
    this.__onCellClick = this.__onCellClick.bind(this);
    this.__onCellMouseDown = this.__onCellMouseDown.bind(this);
    this.__onGridInteraction = this.__onGridInteraction.bind(this);
    this.__onActiveItemChanged = this.__onActiveItemChanged.bind(this);
    this.__onTouchStart = this.__onTouchStart.bind(this);
    this.__onSelectRowCheckboxChange = this.__onSelectRowCheckboxChange.bind(this);
    this.__onSelectAllCheckboxChange = this.__onSelectAllCheckboxChange.bind(this);
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    if (this._grid) {
      this._grid.addEventListener("keyup", this.__onGridInteraction);
      this._grid.addEventListener("keydown", this.__onGridInteraction, { capture: true });
      this._grid.addEventListener("mousedown", this.__onGridInteraction);
      this._grid.addEventListener("active-item-changed", this.__onActiveItemChanged);
      this._grid.addEventListener("touchstart", this.__onTouchStart);
    }
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    if (this._grid) {
      this._grid.removeEventListener("keyup", this.__onGridInteraction);
      this._grid.removeEventListener("keydown", this.__onGridInteraction, { capture: true });
      this._grid.removeEventListener("mousedown", this.__onGridInteraction);
      this._grid.removeEventListener("active-item-changed", this.__onActiveItemChanged);
      this._grid.removeEventListener("touchstart", this.__onTouchStart);
    }
  }
  /**
   * Renders the Select All checkbox to the header cell.
   *
   * @override
   */
  _defaultHeaderRenderer(root2, _column) {
    let checkbox = root2.firstElementChild;
    if (!checkbox) {
      checkbox = document.createElement("vaadin-checkbox");
      checkbox.accessibleName = "Select All";
      checkbox.classList.add("vaadin-grid-select-all-checkbox");
      checkbox.addEventListener("change", this.__onSelectAllCheckboxChange);
      root2.appendChild(checkbox);
    }
    const checked = this.__isChecked(this.selectAll, this._indeterminate);
    checkbox.checked = checked;
    checkbox.indeterminate = this._indeterminate;
    checkbox.style.visibility = this._selectAllHidden ? "hidden" : "";
  }
  /**
   * Renders the Select Row checkbox to the body cell.
   *
   * @override
   */
  _defaultRenderer(root2, _column, { item, selected }) {
    let checkbox = root2.firstElementChild;
    if (!checkbox) {
      checkbox = document.createElement("vaadin-checkbox");
      checkbox.accessibleName = "Select Row";
      checkbox.addEventListener("change", this.__onSelectRowCheckboxChange);
      root2.appendChild(checkbox);
      addListener(root2, "track", this.__onCellTrack);
      setTouchAction(root2, "pinch-zoom");
      root2.addEventListener("mousedown", this.__onCellMouseDown);
      root2.addEventListener("click", this.__onCellClick);
    }
    checkbox.__item = item;
    checkbox.checked = selected;
    const isSelectable = this._grid.__isItemSelectable(item);
    checkbox.readonly = !isSelectable;
    const isHidden = !isSelectable && !selected;
    checkbox.style.visibility = isHidden ? "hidden" : "";
  }
  /**
   * Updates the select all state when the Select All checkbox is switched.
   * The listener handles only user-fired events.
   *
   * @private
   */
  __onSelectAllCheckboxChange(e17) {
    if (this._indeterminate || e17.currentTarget.checked) {
      this._selectAll();
    } else {
      this._deselectAll();
    }
  }
  /** @private */
  __onGridInteraction(e17) {
    this._shiftKeyDown = e17.shiftKey;
    if (this.autoSelect) {
      this._grid.$.scroller.toggleAttribute("range-selecting", this._shiftKeyDown);
    }
  }
  /**
   * Selects or deselects the row when the Select Row checkbox is switched.
   * The listener handles only user-fired events.
   *
   * @private
   */
  __onSelectRowCheckboxChange(e17) {
    this.__toggleItem(e17.currentTarget.__item, e17.currentTarget.checked);
  }
  /** @private */
  __onCellTrack(event) {
    if (!this.dragSelect) {
      return;
    }
    this.__dragCurrentY = event.detail.y;
    this.__dragDy = event.detail.dy;
    if (event.detail.state === "start") {
      if (this.__multiTouchActive) {
        return;
      }
      const renderedRows = this._grid._getRenderedRows();
      const dragStartRow = renderedRows.find((row) => row.contains(event.currentTarget.assignedSlot));
      this.__selectOnDrag = !this._grid._isSelected(dragStartRow._item);
      this.__dragStartIndex = dragStartRow.index;
      this.__dragStartItem = dragStartRow._item;
      this.__dragAutoScroller();
    } else if (event.detail.state === "end") {
      if (this.__dragStartItem) {
        this.__toggleItem(this.__dragStartItem, this.__selectOnDrag);
      }
      setTimeout(() => {
        this.__dragStartIndex = void 0;
      });
    }
  }
  /** @private */
  __onCellMouseDown(e17) {
    if (this.dragSelect) {
      e17.preventDefault();
    }
  }
  /** @private */
  __onTouchStart(e17) {
    if (e17.touches.length > 1) {
      this.__multiTouchActive = true;
      this.__dragStartIndex = void 0;
      this.__dragStartItem = void 0;
    } else {
      this.__multiTouchActive = false;
    }
  }
  /** @private */
  __onCellClick(e17) {
    if (this.__dragStartIndex !== void 0) {
      e17.preventDefault();
    }
  }
  /** @private */
  _onCellKeyDown(e17) {
    const target = e17.composedPath()[0];
    if (e17.keyCode !== 32) {
      return;
    }
    if (target === this._headerCell) {
      if (this.selectAll) {
        this._deselectAll();
      } else {
        this._selectAll();
      }
    } else if (this._cells.includes(target) && !this.autoSelect) {
      const checkbox = target._content.firstElementChild;
      this.__toggleItem(checkbox.__item);
    }
  }
  /** @private */
  __onActiveItemChanged(e17) {
    const activeItem = e17.detail.value;
    if (this.autoSelect) {
      const item = activeItem || this.__previousActiveItem;
      if (item) {
        this.__toggleItem(item);
      }
    }
    this.__previousActiveItem = activeItem;
  }
  /** @private */
  __dragAutoScroller() {
    if (this.__dragStartIndex === void 0) {
      return;
    }
    const renderedRows = this._grid._getRenderedRows();
    const hoveredRow = renderedRows.find((row) => {
      const rowRect = row.getBoundingClientRect();
      return this.__dragCurrentY >= rowRect.top && this.__dragCurrentY <= rowRect.bottom;
    });
    let hoveredIndex = hoveredRow ? hoveredRow.index : void 0;
    const scrollableArea = this.__getScrollableArea();
    if (this.__dragCurrentY < scrollableArea.top) {
      hoveredIndex = this._grid._firstVisibleIndex;
    } else if (this.__dragCurrentY > scrollableArea.bottom) {
      hoveredIndex = this._grid._lastVisibleIndex;
    }
    if (hoveredIndex !== void 0) {
      renderedRows.forEach((row) => {
        if (hoveredIndex > this.__dragStartIndex && row.index >= this.__dragStartIndex && row.index <= hoveredIndex || hoveredIndex < this.__dragStartIndex && row.index <= this.__dragStartIndex && row.index >= hoveredIndex) {
          this.__toggleItem(row._item, this.__selectOnDrag);
          this.__dragStartItem = void 0;
        }
      });
    }
    const scrollTriggerArea = scrollableArea.height * 0.15;
    const maxScrollAmount = 10;
    if (this.__dragDy < 0 && this.__dragCurrentY < scrollableArea.top + scrollTriggerArea) {
      const dy = scrollableArea.top + scrollTriggerArea - this.__dragCurrentY;
      const percentage = Math.min(1, dy / scrollTriggerArea);
      this._grid.$.table.scrollTop -= percentage * maxScrollAmount;
    }
    if (this.__dragDy > 0 && this.__dragCurrentY > scrollableArea.bottom - scrollTriggerArea) {
      const dy = this.__dragCurrentY - (scrollableArea.bottom - scrollTriggerArea);
      const percentage = Math.min(1, dy / scrollTriggerArea);
      this._grid.$.table.scrollTop += percentage * maxScrollAmount;
    }
    setTimeout(() => this.__dragAutoScroller(), 10);
  }
  /**
   * Gets the scrollable area of the grid as a bounding client rect. The
   * scrollable area is the bounding rect of the grid minus the header and
   * footer.
   *
   * @private
   */
  __getScrollableArea() {
    const gridRect = this._grid.$.table.getBoundingClientRect();
    const headerRect = this._grid.$.header.getBoundingClientRect();
    const footerRect = this._grid.$.footer.getBoundingClientRect();
    return {
      top: gridRect.top + headerRect.height,
      bottom: gridRect.bottom - footerRect.height,
      left: gridRect.left,
      right: gridRect.right,
      height: gridRect.height - headerRect.height - footerRect.height,
      width: gridRect.width
    };
  }
  /**
   * Override to handle the user selecting all items.
   * @protected
   */
  _selectAll() {
  }
  /**
   * Override to handle the user deselecting all items.
   * @protected
   */
  _deselectAll() {
  }
  /**
   * Override to handle the user selecting an item.
   * @param {Object} item the item to select
   * @protected
   */
  _selectItem(_item) {
  }
  /**
   * Override to handle the user deselecting an item.
   * @param {Object} item the item to deselect
   * @protected
   */
  _deselectItem(_item) {
  }
  /**
   * Toggles the selected state of the given item.
   *
   * @param item the item to toggle
   * @param {boolean} [selected] whether to select or deselect the item
   * @private
   */
  __toggleItem(item, selected = !this._grid._isSelected(item)) {
    if (selected === this._grid._isSelected(item)) {
      return;
    }
    if (selected) {
      this._selectItem(item);
    } else {
      this._deselectItem(item);
    }
  }
  /**
   * IOS needs indeterminate + checked at the same time
   * @private
   */
  __isChecked(selectAll, indeterminate) {
    return indeterminate || selectAll;
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-selection-column-mixin.js
var GridSelectionColumnMixin = (superClass) => class extends GridSelectionColumnBaseMixin(superClass) {
  static get properties() {
    return {
      /**
       * The previous state of activeItem. When activeItem turns to `null`,
       * previousActiveItem will have an Object with just unselected activeItem
       * @private
       */
      __previousActiveItem: Object
    };
  }
  static get observers() {
    return ["__onSelectAllChanged(selectAll)"];
  }
  constructor() {
    super();
    this.__boundUpdateSelectAllVisibility = this.__updateSelectAllVisibility.bind(this);
    this.__boundOnSelectedItemsChanged = this.__onSelectedItemsChanged.bind(this);
  }
  /** @protected */
  disconnectedCallback() {
    this._grid.removeEventListener("data-provider-changed", this.__boundUpdateSelectAllVisibility);
    this._grid.removeEventListener("is-item-selectable-changed", this.__boundUpdateSelectAllVisibility);
    this._grid.removeEventListener("filter-changed", this.__boundOnSelectedItemsChanged);
    this._grid.removeEventListener("selected-items-changed", this.__boundOnSelectedItemsChanged);
    super.disconnectedCallback();
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    if (this._grid) {
      this._grid.addEventListener("data-provider-changed", this.__boundUpdateSelectAllVisibility);
      this._grid.addEventListener("is-item-selectable-changed", this.__boundUpdateSelectAllVisibility);
      this._grid.addEventListener("filter-changed", this.__boundOnSelectedItemsChanged);
      this._grid.addEventListener("selected-items-changed", this.__boundOnSelectedItemsChanged);
      this.__updateSelectAllVisibility();
    }
  }
  /** @private */
  __onSelectAllChanged(selectAll) {
    if (selectAll === void 0 || !this._grid) {
      return;
    }
    if (!this.__selectAllInitialized) {
      this.__selectAllInitialized = true;
      return;
    }
    if (this._selectAllChangeLock) {
      return;
    }
    if (selectAll && this.__hasArrayDataProvider()) {
      this.__withFilteredItemsArray((items) => {
        this._grid.selectedItems = items;
      });
    } else {
      this._grid.selectedItems = [];
    }
  }
  /**
   * Override a method from `GridSelectionColumnBaseMixin` to handle the user
   * selecting all items.
   *
   * @protected
   * @override
   */
  _selectAll() {
    this.selectAll = true;
  }
  /**
   * Override a method from `GridSelectionColumnBaseMixin` to handle the user
   * deselecting all items.
   *
   * @protected
   * @override
   */
  _deselectAll() {
    this.selectAll = false;
  }
  /**
   * Override a method from `GridSelectionColumnBaseMixin` to handle the user
   * selecting an item.
   *
   * @param {Object} item the item to select
   * @protected
   * @override
   */
  _selectItem(item) {
    if (this._grid.__isItemSelectable(item)) {
      this._grid.selectItem(item);
      this._grid.dispatchEvent(
        new CustomEvent("item-toggle", {
          detail: {
            item,
            selected: true,
            shiftKey: this._shiftKeyDown
          }
        })
      );
    }
  }
  /**
   * Override a method from `GridSelectionColumnBaseMixin` to handle the user
   * deselecting an item.
   *
   * @param {Object} item the item to deselect
   * @protected
   * @override
   */
  _deselectItem(item) {
    if (this._grid.__isItemSelectable(item)) {
      this._grid.deselectItem(item);
      this._grid.dispatchEvent(
        new CustomEvent("item-toggle", {
          detail: {
            item,
            selected: false,
            shiftKey: this._shiftKeyDown
          }
        })
      );
    }
  }
  /** @private */
  __hasArrayDataProvider() {
    return Array.isArray(this._grid.items) && !!this._grid.dataProvider;
  }
  /** @private */
  __onSelectedItemsChanged() {
    this._selectAllChangeLock = true;
    if (this.__hasArrayDataProvider()) {
      this.__withFilteredItemsArray((items) => {
        if (!this._grid.selectedItems.length) {
          this.selectAll = false;
          this._indeterminate = false;
        } else if (items.every((item) => this._grid._isSelected(item))) {
          this.selectAll = true;
          this._indeterminate = false;
        } else {
          this.selectAll = false;
          this._indeterminate = true;
        }
      });
    }
    this._selectAllChangeLock = false;
  }
  /** @private */
  __updateSelectAllVisibility() {
    this._selectAllHidden = !Array.isArray(this._grid.items) || !!this._grid.isItemSelectable;
  }
  /**
   * Assuming the grid uses an items array data provider, fetches all the filtered items
   * from the data provider and invokes the callback with the resulting array.
   *
   * @private
   */
  __withFilteredItemsArray(callback) {
    const params = {
      page: 0,
      pageSize: Infinity,
      sortOrders: [],
      filters: this._grid._mapFilters()
    };
    this._grid.dataProvider(params, (items) => callback(items));
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-selection-column.js
var GridSelectionColumn = class extends GridSelectionColumnMixin(GridColumn) {
  static get is() {
    return "vaadin-grid-selection-column";
  }
};
defineCustomElement(GridSelectionColumn);

// node_modules/@vaadin/grid/theme/lumo/vaadin-grid-sorter-styles.js
registerStyles(
  "vaadin-grid-sorter",
  css`
    :host {
      justify-content: flex-start;
      align-items: baseline;
      -webkit-user-select: none;
      user-select: none;
      cursor: var(--lumo-clickable-cursor);
    }

    [part='content'] {
      display: inline-block;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    [part='indicators'] {
      margin-left: var(--lumo-space-s);
    }

    [part='indicators']::before {
      transform: scale(0.8);
    }

    :host(:not([direction]):not(:hover)) [part='indicators'] {
      color: var(--lumo-tertiary-text-color);
    }

    :host([direction]) {
      color: var(--vaadin-selection-color-text, var(--lumo-primary-text-color));
    }

    [part='order'] {
      font-size: var(--lumo-font-size-xxs);
      line-height: 1;
    }

    /* RTL specific styles */

    :host([dir='rtl']) [part='indicators'] {
      margin-right: var(--lumo-space-s);
      margin-left: 0;
    }
  `,
  { moduleId: "lumo-grid-sorter" }
);

// node_modules/@vaadin/grid/src/vaadin-grid-sorter-mixin.js
var template = document.createElement("template");
template.innerHTML = `
  <style>
    @font-face {
      font-family: 'vaadin-grid-sorter-icons';
      src: url(data:application/font-woff;charset=utf-8;base64,) format('woff');
      font-weight: normal;
      font-style: normal;
    }
  </style>
`;
document.head.appendChild(template.content);
registerStyles(
  "vaadin-grid-sorter",
  css`
    :host {
      display: inline-flex;
      cursor: pointer;
      max-width: 100%;
    }

    [part='content'] {
      flex: 1 1 auto;
    }

    [part='indicators'] {
      position: relative;
      align-self: center;
      flex: none;
    }

    [part='order'] {
      display: inline;
      vertical-align: super;
    }

    [part='indicators']::before {
      font-family: 'vaadin-grid-sorter-icons';
      display: inline-block;
    }

    :host(:not([direction])) [part='indicators']::before {
      content: '\\e901';
    }

    :host([direction='asc']) [part='indicators']::before {
      content: '\\e900';
    }

    :host([direction='desc']) [part='indicators']::before {
      content: '\\e902';
    }
  `,
  { moduleId: "vaadin-grid-sorter-styles" }
);
var GridSorterMixin = (superClass) => class GridSorterMixinClass extends superClass {
  static get properties() {
    return {
      /**
       * JS Path of the property in the item used for sorting the data.
       */
      path: String,
      /**
       * How to sort the data.
       * Possible values are `asc` to use an ascending algorithm, `desc` to sort the data in
       * descending direction, or `null` for not sorting the data.
       * @type {GridSorterDirection | undefined}
       */
      direction: {
        type: String,
        reflectToAttribute: true,
        notify: true,
        value: null,
        sync: true
      },
      /**
       * @type {number | null}
       * @protected
       */
      _order: {
        type: Number,
        value: null,
        sync: true
      }
    };
  }
  static get observers() {
    return ["_pathOrDirectionChanged(path, direction)"];
  }
  /** @protected */
  ready() {
    super.ready();
    this.addEventListener("click", this._onClick.bind(this));
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    if (this._grid) {
      this._grid.__applySorters();
    } else {
      this.__dispatchSorterChangedEvenIfPossible();
    }
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    if (!this.parentNode && this._grid) {
      this._grid.__removeSorters([this]);
    } else if (this._grid) {
      this._grid.__applySorters();
    }
  }
  /** @private */
  _pathOrDirectionChanged() {
    this.__dispatchSorterChangedEvenIfPossible();
  }
  /** @private */
  __dispatchSorterChangedEvenIfPossible() {
    if (this.path === void 0 || this.direction === void 0 || !this.isConnected) {
      return;
    }
    this.dispatchEvent(
      new CustomEvent("sorter-changed", {
        detail: { shiftClick: Boolean(this._shiftClick), fromSorterClick: Boolean(this._fromSorterClick) },
        bubbles: true,
        composed: true
      })
    );
    this._fromSorterClick = false;
    this._shiftClick = false;
  }
  /** @private */
  _getDisplayOrder(order) {
    return order === null ? "" : order + 1;
  }
  /** @private */
  _onClick(e17) {
    if (e17.defaultPrevented) {
      return;
    }
    const activeElement = this.getRootNode().activeElement;
    if (this !== activeElement && this.contains(activeElement)) {
      return;
    }
    e17.preventDefault();
    this._shiftClick = e17.shiftKey;
    this._fromSorterClick = true;
    if (this.direction === "asc") {
      this.direction = "desc";
    } else if (this.direction === "desc") {
      this.direction = null;
    } else {
      this.direction = "asc";
    }
  }
};

// node_modules/@vaadin/grid/src/vaadin-grid-sorter.js
var GridSorter = class extends GridSorterMixin(ThemableMixin(DirMixin(PolymerElement))) {
  static get template() {
    return html`
      <div part="content">
        <slot></slot>
      </div>
      <div part="indicators">
        <span part="order">[[_getDisplayOrder(_order)]]</span>
      </div>
    `;
  }
  static get is() {
    return "vaadin-grid-sorter";
  }
};
defineCustomElement(GridSorter);

// node_modules/@arcgis/core/widgets/FeatureTable/Grid/GridViewModel.js
var d5 = class extends l4 {
  constructor(t5) {
    super(t5), this.actionColumnConfig = null, this.cellPartNameGenerator = (t6, e17) => {
      var _a2;
      let i10 = "";
      (e17 == null ? void 0 : e17.item) && this.rowHighlightIds.includes(e17.item.objectId) && (i10 += " highlight");
      const o8 = this.findColumn(t6 == null ? void 0 : t6.path);
      return o8 ? (o8.invalid && (i10 += " invalid"), o8.textWrap && (i10 += " text-wrap"), "editInfo" in o8 && o8.editInfo && ((_a2 = o8.editInfo.rowData) == null ? void 0 : _a2.index) === e17.index && (i10 += " editing"), i10) : i10;
    }, this.columnPerformanceModeEnabled = true, this.columnReorderingEnabled = true, this.columns = new O(), this.dataProvider = async (t6, e17) => {
      const { store: i10 } = this;
      if (!e17) return;
      if (!i10) return void (e17 && e17([]));
      e17(await i10.fetchItems(t6));
    }, this.editingEnabled = false, this.maxSize = null, this.multipleSelectionEnabled = true, this.multiSortEnabled = false, this.multipleSortPriority = "prepend", this.noDataMessage = null, this.paginationEnabled = false, this.rowDetailsRenderer = null, this.store = null;
  }
  get actionColumn() {
    return this.allColumns.find((t5) => t5.fieldName === o5.action);
  }
  get allColumns() {
    return this.columns.toArray().flatMap((t5) => {
      var _a2;
      return [t5, ...e14(t5) && ((_a2 = t5.columns) == null ? void 0 : _a2.length) ? t5.columns : []];
    });
  }
  get allVisibleColumns() {
    return this.allColumns.filter((t5) => !t5.hidden);
  }
  get columnMenuIsOpen() {
    return this.allColumns.some((t5) => t5.menuIsOpen);
  }
  get editableColumns() {
    return this.allColumns.filter((t5) => "editable" in t5);
  }
  get effectiveSize() {
    const { filterBySelectionEnabled: t5, highlightIds: e17, objectIds: i10, pageSize: o8, size: n19 } = this;
    if (0 === n19) return 0;
    if (this.paginationEnabled) {
      const l17 = this.pageCount === this.pageIndex + 1 && n19 % o8 || o8;
      return t5 ? Math.min(e17.length, l17) : i10.length ? Math.min(i10.length, l17) : l17;
    }
    return t5 ? e17.length : i10.length ? i10.length : n19;
  }
  get filterBySelectionEnabled() {
    return !!this._get("filterBySelectionEnabled");
  }
  set filterBySelectionEnabled(t5) {
    this._set("filterBySelectionEnabled", t5);
  }
  get groupColumns() {
    return this.columns.toArray().filter((t5) => e14(t5));
  }
  get hasInvalidColumnConfiguration() {
    return this.visibleColumns.every((t5) => t5.frozen || t5.frozenToEnd);
  }
  get highlightIds() {
    return this._get("highlightIds") || new O();
  }
  set highlightIds(t5) {
    const o8 = Array.isArray(t5) ? new O(t5) : t5;
    this.multipleSelectionEnabled || o8.splice(1, o8.length - 1), this._set("highlightIds", n5(o8, this._get("highlightIds"), O));
  }
  get isEditing() {
    return this.editableColumns.some((t5) => t5.editInfo);
  }
  get isQuerying() {
    var _a2;
    return ((_a2 = this.store) == null ? void 0 : _a2.isQuerying) ?? false;
  }
  get isQueryingOrSyncing() {
    return this.isQuerying || this.isSyncing;
  }
  get isSyncing() {
    var _a2;
    return ((_a2 = this.store) == null ? void 0 : _a2.isSyncing) ?? false;
  }
  get objectIds() {
    return this._get("objectIds") ?? new O();
  }
  set objectIds(t5) {
    this._set("objectIds", n5(t5, this._get("objectIds"), O));
  }
  get pageCount() {
    const { pageSize: t5, size: e17 } = this;
    return 0 === e17 || 0 === t5 ? 1 : Math.ceil(e17 / t5);
  }
  get pageIndex() {
    return this._get("pageIndex") || 0;
  }
  set pageIndex(t5) {
    const { pageCount: e17 } = this;
    b5(t5, e17) && this._set("pageIndex", t5);
  }
  get pageSize() {
    return this._get("pageSize") ?? 50;
  }
  set pageSize(t5) {
    var _a2;
    (_a2 = this.store) == null ? void 0 : _a2.set("pageSize", t5), this._set("pageSize", t5);
  }
  get rowHighlightIds() {
    return this._get("rowHighlightIds") || new O();
  }
  set rowHighlightIds(t5) {
    this._set("rowHighlightIds", n5(t5, this._get("rowHighlightIds"), O));
  }
  get size() {
    var _a2;
    const t5 = ((_a2 = this.store) == null ? void 0 : _a2.count) ?? 0, { maxSize: e17 } = this;
    return null != t5 && null != e17 ? Math.min(e17, t5) : t5;
  }
  get state() {
    var _a2;
    return ((_a2 = this.store) == null ? void 0 : _a2.state) ?? "disabled";
  }
  get visibleColumns() {
    return this.columns.toArray().filter((t5) => !t5.hidden);
  }
  closeColumnMenus() {
    this.allColumns.forEach((t5) => t5.closeMenu());
  }
  sortColumn(t5, e17) {
    if (!t5) return;
    const i10 = this.findColumn(t5);
    i10 && (i10.direction = e17);
  }
  hideColumn(t5) {
    const e17 = this.findColumn(t5);
    false === (e17 == null ? void 0 : e17.hidden) && (e17.hidden = true);
  }
  showColumn(t5) {
    const e17 = this.findColumn(t5);
    (e17 == null ? void 0 : e17.hidden) && (e17.hidden = false);
  }
  showAllColumns() {
    this.allColumns.forEach((t5) => {
      t5.hidden && (t5.hidden = false);
    });
  }
  findColumn(t5) {
    return this.allColumns.find((e17) => e17.fieldName === t5);
  }
  refresh() {
    var _a2, _b;
    (_a2 = this.store) == null ? void 0 : _a2.reset(), (_b = this.store) == null ? void 0 : _b.load();
  }
  toggleColumnVisibility(t5) {
    const e17 = this.findColumn(t5);
    e17 && (e17.hidden = !e17.hidden);
  }
};
__decorate([m()], d5.prototype, "actionColumn", null), __decorate([m()], d5.prototype, "actionColumnConfig", void 0), __decorate([m()], d5.prototype, "allColumns", null), __decorate([m()], d5.prototype, "allVisibleColumns", null), __decorate([m()], d5.prototype, "cellPartNameGenerator", void 0), __decorate([m()], d5.prototype, "columnMenuIsOpen", null), __decorate([m()], d5.prototype, "columnPerformanceModeEnabled", void 0), __decorate([m()], d5.prototype, "columnReorderingEnabled", void 0), __decorate([m()], d5.prototype, "columns", void 0), __decorate([m()], d5.prototype, "dataProvider", void 0), __decorate([m()], d5.prototype, "editingEnabled", void 0), __decorate([m()], d5.prototype, "editableColumns", null), __decorate([m()], d5.prototype, "effectiveSize", null), __decorate([m()], d5.prototype, "filterBySelectionEnabled", null), __decorate([m()], d5.prototype, "groupColumns", null), __decorate([m()], d5.prototype, "hasInvalidColumnConfiguration", null), __decorate([m()], d5.prototype, "highlightIds", null), __decorate([m()], d5.prototype, "isEditing", null), __decorate([m()], d5.prototype, "isQuerying", null), __decorate([m()], d5.prototype, "isQueryingOrSyncing", null), __decorate([m()], d5.prototype, "isSyncing", null), __decorate([m()], d5.prototype, "maxSize", void 0), __decorate([m()], d5.prototype, "multipleSelectionEnabled", void 0), __decorate([m()], d5.prototype, "multiSortEnabled", void 0), __decorate([m()], d5.prototype, "multipleSortPriority", void 0), __decorate([m()], d5.prototype, "noDataMessage", void 0), __decorate([m()], d5.prototype, "objectIds", null), __decorate([m()], d5.prototype, "pageCount", null), __decorate([m()], d5.prototype, "pageIndex", null), __decorate([m()], d5.prototype, "pageSize", null), __decorate([m()], d5.prototype, "paginationEnabled", void 0), __decorate([m()], d5.prototype, "rowHighlightIds", null), __decorate([m()], d5.prototype, "rowDetailsRenderer", void 0), __decorate([m({ readOnly: true })], d5.prototype, "size", null), __decorate([m()], d5.prototype, "store", void 0), __decorate([m({ readOnly: true })], d5.prototype, "state", null), __decorate([m()], d5.prototype, "visibleColumns", null), d5 = __decorate([a3("esri.widgets.FeatureTable.Grid.GridViewModel")], d5);
var a12 = d5;

// node_modules/@arcgis/core/widgets/FeatureTable/Grid/Grid.js
var c15 = "esri-grid";
var p9 = "compact column-borders";
var _3 = { base: c15, content: `${c15}__content`, grid: `${c15}__grid`, noDataMessage: `${c15}__no-data-message` };
var f7 = { sort: ["Enter", " "] };
var v = class extends B {
  constructor(e17, t5) {
    super(e17, t5), this._columnElements = [], this._grid = null, this._temporaryHighlightId = null, this.itemIdPath = "objectId", this.messages = null, this.selectionColumnEnabled = true, this.viewModel = new a12(), this._onSelectedItemsChange = this._onSelectedItemsChange.bind(this);
  }
  initialize() {
    this.addHandles([this.columns.on("before-changes", () => this.renderNow()), this.columns.on("change", () => this.onColumnChange()), a2(() => this.highlightIds, "before-add", ({ target: e17 }) => {
      !this.multipleSelectionEnabled && e17.length && e17.removeAll();
    }), this.rowHighlightIds.on("change", () => this.generateCellPartNames()), l(() => this.effectiveSize, () => this._updateGridSize()), l(() => this.isEditing, () => {
      this.generateCellPartNames(), this.requestContentUpdate();
    }), l(() => {
      var _a2;
      return (_a2 = this.store) == null ? void 0 : _a2.state;
    }, (e17, t5) => {
      "ready" !== e17 || "loaded" !== t5 && "error" !== t5 || this.refreshPageCache();
    }), a2(() => this._table, "scroll", () => this.viewModel.closeColumnMenus()), o(window, "resize", () => this._updateColumnBorderStyles()), a2(() => this._table, "scrollend", () => {
      this.paginationEnabled || (this.pageIndex = this.getVirtualPageIndex()), this._updateColumnBorderStyles();
    }), l(() => this.multipleSelectionEnabled, (e17) => {
      !e17 && this.highlightIds.length > 1 && this.highlightIds.removeAll();
    })]);
  }
  destroy() {
    this.resetColumns(), this.columns.destroyed || this.columns.destroy();
  }
  resetColumns() {
    this.columns.drain((e17) => !e17.destroyed && e17.destroy());
  }
  get _columnRendering() {
    return this.columnPerformanceModeEnabled ? "lazy" : "eager";
  }
  get _selectedItems() {
    const { highlightIds: e17, store: t5 } = this;
    return e17.toArray().map((e18) => (t5 == null ? void 0 : t5.getItemByObjectId(e18)) ?? { objectId: e18 });
  }
  get _gridIsDisabled() {
    return 0 === this.viewModel.allVisibleColumns.length || this.hasInvalidColumnConfiguration;
  }
  get _noDataMessage() {
    return t.sanitize(this.noDataMessage);
  }
  get _table() {
    var _a2, _b;
    return (_b = (_a2 = this._grid) == null ? void 0 : _a2.$) == null ? void 0 : _b.table;
  }
  get cellPartNameGenerator() {
    return this.viewModel.cellPartNameGenerator;
  }
  set cellPartNameGenerator(e17) {
    this.viewModel.cellPartNameGenerator = e17;
  }
  get columns() {
    return this.viewModel.columns;
  }
  set columns(e17) {
    this.viewModel.columns = e17;
  }
  get columnPerformanceModeEnabled() {
    return this.viewModel.columnPerformanceModeEnabled;
  }
  set columnPerformanceModeEnabled(e17) {
    this.viewModel.columnPerformanceModeEnabled = e17;
  }
  get columnReorderingEnabled() {
    return this.viewModel.columnReorderingEnabled;
  }
  set columnReorderingEnabled(e17) {
    this.viewModel.columnReorderingEnabled = e17;
  }
  get dataProvider() {
    return this.viewModel.dataProvider;
  }
  set dataProvider(e17) {
    this.viewModel.dataProvider = e17;
  }
  get editingEnabled() {
    return this.viewModel.editingEnabled;
  }
  set editingEnabled(e17) {
    this.viewModel.editingEnabled = e17;
  }
  get effectiveSize() {
    return this.viewModel.effectiveSize;
  }
  get groupColumns() {
    return this.viewModel.groupColumns;
  }
  get hasInvalidColumnConfiguration() {
    return this.viewModel.hasInvalidColumnConfiguration;
  }
  get highlightIds() {
    return this.viewModel.highlightIds;
  }
  set highlightIds(e17) {
    this.viewModel.highlightIds = e17;
  }
  get temporaryHighlightId() {
    return this._temporaryHighlightId;
  }
  get isEditing() {
    return this.viewModel.isEditing;
  }
  get isReady() {
    return !!this._grid;
  }
  get label() {
    var _a2;
    return ((_a2 = this.messages) == null ? void 0 : _a2.widgetLabel) ?? "";
  }
  set label(e17) {
    this._overrideIfSome("label", e17);
  }
  get multipleSelectionEnabled() {
    return this.viewModel.multipleSelectionEnabled;
  }
  set multipleSelectionEnabled(e17) {
    this.viewModel.multipleSelectionEnabled = e17;
  }
  get multiSortEnabled() {
    return this.viewModel.multiSortEnabled;
  }
  set multiSortEnabled(e17) {
    this.viewModel.multiSortEnabled = e17;
  }
  get noDataMessage() {
    return this.viewModel.noDataMessage;
  }
  set noDataMessage(e17) {
    this.viewModel.noDataMessage = e17;
  }
  get pageIndex() {
    return this.viewModel.pageIndex;
  }
  set pageIndex(e17) {
    this.viewModel.pageIndex = e17;
  }
  get multipleSortPriority() {
    return this.viewModel.multipleSortPriority;
  }
  set multipleSortPriority(e17) {
    this.viewModel.multipleSortPriority = e17;
  }
  get pageSize() {
    return this.viewModel.pageSize;
  }
  set pageSize(e17) {
    this.viewModel.pageSize = e17;
  }
  get paginationEnabled() {
    return this.viewModel.paginationEnabled;
  }
  set paginationEnabled(e17) {
    this.viewModel.paginationEnabled = e17;
  }
  get rowDetailsRenderer() {
    return this.viewModel.rowDetailsRenderer;
  }
  set rowDetailsRenderer(e17) {
    this.viewModel.rowDetailsRenderer = e17;
  }
  get rowHighlightIds() {
    return this.viewModel.rowHighlightIds;
  }
  set rowHighlightIds(e17) {
    this.viewModel.rowHighlightIds = e17;
  }
  get size() {
    return this.viewModel.size;
  }
  get sortOrders() {
    var _a2;
    return ((_a2 = this._grid) == null ? void 0 : _a2._sorters) ? this._grid._sorters.filter((e17) => !!e17 && e17.path).map(({ direction: e17, path: t5 }) => ({ direction: e17, path: t5 })) : [];
  }
  get store() {
    return this.viewModel.store;
  }
  set store(e17) {
    this.viewModel.store = e17;
  }
  get state() {
    return this.viewModel.state;
  }
  addSorter(e17) {
    var _a2;
    (_a2 = this._grid) == null ? void 0 : _a2.__updateSorter(e17, false, false), this.notifyChange("sortOrders");
  }
  getColumnProps(e17, t5) {
    const { id: i10 } = this, { autoWidth: r12, direction: o8, fieldName: n19, flexGrow: l17, frozen: s22, frozenToEnd: d6, hidden: a17, label: h9, resizable: u8, textAlign: g5, width: m11 } = e17, c17 = `${i10}_${n19}_${t5}`, { renderFunction: p14, footerRenderFunction: _4, headerRenderFunction: f9 } = e17, v2 = _4 ? (e18, t6) => _4({ root: e18, column: t6 }) : void 0, y6 = f9 ? (e18, t6) => f9({ root: e18, column: t6 }) : void 0, C6 = p14 ? (e18, t6, i11) => p14({ root: e18, column: t6, rowData: i11 }) : void 0;
    let w4 = "";
    return e17.direction && (w4 += " direction"), e17.invalid && (w4 += " invalid"), { footerRenderer: v2, headerRenderer: y6, renderer: C6, autoWidth: r12, direction: o8, flexGrow: l17, frozen: s22, frozenToEnd: d6, headerPartName: w4, key: c17, resizable: u8, bind: this, "data-fieldName": n19, header: h9, hidden: this.hasInvalidColumnConfiguration || a17, path: n19, "text-align": g5, width: "number" == typeof m11 ? `${m11}px` : m11, afterCreate: this._afterColumnCreate, afterRemoved: this._afterColumnRemoved };
  }
  clearSelection() {
    this._clearSelection(), this.scheduleRender();
  }
  clearSort() {
    var _a2;
    let e17 = false;
    if (this._grid) {
      if (((_a2 = this._grid._sorters) == null ? void 0 : _a2.length) && (this._grid._sorters.forEach((e18) => {
        e18._order = null, e18.direction = null;
      }), e17 = true), this.columns.length) {
        this.columns.some((e18) => !!e18.direction) && (this.columns.forEach((e18) => e18.direction = null), e17 = true);
      }
      e17 && (this.notifyChange("sortOrders"), this.scheduleRender());
    }
  }
  findColumn(e17) {
    return this.viewModel.findColumn(e17);
  }
  generateCellPartNames() {
    var _a2;
    (_a2 = this._grid) == null ? void 0 : _a2.generateCellPartNames();
  }
  getFirstVisibleRowIndex() {
    var _a2;
    return ((_a2 = this._grid) == null ? void 0 : _a2._firstVisibleIndex) || 0;
  }
  getVirtualPageIndex() {
    return Math.floor(this.getFirstVisibleRowIndex() / this.pageSize);
  }
  getLastVisibleRowIndex() {
    var _a2;
    return ((_a2 = this._grid) == null ? void 0 : _a2._lastVisibleIndex) || 0;
  }
  getVisibleItemsCount() {
    var _a2;
    return ((_a2 = this._grid) == null ? void 0 : _a2._visibleItemsCount) || 0;
  }
  getRowContainingNode(e17) {
    var _a2;
    try {
      return (_a2 = this._grid) == null ? void 0 : _a2._getRowContainingNode(e17);
    } catch {
      return null;
    }
  }
  getSlotElementByName(e17) {
    var _a2, _b;
    return ((_b = (_a2 = this._grid) == null ? void 0 : _a2.shadowRoot) == null ? void 0 : _b.querySelector(`slot[name='${e17}']`)) ?? null;
  }
  hasSorter(e17) {
    var _a2, _b;
    return ((_b = (_a2 = this._grid) == null ? void 0 : _a2._sorters) == null ? void 0 : _b.includes(e17)) || false;
  }
  hideColumn(e17) {
    this.viewModel.hideColumn(e17);
  }
  recalculateColumnWidths() {
    var _a2;
    (_a2 = this._grid) == null ? void 0 : _a2.recalculateColumnWidths();
  }
  async reset() {
    var _a2;
    this._clearSelection(), (_a2 = this.store) == null ? void 0 : _a2.reset(), this.scrollToTop();
  }
  refreshPageCache() {
    var _a2;
    (_a2 = this._grid) == null ? void 0 : _a2.clearCache(), this._temporaryHighlightId = null;
  }
  requestContentUpdate() {
    var _a2;
    (_a2 = this._grid) == null ? void 0 : _a2.requestContentUpdate(), this._temporaryHighlightId = null;
  }
  selectRows(e17) {
    const { itemIdPath: t5 } = this, i10 = e17 == null ? void 0 : e17.filter((e18) => !this.highlightIds.includes(e18[t5])), r12 = i10.map((e18) => e18[t5]);
    r12.length && (this.multipleSelectionEnabled || (this.highlightIds.removeAll(), r12.splice(1)), this.highlightIds.addMany(r12));
  }
  deselectRows(e17) {
    const { itemIdPath: t5 } = this, i10 = (e17 == null ? void 0 : e17.map((e18) => e18[t5])) || [];
    i10.length && this.highlightIds.removeMany(i10);
  }
  showColumn(e17) {
    this.viewModel.showColumn(e17);
  }
  sort({ path: e17, direction: t5 }) {
    this.viewModel.sortColumn(e17, t5), this.notifyChange("sortOrders");
  }
  scrollToIndex(e17) {
    var _a2, _b;
    0 !== this.size && (((_a2 = this._grid) == null ? void 0 : _a2.isConnected) && ((_b = this._grid) == null ? void 0 : _b.scrollToIndex(e17)), this._temporaryHighlightId = null);
  }
  scrollToTop() {
    this.scrollToIndex(0);
  }
  scrollToBottom() {
    this.scrollToIndex(1 / 0);
  }
  scrollLeft(e17) {
    const { _table: t5 } = this;
    t5 && (t5.scrollLeft = e17);
  }
  toggleColumnVisibility(e17) {
    this.viewModel.toggleColumnVisibility(e17);
  }
  onColumnChange() {
    this._columnElements.forEach((e17) => this._applyRenderersToColumnElement(e17)), this.requestContentUpdate();
  }
  render() {
    return n3("div", { bind: this, class: this.classes(_3.base, e4.widget) }, n3("div", { bind: this, class: _3.content }, this._renderGrid()));
  }
  _renderGrid() {
    return n3("vaadin-grid", { afterCreate: this._afterGridCreate, afterUpdate: this._afterGridUpdate, bind: this, cellPartNameGenerator: this.cellPartNameGenerator, class: _3.grid, columnRendering: this._columnRendering, columnReorderingAllowed: this.columnReorderingEnabled, dataProvider: this.dataProvider, disabled: this._gridIsDisabled, id: `${this.id}_grid`, itemIdPath: this.itemIdPath, multiSort: this.multiSortEnabled, multiSortPriority: this.multipleSortPriority, pageSize: this.pageSize, ref: "grid", rowDetailsRenderer: this.rowDetailsRenderer, selectedItems: this._selectedItems, size: this.effectiveSize }, this._renderAllColumns(), this._renderEmptyState());
  }
  _renderAllColumns() {
    return "disabled" !== this.state && this.columns.length ? [this._renderSelectionColumn(), this._renderColumns()] : null;
  }
  _renderSelectionColumn() {
    return n3("vaadin-grid-selection-column", { _selectAllHidden: true, autoWidth: false, bind: this, dragSelect: true, frozen: true, hidden: this.hasInvalidColumnConfiguration || !this.selectionColumnEnabled, selectAll: false, sortable: false, textAlign: "center" });
  }
  _renderColumns() {
    return Array.from(this.columns, (e17, t5) => "columns" in e17 ? this._renderGroupColumn(e17, t5) : n3("vaadin-grid-column", { ...this.getColumnProps(e17, t5) })).filter(N);
  }
  _renderGroupColumn(e17, t5) {
    const i10 = this.getColumnProps(e17, t5);
    if (i10.hidden || !e17.columns) return null;
    const r12 = e17.columns.filter(({ hidden: e18 }) => !e18);
    return r12.length ? n3("vaadin-grid-column-group", { ...i10 }, r12.map((e18) => n3("vaadin-grid-column", { ...this.getColumnProps(e18, t5) }))) : null;
  }
  _renderEmptyState() {
    const e17 = this.viewModel;
    if ("loaded" === e17.state && !e17.isQueryingOrSyncing) return n3("div", { class: _3.noDataMessage, slot: "empty-state" }, this._noDataMessage ?? this.messages.noDataFound);
  }
  _afterGridCreate(e17) {
    const t5 = this._grid = e17;
    t5.setAttribute("theme", p9), customElements.whenDefined("vaadin-grid").then(() => {
      this._appendStyles(), this._addGridEventListeners();
    }), t5.__updateSorter = (e18, i10, r12) => {
      const o8 = t5._sorters, n19 = !o8.includes(e18), { multiSort: l17, multiSortOnShiftClick: s22, multiSortPriority: d6 } = t5, a17 = o8.filter((t6) => t6 !== e18), h9 = l17 && (!s22 || !r12) || s22 && i10;
      e18._order = null, e18.direction ? h9 ? null != e18._initialOrder ? (n19 ? o8[e18._initialOrder] = e18 : o8.splice(e18._initialOrder, 0, e18), e18._initialOrder = null) : "append" === d6 && n19 ? t5._sorters = [...a17, e18] : "prepend" === d6 && (t5._sorters = [e18, ...a17]) : (e18.direction || s22) && (t5._sorters = e18.direction ? [e18] : [], a17.forEach((e19) => {
        e19._order = null, e19.direction = null;
      })) : n19 || (t5._sorters = [...a17]);
    }, t5.__removeSorters = (e18) => {
      if (0 === e18.length) return;
      const i10 = new Set(e18.filter((e19) => !e19.direction));
      t5._sorters = t5._sorters.filter((e19) => !i10.has(e19)), t5.__applySorters();
    };
  }
  _appendStyles() {
    var _a2;
    const e17 = (_a2 = this._grid) == null ? void 0 : _a2.shadowRoot, t5 = document.createElement("style");
    e17 && (t5.textContent = `
      #items [part~="row"][editing],
      #items [part~="row"][editing][selected] {
        z-index: 2;
      }

      #items [part~="editing"],
      #items [part~="editing"][selected] {
        z-index: 3;
      }

      [frozen], [frozen-to-end] {
        z-index: 4;
      }

      [last-frozen] {
        overflow: visible;
      }

      [part~='cell'] ::slotted(vaadin-grid-cell-content) {
        align-items: center;
        box-sizing: border-box !important;
        height: 100%;
        line-height: 2em;
        min-height: 40px;
      }

      #items [part~="text-wrap"] {
        text-wrap: wrap;
      }
    `, e17.appendChild(t5));
  }
  _afterGridUpdate(e17) {
    this._grid || (this._grid = e17);
  }
  _afterColumnCreate(e17) {
    this._columnElements.push(e17);
  }
  _afterColumnRemoved(e17) {
    const t5 = this._columnElements.indexOf(e17, 0);
    t5 > -1 && this._columnElements.splice(t5, 1);
  }
  _updateGridSize() {
    this._grid && (this._grid.size = this.effectiveSize, this.scheduleRender());
  }
  _addGridEventListeners() {
    const e17 = this._grid;
    n(e17), this.addHandles([o(e17, ["click", "dblclick", "keydown", "pointerover", "pointerout"], (e18) => this._onGridInteraction(e18)), o(e17, ["pointerover", "pointerout", "cell-focus"], (e18) => this._onTransientGridInteraction(e18)), o(e17, "selected-items-changed", this._onSelectedItemsChange), o(e17, "sorter-changed", () => {
      this.notifyChange("sortOrders"), this._updateColumnBorderStyles();
    }), o(e17, "column-resize", (e18) => {
      const t5 = e18.detail.resizedColumn, i10 = t5.getAttribute("data-fieldName"), r12 = this.findColumn(i10);
      r12 == null ? void 0 : r12.set({ width: t5.width }), this._updateColumnBorderStyles();
    }), o(e17, "column-reorder", () => this._onColumnOrderChange())]);
  }
  _onGridInteraction(e17) {
    var _a2;
    const t5 = this._grid;
    if (n(t5), ("pointerover" === e17.type || "pointerout" === e17.type) && e17.relatedTarget !== t5) {
      const { target: t6, relatedTarget: i11 } = e17;
      if (!this._isGridCellContentNode(t6) || !this._isGridCellContentNode(i11)) return;
    }
    let i10 = null;
    try {
      i10 = t5.getEventContext(e17);
    } catch (a17) {
    }
    if (!i10) return;
    const { column: o8, index: n19, item: l17, section: s22 } = i10;
    if (!s22) return;
    if ("header" === s22 && "keydown" === e17.type && (o8 == null ? void 0 : o8.path)) {
      const t6 = e17.key;
      f7.sort.includes(t6) && ((_a2 = this.findColumn(o8.path)) == null ? void 0 : _a2.sort());
    }
    const d6 = `cell-${e17.type}`;
    this.emit(d6, { type: d6, context: i10, index: n19, item: l17, native: e17, path: (o8 == null ? void 0 : o8.path) ?? void 0 });
  }
  _onTransientGridInteraction(e17) {
    if ("pointerout" === e17.type) return void (this._temporaryHighlightId = null);
    const t5 = this._grid;
    if (t5) try {
      const { item: i10 } = t5.getEventContext(e17);
      this._temporaryHighlightId = i10 == null ? void 0 : i10.objectId;
    } catch {
      this._temporaryHighlightId = null;
    }
  }
  _isGridCellContentNode(e17) {
    return !!(e17 && e17 instanceof HTMLElement && "vaadin-grid-cell-content" === e17.localName);
  }
  _onColumnOrderChange() {
    const e17 = this._grid;
    n(e17);
    const t5 = e17._getColumnsInOrder(0), i10 = [], o8 = (this.viewModel.groupColumns.length ? e17._getColumnsInOrder(1) : t5).map((e18) => e18.getAttribute("data-fieldName"));
    t5 == null ? void 0 : t5.forEach((e18) => {
      var _a2;
      const t6 = e18.getAttribute("data-fieldName");
      if (null != t6) {
        const e19 = this.findColumn(t6);
        i10.push(t6), e19 && "columns" in e19 && ((_a2 = e19.columns) == null ? void 0 : _a2.length) && e19.columns.sort((e20, t7) => o8.indexOf(e20.fieldName) > o8.indexOf(t7.fieldName) ? 1 : -1);
      }
    }), this.columns.sort((e18, t6) => i10.indexOf(e18.fieldName) > i10.indexOf(t6.fieldName) ? 1 : -1), this.notifyChange("sortOrders"), this.emit("column-reorder", { type: "column-reorder" });
  }
  _clearSelection() {
    this.highlightIds.removeAll(), this._temporaryHighlightId = null;
  }
  _onSelectedItemsChange(e17) {
    const { highlightIds: t5, itemIdPath: i10 } = this, r12 = e17.detail.value.map((e18) => e18[i10]), o8 = r12.filter((e18) => !t5.includes(e18));
    if (!this.multipleSelectionEnabled && o8.length && t5.length) t5.removeAll(), t5.add(o8[0]);
    else {
      const e18 = t5.filter((e19) => !r12.includes(e19));
      t5.removeMany(e18), t5.addMany(o8);
    }
  }
  _applyRenderersToColumnElement(e17) {
    const t5 = e17 == null ? void 0 : e17.path, i10 = null != t5 ? this.findColumn(t5) : void 0;
    if (i10) try {
      const { renderFunction: t6, footerRenderFunction: r12, headerRenderFunction: o8 } = i10;
      t6 && "renderer" in e17 && (e17.renderer = (e18, i11, r13) => t6({ root: e18, column: i11, rowData: r13 })), r12 && (e17.footerRenderer = (e18, t7) => r12({ root: e18, column: t7 })), o8 && (e17.headerRenderer = (e18, t7) => o8({ root: e18, column: t7 }));
    } catch (r12) {
    }
  }
  _updateColumnBorderStyles() {
    var _a2;
    try {
      (_a2 = this._grid) == null ? void 0 : _a2._updateFirstAndLastColumn();
    } catch (e17) {
    }
  }
};
__decorate([m()], v.prototype, "_columnElements", void 0), __decorate([m()], v.prototype, "_columnRendering", null), __decorate([m()], v.prototype, "_selectedItems", null), __decorate([m()], v.prototype, "_grid", void 0), __decorate([m()], v.prototype, "_gridIsDisabled", null), __decorate([m()], v.prototype, "_noDataMessage", null), __decorate([m()], v.prototype, "_table", null), __decorate([m()], v.prototype, "_temporaryHighlightId", void 0), __decorate([m()], v.prototype, "cellPartNameGenerator", null), __decorate([m()], v.prototype, "columns", null), __decorate([m()], v.prototype, "columnPerformanceModeEnabled", null), __decorate([m()], v.prototype, "columnReorderingEnabled", null), __decorate([m()], v.prototype, "dataProvider", null), __decorate([m()], v.prototype, "editingEnabled", null), __decorate([m()], v.prototype, "effectiveSize", null), __decorate([m()], v.prototype, "groupColumns", null), __decorate([m()], v.prototype, "hasInvalidColumnConfiguration", null), __decorate([m()], v.prototype, "highlightIds", null), __decorate([m()], v.prototype, "temporaryHighlightId", null), __decorate([m()], v.prototype, "isEditing", null), __decorate([m()], v.prototype, "isReady", null), __decorate([m()], v.prototype, "itemIdPath", void 0), __decorate([m()], v.prototype, "label", null), __decorate([m(), e3("esri/widgets/FeatureTable/t9n/FeatureTable")], v.prototype, "messages", void 0), __decorate([m()], v.prototype, "multipleSelectionEnabled", null), __decorate([m()], v.prototype, "multiSortEnabled", null), __decorate([m()], v.prototype, "noDataMessage", null), __decorate([m()], v.prototype, "pageIndex", null), __decorate([m()], v.prototype, "multipleSortPriority", null), __decorate([m()], v.prototype, "pageSize", null), __decorate([m()], v.prototype, "paginationEnabled", null), __decorate([m()], v.prototype, "rowDetailsRenderer", null), __decorate([m()], v.prototype, "rowHighlightIds", null), __decorate([m()], v.prototype, "selectionColumnEnabled", void 0), __decorate([m()], v.prototype, "size", null), __decorate([m({ readOnly: true })], v.prototype, "sortOrders", null), __decorate([m()], v.prototype, "store", null), __decorate([m()], v.prototype, "state", null), __decorate([m()], v.prototype, "viewModel", void 0), v = __decorate([a3("esri.widgets.FeatureTable.Grid.Grid")], v);
var y5 = v;

// node_modules/@arcgis/core/widgets/FeatureTable/Grid/GroupColumn.js
var e16 = class extends f5 {
  constructor(o8) {
    super(o8), this.columns = null, this.sortable = false;
  }
  initialize() {
    this._set("fieldName", `group:${this.label}`);
  }
  destroy() {
    var _a2;
    (_a2 = this.columns) == null ? void 0 : _a2.forEach((o8) => !o8.destroyed && o8.destroy());
  }
};
__decorate([m()], e16.prototype, "columns", void 0), __decorate([m({ readOnly: true })], e16.prototype, "sortable", void 0), e16 = __decorate([a3("esri.widgets.FeatureTable.Grid.GroupColumn")], e16);
var i6 = e16;

// node_modules/@arcgis/core/widgets/FeatureTable/support/ColumnTemplateBase.js
var p10 = class extends l6(n4) {
  constructor(o8) {
    super(o8), this.autoWidth = false, this.description = null, this.direction = null, this.flexGrow = 1, this.fieldName = null, this.formatFunction = null, this.frozen = false, this.frozenToEnd = false, this.icon = null, this.iconText = null, this.initialSortPriority = null, this.invalid = false, this.label = null, this.labelTooltipText = null, this.menuConfig = null, this.resizable = true, this.sortable = false, this.textAlign = "start", this.textWrap = false, this.timeZone = null, this.type = null, this.visible = true, this.width = 200;
  }
};
__decorate([m()], p10.prototype, "autoWidth", void 0), __decorate([m()], p10.prototype, "description", void 0), __decorate([m()], p10.prototype, "direction", void 0), __decorate([m()], p10.prototype, "flexGrow", void 0), __decorate([m({ type: String })], p10.prototype, "fieldName", void 0), __decorate([m()], p10.prototype, "formatFunction", void 0), __decorate([m()], p10.prototype, "frozen", void 0), __decorate([m()], p10.prototype, "frozenToEnd", void 0), __decorate([m()], p10.prototype, "icon", void 0), __decorate([m()], p10.prototype, "iconText", void 0), __decorate([m()], p10.prototype, "initialSortPriority", void 0), __decorate([m()], p10.prototype, "invalid", void 0), __decorate([m()], p10.prototype, "label", void 0), __decorate([m()], p10.prototype, "labelTooltipText", void 0), __decorate([m()], p10.prototype, "menuConfig", void 0), __decorate([m()], p10.prototype, "resizable", void 0), __decorate([m()], p10.prototype, "sortable", void 0), __decorate([m()], p10.prototype, "textAlign", void 0), __decorate([m()], p10.prototype, "textWrap", void 0), __decorate([m()], p10.prototype, "timeZone", void 0), __decorate([m({ type: String, json: { read: false, write: true } })], p10.prototype, "type", void 0), __decorate([m()], p10.prototype, "visible", void 0), __decorate([m()], p10.prototype, "width", void 0), p10 = __decorate([a3("esri.widgets.FeatureTable.support.ColumnTemplateBase")], p10);
var s17 = p10;

// node_modules/@arcgis/core/widgets/FeatureTable/support/AttachmentsColumnTemplate.js
var a13 = class extends s17 {
  constructor(t5) {
    super(t5), this.attachmentsViewEnabled = true, this.icon = "attachment", this.sortable = false, this.textAlign = "center", this.thumbnailAppearance = "image", this.thumbnailCount = 8, this.thumbnailsEnabled = true, this.thumbnailIconScale = "m", this.type = "attachment";
  }
};
__decorate([m()], a13.prototype, "attachmentsViewEnabled", void 0), __decorate([m()], a13.prototype, "icon", void 0), __decorate([m()], a13.prototype, "sortable", void 0), __decorate([m()], a13.prototype, "textAlign", void 0), __decorate([m()], a13.prototype, "thumbnailAppearance", void 0), __decorate([m()], a13.prototype, "thumbnailCount", void 0), __decorate([m()], a13.prototype, "thumbnailsEnabled", void 0), __decorate([m()], a13.prototype, "thumbnailIconScale", void 0), __decorate([m({ type: String, json: { read: false, write: true } })], a13.prototype, "type", void 0), a13 = __decorate([a3("esri.widgets.FeatureTable.support.AttachmentsColumnTemplate")], a13);
var i7 = a13;

// node_modules/@arcgis/core/widgets/FeatureTable/support/AttachmentsViewOptions.js
var i8 = class extends b3 {
  constructor(t5) {
    super(t5), this.attachmentId = null, this.attachmentInfos = [], this.candidates = null, this.error = null, this.form = null, this.mode = "list", this.objectId = null;
  }
  initialize() {
    this.addHandles([l(() => this.objectId, () => this.reset()), l(() => this.candidates, () => {
      this.error = null;
    }), l(() => this.attachmentInfos.length, (t5) => {
      0 === t5 && (this.mode = "file");
    })]);
  }
  onEditComplete() {
    this.attachmentId = null, this.candidates = null, this.mode = "list";
  }
  reset() {
    this.attachmentId = null, this.candidates = null, this.error = null;
  }
};
__decorate([m()], i8.prototype, "attachmentId", void 0), __decorate([m()], i8.prototype, "attachmentInfos", void 0), __decorate([m()], i8.prototype, "candidates", void 0), __decorate([m()], i8.prototype, "error", void 0), __decorate([m()], i8.prototype, "form", void 0), __decorate([m()], i8.prototype, "mode", void 0), __decorate([m()], i8.prototype, "objectId", void 0), i8 = __decorate([a3("esri.widgets.FeatureTable.support.AttachmentsViewOptions")], i8);
var a14 = i8;

// node_modules/@arcgis/core/exports/csv.js
function n16(e17) {
  return Object.keys(e17.attributes).map((n19) => {
    const i10 = e17.attributes[n19];
    return "objectid" === n19.toLowerCase() || "fid" === n19.toLowerCase() ? new m3({ name: n19, alias: n19, type: "oid" }) : new m3("number" == typeof i10 ? { name: n19, alias: n19, type: "double" } : { name: n19, alias: n19, type: "string" });
  });
}
function i9(e17) {
  if (!e17.fields) {
    const t5 = e17.features[0];
    if (t5.layer) {
      e17.fields = t5.layer.fields;
      const n19 = Object.keys(t5.attributes), i10 = e17.fields.filter((e18) => n19.includes(e18.name));
      e17.fields = i10;
    } else e17.fields = n16(t5);
  }
  return e17;
}
function o7(e17) {
  return e17.map(({ attributes: e18 }) => e18);
}
function r9(e17) {
  const { delimiter: t5, fields: n19 = [], outFields: i10 = [] } = e17, o8 = t5 || ",", r12 = n19.map((e18) => e18.name);
  return (e18) => {
    let t6 = "";
    return r12.filter((e19) => i10.includes(e19)).forEach((i11) => {
      const r13 = n19.find(({ name: e19 }) => e19 === i11);
      let a17 = e18[i11] || "";
      if ("date" === (r13 == null ? void 0 : r13.type) && (a17 = new Date(a17).toString()), (r13 == null ? void 0 : r13.domain) && "coded-value" === r13.domain.type) {
        const e19 = r13.domain.codedValues.find((e20) => a17 === e20.code);
        e19 && (a17 = e19.name);
      }
      "string" == typeof a17 && a17.includes(o8) && (a17 = `"${a17}"`), t6 += `${a17}${o8}`;
    }), `${t6}\r
`;
  };
}
async function a15(n19, { includeGeometry: a17 = true, delimiter: l17 = ",", outFields: s22 = ["*"] } = {}) {
  n19 = i9(n19), a17 && "point" !== n19.geometryType && i2.getLogger("esri.exports.csv").warn(`The input geometry ${n19.geometryType} is not supported. Geometry will be excluded from export.`);
  const { features: d6 } = n19;
  if (!d6.length) return null;
  let u8 = n19.fields;
  const [m11] = s22;
  "*" === m11 && (s22 = u8.map((e17) => e17.name)), a17 && "point" === n19.geometryType && (u8.some((e17) => "x" === e17.name || "y" === e17.name) || (u8 = [...u8, new m3({ name: "lon", alias: "Longitude", type: "double" }), new m3({ name: "lat", alias: "Latitude", type: "double" })], s22 = [...s22, "lon", "lat"]), d6.forEach(({ attributes: e17, geometry: t5 }) => {
    e17.lon = "point" === (t5 == null ? void 0 : t5.type) ? t5.longitude : null, e17.lat = "point" === (t5 == null ? void 0 : t5.type) ? t5.latitude : null;
  })), u8 = u8.filter((e17) => s22.includes(e17.name));
  const p14 = l17 || ",", c17 = o7(d6), f9 = u8.map((e17) => e17.name).join(p14), y6 = r9({ delimiter: p14, outFields: s22 || u8.map((e17) => e17.name), fields: u8 });
  let g5 = `${f9}${p14}\r
`;
  c17.forEach((e17) => {
    g5 += y6(e17);
  });
  const b7 = new RegExp(`${p14}\r
\\s*$`, "g");
  return g5.replace(b7, "");
}

// node_modules/@arcgis/core/widgets/FeatureTable/support/exportUtils.js
function s18() {
  return "showSaveFilePicker" in window && (() => {
    try {
      return window.self === window.top;
    } catch {
      return false;
    }
  })();
}
async function a16(r12) {
  const { layer: a17, includeGeometry: n19, objectIds: c17, outFields: l17 } = r12, u8 = l17 ?? a17.outFields ?? ["*"], w4 = a17.createQuery();
  w4.objectIds = c17, w4.outFields = u8;
  const p14 = await a17.queryFeatures(w4), m11 = await a15(p14, { includeGeometry: !a17.isTable && (n19 || "point" === p14.geometryType), outFields: u8 });
  if (!m11) throw new s2("export-csv:no-output", "Unable to generate a valid export string");
  const d6 = new Blob([m11], { type: "text/csv" }), f9 = t.sanitize(a17.title);
  if (s18()) try {
    const t5 = await showSaveFilePicker({ suggestedName: f9, types: [{ accept: { "text/csv": [".csv"] } }] }), e17 = await t5.createWritable();
    await e17.write(d6), await e17.close();
  } catch (y6) {
    if (y6 instanceof s2 && "AbortError" !== y6.name) throw y6;
  }
  else ct(d6, `${f9}.csv`);
}
async function n17(t5) {
  if (!(t5 == null ? void 0 : t5.url)) return;
  const e17 = await it(`${t5.url}`);
  ct(e17, t5.name);
}

// node_modules/@arcgis/core/widgets/FeatureTable/support/FeatureStore.js
var C4 = class extends b3 {
  constructor(e17) {
    super(e17), this._attachmentsOperationQueue = new O(), this._defaultOutFields = ["*"], this._editOperationQueue = new O(), this._loaded = false, this._loadError = false, this._loading = false, this._objectIdCache = new O(), this._queryOperationQueue = new O(), this.attachmentsEnabled = false, this.count = 0, this.failures = new O(), this.filterGeometry = null, this.initialSize = null, this.layer = null, this.objectIds = null, this.outFields = null, this.pageCache = new s6(), this.pageSize = 50, this.relationshipConfig = null, this.relatedRecordsEnabled = false, this.returnGeometry = false, this.returnZ = false, this.returnM = false, this.sortOrders = [], this.timeExtent = null, this.view = null, this.where = null;
  }
  initialize() {
    this.addHandles([l(() => [this.filterGeometry, this.layer, this.objectIds, this.outFields, this.pageSize, this.relationshipConfig, this.returnGeometry, this.returnM, this.returnZ, this.timeExtent, this.where], () => this._reset()), l(() => this.orderByFields, () => this._clearCaches(), { equals: h }), f(() => this.attachmentsEnabled, () => this._reset()), f(() => this.relatedRecordsEnabled, () => this._reset())]);
  }
  destroy() {
    var _a2, _b;
    this.layer = null, (_a2 = this.itemCache) == null ? void 0 : _a2.destroy(), (_b = this.failures) == null ? void 0 : _b.destroy();
  }
  get _capabilities() {
    const { layer: e17 } = this;
    return (e17 == null ? void 0 : e17.effectiveCapabilities) ?? (e17 == null ? void 0 : e17.capabilities);
  }
  get _effectiveReturnZ() {
    return this.returnZ && this.supportsZ;
  }
  get _effectiveReturnM() {
    return this.returnM && this.supportsM;
  }
  get _layerWithAttachments() {
    const { layer: e17 } = this;
    return e17 && this._capabilities && this.attachmentsEnabled && this.supportsAttachments && l12(e17) ? e17 : null;
  }
  get _layerWithAttachmentsEditing() {
    const { _layerWithAttachments: e17 } = this;
    return e17 && p4(e17) && this.supportsEditing ? e17 : null;
  }
  get _layerWithEditing() {
    const e17 = this.layer;
    return m6(e17) && this.supportsEditing ? e17 : null;
  }
  get _layerWithRelationships() {
    const e17 = this.layer;
    return d2(e17) && this.supportsQueryRelated ? e17 : null;
  }
  get _relatedLayer() {
    var _a2;
    return (_a2 = this.relationshipConfig) == null ? void 0 : _a2.relatedLayer;
  }
  get _relatedLayerCapabilities() {
    const { _relatedLayer: e17 } = this;
    return (e17 == null ? void 0 : e17.effectiveCapabilities) ?? (e17 == null ? void 0 : e17.capabilities);
  }
  get _relatedLayerSupportsCacheHint() {
    var _a2, _b;
    return !!((_b = (_a2 = this._relatedLayerCapabilities) == null ? void 0 : _a2.queryRelated) == null ? void 0 : _b.supportsCacheHint);
  }
  get _relatedLayerSupportsQuery() {
    var _a2;
    return !!((_a2 = this._relatedLayerCapabilities) == null ? void 0 : _a2.operations.supportsQuery);
  }
  get _relatedLayerSupportsQueryRelated() {
    var _a2, _b;
    const { _relatedLayerCapabilities: e17 } = this;
    return !!((_a2 = e17 == null ? void 0 : e17.queryRelated) == null ? void 0 : _a2.supportsCount) && !!((_b = e17 == null ? void 0 : e17.queryRelated) == null ? void 0 : _b.supportsPagination);
  }
  get _sortedFieldNames() {
    return this.sortOrders.map(({ fieldName: e17 }) => e17);
  }
  get _subtypeCode() {
    var _a2;
    return ((_a2 = this.layer) == null ? void 0 : _a2.subtypeCode) ?? null;
  }
  get _subtypeField() {
    const { layer: e17 } = this;
    return d(e17 == null ? void 0 : e17.parent) ? e17.subtypeField : null;
  }
  get _canUseRelatedLayer() {
    return !(!this._relatedLayerSupportsQuery || !this._relatedLayerSupportsQueryRelated);
  }
  get canAddRelatedFeature() {
    const { state: e17, relationship: t5, supportsAdd: r12, supportsEditing: s22 } = this;
    if ("loaded" !== e17 || !t5 || !r12 || !s22) return false;
    const { count: i10 } = this, { cardinality: a17, role: o8 } = t5, n19 = i10 > 0;
    return ("one-to-one" !== a17 || !n19) && ("one-to-many" !== a17 || "origin" !== o8 || !n19);
  }
  get effectiveOutFields() {
    const { layer: e17, outFields: t5 } = this;
    if (!e17 || !(t5 == null ? void 0 : t5.length)) return this._defaultOutFields;
    const r12 = [];
    t5.forEach((t6) => {
      const s23 = e17.fieldsIndex.normalizeFieldName(t6);
      s23 && r12.push(s23);
    });
    const { objectIdField: s22 } = e17, i10 = e17.fieldsIndex.normalizeFieldName(s22) ?? s22;
    return i10 && !r12.includes(i10) && r12.unshift(i10), r12;
  }
  get effectiveTimeExtent() {
    const { layer: e17, timeExtent: t5 } = this;
    return t5 || e17 && "timeExtent" in e17 && e17.timeExtent || null;
  }
  get effectiveWhere() {
    var _a2;
    const { layer: e17, where: t5 } = this;
    return null != t5 && "" !== t5 ? t5 : m2(e17) && ((_a2 = e17.parent) == null ? void 0 : _a2.definitionExpression) ? e17.parent.definitionExpression : (e17 == null ? void 0 : e17.definitionExpression) || "1=1";
  }
  get isQuerying() {
    return this._queryOperationQueue.length > 0;
  }
  get isSyncing() {
    return this.isSyncingFeatureEdits || this.isSyncingAttachmentEdits;
  }
  get isSyncingAttachmentEdits() {
    return this._attachmentsOperationQueue.length > 0;
  }
  get isSyncingFeatureEdits() {
    return this._editOperationQueue.length > 0;
  }
  get itemCache() {
    return new O({ items: [...this.pageCache.values()].flat() });
  }
  get layerView() {
    const { layer: e17, view: t5 } = this;
    if (!e17 || !t5) return null;
    const r12 = "sublayer" === e17.type || m2(e17) ? e17.parent : e17;
    return t5.allLayerViews.find((e18) => e18.layer === r12);
  }
  get orderByFields() {
    var _a2;
    const { _sortedFieldNames: e17, layer: t5, sortOrders: r12 } = this, s22 = r12.filter(({ fieldName: t6, direction: r13 }, s23) => e17.indexOf(t6) === s23 && null != r13).map(({ fieldName: e18, direction: t6 }) => `${e18} ${t6 == null ? void 0 : t6.toUpperCase()}`);
    if (t5 && !s22.length && !t5.objectIdField && ((_a2 = t5.fields) == null ? void 0 : _a2.length)) {
      const e18 = t5.fields.find((e19) => e19.name);
      if (e18) {
        const r13 = t5.fieldsIndex.normalizeFieldName(e18.name);
        r13 && s22.push(`${r13} ASC`);
      }
    }
    return s22;
  }
  get relationship() {
    var _a2;
    const { relationshipConfig: e17 } = this;
    return null == (e17 == null ? void 0 : e17.relationshipId) ? null : (_a2 = this.relationships) == null ? void 0 : _a2.find((t5) => t5.id === e17.relationshipId);
  }
  get relationshipIds() {
    var _a2;
    return ((_a2 = this.relationships) == null ? void 0 : _a2.map((e17) => e17.id)) ?? [];
  }
  get relationshipInfos() {
    const { _layerWithRelationships: e17, relatedRecordsEnabled: t5, relationships: r12, view: s22 } = this, i10 = [];
    return t5 && (e17 == null ? void 0 : e17.loaded) && (s22 == null ? void 0 : s22.map) && (r12 == null ? void 0 : r12.length) ? (r12.forEach((t6) => {
      const r13 = f4(e17, s22, t6);
      r13 && (r13.load(), i10.push({ layer: e17, relatedLayer: r13, relationshipId: t6.id }));
    }), i10) : i10;
  }
  get relationships() {
    var _a2;
    return (_a2 = this._layerWithRelationships) == null ? void 0 : _a2.relationships;
  }
  get state() {
    const { layer: e17, _loaded: t5, _loadError: r12, _loading: s22 } = this;
    return !e17 || e17.destroyed ? "disabled" : r12 || e17.loadError ? "error" : "loaded" === e17.loadStatus && t5 ? "loaded" : s22 ? "loading" : "ready";
  }
  get supportsAdd() {
    var _a2;
    return !!((_a2 = this._capabilities) == null ? void 0 : _a2.operations.supportsAdd);
  }
  get supportsAddAttachments() {
    return !(!this._layerWithAttachmentsEditing || !this.supportsAdd && !this.supportsUpdate);
  }
  get supportsAttachments() {
    var _a2;
    const { _capabilities: e17 } = this;
    return !(!((_a2 = e17 == null ? void 0 : e17.data) == null ? void 0 : _a2.supportsAttachment) || !(e17 == null ? void 0 : e17.operations.supportsQueryAttachments));
  }
  get supportsCacheHint() {
    var _a2, _b;
    return !!((_b = (_a2 = this._capabilities) == null ? void 0 : _a2.query) == null ? void 0 : _b.supportsCacheHint);
  }
  get supportsCacheHintQueryRelated() {
    var _a2, _b;
    return !!((_b = (_a2 = this._capabilities) == null ? void 0 : _a2.queryRelated) == null ? void 0 : _b.supportsCacheHint);
  }
  get supportsDelete() {
    var _a2;
    return !!((_a2 = this._capabilities) == null ? void 0 : _a2.operations.supportsDelete);
  }
  get supportsEditing() {
    var _a2;
    return !!((_a2 = this._capabilities) == null ? void 0 : _a2.operations.supportsEditing);
  }
  get supportsM() {
    var _a2, _b, _c;
    return !!((_a2 = this.layer) == null ? void 0 : _a2.hasM) && !!((_c = (_b = this._capabilities) == null ? void 0 : _b.data) == null ? void 0 : _c.supportsM);
  }
  get supportsOrderBy() {
    var _a2, _b;
    return !!((_b = (_a2 = this._capabilities) == null ? void 0 : _a2.query) == null ? void 0 : _b.supportsOrderBy);
  }
  get supportsPagination() {
    var _a2, _b;
    return !!((_b = (_a2 = this._capabilities) == null ? void 0 : _a2.query) == null ? void 0 : _b.supportsPagination);
  }
  get supportsQuery() {
    var _a2;
    return !!((_a2 = this._capabilities) == null ? void 0 : _a2.operations.supportsQuery);
  }
  get supportsQueryRelated() {
    var _a2, _b;
    const { _capabilities: e17 } = this;
    return !!((_a2 = e17 == null ? void 0 : e17.queryRelated) == null ? void 0 : _a2.supportsCount) && !!((_b = e17 == null ? void 0 : e17.queryRelated) == null ? void 0 : _b.supportsPagination);
  }
  get supportsResizeAttachments() {
    var _a2;
    const { _capabilities: e17 } = this;
    return !(!this.supportsAttachments || !((_a2 = e17 == null ? void 0 : e17.attachment) == null ? void 0 : _a2.supportsResize));
  }
  get supportsUpdate() {
    var _a2;
    return !!((_a2 = this._capabilities) == null ? void 0 : _a2.operations.supportsUpdate);
  }
  get supportsUpdateAttachments() {
    return !(!this._layerWithAttachmentsEditing || !this.supportsAdd && !this.supportsUpdate);
  }
  get supportsZ() {
    var _a2, _b, _c;
    return !!((_a2 = this.layer) == null ? void 0 : _a2.hasZ) && !!((_c = (_b = this._capabilities) == null ? void 0 : _b.data) == null ? void 0 : _c.supportsZ);
  }
  async addAttachment(e17, t5) {
    const { _layerWithAttachmentsEditing: r12 } = this;
    if (!r12 || !this.supportsAddAttachments) return {};
    const s22 = await this._queueAttachmentsOperation(e17, () => r12.addAttachment(this._createBaseGraphic(r12, e17), t5));
    return this._ensureArrayContent(s22);
  }
  async deleteAttachments(e17, t5) {
    const { _layerWithAttachmentsEditing: r12 } = this;
    if (!r12 || !this.supportsUpdateAttachments || !t5.length) return [];
    const s22 = await this._queueAttachmentsOperation(e17, () => r12.deleteAttachments(this._createBaseGraphic(r12, e17), t5));
    return this._ensureArray(s22);
  }
  async deleteRowsByObjectId(e17) {
    const { _layerWithEditing: t5 } = this;
    if (!this.supportsDelete || !t5) throw new s2("store:delete-error", "Delete is not supported.");
    const r12 = e17.map((e18) => {
      var _a2;
      return (_a2 = this.getItemByObjectId(e18)) == null ? void 0 : _a2.feature;
    }).filter(N);
    return this._queueEditOperation(() => t5.applyEdits({ deleteFeatures: r12 }));
  }
  async fetchItems(e17) {
    const { page: t5 } = e17, { pageSize: r12 } = this, s22 = t5 * r12, i10 = s22 + r12, { layer: a17, state: o8 } = this;
    return a17 && "loaded" === o8 ? this._query({ layer: a17, start: s22, num: i10, page: t5, pageSize: r12 }) : [];
  }
  async getAttachmentsByObjectId(e17, t5) {
    var _a2;
    const r12 = ((_a2 = this.getItemByObjectId(e17)) == null ? void 0 : _a2.attachments) ?? [];
    if (!r12.length && t5) {
      return (await this._queryAttachments([e17]))[e17] ?? [];
    }
    return r12;
  }
  getItemByObjectId(e17) {
    for (const t5 of this.pageCache.values()) for (const r12 of t5) if (r12.objectId === e17) return r12;
  }
  getItemIndexByObjectId(e17) {
    const { pageCache: t5, pageSize: r12 } = this, s22 = t5.entries();
    let i10 = -1;
    for (const [a17, o8] of s22) if (o8.some((t6, s23) => {
      const o9 = t6.objectId === e17;
      return o9 && (i10 = a17 * r12 + s23), o9;
    }), i10 > -1) break;
    return i10;
  }
  async load() {
    const { layer: e17 } = this;
    if (!this._loading && !this._loaded && e17) {
      this._reset(), this._loading = true;
      try {
        await e17.load(), e17.parent && d(e17.parent) && await e17.parent.loadAll(), await e17.when();
        const t5 = this.initialSize;
        this.initialSize = null, await this.refresh(t5), this._loaded = true, this._loading = false;
      } catch (t5) {
        throw this._reset(), this._loadError = true, this._logError("store:load-error", "An error occurred."), t5;
      }
    }
  }
  async query(e17) {
    const { state: t5 } = this;
    return this.supportsQuery && "loaded" === t5 ? this._query(e17) : [];
  }
  async refresh(e17) {
    this._clearCaches(), await this._syncCount(e17), await this._syncObjectIdCache();
  }
  reset() {
    this._reset();
  }
  async updateAttachment(e17, t5, r12) {
    const { _layerWithAttachmentsEditing: s22 } = this;
    if (!s22 || !this.supportsUpdateAttachments) return {};
    const i10 = await this._queueAttachmentsOperation(e17, () => s22.updateAttachment(this._createBaseGraphic(s22, e17), t5, r12));
    return this._ensureArrayContent(i10);
  }
  async updateItem(e17) {
    const { _sortedFieldNames: t5 } = this, r12 = e17.updates.map(({ fieldName: e18 }) => e18), s22 = await this._update(e17);
    return !s22.updateFeatureResults.find((e18) => e18.error) && r12.some((e18) => t5.includes(e18)) && this._clearCaches(), s22;
  }
  async verifyFeaturesByObjectId(e17) {
    const { layer: t5, state: r12 } = this;
    if (!t5 || "loaded" !== r12) return [];
    const { objectIdField: s22 } = t5, i10 = await this._verifyFeaturesByObjectId(e17);
    return e17.map((e18) => i10.some((t6) => {
      const r13 = t6.getObjectId() ?? t6.attributes[s22];
      return e18 === r13;
    }));
  }
  _clearCaches() {
    this.pageCache.clear(), this._objectIdCache.removeAll();
  }
  _createBaseGraphic(e17, r12) {
    const s22 = new _2({ attributes: { [e17.objectIdField]: r12 } });
    return this._subtypeField && null != this._subtypeCode && (s22.attributes[this._subtypeField] = this._subtypeCode), s22;
  }
  _ensureArray(e17) {
    return Array.isArray(e17) ? e17 : [e17];
  }
  _ensureArrayContent(e17) {
    return Array.isArray(e17) ? e17[0] : e17;
  }
  _getObjectIdsForPage(e17, t5) {
    const r12 = this._objectIdCache.toArray();
    return r12.length >= e17 + t5 ? r12.slice(e17, e17 + t5) : r12.slice(e17);
  }
  _logError(e17, t5) {
    i2.getLogger(this).error(e17, t5);
  }
  _reset() {
    this._clearCaches(), this.failures.removeAll(), this._attachmentsOperationQueue.removeAll(), this._editOperationQueue.removeAll(), this._queryOperationQueue.removeAll(), this._loading = false, this._loaded = false, this._loadError = false, this._set("count", 0);
  }
  async _syncCount(e17) {
    null == e17 ? await this._queryCount().then((e18) => this._set("count", e18)) : this._set("count", e17);
  }
  async _syncObjectIdCache() {
    if (this.supportsPagination) return;
    const e17 = await this._queryObjectIds();
    this._objectIdCache.removeAll(), this._objectIdCache.addMany(e17);
  }
  async _verifyFeaturesByObjectId(e17) {
    const { layer: t5 } = this;
    if (!t5 || !this.supportsQuery) throw new s2("store:query-error", "Layer does not support query operation.");
    const { effectiveTimeExtent: r12, effectiveWhere: s22, orderByFields: i10, supportsCacheHint: a17, supportsOrderBy: n19 } = this, u8 = t5.createQuery();
    u8.where = s22, u8.timeExtent = r12, u8.returnGeometry = false, u8.objectIds = e17.length ? e17 : void 0, u8.outFields = [t5.objectIdField], n19 && (u8.orderByFields = i10), a17 && (u8.cacheHint = true);
    return (await t5.queryFeatures(u8)).features;
  }
  async _update(e17) {
    var _a2;
    const { objectId: r12, updates: s22 } = e17, { _layerWithEditing: i10 } = this;
    if (!this.supportsUpdate || !i10) throw new s2("store:update-error", "Update is not supported.");
    const a17 = (_a2 = this.getItemByObjectId(r12)) == null ? void 0 : _a2.feature;
    if (!a17) throw new s2("store:update-error", "Feature with provided 'objectId' not found.");
    const u8 = a(a17.attributes);
    s22.forEach(({ fieldName: e18, value: t5 }) => u8[e18] = t5);
    const p14 = new _2({ attributes: u8 });
    p14.sourceLayer = a17.sourceLayer;
    const l17 = i10.applyEdits({ updateFeatures: [p14] }).then((e18) => {
      const { updateFeatureResults: t5 } = e18, r13 = t5.find((e19) => !!e19.error);
      if (r13) throw r13.error;
      return t5.length && (a17.attributes = u8), e18;
    });
    return this._queueEditOperation(() => l17);
  }
  _prepareFeatureData(e17) {
    const { attachments: t5, features: r12, layer: s22, relatedRecords: i10 } = e17, a17 = s22.objectIdField;
    return r12.map((e18) => {
      const r13 = e18.getObjectId() ?? e18.attributes[a17], o8 = [];
      return e18.layer || e18.sourceLayer || (e18.layer = s22, e18.sourceLayer = s22), i10.forEach((e19, t6) => {
        o8.push({ relationshipId: t6, count: e19[r13] ?? 0 });
      }), { objectId: r13, feature: e18, attachments: t5[r13] ?? null, relatedRecords: o8 };
    });
  }
  async _query(e17) {
    const { page: t5, refresh: r12 } = e17;
    true === r12 && await this.refresh();
    const s22 = this.pageCache.get(t5);
    if (s22) return s22;
    const i10 = await this._queryPage(e17);
    return this.pageCache.set(t5, i10), i10;
  }
  _queryAttachments(e17) {
    const { _layerWithAttachments: t5, effectiveWhere: r12 } = this;
    return t5 ? e17.some((e18) => "number" != typeof e18) ? Promise.reject(new s2("invalid-object-id", "Only numeric object ids are valid to query attachments")) : t5.queryAttachments(new p2({ objectIds: e17, where: r12, returnMetadata: true })) : Promise.resolve({});
  }
  async _queryCount() {
    const { _relatedLayerSupportsCacheHint: e17, layer: t5, relationshipConfig: r12, supportsCacheHint: s22 } = this;
    if (!t5) return 0;
    if (r12 && this._canUseRelatedLayer) {
      const { objectId: t6, relatedLayer: s23, relationshipId: i11 } = r12;
      return (await s23.queryRelatedFeaturesCount(new u3({ cacheHint: e17, relationshipId: i11, objectIds: [t6] })))[t6] ?? 0;
    }
    const { effectiveTimeExtent: i10, effectiveWhere: a17, filterGeometry: o8, objectIds: n19 } = this, u8 = t5.createQuery();
    return u8.geometry = o8, u8.returnGeometry = false, u8.where = a17, u8.timeExtent = i10, u8.objectIds = (n19 == null ? void 0 : n19.length) ? n19 : void 0, s22 && (u8.cacheHint = true), t5.queryFeatureCount(u8);
  }
  async _queryFeatures(e17) {
    const { relationshipConfig: t5 } = this;
    if (t5 && this._canUseRelatedLayer) return this._queryRelatedFeatures(e17, t5);
    const { _effectiveReturnM: r12, _effectiveReturnZ: s22, effectiveOutFields: i10, effectiveTimeExtent: a17, effectiveWhere: o8, filterGeometry: n19, objectIds: u8, orderByFields: p14, pageSize: l17, returnGeometry: h9, supportsCacheHint: d6, supportsOrderBy: c17, supportsPagination: y6 } = this, { layer: m11, start: _4 } = e17, f9 = u8 == null ? void 0 : u8.length, g5 = m11.createQuery();
    g5.returnGeometry = h9, g5.outFields = i10, g5.returnM = r12, g5.returnZ = s22, y6 ? (g5.start = _4, g5.num = l17, g5.where = o8, g5.timeExtent = a17, g5.objectIds = f9 ? u8 : void 0) : g5.objectIds = f9 ? u8 : this._getObjectIdsForPage(_4, l17 ?? 0), c17 && (g5.orderByFields = p14), n19 && (g5.geometry = n19), d6 && (g5.cacheHint = true);
    return (await m11.queryFeatures(g5)).features;
  }
  _queryObjectIds() {
    const { effectiveTimeExtent: e17, effectiveWhere: t5, filterGeometry: r12, layer: s22, objectIds: i10, orderByFields: a17, supportsCacheHint: o8, supportsOrderBy: n19 } = this;
    if (!s22) return Promise.resolve([]);
    const u8 = s22.createQuery();
    return u8.geometry = r12, u8.outFields = [s22.objectIdField], u8.returnGeometry = false, u8.where = t5, u8.timeExtent = e17, u8.objectIds = (i10 == null ? void 0 : i10.length) ? i10 : void 0, n19 && (u8.orderByFields = a17), o8 && (u8.cacheHint = true), s22.queryObjectIds(u8);
  }
  async _queryPage(e17) {
    const { layer: t5 } = e17;
    return this._queueQueryOperation(async () => {
      const r12 = await this._queryFeatures(e17), s22 = r12.map((e18) => e18.getObjectId() ?? e18.attributes[t5.objectIdField]), i10 = await this._queryAttachments(s22), a17 = await this._queryRelatedCounts(s22);
      return this._prepareFeatureData({ layer: t5, features: r12, attachments: i10, relatedRecords: a17 }) || [];
    });
  }
  async _queryRelatedCounts(e17) {
    const { _layerWithRelationships: t5, relationshipIds: r12, supportsCacheHintQueryRelated: s22 } = this, i10 = /* @__PURE__ */ new Map();
    return t5 && (e17 == null ? void 0 : e17.length) && r12.length ? (await Promise.allSettled(r12.map(async (r13) => {
      const a17 = await t5.queryRelatedFeaturesCount(new u3({ cacheHint: s22, relationshipId: r13, objectIds: e17 }));
      i10.set(r13, a17);
    })), i10) : i10;
  }
  async _queryRelatedFeatures(e17, t5) {
    var _a2;
    const { _defaultOutFields: r12, _effectiveReturnM: s22, _effectiveReturnZ: i10, orderByFields: a17, pageSize: o8, returnGeometry: n19, supportsCacheHint: u8 } = this, { layer: p14, start: l17 } = e17, { objectId: h9, relatedLayer: d6, relationshipId: c17 } = t5, y6 = p14.createQuery(), m11 = new u3({ cacheHint: u8, num: o8, objectIds: [h9], orderByFields: a17, outFields: r12, relationshipId: c17, returnGeometry: n19, returnM: s22, returnZ: i10, start: l17, where: y6.where ?? void 0 }), _4 = await d6.queryRelatedFeatures(m11);
    return ((_a2 = _4[h9]) == null ? void 0 : _a2.features) || [];
  }
  _queueAttachmentsOperation(e17, t5) {
    return this._attachmentsOperationQueue.push(t5), t5().then(async (r12) => {
      this._attachmentsOperationQueue.remove(t5);
      const s22 = this._ensureArray(r12), i10 = s22.some((e18) => e18.error), a17 = this.getItemByObjectId(e17);
      if (a17 && (!i10 || s22.length > 1)) {
        const t6 = await this._queryAttachments([e17]);
        a17.attachments = t6[e17];
      }
      return r12;
    }).catch(async (r12) => {
      this._logError("store:attachment-error", "An error occurred.");
      const s22 = { error: r12, retry: () => {
        this.failures.remove(s22), this._queueAttachmentsOperation(e17, t5);
      }, cancel: () => this.failures.remove(s22) };
      throw this.failures.add(s22), this._attachmentsOperationQueue.remove(t5), r12;
    });
  }
  _queueEditOperation(e17) {
    return this._editOperationQueue.push(e17), e17().then((t5) => (this._editOperationQueue.remove(e17), t5)).catch((t5) => {
      this._logError("store:edit-error", "An error occurred.");
      const r12 = { error: t5, retry: () => {
        this.failures.remove(r12), this._queueEditOperation(e17);
      }, cancel: () => this.failures.remove(r12) };
      throw this.failures.add(r12), this._editOperationQueue.remove(e17), t5;
    });
  }
  _queueQueryOperation(e17) {
    return this._queryOperationQueue.push(e17), e17().then((t5) => this._queryOperationQueue.includes(e17) ? t5 : []).catch((t5) => {
      this._logError("store:query-error", "An error occurred.");
      const r12 = { error: t5, retry: () => {
        this.failures.remove(r12), this._queueQueryOperation(e17);
      }, cancel: () => this.failures.remove(r12) };
      return this.failures.add(r12), [];
    }).then((t5) => (this._queryOperationQueue.remove(e17), t5));
  }
};
__decorate([m()], C4.prototype, "_attachmentsOperationQueue", void 0), __decorate([m({ readOnly: true })], C4.prototype, "_capabilities", null), __decorate([m()], C4.prototype, "_defaultOutFields", void 0), __decorate([m()], C4.prototype, "_editOperationQueue", void 0), __decorate([m()], C4.prototype, "_effectiveReturnZ", null), __decorate([m()], C4.prototype, "_effectiveReturnM", null), __decorate([m({ readOnly: true })], C4.prototype, "_layerWithAttachments", null), __decorate([m({ readOnly: true })], C4.prototype, "_layerWithAttachmentsEditing", null), __decorate([m({ readOnly: true })], C4.prototype, "_layerWithEditing", null), __decorate([m({ readOnly: true })], C4.prototype, "_layerWithRelationships", null), __decorate([m()], C4.prototype, "_loaded", void 0), __decorate([m()], C4.prototype, "_loadError", void 0), __decorate([m()], C4.prototype, "_loading", void 0), __decorate([m()], C4.prototype, "_objectIdCache", void 0), __decorate([m()], C4.prototype, "_queryOperationQueue", void 0), __decorate([m({ readOnly: true })], C4.prototype, "_relatedLayer", null), __decorate([m({ readOnly: true })], C4.prototype, "_relatedLayerCapabilities", null), __decorate([m({ readOnly: true })], C4.prototype, "_relatedLayerSupportsCacheHint", null), __decorate([m()], C4.prototype, "_relatedLayerSupportsQuery", null), __decorate([m()], C4.prototype, "_relatedLayerSupportsQueryRelated", null), __decorate([m()], C4.prototype, "_sortedFieldNames", null), __decorate([m()], C4.prototype, "_subtypeCode", null), __decorate([m()], C4.prototype, "_subtypeField", null), __decorate([m()], C4.prototype, "_canUseRelatedLayer", null), __decorate([m()], C4.prototype, "attachmentsEnabled", void 0), __decorate([m()], C4.prototype, "canAddRelatedFeature", null), __decorate([m({ readOnly: true })], C4.prototype, "count", void 0), __decorate([m()], C4.prototype, "effectiveOutFields", null), __decorate([m()], C4.prototype, "effectiveTimeExtent", null), __decorate([m()], C4.prototype, "effectiveWhere", null), __decorate([m({ readOnly: true })], C4.prototype, "failures", void 0), __decorate([m({ types: l7 })], C4.prototype, "filterGeometry", void 0), __decorate([m()], C4.prototype, "initialSize", void 0), __decorate([m({ readOnly: true })], C4.prototype, "isQuerying", null), __decorate([m({ readOnly: true })], C4.prototype, "isSyncing", null), __decorate([m({ readOnly: true })], C4.prototype, "isSyncingAttachmentEdits", null), __decorate([m({ readOnly: true })], C4.prototype, "isSyncingFeatureEdits", null), __decorate([m({ readOnly: true })], C4.prototype, "itemCache", null), __decorate([m()], C4.prototype, "layer", void 0), __decorate([m()], C4.prototype, "layerView", null), __decorate([m()], C4.prototype, "objectIds", void 0), __decorate([m()], C4.prototype, "orderByFields", null), __decorate([m()], C4.prototype, "outFields", void 0), __decorate([m({ readOnly: true })], C4.prototype, "pageCache", void 0), __decorate([m()], C4.prototype, "pageSize", void 0), __decorate([m()], C4.prototype, "relationship", null), __decorate([m()], C4.prototype, "relationshipConfig", void 0), __decorate([m()], C4.prototype, "relationshipIds", null), __decorate([m()], C4.prototype, "relationshipInfos", null), __decorate([m()], C4.prototype, "relationships", null), __decorate([m()], C4.prototype, "relatedRecordsEnabled", void 0), __decorate([m()], C4.prototype, "returnGeometry", void 0), __decorate([m()], C4.prototype, "returnZ", void 0), __decorate([m()], C4.prototype, "returnM", void 0), __decorate([m()], C4.prototype, "sortOrders", void 0), __decorate([m({ readOnly: true })], C4.prototype, "state", null), __decorate([m()], C4.prototype, "supportsAdd", null), __decorate([m()], C4.prototype, "supportsAddAttachments", null), __decorate([m()], C4.prototype, "supportsAttachments", null), __decorate([m()], C4.prototype, "supportsCacheHint", null), __decorate([m()], C4.prototype, "supportsCacheHintQueryRelated", null), __decorate([m()], C4.prototype, "supportsDelete", null), __decorate([m()], C4.prototype, "supportsEditing", null), __decorate([m()], C4.prototype, "supportsM", null), __decorate([m()], C4.prototype, "supportsOrderBy", null), __decorate([m()], C4.prototype, "supportsPagination", null), __decorate([m()], C4.prototype, "supportsQuery", null), __decorate([m()], C4.prototype, "supportsQueryRelated", null), __decorate([m()], C4.prototype, "supportsResizeAttachments", null), __decorate([m()], C4.prototype, "supportsUpdate", null), __decorate([m()], C4.prototype, "supportsUpdateAttachments", null), __decorate([m()], C4.prototype, "supportsZ", null), __decorate([m({ type: m4 })], C4.prototype, "timeExtent", void 0), __decorate([m()], C4.prototype, "view", void 0), __decorate([m()], C4.prototype, "where", void 0), C4 = __decorate([a3("esri.widgets.FeatureTable.support.FeatureStore")], C4);
var w3 = C4;

// node_modules/@arcgis/core/widgets/FeatureTable/support/EditableColumnTemplateMixin.js
var s19 = (s22) => {
  const i10 = s22;
  let p14 = class extends i10 {
    constructor() {
      super(...arguments), this.editable = true, this.hint = null, this.input = null, this.required = false;
    }
  };
  return __decorate([m({ type: Boolean, json: { write: true } })], p14.prototype, "editable", void 0), __decorate([m({ type: String, json: { write: true } })], p14.prototype, "hint", void 0), __decorate([m({ types: P2, json: { read: { source: "inputType" }, write: { target: "inputType" } } })], p14.prototype, "input", void 0), __decorate([m()], p14.prototype, "required", void 0), p14 = __decorate([a3("esri.widgets.FeatureTable.support.EditableColumnTemplateMixin")], p14), p14;
};

// node_modules/@arcgis/core/widgets/FeatureTable/support/FieldColumnTemplate.js
var n18 = class extends s19(s17) {
  constructor(o8) {
    super(o8), this.domain = null, this.editableExpression = null, this.format = null, this.formatFunction = null, this.requiredExpression = null, this.sortable = true, this.type = "field", this.valueExpression = null, this.visibilityExpression = null;
  }
};
__decorate([m({ types: t2, json: { read: { reader: a4 }, write: true } })], n18.prototype, "domain", void 0), __decorate([m({ type: String, json: { write: true } }), m()], n18.prototype, "editableExpression", void 0), __decorate([m()], n18.prototype, "format", void 0), __decorate([m()], n18.prototype, "formatFunction", void 0), __decorate([m({ type: String, json: { write: true } })], n18.prototype, "requiredExpression", void 0), __decorate([m()], n18.prototype, "sortable", void 0), __decorate([m({ type: String, json: { read: false, write: true } })], n18.prototype, "type", void 0), __decorate([m({ type: String, json: { write: true } })], n18.prototype, "valueExpression", void 0), __decorate([m({ type: String, json: { write: true } })], n18.prototype, "visibilityExpression", void 0), n18 = __decorate([a3("esri.widgets.FeatureTable.support.FieldColumnTemplate")], n18);
var l16 = n18;

// node_modules/@arcgis/core/widgets/FeatureTable/support/RelationshipColumnTemplate.js
var s20 = class extends s17 {
  constructor(o8) {
    super(o8), this.collapsed = false, this.icon = "link", this.relationshipId = null, this.sortable = false, this.textAlign = "center", this.type = "relationship";
  }
};
__decorate([m()], s20.prototype, "collapsed", void 0), __decorate([m()], s20.prototype, "icon", void 0), __decorate([m()], s20.prototype, "relationshipId", void 0), __decorate([m()], s20.prototype, "sortable", void 0), __decorate([m()], s20.prototype, "textAlign", void 0), __decorate([m({ type: String, json: { read: false, write: true } })], s20.prototype, "type", void 0), s20 = __decorate([a3("esri.widgets.FeatureTable.support.RelationshipColumnTemplate")], s20);
var p11 = s20;

// node_modules/@arcgis/core/widgets/FeatureTable/support/ColumnTemplate.js
var p12 = class extends s19(s17) {
  constructor(o8) {
    super(o8), this.type = "column";
  }
};
__decorate([m({ type: String, json: { read: false, write: true } })], p12.prototype, "type", void 0), p12 = __decorate([a3("esri.widgets.FeatureTable.support.ColumnTemplate")], p12);
var m10 = p12;

// node_modules/@arcgis/core/widgets/FeatureTable/support/GroupColumnTemplate.js
var u7 = class extends s17 {
  constructor(e17) {
    super(e17), this.columnTemplates = null, this.type = "group";
  }
  castColumnTemplates(e17) {
    if (!e17 || !Array.isArray(e17)) return [];
    const t5 = [];
    return e17.forEach((e18) => {
      if (e18 instanceof l16 || e18 instanceof m10 || e18 instanceof i7 || e18 instanceof p11) t5.push(e18);
      else if (n12(e18)) {
        const r12 = e18.type;
        "field" === r12 ? t5.push(new l16(e18)) : "group" === r12 ? i2.getLogger(this).warn("A GroupColumnTemplate cannot contain other GroupColumn templates. Template removed from 'GroupColumnTemplate.columnTemplates'.") : "column" === r12 ? t5.push(new m10(e18)) : "attachment" === r12 ? t5.push(new i7(e18)) : "relationship" === r12 && t5.push(new p11(e18));
      } else i2.getLogger(this).warn("Property 'type' is missing from the provided template. Template removed from 'tableTemplate.columnTemplates'.");
    }), t5;
  }
};
__decorate([m({ json: { write: true } })], u7.prototype, "columnTemplates", void 0), __decorate([s7("columnTemplates")], u7.prototype, "castColumnTemplates", null), __decorate([m({ type: String, json: { read: false, write: true } })], u7.prototype, "type", void 0), u7 = __decorate([a3("esri.widgets.FeatureTable.support.GroupColumnTemplate")], u7);
var c16 = u7;

// node_modules/@arcgis/core/widgets/FeatureTable/support/TableTemplate.js
var f8 = class extends l6(n4) {
  constructor(o8) {
    super(o8), this.columnTemplates = [];
  }
  castColumnTemplates(o8) {
    if (!o8 || !Array.isArray(o8)) return [];
    const e17 = [];
    return o8.forEach((o9) => {
      if (o9 instanceof l16 || o9 instanceof c16 || o9 instanceof m10 || o9 instanceof i7 || o9 instanceof p11) e17.push(o9);
      else if (n12(o9)) {
        const t5 = o9.type;
        "field" === t5 ? e17.push(new l16(o9)) : "group" === t5 ? e17.push(new c16(o9)) : "column" === t5 ? e17.push(new m10(o9)) : "attachment" === t5 ? e17.push(new i7(o9)) : "relationship" === t5 && e17.push(new p11(o9));
      } else i2.getLogger(this).warn("Property 'type' is missing from the provided template. Template removed from 'tableTemplate.columnTemplates'.");
    }), e17;
  }
};
__decorate([m()], f8.prototype, "columnTemplates", void 0), __decorate([s7("columnTemplates")], f8.prototype, "castColumnTemplates", null), f8 = __decorate([a3("esri.widgets.FeatureTable.support.TableTemplate")], f8);
var T3 = f8;

// node_modules/@arcgis/core/widgets/FeatureTable/support/templateUtils.js
function N4(i10) {
  const t5 = [];
  return i10.forEach((i11) => {
    var _a2;
    const { hidden: l17 } = i11;
    if (e14(i11)) {
      if (l17) return;
      const n19 = (_a2 = i11.columns) == null ? void 0 : _a2.map((e17) => E2(e17)).filter(N);
      if (!(n19 == null ? void 0 : n19.length)) return;
      t5.push(new i3({ elements: n19 }));
    } else {
      const e17 = E2(i11);
      if (!e17) return;
      t5.push(e17);
    }
  }), t5;
}
function E2(e17) {
  const { fieldName: i10, hidden: t5 } = e17;
  if (t4(e17)) {
    if (!t5) return new l8({ fieldName: i10 });
  } else if (i4(e17)) {
    if (!t5) return new l9({ relationshipId: e17.relationshipId });
  } else if (o4(e17) && !t5) return new a6();
}

// node_modules/@arcgis/core/widgets/FeatureTable/FeatureTableViewModel.js
var pe = "80px";
var ue = class extends a12 {
  constructor(e17) {
    super(e17), this._currentTemporaryHighlight = null, this._highlights = new r2(), this._refreshDebounced = L(() => this._refresh()), this._syncTemplateThrottled = l11(() => this._syncAttributeTableTemplate()), this._temporaryHighlightAbortController = null, this.attachmentsViewOptions = new a14(), this.attributeTableTemplate = null, this.autoRefreshEnabled = true, this.columns = new O(), this.dataProvider = async (e18, t5) => {
      const { layer: i10, store: s22 } = this;
      if (!t5) return;
      if (!i10 || !s22) return void (t5 && t5([]));
      await i10.load();
      const { objectIds: l17 } = this;
      if (this.filterBySelectionEnabled && !this.highlightIds.length || 1 === l17.length && -1 === l17.items[0]) return void t5([]);
      "loaded" !== s22.state && "loading" !== s22.state && await s22.load();
      const o8 = this.paginationEnabled ? this.pageIndex : e18.page, n19 = await s22.fetchItems({ ...e18, page: o8 });
      t5 && t5(n19);
    }, this.grid = null, this.highlightEnabled = true, this.layers = null, this.maxSize = null, this.menuConfig = null, this.messages = null, this.messagesCommon = null, this.messagesUnits = null, this.messagesURIUtils = null, this.prompt = null, this.relatedTables = new O(), this.relationshipColumnConfigs = null, this.selectionSource = "table", this.showRelatedTableCallback = null, this.store = null, this.syncTemplateOnChangesEnabled = true, this.tableController = null, this.tableParent = null, this.tableTemplate = null, this.tableTemplateOverride = null, this._onLayerRefresh = this._onLayerRefresh.bind(this), this._onShowPromptCallback = this._onShowPromptCallback.bind(this), this._set("store", new w3()), this._set("grid", new y5({ viewModel: this }));
  }
  initialize() {
    const e17 = async () => {
      this.messages = await f2("esri/widgets/FeatureTable/t9n/FeatureTable"), this.messagesCommon = await f2("esri/t9n/common"), this.messagesUnits = await f2("esri/core/t9n/Units"), this.messagesURIUtils = await f2("esri/widgets/support/t9n/uriUtils");
    };
    e17(), this.addHandles([b(() => {
      e17(), this._generateColumns();
    }), l(() => [this.messages, this.messagesCommon, this.messagesURIUtils], () => {
      const e18 = this.messages;
      this.grid && (this.grid.messages = e18), this.allColumns.forEach((t5) => {
        t5.messages = e18, t5.messagesCommon = this.messagesCommon, t5.messagesURIUtils = this.messagesURIUtils;
      }), this.refreshCellContent();
    }, h2), a2(() => this.highlightIds, "change", (e18) => this._onHighlightIdsChange(e18), { onListenerAdd: () => this._syncSelection(), onListenerRemove: () => this._highlights.removeAll() }), l(() => {
      var _a2;
      return (_a2 = this.grid) == null ? void 0 : _a2.temporaryHighlightId;
    }, (e18) => {
      var _a2;
      (_a2 = this._temporaryHighlightAbortController) == null ? void 0 : _a2.abort(), this._temporaryHighlightAbortController = new AbortController(), y(this._syncTemporaryHighlight(e18, this._temporaryHighlightAbortController.signal));
    }), l(() => this.attachmentsEnabled, () => {
      var _a2;
      return ((_a2 = this.layer) == null ? void 0 : _a2.loaded) && this._generateColumns();
    }), l(() => this._viewSelection, () => {
      this._viewSelectionReady && (this.highlightIds.items = this._viewSelection);
    }), l(() => [this._tableHighlightsReady, this._viewSelectionReady], () => this._syncSelection()), l(() => this.layer, async (e18, t5) => {
      const i10 = this.grid;
      i10 && (i10.clearSort(), e18 && t5 && await this.reset()), this.goToPage(0), this._drainColumns(), this.store.layer = e18, e18 && (e18.loaded ? this._onLayerLoad() : e18.load().catch(() => {
      })), t5 && null != this.attachmentsViewOptions.objectId && (this.attachmentsViewOptions.objectId = null);
    }), l(() => {
      var _a2;
      return (_a2 = this.layer) == null ? void 0 : _a2.loaded;
    }, (e18) => e18 && this._onLayerLoad()), l(() => this.store.state, (e18) => {
      "loaded" === e18 && (this.scrollToTop(), this.refreshCellContent());
    }), l(() => this._effectiveTableTemplate, () => {
      var _a2;
      this.scrollLeft(), ((_a2 = this.layer) == null ? void 0 : _a2.loaded) && this._generateColumns();
    }), l(() => this.editingEnabled, (e18) => {
      this.editableColumns.forEach((t5) => t5.tableEditingEnabled = e18), this.refreshCellContent();
    }), l(() => {
      var _a2;
      return this.timeZone ?? ((_a2 = this.view) == null ? void 0 : _a2.timeZone);
    }, () => this.refreshCellContent()), l(() => this.store.effectiveWhere, () => "loaded" === this.store.state && this.reset()), l(() => {
      var _a2;
      return (_a2 = this.layer) == null ? void 0 : _a2.timeExtent;
    }, (e18, t5) => (e18 || t5) && !this.timeExtent && "loaded" === this.store.state && this.reset()), a2(() => this.layer, "refresh", (e18) => this._onLayerRefresh(e18)), a2(() => this.grid, ["cell-click", "cell-dblclick", "cell-pointerover", "cell-pointerout", "cell-keydown", "column-reorder"], ({ index: e18, item: t5, native: i10, path: s22, type: l17 }) => {
      const o8 = t5 == null ? void 0 : t5.feature, n19 = o8 == null ? void 0 : o8.getObjectId(), r12 = "cell-keydown" === l17 && i10 && i10 instanceof KeyboardEvent && "Enter" === i10.key;
      this.emit(l17, { feature: o8, index: e18, native: i10, type: l17, fieldName: s22 ?? void 0, objectId: n19 }), (r12 || "cell-click" === l17) && this._onCellInteraction(n19);
    }), l(() => this.actionColumnConfig, (e18) => {
      const { actionColumn: t5, columns: i10 } = this;
      t5 ? e18 ? t5.set(e18) : i10.remove(t5) : e18 && i10.length && i10.add(this._generateActionColumn(), false !== e18.frozenToEnd ? void 0 : this._getIndexOfFirstFrozenToEndColumn());
    }), l(() => this.relatedRecordsEnabled, () => {
      var _a2;
      return ((_a2 = this.layer) == null ? void 0 : _a2.loaded) && this._generateColumns();
    }), a2(() => this.objectIds, "change", () => this._onObjectIdsChange(), { onListenerAdd: () => this._onObjectIdsChange() }), l(() => [this.paginationEnabled, this.pageIndex], () => this.refreshPageCache()), l(() => this.activeSortOrders, (e18) => {
      this.store.sortOrders = e18, this._syncTemplateThrottled();
    }), l(() => this.allVisibleColumns, () => this._syncTemplateThrottled()), l(() => this.pageCount, () => {
      this.pageIndex >= this.pageCount && this.goToPage(0);
    }), l(() => this.state, (e18) => {
      "disabled" === e18 && this.relatedTables.length && this.drainRelatedTables();
    }), a2(() => {
      var _a2;
      return (_a2 = this.tableParent) == null ? void 0 : _a2.highlightIds;
    }, "change", ({ added: e18 }) => {
      const { relationshipConfig: t5 } = this, i10 = e18[0];
      if (null == i10 || !t5) return;
      const { relatedLayer: s22, relationshipId: l17 } = t5;
      this.relationshipConfig = { objectId: i10, relatedLayer: s22, relationshipId: l17 };
    }), a2(() => this.tableParent, "cell-click", (e18) => {
      const { tableParent: t5 } = this, { objectId: i10 } = e18;
      t5 && null != i10 && t5.highlightIds.add(i10);
    }), l(() => this.attachmentsViewOptions.objectId, (e18, t5) => {
      null != e18 ? null == t5 && this.showAttachmentsView({ objectId: e18 }) : this.hideAttachmentsView();
    }), l(() => this.layerFieldConfigurations, () => this.refreshCellContent())]);
  }
  destroy() {
    var _a2, _b;
    this._drainColumns(), this._highlights.removeAll(), this._highlights.destroy(), (_a2 = this._currentTemporaryHighlight) == null ? void 0 : _a2.handle.remove(), this._temporaryHighlightAbortController = e(this._temporaryHighlightAbortController), (_b = this.grid) == null ? void 0 : _b.destroy(), this.columns.destroyed || this.columns.destroy(), this.layer = null, this.view = null;
  }
  get _defaultHiddenFields() {
    return G2(this.layer);
  }
  get _effectiveTableTemplate() {
    return this.tableTemplateOverride || this.tableTemplate;
  }
  get _effectiveAttributeTableTemplate() {
    var _a2;
    return this.attributeTableTemplate || ((_a2 = this.layer) == null ? void 0 : _a2.attributeTableTemplate);
  }
  get _highlightableLayerView() {
    const e17 = this.layerView;
    return n7(e17) ? e17 : null;
  }
  get _selectionManager() {
    var _a2, _b;
    const { view: e17 } = this;
    return !e17 || e17.destroyed ? null : ((_b = (_a2 = e17.selectionManager) == null ? void 0 : _a2.sources) == null ? void 0 : _b.length) ? e17.selectionManager : null;
  }
  get _selectableLayer() {
    var _a2, _b;
    const { layer: e17 } = this;
    if (e12(e17) || m2(e17)) {
      const t5 = e17;
      return ((_b = (_a2 = this._selectionManager) == null ? void 0 : _a2.sources) == null ? void 0 : _b.includes(t5)) ? t5 : null;
    }
    return null;
  }
  get _subtypes() {
    return z(this.layer);
  }
  get _tableHighlightsReady() {
    return !("table" !== this.selectionSource || !this.highlightEnabled || !this._highlightableLayerView);
  }
  get _viewSelection() {
    var _a2;
    const { _selectableLayer: e17 } = this;
    return e17 ? ((_a2 = this._selectionManager) == null ? void 0 : _a2.getSelection(e17)) ?? [] : [];
  }
  get _viewSelectionReady() {
    return !(!this.layer || "view" !== this.selectionSource || !this._selectionManager);
  }
  get actionColumn() {
    return super.actionColumn;
  }
  get activeFilters() {
    const { filterGeometry: e17, objectIds: t5, filterBySelectionEnabled: s22, highlightIds: l17 } = this, o8 = new O();
    return e17 && o8.push({ type: "geometry", geometry: e17 }), s22 ? o8.push({ type: "selection", objectIds: l17.toArray() }) : t5.length && o8.push({ type: "objectId", objectIds: t5.toArray() }), o8;
  }
  get activeSortOrders() {
    var _a2;
    return ((_a2 = this.grid) == null ? void 0 : _a2.sortOrders) ? this.grid.sortOrders.map(({ path: e17, direction: t5 }) => ({ fieldName: e17, direction: t5 })).filter((e17) => e17.fieldName && e17.direction) : [];
  }
  get allColumns() {
    return super.allColumns;
  }
  get allRelatedTablesVisible() {
    return !!(this.relatedRecordsEnabled && this.relatedTables.length && this.showAllRelatedTables);
  }
  get allVisibleColumns() {
    return super.allVisibleColumns;
  }
  get attachmentsColumns() {
    return this.allColumns.filter((e17) => o4(e17));
  }
  get attachmentsEnabled() {
    return this.store.attachmentsEnabled;
  }
  set attachmentsEnabled(e17) {
    this.store.attachmentsEnabled = e17;
  }
  get clearPrompt() {
    return () => this.prompt = null;
  }
  set clearPrompt(e17) {
    this._overrideIfSome("clearPrompt", e17);
  }
  get definitionExpression() {
    return this.store.where;
  }
  set definitionExpression(e17) {
    this.store.where = e17;
  }
  get effectiveSize() {
    return super.effectiveSize;
  }
  get fieldColumns() {
    return this.allColumns.filter((e17) => t4(e17));
  }
  get filterGeometry() {
    return this.store.filterGeometry;
  }
  set filterGeometry(e17) {
    this.store.filterGeometry = e17;
  }
  get filterBySelectionEnabled() {
    return !!this._get("filterBySelectionEnabled");
  }
  set filterBySelectionEnabled(e17) {
    const { objectIds: t5 } = this, i10 = t5.length;
    e17 ? (i10 && (t5.removeAll(), this._logWarning("Selection filter has been applied with an objectID filter. Object ID filter has been removed.")), this.filterGeometry && this._logWarning("Selection filter has been applied with a geometry filter. Results may appear inconsistent"), this._syncObjectIdsWithStore(this.highlightIds.toArray())) : i10 || this._syncObjectIdsWithStore(null), this._set("filterBySelectionEnabled", e17);
  }
  get groupColumns() {
    return super.groupColumns;
  }
  get hiddenFields() {
    return this._get("hiddenFields") ?? new O();
  }
  set hiddenFields(e17) {
    this._set("hiddenFields", n5(e17, this._get("hiddenFields"), O));
  }
  get highlightIds() {
    return super.highlightIds;
  }
  set highlightIds(e17) {
    super.highlightIds = e17;
  }
  get initialSize() {
    return this.store.initialSize;
  }
  set initialSize(e17) {
    this.store.initialSize = e17;
  }
  get isEditing() {
    return super.isEditing;
  }
  get isQuerying() {
    return super.isQuerying;
  }
  get isSyncing() {
    return super.isSyncing;
  }
  get isQueryingOrSyncing() {
    return this.isQuerying || this.isSyncing || this.relatedTables.some((e17) => !!e17.isQueryingOrSyncing);
  }
  get isSyncingAttachments() {
    return this.store.isSyncingAttachmentEdits || this.relatedTables.some((e17) => !!e17.isSyncingAttachments);
  }
  get layer() {
    return this._get("layer");
  }
  set layer(e17) {
    this._highlights.removeAll(), this.drainRelatedTables(), this._set("layer", e17);
  }
  get layerFieldConfigurations() {
    const e17 = this.layer;
    return u4(e17) ? e17.fieldConfigurations : void 0;
  }
  get layerView() {
    return this.store.layerView;
  }
  get objectIds() {
    return super.objectIds;
  }
  set objectIds(e17) {
    super.objectIds = e17;
  }
  get outFields() {
    return this.store.outFields;
  }
  set outFields(e17) {
    this.store.outFields = e17;
  }
  get pageCount() {
    return super.pageCount;
  }
  get pageIndex() {
    return super.pageIndex;
  }
  set pageIndex(e17) {
    super.pageIndex = e17;
  }
  get pageSize() {
    return super.pageSize;
  }
  set pageSize(e17) {
    super.pageSize = e17;
  }
  get relatedRecordsEnabled() {
    return this.store.relatedRecordsEnabled;
  }
  set relatedRecordsEnabled(e17) {
    this.store.relatedRecordsEnabled = e17;
  }
  get relatedTable() {
    return this.relatedTables.at(-1);
  }
  get relationship() {
    var _a2, _b;
    const e17 = (_a2 = this.relationshipConfig) == null ? void 0 : _a2.relationshipId;
    return null == e17 ? null : (_b = this.relationships) == null ? void 0 : _b.find(({ id: t5 }) => t5 === e17);
  }
  get relationshipColumns() {
    return this.allColumns.filter((e17) => i4(e17));
  }
  get relationshipConfig() {
    return this.store.relationshipConfig;
  }
  set relationshipConfig(e17) {
    this.highlightIds.removeAll(), this.objectIds.removeAll(), this.filterBySelectionEnabled = false, this.store.relationshipConfig = e17;
  }
  get relationshipInfos() {
    const { layer: e17, layers: t5, relationships: i10, tableController: s22 } = this, l17 = this.store.relationshipInfos;
    if (l17.length || !d2(e17) || !(i10 == null ? void 0 : i10.length)) return l17;
    const o8 = [], n19 = (s22 == null ? void 0 : s22.layers) || t5 || [];
    return i10.forEach((t6) => {
      const i11 = y4(e17, n19, t6);
      i11 && (i11.load(), o8.push({ layer: e17, relatedLayer: i11, relationshipId: t6.id }));
    }), o8;
  }
  get relationships() {
    return this.store.relationships;
  }
  get returnGeometryEnabled() {
    return this.store.returnGeometry;
  }
  set returnGeometryEnabled(e17) {
    this.store.returnGeometry = e17;
  }
  get returnMEnabled() {
    return this.store.returnM;
  }
  set returnMEnabled(e17) {
    this.store.returnM = e17;
  }
  get returnZEnabled() {
    return this.store.returnZ;
  }
  set returnZEnabled(e17) {
    this.store.returnZ = e17;
  }
  get rowHighlightIds() {
    return super.rowHighlightIds;
  }
  set rowHighlightIds(e17) {
    super.rowHighlightIds = e17;
  }
  get showAllRelatedTables() {
    return this._get("showAllRelatedTables");
  }
  set showAllRelatedTables(e17) {
    const { relatedTable: t5 } = this;
    this._set("showAllRelatedTables", e17), t5 && (e17 ? this._showAllRelatedTables(t5) : this._hideAllRelatedTables(t5));
  }
  get showPrompt() {
    return (e17) => {
      var _a2, _b;
      (_b = (_a2 = this.prompt) == null ? void 0 : _a2.cancel) == null ? void 0 : _b.call(_a2), this.prompt = e17;
    };
  }
  set showPrompt(e17) {
    this._overrideIfSome("showPrompt", e17);
  }
  get size() {
    return super.size;
  }
  get state() {
    return super.state;
  }
  get supportsAttachments() {
    return this.store.supportsAttachments;
  }
  get supportsAddAttachments() {
    const { store: { supportsAddAttachments: e17, supportsEditing: t5 } } = this;
    return this.editingEnabled && t5 && e17;
  }
  get supportsDeleteAttachments() {
    return this.supportsUpdateAttachments;
  }
  get supportsResizeAttachments() {
    return this.store.supportsResizeAttachments;
  }
  get supportsUpdateAttachments() {
    const { store: { supportsUpdateAttachments: e17, supportsEditing: t5 } } = this;
    return this.editingEnabled && t5 && e17;
  }
  get timeExtent() {
    return this.store.timeExtent;
  }
  set timeExtent(e17) {
    this.store.timeExtent = e17;
  }
  get timeZone() {
    return this._get("timeZone");
  }
  set timeZone(e17) {
    this.allColumns.forEach((t5) => t5.tableTimeZone = e17), this._set("timeZone", e17);
  }
  get view() {
    return this.store.view;
  }
  set view(e17) {
    this.store.view = e17, this.fieldColumns.forEach((t5) => t5.view = e17), this.refreshCellContent();
  }
  get visibleColumns() {
    return super.visibleColumns;
  }
  async addAttachment(e17, t5) {
    const { attachmentsViewOptions: i10, store: s22 } = this, l17 = i10.objectId === e17;
    await s22.addAttachment(e17, t5).catch((e18) => {
      l17 && e18 && "object" == typeof e18 && "error" in e18 && e18.error instanceof s2 && (this._logWarning(`Unable to add attachment. ${e18.error.name} ${e18.error.message}`), i10.error = e18.error);
    }), l17 && !i10.error && (i10.onEditComplete(), i10.attachmentInfos = await s22.getAttachmentsByObjectId(e17, true)), this.refreshCellContent();
  }
  async deleteAttachments(e17, t5) {
    const { attachmentsViewOptions: i10, store: s22 } = this, l17 = i10.objectId === e17;
    await s22.deleteAttachments(e17, t5).catch((e18) => {
      l17 && e18 && "object" == typeof e18 && "error" in e18 && e18.error instanceof s2 && (this._logWarning(`Unable to delete attachment(s). ${e18.error.name} ${e18.error.message}`), i10.error = e18.error);
    }), l17 && !i10.error && (i10.onEditComplete(), i10.attachmentInfos = await s22.getAttachmentsByObjectId(e17, true)), this.refreshCellContent();
  }
  collapseRelatedTable(e17, t5) {
    const { relatedLayer: s22, objectId: l17, relationshipId: o8 } = t5, { objectIdField: n19 } = s22, r12 = F2(s22, o8), a17 = s22.displayField || (r12 == null ? void 0 : r12.keyField) || n19;
    e17.set({ highlightIds: new O([l17]), multipleSelectionEnabled: false, tableTemplateOverride: this._getTableTemplateForRelatedTableView(e17, a17, o8) });
    const h9 = e17.hiddenFields;
    h9.includes(o5.action) || h9.add(o5.action), h9.includes(o5.attachments) || h9.add(o5.attachments), e17.scrollLeft();
  }
  async deleteSelection() {
    const e17 = this.highlightIds.toArray();
    if (!(e17 == null ? void 0 : e17.length)) return;
    const { deleteFeatureResults: t5 } = await this.store.deleteRowsByObjectId(e17), i10 = t5.filter((e18) => !e18.error).map((e18) => e18.objectId);
    i10.length && (this.highlightIds.removeMany(i10), await this.refresh());
  }
  async downloadAttachmentById(e17, t5) {
    const i10 = (await this.store.getAttachmentsByObjectId(e17, true)).find((e18) => e18.id === t5);
    await n17(i10);
  }
  drainRelatedTables() {
    this.relatedTables.drain((e17) => e17.destroy()), this.set({ multipleSelectionEnabled: true, relationshipColumnConfigs: null, showAllRelatedTables: false, tableTemplateOverride: null }), this.hiddenFields.removeMany([o5.action, o5.attachments]);
  }
  drainRelatedTablesAboveIndex(e17) {
    const { relatedTables: t5 } = this, i10 = t5.slice(e17 + 1);
    t5.removeMany(i10).forEach((e18) => e18.destroy());
    const s22 = t5.at(-1);
    s22 && (s22.attachmentsViewOptions.objectId = null, this.allRelatedTablesVisible ? this._showAllRelatedTables(s22) : this._hideAllRelatedTables(s22));
  }
  async exportSelectionToCSV(e17) {
    const { highlightIds: t5, layer: i10, outFields: s22 } = this;
    i10 && t5.length ? await a16({ layer: i10, objectIds: t5.toArray(), outFields: s22, includeGeometry: e17 }) : this._logWarning("Export failed.");
  }
  filterBySelection() {
    this.filterBySelectionEnabled ? this._logWarning("Property 'filterBySelectionEnabled' is already 'true'. This method has no effect.") : (s(i2.getLogger(this), "`FeatureTable.filterBySelection` is deprecated in favor of 'filterBySelectionEnabled' and 'objectIds'", { replacement: "FeatureTable.filterBySelectionEnabled or FeatureTable.objectIds", version: "4.30", warnOnce: true }), this.objectIds.addMany(this.highlightIds.toArray()));
  }
  getObjectIdIndex(e17) {
    return this.store.getItemIndexByObjectId(e17);
  }
  getValue(e17, t5) {
    var _a2;
    const i10 = this.store.getItemByObjectId(e17);
    return (_a2 = i10 == null ? void 0 : i10.feature) == null ? void 0 : _a2.attributes[t5];
  }
  getVirtualPageIndex() {
    var _a2;
    return ((_a2 = this.grid) == null ? void 0 : _a2.getVirtualPageIndex()) ?? 0;
  }
  goToPage(e17) {
    if (null == e17 || !Number.isInteger(e17)) return void this._logWarning("Invalid 'page' parameter provided to 'goToPage()'. Current page will remain the same.");
    const { pageCount: t5 } = this;
    (e17 >= t5 || -1 === e17) && (e17 = t5 - 1), e17 < -1 && (e17 = 0), e17 !== this.pageIndex && (this.pageIndex = e17), this.paginationEnabled || this.scrollToIndex(e17 * this.pageSize);
  }
  hideAttachmentsView() {
    this.attachmentsViewOptions.objectId = null, this.set({ relationshipColumnConfigs: null, multipleSelectionEnabled: true, tableTemplateOverride: null }), this.hiddenFields.remove(o5.action);
  }
  nextPage() {
    const e17 = this.paginationEnabled ? this.pageIndex : this.getVirtualPageIndex();
    this.goToPage(e17 + 1);
  }
  previousPage() {
    const e17 = this.paginationEnabled ? this.pageIndex : this.getVirtualPageIndex();
    this.goToPage(e17 - 1);
  }
  async refresh() {
    await Promise.all([...this.relatedTables.map((e17) => e17.refresh()), y(this._refreshDebounced())]);
  }
  refreshCellContent() {
    var _a2;
    (_a2 = this.grid) == null ? void 0 : _a2.requestContentUpdate();
  }
  refreshPageCache() {
    var _a2;
    (_a2 = this.grid) == null ? void 0 : _a2.refreshPageCache();
  }
  async reset() {
    var _a2;
    this.goToPage(0), await ((_a2 = this.grid) == null ? void 0 : _a2.reset());
  }
  scrollLeft() {
    var _a2;
    (_a2 = this.grid) == null ? void 0 : _a2.scrollLeft(0);
  }
  scrollToBottom() {
    var _a2;
    (_a2 = this.grid) == null ? void 0 : _a2.scrollToBottom();
  }
  scrollToIndex(e17) {
    var _a2;
    (_a2 = this.grid) == null ? void 0 : _a2.scrollToIndex(e17);
  }
  scrollToRow(e17) {
    var _a2;
    const t5 = this.store.getItemIndexByObjectId(e17);
    t5 > -1 ? (_a2 = this.grid) == null ? void 0 : _a2.scrollToIndex(t5) : this._logWarning("Row not found. Associated data may not be loaded yet.");
  }
  scrollToTop() {
    var _a2;
    (_a2 = this.grid) == null ? void 0 : _a2.scrollToTop();
  }
  async saveAttachmentsViewForm() {
    const { attachmentId: e17, form: t5, objectId: i10 } = this.attachmentsViewOptions;
    null != i10 && t5 && (null != e17 ? await this.updateAttachment(i10, e17, t5) : await this.addAttachment(i10, t5));
  }
  async showAttachmentsView({ objectId: e17 }) {
    const { attachmentsViewOptions: t5, hiddenFields: s22, layer: l17 } = this;
    if (!this.attachmentsEnabled || !l17) return void this._logWarning("The 'attachmentsEnabled' property is currently false.");
    await l17.load();
    const o8 = this._getDefaultLayerDisplayField(l17), n19 = await this.store.getAttachmentsByObjectId(e17, true), r12 = this.store.getItemIndexByObjectId(e17);
    t5.set({ attachmentInfos: n19, mode: n19.length ? "list" : "file", objectId: e17 }), this.set({ highlightIds: new O([e17]), multipleSelectionEnabled: false, relationshipColumnConfigs: [], tableTemplateOverride: this._getTableTemplateForAttachmentsView(o8) }), s22.includes(o5.action) || s22.add(o5.action), null != r12 && this.scrollToIndex(r12);
  }
  syncAttributeTableTemplate() {
    this._syncAttributeTableTemplate(true);
  }
  clearSelectionFilter() {
    s(i2.getLogger(this), "`FeatureTable.clearSelectionFilter` is deprecated in favor of 'filterBySelectionEnabled' and 'objectIds'", { replacement: "FeatureTable.filterBySelectionEnabled or FeatureTable.objectIds", version: "4.30", warnOnce: true }), this.objectIds.removeAll();
  }
  async updateAttachment(e17, t5, i10) {
    const { attachmentsViewOptions: s22, store: l17 } = this, n19 = s22.objectId === e17;
    await l17.updateAttachment(e17, t5, i10).catch((e18) => {
      n19 && e18 && "object" == typeof e18 && "error" in e18 && e18.error instanceof s2 && (this._logWarning(`Unable to update attachment. ${e18.error.name} ${e18.error.message}`), s22.error = e18.error);
    }), n19 && !s22.error && (s22.onEditComplete(), s22.attachmentInfos = await l17.getAttachmentsByObjectId(e17, true)), this.refreshCellContent();
  }
  async zoomToSelection() {
    const { layer: e17, view: t5 } = this, i10 = this.highlightIds.toArray();
    if (!e17 || !t5 || !i10.length) return;
    const s22 = e17.createQuery();
    s22.objectIds = i10, s22.returnGeometry = true;
    const l17 = await e17.queryFeatures(s22);
    try {
      await o3(t5, l17.features);
    } catch (o8) {
      "AbortError" !== o8.name && console.error(o8);
    }
  }
  _syncAttributeTableTemplate(e17 = false) {
    const { _effectiveAttributeTableTemplate: t5, layer: i10, tableTemplateOverride: s22, syncTemplateOnChangesEnabled: l17 } = this;
    if (!i10 || !t5 || !l17 && !e17 || s22 || this.tableParent || this.relatedTable || null !== this.attachmentsViewOptions.objectId) return;
    const { activeSortOrders: o8, allColumns: n19, columns: r12 } = this, a17 = [], h9 = N4(r12.toArray());
    o8.forEach(({ fieldName: e18, direction: t6 }) => {
      e18 && t6 && a17.push(new c5({ field: e18, order: t6 }));
    }), n19.forEach(({ fieldName: e18, direction: t6 }) => {
      t6 && !a17.some((t7) => t7.field === e18) && a17.push(new c5({ field: e18, order: t6 }));
    }), t5.elements = h9, t5.orderByFields = a17;
  }
  async _refresh() {
    await this.store.refresh();
    const e17 = this.highlightIds.toArray();
    if (e17.length) {
      (await this.store.verifyFeaturesByObjectId(e17)).forEach((t6, i10) => {
        t6 || this.highlightIds.remove(e17[i10]);
      });
    }
    this.refreshPageCache();
    const t5 = this.attachmentsViewOptions;
    null != (t5 == null ? void 0 : t5.objectId) && (t5.attachmentInfos = await this.store.getAttachmentsByObjectId(t5.objectId, true));
  }
  async _onLayerLoad() {
    var _a2, _b;
    const { layer: e17, pageSize: t5 } = this;
    if (!e17) return;
    e17.parent && d(e17.parent) && await e17.parent.loadAll();
    const i10 = (_a2 = e17.capabilities.query) == null ? void 0 : _a2.maxRecordCount, s22 = i10 && i10 < t5, l17 = s22 ? i10 : t5;
    s22 && (this._logWarning("The value for 'pageSize' has been adjusted due to the provided layer's max record count."), this.pageSize = l17), (_b = this.grid) == null ? void 0 : _b.set("pageSize", l17), this.store.load(), this._generateColumns();
  }
  _onLayerRefresh(e17) {
    this.autoRefreshEnabled && e17.dataChanged && this.refresh();
  }
  _generateColumns() {
    this._drainColumns();
    const { _effectiveTableTemplate: e17, _effectiveAttributeTableTemplate: t5 } = this, i10 = e17 == null ? void 0 : e17.columnTemplates, s22 = this._generateColumnsFromColumnTemplates(i10) ?? this._generateColumnsFromAttributeTableTemplate(t5) ?? this._generateDefaultFieldColumns();
    s22.length && (this.attachmentsEnabled && !s22.some((e18) => o4(e18)) && s22.push(this._generateDefaultAttachmentsColumn()), this.relatedRecordsEnabled && this.relationshipInfos.length && !s22.some((e18) => i4(e18)) && s22.push(...this._generateDefaultRelationshipColumns()), this.actionColumnConfig && s22.push(this._generateActionColumn()), s22.sort((e18, t6) => e18.frozen && t6.frozen || e18.frozenToEnd && t6.frozenToEnd ? 0 : e18.frozen || t6.frozenToEnd ? -1 : 0), this.columns.addMany(s22));
  }
  _generateColumnsFromAttributeTableTemplate(e17) {
    var _a2;
    const { layer: i10 } = this;
    if (!e17 || !i10) return null;
    const { elements: s22, orderByFields: l17 } = e17, o8 = b4(i10), n19 = [];
    n19.push(...this._generateColumnsFromAttributeTableElements(s22, l17));
    const r12 = o8.map((e18) => {
      if (!e18.fieldName || I2(e18.fieldName, n19)) return null;
      const { fieldName: t5 } = e18, s23 = w(i10, t5);
      return s23 ? this._generateDefaultFieldColumn(s23, { hidden: true, orderByFields: l17 }) : (this._logTemplateWarning("A valid 'field' could not be found for the provided template."), null);
    }).filter(N);
    return n19.push(...r12), this.attachmentsEnabled && l12(i10) && !n19.some((e18) => o4(e18)) && n19.push(this._generateDefaultAttachmentsColumn({ hidden: true })), this.relatedRecordsEnabled && d2(i10) && ((_a2 = i10.relationships) == null ? void 0 : _a2.forEach(({ id: e18 }, t5) => {
      const i11 = this.relationshipInfos.find((t6) => t6.relationshipId === e18);
      i11 && !n19.some((t6) => i4(t6) && t6.relationshipId === e18) && n19.push(this._generateDefaultRelationshipColumn({ hidden: true, info: i11, index: t5 }));
    })), n19.length ? n19 : null;
  }
  _generateColumnsFromAttributeTableElements(e17, t5) {
    const { layer: i10 } = this;
    return i10 ? (e17 == null ? void 0 : e17.map((e18, i11) => {
      switch (e18.type) {
        case "field":
          return this._createFieldColumnFromElement({ element: e18, orderByFields: t5 });
        case "group":
          return this._createGroupColumnFromElement({ element: e18, orderByFields: t5 });
        case "attachment":
          return this._createAttachmentColumnFromElement({ element: e18, index: i11 });
        case "relationship":
          return this._createRelationshipColumnFromElement({ element: e18, index: i11 });
      }
    }).filter((e18) => !!e18)) ?? [] : [];
  }
  _createFieldColumnFromElement(e17) {
    var _a2;
    const { layer: t5 } = this;
    if (!t5) return null;
    const { element: i10, orderByFields: s22 } = e17, { fieldName: l17 } = i10;
    if (null == l17) return this._logTemplateWarning("A valid 'fieldName' must be provided."), null;
    const o8 = w(t5, l17);
    if (!o8) return this._logTemplateWarning("A valid 'field' could not be found for the provided template."), null;
    const { description: n19, label: r12 } = i10, a17 = (s22 == null ? void 0 : s22.findIndex(({ field: e18 }) => e18 && e18 === l17)) ?? -1, h9 = a17 > -1 ? (_a2 = s22 == null ? void 0 : s22.at(a17)) == null ? void 0 : _a2.order : void 0, d6 = a17 > -1 ? a17 : void 0, { editingEnabled: m11, grid: c17, messages: p14, messagesCommon: u8, messagesURIUtils: g5, store: b7, timeZone: f9, view: y6 } = this;
    return new G5({ description: n19, direction: h9, field: o8, fieldName: l17, grid: c17, initialSortPriority: d6, label: r12, layer: t5, messages: p14, messagesCommon: u8, messagesURIUtils: g5, store: b7, tableEditingEnabled: m11, tableTimeZone: f9, view: y6, onShowPromptCallback: this._onShowPromptCallback });
  }
  _createGroupColumnFromElement(e17) {
    const { element: t5, orderByFields: i10 } = e17, { description: s22, label: l17 } = t5;
    if (!l17) return this._logTemplateWarning("Group columns require a label."), null;
    if (!t5.elements.length) return this._logTemplateWarning("Group columns require child columns."), null;
    const { grid: o8, messages: n19, messagesCommon: r12, messagesURIUtils: a17, store: h9, timeZone: d6 } = this;
    return new i6({ columns: this._generateColumnsFromAttributeTableElements(t5.elements, i10), description: s22, grid: o8, label: l17, messages: n19, messagesCommon: r12, messagesURIUtils: a17, store: h9, tableTimeZone: d6 });
  }
  _createAttachmentColumnFromElement(e17) {
    if (!this.attachmentsEnabled) return void this._logTemplateWarning("The 'attachmentsEnabled' property is currently false.");
    const { element: t5, index: i10 } = e17, { description: s22, label: l17 } = t5, { grid: o8, layer: n19, messages: r12, messagesCommon: a17, messagesURIUtils: h9, store: d6, timeZone: m11 } = this;
    return new u5({ description: s22, fieldName: `${o5.attachments}-${i10}`, grid: o8, label: l17, layer: n19, messages: r12, messagesCommon: a17, messagesURIUtils: h9, store: d6, tableTimeZone: m11, onShowAttachments: (e18) => this._onShowAttachments(e18) });
  }
  _createRelationshipColumnFromElement(e17) {
    if (!this.relatedRecordsEnabled) return void this._logTemplateWarning("Relationship columns require related records to be enabled on the table.");
    const { element: t5, index: i10 } = e17, { description: s22, label: l17, relationshipId: o8 } = t5, n19 = this.relationshipInfos.find((e18) => e18.relationshipId === o8);
    if (!n19) return this._logTemplateWarning("Unable to find valid related layer target based on the provided relationship id."), null;
    const { grid: r12, messages: a17, messagesCommon: h9, messagesURIUtils: d6, store: m11, timeZone: c17 } = this;
    return new c14({ description: s22, fieldName: `${o5.relationship}-${o8}-${i10}`, grid: r12, label: l17, messages: a17, messagesCommon: h9, messagesURIUtils: d6, relationshipId: o8, store: m11, tableTimeZone: c17, layer: n19.relatedLayer, relatedLayer: n19.layer, showRelatedTableCallback: (e18) => this._onShowRelatedTable(e18) });
  }
  _generateColumnsFromColumnTemplates(e17) {
    const { editingEnabled: t5, grid: i10, layer: s22, messages: l17, messagesCommon: o8, messagesURIUtils: n19, store: r12, timeZone: a17, view: h9 } = this;
    if (!(e17 == null ? void 0 : e17.length) || !s22) return null;
    const d6 = [];
    return e17.forEach((e18, m11) => {
      var _a2;
      const { autoWidth: c17, description: p14, direction: u8, flexGrow: g5, fieldName: b7, formatFunction: f9, frozen: y6, frozenToEnd: T4, icon: w4, iconText: _4, initialSortPriority: C6, invalid: I4, label: v2, labelTooltipText: E3, menuConfig: A2, resizable: F4, sortable: j5, textAlign: S2, textWrap: R2, timeZone: x2, type: U5, width: O3 } = e18;
      if (!U5) return void this._logTemplateWarning("Property 'type' is missing from the provided template.");
      if (y6 && T4) return void this._logTemplateWarning("Properties 'frozen' and 'frozenToEnd' cannot both be true for the same column.");
      const z4 = false === e18.visible;
      if ("group" === U5) {
        if (!((_a2 = e18.columnTemplates) == null ? void 0 : _a2.length)) return void this._logTemplateWarning("Group columns must contain column templates.");
        if (!e18.label) return void this._logTemplateWarning("Group columns require a label.");
        const t6 = this._generateColumnsFromColumnTemplates(e18.columnTemplates), s23 = z4 || this._isFieldHidden(e18.label);
        d6.push(new i6({ autoWidth: c17, columns: t6, description: p14, flexGrow: g5, frozen: y6, frozenToEnd: T4, grid: i10, hidden: s23, icon: w4, iconText: _4, invalid: I4, label: v2, labelTooltipText: E3, menuConfig: A2, messages: l17, messagesCommon: o8, messagesURIUtils: n19, resizable: F4, tableTimeZone: a17, textAlign: S2, textWrap: R2, timeZone: x2, width: O3 }));
      } else if ("attachment" === U5) {
        if (!this.attachmentsEnabled) return void this._logTemplateWarning("Attachment columns require attachments to be enabled on the table.");
        d6.push(this._generateAttachmentsColumnFromTemplate(e18, m11));
      } else if ("relationship" === U5) {
        if (!this.relatedRecordsEnabled) return void this._logTemplateWarning("Relationship columns require related records to be enabled on the table.");
        const { relationshipId: t6 } = e18;
        if (null == t6) return void this._logTemplateWarning("Property 'relationshipId' is missing from the provided template.");
        const i11 = this.relationshipInfos.find((e19) => e19.relationshipId === t6);
        if (!i11) return void this._logTemplateWarning("Unable to find valid related layer target based on the provided relationship id.");
        d6.push(this._generateRelationshipColumnFromTemplate(i11, e18, m11));
      } else if (!b7) return void this._logTemplateWarning("Value for 'fieldName' property was missing from the provided template.");
      const P4 = z4 || this._isFieldHidden(b7);
      if ("column" === U5) d6.push(new f5({ autoWidth: c17, description: p14, direction: u8, fieldName: b7, flexGrow: g5, formatFunction: f9, frozen: y6, frozenToEnd: T4, grid: i10, hidden: P4, icon: w4, iconText: _4, initialSortPriority: C6, invalid: I4, label: v2, labelTooltipText: E3, menuConfig: A2, messages: l17, messagesCommon: o8, messagesURIUtils: n19, resizable: F4, sortable: j5, tableTimeZone: a17, textAlign: S2, textWrap: R2, timeZone: x2, width: O3 }));
      else if ("field" === U5) {
        const m12 = w(s22, e18.fieldName);
        if (!m12) return void this._logTemplateWarning("A valid 'field' could not be found for the provided template.");
        const { editable: b8, required: U6 } = e18;
        d6.push(new G5({ autoWidth: c17, description: p14, direction: u8, editable: b8, field: m12, fieldName: m12.name, flexGrow: g5, formatFunction: f9, frozen: y6, frozenToEnd: T4, grid: i10, hidden: P4, icon: w4, iconText: _4, initialSortPriority: C6, invalid: I4, label: v2, labelTooltipText: E3, layer: s22, messages: l17, messagesCommon: o8, messagesURIUtils: n19, menuConfig: A2, required: U6, resizable: F4, sortable: j5, store: r12, tableEditingEnabled: t5, tableTimeZone: a17, template: e18, textAlign: S2, textWrap: R2, timeZone: x2, width: O3, view: h9, onShowPromptCallback: this._onShowPromptCallback }));
      }
    }), d6.length ? d6 : null;
  }
  _generateDefaultFieldColumns() {
    var _a2, _b;
    return ((_b = (_a2 = this.layer) == null ? void 0 : _a2.fields) == null ? void 0 : _b.map((e17) => this._generateDefaultFieldColumn(e17)).filter(N)) ?? [];
  }
  _generateDefaultFieldColumn(e17, t5) {
    var _a2;
    const { editingEnabled: i10, grid: s22, layer: l17, messages: o8, messagesCommon: n19, messagesURIUtils: r12, store: a17, timeZone: h9, view: d6 } = this, m11 = t5 == null ? void 0 : t5.orderByFields;
    if (!e17.visible || "geometry" === e17.type) return null;
    const c17 = e17.name, p14 = (m11 == null ? void 0 : m11.findIndex(({ field: e18 }) => e18 && e18 === c17)) ?? -1, u8 = p14 > -1 ? (_a2 = m11 == null ? void 0 : m11.at(p14)) == null ? void 0 : _a2.order : void 0, g5 = p14 > -1 ? p14 : void 0;
    return new G5({ direction: u8, field: e17, fieldName: c17, grid: s22, hidden: (t5 == null ? void 0 : t5.hidden) || this._isFieldHidden(c17), initialSortPriority: g5, layer: l17, messages: o8, messagesCommon: n19, messagesURIUtils: r12, store: a17, tableEditingEnabled: i10, tableTimeZone: h9, view: d6, onShowPromptCallback: this._onShowPromptCallback });
  }
  _generateAttachmentsColumnFromTemplate(e17, t5) {
    const { grid: i10, layer: s22, messages: l17, messagesCommon: o8, messagesURIUtils: n19, store: r12, timeZone: a17 } = this, { attachmentsViewEnabled: h9, autoWidth: d6, description: m11, flexGrow: c17, fieldName: p14, formatFunction: u8, frozen: g5, frozenToEnd: b7, icon: f9, iconText: y6, invalid: T4, label: w4, labelTooltipText: _4, menuConfig: C6, resizable: I4, textAlign: v2, textWrap: E3, thumbnailAppearance: A2, thumbnailCount: F4, thumbnailIconScale: j5, thumbnailsEnabled: S2, timeZone: R2, width: x2 } = e17;
    return new u5({ attachmentsViewEnabled: h9, autoWidth: d6, description: m11, fieldName: `${o5.attachments}-${t5}`, flexGrow: c17, formatFunction: u8, frozen: g5, frozenToEnd: b7, grid: i10, hidden: false === e17.visible || this._isFieldHidden(w4) || this._isFieldHidden(p14), icon: f9, iconText: y6, invalid: T4, label: w4, labelTooltipText: _4, layer: s22, menuConfig: C6, messages: l17, messagesCommon: o8, messagesURIUtils: n19, resizable: I4, store: r12, tableTimeZone: a17, textAlign: v2, textWrap: E3, thumbnailAppearance: A2, thumbnailCount: F4, thumbnailIconScale: j5, thumbnailsEnabled: S2, timeZone: R2, width: x2, onShowAttachments: (e18) => this._onShowAttachments(e18) });
  }
  _generateDefaultAttachmentsColumn(e17) {
    const { grid: t5, layer: i10, messages: s22, messagesCommon: l17, messagesURIUtils: o8, store: n19, timeZone: r12 } = this, a17 = o5.attachments;
    return new u5({ fieldName: a17, grid: t5, hidden: (e17 == null ? void 0 : e17.hidden) || this._isFieldHidden(a17), layer: i10, messages: s22, messagesCommon: l17, messagesURIUtils: o8, store: n19, tableTimeZone: r12, onShowAttachments: (e18) => this._onShowAttachments(e18) });
  }
  _generateRelationshipColumnFromTemplate(e17, t5, i10) {
    const { grid: s22, messages: l17, messagesCommon: o8, messagesURIUtils: n19, store: r12, timeZone: a17 } = this, { autoWidth: h9, collapsed: d6, description: m11, flexGrow: c17, fieldName: p14, formatFunction: u8, frozen: g5, frozenToEnd: b7, icon: f9, iconText: y6, invalid: T4, label: w4, labelTooltipText: _4, menuConfig: C6, relationshipId: I4, resizable: v2, textAlign: E3, textWrap: A2, timeZone: F4, width: j5 } = t5;
    return new c14({ autoWidth: h9, collapsed: d6, description: m11, fieldName: `${o5.relationship}-${I4}-${i10}`, flexGrow: c17, formatFunction: u8, frozen: g5, frozenToEnd: b7, grid: s22, hidden: false === t5.visible || this._isFieldHidden(w4) || this._isFieldHidden(p14), icon: f9, iconText: y6, invalid: T4, label: w4, labelTooltipText: _4, menuConfig: C6, messages: l17, messagesCommon: o8, messagesURIUtils: n19, relationshipId: I4, resizable: v2, store: r12, tableTimeZone: a17, textAlign: E3, textWrap: A2, timeZone: F4, width: j5, layer: e17.relatedLayer, relatedLayer: e17.layer, showRelatedTableCallback: (e18) => this._onShowRelatedTable(e18) });
  }
  _generateDefaultRelationshipColumns() {
    const { relationshipInfos: e17, relationshipColumnConfigs: t5 } = this, i10 = [];
    return (e17 == null ? void 0 : e17.length) ? (t5 ? t5.forEach((t6, s22) => {
      const { relationshipId: l17 } = t6, o8 = e17.find((e18) => e18.relationshipId === l17);
      o8 && i10.push(this._generateDefaultRelationshipColumn({ config: t6, index: s22, info: o8 }));
    }) : e17.forEach((e18, t6) => i10.push(this._generateDefaultRelationshipColumn({ index: t6, info: e18 }))), i10) : i10;
  }
  _generateDefaultRelationshipColumn(e17) {
    const { config: t5, hidden: i10, index: s22, info: l17 } = e17, { grid: o8, messages: n19, messagesCommon: r12, messagesURIUtils: a17 } = this, { relationshipId: h9 } = l17;
    return new c14({ ...t5, fieldName: `${o5.relationship}-${h9}-${s22}`, grid: o8, hidden: i10, messages: n19, messagesCommon: r12, messagesURIUtils: a17, layer: l17.relatedLayer, relatedLayer: l17.layer, relationshipId: h9, showRelatedTableCallback: (e18) => this._onShowRelatedTable(e18) });
  }
  _generateActionColumn() {
    return new d3({ hidden: this._isFieldHidden(o5.action), ...this.actionColumnConfig });
  }
  _isFieldHidden(e17) {
    const t5 = e17 == null ? void 0 : e17.toLowerCase();
    return (this.hiddenFields ?? this._defaultHiddenFields).some((e18) => e18.toLowerCase() === t5);
  }
  _addTableHighlight(e17) {
    if (!this.highlightEnabled) return;
    const { _highlightableLayerView: t5, layer: i10 } = this;
    if (t5 && i10) {
      const s22 = this.store.getItemByObjectId(e17), l17 = (s22 == null ? void 0 : s22.feature) ?? e17;
      this._highlights.add(t5.highlight(l17, { name: c9 }), j2(i10, e17));
    }
  }
  _removeTableHighlight(e17) {
    const { layer: t5 } = this;
    t5 && this._highlights.remove(j2(t5, e17));
  }
  async _syncTemporaryHighlight(e17, t5) {
    var _a2, _b, _c;
    if (!this.highlightEnabled) return (_a2 = this._currentTemporaryHighlight) == null ? void 0 : _a2.handle.remove(), void (this._currentTemporaryHighlight = null);
    if (await T(25), s3(t5), !this._currentTemporaryHighlight && !e17) return;
    const { id: i10 } = this._currentTemporaryHighlight ?? {};
    if (i10 === e17) return;
    (_c = (_b = this._currentTemporaryHighlight) == null ? void 0 : _b.handle) == null ? void 0 : _c.remove(), this._currentTemporaryHighlight = null;
    const { _highlightableLayerView: s22, layer: l17, highlightEnabled: o8 } = this;
    e17 && s22 && l17 && o8 && (this._currentTemporaryHighlight = { id: e17, handle: s22.highlight(e17, { name: m5 }) });
  }
  _syncSelection() {
    this._highlights.removeAll(), this._tableHighlightsReady ? this.highlightIds.forEach((e17) => this._addTableHighlight(e17)) : this._viewSelectionReady && (this.highlightIds.items = this._viewSelection);
  }
  _appendToViewSelection(e17) {
    var _a2;
    const { _selectableLayer: t5 } = this;
    t5 && ((_a2 = this._selectionManager) == null ? void 0 : _a2.appendToSelection(t5, e17));
  }
  _removeFromViewSelection(e17) {
    var _a2;
    const { _selectableLayer: t5 } = this;
    t5 && ((_a2 = this._selectionManager) == null ? void 0 : _a2.removeFromSelection(t5, e17));
  }
  async _onHighlightIdsChange(e17) {
    const { added: t5, removed: i10 } = e17, { attachmentsViewOptions: s22 } = this;
    if (this._tableHighlightsReady ? (i10.forEach((e18) => this._removeTableHighlight(e18)), t5.forEach((e18) => this._addTableHighlight(e18))) : this._viewSelectionReady && (this._removeFromViewSelection(i10), this._appendToViewSelection(t5)), this.filterBySelectionEnabled && this._syncObjectIdsWithStore(this.highlightIds.toArray()), null != s22.objectId) {
      const e18 = this.highlightIds.at(0);
      if (null != e18) {
        const t6 = await this.store.getAttachmentsByObjectId(e18, true);
        s22.set({ attachmentInfos: t6, objectId: e18, mode: t6.length ? "list" : "file" });
      }
    }
  }
  _onShowRelatedTable(e17) {
    const { highlightIds: t5 } = this, { objectId: i10 } = e17;
    t5.removeAll(), t5.add(i10), this.showRelatedTableCallback ? this.showRelatedTableCallback(e17) : this.emit("show-related-table", e17);
  }
  _onObjectIdsChange() {
    const e17 = this.objectIds.toArray();
    e17.length && this.filterBySelectionEnabled && (this.filterBySelectionEnabled = false, this._logWarning("Object ID filter was applied while a selection filter was applied. Selection filter has been removed.")), this._syncObjectIdsWithStore(e17);
  }
  _syncObjectIdsWithStore(e17) {
    this.store.objectIds = e17, this.refreshPageCache();
  }
  _drainColumns() {
    this.columns.drain((e17) => !e17.destroyed && e17.destroy());
  }
  _showAllRelatedTables(e17) {
    const t5 = e17.layer;
    if (!t5) return;
    const i10 = this._getDefaultLayerDisplayField(t5);
    e17.set({ relationshipColumnConfigs: [], tableTemplateOverride: this._getTableTemplateForShowAllTablesView(e17, i10) });
    const s22 = e17.hiddenFields;
    s22.includes(o5.action) || s22.add(o5.action), s22.includes(o5.attachments) || s22.add(o5.attachments);
  }
  _hideAllRelatedTables(e17) {
    const { layer: t5, relationship: i10 } = e17;
    if (t5 && null != (i10 == null ? void 0 : i10.id)) if (null != e17.attachmentsViewOptions.objectId) {
      const i11 = this._getDefaultLayerDisplayField(t5);
      e17.set({ multipleSelectionEnabled: false, relationshipColumnConfigs: [], showAllRelatedTables: false, tableTemplateOverride: this._getTableTemplateForAttachmentsView(i11) });
    } else e17.set({ multipleSelectionEnabled: true, relationshipColumnConfigs: g2(t5, i10.id), showAllRelatedTables: false, tableTemplateOverride: null }), e17.hiddenFields.removeMany([o5.action, o5.attachments]);
  }
  _getTableTemplateForRelatedTableView(e17, t5, i10) {
    return new T3({ columnTemplates: [new l16({ fieldName: t5, editable: false, menuConfig: { selectionMode: "single", icon: "chevron-down", items: this._extractFieldColumnInfosFromTableTemplate(e17.viewModel).map(([s22, l17]) => ({ selected: s22 === t5, label: l17 || s22, clickFunction: () => {
      e17.tableTemplateOverride = this._getTableTemplateForRelatedTableView(e17, s22, i10);
    } })) }, resizable: false }), new p11({ autoWidth: false, collapsed: true, flexGrow: 0, label: "", resizable: false, relationshipId: i10, width: pe })] });
  }
  _getTableTemplateForShowAllTablesView(e17, t5) {
    const i10 = [new l16({ fieldName: t5, editable: false, menuConfig: { selectionMode: "single", icon: "chevron-down", items: this._extractFieldColumnInfosFromTableTemplate(e17.viewModel).map(([i11, s22]) => ({ selected: i11 === t5, label: s22 || i11, clickFunction: () => {
      e17.tableTemplateOverride = this._getTableTemplateForShowAllTablesView(e17, i11);
    } })) }, resizable: false })];
    return null != e17.attachmentsViewOptions.objectId && i10.push(new i7({ attachmentsViewEnabled: false, autoWidth: false, flexGrow: 0, label: "", resizable: false, thumbnailsEnabled: false, width: pe })), new T3({ columnTemplates: i10 });
  }
  async _onShowPromptCallback(e17) {
    const { column: t5, objectId: i10, oldValue: s22, value: l17 } = e17, { _subtypes: o8, layer: n19 } = this;
    if (!n19 || !o8.length) return void t5.cancel();
    const r12 = o8.find((e18) => e18.code === l17);
    if (!r12) return void t5.cancel();
    const a17 = $(), h9 = this._createSubtypeEditPrompt(s22, r12, a17), d6 = !!n19.parent;
    try {
      this.showPrompt(h9);
      const e18 = await a17.promise, s23 = [{ fieldName: t5.fieldName, value: l17 }];
      switch (e18) {
        case "update-fields":
          for (const [e19, t6] of Object.entries(r12.defaultValues)) null != t6 && s23.push({ fieldName: e19, value: t6 });
          await t5.updateItems({ objectId: i10, updates: s23 }), d6 && this.refresh();
          break;
        case "keep-existing":
          await t5.updateItems({ objectId: i10, updates: s23 }), d6 && this.refresh();
          break;
        case "undo":
          t5.cancel();
      }
    } finally {
      this.clearPrompt();
    }
  }
  _createSubtypeEditPrompt(e17, t5, i10) {
    var _a2;
    const { _subtypes: s22, messages: l17, messagesCommon: o8 } = this, n19 = ((_a2 = s22.find((t6) => t6.code === e17)) == null ? void 0 : _a2.name) ?? `${e17}`;
    let r12 = "update-fields";
    const a17 = [{ label: l17.subtypes.useDefaultValuesOption, value: "update-fields" }, { label: l17.subtypes.keepCurrentValuesOption, value: "keep-existing" }];
    return { context: "info", title: l17.subtypes.changeWarningTitle, message: s5(l17.subtypes.changeWarning, { originalType: n19, newType: t5.name }), radios: a17, defaultRadioSelection: "update-fields", onRadioSelection: (e18) => {
      r12 = e18;
    }, actions: { primary: { label: o8.apply, action: () => i10.resolve(r12), type: "positive" }, secondary: { label: o8.cancel, type: "neutral", action: () => i10.resolve("undo") } }, cancel: () => i10.reject() };
  }
  _onShowAttachments({ objectId: e17 }) {
    this.attachmentsViewOptions.objectId = e17;
  }
  _getTableTemplateForAttachmentsView(e17) {
    return new T3({ columnTemplates: [new l16({ fieldName: e17, editable: false, menuConfig: { selectionMode: "single", icon: "chevron-down", items: this._extractFieldColumnInfosFromTableTemplate(this).map(([t5, i10]) => ({ selected: t5 === e17, label: i10 || t5, clickFunction: () => {
      this.tableTemplateOverride = this._getTableTemplateForAttachmentsView(t5);
    } })) }, resizable: false }), new i7({ attachmentsViewEnabled: false, autoWidth: false, flexGrow: 0, label: "", resizable: false, thumbnailsEnabled: false, width: pe })] });
  }
  _extractFieldColumnInfosFromTableTemplate(e17) {
    const t5 = e17 ?? this, { layer: i10, tableTemplate: s22 } = t5;
    return s22 ? s22.columnTemplates.filter(({ type: e18 }) => "field" === e18 || "column" === e18).map(({ fieldName: e18, label: t6 }) => {
      if (null != t6 && "" !== t6) return [e18, t6];
      const s23 = i10 == null ? void 0 : i10.fieldsIndex.get(e18);
      return [e18, s23 && e10(s23, i10)];
    }) : (i10 == null ? void 0 : i10.fields.map((e18) => [e18.name, e10(e18, i10)])) ?? [];
  }
  _getDefaultLayerDisplayField(e17) {
    const { displayField: t5, objectIdField: i10 } = e17;
    return t5 && "" !== t5.trim() ? t5 : i10 || (e17.fields.length ? e17.fields[0].name : "");
  }
  _getIndexOfFirstFrozenToEndColumn() {
    const e17 = this.columns.findIndex((e18) => e18.frozenToEnd);
    return e17 > -1 ? e17 : void 0;
  }
  _logWarning(e17, t5) {
    t5 ? i2.getLogger(this).warnOnce(e17) : i2.getLogger(this).warn(e17);
  }
  _logTemplateWarning(e17) {
    this._logWarning(`${e17} Skipped generating a column using the provided template.`, true);
  }
  _onCellInteraction(e17) {
    null != e17 && null != this.attachmentsViewOptions.objectId && this.highlightIds.add(e17);
  }
};
__decorate([m()], ue.prototype, "_defaultHiddenFields", null), __decorate([m()], ue.prototype, "_effectiveTableTemplate", null), __decorate([m()], ue.prototype, "_effectiveAttributeTableTemplate", null), __decorate([m()], ue.prototype, "_highlights", void 0), __decorate([m()], ue.prototype, "_highlightableLayerView", null), __decorate([m()], ue.prototype, "_selectionManager", null), __decorate([m()], ue.prototype, "_selectableLayer", null), __decorate([m()], ue.prototype, "_subtypes", null), __decorate([m()], ue.prototype, "_tableHighlightsReady", null), __decorate([m()], ue.prototype, "_viewSelection", null), __decorate([m()], ue.prototype, "_viewSelectionReady", null), __decorate([m()], ue.prototype, "activeFilters", null), __decorate([m({ readOnly: true })], ue.prototype, "activeSortOrders", null), __decorate([m()], ue.prototype, "allRelatedTablesVisible", null), __decorate([m()], ue.prototype, "attachmentsColumns", null), __decorate([m()], ue.prototype, "attachmentsEnabled", null), __decorate([m()], ue.prototype, "attachmentsViewOptions", void 0), __decorate([m({ type: c6 })], ue.prototype, "attributeTableTemplate", void 0), __decorate([m()], ue.prototype, "autoRefreshEnabled", void 0), __decorate([m()], ue.prototype, "clearPrompt", null), __decorate([m({ readOnly: true })], ue.prototype, "columns", void 0), __decorate([m()], ue.prototype, "dataProvider", void 0), __decorate([m()], ue.prototype, "definitionExpression", null), __decorate([m()], ue.prototype, "fieldColumns", null), __decorate([m()], ue.prototype, "filterGeometry", null), __decorate([m()], ue.prototype, "filterBySelectionEnabled", null), __decorate([m({ readOnly: true })], ue.prototype, "grid", void 0), __decorate([m()], ue.prototype, "hiddenFields", null), __decorate([m()], ue.prototype, "highlightEnabled", void 0), __decorate([m()], ue.prototype, "initialSize", null), __decorate([m()], ue.prototype, "isQueryingOrSyncing", null), __decorate([m()], ue.prototype, "isSyncingAttachments", null), __decorate([m()], ue.prototype, "layer", null), __decorate([m()], ue.prototype, "layers", void 0), __decorate([m()], ue.prototype, "layerFieldConfigurations", null), __decorate([m()], ue.prototype, "layerView", null), __decorate([m()], ue.prototype, "menuConfig", void 0), __decorate([m()], ue.prototype, "messages", void 0), __decorate([m(), e3("esri/t9n/common")], ue.prototype, "messagesCommon", void 0), __decorate([m(), e3("esri/core/t9n/Units")], ue.prototype, "messagesUnits", void 0), __decorate([m(), e3("esri/widgets/support/t9n/uriUtils")], ue.prototype, "messagesURIUtils", void 0), __decorate([m()], ue.prototype, "outFields", null), __decorate([m()], ue.prototype, "prompt", void 0), __decorate([m()], ue.prototype, "relatedRecordsEnabled", null), __decorate([m()], ue.prototype, "relatedTable", null), __decorate([m()], ue.prototype, "relatedTables", void 0), __decorate([m()], ue.prototype, "relationship", null), __decorate([m()], ue.prototype, "relationshipColumnConfigs", void 0), __decorate([m()], ue.prototype, "relationshipColumns", null), __decorate([m()], ue.prototype, "relationshipConfig", null), __decorate([m()], ue.prototype, "relationshipInfos", null), __decorate([m()], ue.prototype, "relationships", null), __decorate([m()], ue.prototype, "returnGeometryEnabled", null), __decorate([m()], ue.prototype, "returnMEnabled", null), __decorate([m()], ue.prototype, "returnZEnabled", null), __decorate([m()], ue.prototype, "selectionSource", void 0), __decorate([m()], ue.prototype, "showAllRelatedTables", null), __decorate([m()], ue.prototype, "showPrompt", null), __decorate([m()], ue.prototype, "showRelatedTableCallback", void 0), __decorate([m({ readOnly: true, type: w3 })], ue.prototype, "store", void 0), __decorate([m()], ue.prototype, "supportsAttachments", null), __decorate([m()], ue.prototype, "supportsAddAttachments", null), __decorate([m()], ue.prototype, "supportsDeleteAttachments", null), __decorate([m()], ue.prototype, "supportsResizeAttachments", null), __decorate([m()], ue.prototype, "supportsUpdateAttachments", null), __decorate([m()], ue.prototype, "syncTemplateOnChangesEnabled", void 0), __decorate([m()], ue.prototype, "tableController", void 0), __decorate([m()], ue.prototype, "tableParent", void 0), __decorate([m({ type: T3 })], ue.prototype, "tableTemplate", void 0), __decorate([m()], ue.prototype, "tableTemplateOverride", void 0), __decorate([m()], ue.prototype, "timeExtent", null), __decorate([m()], ue.prototype, "timeZone", null), __decorate([m()], ue.prototype, "view", null), ue = __decorate([a3("esri.widgets.FeatureTable.FeatureTableViewModel")], ue);
var ge2 = ue;

// node_modules/@arcgis/core/widgets/FeatureTable/TableMenuVisibleElements.js
var r10 = class extends b3 {
  constructor(e17) {
    super(e17), this.clearSelection = true, this.deleteSelection = true, this.exportSelectionToCSV = true, this.refreshData = true, this.selectedRecordsShowAllToggle = true, this.selectedRecordsShowSelectedToggle = true, this.toggleColumns = true, this.zoomToSelection = true;
  }
};
__decorate([m({ type: Boolean, nonNullable: true })], r10.prototype, "clearSelection", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r10.prototype, "deleteSelection", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r10.prototype, "exportSelectionToCSV", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r10.prototype, "refreshData", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r10.prototype, "selectedRecordsShowAllToggle", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r10.prototype, "selectedRecordsShowSelectedToggle", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r10.prototype, "toggleColumns", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r10.prototype, "zoomToSelection", void 0), r10 = __decorate([a3("esri.widgets.FeatureTable.TableMenuVisibleElements")], r10);
var s21 = r10;

// node_modules/@arcgis/core/widgets/FeatureTable/VisibleElements.js
var r11 = class extends b3 {
  constructor(o8) {
    super(o8), this.columnDescriptions = true, this.columnMenus = true, this.columnMenuItems = new c8(), this.close = false, this.header = true, this.layerDropdown = false, this.layerDropdownIcons = true, this.menu = true, this.menuItems = new s21(), this.progress = true, this.selectionColumn = true, this.tooltips = true;
  }
};
__decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "columnDescriptions", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "columnMenus", void 0), __decorate([m({ type: c8, nonNullable: true })], r11.prototype, "columnMenuItems", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "close", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "header", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "layerDropdown", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "layerDropdownIcons", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "menu", void 0), __decorate([m({ type: s21, nonNullable: true })], r11.prototype, "menuItems", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "progress", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "selectionColumn", void 0), __decorate([m({ type: Boolean, nonNullable: true })], r11.prototype, "tooltips", void 0), r11 = __decorate([a3("esri.widgets.FeatureTable.VisibleElements")], r11);
var p13 = r11;

// node_modules/@arcgis/core/widgets/FeatureTable.js
var j4;
var z3 = "esri-feature-table";
var L2 = `${z3}__attachments-view`;
var U4 = { base: z3, content: `${z3}__content`, menuPopover: `${z3}__menu-popover`, layerDropdownMenu: `${z3}__layer-switcher-menu`, tableContainer: `${z3}__table-container`, tableContainerWithAttachments: `${z3}__table-container--attachments`, tableNavigation: `${z3}__table-navigation`, attachmentsView: L2, attachmentsViewContent: `${L2}__content`, attachmentsViewDropArea: `${L2}__drop-area`, attachmentsViewIcon: `${L2}__icon`, attachmentsViewInformation: `${L2}__information`, attachmentsViewList: `${L2}__list`, attachmentsViewListFileSize: `${L2}__list__filesize`, attachmentsViewListItemDelete: `${L2}__list-item--delete`, attachmentsViewListThumbnail: `${L2}__list__thumbnail`, expanded: `${z3}__expanded`, collapsed: `${z3}__collapsed` };
var $3 = { chevronRight: "chevron-right", clearSelection: "clear-selection", downloadTo: "download-to", exclamationMarkTriangle: "exclamation-mark-triangle", exportToCSV: "file-csv", folderOpen: "folder-open", delete: "trash", launch: "launch", layers: "layers", moveUp: "move-up", plus: "plus", refresh: "refresh", replaceImage: "replace-image", save: "save", showAll: "selection-x", showColumn: "show-column", showSelected: "selection-filter", upload: "upload", zoomToSelection: "zoom-to-object" };
var B2 = j4 = class extends B {
  constructor(e17, t5) {
    super(e17, t5), this._attachmentsInput = null, this._columnVisibilityActions = /* @__PURE__ */ new Map(), this.attachmentsList = null, this.description = null, this.disabled = false, this.navigationScale = "m", this.title = null, this.viewModel = new ge2(), this.visibleElements = new p13(), this._showDeletePrompt = this._showDeletePrompt.bind(this), this._onDeleteSelectionClick = this._onDeleteSelectionClick.bind(this);
  }
  initialize() {
    this.addHandles([l(() => [this.isQueryingOrSyncing, this.editingEnabled], () => this.scheduleRender()), l(() => [this._effectiveVisibleElements, this._effectiveVisibleElements.columnDescriptions, this._effectiveVisibleElements.columnMenus, this._effectiveVisibleElements.columnMenuItems, this.columns.length], () => {
      this.columns.forEach((e17) => e17.visibleElements = this._effectiveVisibleElements), this.refreshCellContent();
    }, U), l(() => this._effectiveVisibleElements.selectionColumn, (e17) => {
      this.grid && (this.grid.selectionColumnEnabled = e17);
    }, h2), a2(() => this.viewModel, "show-related-table", (e17) => this._onShowRelatedTable(e17)), l(() => this.editingEnabled, () => {
      var _a2;
      const { attachmentsViewOptions: e17 } = this;
      this.clearPrompt(), e17 && ((_a2 = e17.form) == null ? void 0 : _a2.reset(), e17.candidates = null);
    })]);
  }
  loadDependencies() {
    return Promise.all([c2({ action: () => import("./calcite-action-N754OQKO.js"), alert: () => import("./calcite-alert-S57QEGFJ.js"), button: () => import("./calcite-button-LQNLDGVJ.js"), chip: () => import("./calcite-chip-GZAR2ZGC.js"), dropdown: () => import("./calcite-dropdown-X3NUDWH3.js"), "dropdown-group": () => import("./calcite-dropdown-group-MUIQVPXG.js"), "dropdown-item": () => import("./calcite-dropdown-item-KWOBFAHU.js"), icon: () => import("./calcite-icon-J7CTACBF.js"), label: () => import("./calcite-label-VMVGTYRU.js"), list: () => import("./calcite-list-5P7FHET7.js"), "list-item": () => import("./calcite-list-item-WMPDN2QM.js"), pagination: () => import("./calcite-pagination-W46ARJNO.js"), panel: () => import("./calcite-panel-FR3BLUC3.js"), popover: () => import("./calcite-popover-PKJWPRL4.js"), progress: () => import("./calcite-progress-VKQC7QX7.js"), switch: () => import("./calcite-switch-4O42Z6ZD.js"), tooltip: () => import("./calcite-tooltip-K5LQSGST.js") }), s11(), s12()]);
  }
  destroy() {
    this.drainRelatedTables(), this.clearPrompt();
  }
  get _effectiveDescription() {
    const { description: e17 } = this;
    return null != e17 ? t.sanitize("function" == typeof e17 ? e17() : e17) : void 0;
  }
  get _effectiveLayers() {
    const { layer: e17, layers: t5 } = this, i10 = (t5 == null ? void 0 : t5.length) ? t5.filter(a8) : [], l17 = i10.length ? i10 : [...this._viewLayers];
    return e17 && !l17.includes(e17) && l17.push(e17), l17;
  }
  get _effectiveTitle() {
    const { layer: e17, messages: t5, state: i10, highlightIds: l17, title: o8, size: n19 } = this;
    if (o8) return t.sanitize("function" == typeof o8 ? o8() : o8);
    if (!e17) return t5.noLayer;
    switch (i10) {
      case "disabled":
        return t5.errorLayer;
      case "ready":
      case "loading":
        return t5.loading;
      case "error":
        return t5.errorData;
    }
    return s5(t5.header, { title: t.sanitize(e17.title), count: n19, selected: l17.length });
  }
  get _effectiveVisibleElements() {
    return this.visibleElementsOverride ?? this.visibleElements;
  }
  get _filteredTables() {
    const e17 = this.relatedTables.toArray(), t5 = 1 === e17.length;
    return this.allRelatedTablesVisible ? e17 : t5 ? [e17[0]] : this._shouldShowAttachmentsView ? e17.slice(-1) : e17.slice(-2);
  }
  get _hasAttachmentsViewError() {
    return null != this.attachmentsViewOptions.error;
  }
  get _hasCustomMenuItems() {
    var _a2, _b;
    return !!((_b = (_a2 = this.menuConfig) == null ? void 0 : _a2.items) == null ? void 0 : _b.length);
  }
  get _hasDefaultMenuItems() {
    return !!(this._showClearSelectionAction || this._showDeleteSelectionAction || this._showExportSelectionToCSVAction || this._showRefreshDataAction || this._showZoomToSelectionAction || this._showSelectedRecordsShowSelectedAction || this._showSelectedRecordsShowAllAction || this._showColumnsVisibilityAction);
  }
  get _shouldShowGrid() {
    return this.allRelatedTablesVisible || 1 === this.relatedTables.length && !this._shouldShowAttachmentsView;
  }
  get _shouldShowMenu() {
    const { header: e17, menu: t5 } = this._effectiveVisibleElements;
    return !(!e17 || !t5 || !this._hasDefaultMenuItems && !this._hasCustomMenuItems);
  }
  get _shouldShowAttachmentsView() {
    return !(null == this.attachmentsViewOptions.objectId && !this.relatedTables.some((e17) => null != e17.attachmentsViewOptions.objectId));
  }
  get _showClearSelectionAction() {
    var _a2;
    return !(!this.highlightIds.length || !((_a2 = this._effectiveVisibleElements.menuItems) == null ? void 0 : _a2.clearSelection) || this.relatedTables.length || this._shouldShowAttachmentsView);
  }
  get _showColumnsVisibilityAction() {
    const { header: e17, menu: t5, menuItems: i10 } = this._effectiveVisibleElements;
    return !(!(e17 && t5 && (i10 == null ? void 0 : i10.toggleColumns)) || this.showRelatedTableCallback || this.allRelatedTablesVisible || this._shouldShowAttachmentsView);
  }
  get _showDeleteSelectionAction() {
    var _a2, _b, _c, _d;
    return !(!(this.editingEnabled && this.highlightIds.length && ((_a2 = this._effectiveVisibleElements.menuItems) == null ? void 0 : _a2.deleteSelection) && ((_d = (_c = (_b = this.layer) == null ? void 0 : _b.capabilities) == null ? void 0 : _c.operations) == null ? void 0 : _d.supportsDelete)) || this.relatedTables.length || this._shouldShowAttachmentsView);
  }
  get _showExportSelectionToCSVAction() {
    return !(!(this._effectiveVisibleElements.menuItems.exportSelectionToCSV && this.layer && this.highlightIds.length) || this.relatedTables.length);
  }
  get _showLayerDropdown() {
    return !(!this._effectiveLayers.length || !this._effectiveVisibleElements.layerDropdown);
  }
  get _shouldShowNavigationBar() {
    const e17 = !!this.relatedTable || this._shouldShowAttachmentsView;
    return !this.tableParent && e17;
  }
  get _showRefreshDataAction() {
    var _a2;
    return !!((_a2 = this._effectiveVisibleElements.menuItems) == null ? void 0 : _a2.refreshData);
  }
  get _showSelectedRecordsShowSelectedAction() {
    var _a2;
    const e17 = this.objectIds.length, t5 = this.highlightIds.length;
    return !(!t5 || !((_a2 = this._effectiveVisibleElements.menuItems) == null ? void 0 : _a2.selectedRecordsShowSelectedToggle) || e17 && t5 === e17 || this.relatedTables.length || this._shouldShowAttachmentsView);
  }
  get _showSelectedRecordsShowAllAction() {
    var _a2;
    return !(!((_a2 = this._effectiveVisibleElements.menuItems) == null ? void 0 : _a2.selectedRecordsShowAllToggle) || !this.objectIds.length || this.relatedTables.length);
  }
  get _showZoomToSelectionAction() {
    var _a2, _b;
    return !(!this.view || !this.highlightIds.length || ((_a2 = this.effectiveTable.layer) == null ? void 0 : _a2.isTable) || !((_b = this._effectiveVisibleElements.menuItems) == null ? void 0 : _b.zoomToSelection));
  }
  get _viewLayers() {
    var _a2;
    const e17 = (_a2 = this.view) == null ? void 0 : _a2.map;
    if (!e17) return [];
    const t5 = /* @__PURE__ */ new Set(), i10 = (e18) => {
      var _a3, _b;
      !a8(e18) || d(e18) || t5.has(e18) || "sublayer" === e18.type && !((_b = (_a3 = e18.capabilities) == null ? void 0 : _a3.operations) == null ? void 0 : _b.supportsQuery) || (e18.load(), t5.add(e18));
    }, l17 = (e18) => {
      var _a3, _b, _c, _d, _e;
      o2(e18) || "catalog-footprint" === e18.type || (l5(e18) ? (e18.load(), (_a3 = e18.layers) == null ? void 0 : _a3.forEach(i10), (_b = e18.tables) == null ? void 0 : _b.forEach(i10)) : c3(e18) ? (e18.loadAll(), (_c = e18.allSublayers) == null ? void 0 : _c.forEach(i10), (_d = e18.subtables) == null ? void 0 : _d.forEach(i10)) : d(e18) ? (e18.loadAll(), (_e = e18.sublayers) == null ? void 0 : _e.forEach(i10)) : i10(e18));
    };
    return e17.allLayers.forEach(l17), e17.allTables.forEach(l17), [...t5.values()];
  }
  get grid() {
    return this.viewModel.grid;
  }
  get actionColumn() {
    return this.viewModel.actionColumn;
  }
  get actionColumnConfig() {
    return this.viewModel.actionColumnConfig;
  }
  set actionColumnConfig(e17) {
    this.viewModel.actionColumnConfig = e17;
  }
  get activeFilters() {
    return this.viewModel.activeFilters;
  }
  get activeSortOrders() {
    return this.viewModel.activeSortOrders;
  }
  get allColumns() {
    return this.viewModel.allColumns;
  }
  get allRelatedTablesVisible() {
    return this.viewModel.allRelatedTablesVisible;
  }
  get allVisibleColumns() {
    return this.viewModel.allVisibleColumns;
  }
  get attachmentsColumns() {
    return this.viewModel.attachmentsColumns;
  }
  get attachmentsEnabled() {
    return this.viewModel.attachmentsEnabled;
  }
  set attachmentsEnabled(e17) {
    this.viewModel.attachmentsEnabled = e17;
  }
  get attachmentsViewOptions() {
    return this.viewModel.attachmentsViewOptions;
  }
  set attachmentsViewOptions(e17) {
    this.viewModel.attachmentsViewOptions = e17;
  }
  get attributeTableTemplate() {
    return this.viewModel.attributeTableTemplate;
  }
  set attributeTableTemplate(e17) {
    this.viewModel.attributeTableTemplate = e17;
  }
  get autoRefreshEnabled() {
    return this.viewModel.autoRefreshEnabled;
  }
  set autoRefreshEnabled(e17) {
    this.viewModel.autoRefreshEnabled = e17;
  }
  get clearPrompt() {
    return this.viewModel.clearPrompt;
  }
  set clearPrompt(e17) {
    this.viewModel.clearPrompt = e17;
  }
  get columnPerformanceModeEnabled() {
    return this.viewModel.columnPerformanceModeEnabled;
  }
  set columnPerformanceModeEnabled(e17) {
    this.viewModel.columnPerformanceModeEnabled = e17;
  }
  get columnReorderingEnabled() {
    return this.viewModel.columnReorderingEnabled;
  }
  set columnReorderingEnabled(e17) {
    this.viewModel.columnReorderingEnabled = e17;
  }
  get columns() {
    return this.viewModel.columns;
  }
  get definitionExpression() {
    return this.viewModel.definitionExpression;
  }
  set definitionExpression(e17) {
    this.viewModel.definitionExpression = e17;
  }
  set container(e17) {
    super.container = e17;
  }
  get editingEnabled() {
    return this.viewModel.editingEnabled;
  }
  set editingEnabled(e17) {
    this.viewModel.editingEnabled = e17;
  }
  get effectiveSize() {
    return this.viewModel.effectiveSize;
  }
  get effectiveTable() {
    return this.relatedTable || this;
  }
  get fieldColumns() {
    return this.viewModel.fieldColumns;
  }
  get filterGeometry() {
    return this.viewModel.filterGeometry;
  }
  set filterGeometry(e17) {
    this.viewModel.filterGeometry = e17;
  }
  get filterBySelectionEnabled() {
    return this.viewModel.filterBySelectionEnabled;
  }
  set filterBySelectionEnabled(e17) {
    this.viewModel.filterBySelectionEnabled = e17;
  }
  get groupColumns() {
    return this.viewModel.groupColumns;
  }
  get hiddenFields() {
    return this.viewModel.hiddenFields;
  }
  set hiddenFields(e17) {
    this.viewModel.hiddenFields = e17;
  }
  get highlightEnabled() {
    return this.viewModel.highlightEnabled;
  }
  set highlightEnabled(e17) {
    this.viewModel.highlightEnabled = e17;
  }
  get highlightIds() {
    return this.viewModel.highlightIds;
  }
  set highlightIds(e17) {
    this.viewModel.highlightIds = e17;
  }
  get icon() {
    return "table";
  }
  set icon(e17) {
    this._overrideIfSome("icon", e17);
  }
  get initialSize() {
    return this.viewModel.initialSize;
  }
  set initialSize(e17) {
    this.viewModel.initialSize = e17;
  }
  get isQueryingOrSyncing() {
    return this.viewModel.isQueryingOrSyncing;
  }
  get isSyncingAttachments() {
    return this.viewModel.isSyncingAttachments;
  }
  get label() {
    var _a2;
    return ((_a2 = this.messages) == null ? void 0 : _a2.widgetLabel) ?? "";
  }
  set label(e17) {
    this._overrideIfSome("label", e17);
  }
  get layer() {
    return this.viewModel.layer;
  }
  set layer(e17) {
    this.viewModel.layer = e17;
  }
  get layers() {
    return this.viewModel.layers;
  }
  set layers(e17) {
    this.viewModel.layers = e17;
  }
  get layerView() {
    return this.viewModel.layerView;
  }
  get maxSize() {
    return this.viewModel.maxSize;
  }
  set maxSize(e17) {
    this.viewModel.maxSize = e17;
  }
  get messages() {
    return this.viewModel.messages;
  }
  set messages(e17) {
    this.viewModel.messages = e17;
  }
  get messagesCommon() {
    return this.viewModel.messagesCommon;
  }
  set messagesCommon(e17) {
    this.viewModel.messagesCommon = e17;
  }
  get messagesUnits() {
    return this.viewModel.messagesUnits;
  }
  set messagesUnits(e17) {
    this.viewModel.messagesUnits = e17;
  }
  get messagesURIUtils() {
    return this.viewModel.messagesURIUtils;
  }
  set messagesURIUtils(e17) {
    this.viewModel.messagesURIUtils = e17;
  }
  get menuConfig() {
    return this.viewModel.menuConfig;
  }
  set menuConfig(e17) {
    this.viewModel.menuConfig = e17;
  }
  get multipleSelectionEnabled() {
    return this.viewModel.multipleSelectionEnabled;
  }
  set multipleSelectionEnabled(e17) {
    this.viewModel.multipleSelectionEnabled = e17;
  }
  get multiSortEnabled() {
    return this.viewModel.multiSortEnabled;
  }
  set multiSortEnabled(e17) {
    this.viewModel.multiSortEnabled = e17;
  }
  get multipleSortPriority() {
    return this.viewModel.multipleSortPriority;
  }
  set multipleSortPriority(e17) {
    this.viewModel.multipleSortPriority = e17;
  }
  get noDataMessage() {
    return this.viewModel.noDataMessage;
  }
  set noDataMessage(e17) {
    this.viewModel.noDataMessage = e17;
  }
  get objectIds() {
    return this.viewModel.objectIds;
  }
  set objectIds(e17) {
    this.viewModel.objectIds = e17;
  }
  get outFields() {
    return this.viewModel.outFields;
  }
  set outFields(e17) {
    this.viewModel.outFields = e17;
  }
  get pageCount() {
    return this.viewModel.pageCount;
  }
  get pageIndex() {
    return this.viewModel.pageIndex;
  }
  set pageIndex(e17) {
    this.viewModel.pageIndex = e17;
  }
  get pageSize() {
    return this.viewModel.pageSize;
  }
  set pageSize(e17) {
    this.viewModel.pageSize = e17;
  }
  get paginationEnabled() {
    return this.viewModel.paginationEnabled;
  }
  set paginationEnabled(e17) {
    this.viewModel.paginationEnabled = e17;
  }
  get prompt() {
    return this.viewModel.prompt;
  }
  set prompt(e17) {
    this.viewModel.prompt = e17;
  }
  get relatedRecordsEnabled() {
    return this.viewModel.relatedRecordsEnabled;
  }
  set relatedRecordsEnabled(e17) {
    this.viewModel.relatedRecordsEnabled = e17;
  }
  get relatedTable() {
    return this.viewModel.relatedTable;
  }
  get relatedTables() {
    return this.viewModel.relatedTables;
  }
  set relatedTables(e17) {
    this.viewModel.relatedTables = e17;
  }
  get relationship() {
    return this.viewModel.relationship;
  }
  get relationshipColumnConfigs() {
    return this.viewModel.relationshipColumnConfigs;
  }
  set relationshipColumnConfigs(e17) {
    this.viewModel.relationshipColumnConfigs = e17;
  }
  get relationshipColumns() {
    return this.viewModel.relationshipColumns;
  }
  get relationshipConfig() {
    return this.viewModel.relationshipConfig;
  }
  set relationshipConfig(e17) {
    this.viewModel.relationshipConfig = e17;
  }
  get relationshipInfos() {
    return this.viewModel.relationshipInfos;
  }
  get returnGeometryEnabled() {
    return this.viewModel.returnGeometryEnabled;
  }
  set returnGeometryEnabled(e17) {
    this.viewModel.returnGeometryEnabled = e17;
  }
  get returnMEnabled() {
    return this.viewModel.returnMEnabled;
  }
  set returnMEnabled(e17) {
    this.viewModel.returnMEnabled = e17;
  }
  get returnZEnabled() {
    return this.viewModel.returnZEnabled;
  }
  set returnZEnabled(e17) {
    this.viewModel.returnZEnabled = e17;
  }
  get rowHighlightIds() {
    return this.viewModel.rowHighlightIds;
  }
  set rowHighlightIds(e17) {
    this.viewModel.rowHighlightIds = e17;
  }
  get selectionSource() {
    return this.viewModel.selectionSource;
  }
  set selectionSource(e17) {
    this.viewModel.selectionSource = e17;
  }
  get showAllRelatedTables() {
    return this.viewModel.showAllRelatedTables;
  }
  set showAllRelatedTables(e17) {
    this.viewModel.showAllRelatedTables = e17;
  }
  get showPrompt() {
    return this.viewModel.showPrompt;
  }
  set showPrompt(e17) {
    this.viewModel.showPrompt = e17;
  }
  get showRelatedTableCallback() {
    return this.viewModel.showRelatedTableCallback;
  }
  set showRelatedTableCallback(e17) {
    this.viewModel.showRelatedTableCallback = e17;
  }
  get size() {
    return this.viewModel.size;
  }
  get state() {
    return this.viewModel.state;
  }
  get supportsAddAttachments() {
    return this.viewModel.supportsAddAttachments;
  }
  get supportsAttachments() {
    return this.viewModel.supportsAttachments;
  }
  get supportsDeleteAttachments() {
    return this.viewModel.supportsDeleteAttachments;
  }
  get supportsResizeAttachments() {
    return this.viewModel.supportsResizeAttachments;
  }
  get supportsUpdateAttachments() {
    return this.viewModel.supportsUpdateAttachments;
  }
  get syncTemplateOnChangesEnabled() {
    return this.viewModel.syncTemplateOnChangesEnabled;
  }
  set syncTemplateOnChangesEnabled(e17) {
    this.viewModel.syncTemplateOnChangesEnabled = e17;
  }
  get tableController() {
    return this.viewModel.tableController;
  }
  set tableController(e17) {
    this.viewModel.tableController = e17;
  }
  get tableParent() {
    return this.viewModel.tableParent;
  }
  set tableParent(e17) {
    this.viewModel.tableParent = e17;
  }
  get tableTemplate() {
    return this.viewModel.tableTemplate;
  }
  set tableTemplate(e17) {
    this.viewModel.tableTemplate = e17;
  }
  get tableTemplateOverride() {
    return this.viewModel.tableTemplateOverride;
  }
  set tableTemplateOverride(e17) {
    this.viewModel.tableTemplateOverride = e17;
  }
  get timeExtent() {
    return this.viewModel.timeExtent;
  }
  set timeExtent(e17) {
    this.viewModel.timeExtent = e17;
  }
  get timeZone() {
    return this.viewModel.timeZone;
  }
  set timeZone(e17) {
    this.viewModel.timeZone = e17;
  }
  get view() {
    return this.viewModel.view;
  }
  set view(e17) {
    this.viewModel.view = e17;
  }
  get visibleColumns() {
    return this.viewModel.visibleColumns;
  }
  get visibleElementsOverride() {
    const { attachmentsViewOptions: e17, relatedTable: t5, tableController: i10, visibleElements: l17 } = this, { columnDescriptions: o8, columnMenus: n19, tooltips: s22 } = l17, a17 = null != e17.objectId;
    if (t5) return new p13({ columnDescriptions: o8, columnMenus: n19, tooltips: s22, close: l17.close, header: l17.header, layerDropdown: l17.layerDropdown, layerDropdownIcons: l17.layerDropdownIcons, menu: l17.menu, menuItems: l17.menuItems, progress: l17.progress, selectionColumn: false, columnMenuItems: new c8({ sortAscending: false, sortDescending: false }) });
    if (i10) {
      const e18 = i10.relatedTable === this, t6 = i10.allRelatedTablesVisible, r12 = e18 && l17.selectionColumn && !t6 && !a17, c17 = e18 && !t6 && !a17;
      return new p13({ columnDescriptions: o8, columnMenus: n19, tooltips: s22, selectionColumn: r12, close: false, header: false, layerDropdown: false, menu: false, progress: false, columnMenuItems: new c8({ sortAscending: c17, sortDescending: c17 }) });
    }
    return a17 ? new p13({ columnDescriptions: o8, columnMenus: n19, tooltips: s22, close: l17.close, header: l17.header, layerDropdown: l17.layerDropdown, layerDropdownIcons: l17.layerDropdownIcons, menu: l17.menu, menuItems: l17.menuItems, progress: l17.progress, selectionColumn: false, columnMenuItems: new c8({ sortAscending: false, sortDescending: false }) }) : null;
  }
  clearSelectionFilter() {
    this.viewModel.clearSelectionFilter();
  }
  async deleteAttachments(e17, t5, i10) {
    null != e17 && (t5 == null ? void 0 : t5.length) && (i10 ? this._showDeleteAttachmentPrompt(e17, t5) : await this.viewModel.deleteAttachments(e17, t5));
  }
  deleteSelection(e17) {
    return this.highlightIds.length ? e17 ? (this._showDeletePrompt(), this.scheduleRender(), Promise.resolve()) : this.viewModel.deleteSelection() : Promise.resolve();
  }
  async downloadAttachmentById(e17, t5) {
    return this.viewModel.downloadAttachmentById(e17, t5);
  }
  drainRelatedTables() {
    this.viewModel.drainRelatedTables();
  }
  exportSelectionToCSV(e17) {
    return this.viewModel.exportSelectionToCSV(e17);
  }
  filterBySelection() {
    this.viewModel.filterBySelection();
  }
  findColumn(e17) {
    return this.viewModel.findColumn(e17);
  }
  goToPage(e17) {
    this.viewModel.goToPage(e17);
  }
  hideColumn(e17) {
    var _a2;
    (_a2 = this.grid) == null ? void 0 : _a2.hideColumn(e17);
  }
  nextPage() {
    this.viewModel.nextPage();
  }
  previousPage() {
    this.viewModel.previousPage();
  }
  async refresh() {
    await this.viewModel.refresh();
  }
  refreshCellContent() {
    this.viewModel.refreshCellContent();
  }
  async saveAttachmentsViewForm() {
    await this.viewModel.saveAttachmentsViewForm();
  }
  showAllColumns() {
    this.viewModel.showAllColumns();
  }
  showColumn(e17) {
    var _a2;
    (_a2 = this.grid) == null ? void 0 : _a2.showColumn(e17);
  }
  sortColumn(e17, t5) {
    this.viewModel.sortColumn(e17, t5);
  }
  scrollLeft() {
    this.viewModel.scrollLeft();
  }
  scrollToBottom() {
    this.viewModel.scrollToBottom();
  }
  scrollToIndex(e17) {
    this.viewModel.scrollToIndex(e17);
  }
  scrollToRow(e17) {
    this.viewModel.scrollToRow(e17);
  }
  scrollToTop() {
    this.viewModel.scrollToTop();
  }
  toggleColumnVisibility(e17) {
    this.viewModel.toggleColumnVisibility(e17);
  }
  zoomToSelection() {
    this.effectiveTable.viewModel.zoomToSelection();
  }
  render() {
    var _a2;
    const { _effectiveVisibleElements: e17, isQueryingOrSyncing: t5, menuConfig: i10, state: l17 } = this;
    return n3("div", { bind: this, class: this.classes(U4.base, e4.widget) }, n3("calcite-panel", { bind: this, class: U4.content, closable: e17.close, description: this._effectiveDescription, disabled: this.disabled || "disabled" === l17 && !((_a2 = this._effectiveLayers) == null ? void 0 : _a2.length), heading: e17.header ? this._effectiveTitle : void 0, key: "table-content", loading: "loading" === l17, menuOpen: !!(i10 == null ? void 0 : i10.open), overlayPositioning: "fixed", onCalcitePanelClose: (e18) => {
      e18.currentTarget.closed = false, this.emit("close");
    } }, e17.progress ? n3("calcite-progress", { type: t5 ? "indeterminate" : "determinate" }) : null, this._showLayerDropdown ? this._renderLayerDropdown() : null, this._renderColumnVisibilityAction(), this._renderHeaderMenuActions(), this._renderNavigationBar(), this._renderTables(), this._renderError()), this._renderColumnVisibilityPopover(), this._renderPrompt());
  }
  _renderTables() {
    const { grid: e17, relatedTables: t5 } = this, i10 = this.classes(U4.tableContainer);
    if (t5.length) {
      const l17 = this.classes(U4.base, this.allRelatedTablesVisible ? U4.expanded : U4.collapsed, this._shouldShowAttachmentsView ? U4.tableContainerWithAttachments : void 0);
      return n3("div", { class: i10 }, n3("calcite-panel", { overlayPositioning: "fixed" }, n3("div", null, [this._shouldShowGrid ? n3("div", { class: l17 }, e17 == null ? void 0 : e17.render(), this._renderPagination()) : void 0, ...this._filteredTables.map((e18) => {
        const i11 = t5.indexOf(e18);
        return n3("div", { class: l17, key: `related-${i11}` }, e18.render());
      }), ,])));
    }
    return null != this.attachmentsViewOptions.objectId ? n3("div", { class: i10 }, n3("calcite-panel", { overlayPositioning: "fixed" }, n3("div", null, [n3("div", { class: this.classes(U4.base, U4.collapsed) }, e17 == null ? void 0 : e17.render(), this._renderPagination()), this._renderAttachmentsView()]))) : [n3("div", { class: i10 }, e17 == null ? void 0 : e17.render()), this._renderPagination()];
  }
  _renderPagination() {
    if (this.paginationEnabled) return n3("calcite-pagination", { pageSize: 1, startItem: this.pageIndex + 1, totalItems: this.pageCount, onCalcitePaginationChange: (e17) => this.goToPage(e17.currentTarget.startItem - 1) });
  }
  _renderError() {
    const { messages: e17 } = this;
    return n3("calcite-alert", { autoClose: true, autoCloseDuration: "fast", icon: $3.exclamationMarkTriangle, kind: "danger", label: e17.errorOccured, open: this._hasAttachmentsViewError, slot: "alerts", onCalciteAlertClose: () => {
      this.attachmentsViewOptions.error = null;
    } }, n3("span", { slot: "message" }, e17.errorOccured));
  }
  _renderPrompt() {
    return this.prompt ? n3(r6, { ...this.prompt, headingLevel: 2 }) : void 0;
  }
  _showDeletePrompt() {
    const { messages: e17, messagesCommon: t5 } = this, i10 = s5(e17.deleteSelectionCount, { count: this.highlightIds.length });
    this.showPrompt({ title: i10, message: e17.deleteRecordsRemoved, context: "danger", actions: { primary: { label: t5.delete, action: this._onDeleteSelectionClick }, secondary: { label: e17.keepRecords, action: () => this.clearPrompt() } } });
  }
  _showDeleteAttachmentPrompt(e17, t5) {
    const { messages: i10, messagesCommon: l17 } = this, o8 = s5(i10.deleteAttachmentCount, { count: t5.length });
    this.showPrompt({ title: o8, message: i10.deleteAttachmentConfirmation, context: "danger", actions: { primary: { label: l17.delete, action: async () => {
      try {
        await this.deleteAttachments(e17, t5, false);
      } finally {
        this.clearPrompt();
      }
    } }, secondary: { label: l17.cancel, action: () => this.clearPrompt() } } });
  }
  async _onDeleteSelectionClick() {
    try {
      await this.viewModel.deleteSelection();
    } finally {
      this.clearPrompt();
    }
  }
  _renderHeaderMenuAction(e17) {
    var _a2;
    const { disabled: t5, hidden: i10, icon: l17, text: o8, clickFunction: n19 } = e17;
    if (!i10) return n3("calcite-action", { disabled: t5 ?? ((_a2 = this.menuConfig) == null ? void 0 : _a2.disabled), icon: l17 ?? void 0, key: o8, onclick: () => n19(), slot: "header-menu-actions", text: o8, textEnabled: true, title: o8 });
  }
  _renderHeaderMenuActions() {
    return this._shouldShowMenu ? [this._renderDefaultHeaderMenuActions(), this._renderCustomHeaderMenuActions()] : [];
  }
  _renderDefaultHeaderMenuActions() {
    if (!this._hasDefaultMenuItems) return [];
    const { messages: e17 } = this;
    return [this._showRefreshDataAction && { icon: $3.refresh, text: e17.refreshData, clickFunction: () => this.refresh() }, this._showDeleteSelectionAction && { icon: $3.delete, text: e17.deleteSelection, clickFunction: () => this.deleteSelection(true) }, this._showClearSelectionAction && { icon: $3.clearSelection, text: e17.clearSelection, clickFunction: () => {
      var _a2;
      return (_a2 = this.highlightIds) == null ? void 0 : _a2.removeAll();
    } }, this._showZoomToSelectionAction && { icon: $3.zoomToSelection, text: e17.zoomToSelection, clickFunction: () => this.zoomToSelection() }, this._showSelectedRecordsShowSelectedAction && { icon: $3.showSelected, text: e17.showSelected, clickFunction: () => {
      const { objectIds: e18 } = this;
      e18.removeAll(), e18.addMany(this.highlightIds.toArray());
    } }, this._showSelectedRecordsShowAllAction && { icon: $3.showAll, text: e17.showAllRecords, clickFunction: () => this.objectIds.removeAll() }, this._showExportSelectionToCSVAction && { icon: $3.exportToCSV, text: e17.exportSelectionCSV, clickFunction: () => this.exportSelectionToCSV() }].filter(N).map((e18) => e18 && this._renderHeaderMenuAction(e18));
  }
  _renderCustomHeaderMenuActions() {
    var _a2, _b;
    return ((_b = (_a2 = this.menuConfig) == null ? void 0 : _a2.items) == null ? void 0 : _b.map(({ disabled: e17, hidden: t5, icon: i10, label: l17, clickFunction: o8 }) => this._renderHeaderMenuAction({ hidden: "function" == typeof t5 ? t5() : t5, icon: i10, text: l17, disabled: e17, clickFunction: (e18) => o8(e18) }))) ?? [];
  }
  _renderColumnVisibilityAction() {
    var _a2;
    if (!this._showColumnsVisibilityAction) return [];
    const { _columnVisibilityActions: e17, effectiveTable: t5, id: i10 } = this, l17 = this.messages.toggleColumns;
    return n3("calcite-action", { afterCreate: (t6) => e17.set(i10, t6), afterRemoved: () => e17.delete(i10), appearance: "transparent", bind: this, disabled: (_a2 = t5.menuConfig) == null ? void 0 : _a2.disabled, icon: $3.showColumn, id: `${i10}__toggle-columns-action`, slot: "header-actions-end", text: l17 }, this._effectiveVisibleElements.tooltips ? n3("calcite-tooltip", { closeOnClick: true, overlayPositioning: "fixed", placement: "bottom-end", slot: "tooltip" }, l17) : null);
  }
  _renderColumnVisibilityPopover() {
    var _a2;
    if (!this._showColumnsVisibilityAction || ((_a2 = this.effectiveTable.menuConfig) == null ? void 0 : _a2.disabled)) return;
    const { effectiveTable: e17 } = this, t5 = this.messages.toggleColumns;
    return n3("calcite-popover", { autoClose: true, class: U4.menuPopover, label: t5, overlayPositioning: "fixed", placement: "top-end", pointerDisabled: true, referenceElement: this._columnVisibilityActions.get(this.id) }, n3("calcite-list", { displayMode: "nested", filterEnabled: true, filterPlaceholder: t5, label: t5, selectionMode: "multiple" }, e17.columns.toArray().map((t6) => {
      var _a3;
      const i10 = e14(t6) ? (_a3 = t6.columns) == null ? void 0 : _a3.map((t7) => this._renderColumnListItem({ column: t7, table: e17, ignoreSelect: true })) : void 0;
      return this._renderColumnListItem({ column: t6, table: e17, items: i10 });
    })));
  }
  _renderColumnListItem(e17) {
    const { column: t5, table: i10, items: l17, ignoreSelect: o8 } = e17, { effectiveLabel: n19, fieldName: s22, hidden: a17 } = t5;
    return n3("calcite-list-item", { expanded: !(!(l17 == null ? void 0 : l17.length) || a17) || void 0, key: `toggle-columns__item-${s22}`, label: n19, selected: !a17, value: s22, onCalciteListItemSelect: (e18) => {
      o8 && e18.stopPropagation(), i10.toggleColumnVisibility(e18.currentTarget.value);
    } }, l17);
  }
  _renderLayerDropdown() {
    const e17 = this.messages.selectALayer;
    return n3("div", { class: U4.layerDropdownMenu, key: `${this.id}-layerDropdown`, slot: "header-actions-start" }, n3("calcite-dropdown", { bind: this, maxItems: 5, overlayPositioning: "fixed", placement: "bottom-start" }, n3("calcite-action", { icon: $3.layers, slot: "trigger", text: e17, title: e17 }), n3("calcite-dropdown-group", { selectionMode: "single" }, this._effectiveLayers.map((e18, t5) => this._renderLayerDropdownItem(e18, t5)) ?? null)));
  }
  _renderLayerDropdownItem(e17, t5) {
    const i10 = this.visibleElements.layerDropdownIcons;
    return n3("calcite-dropdown-item", { iconStart: i10 ? r5(e17) : void 0, key: `dropdown-item-${t5}`, label: e17.title ?? "", selected: e17 === this.layer, value: e17.id, onCalciteDropdownItemSelect: () => {
      this.layer = e17;
    } }, e17.title);
  }
  _onRelatedNavigationItemClick(e17) {
    this.viewModel.drainRelatedTablesAboveIndex(e17);
  }
  _collapseRelatedTable(e17) {
    this.viewModel.collapseRelatedTable(this.effectiveTable, e17);
  }
  _onShowRelatedTable(e17) {
    this.showRelatedTableCallback || (this._collapseRelatedTable(e17), this._createRelatedTable(e17));
  }
  async _createRelatedTable(e17) {
    const { editingEnabled: t5, effectiveTable: i10, paginationEnabled: l17, pageSize: o8, view: n19, viewModel: s22 } = this, { layer: a17, objectId: r12, relatedLayer: c17, relationshipId: d6 } = e17;
    await a17.load();
    const h9 = this.attachmentsEnabled && l12(a17), m11 = new j4({ layer: a17, attachmentsEnabled: h9, editingEnabled: t5, paginationEnabled: l17, pageSize: o8, relatedRecordsEnabled: true, relationshipConfig: { objectId: r12, relatedLayer: c17, relationshipId: d6 }, relationshipColumnConfigs: g2(a17, d6), tableController: this, tableParent: i10, view: n19, showRelatedTableCallback: (e18) => s22.emit("show-related-table", e18) });
    this.relatedTables.add(m11), this.scheduleRender();
  }
  _renderAttachmentsView() {
    const { attachmentInfos: e17, mode: t5, objectId: i10 } = this.attachmentsViewOptions;
    return "file" !== t5 && e17.length && null != i10 ? this._renderAttachmentsViewList() : this._renderAttachmentsViewFilePane();
  }
  _renderAttachmentsViewFilePane() {
    const { attachmentsViewOptions: e17, isSyncingAttachments: t5, supportsAddAttachments: i10, messages: l17, messagesUnits: o8 } = this, { attachmentId: n19, attachmentInfos: s22, candidates: r12, objectId: c17 } = e17, d6 = s22.find((e18) => e18.id === n19), h9 = s22.length, m11 = r12 == null ? void 0 : r12.length, p14 = d6 ? n3("calcite-label", { alignment: "center", key: "replace-file-node", scale: "l" }, l17.replaceFile) : void 0, u8 = m11 ? void 0 : n3("calcite-label", { alignment: "center", key: "message-node", scale: "m" }, i10 ? null == c17 ? l17.noFeature : l17.dragAndDropToUpload : l17.editingRestricted), g5 = Array.from(r12 ?? []).pop(), w4 = g5 ? n3("calcite-label", { key: "filename-node" }, g5.name) : void 0, b7 = i10 && null != c17, v2 = b7 && m11, y6 = n3("input", { afterCreate: y2, afterRemoved: h4, afterUpdate: y2, bind: this, "data-node-ref": "_attachmentsInput", files: r12, hidden: true, name: "attachment", onchange: (e18) => this._onAttachmentsViewCandidateChange(e18), type: "file" }), f9 = b7 ? n3("calcite-button", { appearance: "outline-fill", bind: this, disabled: t5, iconStart: $3.folderOpen, key: "select-node", loading: false, onclick: () => {
      this._attachmentsInput && (this._attachmentsInput.value = "", this._attachmentsInput.click());
    }, width: "full" }, l17.selectFile) : void 0, _4 = v2 ? n3("calcite-button", { appearance: "outline-fill", bind: this, disabled: t5, iconStart: $3.save, key: "save-node", loading: t5, onclick: () => {
      this.saveAttachmentsViewForm();
    }, width: "full" }, d6 ? l17.updateAttachment : l17.addAttachment) : void 0, M4 = h9 || m11 ? n3("calcite-button", { appearance: "transparent", bind: this, disabled: t5, key: "cancel-node", onclick: () => {
      var _a2;
      this._attachmentsInput && (this._attachmentsInput.value = ""), e17.reset(), ((_a2 = e17.attachmentInfos) == null ? void 0 : _a2.length) && (e17.mode = "list");
    }, width: "full" }, this.messagesCommon.cancel) : void 0;
    return [n3("div", { class: U4.attachmentsView, key: "attachments-view" }, n3("div", { class: U4.attachmentsViewContent, key: "attachments-view-content" }, n3("div", { bind: this, class: U4.attachmentsViewDropArea, ondragover: (e18) => e18.preventDefault(), ondrop: this._onAttachmentsViewDrop }, n3("calcite-icon", { class: U4.attachmentsViewIcon, icon: $3.upload, scale: "l" }), p14, u8, n3("form", { afterCreate: (t6) => e17.form = t6, afterRemoved: () => e17.form = null, bind: this, key: "attachments-form" }, n3("fieldset", null, w4, y6, f9, _4, M4)))), null != c17 && null != d6 ? n3("div", { class: U4.attachmentsViewInformation, key: "information" }, this._renderAttachmentsViewThumbnail(d6, 200), n3("label", { key: "file-label" }, l17.fileName), n3("span", { key: "file-span" }, d6.name), n3("label", { key: "size-label" }, l17.size), n3("span", { key: "size-span" }, G3(o8, d6.size ?? 0))) : void 0)];
  }
  _renderAttachmentsViewList() {
    const { attachmentsViewOptions: e17, isSyncingAttachments: t5, messages: i10, messagesUnits: l17, supportsDeleteAttachments: o8, supportsUpdateAttachments: n19 } = this, { attachmentInfos: s22, objectId: r12 } = e17;
    return n3("calcite-list", { afterCreate: y2, afterRemoved: h4, afterUpdate: y2, bind: this, class: U4.attachmentsViewList, "data-node-ref": "attachmentsList", key: `attachments-list-${r12}`, label: i10 == null ? void 0 : i10.attachments, loading: t5, ondragover: (e18) => e18.preventDefault(), ondrop: this._onAttachmentsViewDrop, selectionMode: "multiple", onCalciteListChange: () => this.scheduleRender() }, ...s22 == null ? void 0 : s22.map((t6) => {
      const { id: s23, name: c17, size: d6, url: h9 } = t6, m11 = G3(l17, d6 ?? 0);
      return n3("calcite-list-item", { afterRemoved: () => this.scheduleRender(), key: `attachment-${s23}`, label: c17, value: s23 }, this._renderAttachmentsViewListThumbnail(t6), n3("span", { class: U4.attachmentsViewListFileSize, slot: "actions-end", title: m11 }, m11), n3("calcite-action", { appearance: "transparent", icon: $3.downloadTo, key: `download-attachment-${s23}`, label: `${c17}`, onclick: (e18) => {
        e18.preventDefault(), this.downloadAttachmentById(r12 ?? -1, s23);
      }, slot: "actions-end", text: i10.downloadAttachment, title: i10.downloadAttachment }), n19 && null != r12 ? n3("calcite-action", { appearance: "transparent", disabled: !n19, icon: $3.replaceImage, key: `replace-attachment-${s23}`, label: `${c17}`, onclick: () => {
        e17.mode = "file", e17.attachmentId = s23;
      }, slot: "actions-end", text: i10.updateAttachment, title: i10.updateAttachment }) : void 0, n3("calcite-action", { appearance: "transparent", icon: $3.launch, key: `launch-attachment-${s23}`, label: `${c17}`, onclick: () => {
        h9 && window.open(h9, "_blank");
      }, slot: "actions-end", text: i10.viewAttachment, title: i10.viewAttachment }), o8 && null != r12 ? n3("calcite-action", { appearance: "transparent", class: U4.attachmentsViewListItemDelete, disabled: !o8, icon: $3.delete, key: `delete-attachment-${s23}`, label: `${c17}`, onclick: () => {
        this.deleteAttachments(r12, [s23], true);
      }, slot: "actions-end", text: i10.deleteAttachment, title: i10.deleteAttachment }) : void 0);
    }));
  }
  _renderAttachmentsViewListThumbnail(e17) {
    return n3("div", { class: U4.attachmentsViewListThumbnail, slot: "content-start" }, n3("a", { href: e17.url ?? "", rel: "noreferrer", target: "_blank" }, this._renderAttachmentsViewThumbnail(e17)));
  }
  _renderAttachmentsViewThumbnail(e17, t5 = 64) {
    const { contentType: i10, name: l17, size: o8, url: n19 } = e17, s22 = `${n19}${(n19 == null ? void 0 : n19.includes("?")) ? "&" : "?"}w=${t5}&s=${o8}`;
    return this.supportsResizeAttachments && e8(i10) ? n3("img", { alt: l17, key: `thumbnail-image-${l17}`, src: s22, title: l17 }) : n3("calcite-icon", { icon: a7(i10), key: `thumbnail-icon-${l17}`, scale: "l", textLabel: l17, title: l17 });
  }
  _onAttachmentsViewCandidateChange({ target: e17 }) {
    this.attachmentsViewOptions.candidates = e17 == null ? void 0 : e17.files;
  }
  _onAttachmentsViewDrop(e17) {
    var _a2;
    e17.preventDefault(), (this.supportsAddAttachments || this.supportsUpdateAttachments) && this.attachmentsViewOptions.set({ mode: "file", candidates: (_a2 = e17.dataTransfer) == null ? void 0 : _a2.files });
  }
  _renderNavigationBar() {
    var _a2, _b;
    const { effectiveTable: e17, messages: t5, messagesCommon: i10, navigationScale: l17, relatedTable: o8, relatedTables: n19 } = this;
    if (!this._shouldShowNavigationBar) return;
    const { attachmentsList: s22, attachmentsViewOptions: a17, layer: r12, supportsAddAttachments: c17, supportsDeleteAttachments: d6 } = e17;
    if (!r12) return;
    const { attachmentInfos: h9, objectId: m11 } = a17, p14 = null != m11, u8 = h9.length, g5 = ((_a2 = s22 == null ? void 0 : s22.selectedItems) == null ? void 0 : _a2.length) ?? 0, w4 = `${r12.objectIdField}: ${m11}`, b7 = s5(t5.selectedCount, { count: g5 }), v2 = s5(t5.attachmentsCount, { count: u8 }), y6 = p14 ? n3("calcite-action", { bind: this, icon: $3.chevronRight, iconFlipRtl: true, key: "navigation-feature", onclick: () => e17.scrollToRow(m11), scale: l17, text: w4, textEnabled: true, title: w4 }) : void 0, f9 = p14 ? n3("calcite-action", { icon: $3.chevronRight, iconFlipRtl: true, key: "navigation-attachments-label", label: v2, onclick: () => {
      u8 > 0 && (a17.mode = "list");
    }, scale: l17, text: v2, textEnabled: true, title: v2 }) : void 0, _4 = p14 ? n3("calcite-chip", { closable: true, closed: 0 === g5, key: "navigation-chip", kind: "inverse", label: b7, scale: l17, selected: true, title: b7, value: "selected", onCalciteChipClose: () => {
      var _a3;
      return (_a3 = s22 == null ? void 0 : s22.selectedItems) == null ? void 0 : _a3.forEach((e18) => e18.selected = false);
    } }, b7) : void 0, M4 = o8 ? n3("calcite-label", { layout: "inline", scale: l17 }, t5.showAllTables, n3("calcite-switch", { checked: this.allRelatedTablesVisible, scale: l17, onCalciteSwitchChange: (e18) => this.showAllRelatedTables = !!e18.currentTarget.checked })) : void 0, C6 = p14 && "list" === a17.mode && null != m11 && g5 && d6 ? n3("calcite-action", { icon: $3.delete, iconFlipRtl: true, key: "attachments-trash-all", onclick: () => {
      var _a3;
      e17.deleteAttachments(m11, ((_a3 = s22 == null ? void 0 : s22.selectedItems) == null ? void 0 : _a3.map((e18) => e18.value)) ?? [], true);
    }, scale: l17, text: i10.delete, textEnabled: true, title: s5(t5.deleteAttachmentCount, { count: g5 }) }) : void 0, A2 = p14 && "list" === a17.mode && c17 ? n3("calcite-action", { bind: this, icon: $3.plus, iconFlipRtl: true, key: "attachments-add", onclick: () => {
      a17.attachmentId = null, a17.mode = "file";
    }, scale: l17, text: t5.addAttachment, textEnabled: true, title: t5.addAttachment }) : void 0;
    return n3("div", { class: U4.tableNavigation, key: "table-nav" }, n3("calcite-action", { icon: $3.moveUp, iconFlipRtl: true, key: "go-back", onclick: () => {
      o8 && this.drainRelatedTables(), e17.attachmentsViewOptions.objectId = null;
    }, scale: l17, text: ((_b = this.layer) == null ? void 0 : _b.title) ?? "", textEnabled: false, title: o8 ? t5.exitRelatedRecords : t5.exitAttachments }), n19.toArray().map((e18, t6) => this._renderRelatedTableNavigationAction(e18, t6)), y6, f9, n3("div", null, _4, M4, C6, A2));
  }
  _renderRelatedTableNavigationAction(e17, t5) {
    var _a2;
    const i10 = this._getLabelForRelatedTableNavigationAction(e17);
    return n3("calcite-action", { icon: $3.chevronRight, iconFlipRtl: true, key: t5, onclick: () => this._onRelatedNavigationItemClick(t5), scale: this.navigationScale, text: i10, textEnabled: true, title: ((_a2 = e17.layer) == null ? void 0 : _a2.title) || "" });
  }
  _getLabelForRelatedTableNavigationAction(e17) {
    var _a2;
    const t5 = (_a2 = e17.layer) == null ? void 0 : _a2.title;
    if (!t5) return "";
    const { relatedTables: i10 } = this;
    if (i10.length <= 1) return t5;
    return i10.indexOf(e17) !== i10.length - 1 && t5.length > 20 ? `${t5.slice(0, 20)}...` : t5;
  }
};
__decorate([m()], B2.prototype, "_attachmentsInput", void 0), __decorate([m()], B2.prototype, "_columnVisibilityActions", void 0), __decorate([m()], B2.prototype, "_effectiveDescription", null), __decorate([m()], B2.prototype, "_effectiveLayers", null), __decorate([m()], B2.prototype, "_effectiveTitle", null), __decorate([m()], B2.prototype, "_effectiveVisibleElements", null), __decorate([m()], B2.prototype, "_filteredTables", null), __decorate([m()], B2.prototype, "_hasAttachmentsViewError", null), __decorate([m()], B2.prototype, "_hasCustomMenuItems", null), __decorate([m()], B2.prototype, "_hasDefaultMenuItems", null), __decorate([m()], B2.prototype, "_shouldShowGrid", null), __decorate([m()], B2.prototype, "_shouldShowMenu", null), __decorate([m()], B2.prototype, "_shouldShowAttachmentsView", null), __decorate([m()], B2.prototype, "_showClearSelectionAction", null), __decorate([m()], B2.prototype, "_showColumnsVisibilityAction", null), __decorate([m()], B2.prototype, "_showDeleteSelectionAction", null), __decorate([m()], B2.prototype, "_showExportSelectionToCSVAction", null), __decorate([m()], B2.prototype, "_showLayerDropdown", null), __decorate([m()], B2.prototype, "_shouldShowNavigationBar", null), __decorate([m()], B2.prototype, "_showRefreshDataAction", null), __decorate([m()], B2.prototype, "_showSelectedRecordsShowSelectedAction", null), __decorate([m()], B2.prototype, "_showSelectedRecordsShowAllAction", null), __decorate([m()], B2.prototype, "_showZoomToSelectionAction", null), __decorate([m()], B2.prototype, "_viewLayers", null), __decorate([m()], B2.prototype, "attachmentsList", void 0), __decorate([m({ readOnly: true })], B2.prototype, "grid", null), __decorate([m()], B2.prototype, "actionColumn", null), __decorate([m()], B2.prototype, "actionColumnConfig", null), __decorate([m({ readOnly: true })], B2.prototype, "activeFilters", null), __decorate([m({ readOnly: true })], B2.prototype, "activeSortOrders", null), __decorate([m()], B2.prototype, "allColumns", null), __decorate([m({ readOnly: true })], B2.prototype, "allRelatedTablesVisible", null), __decorate([m()], B2.prototype, "allVisibleColumns", null), __decorate([m()], B2.prototype, "attachmentsColumns", null), __decorate([m()], B2.prototype, "attachmentsEnabled", null), __decorate([m()], B2.prototype, "attachmentsViewOptions", null), __decorate([m({ type: c6 })], B2.prototype, "attributeTableTemplate", null), __decorate([m()], B2.prototype, "autoRefreshEnabled", null), __decorate([m()], B2.prototype, "clearPrompt", null), __decorate([m()], B2.prototype, "columnPerformanceModeEnabled", null), __decorate([m()], B2.prototype, "columnReorderingEnabled", null), __decorate([m({ readOnly: true })], B2.prototype, "columns", null), __decorate([m()], B2.prototype, "definitionExpression", null), __decorate([m()], B2.prototype, "description", void 0), __decorate([m()], B2.prototype, "disabled", void 0), __decorate([m()], B2.prototype, "editingEnabled", null), __decorate([m()], B2.prototype, "effectiveSize", null), __decorate([m()], B2.prototype, "effectiveTable", null), __decorate([m()], B2.prototype, "fieldColumns", null), __decorate([m()], B2.prototype, "filterGeometry", null), __decorate([m()], B2.prototype, "filterBySelectionEnabled", null), __decorate([m()], B2.prototype, "groupColumns", null), __decorate([m()], B2.prototype, "hiddenFields", null), __decorate([m()], B2.prototype, "highlightEnabled", null), __decorate([m()], B2.prototype, "highlightIds", null), __decorate([m()], B2.prototype, "icon", null), __decorate([m()], B2.prototype, "initialSize", null), __decorate([m()], B2.prototype, "isQueryingOrSyncing", null), __decorate([m()], B2.prototype, "isSyncingAttachments", null), __decorate([m()], B2.prototype, "label", null), __decorate([m()], B2.prototype, "layer", null), __decorate([m()], B2.prototype, "layers", null), __decorate([m()], B2.prototype, "layerView", null), __decorate([m()], B2.prototype, "maxSize", null), __decorate([m(), e3("esri/widgets/FeatureTable/t9n/FeatureTable")], B2.prototype, "messages", null), __decorate([m(), e3("esri/t9n/common")], B2.prototype, "messagesCommon", null), __decorate([m(), e3("esri/core/t9n/Units")], B2.prototype, "messagesUnits", null), __decorate([m(), e3("esri/widgets/support/t9n/uriUtils")], B2.prototype, "messagesURIUtils", null), __decorate([m()], B2.prototype, "menuConfig", null), __decorate([m()], B2.prototype, "multipleSelectionEnabled", null), __decorate([m()], B2.prototype, "multiSortEnabled", null), __decorate([m()], B2.prototype, "multipleSortPriority", null), __decorate([m()], B2.prototype, "navigationScale", void 0), __decorate([m()], B2.prototype, "noDataMessage", null), __decorate([m()], B2.prototype, "objectIds", null), __decorate([m()], B2.prototype, "outFields", null), __decorate([m()], B2.prototype, "pageCount", null), __decorate([m()], B2.prototype, "pageIndex", null), __decorate([m()], B2.prototype, "pageSize", null), __decorate([m()], B2.prototype, "paginationEnabled", null), __decorate([m()], B2.prototype, "prompt", null), __decorate([m()], B2.prototype, "relatedRecordsEnabled", null), __decorate([m()], B2.prototype, "relatedTable", null), __decorate([m()], B2.prototype, "relatedTables", null), __decorate([m()], B2.prototype, "relationship", null), __decorate([m()], B2.prototype, "relationshipColumnConfigs", null), __decorate([m()], B2.prototype, "relationshipColumns", null), __decorate([m()], B2.prototype, "relationshipConfig", null), __decorate([m()], B2.prototype, "relationshipInfos", null), __decorate([m()], B2.prototype, "returnGeometryEnabled", null), __decorate([m()], B2.prototype, "returnMEnabled", null), __decorate([m()], B2.prototype, "returnZEnabled", null), __decorate([m()], B2.prototype, "rowHighlightIds", null), __decorate([m()], B2.prototype, "selectionSource", null), __decorate([m()], B2.prototype, "showAllRelatedTables", null), __decorate([m()], B2.prototype, "showPrompt", null), __decorate([m()], B2.prototype, "showRelatedTableCallback", null), __decorate([m()], B2.prototype, "size", null), __decorate([m({ readOnly: true })], B2.prototype, "state", null), __decorate([m()], B2.prototype, "supportsAddAttachments", null), __decorate([m()], B2.prototype, "supportsAttachments", null), __decorate([m()], B2.prototype, "supportsDeleteAttachments", null), __decorate([m()], B2.prototype, "supportsResizeAttachments", null), __decorate([m()], B2.prototype, "supportsUpdateAttachments", null), __decorate([m()], B2.prototype, "syncTemplateOnChangesEnabled", null), __decorate([m({ constructOnly: true })], B2.prototype, "tableController", null), __decorate([m({ constructOnly: true })], B2.prototype, "tableParent", null), __decorate([m()], B2.prototype, "tableTemplate", null), __decorate([m()], B2.prototype, "tableTemplateOverride", null), __decorate([m()], B2.prototype, "title", void 0), __decorate([m()], B2.prototype, "timeExtent", null), __decorate([m()], B2.prototype, "timeZone", null), __decorate([m()], B2.prototype, "view", null), __decorate([m({ type: ge2 }), e5(["cell-click", "cell-dblclick", "cell-pointerover", "cell-pointerout", "cell-keydown", "column-reorder", "show-related-table"])], B2.prototype, "viewModel", void 0), __decorate([m()], B2.prototype, "visibleColumns", null), __decorate([m({ type: p13, nonNullable: true })], B2.prototype, "visibleElements", void 0), __decorate([m({ type: p13 })], B2.prototype, "visibleElementsOverride", null), B2 = j4 = __decorate([a3("esri.widgets.FeatureTable")], B2);
var N5 = B2;
export {
  N5 as default
};
/*! Bundled license information:

@vaadin/component-base/src/define.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-lumo-styles/version.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-themable-mixin/src/css-utils.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-themable-mixin/vaadin-theme-property-mixin.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-themable-mixin/vaadin-themable-mixin.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-themable-mixin/register-styles.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-lumo-styles/color.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-lumo-styles/font-icons.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-lumo-styles/sizing.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-lumo-styles/spacing.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-lumo-styles/style.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-lumo-styles/typography.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@polymer/polymer/lib/utils/boot.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/utils/resolve-url.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/utils/settings.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/utils/mixin.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/elements/dom-module.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/utils/style-gather.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/utils/wrap.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/utils/path.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/utils/case-map.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/utils/async.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/mixins/properties-changed.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/mixins/property-accessors.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/mixins/template-stamp.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/mixins/property-effects.js:
  (**
   * @fileoverview
   * @suppress {checkPrototypalTypes}
   * @license Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
   * This code may only be used under the BSD style license found at
   * http://polymer.github.io/LICENSE.txt The complete set of authors may be found
   * at http://polymer.github.io/AUTHORS.txt The complete set of contributors may
   * be found at http://polymer.github.io/CONTRIBUTORS.txt Code distributed by
   * Google as part of the polymer project is also subject to an additional IP
   * rights grant found at http://polymer.github.io/PATENTS.txt
   *)

@polymer/polymer/lib/utils/telemetry.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/mixins/properties-mixin.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/lib/mixins/element-mixin.js:
  (**
   * @fileoverview
   * @suppress {checkPrototypalTypes}
   * @license Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
   * This code may only be used under the BSD style license found at
   * http://polymer.github.io/LICENSE.txt The complete set of authors may be found
   * at http://polymer.github.io/AUTHORS.txt The complete set of contributors may
   * be found at http://polymer.github.io/CONTRIBUTORS.txt Code distributed by
   * Google as part of the polymer project is also subject to an additional IP
   * rights grant found at http://polymer.github.io/PATENTS.txt
   *)

@polymer/polymer/lib/utils/html-tag.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@polymer/polymer/polymer-element.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@vaadin/component-base/src/async.js:
  (**
   * @license
   * Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
   * This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
   * The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
   * The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
   * Code distributed by Google as part of the polymer project is also
   * subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
   *)

@vaadin/component-base/src/debounce.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@vaadin/component-base/src/dir-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/path-utils.js:
  (**
   * @license
   * Copyright (c) 2023 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/templates.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/dom-utils.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-helpers.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-column-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-column.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/controller-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/vaadin-usage-statistics/vaadin-usage-statistics-collect.js:
  (*! vaadin-dev-mode:start
    (function () {
  'use strict';
  
  var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) {
    return typeof obj;
  } : function (obj) {
    return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
  };
  
  var classCallCheck = function (instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  };
  
  var createClass = function () {
    function defineProperties(target, props) {
      for (var i = 0; i < props.length; i++) {
        var descriptor = props[i];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
  
    return function (Constructor, protoProps, staticProps) {
      if (protoProps) defineProperties(Constructor.prototype, protoProps);
      if (staticProps) defineProperties(Constructor, staticProps);
      return Constructor;
    };
  }();
  
  var getPolymerVersion = function getPolymerVersion() {
    return window.Polymer && window.Polymer.version;
  };
  
  var StatisticsGatherer = function () {
    function StatisticsGatherer(logger) {
      classCallCheck(this, StatisticsGatherer);
  
      this.now = new Date().getTime();
      this.logger = logger;
    }
  
    createClass(StatisticsGatherer, [{
      key: 'frameworkVersionDetectors',
      value: function frameworkVersionDetectors() {
        return {
          'Flow': function Flow() {
            if (window.Vaadin && window.Vaadin.Flow && window.Vaadin.Flow.clients) {
              var flowVersions = Object.keys(window.Vaadin.Flow.clients).map(function (key) {
                return window.Vaadin.Flow.clients[key];
              }).filter(function (client) {
                return client.getVersionInfo;
              }).map(function (client) {
                return client.getVersionInfo().flow;
              });
              if (flowVersions.length > 0) {
                return flowVersions[0];
              }
            }
          },
          'Vaadin Framework': function VaadinFramework() {
            if (window.vaadin && window.vaadin.clients) {
              var frameworkVersions = Object.values(window.vaadin.clients).filter(function (client) {
                return client.getVersionInfo;
              }).map(function (client) {
                return client.getVersionInfo().vaadinVersion;
              });
              if (frameworkVersions.length > 0) {
                return frameworkVersions[0];
              }
            }
          },
          'AngularJs': function AngularJs() {
            if (window.angular && window.angular.version && window.angular.version) {
              return window.angular.version.full;
            }
          },
          'Angular': function Angular() {
            if (window.ng) {
              var tags = document.querySelectorAll("[ng-version]");
              if (tags.length > 0) {
                return tags[0].getAttribute("ng-version");
              }
              return "Unknown";
            }
          },
          'Backbone.js': function BackboneJs() {
            if (window.Backbone) {
              return window.Backbone.VERSION;
            }
          },
          'React': function React() {
            var reactSelector = '[data-reactroot], [data-reactid]';
            if (!!document.querySelector(reactSelector)) {
              // React does not publish the version by default
              return "unknown";
            }
          },
          'Ember': function Ember() {
            if (window.Em && window.Em.VERSION) {
              return window.Em.VERSION;
            } else if (window.Ember && window.Ember.VERSION) {
              return window.Ember.VERSION;
            }
          },
          'jQuery': function (_jQuery) {
            function jQuery() {
              return _jQuery.apply(this, arguments);
            }
  
            jQuery.toString = function () {
              return _jQuery.toString();
            };
  
            return jQuery;
          }(function () {
            if (typeof jQuery === 'function' && jQuery.prototype.jquery !== undefined) {
              return jQuery.prototype.jquery;
            }
          }),
          'Polymer': function Polymer() {
            var version = getPolymerVersion();
            if (version) {
              return version;
            }
          },
          'LitElement': function LitElement() {
            var version = window.litElementVersions && window.litElementVersions[0];
            if (version) {
              return version;
            }
          },
          'LitHtml': function LitHtml() {
            var version = window.litHtmlVersions && window.litHtmlVersions[0];
            if (version) {
              return version;
            }
          },
          'Vue.js': function VueJs() {
            if (window.Vue) {
              return window.Vue.version;
            }
          }
        };
      }
    }, {
      key: 'getUsedVaadinElements',
      value: function getUsedVaadinElements(elements) {
        var version = getPolymerVersion();
        var elementClasses = void 0;
        // NOTE: In case you edit the code here, YOU MUST UPDATE any statistics reporting code in Flow.
        // Check all locations calling the method getEntries() in
        // https://github.com/vaadin/flow/blob/master/flow-server/src/main/java/com/vaadin/flow/internal/UsageStatistics.java#L106
        // Currently it is only used by BootstrapHandler.
        if (version && version.indexOf('2') === 0) {
          // Polymer 2: components classes are stored in window.Vaadin
          elementClasses = Object.keys(window.Vaadin).map(function (c) {
            return window.Vaadin[c];
          }).filter(function (c) {
            return c.is;
          });
        } else {
          // Polymer 3: components classes are stored in window.Vaadin.registrations
          elementClasses = window.Vaadin.registrations || [];
        }
        elementClasses.forEach(function (klass) {
          var version = klass.version ? klass.version : "0.0.0";
          elements[klass.is] = { version: version };
        });
      }
    }, {
      key: 'getUsedVaadinThemes',
      value: function getUsedVaadinThemes(themes) {
        ['Lumo', 'Material'].forEach(function (themeName) {
          var theme;
          var version = getPolymerVersion();
          if (version && version.indexOf('2') === 0) {
            // Polymer 2: themes are stored in window.Vaadin
            theme = window.Vaadin[themeName];
          } else {
            // Polymer 3: themes are stored in custom element registry
            theme = customElements.get('vaadin-' + themeName.toLowerCase() + '-styles');
          }
          if (theme && theme.version) {
            themes[themeName] = { version: theme.version };
          }
        });
      }
    }, {
      key: 'getFrameworks',
      value: function getFrameworks(frameworks) {
        var detectors = this.frameworkVersionDetectors();
        Object.keys(detectors).forEach(function (framework) {
          var detector = detectors[framework];
          try {
            var version = detector();
            if (version) {
              frameworks[framework] = { version: version };
            }
          } catch (e) {}
        });
      }
    }, {
      key: 'gather',
      value: function gather(storage) {
        var storedStats = storage.read();
        var gatheredStats = {};
        var types = ["elements", "frameworks", "themes"];
  
        types.forEach(function (type) {
          gatheredStats[type] = {};
          if (!storedStats[type]) {
            storedStats[type] = {};
          }
        });
  
        var previousStats = JSON.stringify(storedStats);
  
        this.getUsedVaadinElements(gatheredStats.elements);
        this.getFrameworks(gatheredStats.frameworks);
        this.getUsedVaadinThemes(gatheredStats.themes);
  
        var now = this.now;
        types.forEach(function (type) {
          var keys = Object.keys(gatheredStats[type]);
          keys.forEach(function (key) {
            if (!storedStats[type][key] || _typeof(storedStats[type][key]) != _typeof({})) {
              storedStats[type][key] = { firstUsed: now };
            }
            // Discards any previously logged version number
            storedStats[type][key].version = gatheredStats[type][key].version;
            storedStats[type][key].lastUsed = now;
          });
        });
  
        var newStats = JSON.stringify(storedStats);
        storage.write(newStats);
        if (newStats != previousStats && Object.keys(storedStats).length > 0) {
          this.logger.debug("New stats: " + newStats);
        }
      }
    }]);
    return StatisticsGatherer;
  }();
  
  var StatisticsStorage = function () {
    function StatisticsStorage(key) {
      classCallCheck(this, StatisticsStorage);
  
      this.key = key;
    }
  
    createClass(StatisticsStorage, [{
      key: 'read',
      value: function read() {
        var localStorageStatsString = localStorage.getItem(this.key);
        try {
          return JSON.parse(localStorageStatsString ? localStorageStatsString : '{}');
        } catch (e) {
          return {};
        }
      }
    }, {
      key: 'write',
      value: function write(data) {
        localStorage.setItem(this.key, data);
      }
    }, {
      key: 'clear',
      value: function clear() {
        localStorage.removeItem(this.key);
      }
    }, {
      key: 'isEmpty',
      value: function isEmpty() {
        var storedStats = this.read();
        var empty = true;
        Object.keys(storedStats).forEach(function (key) {
          if (Object.keys(storedStats[key]).length > 0) {
            empty = false;
          }
        });
  
        return empty;
      }
    }]);
    return StatisticsStorage;
  }();
  
  var StatisticsSender = function () {
    function StatisticsSender(url, logger) {
      classCallCheck(this, StatisticsSender);
  
      this.url = url;
      this.logger = logger;
    }
  
    createClass(StatisticsSender, [{
      key: 'send',
      value: function send(data, errorHandler) {
        var logger = this.logger;
  
        if (navigator.onLine === false) {
          logger.debug("Offline, can't send");
          errorHandler();
          return;
        }
        logger.debug("Sending data to " + this.url);
  
        var req = new XMLHttpRequest();
        req.withCredentials = true;
        req.addEventListener("load", function () {
          // Stats sent, nothing more to do
          logger.debug("Response: " + req.responseText);
        });
        req.addEventListener("error", function () {
          logger.debug("Send failed");
          errorHandler();
        });
        req.addEventListener("abort", function () {
          logger.debug("Send aborted");
          errorHandler();
        });
        req.open("POST", this.url);
        req.setRequestHeader("Content-Type", "application/json");
        req.send(data);
      }
    }]);
    return StatisticsSender;
  }();
  
  var StatisticsLogger = function () {
    function StatisticsLogger(id) {
      classCallCheck(this, StatisticsLogger);
  
      this.id = id;
    }
  
    createClass(StatisticsLogger, [{
      key: '_isDebug',
      value: function _isDebug() {
        return localStorage.getItem("vaadin." + this.id + ".debug");
      }
    }, {
      key: 'debug',
      value: function debug(msg) {
        if (this._isDebug()) {
          console.info(this.id + ": " + msg);
        }
      }
    }]);
    return StatisticsLogger;
  }();
  
  var UsageStatistics = function () {
    function UsageStatistics() {
      classCallCheck(this, UsageStatistics);
  
      this.now = new Date();
      this.timeNow = this.now.getTime();
      this.gatherDelay = 10; // Delay between loading this file and gathering stats
      this.initialDelay = 24 * 60 * 60;
  
      this.logger = new StatisticsLogger("statistics");
      this.storage = new StatisticsStorage("vaadin.statistics.basket");
      this.gatherer = new StatisticsGatherer(this.logger);
      this.sender = new StatisticsSender("https://tools.vaadin.com/usage-stats/submit", this.logger);
    }
  
    createClass(UsageStatistics, [{
      key: 'maybeGatherAndSend',
      value: function maybeGatherAndSend() {
        var _this = this;
  
        if (localStorage.getItem(UsageStatistics.optOutKey)) {
          return;
        }
        this.gatherer.gather(this.storage);
        setTimeout(function () {
          _this.maybeSend();
        }, this.gatherDelay * 1000);
      }
    }, {
      key: 'lottery',
      value: function lottery() {
        return true;
      }
    }, {
      key: 'currentMonth',
      value: function currentMonth() {
        return this.now.getYear() * 12 + this.now.getMonth();
      }
    }, {
      key: 'maybeSend',
      value: function maybeSend() {
        var firstUse = Number(localStorage.getItem(UsageStatistics.firstUseKey));
        var monthProcessed = Number(localStorage.getItem(UsageStatistics.monthProcessedKey));
  
        if (!firstUse) {
          // Use a grace period to avoid interfering with tests, incognito mode etc
          firstUse = this.timeNow;
          localStorage.setItem(UsageStatistics.firstUseKey, firstUse);
        }
  
        if (this.timeNow < firstUse + this.initialDelay * 1000) {
          this.logger.debug("No statistics will be sent until the initial delay of " + this.initialDelay + "s has passed");
          return;
        }
        if (this.currentMonth() <= monthProcessed) {
          this.logger.debug("This month has already been processed");
          return;
        }
        localStorage.setItem(UsageStatistics.monthProcessedKey, this.currentMonth());
        // Use random sampling
        if (this.lottery()) {
          this.logger.debug("Congratulations, we have a winner!");
        } else {
          this.logger.debug("Sorry, no stats from you this time");
          return;
        }
  
        this.send();
      }
    }, {
      key: 'send',
      value: function send() {
        // Ensure we have the latest data
        this.gatherer.gather(this.storage);
  
        // Read, send and clean up
        var data = this.storage.read();
        data["firstUse"] = Number(localStorage.getItem(UsageStatistics.firstUseKey));
        data["usageStatisticsVersion"] = UsageStatistics.version;
        var info = 'This request contains usage statistics gathered from the application running in development mode. \n\nStatistics gathering is automatically disabled and excluded from production builds.\n\nFor details and to opt-out, see https://github.com/vaadin/vaadin-usage-statistics.\n\n\n\n';
        var self = this;
        this.sender.send(info + JSON.stringify(data), function () {
          // Revert the 'month processed' flag
          localStorage.setItem(UsageStatistics.monthProcessedKey, self.currentMonth() - 1);
        });
      }
    }], [{
      key: 'version',
      get: function get$1() {
        return '2.1.2';
      }
    }, {
      key: 'firstUseKey',
      get: function get$1() {
        return 'vaadin.statistics.firstuse';
      }
    }, {
      key: 'monthProcessedKey',
      get: function get$1() {
        return 'vaadin.statistics.monthProcessed';
      }
    }, {
      key: 'optOutKey',
      get: function get$1() {
        return 'vaadin.statistics.optout';
      }
    }]);
    return UsageStatistics;
  }();
  
  try {
    window.Vaadin = window.Vaadin || {};
    window.Vaadin.usageStatsChecker = window.Vaadin.usageStatsChecker || new UsageStatistics();
    window.Vaadin.usageStatsChecker.maybeGatherAndSend();
  } catch (e) {
    // Intentionally ignored as this is not a problem in the app being developed
  }
  
  }());
  
    vaadin-dev-mode:end **)

@vaadin/component-base/src/element-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/disabled-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/tabindex-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/browser-utils.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/gestures.js:
  (**
  @license
  Copyright (c) 2017 The Polymer Project Authors. All rights reserved.
  This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
  The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
  The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
  Code distributed by Google as part of the polymer project is also
  subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
  *)

@vaadin/component-base/src/slot-observer.js:
  (**
   * @license
   * Copyright (c) 2023 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/unique-id-utils.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/slot-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/tooltip-controller.js:
  (**
   * @license
   * Copyright (c) 2022 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/iron-list-core.js:
  (**
   * @license
   * Copyright (c) 2016 The Polymer Project Authors. All rights reserved.
   * This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt
   * The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt
   * The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt
   * Code distributed by Google as part of the polymer project is also
   * subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt
   *)

@vaadin/component-base/src/virtualizer-iron-list-adapter.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-a11y-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/focus-utils.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-active-item-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-array-data-provider-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-column-auto-width-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-column-reordering-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-column-resizing-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/data-provider-controller/helpers.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/data-provider-controller/cache.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/data-provider-controller/data-provider-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-data-provider-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-drag-and-drop-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-dynamic-columns-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-event-context-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-filter-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-keyboard-navigation-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-row-details-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/announce.js:
  (**
   * @license
   * Copyright (c) 2022 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/keyboard-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/active-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/focus-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/delegate-focus-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/aria-id-reference.js:
  (**
   * @license
   * Copyright (c) 2023 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/field-aria-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/focus-trap-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/focus-restoration-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/keyboard-direction-mixin.js:
  (**
   * @license
   * Copyright (c) 2022 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/dir-utils.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/a11y-base/src/list-mixin.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/resize-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-scroll-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-selection-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-sort-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-styling-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-styles.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-column-group-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-column-group.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/slot-styles-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/delegate-state-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/input-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/checked-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/component-base/src/slot-child-observe-controller.js:
  (**
   * @license
   * Copyright (c) 2022 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/error-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/helper-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/label-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/label-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/validate-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/field-mixin.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/input-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/field-base/src/labelled-input-controller.js:
  (**
   * @license
   * Copyright (c) 2021 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/checkbox/src/vaadin-checkbox-mixin.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/checkbox/src/vaadin-checkbox-styles.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/checkbox/src/vaadin-checkbox.js:
  (**
   * @license
   * Copyright (c) 2017 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-selection-column-base-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-selection-column-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-selection-column.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-sorter-mixin.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)

@vaadin/grid/src/vaadin-grid-sorter.js:
  (**
   * @license
   * Copyright (c) 2016 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)
*/
//# sourceMappingURL=@arcgis_core_widgets_FeatureTable.js.map
