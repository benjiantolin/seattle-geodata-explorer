import {
  l
} from "./chunk-AY7UOFFJ.js";
import {
  R2 as R,
  T,
  Y,
  at,
  mt
} from "./chunk-YPTLL3BY.js";
import {
  c
} from "./chunk-RU3G6FMK.js";
import {
  v
} from "./chunk-FFR42QZP.js";
import {
  O
} from "./chunk-FK33OKH6.js";
import {
  p
} from "./chunk-4BIUU5VB.js";
import {
  s,
  y
} from "./chunk-VXIMEMZC.js";
import {
  e,
  u
} from "./chunk-LYOZ7XMW.js";

// node_modules/@arcgis/core/symbols/cim/CIMResourceManager.js
var i = class {
  constructor() {
    this._resourceMap = /* @__PURE__ */ new Map(), this._inFlightResourceMap = /* @__PURE__ */ new Map();
  }
  destroy() {
    this._inFlightResourceMap.clear(), this._resourceMap.clear();
  }
  getResource(e2) {
    return this._resourceMap.get(e2) ?? null;
  }
  async fetchResource(e2, i2) {
    const h2 = this._resourceMap.get(e2);
    if (h2) return { width: h2.width, height: h2.height };
    let r = this._inFlightResourceMap.get(e2);
    return r ? r.then((e3) => ({ width: e3.width, height: e3.height })) : (r = p(e2, i2), this._inFlightResourceMap.set(e2, r), r.then((t) => (this._inFlightResourceMap.delete(e2), this._resourceMap.set(e2, t), { width: t.width, height: t.height }), () => ({ width: 0, height: 0 })));
  }
  deleteResource(e2) {
    this._inFlightResourceMap.delete(e2), this._resourceMap.delete(e2);
  }
  loadFont(t) {
    return c(t);
  }
};

// node_modules/@arcgis/core/symbols/cim/CIMSymbolRasterizer.js
var m = 96 / 72;
var g = class {
  constructor(e2) {
    this._spatialReference = e2, this._imageDataCanvas = null, this._cimResourceManager = new i();
  }
  get _canvas() {
    return this._imageDataCanvas || (this._imageDataCanvas = document.createElement("canvas")), this._imageDataCanvas;
  }
  get resourceManager() {
    return this._cimResourceManager;
  }
  async rasterizeCIMSymbolAsync(i2, r, h2, o, m3, g3, c4, d, w, u3) {
    if (!i2) return null;
    const { data: p2 } = i2;
    if (!p2 || "CIMSymbolReference" !== p2.type || !p2.symbol) return null;
    const { symbol: f } = p2;
    g3 || (g3 = O(f));
    const x = await v.resolveSymbolOverrides(p2, r, this._spatialReference, m3, g3, c4, d), b = this._cimResourceManager, M = [];
    Y.fetchResources(x, b, M), Y.fetchFonts(x, b, M), M.length > 0 && await Promise.all(M);
    const { width: C, height: R2 } = h2;
    let v2 = y2(g3, C, R2, o, u3);
    const I = Y.getEnvelope(x, v2, b);
    if (!I) return null;
    I.x === 1 / 0 && (I.x = C + 2), I.y === 1 / 0 && (I.y = -R2 / 2), I.width === -1 / 0 && (I.width = C), I.height === -1 / 0 && (I.height = R2);
    let S = 1, _ = 0, P = 0;
    switch (f.type) {
      case "CIMPointSymbol":
      case "CIMTextSymbol":
        {
          let e2 = 1;
          I.width > C && (e2 = C / I.width);
          let t = 1;
          I.height > R2 && (t = R2 / I.height), "preview" === o && (I.width < C && (e2 = C / I.width), I.height < R2 && (t = R2 / I.height)), S = Math.min(e2, t), _ = I.x + I.width / 2, P = I.y + I.height / 2;
        }
        break;
      case "CIMLineSymbol":
        if (u3) {
          P = I.y + I.height / 2, _ = I.x + I.width / 2;
          const e2 = I.width - C, t = I.height - R2;
          v2 = { paths: T(v2.paths, { xmin: -1 * I.width / 2 + e2, xmax: I.width / 2 - e2, ymin: -1 * I.height / 2 + t, ymax: I.height / 2 - t, width: I.width - 2 * e2, height: I.height - 2 * t }) };
        } else {
          (w || I.height > R2) && (S = R2 / I.height), P = I.y + I.height / 2;
          const i3 = I.x * S + C / 2, r2 = (I.x + I.width) * S + C / 2, h3 = s(v2) ? v2.paths : y(v2) ? v2.rings : null;
          if (null === h3) throw new Error("Bad geometry, can't rasterise symbol!");
          h3[0][0][0] -= i3 / S, h3[0][2][0] -= (r2 - C) / S;
        }
        break;
      case "CIMPolygonSymbol":
        if (u3) {
          P = I.y + I.height / 2, _ = I.x + I.width / 2;
          const e2 = I.width - C, t = I.height - R2;
          v2 = { paths: T(v2.rings, { xmin: -1 * I.width / 2 + e2, xmax: I.width / 2 - e2, ymin: -1 * I.height / 2 + t, ymax: I.height / 2 - t, width: I.width - 2 * e2, height: I.height - 2 * t }) };
        } else {
          _ = I.x + I.width / 2, P = I.y + I.height / 2;
          const e2 = I.x * S + C / 2, t = (I.x + I.width) * S + C / 2, i3 = I.y * S + R2 / 2, r2 = (I.y + I.height) * S + R2 / 2, { rings: h3 } = v2;
          e2 < 0 && (h3[0][0][0] -= e2, h3[0][3][0] -= e2, h3[0][4][0] -= e2), i3 < 0 && (h3[0][0][1] += i3, h3[0][1][1] += i3, h3[0][4][1] += i3), t > C && (h3[0][1][0] -= t - C, h3[0][2][0] -= t - C), r2 > R2 && (h3[0][2][1] += r2 - R2, h3[0][3][1] += r2 - R2);
        }
    }
    const j = { type: "cim", data: { type: "CIMSymbolReference", symbol: x } };
    return this.rasterize(j, C, R2, _, P, S, g3, 1, v2);
  }
  rasterize(e2, t, i2, s3, n, a2, o, g3 = 0, c4 = null, d = window.devicePixelRatio || 1) {
    const { data: w } = e2;
    if (!w || "CIMSymbolReference" !== w.type || !w.symbol) return null;
    const { symbol: u3 } = w, p2 = this._canvas, f = d * m;
    p2.width = t * f, p2.height = i2 * f, o || (o = O(u3)), c4 || (c4 = y2(o, t, i2, "legend")), p2.width += 2 * g3, p2.height += 2 * g3;
    const x = p2.getContext("2d", { willReadFrequently: true }), b = at.createIdentity();
    b.translate(-s3, -n), b.scale(a2 * f, -a2 * f), b.translate(t * f / 2 + g3, i2 * f / 2 + g3), x.clearRect(0, 0, p2.width, p2.height);
    return new mt(x, this._cimResourceManager, b, true).drawSymbol(u3, c4), x.getImageData(0, 0, p2.width, p2.height);
  }
};
function c2(e2, t, i2, r) {
  if ("esriGeometryPolygon" === t) {
    return { rings: R(T(e2.rings, { xmin: 0, ymin: 0, width: i2, height: r }), -1 * i2 / 2, -1 * r / 2) };
  }
  if ("esriGeometryPolyline" === t) {
    return { paths: R(T(e2.paths, { xmin: 0, ymin: 0, width: i2, height: r }), -1 * i2 / 2, -1 * r / 2) };
  }
  return null;
}
function y2(e2, t, i2, r, h2) {
  const s3 = 1, n = -t / 2 + s3, a2 = t / 2 - s3, o = i2 / 2 - s3, l2 = -i2 / 2 + s3;
  if (h2 && ("esriGeometryPolygon" === e2 || "esriGeometryPolyline" === e2)) {
    const r2 = c2(h2, e2, t, i2);
    if (r2) return r2;
  }
  switch (e2) {
    case "esriGeometryPoint":
      return { x: 0, y: 0 };
    case "esriGeometryPolyline":
      return { paths: [[[n, 0], [0, 0], [a2, 0]]] };
    default:
      return "legend" === r ? { rings: [[[n, o], [a2, 0], [a2, l2], [n, l2], [n, o]]] } : { rings: [[[n, o], [a2, o], [a2, l2], [n, l2], [n, o]]] };
  }
}

// node_modules/@arcgis/core/symbols/support/previewCIMSymbol.js
var a = new g(null);
var s2 = e(22);
var m2 = e(120);
var h = e(50);
var c3 = 1;
async function y3(e2, t, i2) {
  const o = t == null ? void 0 : t.size;
  let l2 = null != o && "object" == typeof o && "width" in o ? o.width : o, r = null != o && "object" == typeof o && "height" in o ? o.height : o;
  if (!l2 || !r) if ("esriGeometryPolygon" === i2) l2 = r = t.maxSize ? Math.min(t.maxSize, s2) : s2;
  else {
    const o2 = await g2(e2, t, i2);
    o2 && (l2 = o2.width, r = o2.height), "esriGeometryPolyline" === i2 && (l2 = t.maxSize ? Math.min(t.maxSize, h) : h), l2 = null != l2 && isFinite(l2) ? Math.min(l2, m2) : s2, r = null != r && isFinite(r) ? Math.max(Math.min(r, m2), c3) : s2;
  }
  return "legend" === t.style && "esriGeometryPolyline" === i2 && (l2 = h), { width: l2, height: r };
}
async function g2(e2, t = {}, o) {
  var _a;
  const n = t.cimOptions || t;
  o ?? (o = n.geometryType || O((_a = e2 == null ? void 0 : e2.data) == null ? void 0 : _a.symbol));
  const { feature: s3, fieldMap: m3, viewParams: h2 } = n, c4 = await v.resolveSymbolOverrides(e2.data, s3, null, m3, o, null, h2);
  if (!c4) return null;
  (e2 = e2.clone()).data = { type: "CIMSymbolReference", symbol: c4 }, e2.data.primitiveOverrides = void 0;
  const y4 = [];
  return Y.fetchResources(c4, a.resourceManager, y4), Y.fetchFonts(c4, a.resourceManager, y4), y4.length > 0 && await Promise.all(y4), Y.getEnvelope(c4, null, a.resourceManager);
}
async function u2(i2, o = {}) {
  var _a, _b;
  const { node: l2, opacity: s3, symbolConfig: m3 } = o, h2 = null != m3 && "object" == typeof m3 && "isSquareFill" in m3 && m3.isSquareFill, c4 = o.cimOptions || o, g3 = c4.geometryType || O((_a = i2 == null ? void 0 : i2.data) == null ? void 0 : _a.symbol), u3 = await y3(i2, o, g3), { feature: d, fieldMap: f } = c4, p2 = (o == null ? void 0 : o.geometry) || h2 || "esriGeometryPolygon" !== g3 ? "preview" : "legend";
  let w = u3;
  const b = u3;
  if ((o == null ? void 0 : o.geometry) && ("esriGeometryPolygon" === g3 || "esriGeometryPolyline" === g3)) {
    const i3 = 200;
    if (u(u3.width) < i3 || u(u3.height) < i3) {
      const e2 = u3.width > u3.height ? e(i3) * u3.height / u3.width : e(i3);
      w = { width: u3.width > u3.height ? e(i3) : e(i3) * u3.width / u3.height, height: e2 };
    }
  }
  const M = await a.rasterizeCIMSymbolAsync(i2, d, w, p2, f, g3, null, c4.viewParams, c4.allowScalingUp, (_b = o == null ? void 0 : o.geometry) == null ? void 0 : _b.toJSON());
  if (!M) return null;
  const { width: S, height: v2 } = M, j = document.createElement("canvas");
  j.width = S, j.height = v2;
  j.getContext("2d").putImageData(M, 0, 0);
  const P = u(b.width), x = u(b.height), z = new Image(P, x);
  z.src = j.toDataURL(), z.ariaLabel = o.ariaLabel ?? null, z.alt = o.ariaLabel ?? "", null != s3 && (z.style.opacity = `${s3}`);
  let C = z;
  if (o.cssEffectFilter) {
    const e2 = { shape: { type: "image", x: 0, y: 0, width: P, height: x, src: z.src }, fill: null, stroke: null, offset: [0, 0] };
    C = l([[e2]], [P, x], o);
  }
  return l2 && C && l2.appendChild(C), C;
}

export {
  g2 as g,
  u2 as u
};
//# sourceMappingURL=chunk-KZCSMTI6.js.map
